# Next Steps

## 当前优先项

### 2026-06-07 当前唯一执行锚点（修正版）

下面这一段优先于后面的历史记录；后文只保留证据和演进过程，不再覆盖当前推进规则。

当前目标只剩两条主线，按验收切片推进，不再用“还差多少大功能”泛化估算：

1. 群组对话是日常协作入口：用户在群里自然发布目标，主模型识别目标、选择群内合适 Agent、拆分任务并隐藏机器协议；Agent 的审批、执行、失败、完成、产物都必须回到 Chat 与 Run Detail；所有 Agent 终态后，主模型要把成功项、失败/拒绝项、原因、产物和下一步整理成用户能读懂的结果。
2. Workflow 是显式自动化入口：只能通过 Workflow Studio 保存运行、Chat 显式 `@Workflow`、Runs 手动运行或 Run Detail 重跑触发；它不进入群聊隐式派发，不和群组 Agent 派活协议混用。显式触发时必须能追踪父 Workflow、子 Agent、审批、artifact、失败节点、取消和重跑。

当前硬规定：

1. 每轮只做一条可判定切片，开工前写清用户入口、预期可见状态、后端终态、Run Detail / API 证据和最小回归；完成后必须补 session/run/workflow id、测试名或 blocker 文件。
2. 只承认四类进度：代码修复、回归测试、真实 Browser/API/Chat Profile smoke、或者把 blocker 收敛到明确文件/接口/状态机。持续运行时间、重复观察、截图复述、未验证猜测都不算推进。
3. 等待规则强制降级：真实链路只短暂观察一次；没有进入 `completed` / `failed` / `cancelled` / `approval_required` / `processing` 等可判定状态，就立即查 API 和 timeline。API 正确修前端同步，API 不正确修后端状态机，状态正确但文字不清楚才修 prompt / 清洗边界。
4. 群组对话当前队列只修用户会直接遇到的 blocker：协议/JSON 外露、派错 Agent、审批不显眼、批准后无进度、连续审批不可区分、成功/失败没有回主模型、Run Detail 查不到上下文、连续补充或多轮 RunGroup 串台。
5. Workflow 当前队列只修显式运行闭环：Studio 当前画布保存运行、Chat 显式 `@Workflow`、Runs 手动运行/重跑、真实 Agent 节点长链路、子 Agent 连续审批、artifact 继承、失败节点归因和父子 Run Detail 实时刷新。
6. 审批体验最低标准：用户不翻历史也能在输入区附近或当前 Run Detail 知道谁在请求、请求什么工具、关联任务是什么、命令/参数是什么、批准后正在执行还是等待下一次审批、还剩几项、最终成功还是失败。
7. 复盘体验最低标准：Run Detail / Runs History 能完整回答谁做了什么、用了什么工具、为什么卡住、审批了什么、stdout/stderr 是什么、产物在哪里、失败原因是什么、父子 Run 如何关联、能否取消或重跑；长内容可以折叠，但展开后不能省略关键细节。

当前距离目标的可执行判断：群组对话普通自然目标派发、多 Agent 同时审批输入区队列、逐项批准、混合失败/拒绝终态、最终主模型汇总、真实模型长链路协议清洗、运行中补充并入当前 RunGroup 且不另起派发，已经有当前代码回归或真实前端 smoke；剩余重点不是补大结构，而是继续压真实长链路里的 Run Detail 可追踪、输入区审批队列刷新和失败/取消复盘。Workflow 显式入口、Run Detail 连续审批、Chat 输入区连续审批、Runs 手动重跑、真实 Agent 节点长链路、artifact 继承、子 Agent 多次审批和失败节点归因已经有当前代码回归或真实前端/API smoke；剩余重点收窄为 Chat 显式 `@Workflow` 输入区审批队列刷新、取消/重跑复盘一致性，以及失败 Workflow 中未执行 artifact 的视觉语义不要误导用户。

下一条固定切片：Chat 显式 `@Workflow` 子 Agent 连续审批输入区 smoke。入口是主会话或群聊显式 `@Workflow` 运行一个需要两次 `terminal.run` 审批的 Workflow；成功条件是第一次批准后，输入区附近仍显眼展示第二个审批请求，Run Detail 链接仍指向同一个父 Workflow / 子 Agent，第二次批准后 Chat 气泡完成、artifact 可打开、`approval_count` / `processing_count` 归零。附带 UI 收敛：失败 Workflow Steps 里的未执行 artifact 只能显示为计划/跳过/禁用项，不能像已生成产物一样给用户一个可点击入口。若真实链路没有进入明确状态，当轮立即降级到 API / timeline，不继续等待。

2026-06-07 本轮继续推进：先收敛真实模型长链路中最容易导致截图式“JSON 闪现/回退”的确定性缺口。已修复 `apps/shell/chat_api.py` 的群组派发流式清洗：当主模型在自然语言后开始输出未闭合的 `json` 代码围栏、且只吐到 `{` / `[` 前缀时，从围栏起点隐藏，避免用户看到半截派活协议。新增回归 `test_group_main_model_dispatch_stream_hides_open_json_fence_prefix`，并通过 4 条协议隐藏回归：`test_group_main_model_dispatch_stream_stays_loading`、`test_group_main_model_dispatch_stream_hides_partial_json_without_rewind`、`test_group_main_model_dispatch_stream_hides_open_json_fence_prefix`、`test_group_main_model_dispatch_stream_hides_compact_tag_and_smart_quotes`。同时复跑 4 条连续补充/混合终态回归：`test_plain_group_followup_during_running_agent_enters_main_summary`、`test_group_followup_targets_latest_active_delegated_batch`、`test_group_followup_after_unsynced_completed_dispatch_enters_summary`、`test_plain_group_goal_mixed_agent_outcomes_waits_and_summarizes`，确认运行中补充仍进入当前 RunGroup 汇总，第二轮新目标不串台。当前切片剩余项：用真实 Chat Profile / Browser 再跑一次长输出 + 运行中补充 smoke，拿到 session/run id 后再把该切片从“确定性修复”提升为“真实链路通过”。

2026-06-07 本轮真实链路补证：真实模型 smoke `917287` 暴露了运行中补充的实际 blocker：用户消息已带 `group_followup_for_task_ids`，但主模型仍可能把补充再次解释成新派发，导致补充任务创建第二个 RunGroup。已在 `apps/shell/chat_api.py` 增加 `_with_group_followup_context` 和完成态兜底：补充消息会显式提示主模型不要另起派发；如果模型仍输出派发协议，后端会忽略该派发并记录 `group_followup_dispatch_ignored`。新增回归 `test_group_followup_dispatch_payload_is_ignored_and_stays_in_current_summary`，并复跑连续补充相关 4 条回归通过。重启后用真实主模型 + 本地 Agent stub 跑通 `917288`：session `dbd01e65`，父任务 `84c694f5e5ca`，RunGroup `run_group_32a104a7e9f5`，Agent Run `agent_run_ac4e733fd72c`，补充任务 `0dd91992e6e8`；补充内容进入最终主模型汇总，最终 `processing_count=0`、`approval_count=0`。Browser 打开 `#/chat?session_id=dbd01e65` 精确复核：无批准/拒绝按钮残留、无 `<yachiyo_group_dispatch>` / `dispatch_group_agent` / ```json 泄露，最终总结包含 “Coding Followup Guard 917288” 和 “移动端验收风险”。该切片从队列移出，不再重复等待同类 smoke。

2026-06-07 Workflow 重跑切片补证：已把 Run Detail 的“重新运行”改成真实后端重跑动作，而不是前端重新拼一次普通运行。新增 `apps/shell/agent_runtime.py::rerun_run` 和 bridge route `POST /ui/runs/{run_id}/rerun`；新 Run 会使用 source `rerun` 创建独立 run_group，并在 timeline 开头写入 `run.rerun.started`，包含 `rerun_of_run_id`、原状态、原目标和原目标名称。前端 `AgentStudioView.tsx` 的重跑按钮改走 `rerunRun(run_id)`，Execution 中显示“从原 Run 重跑”。回归 `test_workflow_artifact_review_route_exposes_outputs_and_reruns` 改为走真实 rerun route，并通过；同时复跑 `test_workflow_child_approval_route_approve_resumes_parent_workflow`、`test_workflow_child_consecutive_approvals_keep_parent_waiting`、`test_workflow_child_artifact_route_reads_source_run_artifact`、`test_workflow_child_consecutive_approvals_keep_chat_prompt_visible`、`test_list_sessions_counts_workflow_approval_required`，5 条通过；`npm run build --prefix apps/frontend` 通过。真实 API + Browser smoke `917289`：临时 Agent `agent_codex-workflow-rerun-agent-917289_94ffa52e`、Workflow `workflow_codex-workflow-rerun-smoke-917289_e28431c1`，首次 Run `workflow_run_c10711e504c5` completed，route 重跑生成 `workflow_run_54826c794d48` completed；artifact `reports/workflow-rerun-917289.md` 内容为 `workflow-rerun-agent-result-917289 call 2`；Browser 打开 `#/agents/workflow_run_54826c794d48` 可见 Run Detail、新 run id、原 run id、`从原 Run 重跑`、Workflow Steps、artifact 路径和重跑结果。该切片从队列移出，不再重复等待同类重跑 smoke。

2026-06-07 Workflow 连续审批与失败归因补证：真实 API + Browser smoke `917290` 已覆盖上一条固定切片。连续审批路径创建临时 Agent `agent_codex-workflow-consecutive-approval-917290-38118_6ce8a8bb` 与 Workflow `workflow_codex-workflow-consecutive-approval-smoke-917290_89a56124`，父 Run `workflow_run_b7d9a374ca24` / RunGroup `run_group_fa9e06e08a59` / 子 Run `agent_run_f2045fd4268f`；第一次批准后子 Run 和父 Workflow 继续 `approval_required`，pending command 从 `printf workflow-first-917290` 切到 `printf workflow-second-917290`，Browser 打开 `#/agents/workflow_run_b7d9a374ca24` 可见第二次审批、`批准子 Agent` / `拒绝子 Agent`、Workflow Steps 和父子 Run 关系；第二次批准后父子 Run 和 RunGroup completed，artifact `reports/workflow-consecutive-917290.md` 写入 `workflow-consecutive-complete-917290`。失败归因路径创建临时 Agent `agent_codex-workflow-failure-attribution-917290-171549_0d54d24d` 与 Workflow `workflow_codex-workflow-failure-attribution-smoke-917290-_d53f1705`，父 Run `workflow_run_15fb19f2799b` / RunGroup `run_group_ff206508a3c8` / 子 Run `agent_run_2526c2927128`；批准 `terminal.run` 命令 `printf workflow-child-failure-917290; exit 7` 后，子 Run、父 Workflow 和 RunGroup 都 failed，失败原因包含命令、退出码 `7` 和 stdout `workflow-child-failure-917290`，后续 artifact route 返回 404，timeline 没有 executed artifact node。Browser 打开 `#/agents/workflow_run_15fb19f2799b` 可见父/子 Run、`terminal.run 执行失败`、命令、退出码、stdout、Workflow Steps 与 `Artifacts · 0`；本次临时 stub 端口 `127.0.0.1:8765` 已关闭。记录一个 UI 债务：失败 Workflow Steps 中计划的 artifact path 仍显示成按钮式入口，下一条切片顺手改为计划/跳过/禁用表现，避免误导用户以为产物已经生成。

2026-06-07 明日接手说明（从这里继续，不重开目标讨论）：

1. 当前分支：`phase-4/develop`。本次收口会分三类提交：Runtime/Chat/Workflow 后端闭环，前端 Chat/Runs/Run Detail 体验，进度记录。
2. 当前目标只剩两条主线：群组对话作为日常协作入口，Workflow 作为显式自动化入口。不要再把 Workflow 加回群聊隐式派发；群聊只在用户显式 `@Workflow` 时触发 Workflow。
3. 明天第一条固定切片：Chat 显式 `@Workflow` 子 Agent 连续审批输入区 smoke。入口是主会话或群聊显式 `@Workflow` 运行需要两次 `terminal.run` 审批的 Workflow；第一次批准后，输入区附近必须继续显示第二个审批请求，Run Detail 链接仍指向同一个父 Workflow / 子 Agent；第二次批准后 Chat 气泡 completed，artifact 可打开，`approval_count=0`、`processing_count=0`。
4. 顺手修的 UI 债务：失败 Workflow 的后续 Artifact 节点如果没有执行，只能显示成“计划中 / 已跳过 / 等待前置节点”这类禁用状态，不能在 Workflow Steps 中渲染成可点击 artifact 按钮。当前证据是 `workflow_run_15fb19f2799b`：Artifacts 区为 `0`，artifact route 404，但 Steps 里仍出现 `reports/workflow-failure-917290.md` 按钮式入口。
5. 优先查看文件：`apps/frontend/src/views/AgentStudioView.tsx` 的 Workflow Steps / artifact render、父子 Run Detail bridge 和 rerun 展示；`apps/frontend/src/views/ChatView.tsx` 的输入区 pending approval 提醒；`apps/frontend/src/lib/agents.ts` 的 run / rerun / approval API client；`apps/shell/agent_runtime.py` 的 Workflow parent/child 状态机；`apps/shell/chat_api.py` 的 Chat runnable、approval count、group follow-up 和协议隐藏。
6. 关键证据不要丢：`917288` 证明群组运行中补充不会另起派发；`917289` 证明 Run Detail 真实 rerun；`917290` 证明 Workflow 子 Agent 连续审批和失败归因；历史 `917276` 已证明 Chat 显式 Workflow 连续审批曾经在当前代码路径上通过，但明天仍要用最新工作树复核。
7. 推荐验证命令：`.venv/bin/pytest tests/test_agent_runtime.py::test_workflow_child_consecutive_approvals_keep_parent_waiting tests/test_agent_runtime.py::test_workflow_child_approval_route_approve_resumes_parent_workflow tests/test_agent_runtime.py::test_workflow_child_artifact_route_reads_source_run_artifact tests/test_chat_api.py::test_workflow_child_consecutive_approvals_keep_chat_prompt_visible tests/test_chat_api.py::test_group_followup_dispatch_payload_is_ignored_and_stays_in_current_summary`；前端改动后跑 `npm run build --prefix apps/frontend`；真实 UI 复核用 in-app Browser 打开 `http://127.0.0.1:5174/#/chat` 或具体 `#/agents/<run_id>`。
8. 注意事项：仓库里有一个未跟踪的 `~/tmp/cloc-mini.py`，是 smoke 运行时误写出的临时脚本，不属于产品改动；除非专门清理运行垃圾，否则不要提交它。每轮 smoke 后确认 `127.0.0.1:8765` 没有遗留临时 stub 监听。

### 2026-06-06 验收推进目标与规则

当前阶段从“继续补功能”切换为“按验收矩阵推进并修 blocker”。目标不是再扩展新概念，也不是等到所有真实压测条件都齐了才动手，而是每轮都把至少一条核心路径向“可复现、可验证、可使用”推进一格。持续运行时间不算进度；只有代码修复、回归覆盖、真实前端/真实 Chat Profile 记录、或者明确收窄的 blocker 才算进度。

本轮修正规定：后端确定性验收矩阵已经覆盖 8 条核心路径，接下来不再把“继续补后端 case”当作主要进度。剩余进度按真实使用链路收口：先用 Browser/CDP smoke 验证 Runs / Run Detail / Workflow Studio 的可见性，再用真实 Chat Profile 压测群组普通派发、连续审批、失败回主模型和中途补充；发现真实 blocker 就立刻缩小成最短修复项，不继续等待理想环境，也不在同一个模糊问题上反复绕圈。

修正后的产品目标：

1. 群组对话是日常协作主入口：用户在群里自然发布目标，主模型负责理解目标、选择群内合适 Agent、拆分任务、隐藏机器派发协议，并把 Agent 的完成、失败、取消、审批请求和产物整理回用户能读懂的群聊结果。
2. Workflow 保留为显式、可复用、可复盘的自动化入口：用户通过 Workflow Studio 保存并运行、Chat 中显式 `@Workflow`、Runs 手动运行来触发；Workflow 不作为群聊主模型的隐式派发成员，也不和群组 Agent 派活协议混用。
3. Runs / Run Detail 是复盘和治理入口：用户可以从聊天气泡、输入区审批提醒或 Agent Studio 跳转，查看每个 Agent / Workflow 做了什么、卡在哪里、需要批什么、产物在哪里，以及是否应该取消或重跑。

当前距离目标的判定：

1. 群组对话：确定性后端路径基本就位；普通群聊派发、工具审批通过、成功/拒绝混合结果、中途补充并入最终汇总、多轮连续任务独立 RunGroup 都已有真实 Chat Profile + Browser smoke 记录。剩余关键工作是继续压测更长真实模型链路，以及真实模型偶发协议痕迹/前端同步里的 blocker。
2. Workflow：保存、运行、审批、失败、产物、重跑的后端路径基本就位；Workflow Studio 前端已补真实点击 smoke，确认新草稿添加 Artifact、保存并运行、Run Detail 展示产物，以及 Approval 节点在当前后端中进入 `approval_required`、从 Run Detail 批准后继续到配置 artifact path；普通 Chat 与群聊显式 `@Workflow` 触发 Start->Artifact 最小流程都已有真实前端 smoke。剩余关键工作是 Run Detail 长时间观察、真实子 Agent 工具审批链路和带真实 Agent 节点的模型执行压测；Workflow 触发体验下一步优先验收真实 Agent 节点、子 Agent 审批批准后继续和失败原因可定位。
3. Agent Studio Runs：分组、筛选、详情、折叠、长内容、重跑入口已经进入可验收状态；剩余关键工作是确认真实运行时的刷新和审批轮询不会卡住。
4. 当前阶段完成标准：8 条矩阵不仅有测试，还至少各有一次真实前端或真实 Chat Profile 记录；如果某条真实链路失败，优先修该条，不新增旁支需求。

阶段验收目标：

1. 群组对话验收目标：用户在群里发布目标后，主模型能识别目标、选择合适的群内 Agent、明确派发任务；Agent 的完成、失败、取消、审批请求和产物都要回到主模型整理，用户在聊天区和 Run Detail 都能看懂当前发生了什么。
2. Workflow 验收目标：用户能正常设计、保存、校验、运行 Workflow；运行中能看到步骤进度、审批内容、失败节点、子 Agent 产物和最终结果；完成、失败、取消、重跑都要有明确入口和可复盘记录。

推进规则：

1. 每轮先选验收矩阵中的一条，交付“回归覆盖 / blocker 修复 / 真实链路记录”之一；如果真实 Chat Profile 或 Browser 暂时不可压测，先补最接近真实链路的 route/API 回归并记录缺口，不停在等待。
2. 只修阻塞核心链路的问题。除非直接影响群组对话或 Workflow 验收，否则暂缓 Runs 清理、复杂 artifact viewer、Provider/TTS/DMG/文档站等旁支任务。
3. 每个修复都要绑定一个用户可见症状，例如 JSON 外露、审批不可见、批准后无进度、Agent 结果未回主模型、Workflow 跑旧画布、Run Detail 查不到失败节点。
4. 每条验收项必须留下可追溯证据：测试名、通过的命令、前端 build / Browser smoke 状态，以及仍未覆盖的真实模型风险。
5. 不把“单测通过”当作最终完成，但也不因为缺真实压测就原地打转。单测和 route/API 回归负责锁住确定性链路，真实 Chat Profile 压测负责最后放行当前阶段。
6. 如果真实模型输出继续暴露协议格式偏差，优先收紧 prompt 和解析边界；如果只是小概率模型胡乱输出，优先提供清晰失败提示和主模型整理，不无限扩展兼容形态。
7. 审批体验的完成标准是用户无需翻历史也能知道谁在请求、请求什么、为什么需要批、批准后正在做什么、是否还有下一次审批，以及审批入口来自聊天提醒还是 Run Detail。
8. Workflow 的完成标准是用户能从聊天气泡或 Runs 详情一路追到每个节点、每个子 Agent、每个产物和失败原因，而不是只能看到最终一段文本。
9. 每次开始一条验收项时先写清当前预期、入口和成功条件；完成后立刻补记录，不把证据留到很久以后回忆。
10. 如果连续两轮都没有代码、测试或文档证据推进同一验收项，必须停下来重新缩小范围，先修最短可验证 blocker。
11. 每次真实 smoke 都要记录“环境、入口、看到了什么、仍没覆盖什么”。如果页面先于数据创建导致旧状态，必须硬刷新或重新进入页面复核，不能把旧截图误判为代码 blocker。
12. 如果一次真实链路超过一轮仍没有进入可判定状态，要主动改走更短路径：先定位 API 状态、再定位前端同步、最后才判断模型行为，避免长时间只看 UI 等结果。

2026-06-07 推进修正规定：

1. 当前不再以“继续设计完整形态”为主任务，而是以验收矩阵为工作队列；每次只选择一条最短闭环，跑到成功、失败或可命名 blocker。
2. 群组对话优先验证用户真实习惯：用户发目标、主模型派活、Agent 执行/审批/失败、主模型整理、用户能在 Chat 和 Run Detail 追踪。任何协议外露、审批不可见、批准后无进度、结果未回主模型，都直接归为 blocker。
3. Workflow 优先验证显式触发：Workflow Studio 保存运行、Chat 中显式 `@Workflow`、Runs 手动重跑。Workflow 不参与群组隐式派活，除非用户明确点名。
4. 如果真实 smoke 卡住超过一个工作轮次，立刻切到 API 状态和最小复现，不继续等待模型或页面自然刷新；修完后必须回到真实入口复核。
5. 只有四类事项算推进：代码修复、回归测试、真实前端/真实 Chat Profile 证据、或者把 blocker 缩小到一个明确文件/接口/状态机问题。持续运行时长、重复观察和未验证猜测不算进度。

2026-06-07 目标与规定再修订（推进版）：

1. 接下来只围绕两条主线推进：群组对话做到“自然发布目标 -> 主模型派活 -> Agent 执行/审批/失败 -> 主模型整理 -> Chat 与 Run Detail 可复盘”；Workflow 做到“Studio 设计保存 -> 显式触发运行 -> 节点/子 Agent/审批/产物可追踪 -> 完成/失败/取消/重跑可复盘”。
2. 群组对话不再新增大概念，只修用户会直接感知的问题：协议/JSON 外露、派错 Agent、审批不显眼、批准后没进度、连续审批不可区分、Agent 完成/失败没有回主模型、Run Detail 找不到上下文。
3. Workflow 不参与群聊隐式派发；它只通过 Workflow Studio、Chat 显式 `@Workflow`、Runs 手动运行/重跑触发。群聊里显式 `@Workflow` 时必须保持当前会话上下文，但不能让主模型把 Workflow 当成普通 Agent 再派一次。
4. 每条真实链路都用同一个判定顺序推进：先看 Chat 用户可见状态，再看 Run Detail 复盘状态，再查 API 状态，最后才判断模型行为。任何一步出现不一致，先缩成一个可命名 blocker，而不是继续等待页面自然变化。
5. 审批链路的最低可用标准：用户在输入区附近或当前 Run Detail 能立即知道“谁请求、请求什么工具、为什么、命令/参数是什么、点批准后正在做什么、是否还有下一次审批、最终成功还是失败”。缺任意一项都算 UX blocker。
6. Run Detail 的最低可用标准：完整显示任务、模型回复、工具调用、审批、stdout/stderr、artifact、失败原因和父子 Run 关系；长内容可以折叠，但展开后不能省略关键细节。
7. Workflow Agent 节点的验收优先级高于继续装饰 UI：先证明子 Agent 审批批准后父 Workflow 会继续、artifact 能拿到子结果、Chat 气泡能结束；再处理更细的视觉层级。
8. 每次推进只认三个结果：通过并补证据、失败并修复、失败但定位到明确文件/接口/状态机。超过一个工作轮次仍无法判定时，必须主动降级到最短 API smoke。

2026-06-07 目标与规定再更新（去等待版）：

1. 当前目标不再按“功能还差多少”泛化推进，而是拆成可判定的验收队列。每次只做一条链路，从用户入口开始，到 Chat 可见状态、Run Detail 可复盘、API 状态一致、测试或 smoke 证据落档为止。
2. 任何需要等待模型或页面自然变化的场景，最多观察一个短轮次；如果没有进入成功、失败或审批等明确状态，立即切到 API / state machine / 最小复现定位，不继续硬等。
3. 群组对话的下一步只围绕真实用户习惯修：发布目标、派给合适 Agent、审批显眼、批准后有进度、连续审批可区分、Agent 成功/失败回主模型、最终总结可读。
4. Workflow 的下一步只围绕显式自动化闭环修：Studio 保存运行、Chat 显式 `@Workflow`、Runs 手动重跑、子 Agent 审批、失败节点定位、artifact 和最终结果可追踪。
5. Run Detail / Runs History 是推进核心，不再只是辅助页面；每个运行必须能回答“谁做了什么、用了什么工具、为什么卡住、审批了什么、产物在哪里、失败原因是什么、能否重跑”。
6. 每轮结束必须更新证据：通过就记录入口和 run/session id，失败就记录 blocker 名称和修复位置；如果只有观察没有结论，视为本轮未推进，下一步必须缩小范围。

2026-06-07 目标与规定再更新（推进锁定版）：

1. 当前目标锁定为两条主线：群组对话作为日常协作入口，必须做到“自然目标 -> 主模型派活 -> Agent 执行/审批/失败 -> 主模型整理 -> Chat 与 Run Detail 可复盘”；Workflow 作为显式自动化入口，必须做到“Studio 设计保存 -> 显式触发运行 -> 节点/子 Agent/审批/产物可追踪 -> 完成/失败/取消/重跑可复盘”。
2. 当前非目标也明确锁定：不做 SSE，不扩 Provider/TTS/DMG/文档站，不把 Workflow 混进群聊隐式派发，不因为视觉细节反复打磨而拖住核心链路；只有直接影响用户完成群聊协作或 Workflow 执行复盘的问题才进入本阶段。
3. 群组对话下一批队列按用户注意力推进：先验收多 Agent 同时待审批时输入区附近的数量、定位和逐项处理；再验收批准/拒绝/失败后主模型汇总是否稳定收束；再用真实模型复测协议隐藏、长输出、连续补充和多轮 RunGroup 不串台。
4. Workflow 下一批队列按显式运行闭环推进：先复测 Chat 显式 `@Workflow` 和 Runs 手动重跑；再压测真实 Agent 节点长链路、子 Agent 审批、artifact 继承、失败节点归因和父子 Run Detail 实时刷新。
5. 每一轮开工前必须写清入口、预期和成功条件；每一轮结束只接受四种结果：代码修复、回归测试、真实 Browser/Chat Profile smoke 证据、或者把 blocker 收敛到明确文件/接口/状态机。持续运行时间、重复观察、未验证猜测都不算进度。
6. 等待规则改成强制降级：一个真实链路只短暂观察一次；如果没有进入 completed / failed / cancelled / approval_required / processing 等可判定状态，立即查 API；API 正确就修前端同步，API 不正确就修后端状态机，状态正确但文字糟糕才修 prompt / 清洗边界。
7. 审批体验的硬标准是用户不翻历史也能知道：谁在请求、请求什么工具、关联任务是什么、命令/参数是什么、批准后正在执行还是等待下一次审批、还剩几项、最终成功还是失败。缺任意一项都当作 blocker。
8. 复盘体验的硬标准是 Run Detail / Runs History 能完整回答：谁做了什么、用了什么工具、为什么卡住、审批了什么、stdout/stderr 是什么、产物在哪里、失败原因是什么、父子 Run 如何关联、能否取消或重跑。长内容可以折叠，但展开后不能省略关键细节。
9. 每次修完立即跑最小必要验证：相关 pytest、frontend build 或 `git diff --check`；真实 smoke 必须记录 session id、run id、入口、看到的状态和未覆盖风险。没有证据的“应该好了”不进入完成记录。

2026-06-07 推进规则补充：

1. 当前进入交付推进模式：每个小目标只接受当前代码证据，不再用历史观察替代验收；证据至少包括一个相关回归、一个真实 API / Browser smoke，或一个明确收敛到文件/接口/状态机的 blocker。
2. 群组对话优先验收用户会真的遇到的连续链路：普通目标派发、多个 Agent 同时审批、批准/拒绝后的后续审批与进度、全部终态后的主模型汇总、Run Detail 可追溯。视觉调优只在影响这些链路理解时插入。
3. Workflow 优先验收显式自动化闭环：Studio 当前画布保存运行、Runs 手动运行、Chat 显式 `@Workflow`、父 Workflow 与子 Agent Run 的关联、子 Agent 审批/产物/失败归因、重跑和取消。Workflow 不进入群聊隐式派发。
4. 等待超过一次轮询窗口仍没有可判定状态时，立即切到后端 API 和 run timeline；API 正确就修前端同步，API 不正确就修运行状态机，模型输出不稳定才修 prompt / 清洗策略。
5. 本轮剩余推进顺序已按当前进度重排：普通群聊目标的多 Agent 混合终态回归和 Bridge API smoke 已完成；当前先补 Workflow 显式审批拒绝的当前代码 reload smoke，确认 Run / Chat / artifact / timeline 都按最新状态机收束；随后继续 Runs 手动重跑、Workflow 真实 Agent 节点长链路、子 Agent 审批与失败归因压测。已经通过的 Workflow 子 Agent artifact / 父子 Run Detail smoke 不再占用下一步队列。

2026-06-07 修正后推进目标与硬规定（当前执行版）：

1. 目标描述改成验收队列，不再用“距离目标还差多少”模糊估算；每次推进必须锁定一个用户入口、一个状态机结果、一个可复盘页面或 API 证据。
2. 群组对话的当前成功条件是普通用户只在群里发布目标，主模型能自动派给合适 Agent；Agent 的审批、执行、失败和完成都能被用户在输入区/消息区/Run Detail 看到；所有 Agent 进入终态后，主模型必须一次性整理成功项、失败项、原因和下一步。
3. Workflow 的当前成功条件是用户显式选择 Workflow（Studio 保存运行、Chat `@Workflow`、Runs 手动运行或 Run Detail 重跑），父 Workflow、子 Agent、审批、artifact、失败节点和重跑都能在 Run Detail / Runs History 中完整追踪。
4. 等待规则保持强制：真实链路只短暂观察一次；没有进入 completed / failed / cancelled / approval_required / processing 这类可判定状态时，立刻查 API 和 run timeline，不再盯页面硬等。
5. 每轮只承认四类进度：代码修复、回归测试、真实 Browser/API smoke、或把 blocker 收敛到明确文件/接口/状态机。纯观察、重复截图、未验证推测都不算推进。
6. 文档记录必须跟着证据走：新增测试写测试名，真实 smoke 写 session id / run id / workflow id，失败写 blocker 和修复位置；如果没有这些信息，本轮不能标为完成。

2026-06-07 推进校准（不再原地等待）：

1. 接下来的目标写成验收切片，而不是继续讨论“大功能还差多少”。每个切片必须同时写清用户入口、预期用户可见状态、后端状态机终态、Run Detail / API 复盘证据和最小回归或 smoke。
2. 群组对话剩余距离按 blocker 计数：协议隐藏、普通目标自动派发、多 Agent 同时审批、连续审批、成功/失败混合汇总、长目标不截断和多轮 RunGroup 不串台已经有回归或 smoke；剩余优先项是更长真实模型链路、真实模型偶发错误输出清洗、输入区审批队列与 Run Detail 刷新的最终前端复核。
3. Workflow 剩余距离按显式运行闭环计数：Studio 保存运行、Chat 显式 `@Workflow`、Workflow 自身审批批准、Workflow 自身审批拒绝、子 Agent 失败冒泡、父子 Run Detail 桥接和子 Agent 多次审批后端/Chat 计数已经有回归或 smoke；剩余优先项是 Runs 手动重跑、真实 Agent 节点长链路、子 Agent 多次审批的真实前端 Run Detail smoke 和 artifact 继承。
4. 一条切片最多只允许一次短观察窗口。没有进入 `completed` / `failed` / `cancelled` / `approval_required` / `processing` 的明确状态，就立即降级到 API 和 timeline；API 正确修前端同步，API 不正确修后端状态机，状态都正确但文字不清楚才修 prompt / 清洗。
5. 每轮结束必须留下“可继续推进”的结果：通过并记录 session/run/workflow id，失败并写 blocker 名称和修复文件，或者明确下一条最短 smoke。不能用“还在等模型/页面刷新”作为本轮结论。
6. 当前下一条执行切片已推进到 Run Detail 侧：Workflow 显式 `@Workflow` 自身审批拒绝已在当前重启后的后端代码上确认 Chat 终态、RunGroup 终态、artifact 404、timeline 同时包含 `workflow.node.approval_rejected` 和顶层 `workflow.run.cancelled`；Workflow 子 Agent 多次审批已补 route/Chat 回归，并完成真实 Run Detail 第二次审批 smoke。下一条切片转为 Chat 显式 `@Workflow` 输入区体验：确认子 Agent 第一次批准后，Chat 输入区附近仍显眼展示第二个审批请求，并在第二次批准后收束到 completed / artifact / 计数归零。

验收矩阵按这个顺序推进：

1. 群聊普通目标：不 @ 任何人，主模型自动选择两个合适 Agent，隐藏派发协议，Agent 完成后主模型统一总结。2026-06-06 追加：已补确定性后端验收回归 `test_plain_group_goal_dispatches_two_agents_and_summarizes`，覆盖普通群聊消息、两 Agent 派发、协议隐藏、RunGroup 复用、Agent 结果回主模型汇总，以及汇总完成后父消息状态收束。2026-06-06 追加真实 Chat Profile smoke：创建隔离群组 `Codex Group Smoke 0002`（session `57ad1c4a`，成员 `agent_coding` / `agent_design`），发送普通目标“不 @ 任何人”；主模型任务 `837f2f9726d2` 在约 16.9s 完成，聊天气泡自然语言说明“我把 2 个任务分别派给 编码Agent Iroha、设计Agent Kaguya 了”，未暴露 `<yachiyo_group_dispatch>` / `dispatchgroupagent` / JSON；后端 session context 绑定 RunGroup `run_group_210a0c5e5202`，创建 `agent_run_71f7ec826b29` 与 `agent_run_b570161cf755`，两个 Agent 均完成，Design 产物 `design/char-stats-card-copy.md` 在聊天气泡显示 `产物 1` 与运行详情入口；最终主模型汇总任务 `662203353a35` 完成，明确列出 Iroha 代码方案、Kaguya UI 文案产物和“失败/取消项：无”。前端 Chat 页面 `#/chat?session_id=57ad1c4a` 可见 5 条消息（用户目标、派发、两个 Agent 结果、主模型汇总），和 API 状态一致。
   2026-06-07 追加多轮连续任务真实 Chat Profile + Browser smoke：创建隔离群组 `Codex Group Multi Turn Smoke 0007`（session `98966277`，成员 `agent_coding` / `agent_design`），第一轮主任务 `cf134dc76a29` 派给 Iroha / Kaguya 两个纯文字 marker 任务，生成 RunGroup `run_group_43105e6e5a6d`，Agent Run `agent_run_1351de485ff2` 与 `agent_run_7ebf1be750e9` 均 completed，最终主模型汇总任务 `4d44b676abe1` 只整理 `round-one-iroha-marker` / `round-one-kaguya-marker`。同一群组继续发送第二轮新目标 `b69b7e4b5826`，明确不是第一轮补充；第二轮生成新的 RunGroup `run_group_f10d010191fe`，Agent Run `agent_run_b863498b40b8` 与 `agent_run_5dd958ab0c7c` 的 `delegated_by_task_id` 均指向第二轮父任务，最终汇总任务 `502dc35b3262` 只整理 `round-two-iroha-marker` / `round-two-kaguya-marker`，并确认未包含第一轮内容。API 最终 `processing_count=0`、`approval_count=0`；Browser 打开 `#/chat?session_id=98966277` 可见两轮连续完成、无协议 JSON、无待审批/处理中残留。
   2026-06-07 追加长输出/协议隐藏推进：群内直接运行 Agent 的完成气泡不再用 140 字预览截断“任务：”，改为完整保留用户目标；新增回归 `test_group_direct_agent_completion_keeps_full_goal_in_chat`，确认长目标尾部 `long-goal-tail-marker-917263` 会出现在 Agent 气泡和主模型汇总任务描述中，且任务行没有 `...`。同时新增 `test_group_main_model_dispatch_bare_json_block_is_hidden_and_dispatched`，覆盖主模型完成态输出“自然语言 + 裸 JSON 派发块”时，Chat 会隐藏 `{"action":"dispatch_group_agent"...}` 并正常派发，不只依赖 `<yachiyo_group_dispatch>` 标签。真实 Bridge + Browser smoke：重启 backend 加载新代码后，创建临时 Agent `agent_codex_long_goal_display_smoke_917265` 和群组 `Codex Long Goal Display Chat Smoke 917265`（session `6dc51db4`），发送带尾部 `long-goal-display-tail-marker-917265` 的长目标，Agent Run `agent_run_09cdd42a698a` / RunGroup `run_group_2677db6a3fc4` completed，API 和 in-app Browser 均确认 Agent 完成气泡自身完整包含 tail marker，且没有 `...` 截断；主模型整理任务 `66a0ef3c1a10` completed，最终 Chat `processing_count=0`、`approval_count=0`。本次临时 stub 端口 `127.0.0.1:8765` 已关闭。
   2026-06-07 追加当前代码协议隐藏复核：复跑 `test_group_main_model_dispatch_tagged_tasks_block_is_hidden_and_dispatched`、`test_group_main_model_dispatch_bare_json_block_is_hidden_and_dispatched`、`test_group_main_model_dispatch_stream_hides_partial_json_without_rewind`、`test_group_dispatch_pending_clears_when_final_message_has_no_dispatch`、`test_group_dispatch_hides_protocol_meta_intro_lines`、`test_get_messages_hides_internal_reasoning_activity`，6 条通过，确认当前代码仍覆盖标签块、裸 JSON、流式半截 JSON、防回退、协议元话术清洗和内部 activity 隐藏。
2. 群聊审批目标：一个 Agent 触发 `terminal.run` 或 `workspace.write_patch`，审批卡、输入区提醒、批准后进度、后续主模型总结都要连续可见。2026-06-06 追加：已补确定性后端验收回归 `test_plain_group_goal_approval_flow_continues_to_main_summary`，覆盖普通群聊消息派发到 Agent、`terminal.run` 待审批内容写入聊天消息、`approval_count` 驱动输入区提醒、批准后审批卡清空并显示继续执行进度、Agent 完成后才创建主模型汇总、汇总完成后父消息状态收束。
   2026-06-06 追加真实 Chat Profile + Browser smoke：首次用 `Codex Group Approval Smoke 0003`（session `72eb248e`）压测时暴露真实 blocker：主模型自然语言派给 `Iroha`，但群组派发解析只做全局 name/nickname 精确匹配，未把短名 `Iroha` 命中到当前群成员 `编码Agent Iroha`，导致后端记录“未找到群组 Agent”，后续主模型还把未执行项整理成了虚假的 approval_required。已修复群聊派发层的群成员别名解析，当前群组内会优先按成员 id/name/nickname/category、尾词短名和常见类别别名（如 `Iroha`、`coding`、`Code`）解析，再回退全局 runnable 查找；新增回归 `test_group_dispatch_resolves_member_short_alias_and_category`，并复跑 `tests/test_chat_api.py` → 126 passed。
   修复后创建隔离群组 `Codex Group Approval Smoke 0004`（session `c3377a59`，成员 `agent_coding`，name `Coding Agent`，nickname `编码Agent Iroha`，category `coding`），发送目标明确派给 `Iroha` 并要求 `terminal.run` 执行 `printf group-approval-smoke-fixed`；主模型任务 `2d63e6a10327` 完成自然语言派发，未暴露机器协议，并创建 RunGroup `run_group_1021ce859343` 与 Agent Run `agent_run_51ccef438af6`。API 显示 `approval_count=1`、`run_status=approval_required`，前端 Chat 页面 `#/chat?session_id=c3377a59` 显示结构化审批卡、BASH 请求内容和输入框上方待审批提醒；从前端点击“批准”后按钮立即释放并转为处理中，Run 完成，命令 stdout 为 `group-approval-smoke-fixed`，`approval_count` / `processing_count` 归零；最终主模型汇总任务 `d8c0a38e1604` 完成，明确说明 Agent 发起请求、用户批准、命令执行、输出和无文件/无补丁。前端最终状态显示群组就绪、Agent 完成、主模型已整理，无残留待审批提醒。
   2026-06-07 追加连续审批回归：新增 `test_agent_run_consecutive_terminal_approvals_update_pending_request`，覆盖同一 Agent 一次模型回复连续请求两个 `terminal.run` 时，第一次批准后 Run 必须继续停在 `approval_required` 并把 `pending_approval` 更新成第二条命令，第二次批准后才进入 completed；补强 `test_group_agent_consecutive_approval_updates_pending_request`，确认 Chat API 在第一条和第二条待审批期间都保持 `approval_count=1`，同一条 Agent 气泡会替换成新的审批内容，输入区提醒不会在连续审批之间消失。已通过命令：`.venv/bin/pytest tests/test_agent_runtime.py::test_agent_run_consecutive_terminal_approvals_update_pending_request tests/test_chat_api.py::test_group_agent_consecutive_approval_updates_pending_request`。
   2026-06-07 追加真实 Chat 连续审批 smoke：用临时 OpenAI-compatible stub（仅监听 `127.0.0.1:8765`，结束后已关闭）创建临时 Agent `agent_codex_consecutive_approval_smoke_917263` 和群组 `Codex Consecutive Approval Chat Smoke 917263`（session `595b9c39`），通过真实 Bridge `/ui/chat/messages` 以 `runnable_id` 直接触发 Agent Run `agent_run_489633f8b1c0` / RunGroup `run_group_430a6082e0b7`。第一轮 Chat API 显示 `approval_count=1`、气泡请求 `printf chat-consecutive-first`；批准后 Run 保持 `approval_required`，`pending_approval` 切换为 `printf chat-consecutive-second`，Chat 气泡同步替换且 `approval_count` 仍为 1；第二次批准后 Run completed，timeline 包含两次 `agent.tool.approval_approved`、两个 stdout（`chat-consecutive-first` / `chat-consecutive-second`），Chat `approval_count=0`、`processing_count=0`，并创建主模型整理任务 `bf3def5b6d2c`。in-app Browser 打开 `#/chat?session_id=595b9c39` 确认页面可见 Agent 完成、运行详情入口、两条命令输出和主模型总结“连续审批 Smoke 测试 — 全部通过”，且无可见批准/拒绝残留按钮。
   2026-06-07 追加多 Agent 同时审批输入区导航 smoke：用临时 OpenAI-compatible stub（仅监听 `127.0.0.1:8765`，结束后已关闭并确认端口释放）创建 `agent_codex_multi_approval_alpha_917267` / `agent_codex_multi_approval_beta_917267` 和隔离群组 `Codex Multi Approval Chat Smoke 917267`（session `78454cbd`）。通过真实 Bridge 直接触发两个 Agent Run：`agent_run_f2dc9a737522` 与 `agent_run_33eb4f69e4b4`，API 显示两条 Agent 气泡同时 `approval_required`、`approval_count=2`、输入区提醒在 Browser 中显示 `待审批 2/2`，并提供上一项、下一项、定位消息、运行详情、批准、拒绝。点击“上一项”后提醒切到 `待审批 1/2` 且指向 Multi Alpha；从输入区批准第一项后，API 变为 `approval_count=1`，页面只剩 Multi Beta 单项待审批；批准第二项后，输入区审批提醒和所有批准/拒绝残留按钮消失，API 最终 `approval_count=0`、`processing_count=0`，两个 Agent Run 均 completed。限制记录：本次 stub 因共享上下文让 Beta 命令也用了 `multi-approval-alpha-917267` marker，但两个 requester、run 和审批队列项独立；主模型最终总结也指出 Beta 标识异常，说明 Agent 结果回主模型复盘链路能暴露执行内容不一致。
   2026-06-07 追加当前代码群聊审批队列补强：复跑 `test_group_multiple_agent_approvals_wait_until_every_delegate_terminal`、`test_group_agent_consecutive_approval_updates_pending_request`、`test_group_agent_approval_rejection_creates_main_summary`、`test_group_agent_approval_waits_for_all_delegates_before_summary`，4 条通过，确认两个 delegated Agent 同时待审批、一个完成一个失败/拒绝、补充归入当前批次、最终主模型汇总收束仍稳定。前端同时补强 `ChatView` 的 composer 待审批队列：当同一 run 批准后立刻返回下一次 `approval_required`、但消息刷新还短暂停在旧 pending 时，`approvalRequiredItems` 会优先使用该 run 的 override 签名和详情生成新待审批项，避免旧 item 被本地 suppress 后输入区暂时没有任何审批提示。已通过命令：`npm run build`（`apps/frontend`）和上述 4 条 Chat 回归。
   2026-06-07 追加自然群聊多 Agent 同时审批真实前端 smoke：为避免依赖真实模型随机性，临时创建 Chat Profile `profile_codex_group_smoke_917277` 指向本地 OpenAI-compatible stub（仅监听 `127.0.0.1:8765`，结束后关闭并恢复默认 Chat Profile `profile_f37d53e553a1` 与 Hermes Xiaomi 配置），并创建临时 Agent `agent_codex_group_design_917277` / `agent_codex_group_coding_917277`。隔离群组 `Codex Group Approval Queue Smoke 917281`（session `6a35a86c`，成员主模型 + 2 Agent）中发送普通目标“不 @ 任何人”，主模型 streaming 返回自然说明和隐藏的 `<yachiyo_group_dispatch>`，Chat 仅显示“我把 2 个任务分别派给 Design Approval Agent 917277、Coding Approval Agent 917277 了”，未暴露机器块。两个 Agent Run `agent_run_692db37f743f` / `agent_run_a8261123ba7e` 同时进入 `approval_required`，API 显示 `processing_count=2`、`approval_count=2`，命令分别为 `printf group-design-approval-917277` 与 `printf group-coding-approval-917277`。Browser 打开 `#/chat?session_id=6a35a86c` 后，消息区有两张结构化审批卡，输入区附近显示 `待审批 1/2`、当前 Agent、关联任务、定位消息、运行详情、批准/拒绝；从输入区批准第一项后，API 变为 `processing_count=1`、`approval_count=1`，输入区保留 Coding 单项待审批且命令为 `printf group-coding-approval-917277`；批准第二项后，输入区审批提醒和批准/拒绝按钮消失，两个 Agent completed，主模型汇总 `最终汇总：Design 完成 group-design-approval-917277；Coding 完成 group-coding-approval-917277。成功项 2，失败项 0，产物见运行详情。` 出现，最终 API `processing_count=0`、`approval_count=0`、`is_processing=false`。本次补齐了 `917267` 的 marker 限制，并覆盖自然群聊入口、主模型派发、双 Agent 审批队列、逐项处理和最终汇总的当前代码真实前端证据。
3. 群聊异常目标：一个 Agent 完成、一个失败或被拒绝，主模型总结必须明确成功项、失败项、原因和可继续动作。2026-06-06 追加：已收紧群组 Agent 汇总 prompt，要求明确区分成功项、失败/取消/拒绝项、失败原因、未执行派活、可验收内容/产物和用户下一步可选动作；`test_group_multiple_agent_approvals_wait_until_every_delegate_terminal` 已锁定一个 Agent 完成、另一个 Agent 失败时，主模型汇总任务会带上成功汇报、失败原因、执行线索和该结构化整理要求。
   2026-06-07 追加真实 Chat Profile + Browser smoke：创建隔离群组 `Codex Group Mixed Outcome Smoke 0005`（session `ec5f8d5b`，成员 `agent_design` / `agent_coding`），发送普通群聊目标要求 Kaguya 做纯文字天气卡片 UI 方案、Iroha 请求 `terminal.run` 执行 `printf group-reject-smoke` 并由用户故意拒绝。主模型任务 `a65283520951` 自然语言拆分两项并创建 RunGroup `run_group_181e01b44ad8`；Kaguya Run `agent_run_0787193c26db` completed，未调用工具、未创建文件，输出布局、HEX 配色和字号层级；Iroha Run `agent_run_7950de593b8b` 进入 `approval_required`，前端 Chat 显示结构化审批卡、BASH 请求内容和输入框待审批提醒。从前端点击“拒绝”后，Iroha 消息转为 failed/cancelled 并显示“工具审批已拒绝：Rejected from chat”，`approval_count` 归零；最终主模型汇总任务 `f63d55ff8ac5` completed，明确列出 Kaguya 成功项、Iroha 用户拒绝项、拒绝原因和下一步可选动作。Smoke 同时暴露轻微可见协议痕迹：主模型正文出现“派活协议需要直接在回复里输出”，activity 里出现内部 `send_message` 派发壳；已在 Chat 可见内容清洗层删除明显“派活协议/机器派发协议/dispatch_group_agent”元话术，并在 Chat activity 过滤中隐藏 `send_message` 和 group dispatch 内部协议事件。新增回归 `test_group_dispatch_hides_protocol_meta_intro_lines`，扩展 `test_get_messages_hides_internal_reasoning_activity`，复跑 `tests/test_chat_api.py` → 127 passed。
   2026-06-07 追加汇总收束回归：扩展 `test_group_multiple_agent_approvals_wait_until_every_delegate_terminal`，在两个 Agent 同属一个主模型派发批次、两者都先进入待审批、随后一个 completed、一个 failed 的路径上，继续模拟主模型汇总任务 completed；断言父派发消息清掉 `group_agent_summary_pending`，写入 `group_agent_summary_status=completed`，最终 `approval_count=0`、`processing_count=0`、`is_processing=False`。已通过命令：`.venv/bin/pytest tests/test_chat_api.py::test_group_multiple_agent_approvals_wait_until_every_delegate_terminal`。
   2026-06-07 当前执行版追加普通群聊混合终态回归：新增 `test_plain_group_goal_mixed_agent_outcomes_waits_and_summarizes`，覆盖用户不写 `@主模型`、只在群里发布普通目标时，主模型派给 Design / Coding 两个 Agent；两者同时进入 `approval_required` 时 `approval_count=2` 且不提前创建主模型汇总；Design 完成后仍等待 Coding；Coding failed 后才创建主模型汇总任务，并要求总结成功项、失败项、失败原因、执行线索、可验收内容和下一步。汇总完成后父派发消息清掉 `group_agent_summary_pending`，写入 `group_agent_summary_status=completed`，最终 `approval_count=0`、`processing_count=0`、`is_processing=False`。已通过命令：`.venv/bin/pytest tests/test_chat_api.py::test_plain_group_goal_dispatches_two_agents_and_summarizes tests/test_chat_api.py::test_plain_group_goal_mixed_agent_outcomes_waits_and_summarizes tests/test_chat_api.py::test_group_multiple_agent_approvals_wait_until_every_delegate_terminal`。
   2026-06-07 当前执行版追加真实 Bridge API smoke：启动临时 OpenAI-compatible stub（仅 `127.0.0.1:8765`，结束后已关闭并确认端口释放），创建临时 Agent `agent_codex_plain_mixed_design_917271` / `agent_codex_plain_mixed_coding_917271` 和隔离群组 `Codex Plain Mixed API Smoke 917271`（session `7ecb1718`）。用户只发送普通群聊目标、不写 `@主模型`，主模型任务 `467c976e10da` 自然语言派给 `DesignSmoke917271` / `CodingSmoke917271` 两个 Agent，未暴露派发 JSON，并创建 RunGroup `run_group_1c2fa8780b18`。Design Run `agent_run_6a349a179d0f` completed，返回 `plain-mixed-api-smoke-design-ok`；Coding Run `agent_run_1324b7fe6f5c` 进入 `approval_required`，Chat API 显示 `approval_count=1`、审批内容为 `terminal.run` 命令 `printf plain-mixed-api-smoke-coding-fail; exit 7`。通过 Bridge `POST /ui/runs/agent_run_1324b7fe6f5c/approval/approve` 批准后，Coding Run failed，timeline 包含 `agent.tool.approval_approved`、`agent.tool.call` returncode 7、`agent.tool.failed`、`agent.run.failed`；随后主模型汇总任务 `6d8411b2961f` completed，明确列出 Design 成功项、Coding 失败项、命令、退出码、stdout 和下一步。最终 Chat API `processing_count=0`、`approval_count=0`，父派发消息 metadata 写入 `group_agent_summary_status=completed`。本轮 browser 控制工具未暴露，未补截图级证据；以 Bridge API 和 Run Detail API 为当前真实证据。
   2026-06-07 当前执行版追加自然群聊混合拒绝真实前端 smoke：临时创建 Chat Profile `profile_codex_group_mixed_917282` 指向本地 OpenAI-compatible stub（仅 `127.0.0.1:8765`，结束后恢复默认 Chat Profile `profile_f37d53e553a1` 与 Hermes Xiaomi 配置），最终使用修正版 marker `917285` 创建临时 Agent `agent_codex_group_mixed_design_917285` / `agent_codex_group_mixed_coding_917285` 和隔离群组 `Codex Group Mixed Outcome Smoke 917285`（session `3b9979cc`）。用户只发送普通群聊目标、不写 `@主模型`；主模型 streaming 返回自然说明和隐藏派发块，Chat 只显示“我把 2 个任务分别派给 Design Mixed Agent 917285、Coding Mixed Agent 917285 了”，未暴露机器块，并创建 RunGroup `run_group_aa31ec112d61`。Design Run `agent_run_2deec4c3c869` completed，结果包含 `group-mixed-design-917285`；Coding Run `agent_run_c9a90c528436` 进入 `approval_required`，消息区显示结构化审批卡，输入区附近显示 `待审批`、当前 Agent、`terminal.run`、关联任务、命令 `printf group-mixed-coding-917285`、定位消息、运行详情、批准/拒绝。通过 in-app Browser 从输入区点击“拒绝”后，批准/拒绝按钮立即归零，Coding 气泡转为 failed/cancelled 并显示 `工具审批已拒绝：Rejected from chat`；随后主模型汇总任务 `a93d28a595d3` completed，最终 Chat API `processing_count=0`、`approval_count=0`、`is_processing=false`，前端显示群组 `就绪 · 群组 · 3 成员`，无待审批残留。最终汇总明确写出 `Design 成功完成 group-mixed-design-917285`、`Coding 的 terminal.run 请求被用户拒绝`、`成功项 1`、`失败/拒绝项 1` 和下一步；Run Detail API 也确认 Coding timeline 包含 `agent.tool.approval_required` 与 `agent.tool.approval_rejected`，pending approval 清空。
4. 群聊中途补充：Agent 运行期间用户追加约束或纠偏，最终主模型整理必须带上补充内容，且不会串到旧 RunGroup。2026-06-06 追加：已补确定性后端验收回归 `test_plain_group_followup_during_running_agent_enters_main_summary`，覆盖普通群聊目标派发后 Agent 仍在 `processing` 时，用户追加补充会标记到当前 `delegated_by_task_id`，Agent 完成后主模型汇总包含补充内容，并排除后来明显新开的目标；同时继续保留 `test_group_followup_targets_latest_active_delegated_batch` 和 `test_group_followup_after_unsynced_completed_dispatch_enters_summary` 覆盖最新批次归属与 UI 尚未同步完成时的补充归属。
   2026-06-07 追加真实 Chat Profile + Browser smoke：创建隔离群组 `Codex Group Followup Smoke 0006`（session `f2b752cc`，成员 `agent_coding` / `编码Agent Iroha`），发送普通群聊目标要求主模型派给 `Iroha`，Agent 必须请求 `terminal.run` 执行 `printf group-followup-smoke`，并等待用户审批；主模型任务 `b1e3af45c65f` 自然语言派发并创建 RunGroup `run_group_aaae112c76d3`，未暴露机器派发 JSON。Agent Run `agent_run_1d6676539c04` 进入 `approval_required`，前端 Chat 同时显示结构化审批卡和输入区待审批提醒；审批前发送补充任务 `035f44e87ab4`，用户消息 metadata 写入 `group_followup_for_task_ids=['b1e3af45c65f']`，前端显示“已作为当前群组任务补充”，主模型只做记录确认，没有重新派发。随后从前端点击“批准”，审批 `approval_32eb5be1e311` 清空，Agent Run completed，stdout 为 `group-followup-smoke`；最终主模型汇总任务 `cfb825482910` completed，明确写出 `紫阳花 follow-up marker`，说明这是审批前追加的用户补充，并确认未创建文件、未写补丁、未触发重新派发。API 最终状态 `processing_count=0`、`approval_count=0`，Browser 可见最终汇总与输入区无残留待审批提醒。
5. Workflow 设计运行：新建或修改 Workflow，配置 Step Task、Approval Criteria 和 Artifact Path，保存并运行必须使用最新画布。2026-06-06 追加：已补 Bridge route 层确定性验收回归 `test_workflow_routes_save_and_run_latest_canvas_with_step_approval_and_artifact`，覆盖先保存旧图、再用 ReactFlow 画布形态更新为包含 Step Task / Approval Criteria / Artifact Path 的新图，随后立即创建 Workflow Run；断言运行快照、Agent child goal、审批 pending 内容、批准后 artifact 写出都来自最新画布，旧 Agent 不参与运行。2026-06-06 追加真实前端/CDP smoke：在 Vite `http://127.0.0.1:5174` + 当前代码 Bridge `http://127.0.0.1:8421` 中，从 Workflow Studio 新建 `Codex UI Smoke Workflow 145013`，通过前端按钮添加 Artifact、填写目标并点击“保存并运行 Workflow”，后端保存画布为 Start + Artifact 两节点一条边，Run `workflow_run_1e92c4995e43` 完成并写出 `artifact.md`；随后新建 `Codex UI Approval Smoke 150738`，前端按钮添加 Approval + Artifact，配置 Approval Criteria 和 `smoke/ui-approval.md`，保存运行后运行快照包含当前画布节点、边、criteria 和 artifact path。
   2026-06-07 追加 Chat 显式触发真实前端 smoke：新建普通 Chat 会话 `8bb1350c`，通过 API 创建最小 Workflow `Codex Chat Trigger Workflow 0008`（workflow `workflow_codex-chat-trigger-workflow-0008_38b1195a`，Start -> Artifact，artifact path `smoke/chat-workflow-trigger.md`），在 Chat 中发送 `@Codex Chat Trigger Workflow 0008 请把这段目标原样写入 workflow artifact：chat-workflow-trigger-marker。`；Chat 入口返回 Workflow Run `workflow_run_5315ff92e7a0`、RunGroup `run_group_77fa43b26d9a`，随后 Run completed，timeline 包含 `workflow.run.started`、`workflow.node.start`、`workflow.node.artifact`、`workflow.run.completed`，artifact 内容为 `请把这段目标原样写入 workflow artifact：chat-workflow-trigger-marker。`。Browser 打开 `#/chat?session_id=8bb1350c` 可见 Workflow 气泡 completed、`产物 1` 和“运行详情”；打开 Runs / Run Detail 可见 `Workflow Steps · 2`、Execution 4 条事件、artifact `smoke/chat-workflow-trigger.md`、`准备重跑 / 重新运行 / 打开 Workflow Studio`。
   2026-06-07 追加群聊显式触发真实前端 smoke：创建最小 Workflow `Codex Group Trigger Workflow 0009`（workflow `workflow_codex-group-trigger-workflow-0009_bcf2440c`，Start -> Artifact，artifact path `smoke/group-chat-workflow-trigger.md`），创建隔离群组 `Codex Group Workflow Mention Smoke 0009`（session `072103da`，成员 `main` / `agent_coding` / `agent_design`），在群聊中发送 `@Codex Group Trigger Workflow 0009 群聊显式 Workflow smoke：请把 group-workflow-mention-marker 写入 workflow artifact，并保持当前群聊上下文；不要让主模型把 Workflow 当成群组 Agent 派发。`；入口直接返回 Workflow Run `workflow_run_24c0cf44f494`、RunGroup `run_group_adee7f9be9a7`，没有创建主模型派发任务。API 最终显示 session context 仍为 `conversation_kind=group`、群组名和 3 个成员保留，消息只有用户显式 `@Workflow` 与 Workflow 完成气泡两条，`processing_count=0`、`approval_count=0`、无 `group_agent_summary_for_task_id`；Run completed，timeline 包含 `workflow.run.started`、`workflow.node.start`、`workflow.node.artifact`、`workflow.run.completed`，artifact 内容为 `群聊显式 Workflow smoke：请把 group-workflow-mention-marker 写入 workflow artifact，并保持当前群聊上下文；不要让主模型把 Workflow 当成群组 Agent 派发。`。Browser 打开 `#/chat?session_id=072103da` 可见群聊仍为“就绪 · 群组 · 3 成员”、Workflow 气泡 completed、`产物 1` 和“运行详情”；Run Detail 可见 `Workflow Steps · 2`、Execution 4 条事件、artifact `smoke/group-chat-workflow-trigger.md`、`准备重跑 / 重新运行 / 打开 Workflow Studio`。
   2026-06-07 追加 Workflow 子 Agent 审批真实前端 smoke：创建 Workflow `Codex Workflow Child Approve Smoke 0010`（workflow `workflow_codex-workflow-child-approve-smoke-0010_0a4f5c06`，Start -> Coding Agent -> Artifact，artifact path `smoke/workflow-child-approve.md`），普通 Chat 会话 `27bebcb8` 中显式 `@Workflow` 触发，生成父 Run `workflow_run_83079c986af9`、RunGroup `run_group_2d0793238d4e` 和子 Agent Run `agent_run_010b1e108d2a`。父 Workflow 进入 `approval_required` 后，Run Detail 顶部明确显示“Workflow 正在等待子 Agent 审批”、子节点 `Coding Approval Child`、工具 `terminal.run`、命令 `printf workflow-child-approve-smoke`、完整 Agent 目标，以及“批准子 Agent / 拒绝子 Agent / 打开子 Run”；从前端点击“批准子 Agent”后页面即时提示“已批准，Run 正在继续执行”，API 显示子 Agent `agent.tool.approval_approved`、`agent.run.resumed`、`terminal.run` stdout 为 `workflow-child-approve-smoke`，父 Workflow timeline 追加 `workflow.run.child_resumed`、`workflow.run.resumed`、`workflow.node.artifact`、`workflow.run.completed`。最终 Chat 气泡 completed，`processing_count=0`、`approval_count=0`；父 artifact `smoke/workflow-child-approve.md` 通过 `/ui/runs/workflow_run_83079c986af9/artifacts/smoke%2Fworkflow-child-approve.md` 读取，确认包含 stdout marker；Run Detail 自动刷新到 completed，`Workflow Steps · 3` 三个节点完成，`Execution · 8` 可看到 Child Run 完成记录，`Artifacts · 1` 可打开 Workflow artifact。
   2026-06-07 追加当前代码重跑验证：backend 重启加载最新代码后，启动临时 stub（仅 `127.0.0.1:8765`，结束后已关闭），创建子 Agent `agent_codex_workflow_child_current_smoke_917266` 和 Workflow `workflow_codex_current_child_approval_smoke_917266`（Start -> `Current Approval Child` -> Artifact `smoke/workflow-current-child-approval-917266.md`）。普通 Chat 新会话 `7d97cdcd` 中通过显式 Workflow runnable 触发，父 Run `workflow_run_f5e84647073a` / RunGroup `run_group_bae62a12587b` 进入 `approval_required`，Chat 显示 `approval_count=1`，消息明确列出等待对象、Workflow 节点、审批工具 `terminal.run` 和命令 `printf workflow-current-child-approval-917266`。Browser 打开父 Run Detail `#/agents/workflow_run_f5e84647073a`，确认顶部可见“批准子 Agent / 拒绝子 Agent / 打开子 Run”和完整命令；从 Run Detail 点击“批准子 Agent”后页面自动刷新到 completed，`Workflow Steps · 3` 显示 Start、Agent、Artifact 全部完成，Execution 可见 `workflow.run.child_resumed`、`workflow.run.resumed`、`workflow.node.artifact`、`workflow.run.completed`，且 `Artifacts · 1` 列出 `smoke/workflow-current-child-approval-917266.md`。API 确认父 Run completed、子 Run `agent_run_d6748662681a` completed，子 Agent timeline 包含 `agent.tool.approval_approved`、stdout `workflow-current-child-approval-917266`，父 artifact 内容为 `Workflow child approval result marker: workflow-current-child-complete-917266`；Chat 最终 `processing_count=0`、`approval_count=0`，Workflow 气泡显示 completed、产物 1 个和运行详情入口。
   2026-06-07 追加 Workflow 子 Agent 失败分支真实前端 smoke 与修复：先用 `Codex Workflow Child Failure Smoke 0011`（session `dba18ae5`，父 Run `workflow_run_c829b24eb644`，子 Run `agent_run_0bc5a2a8a1bd`）复现 blocker：子 Agent 批准后执行 `python3 -c "import sys; print('workflow-child-failure-smoke'); sys.exit(7)"`，模型正文知道 exit code 7 是失败，但 Agent Run / 父 Workflow / Chat 仍被标为 completed，且后续 Artifact 节点继续执行。已修复 Agent Runtime：`terminal.run` 实际执行后如果 `ok=False`（例如非零退出码），会记录 `agent.tool.failed` 并把 Agent Run 置为 failed，不再让模型总结把失败包装成成功；父 Workflow 原有 `child_status != completed` 分支会继续把失败冒泡给 Workflow / RunGroup / Chat。随后 `Codex Workflow Child Failure Smoke 0012`（session `44a0fa08`，父 Run `workflow_run_444ec5de0eb0`）复现第二个 blocker：用户目标写了“不要执行后续 artifact 节点，只使用 terminal.run”，目标解析器把“不要执行后续节点”误判成禁止 `terminal.run`，导致工具调用被 `blocked_by_user_goal` 跳过并再次 completed；已给 `terminal.run` 增加显式授权覆盖，目标中出现“必须/请求/只使用 terminal.run”时不再被后续节点禁止语句误伤。最终用 `Codex Workflow Child Failure Smoke 0013`（workflow `workflow_codex-workflow-child-failure-smoke-0013_6bfb41fc`，session `ac937187`，父 Run `workflow_run_d41d940ed03c`，子 Run `agent_run_bfa2e38b0958`，RunGroup `run_group_9da693f9ae70`）复测通过：Run Detail 显示 `terminal.run` 审批和命令 `printf workflow-child-failure-after-fix-smoke; exit 7`，前端批准后提示“已批准，但 Run 执行失败”；API 显示子 Agent `failed`，timeline 包含 `agent.tool.failed` 且 returncode 7，父 Workflow `failed`，timeline 包含 `workflow.run.failed` 且节点为 `Coding Failing Child`，Chat 气泡 `failed` 并显示失败节点与 stdout，`processing_count=0`、`approval_count=0`，父 artifacts 为空，确认后续 Artifact 节点未执行。新增回归 `test_agent_run_explicit_terminal_goal_not_blocked_by_downstream_no_execute_text`、`test_agent_run_fails_when_approved_terminal_returns_nonzero`、`test_workflow_fails_when_child_terminal_returns_nonzero_after_approval`，并复跑相关 4 条测试通过。剩余 Workflow 风险收窄为更长真实模型链路和真实用户设计复杂 Workflow。
   2026-06-07 追加 Run Detail 重跑复核：从 `workflow_run_d41d940ed03c` 打开 Run Detail 时发现“重新运行”初始被前端误挡，原因是前端 readiness 只认 Agent 自己的 `model_profile_id`，没有和后端一样允许内置默认 Agent（如 `agent_coding`）回退到默认 Chat Profile；已在 `AgentStudioView.tsx` 增加默认 Agent ID 规则并复跑 `npm run build` 通过。刷新后直接点击“重新运行”，创建新父 Run `workflow_run_3eaa8e4f02ec`（RunGroup `run_group_82e97f8ed445`）并自动切到新 Run Detail；页面顶部显示子 Agent 审批，工具 `terminal.run`，命令 `printf workflow-child-failure-after-fix-smoke; exit 7`，子 Run `agent_run_6a114532d456`。点击“批准子 Agent”后，UI 和 API 均显示父 Workflow `failed`、子 Agent `failed`、父 artifacts `[]`，child timeline 包含 `agent.tool.call` returncode 7、`agent.tool.failed`、`agent.run.failed`，父 timeline 包含 `workflow.run.child_resumed` 后 `workflow.run.failed`；后续 `Should Not Run Artifact` 节点保持等待/未执行。至此重跑后的子 Agent 审批、非零退出失败传播和 artifact 跳过均已真实前端复核。
   2026-06-07 追加当前代码 Chat 显式 Workflow + Run Detail 重跑复核：创建最小 Workflow `Codex Current Chat Rerun Smoke 917268`（workflow `workflow_codex_current_chat_rerun_917268`，Start -> Artifact，artifact path `smoke/current-chat-rerun-917268.md`），普通 Chat 新会话 `5ba49046` 中显式 `@Workflow` 触发，生成 Run `workflow_run_92c5c609c40a` / RunGroup `run_group_2508d6917d8a` 并 completed；API 读取 artifact 内容为 `current-workflow-rerun-marker-917268：请把这句话原样写入 artifact，作为当前代码显式 Workflow smoke。`，Chat 最终 `processing_count=0`、`approval_count=0`。Browser 打开 Run Detail 后发现当前代码仍有一个重跑 blocker：`重新运行` 被禁用，title 为 `Workflow 必须且只能有一个 Start 节点`；原因是前端 `workflowNodes()` 把后端 `type: "start"` 转成 ReactFlow `type: "input"` 时，如果旧/外部数据缺少 `data.kind`，Run Detail 重跑校验会把 Start 误判为 `input`。已修复 `AgentStudioView.tsx` 的 workflow node 归一化：从 `data.kind / data.node_type / node.type` 计算业务 kind，并写回 `data.kind`，同时按 kind 映射 ReactFlow type。刷新后 `重新运行` 可用，从 Run Detail 点击后创建新 Run `workflow_run_95a0a4453976` / RunGroup `run_group_16e3dabce239`，页面自动切到新 Run，显示 completed、Workflow Steps 和 artifact；API 确认新 Run completed、artifact `smoke/current-chat-rerun-917268.md` 可读且内容包含 marker。已通过命令：`npm run build`（`apps/frontend`）。
   2026-06-07 追加 Runs 面板手动运行当前代码复核：在 Browser 打开 `#/agents/runs`，Run Agent / Workflow 面板中选择 `Codex Current Chat Rerun Smoke 917268`，输入目标 `manual-runs-panel-workflow-marker-917269：请把这句话通过 Runs 面板手动运行写入 Workflow artifact。` 后按钮从禁用变为可运行；点击“运行”创建新 Run `workflow_run_9610ed76a27d` / RunGroup `run_group_cadc7e48a81a`，页面自动进入 Run Detail 并显示 completed、Workflow Steps、artifact 和 marker。API 确认新 Run completed，artifact `smoke/current-chat-rerun-917268.md` 内容为 `manual-runs-panel-workflow-marker-917269：请把这句话通过 Runs 面板手动运行写入 Workflow artifact。`，timeline 为 `workflow.run.started -> workflow.node.start -> workflow.node.artifact -> workflow.run.completed`。至此当前代码下 Workflow 的 Chat 显式触发、Run Detail 重跑、Runs 面板手动运行三条显式入口都已有真实前端/API 证据。
   2026-06-07 追加 Workflow 子 Agent artifact / 父子 Run Detail 当前代码 smoke：启动临时 OpenAI-compatible stub（仅 `127.0.0.1:8765`），创建 Agent `agent_codex_child_artifact_smoke_917270` 与 Workflow `workflow_codex_child_artifact_smoke_917270`（Start -> Child Artifact Agent -> Parent Artifact `smoke/parent-child-artifact-917270.md`）。通过 Bridge `/ui/workflow-runs` 运行得到父 Run `workflow_run_52a90da2f2f2` / RunGroup `run_group_a3e9f051f7de`，子 Run `agent_run_20da76594689`，两者均 completed；父 Run artifacts 同时包含 child artifact 引用 `child-artifact-917270.md`（`kind=workflow_child_artifact`，source Run `agent_run_20da76594689`，source Agent `Codex Child Artifact Smoke 917270`）和父 artifact `smoke/parent-child-artifact-917270.md`，两个 artifact 路由读回内容均包含 `child-artifact-marker-917270`。Browser 打开父 Run Detail `#/agents/workflow_run_52a90da2f2f2` 可见 `Workflow Steps`、`Child Artifact Agent`、`Open Run`、child artifact、父 artifact 和 marker；点击 `Open Run` 后发现 child 详情缺少返回父 Workflow 入口，原因是前端只把 `run_group_source === "workflow"` 识别为 Workflow child，而当前显式 smoke source 为 `workflow_child_artifact_smoke`。已修复 `AgentStudioView.tsx`：新增潜在 Workflow child 判断，并从已加载父 Workflow timeline / RunGroup `child_run_ids` 兜底恢复父 Run；直接打开 `#/agents/agent_run_20da76594689` 已显示 `返回 Workflow：Codex Child Artifact Workflow Smoke 917270`，点击后回到父 Run Detail，父页仍显示 Workflow Steps、child run、child artifact、父 artifact 和 marker。已通过命令：`npm run build`（`apps/frontend`）。临时 stub 后续关闭并确认端口释放。
   2026-06-07 当前执行版追加 Chat 显式 Workflow 自身审批 API smoke：创建 Workflow `Codex Chat Approval Workflow 917272`（workflow `workflow_codex_chat_approval_917272`，Start -> Approval `Review Gate` -> Artifact `smoke/chat-approval-workflow-917272.md`），通过 Chat runnable 入口显式触发，生成 Run `workflow_run_69ac98887d17` / RunGroup `run_group_a6caf1ad82be`。Run 先进入 `approval_required`，Chat API 显示 `processing_count=1`、`approval_count=1`，Workflow 气泡携带 `pending_approval.tool=workflow.approval`、checkpoint `Review Gate`、criteria `Confirm chat workflow approval marker 917272 before writing artifact.` 和原始目标上下文。通过 Bridge `POST /ui/runs/workflow_run_69ac98887d17/approval/approve` 批准后，Run completed，timeline 依次包含 `workflow.node.approval_required`、`workflow.node.approval_approved`、`workflow.node.artifact`、`workflow.run.completed`；artifact 路由读回 `smoke/chat-approval-workflow-917272.md`，内容包含 `approval-workflow-marker-917272`。最终 Chat API `processing_count=0`、`approval_count=0`，Workflow 气泡 completed、`run_artifact_count=1`、pending approval 清空。相关回归已通过：`.venv/bin/pytest tests/test_agent_runtime.py::test_workflow_approval_node_pauses_and_resumes tests/test_chat_api.py::test_workflow_approval_chat_message_carries_pending_details tests/test_chat_api.py::test_workflow_mention_creates_workflow_run_from_chat`。
   2026-06-07 当前执行版追加 Chat 显式 Workflow 自身审批拒绝 API smoke 与修复：新建普通 Chat 会话 `fb6c44f5`，创建 Workflow `Codex Chat Approval Reject Workflow 917273`（workflow `workflow_codex_chat_approval_reject_917273`，Start -> Approval `Reject Gate` -> Artifact `smoke/chat-approval-reject-917273.md`），通过 Chat runnable 入口触发，生成 Run `workflow_run_92049121044b` / RunGroup `run_group_3645dbc0f09a`。Run 进入 `approval_required` 后，Chat API 显示 `approval_count=1`，Workflow 气泡携带 `pending_approval.tool=workflow.approval`、checkpoint、criteria 和原始目标上下文；通过 Bridge `POST /ui/runs/workflow_run_92049121044b/approval/reject` 拒绝后，Run `cancelled`，Chat 气泡显示 failed/cancelled、取消节点 `Reject Gate（approval）` 和原因 `Workflow 审批已拒绝：Codex reject smoke 917273`，最终 `processing_count=0`、`approval_count=0`，artifact 路由返回 404，确认后续 Artifact 节点未写出。Smoke 暴露一个复盘细节：运行中代码的 timeline 只有 `workflow.node.approval_rejected`，缺少顶层 `workflow.run.cancelled`；已在 `apps/shell/agent_runtime.py` 的 Workflow 自身审批拒绝分支追加 `workflow.run.cancelled` 事件，并在 `test_workflow_approval_node_reject_cancels_run` 锁定审批节点 id/kind/label/criteria 和 input_preview。已通过命令：`.venv/bin/pytest tests/test_agent_runtime.py::test_workflow_approval_node_reject_cancels_run tests/test_chat_api.py::test_workflow_cancelled_summary_message_is_terminal_failed tests/test_chat_api.py::test_workflow_approval_chat_message_carries_pending_details`。
   2026-06-07 当前代码 reload 后追加拒绝复核：backend 重启加载最新 `agent_runtime.py` 后，继续使用 Chat 显式 Workflow 拒绝 smoke `917274`（session `97751ff3`，workflow `workflow_codex_chat_approval_reject_917274`，Run `workflow_run_7a01899e6bd8`，RunGroup `run_group_8f173a2ea6e9`）。Run 从 `approval_required` 通过 Bridge `POST /ui/runs/workflow_run_7a01899e6bd8/approval/reject` 拒绝后进入 `cancelled`；Chat 消息 `failed`，正文显示 Workflow 已取消、取消节点 `Reject Gate（approval）` 和原因 `Workflow 审批已拒绝：Codex reject smoke 917274`；`processing_count=0`、`approval_count=0`；artifact 路由 `smoke/chat-approval-reject-917274.md` 返回 404。Run timeline 同时包含 `workflow.node.approval_rejected` 和顶层 `workflow.run.cancelled`，后者带 `workflow_node_id=gate`、`workflow_node_kind=approval`、`workflow_node_label=Reject Gate`、`workflow_node_approval_criteria=Reject this smoke marker 917274 to prove cancellation path.`，确认当前代码的拒绝复盘事件已真实生效。
   2026-06-07 追加 Workflow 子 Agent 多次审批回归：在 `apps/shell/agent_runtime.py` 的父 Workflow 恢复路径中补充第二次及后续 child approval 的 `workflow.run.approval_required` timeline 事件，避免第一次批准后子 Agent 再次申请工具时，Run Detail 只剩旧的第一次等待痕迹。新增 `test_workflow_child_consecutive_approvals_keep_parent_waiting`，覆盖父 Workflow 等待子 Agent、第一次批准后 child Run 继续 `approval_required` 且 pending command 切到第二条、父 Workflow 和 RunGroup 继续 `approval_required`、timeline 中有第二条 `workflow.run.approval_required`、第二次批准后父子 Run completed。新增 `test_workflow_child_consecutive_approvals_keep_chat_prompt_visible`，覆盖 Chat API 在第一次批准后 `approval_count` 仍为 1、子 Agent 气泡和 Workflow 等待气泡都显示第二条命令、第二次批准后计数归零并清理 `workflow_waiting_pending_approval`。已通过命令：`.venv/bin/pytest tests/test_agent_runtime.py::test_workflow_child_approval_route_approve_resumes_parent_workflow tests/test_agent_runtime.py::test_workflow_child_consecutive_approvals_keep_parent_waiting tests/test_agent_runtime.py::test_workflow_child_approval_route_reject_cancels_parent_workflow tests/test_agent_runtime.py::test_agent_run_consecutive_terminal_approvals_update_pending_request`，以及 `.venv/bin/pytest tests/test_agent_runtime.py::test_workflow_child_consecutive_approvals_keep_parent_waiting tests/test_chat_api.py::test_workflow_child_consecutive_approvals_keep_chat_prompt_visible tests/test_chat_api.py::test_workflow_waiting_for_child_agent_approval_counts_only_child tests/test_chat_api.py::test_list_sessions_clears_workflow_child_approval_after_parent_resumes`。
   2026-06-07 追加 Workflow 子 Agent 连续审批真实 Run Detail smoke：backend 重启加载当前代码后，启动临时 OpenAI-compatible stub（仅 `127.0.0.1:8765`，结束后已关闭），创建 Agent `agent_codex_workflow_consecutive_ui_917275` 和 Workflow `workflow_codex_workflow_consecutive_ui_917275`（Start -> Consecutive Approval Child -> Artifact `smoke/workflow-consecutive-ui-917275.md`）。通过 Bridge `/ui/workflow-runs` 运行得到父 Run `workflow_run_e3b4a76a0137` / RunGroup `run_group_9842b8b6b868`，子 Run `agent_run_20615d940c47`。Browser 打开 `#/agents/workflow_run_e3b4a76a0137` 后，Run Detail 首次显示“Workflow 正在等待子 Agent 审批”、工具 `terminal.run`、BASH 命令 `printf workflow-ui-first-917275`、`批准子 Agent / 拒绝子 Agent / 打开子 Run`；第一次点击“批准子 Agent”后，父 Workflow 仍处于 `approval_required`，Run Detail 审批卡刷新为第二条命令 `printf workflow-ui-second-917275`，不再停留在旧请求。第二次批准后父 Run 和子 Run 均 completed，页面显示最终结果 `Workflow UI consecutive approvals completed: workflow-ui-first-917275 + workflow-ui-second-917275`、artifact `smoke/workflow-consecutive-ui-917275.md` 与“重新运行”入口；API 确认 artifact 内容包含两个 marker，父 timeline 有 2 条 `workflow.run.approval_required`，子 timeline 有 2 条 `agent.tool.approval_approved`，分别对应两条命令。该 smoke 覆盖 Run Detail 侧连续审批刷新；仍未覆盖 Chat 显式 `@Workflow` 输入区提醒在第一/第二次审批之间持续可见，下一条切片专门复核这一点。
   2026-06-07 追加 Chat 显式 Workflow 子 Agent 连续审批真实前端 smoke：继续使用当前 backend 和临时 OpenAI-compatible stub（仅 `127.0.0.1:8765`，结束后已关闭），创建 Agent `agent_codex_workflow_chat_ui_917276` 和 Workflow `workflow_codex_workflow_chat_ui_917276`（Start -> Chat Consecutive Approval Child -> Artifact `smoke/workflow-chat-consecutive-ui-917276.md`）。通过 Chat 新会话 `707a01a5` 显式 runnable 入口触发，父 Run `workflow_run_f12f6f7abded` / RunGroup `run_group_2910a90b6941`，子 Run `agent_run_9cab8aa54f15`。第一次等待时 API 显示 `processing_count=1`、`approval_count=1`，Workflow 气泡 metadata 中 `workflow_waiting_pending_approval.input_preview.command=printf workflow-chat-ui-first-917276`；Browser 打开 `#/chat?session_id=707a01a5` 后，输入区附近显示“待审批”、当前节点、`terminal.run`、命令 `printf workflow-chat-ui-first-917276`、定位消息、运行详情、批准和拒绝。点击输入区“批准”后，输入区提醒更新为第二条命令 `printf workflow-chat-ui-second-917276`，仍保留 1 个批准入口，不再显示旧 first 请求；第二次批准后 Chat 气泡 completed，显示 `产物 1`、最终文本 `Workflow Chat UI consecutive approvals completed: workflow-chat-ui-first-917276 + workflow-chat-ui-second-917276`，输入区待审批消失。API 复核最终 `processing_count=0`、`approval_count=0`，父 Run 和子 Run completed，artifact `smoke/workflow-chat-consecutive-ui-917276.md` 包含两个 marker，父 timeline 有 2 条 `workflow.run.approval_required`，子 timeline 有两条已批准命令。至此 Workflow 子 Agent 连续审批在 Run Detail 和 Chat 输入区两侧都有当前代码真实 smoke 证据。
   2026-06-07 追加 Runs History 子 Run 隐藏防回退：当前 Bridge `/ui/runs?limit=120` 对 `run_group_a3e9f051f7de` 只返回父 Workflow Run `workflow_run_52a90da2f2f2`，不把子 Agent Run `agent_run_20da76594689` 混入普通 Agent History；后端 `list_runs` SQL 按“同 RunGroup 存在 workflow_run”排除 child agent，不依赖 `run_group_source` 是否等于字面 `workflow`。新增回归 `test_list_runs_hides_workflow_child_agents_for_custom_workflow_sources`，覆盖 `source="workflow_child_artifact_smoke"` 这类显式 smoke / 自定义 workflow source 仍只展示父 Workflow、隐藏 child agent。已通过命令：`.venv/bin/pytest tests/test_agent_runtime.py::test_list_runs_returns_roots_and_standalone_agents_only tests/test_agent_runtime.py::test_list_runs_hides_workflow_child_agents_for_delegated_workflows tests/test_agent_runtime.py::test_list_runs_hides_workflow_child_agents_for_custom_workflow_sources`。
   2026-06-07 追加多 Agent Workflow live smoke：启动本地 OpenAI-compatible stub，只服务 `127.0.0.1`，通过当前 Bridge 创建临时 Agent `agent_codex-multi-agent-design-smoke-799115_57d4267d` / `agent_codex-multi-agent-code-smoke-799115_042e4377` 和 Workflow `workflow_codex-multi-agent-workflow-smoke-799115_fc2a9e73`（Start -> Design child -> Code child -> Final report）。首次 smoke 因 stub 按目标文本而非 Agent 名称判断，Code 节点被错误返回 `artifact.write`，父 Run 明确 failed 到 `Code child`，证明失败归因仍能定位到具体节点；修正 stub 后重新运行父 Run `workflow_run_6900b34ebc2c`，RunGroup `run_group_7854e8042e97` completed，timeline 顺序为 Start completed、Design child completed、Code child completed、Final report completed、Workflow completed；父 artifacts 同时包含 child artifact `design.md`（source Run `agent_run_893c981963c3`，内容 `multi-agent-design-artifact-marker`）和 workflow artifact `reports/multi-agent-live-smoke.md`（内容 `Code child completed with multi-agent-code-result-marker.`）。Browser 打开 `#/agents/workflow_run_6900b34ebc2c` 可见 `Workflow Steps · 4`，两个 Agent 节点均有 `Open Run`，Design 节点直接列出 `design.md`，Artifacts 区列出 `Codex Multi Agent Design Smoke 799115 / design.md` 与 `reports/multi-agent-live-smoke.md`；硬刷新后重跑按钮可用，从 Run Detail 点击“重新运行”生成新 Run `workflow_run_91cafba0cfe5`，再次 completed，child runs `agent_run_29713bd27642` / `agent_run_3d55d3ebffcb`，两个 artifact 内容通过 API 读取确认无误。临时 stub 已停止。
6. Workflow 审批运行：Workflow 自身审批和子 Agent 工具审批都要能从聊天提醒与 Run Detail 批准/拒绝，并继续或终止到正确节点。2026-06-06 追加：现有 Chat 回归已覆盖 Workflow 自身审批消息、Workflow 等待子 Agent 审批时的聊天提醒、`approval_count` 计数和批准后消息收束；Runtime 回归已覆盖 Workflow 自身审批批准/拒绝/取消、子 Agent 工具审批批准/拒绝/取消和父 Workflow 节点归因。本轮新增 Bridge route 层验收回归 `test_workflow_child_approval_route_approve_resumes_parent_workflow` 与 `test_workflow_child_approval_route_reject_cancels_parent_workflow`，锁定 Run Detail 直接批准 child approval 后父 Workflow 会继续到 Artifact 并完成，直接拒绝 child approval 后父 Workflow 会取消并记录具体失败节点；真实前端/CDP smoke 已覆盖 Workflow 自身 Approval 节点：Run `workflow_run_5b3de3cec557` 先停在 `approval_required`，Run Detail 显示审批卡，点击批准后记录 `workflow.node.approval_approved`，继续写出配置的 `smoke/ui-approval.md` 并完成。Smoke 还发现 8420 旧后端进程会把 Approval 当旧 checkpoint，已记录为验证环境风险；已在本轮末尾将 8420 重启到当前工作树代码，后续默认浏览器测试不应再踩旧进程。Run Detail 操作按钮已本地化为“批准 / 拒绝 / 取消 Run / 批准子 Agent / 拒绝子 Agent / 打开子 Run”。2026-06-06 追加真实前端/CDP smoke：用临时本地 OpenAI-compatible stub 在当前 Bridge `8420` 创建 `Codex Child Approval UI Smoke Workflow 2359`，父 Run `workflow_run_ac433991f72f` 停在 `approval_required`，子 Run `agent_run_de2041e7b3e9` 请求 `terminal.run`；父 Workflow Run Detail 首屏显示“Workflow 正在等待子 Agent 审批”，轮询后补齐 `terminal.run`、BASH 请求内容 `printf child-approval-smoke`、`批准子 Agent / 拒绝子 Agent / 打开子 Run`，从父详情点击“拒绝子 Agent”后父 Run 与子 Run 均变为 `cancelled`，Workflow Steps 与 Execution timeline 都归因到 `Child Approval Smoke Agent` 并显示 `工具审批已拒绝：Tool approval rejected`。仍未覆盖真实模型批准后继续生成并写 Artifact 的前端长轮询，只由 route/runtime 回归证明批准路径。
7. Workflow 失败复盘：停用 Agent、坏节点、工具失败、子 Agent 失败时，聊天气泡和 Run Detail 都要指向具体失败节点与原因。2026-06-06 追加：已有 Runtime 回归覆盖保存/运行前拦截缺失 Agent、停用 Agent、坏 Artifact Path、Start-only 草稿，以及审批恢复后下游 Agent 被停用时父 Workflow 失败并记录节点；已有 Chat 回归覆盖 `workflow.run.failed` 会在聊天气泡显示“失败节点”。本轮新增真实 Workflow + Chat API 回归 `test_workflow_tool_failure_writes_child_and_parent_failure_hints`，锁定子 Agent 请求未授权 `terminal.run` 失败时，child Run timeline 记录 `agent.tool.denied`，父 Workflow timeline 记录带 `workflow_node_id/kind/label/child_run_id` 的 `workflow.run.failed`，聊天区同时写出子 Agent 失败气泡和父 Workflow 失败气泡，并显示具体失败节点与原因；真实 Run Detail 前端点击 smoke 仍需继续执行。
8. Workflow 产物复盘：父 Workflow Run Detail 要能看到子 Agent 真实产物、Artifact 节点产物、最终结果和重跑入口。2026-06-06 追加：已有回归覆盖父 Workflow 汇总 child Agent artifact ref、读取 child source run artifact、Artifact 节点配置路径和去重路径。本轮新增 Bridge route 层验收回归 `test_workflow_artifact_review_route_exposes_outputs_and_reruns`，锁定父 Workflow Run 会同时暴露最终 `result`、原 `runnable_id/user_goal`、子 Agent 真实产物引用、Workflow Artifact 节点产物引用、可读取的 artifact 内容、完整节点 timeline，以及用父 Run 原目标重新创建 Workflow Run 的后端重跑路径。2026-06-06 追加真实前端/CDP smoke：使用现有 Bridge `http://127.0.0.1:8420` 与 Vite `http://127.0.0.1:5174` 创建 `Codex Smoke Workflow 2026-06-06`，后端 `/ui/runs` 返回 18 条且第一条为 `workflow_run_98fef26cb346`，硬刷新 Runs 页后前端显示 `Run History · 18`、`Workflows 1`，点击该 Workflow Run 后详情页可见 Workflow 名称、步骤/Timeline、`smoke-report.md` artifact、最终结果和“准备重跑 / 重新运行”入口。Smoke 过程中发现旧 timeline 缺 `workflow_node_id` 时，Run Detail 会把 spec step 和 observed event 重复显示成 pending + completed；已修复前端 spec/observed step 宽松合并，复测显示 `Workflow Steps · 2`，Start 与 Smoke Report 均为“已完成”，Execution timeline 保持 4 个事件。仍未覆盖真实模型驱动的 Workflow 子 Agent 审批长轮询。

收口条件：以上 8 条验收路径完成后，再判断 Phase 4 Agent / Workflow 是否可以标记为当前阶段完成；如果某条失败，优先修该条最短 blocker，不再扩散新需求。

### 2026-06-04 下一轮 Agent / Workflow 可行性讨论

1. 先定义 Agent 产物契约：Design Agent 应产出原型文件或 Markdown，Coding Agent 应产出代码文件或 patch，Review Agent 应产出 Markdown review；每类产物需要明确保存位置、artifact metadata、权限与预览方式。2026-06-04 追加：Agent Runtime 的 `Output Contract` 已从裸字段升级为运行上下文里的交付规则，`chat` / `markdown` / `report` / `diff` / `artifacts` 会明确说明何时直接内联回复、何时写 artifact、何时不得仅因 `diff` 契约就调用 `workspace.write_patch`；system prompt 也提醒不要只为了输出契约申请工具，减少群组派发后无谓审批和契约误读。
2. Workflow 完整跑完后，Runs 需要能展示每个 Agent 的产物，而不只是最后节点结果；当前 `Workflow Steps` 已能展示每个子 Agent 文本输出，下一步要把文件 artifact 和结构化产物一并纳入步骤视图。2026-06-04 追加：父 Workflow Run 现在会记录每个 child Agent 的 outcome 摘要和 artifact 引用，子 Agent 审批恢复后父 timeline 对应步骤会从 `approval_required` 更新为真实完成状态；父 Run Detail 的 Artifacts 区可以直接打开 child Agent artifact，不需要用户手动跳到子 Run 再查文件。Runs 前端已拆分 History 主列表和详情缓存：父 Workflow 详情会按需拉取 child Agent run，但不会把这些 child run 混进 History 分组、筛选和计数里，避免查询列表被 Workflow 内部步骤刷屏；后端 History 查询也会隐藏任何包含 Workflow 根 Run 的 RunGroup 内部 Agent Run，即使该 Workflow 是通过 delegation / 统一委派入口创建，也只在主列表显示 Workflow 根 Run；直接从深链或聊天打开 Workflow child Agent Run 时，会按 run_group_id 拉取 RunGroup 并补查父 Workflow Run，从而稳定显示“返回 Workflow”入口。Workflow Run timeline 现在给 Start / Agent / Approval / Artifact 节点都写入 `workflow_node_id`、状态和必要的 artifact 信息；Run Detail 的 `Workflow Steps` 已从单纯 child Agent 列表升级为全节点步骤视图，能直接看到流程开始、Agent 执行、人工审批和 artifact 写出节点；Approval 步骤会把 `pending_approval.input_preview` 转成“审批节点 / 当前上下文”的可读摘要，避免只在顶部审批卡或原始 JSON 里才能看懂它在等什么；Workflow Run 创建时会把运行时路径快照写入 `workflow.run.started.workflow_path`，步骤区优先用这个快照补齐尚未执行的下游节点并标成 `pending`，暂停、失败或 Workflow 之后被编辑时，历史 Run Detail 仍能展示运行当时的完整流程；旧 Run 没有快照时，前端只有在当前 Workflow 设计与已发生的 timeline 节点能按 kind/label 或 node id 对齐时，才用当前设计补 pending，避免把已编辑后的新节点误混进历史 Run。Workflow Run 现在还会在 `workflow.run.started.workflow_snapshot` 里保存可执行 nodes/edges；Workflow 自身审批节点恢复、以及子 Agent 工具审批恢复父 Workflow 时，都会优先按运行时快照继续，而不是按用户后来编辑过的当前画布继续，避免暂停审批期间改图导致历史 Run 跑偏。Workflow 失败/取消事件现在也会携带失败节点的 id/kind/label，Run Detail 会把 `workflow.run.failed` / `workflow.run.cancelled` 映射回对应步骤卡并标成 failed/cancelled；Timeline 也会把 `workflow.run.cancelled` / `run.cancelled` 显示为明确取消事件和危险色调，不再露出 raw event 或普通状态。停用下游 Agent、Artifact 写入失败、子 Agent 失败或子 Agent 工具审批被拒绝，都能在流程图层级定位到具体节点。Workflow Artifact 节点现在可以配置产物路径，留空时按 Label 自动生成；重复路径会去重为 `summary.md`、`summary-2.md` 或 `reports/final-report.md`、`reports/final-report-2.md` 这类稳定名称，并在 artifact metadata 中保留来源节点 id/label，避免用户自由设计多个产物节点时互相覆盖或难以区分；Run Detail 的 pending Artifact 步骤也会显示这些预计写出路径，让用户在流程尚未走到产物节点前就知道最终文件会落在哪里。
3. Chat 群组已支持主模型派发、流式派发隐藏、AgentRun 审批恢复、单 Agent 重试和主模型统一汇总；2026-06-04 已补后端与前端双层防抖，主模型派活流式 JSON 片段不再把气泡回退/重刷，`running` 状态会归一化为 processing，占位消息继续显示三点 loading；同日追加兼容 `<yachiyogroupdispatch>` / `dispatchgroupagent` / 智能引号 JSON 等模型输出变体。2026-06-04 追加：executor 层的群组派活提示已和 Chat API 层统一为“先自然语言说明安排，再附加 `<yachiyo_group_dispatch>` 机器块”，不再一边要求只输出 JSON、一边要求计划说明；完成态也已覆盖 tagged `tasks` 派活块会被聊天层隐藏并派发；2026-06-05 追加：同一条主模型回复里分段输出多个 `<yachiyo_group_dispatch>` 机器块时会全部收集并派发到同一轮 RunGroup，不会只执行第一个标签块；派发块字段名也兼容 `type/kind=agent`、`agentName/userGoal/taskGoal/objective/runnableId`、`Delegations` 等真实模型常见变体；同日继续补 `tool/input/arguments/parameters/params/payload/request` 工具调用风格 envelope，其中 arguments 是 JSON 字符串也能解析；如果真实模型流式阶段吐过派发 JSON 片段、但最终回复没有形成有效派发，完成态会清掉 `group_dispatch_pending` / stream 可见内容 metadata，并显示最终普通回复，不会残留“正在派发”状态。2026-06-04 追加：Agent Run 写回聊天消息时会把非 context artifact 的数量和路径摘要写入 metadata，聊天气泡时间栏会显示“产物 N”入口并打开对应 Run Detail，避免用户不知道某个 Agent 是否真的产出了文件；内部 `agent-context.md` 不计入产物数量，避免误导；主模型群组汇总任务的 Prompt 也会包含这些 Agent 产物路径，让最终总结能自然说明交付物在哪里。产物 metadata 已拆分真实数量和最多 8 个路径摘要，Agent 产物超过摘要上限时聊天仍显示真实 `产物 N`，主模型汇总 Prompt 会提示其余产物见 Run Detail；群组 Agent 完成或失败正文现在也会在实际有产物时直接显示“产物：N 个，见运行详情”，避免用户只扫正文时漏掉时间栏里的产物入口；失败但已有草稿/中间产物的情况也会提示可查。混合派发中若部分目标不在当前群组，聊天正文会保留“以下派活没有执行”的可见提示，主模型汇总 Prompt 也会结构化列出未执行派活，避免最终总结漏报哪些目标没有真正运行；测试已覆盖合法 Agent 继续执行、非法 Agent 明确跳过，并把跳过项交给主模型整理。若一次派发全部被跳过、没有任何 Agent 实际执行，现在也会创建主模型汇总任务，让 Yachiyo 自然说明未执行原因并给用户下一步建议，而不是只停在一条失败提示。2026-06-04 追加：群组会话和运行批次已解耦，群聊可以长期保留同一组成员，但每次主模型派发或群内直接 @Agent 都会开启新的 RunGroup；同一轮派给多个 Agent 时仍共享同一个 RunGroup，下一轮用户目标会重新开批次，避免 Runs History 把不同目标混在同一条历史里。群内直接 @Agent 的完成、失败、取消现在也会自动创建主模型整理任务，Prompt 会包含用户原始请求、Agent 状态、汇报和产物摘要；Agent 气泡文案同步改成“已交给主模型整理”，主模型总结完成后会回写清理等待标记，避免只在文字上说验收但实际没有主模型收尾；群内直接 @Agent 若先进入工具审批，现在会保持明确审批卡内容且不提前创建主模型整理任务，Run 完成或审批被拒绝/取消后才生成主模型整理任务；直接 @Agent 的 Run 创建失败和已创建后执行失败都已覆盖主模型整理回归。普通群聊消息不写 `@主模型` 的路径也已补回归：用户直接在群里发布目标时会创建主模型任务，主模型完成态如果返回派发块，同样会隐藏机器块、派发 Agent、写回 Agent 结果并创建主模型汇总任务。
4. 审批链路已补双 Agent 回归：一个 delegated Agent 进入审批、另一个先完成时，不会提前创建主模型汇总任务，完成的 Agent 消息也会保留；审批恢复完成后才统一触发主模型汇总。delegated Agent 完成消息现在会保留完整任务与结果正文，不再只显示被截断的任务预览；审批等待态在前端改为独立工具请求卡片，关联任务和请求参数分层展示，`terminal.run` 命令按 bash 代码块显示；连续审批会绕过 typewriter 防回退保护并刷新为新的 pending approval，状态栏也会提示下一次审批的工具与关键参数；批准后 Run 回到 processing 时会清掉审批卡片并展示“已批准工具调用 / Agent 正在继续执行”的进度卡，输入框上方也会浮出待审批提醒条，避免用户只盯着输入区时错过审批；2026-06-05 追加：批准动作自身不再等待完整 Agent Run 长轮询，前端会立即释放按钮 busy 并后台轮询后续执行，避免批准后看起来卡在“处理中”。普通运行中的 Agent 消息会从最新 Run timeline 提取具体阶段，显示运行环境已准备、正在解析模型响应、正在处理工具结果或已写出产物等短进度，不再只有泛泛的“Agent 正在执行”，同时会隐藏工具调用 JSON、`<yachiyo...>` 等内部协议片段。2026-06-04 追加：聊天内带 `run_id` 的 Agent 消息、审批卡和等待卡都会提供“运行详情”入口，直接打开 Agent Studio Run Detail / Timeline，用于查看 Agent 实际工具、产物和失败原因；Agent Studio Runs 已按 Agent/Workflow 分组展示 History，Run Detail 改成任务目标、结果、执行步骤和产物的层级视图，timeline 用纵向 step rail 展示模型响应、工具调用和失败点；Run Detail 顶部待审批区域也使用结构化请求视图展示工具、Run、关联任务、审批节点/路径/工作目录，`terminal.run` 内容以 bash 代码块显示，父 Workflow 等待子 Agent 审批时同样可读；History 分组可折叠，支持按完成、失败、进行中筛选，并能搜索目标、结果、Agent 名称、Run ID、timeline 和 artifact 线索，Run Detail 与 History 卡片不再省略任务和模型响应细节；群组派发时若 Agent Run 创建阶段直接失败，也会生成“执行失败，已把失败原因交给主模型整理”的 Agent 气泡，并把失败状态与报告写入主模型汇总任务；审批被拒绝导致 delegated Agent 取消时，也会生成“已取消，已把当前状态交给主模型整理”的 Agent 气泡并触发主模型汇总任务；取消态聊天气泡和 Workflow 取消汇总现在都会映射成 failed/终止状态并带 error，不再显示成 completed，避免用户误判为成功交付；主模型汇总任务完成或失败后，会清掉父派发消息上的 `group_agent_summary_pending` 并记录汇总状态；原 Agent / 派发气泡以及底部全局状态也会显示等待整理或整理失败原因，避免历史里继续显示像还在等待汇总，也避免用户必须自己对应哪条 summary 失败；输入框上方的待审批提醒现在支持多审批导航，会显示第 X/Y 个待审批并提供上一项/下一项定位，避免多个 Agent 同时申请工具时只看到最后一个；后端已补双 Agent 同时待审批回归：两个 Agent 都待审批时不创建主模型汇总，只完成其中一个仍不汇总，全部终态后才把完成与失败报告一起交给主模型；Chat 侧 Agent Run 发送、重试、审批恢复和轮询也会把后端 `running` 归一为 `processing`，避免状态名不一致导致看起来卡住；Agent Run 轮询结束或超时时会尊重刷新后的会话级 `is_processing`，如果主模型汇总已经开始，不会把聊天整体状态误改成已结束；前端 smoke 发现历史 activity 的 `event_id` 可能重复，消息内 activity row key 已附加列表位置，避免同一时间多条“群组任务已派发”等事件导致 React 重复 key 警告和潜在漏渲染；`workspace.read` 读目录这类工具形态误用现在会返回可恢复 hint 和建议工具，不再直接让 Run 失败或让用户误判为 Agent Studio 权限没挂上；如果模型仍反复请求工具直到循环超限，失败摘要会带上最后一次工具调用和建议，便于主模型整理和用户判断下一步。
5. Workflow 入口边界已按最新目标调整：Chat / 群聊里的 `@` 候选会显示主模型、Agent 和 Workflow；手动输入 `@Workflow` 会直接创建异步 Workflow Run，立刻写入 processing 气泡并在后台完成后更新同一条 Workflow 消息，主会话显式触发会切到 Workflow 会话，群聊显式触发会保持群聊上下文且不会把 Workflow 加成群成员；如果主模型误输出 Workflow 派发块，群聊仍会明确提示 Workflow 不能在群聊派发协议里直接执行，并引导到 Workflow Studio / Runs，而不是误报“未找到群组 Agent”。测试已覆盖主会话 `@Workflow` 运行、群聊 `@Workflow` 运行且保持群组、直接 `@Agent` 完成后再 `@Workflow` 仍会触发主模型整理，以及主模型群组误派 Workflow 的拦截提示。Workflow Studio 的运行入口已改为“保存并运行”：新建草稿可一键保存后运行，已保存 Workflow 会先持久化当前画布节点/连线再创建 Run，避免跑到旧版本。Workflow 节点设置已覆盖所有非 Start 节点：Agent、Approval、Artifact 都能改 Label 和移除，Agent 节点额外可选择具体 Agent，避免添加审批/产物节点后无法配置；toolbar 的 Agent 按钮现在新增“选择 Agent”的空节点，不再默认绑定第一个 Agent，只有从下方具体 Agent palette 添加时才预选；Workflow Studio 初次进入也不再默认载入第一个已有 Workflow，而是保持新建草稿画布，只有用户点击列表或保存后才选中具体 Workflow，避免误编辑已有流程。Agent Studio 初始加载和 Runs 页手动运行入口也不再默认选择第一个 Agent/Workflow，Target 需要用户明确选择后才能运行；Run Detail 空状态也不再保留任何自动选中第一条 Run 的兜底，历史查看必须由用户明确选择；Agent Quick Run、Workflow 保存并运行和 Runs 手动运行都会在停用目标、硬校验错误、目标为空、缺少可执行节点或节点 Agent 配置不可运行时禁用并提示原因，避免只填 Goal 时误触发错误目标、运行空流程或运行必然失败的 Agent；Runs 手动运行选择 Workflow 时也会复用 Workflow Studio 校验和 Agent readiness 预检，旧数据或导入数据若存在断链、缺失/停用 Agent、未知节点、不可用 Chat Profile、Custom API 配置不完整、挂载 Skill 停用等问题，会在目标预览下提前显示并禁用运行，若错误不止一条会列出完整错误清单。Workflow Studio 已补前端设计态校验：按后端线性流程规则提示缺失 Agent、停用/不存在 Agent、断链、多入口、多下一步、环、无效 edge、未知节点类型等问题，并在保存/运行前拦截明确错误；只有 Start 节点等低价值草稿状态仍作为保存 warning，但运行态已改为硬拦截。2026-06-04 追加：后端 `validate_workflow` / `create_workflow` / `update_workflow` / `create_workflow_run` / 审批恢复路径也会校验节点类型以及 Agent 节点是否已选择、存在且启用；2026-06-05 追加：后端节点类型解析已兼容 ReactFlow 原始 `input/default/output` 节点与 `data.kind` 业务类型，导入画布或未清洗节点数据也能保存并运行；`create_workflow_run` 还会要求至少一个可执行节点（Agent、Approval 或 Artifact），Start-only Workflow 可保存但不能运行，并会在创建 Run 前预检节点 Agent 的 Chat Profile / Custom API / Skill 可运行性；保存后 Agent 被停用会在运行前明确拒绝，未知节点类型会在保存/运行前拒绝，审批等待期间后续 Agent 被停用会把父 Workflow Run 和 RunGroup 标成 failed，不再继续执行或卡在不透明状态。Workflow 的 Approval 节点已从普通 checkpoint 升级为真实人工审批节点：运行到该节点时父 Workflow Run 会进入 `approval_required`，Runs 详情可直接 Approve / Reject，批准后从后续节点继续，拒绝后取消 Workflow；拒绝事件会记录节点 id/kind/label/status、清空 pending approval，并把拒绝原因同步到 Run 和 RunGroup 摘要，Run Detail 后续能明确定位是哪一个审批节点被拒绝；用户直接取消停在 Workflow 自身审批节点的父 Run 时，也会写入带审批节点 id/kind/label/status 的 `workflow.run.cancelled`，清空 pending approval，并把源为 Workflow 的 RunGroup 一起标成 cancelled，避免 History 和步骤视图还像仍在等待审批；用户取消正在等待子 Agent 工具审批的父 Workflow 时，后端会同步取消对应 child Agent Run、清空 child pending approval，并把 child outcome 合并回父 Workflow timeline，避免留下孤儿审批。Runs 详情页已补 Workflow 父子审批桥接：当父 Workflow 因子 Agent 工具审批暂停时，会在父 Run Detail 顶部明确展示等待审批的 Child Run、工具名、参数预览，并允许直接 Approve / Reject Child 后留在父 Workflow 详情页观察后续步骤；从 Workflow 子 Agent Run 详情钻取时也会显示“返回父 Workflow”入口，避免只看到单个 Agent 运行而丢失整体流程上下文；从子 Agent Run 详情直接批准/拒绝后，前端会立即写回该 child Run 的详情缓存，不会因为 child run 不在 History 主列表里而继续停在旧的待审批状态；从父 Workflow 详情直接批准/拒绝 Child 时，也会立即拉取并缓存父 Workflow Run，让审批卡、步骤和最终状态尽快同步，不必等下一轮轮询才知道 Workflow 是否继续推进；Run Detail 审批 action 完成后也会按返回状态提示“继续执行 / 需要下一次审批 / 已完成 / 已失败”，不再只显示泛泛的 action 完成。Run Detail 现在会对选中的 processing / approval_required Run 及其 Workflow child Run 做轻量轮询，并在详情 meta 显示“实时更新”，用户停在运行详情页时可以看到步骤、状态和子 Run 自动推进，不需要手动刷新；同时 Run Detail 顶部会对进行中或待审批 Run 提供带确认弹窗的 `Cancel Run`，取消后立即刷新详情缓存，避免用户只能硬等一个卡住的 Agent / Workflow，也避免误触裸取消；完成/失败/取消后的 Run Detail 还提供“准备重跑”和“重新运行”，前者把原目标和任务填回 Runs 面板，后者复用当前 Agent Studio 的权限、模型和 Workflow 校验直接创建新 Run。Runs 页面现在也会把后端 `running` 统一显示和筛选为 `processing`，确保“进行中”筛选能覆盖正在执行的 Agent / Workflow，不再露出 raw 状态名。
6. 2026-06-04 追加：已用 in-app Browser 对当前前端做一轮 smoke，覆盖 Chat 群组历史页、Runs 页、Workflow Studio 新草稿默认态、已有 Workflow 切换和保存运行入口；页面均无 console error，群组输入框、运行详情入口、Runs 分组/筛选、Workflow 新草稿“不默认选中第一个”、已有 Workflow 节点设置都能正常显示。Agent Studio hash deep link 也补了 query 兼容，`#/agents?tab=runs` 这类外部/手写链接会进入 Runs，不再掉回主控台；应用自身生成的 `#/agents/runs`、`#/agents/<runId>` 仍保持不变。2026-06-05 追加：Chat 输入区在会话 processing 时仍可继续编辑并用按钮或 Enter 发送新消息，停止当前任务拆成独立按钮，Agent Run 创建后的轮询改为后台进行；当前会话状态、停止按钮和会话列表都会暴露并显示并发处理数量（例如“处理中 2 项”），计数口径合并主模型 Task 与 active Agent/Workflow Run；停止当前任务现在也会实际取消当前会话里 active Agent / Workflow Run，并刷新对应消息到取消终态，避免只停主模型 Task、子 Run 继续跑；会话列表和 session info 也会同步当前会话 Task / Agent / Workflow Run 终态，避免不打开消息流时列表状态先变、气泡仍旧 processing；待审批数量也会通过 `approval_count` 暴露到会话列表、消息 payload 和 session info，列表预览优先显示“待审批 / 待审批 N”，审批卡和输入框上方待审批提醒也会兼容 Agent 与 Workflow 两套 run metadata，Workflow 自身审批消息会携带审批节点和当前上下文；如果 Workflow 是因为子 Agent 工具审批而暂停，父 Workflow 只显示等待进度、不再制造第二个待审批入口，避免后台 Agent/Workflow 请求确认时被“处理中”淹没或只显示泛泛的工具确认，回归已覆盖群聊 Agent 工具审批、Workflow 自身人工审批、Workflow 等待子 Agent 审批，以及批准子 Agent 后父子 Chat 消息终态和计数归零；群组派发 Agent 与直接 `@Agent` 的异步完成回调都会写回原群聊，用户切走会话后仍能在原群聊得到 Agent 结果和主模型汇总任务；群组 Agent 执行期间用户追加普通群聊/`@主模型` 补充时，主模型整理 Prompt 也会带上后续补充/纠偏，补充消息会在保存时标记所属父任务 / Agent 消息，多个群组任务同时 active 时只归到最近一个 active 或已终态但尚未汇总的目标；接收新的群聊消息前会先静默同步已完成的派发与 Run 状态，并把创建主模型汇总任务延后到用户消息落库之后，修复第二批任务刚派出/刚完成但 UI 尚未轮询刷新时，补充消息误归到旧任务或没有进入第二批汇总的时序问题；直接 `@Agent` runnable command 入口也纳入同一套群聊静默同步和待汇总扫描，用户连续点名多个 Agent 时，前一个已完成结果会及时交给主模型整理且不会被新的 `@Agent` 指令污染；点名群外 Agent、手动 @Workflow 运行或其他 runnable error/guidance 分支也会在错误/运行消息落库后扫描待汇总 Agent，避免用户误操作后旧结果卡住；明显新目标提示、独立 `@Agent` 指令、以及没有补充/修正语气的 `@主模型` 新请求不会污染当前汇总，避免重叠目标互相串台。后端多任务回归覆盖重叠消息全部完成后的配对排序、processing/approval 计数、状态收束、Run 取消、会话列表状态同步、切会话后的群聊派发/直接 @Agent 回写、执行期间用户补充进入汇总，以及 Workflow 失败/取消终态在 Chat 中显示失败节点。本轮补充 Browser smoke 覆盖 Runs 不默认选择 run、Run Detail 层级内容、Workflow Studio 新草稿与 palette、群组设置头像入口和无 URL 栏、会话 ID 只读值与关闭流程；下一轮重点用真实 Chat Profile 压测长输出、派发 JSON 流式片段、失败/取消、连续审批和中途补充约束，确认当前单元测试和 Browser smoke 覆盖的闪烁/等待态/审批同步在真实模型下也稳定。
7. Yachiyo 自动派发任务已接入第一层 Chat 可观察性：自动委派的 Agent / Workflow Run 会把 `run_id/run_status/pending_approval` 写入 activity metadata，待审批不会再误显示为委派失败，Chat 活动行在有 Run ID 时可打开 Run Detail，输入框上方的统一待审批队列也会合并 activity 来源审批并本地抑制刚处理过的 item；activity 来源审批后的 delegated Run 终态会创建主模型整理任务，把结果交回当前对话。下一步要用真实 Chat Profile 压测自动委派后的连续审批、子 Run 卡住、审批后继续委派，以及是否需要后端级 approval event 签名来区分同一 Run 的多次审批。
8. 2026-06-05 追加：Run Detail 已支持区块折叠和长 payload 完整展开，Workflow Studio 运行区和 Runs 手动运行入口都会展示运行顺序预览；后续复盘重点从“能不能看到细节/点下去会跑什么”转向真实模型压测：确认长输出、连续工具审批、失败节点、child Agent artifact、Workflow 重新运行入口和默认模板步骤在真实运行里都能顺畅定位。
9. 保留 Runs 清理按钮为后续 UX/数据管理任务：需要先决定软删除、撤销窗口、RunGroup 与子 Run 的删除关系，以及 artifact 是否一起清理。
10. 默认 `Phase 4 Agent 全线流通测试` 仍需要单独做 Prompt/Skill 质量调校；真实模型曾暴露长上下文超时、工具误请求和输出约束不足，后续应配合产物契约一起收紧。

### Phase 4 模型 / Agent / TTS 后续

1. 继续收紧默认 Agent 工具策略和 Prompt 质量：2026-06-05 已加第一层运行时护栏，用户明确“不创建/不保存/只展示代码”时会拦截误触发的 `workspace.write_patch` / `artifact.write`，明确“不运行/不执行”时会拦截 `terminal.run`，并把可恢复 tool result 喂回模型要求 inline 交付；中文“代码完整展示即可”和 Workflow 子 Agent “不需要运行命令或脚本”已补回归；后续仍需要用真实模型继续调校低风险 brief，减少 Coding / Research / Design 自发请求工具和循环。
2. Agent Runtime 后续增强：补更完整的运行中取消 UI、复杂 artifact viewer、真实模型长输出稳定性，以及 Runs 页面按 Agent 履历聚合。
3. 手工验证主 Agent 自动委派体验：用真实 Chat Profile 创建启用 Agent / Workflow，检查主会话是否能触发 `run_yachiyo_agent` / `run_yachiyo_workflow`、Run 留档、结果回填和 3 次上限。2026-06-05 已补自动委派解析容错：普通主会话可识别 `<yachiyo_delegation>`、智能引号、`type/kind`、`agentName/userGoal/objective/runnableId` 等模型输出变体，并会扫描长回复中的多个 JSON 对象寻找有效委派；群组协调任务仍禁用这条路径，避免和群组派发协议混用。
4. 用真实 Chat Profile 继续压测 Chat Agent / Workflow 群组体验：覆盖 `@` mention 菜单、Agent 私聊续聊、手动群组主模型派发、审批通过后的主模型汇总、Workflow 群组内 `@Agent` 插手和 `@主模型` 干预路径。
5. 为 Skill Library 补后续管理能力：批量启停、导入/同步历史、打开 Hermes roots、安装命令历史、文件夹重命名/排序，以及后续 marketplace/全局 skills 浏览边界。
6. 2026-06-05 已补第一版 Agent Studio 能力状态展示：缺 Chat Profile / 默认 Chat Profile 不可用 / Custom API 配置不完整、挂载 Skill 已停用、高风险 `workspace.write_patch` / `terminal.run` 会审批、写入 scope 为空等会在编辑页、Quick Run、Runs 单 Agent 入口和 Workflow 节点预览里前置提示；后端也会在创建 Agent Run / Workflow Run 前拦截 Custom API 缺字段、停用 Skill、停用目标、Workflow 节点 follow_main Agent 无可用默认 Chat Profile 等确定本地配置错误，后续再继续细化真实模型/Skill 质量状态。
7. 把 `apps/shell/provider_catalog_sync.py` 接入每日或低频后台更新机制，更新 `~/.hermes/yachiyo/provider-capabilities.json`；刷新失败不能阻塞应用启动。
8. 继续扩展 provider adapter：为 Xiaomi MiMo、OpenRouter、Gemini、DashScope、DeepSeek、MiniMax 等常用源沉淀 `/models` 路径、鉴权 header、模型 ID 规范、chat payload、vision payload 和错误归因。
9. 手工验证真实 provider：至少覆盖 Xiaomi MiMo `mimo-v2.5` / `mimo-v2-omni`、OpenRouter 视觉模型、Gemini、DashScope 和 DeepSeek；确认文本模型不能误保存为 vision Profile。
10. 完成 TTS 真实测试语义：HTTP TTS / Command TTS / API TTS 需要能配置 endpoint、voice、timeout、test text 并进行可证实测试；GPT-SoVITS 完整参数继续放在“主动关怀与桌面观察”页。
11. 更新用户手册和截图索引中的旧命名：将“主动关怀语音”逐步统一为“主动关怀与桌面观察”，同时说明 GPT-SoVITS 是该页内的本地 TTS 服务模块。

1. 推送后验证 `Build macOS DMG`：`develop-latest` / `main-latest` 的 latest JSON 应包含 `changelog.generated_from=git`、`sections`、`commits` 和 `compare_url`；GitHub versioned release notes 与 rolling latest release notes 都应展示同一份“更新日志”。
2. 用下一版 develop DMG 验证应用更新页：检查更新后应显示“更新内容”，下载时有进度，下载完成后按钮切换为“安装并重启”；退出页面或重启后仍能识别已下载但未安装的更高版本 DMG。
3. 验证更新安装后的首次重开体验：Bridge/backend 尚未就绪时安装向导应显示“正在启动本地 Bridge”并自动重试，而不是直接红色报错；Bridge 可用后应自动恢复主控台或安装状态。
4. 验证自签名免费分发链路：latest DMG 可挂载；首次打开 `.app` 仍应是未知开发者/Gatekeeper 允许打开流程，而不是“移到废纸篓”的 DMG 挂载前拒绝。
5. 验证 Hermes Agent 脏安装修复：删掉损坏的 `~/.local/bin/hermes` 或保留旧 `~/.hermes/hermes-agent` 时，Yachiyo 应给出可修复状态；修复后图片链路测试不应再报 Hermes Python 环境缺失或 `env: ... Permission denied`。

1. 基于本轮新增的 `docs/user-manual.md`、`docs/screenshot-index.md`、`docs/experience-report-2026-05-05.md` 和 `docs/public/images/hermes-yachiyo/first-run/` 创建 VitePress 文档站点结构，并按安装、模型配置、桌面表现态、资源导入、工具中心、维护排障拆页。
2. VitePress 接入后检查所有 `/images/hermes-yachiyo/first-run/*.png` 引用是否能在站点中正确加载，并确认截图中 API Key 仍保持遮蔽。
3. 发布说明中补充本轮首用真实发现：GitHub 克隆中断时的手动安装 fallback、GPT-SoVITS 首次加载可能较慢、导入资源会显著增加备份体积、工具中心外部 Key 缺失属于预期受限状态。
4. 下一轮 DMG 复测时重点确认：主动关怀语音页能展示最近一次自动 TTS 错误；工具中心 `tts` 卡片能跳到主动关怀语音；备份页大资源提示可见；1x1/极小图片附件会被前端拦截。

1. 用最新 develop DMG 再做一次清机首装复测：安装 Hermes 后应滚动到模型配置区；不填 API Key 初始化会提示风险；初始化完成后进入主控台并默认打开 Bubble；再次点击 Dock 图标不能回到安装向导，也不能让 Bubble 消失。
2. 手工复测主动关怀截图链路：授权屏幕录制后触发主动桌面观察，应能把桌面截图作为会话附件传入图片识别；点击附件只在应用内查看，不应自动弹出 Chrome/默认浏览器预览。
3. 手工复测模型配置为 `auto + OpenRouter Base URL` 的图片输入：只有 `AUTO_API_KEY` 旧配置时也应被识别为 OpenRouter key 可用；主动关怀/手动发图不应再误报 API Key 无效；若模型确实不支持图片，应给出真实能力说明。
4. 手工复测 Live2D 资源 gate 与导入：清空 `~/.hermes/yachiyo/assets/live2d` 与 `live2d_mode.model_path` 后选择 Live2D，应跳到配置页且不启动 Live2D；导入中文/日文文件名 ZIP 后状态栏不应乱码，保存后真模型应能加载或至少静态预览可点击。
5. 手工复测主动关怀 TTS 本地服务链路：导入八千代 GPT-SoVITS 语音包后确认权重/参考音频路径和默认启动命令自动填充；测试“部署本地服务”“打开服务终端”“安装开机自启”“刷新状态”“保存并测试”，并确认 TTS 开启时主动消息不会早于音频附件出现。
6. 推送后确认 GitHub Actions `Build macOS DMG`：release tag/DMG 名称应带自动发布版本号；应用 release 不应再包含八千代 GPT-SoVITS 语音 ZIP；重跑同一个 workflow run 时应覆盖同名 DMG asset，不再因 `ReleaseAsset.name already exists` 失败。
7. 如需更新八千代 GPT-SoVITS 语音资源，手动触发 `Publish TTS Voice Assets` workflow，并提供已经调配好的 ZIP URL；确认它更新独立的 `tts-assets-yachiyo-gpt-sovits-v4` release。
8. 手工复测卸载：分别验证仅卸载资料、卸载前备份、包含 Hermes Agent、以及“同时删除当前应用本体”四条路径；删除 `.app` 是 macOS best-effort，失败时应提示用户手动移除 Applications 中的应用。
9. 后续打包前补一次真实 macOS 权限验收：主动关怀开启时是否能触发系统屏幕录制权限提示；未授权时是否回退关闭并显示原因；授权后主动桌面观察截图应真实附加到会话，而不是只生成文本 fallback。

10. 手工验证 Tool Center 修复：运行 `hermes doctor` 后确认 Doctor 已确认可用的工具不再显示“待检测”；确认 `browser` 与 `browser-cdp` 分开显示，CDP 受限不再误伤基础浏览器自动化。
11. 手工验证工具配置入口：在 Tool Center 分别打开 `web`、`image_gen`、`browser-cdp`、Home Assistant、MoA、RL 等当前 `hermes tools list` 暴露的配置，确认 env 字段只显示变量名和已配置状态，不显示密钥明文；保存后可点击“保存并测试 / 测试配置”查看静态配置检查与 Doctor 对应状态。
12. 联网与网页读取的真实启用仍需要用户提供 Firecrawl / Exa / Parallel / Tavily / Nous Gateway 之一；配置后应分别验证 `hermes doctor` 状态、`web` 工具调用和网页读取结果。
13. 图片生成的真实启用仍需要当前 Hermes 已知 provider 的密钥或 image_gen plugin；配置页先只列 Hermes 已暴露/已安装的 provider，后续若 Hermes 新增 provider，再由 tools/plugin manifest 驱动 UI 扩展。
14. 手工验证 Hermes 更新入口：点击“检查更新”确认能显示当前版本/落后 commits；确认默认 `--no-backup` 更新不会停在 stash 恢复确认，勾选完整备份时能清楚提示耗时风险，完成后自动刷新 tools list、Doctor 缓存和 Tool Center provider 列表。
15. 手工验证 Browser CDP 修补：点击“启动/连接本机 Chrome”后确认能写入 `browser.cdp_url=http://127.0.0.1:9222`；若自动启动失败，复制页面返回的手动命令执行后再次运行 Doctor。
16. 继续对照旧 pywebview Chat Window 做 React 体验补全：当前已恢复单例窗口、轮询流式/typewriter/Markdown/复制/会话切换、外链打开策略、基础快捷键和处理中取消入口；后续补更完整的错误边界和消息操作细节。
17. TODO：制定旧 pywebview shell 退休清单。等 Electron 前端稳定覆盖聊天、主控台、设置、Bubble、Live2D、安装引导、备份/卸载和打包启动链路后，直接删除所有 pywebview UI 代码与 legacy 入口，只保留 Electron + Bridge 路径。
18. 手工验证 AstrBot `/y ask` / `/y chat`：allow-list 拒绝、状态/截图/窗口摘要、自然语言低风险任务创建。
19. 调研 Hermes 原生 memory API / CLI / 存储边界，决定 `HermesMemoryAdapter` 第一版能力。
20. 继续推进 AstrBot 宿主绑定：在 AstrBot 插件框架中注册 `/y` 命令监听并调用 `on_y_command()`。
21. 继续对接 Hapi `/codex` 真实端点，保持 Codex CLI 执行不进入 Hermes-Yachiyo。
22. 完善任务持久化与安全策略模块（packages/tasking / packages/security），并补跨平台本地能力适配。
