"""Custom API Agent model/tool loop."""

from __future__ import annotations

import hashlib
import json
import posixpath
import re
from collections.abc import Iterable, Mapping, Sequence
from copy import deepcopy
from dataclasses import replace
from typing import Any, Callable

from apps.shell.agent.runtime.app_aliases import APP_ALIASES, compact_app_alias
from apps.shell.agent.runtime.abstract_capability_planning import (
    abstract_capability_action_catalog,
)
from apps.shell.agent.runtime.approval_tool_sets import (
    APPROVAL_PLAN_TOOLS as _DAILY_DESKTOP_APPROVAL_PLAN_TOOLS,
)
from apps.shell.agent.runtime.desktop_recovery_metadata import (
    daily_desktop_metadata_tool_request,
)
from apps.shell.agent.runtime.dispatch_semantics import is_semantic_safe_shortcut
from apps.shell.agent.runtime.desktop_tool_labels import (
    DAILY_DESKTOP_TOOL_LABELS as _DAILY_DESKTOP_TOOL_LABELS,
)
from apps.shell.agent.runtime.errors import (
    AgentApprovalRequired,
    AgentDirectOutcomeUnverified,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_event_payload as _runtime_task_progress_event_payload,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_planner_base_event_type as _runtime_planner_base_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_planner_event_payload as _runtime_planner_event_payload,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_planner_event_type as _runtime_planner_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_planner_timeline_event as _runtime_planner_timeline_event,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_progress_base_event_type as _runtime_progress_base_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_progress_event_payload as _runtime_progress_event_payload,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_progress_event_type as _runtime_progress_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_replan_base_event_type as _runtime_replan_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_replan_request_event_payload as _runtime_replan_request_event_payload,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_replan_request_event_type as _runtime_replan_request_event_type,
)
from apps.shell.agent.runtime.event_scopes import (
    runtime_scope_context as _runtime_planner_scope_context,
)
from apps.shell.agent.runtime.followup_content_snapshot import (
    followup_content_snapshot_for_tool_call,
    followup_content_snapshots,
    latest_followup_content_snapshot,
)
from apps.shell.agent.runtime.goal_contract import GoalContract, GoalCoordinator
from apps.shell.agent.runtime.goal_runtime import (
    _runtime_execution_provider_identity,
    _runtime_owned_terminal_event,
    complete_response_only_goal,
    goal_assessment_event_payload,
    goal_contract_context_message,
    goal_contract_event_payload,
    goal_replan_context_message,
    pending_semantic_artifact_assessment_candidates,
    planned_goal_contract_payload,
    runtime_goal_assessment,
    runtime_goal_contract,
)
from apps.shell.agent.runtime.semantic_artifact_verification import (
    SemanticArtifactVerificationResult,
    verify_semantic_artifact,
)
from apps.shell.agent.runtime.model_messages import messages_require_model_first
from apps.shell.agent.runtime.model_intent_planning import (
    ModelIntentClarificationResolution,
    ModelIntentPlanningError,
    direct_tool_selection_from_user_clarification,
    model_intent_planning_tool_schema,
    model_intent_proposal_from_tool_requests,
    model_intent_resolution_from_proposal,
    planner_selection_needs_model_assistance,
)
from apps.shell.agent.runtime.outcome_evaluator import (
    evaluate_goal_contract_outcome,
    evaluate_main_chat_outcome,
)
from apps.shell.agent.runtime.outcome_loop import (
    OutcomeLoopCoordinator,
    OutcomeLoopDisposition,
    OutcomeLoopResult,
)
from apps.shell.yachiyo_agent.runtime_planner import clarification_authority_for_goal
from apps.shell.agent.runtime.recovery_actions import (
    RecoveryActionContext,
    RecoveryActionDisposition,
    RecoveryActionExecutionMode,
    RecoveryActionRegistry,
    RecoveryActionResult,
    RecoveryActionScope,
    RecoveryModelTurn,
    RecoveryToolBatch,
    RecoveryToolResult,
)
from apps.shell.agent.runtime.recovery_adapters.background_window import (
    BackgroundWindowRecoveryAdapter,
)
from apps.shell.agent.runtime.recovery_lineage import (
    RUNTIME_PRIVATE_REPLAN_CONTEXT_KEY,
    RUNTIME_PRIVATE_RECOVERY_AUTHORITY,
    RUNTIME_PRIVATE_RECOVERY_CONTEXT_KEY,
    RUNTIME_PRIVATE_RECOVERY_CONTEXT_VERSION,
    RuntimeAuthorizedReplanPayload,
    canonical_replan_action_signature,
    copy_live_replan_payload,
    mint_private_replan_context,
    trusted_private_replan_action_signatures,
)
from apps.shell.agent.runtime.recovery_identity import (
    ensure_recovery_action_identity,
    recovery_action_identities_from_timeline,
    recovery_request_repeats_stalled_discovery,
)
from apps.shell.agent.runtime.recovery_ownership import (
    RecoveryOwnershipStatus,
    resolve_legacy_replan_ownership,
)
from apps.shell.agent.runtime.recovery_policies import (
    RecoveryAssessment,
    assess_latest_tool_recovery,
    background_window_runtime_recovery_tools,
    recovery_attempt_lineage_from_timeline,
)
from apps.shell.agent.runtime.replan_deferred import (
    materialized_deferred_items,
    safe_deferred_continuation_request,
)
from apps.shell.agent.runtime.tool_execution import (
    _approval_dependency_block_result,
    _broker_tool_precondition_failure,
    _discovered_app_name_for_query,
    _discovered_app_resolution_evidence,
    _post_action_verification_request,
)
from apps.shell.agent.runtime.tool_capabilities import capability_ids_for_tool
from apps.shell.agent.runtime.tool_loop import stage_tool_result_messages
from apps.shell.agent.runtime.tool_requests import ensure_tool_call_id
from apps.shell.agent.runtime.tool_outcomes import OutcomeStatus, from_tool_result
from apps.shell.agent.runtime.verification_receipts import (
    EXACT_FILE_CONTENT_PRESENT_PREDICATE,
    RUNTIME_SEMANTIC_ARTIFACT_VERIFIER_AUTHORITY,
    SEMANTIC_ARTIFACT_ADEQUACY_PREDICATE,
    SEMANTIC_ARTIFACT_ASSESSED_EVENT,
    declared_workspace_output_path,
    normalized_workspace_relative_path,
)
from apps.shell.agent.tools.policy import (
    DAILY_BROWSER_TOOL_NAMES,
    DAILY_DESKTOP_TOOL_NAMES,
)
from apps.shell.yachiyo_agent.app_name_hints import compact_app_name_hint
from apps.shell.yachiyo_agent.capability_registry import capability_snapshots
from apps.shell.yachiyo_agent.desktop_plan_hints import (
    click_target_hint,
    type_into_ui_hint,
)
from apps.shell.yachiyo_agent.desktop_execution_policy import (
    runtime_execution_envelope_with_desktop_execution_policy,
)
from apps.shell.yachiyo_agent.entrypoint_tool_selection import (
    DirectToolSelection,
    planner_first_direct_tool_selection,
)
from apps.shell.yachiyo_agent.planner_execution import (
    planner_execution_tool_requests,
    planner_tool_requests,
    runtime_execution_verified_tool_requests,
)
from apps.shell.yachiyo_agent.policy import READ_ONLY_OBSERVATION_TOOLS
from apps.shell.yachiyo_agent.planner_projection import (
    planner_selection_payload,
    runtime_planner_decision,
)
from apps.shell.yachiyo_agent.policy import desktop_tool_execution_mode_for_input
from apps.shell.yachiyo_agent.runtime_doctrine import YACHIYO_RUNTIME_OPERATING_MANUAL
from apps.shell.yachiyo_agent.runtime_execution import (
    runtime_execution_blocked_requests_from_envelope_payload,
    runtime_execution_envelope_payload,
    runtime_execution_requests_from_envelope_payload,
    runtime_execution_requests_from_metadata,
)
from packages.security import redact_sensitive_text

_DIRECT_DAILY_DESKTOP_TOOLS = {
    *DAILY_DESKTOP_TOOL_NAMES,
    "artifact.write",
    "data.analyze",
    "terminal.run",
}

_DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS = {
    "file.read",
    "file.search",
    "fs.find_files",
    "fs.read_file",
    "workspace.list",
    "workspace.read",
}

_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS = {
    "app.focus",
    "app.open",
    "browser.current_page",
    "browser.screenshot",
    "desktop.active_window",
    "desktop.verify",
    "desktop.focus_app",
    "desktop.list_apps",
    "desktop.open_app",
    "desktop.open_path",
    "desktop.read_ui",
    "desktop.running_apps",
    "desktop.ui_elements",
    "desktop.windows",
    "file.read",
    "fs.read_file",
    "screen.capture",
    "workspace.read",
}

_RUNTIME_GOAL_BOUNDED_BACKGROUND_WINDOW_TOOLS = (
    "desktop.safe_shortcut",
    "desktop.read_ui",
)

_RUNTIME_REPLAN_INTERNAL_PERMISSION_TARGETS = {
    "app_launch",
    "runtime_observation",
}

_RUNTIME_REPLAN_AUTO_SAFE_CAPABILITY_TARGETS = {
    "app_control",
    "app_discovery",
    "app_inspection",
    "browser_capture",
    "browser_read",
    "ui_inspection",
    "window_inspection",
    "workspace_read",
}

_RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE = (
    "runtime_recovery_coordinator"
)

_RUNTIME_REPLAN_INGESTION_SOURCES = frozenset(
    {
        "runtime_tool_request_runner",
        _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE,
    }
)

_RUNTIME_PLANNER_AUTHORITY_CONFLICT_CODES = frozenset(
    {
        "goal_contract_conflict",
        "goal_contract_conflict: original_goal",
        "runtime_execution_action_target_conflict",
        "runtime_execution_action_target_step_conflict",
        "runtime_execution_goal_action_target_conflict",
        "runtime_execution_input_bindings_conflict",
    }
)


class _RuntimePlannerFailure(RuntimeError):
    """Private fail-closed signal; never expose the originating exception."""

    def __init__(self, reason: str) -> None:
        super().__init__(reason)
        self.reason = str(reason or "planner_internal_failure").strip()


def _model_intent_plan_may_replace_execution_envelope(
    envelope: Mapping[str, Any] | None,
    deterministic_selection: DirectToolSelection,
) -> bool:
    """Only supersede the exact first-pass planner envelope for this goal."""

    if not isinstance(envelope, Mapping):
        return True
    requests = envelope.get("requests")
    if not isinstance(requests, list) or not requests:
        return True
    expected_decision_id = str(
        getattr(deterministic_selection.decision, "decision_id", "") or ""
    ).strip()
    envelope_decision_id = str(envelope.get("decision_id") or "").strip()
    return bool(
        expected_decision_id
        and envelope_decision_id
        and envelope_decision_id == expected_decision_id
    )


def _initial_model_intent_planning_messages(
    original_goal: str,
    deterministic_selection: DirectToolSelection,
    allowed_tools: Iterable[str],
    *,
    clarification_authority: tuple[str, str] | None = None,
) -> list[dict[str, str]]:
    decision = deterministic_selection.decision
    raw_candidates = getattr(decision, "candidate_intents", ()) or ()
    candidates = [
        {
            "kind": str(getattr(candidate, "kind", "") or "").strip(),
            "confidence": round(float(getattr(candidate, "confidence", 0) or 0), 3),
            "missing_inputs": list(getattr(candidate, "missing_inputs", ()) or ()),
        }
        for candidate in raw_candidates
        if str(getattr(candidate, "kind", "") or "").strip()
        and float(getattr(candidate, "confidence", 0) or 0) > 0
    ]
    selected = getattr(decision, "selected_intent", None)
    selected_kind = str(getattr(selected, "kind", "") or "").strip()
    capability_ids = sorted(
        {
            capability_id
            for tool_name in allowed_tools
            for capability_id in capability_ids_for_tool(str(tool_name or "").strip())
        }
    )
    planning_context = {
        "original_goal": str(original_goal or "").strip(),
        "deterministic_selected_intent": selected_kind,
        "deterministic_candidates": candidates,
        "available_capabilities": capability_ids,
        "available_abstract_actions": abstract_capability_action_catalog(
            allowed_tools
        ),
    }
    if clarification_authority is not None:
        planning_context["clarification_continuation"] = {
            "original_goal": clarification_authority[0],
            "user_reply": clarification_authority[1],
        }
    return [
        {
            "role": "system",
            "content": (
                "You are the intent-disambiguation stage of a desktop agent. "
                "Do not execute tools and do not provide chain-of-thought. Call exactly "
                "runtime_propose_task_intent. Choose the smallest intent category that "
                "preserves the user's requested effect. planning_goal must be a concise, "
                "explicit paraphrase of the original goal; preserve every target, value, "
                "time expression, constraint, and requested output, and invent nothing. "
                "action_evidence must be one short exact quote from the user-authored "
                "goal that explicitly requests the action; never paraphrase it. "
                "When the deterministic plan is unresolved or the goal contains "
                "multiple requested effects, propose ordered subgoals using only "
                "the exact capability_id, action_id, and semantic input slots in "
                "available_abstract_actions. Preserve every requested effect; do "
                "not silently drop a clause. Each subgoal action_evidence and each "
                "input-slot evidence_quote must be an exact user-authored quote. "
                "If a material input is genuinely missing, include one short "
                "clarification_question. Never propose tool names, IDs, approvals, risk, "
                "separate action-target fields, or completion evidence; Runtime owns "
                "all of them. A target explicitly written by the user must still be "
                "preserved verbatim inside planning_goal."
                " When clarification_continuation is present, resolve the previous "
                "goal with the current user_reply. Treat both fields as user-authored, "
                "keep the previous action unchanged, and use the reply only to fill "
                "the missing object, scope, value, time, or expected result."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                planning_context,
                ensure_ascii=False,
                separators=(",", ":"),
            ),
        },
    ]


def _legacy_approval_gate_requests(
    execution_tool_requests: list[dict[str, Any]],
    first_approval_index: int,
) -> tuple[list[dict[str, Any]], dict[str, Any], list[dict[str, Any]]]:
    """Give a legacy approval prefix temporary identities for Runner-equivalent gating."""

    used_step_ids = {
        str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        for request in execution_tool_requests
        if isinstance(request, Mapping)
        and str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    }
    prefix_requests: list[dict[str, Any]] = []
    dependency_step_ids: list[str] = []
    assigned_step_ids: set[str] = set()
    for index, request in enumerate(execution_tool_requests[:first_approval_index]):
        request_copy = dict(request)
        if _clipboard_transfer_shortcut_request(request_copy):
            # A copy/paste keystroke proves dispatch only.  The compatibility
            # approval prefix must not turn that acknowledgement into a
            # successful dependency for a later submit action.
            request_copy["requires_post_action_verification"] = True
        step_id = str(
            request_copy.get("step_id") or request_copy.get("planner_step_id") or ""
        ).strip()
        if not step_id or step_id in assigned_step_ids:
            base_step_id = f"legacy-approval-prefix-{index + 1}"
            step_id = base_step_id
            suffix = 2
            while step_id in used_step_ids or step_id in assigned_step_ids:
                step_id = f"{base_step_id}-{suffix}"
                suffix += 1
            request_copy["step_id"] = step_id
        assigned_step_ids.add(step_id)
        used_step_ids.add(step_id)
        dependency_step_ids.append(step_id)
        prefix_requests.append(request_copy)

    approval_request = dict(execution_tool_requests[first_approval_index])
    gate_approval_request = {
        **approval_request,
        "depends_on": dependency_step_ids,
    }
    return (
        prefix_requests,
        approval_request,
        [*prefix_requests, gate_approval_request],
    )


def _clipboard_transfer_shortcut_request(request: Mapping[str, Any]) -> bool:
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    if tool_name not in {
        "desktop.safe_shortcut",
        "desktop.shortcut",
        "app.open_and_safe_shortcut",
        "app.focus_and_safe_shortcut",
    }:
        return False
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    return str(payload.get("action") or "").strip().lower() in {
        "copy",
        "paste",
    }


def _legacy_approval_gate_timeline(
    timeline: list[dict[str, Any]],
    prefix_requests: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    """Correlate trace-poor legacy adapter events without treating absence as success."""

    gate_timeline = [dict(event) for event in timeline]
    cursor = max(0, tool_timeline_start)
    for request in prefix_requests:
        step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        if not step_id:
            continue
        request_decision_id = str(request.get("decision_id") or "").strip()
        correlated_index = -1
        for index in range(cursor, len(gate_timeline)):
            event = gate_timeline[index]
            payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
            event_type = str(event.get("event") or event.get("event_type") or "").strip()
            if not event_type.endswith(
                ("agent.tool.call", "agent.tool.skipped", "agent.tool.failed")
            ):
                continue
            event_step_id = str(
                event.get("step_id")
                or payload.get("step_id")
                or event.get("planner_step_id")
                or payload.get("planner_step_id")
                or ""
            ).strip()
            if event_step_id == step_id:
                correlated_index = index
                break
        if correlated_index >= 0:
            correlated_event = gate_timeline[correlated_index]
            correlated_payload = (
                correlated_event.get("payload")
                if isinstance(correlated_event.get("payload"), Mapping)
                else {}
            )
            correlated_decision_id = str(
                correlated_event.get("decision_id")
                or correlated_payload.get("decision_id")
                or ""
            ).strip()
            if request_decision_id and not correlated_decision_id:
                gate_timeline[correlated_index] = {
                    **correlated_event,
                    "decision_id": request_decision_id,
                }
            cursor = correlated_index + 1
            continue

        expected_tool = str(request.get("tool") or request.get("tool_name") or "").strip()
        for index in range(cursor, len(gate_timeline)):
            event = gate_timeline[index]
            payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
            event_type = str(event.get("event") or event.get("event_type") or "").strip()
            if not event_type.endswith(
                ("agent.tool.call", "agent.tool.skipped", "agent.tool.failed")
            ):
                continue
            event_tool = str(
                event.get("tool")
                or payload.get("tool")
                or event.get("detail")
                or ""
            ).strip()
            if expected_tool and event_tool != expected_tool:
                continue
            correlated_scope = (
                {"decision_id": request_decision_id}
                if request_decision_id
                else {}
            )
            gate_timeline[index] = {
                **event,
                "step_id": step_id,
                **correlated_scope,
            }
            cursor = index + 1
            break
    return gate_timeline


_RUNTIME_REPLAN_NO_REPEAT_AFTER_SUCCESS_TOOLS = {
    "app.open",
    "desktop.open_app",
}

_STRICT_CANONICAL_APP_IDENTITIES = {
    **{
        compact_app_alias(alias): compact_app_alias(app_name)
        for alias, app_name in APP_ALIASES.items()
    },
    **{
        compact_app_alias(app_name): compact_app_alias(app_name)
        for app_name in APP_ALIASES.values()
    },
}

_DAILY_DESKTOP_DISCOVERY_TOOLS = {
    "desktop.list_apps",
    "desktop.running_apps",
    "desktop.windows",
    "desktop.permissions",
}

_DAILY_DESKTOP_DISCOVERY_PREFIX_TOOLS = {
    "desktop.list_apps",
    "desktop.running_apps",
    "desktop.permissions",
}

_DISCOVERED_APP_SELECTION_SOURCES = {
    "desktop.list_apps",
    "desktop.running_apps",
}

_DISCOVERED_APP_PLACEHOLDERS = {
    "desktop.list_apps": "<selected app from desktop.list_apps>",
    "desktop.running_apps": "<selected app from desktop.running_apps>",
}

_DAILY_DESKTOP_VERIFY_TOOLS = {
    "desktop.active_window",
    "desktop.verify",
    "desktop.list_windows",
    "desktop.running_apps",
    "desktop.read_ui",
    "screen.capture",
    "desktop.windows",
    "desktop.ui_elements",
}

_DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS = {
    "media.apple_music_control",
    "media.apple_music_open_and_play",
    "media.apple_music_play",
    "media.music_app_control",
    "media.music_app_open_and_play",
    "media.system_control",
}

_DAILY_DESKTOP_PLAN_SOURCES = {
    "daily_desktop_intent",
    "runtime_planner",
}

_APP_FOREGROUND_ACTION_TOOLS = {
    "app.open_and_safe_type_text",
    "app.focus_and_safe_type_text",
    "app.open_and_safe_shortcut",
    "app.focus_and_safe_shortcut",
    "app.open_and_safe_key",
    "app.focus_and_safe_key",
    "app.open_and_hotkey",
    "app.focus_and_hotkey",
    "app.open_and_safe_scroll",
    "app.focus_and_safe_scroll",
    "app.open_and_safe_click",
    "app.focus_and_safe_click",
    "app.open_and_click_ui_element",
    "app.focus_and_click_ui_element",
    "app.open_and_type_into_ui_element",
    "app.focus_and_type_into_ui_element",
}


def _desktop_execution_mode_payload(
    tool_name: str,
    input_preview: Mapping[str, Any] | None = None,
) -> dict[str, Any]:
    snapshot = desktop_tool_execution_mode_for_input(tool_name, input_preview)
    payload = snapshot.model_dump(mode="json")
    return {
        "execution_mode": str(payload.get("mode") or ""),
        "execution_isolation": str(payload.get("isolation") or ""),
        "foreground_control": bool(payload.get("foreground_control")),
        "keyboard_mouse_capture": bool(payload.get("keyboard_mouse_capture")),
        "sandbox_recommended": bool(payload.get("sandbox_recommended")),
        "user_handoff_recommended": bool(payload.get("user_handoff_recommended")),
        "approval_recommended": bool(payload.get("approval_recommended")),
        "desktop_execution_mode": payload,
    }


def _desktop_execution_policy_from_context(
    agent: Mapping[str, Any],
    runtime_execution_metadata: Mapping[str, Any] | None,
) -> dict[str, Any]:
    for container in (
        runtime_execution_metadata if isinstance(runtime_execution_metadata, Mapping) else {},
        agent.get("tool_policy") if isinstance(agent.get("tool_policy"), Mapping) else {},
        agent,
    ):
        policy = _desktop_execution_policy_from_mapping(container)
        if policy:
            return policy
    return {}


def _desktop_execution_policy_from_mapping(payload: Mapping[str, Any]) -> dict[str, Any]:
    for key in (
        "desktop_execution_policy",
        "yachiyo_desktop_execution_policy",
        "desktop_interaction_policy",
    ):
        raw = payload.get(key)
        if isinstance(raw, Mapping):
            return dict(raw)
        if isinstance(raw, str) and raw.strip():
            return {"mode": raw.strip()}
    return {}


def _tool_requests_with_desktop_execution_policy(
    tool_requests: Iterable[dict[str, Any]] | None,
    policy: Mapping[str, Any],
) -> list[dict[str, Any]]:
    requests = [
        dict(request)
        for request in (tool_requests or [])
        if isinstance(request, dict)
    ]
    if not policy:
        return requests
    return [
        {**request, "desktop_execution_policy": dict(policy)}
        for request in requests
    ]


def _tool_request_with_desktop_execution_policy(
    tool_request: dict[str, Any] | None,
    policy: Mapping[str, Any],
) -> dict[str, Any] | None:
    if not isinstance(tool_request, dict):
        return None
    if not policy:
        return dict(tool_request)
    return {**tool_request, "desktop_execution_policy": dict(policy)}


_OPEN_PATH_WITH_APP_TOOLS = {"desktop.open_path_with_app", "app.open_path_with_app"}

_DISCOVERED_APP_DIRECT_COMPLETION_TOOLS = {
    "app.open",
    "desktop.open_app",
    "app.focus",
    "desktop.focus_app",
    *_OPEN_PATH_WITH_APP_TOOLS,
}

_DISCOVERED_APP_DIRECT_VERIFICATION_TOOLS = {
    *_DAILY_DESKTOP_VERIFY_TOOLS,
    "screen.capture",
}


def _completed_legacy_recovery_batch(
    timeline: Sequence[Mapping[str, Any]],
    *,
    request_id: str,
    action_id: str,
) -> RecoveryToolBatch | None:
    """Recover one already-executed, source-correlated legacy action result."""

    expected_request_id = str(request_id or "").strip()
    expected_action_id = str(action_id or "").strip()
    if not expected_request_id:
        return None
    candidates: list[tuple[dict[str, Any], RecoveryToolResult]] = []
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        event_type = str(event.get("event") or event.get("event_type") or "").strip()
        base_event_type = _runtime_progress_base_event_type(event_type)
        if base_event_type not in {
            "agent.tool.call",
            "agent.tool.failed",
            "agent.tool.skipped",
        }:
            continue
        payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
        event_request_id = str(
            event.get("replan_request_id") or payload.get("replan_request_id") or ""
        ).strip()
        if event_request_id != expected_request_id:
            continue
        event_action_id = str(
            event.get("replan_recovery_action_id")
            or payload.get("replan_recovery_action_id")
            or ""
        ).strip()
        if expected_action_id and event_action_id != expected_action_id:
            continue
        tool_name = str(
            event.get("detail")
            or event.get("tool")
            or payload.get("tool")
            or ""
        ).strip()
        tool_call_id = str(
            event.get("tool_call_id") or payload.get("tool_call_id") or ""
        ).strip()
        if not tool_name or not tool_call_id:
            continue
        raw_input = event.get("input_preview")
        if not isinstance(raw_input, Mapping):
            raw_input = payload.get("input_preview")
        result = event.get("result")
        if result is None:
            result = payload.get("result")
        request = {
            "tool": tool_name,
            "tool_call_id": tool_call_id,
            "input": dict(raw_input) if isinstance(raw_input, Mapping) else {},
        }
        candidates.append(
            (
                request,
                RecoveryToolResult(
                    tool_call_id=tool_call_id,
                    result=result,
                    event_type=base_event_type,
                ),
            )
        )
    if len(candidates) != 1:
        return None
    request, result = candidates[0]
    return RecoveryToolBatch(requests=(request,), results=(result,))


def _is_exact_background_window_batch(
    requests: Sequence[Mapping[str, Any]],
) -> bool:
    """Admit no arbitrary tool selection through the private execution grant."""

    if tuple(
        str(request.get("tool") or "").strip() for request in requests
    ) != _RUNTIME_GOAL_BOUNDED_BACKGROUND_WINDOW_TOOLS:
        return False
    shortcut_input = requests[0].get("input")
    observation_input = requests[1].get("input")
    return bool(
        isinstance(shortcut_input, Mapping)
        and dict(shortcut_input) == {"action": "new_document"}
        and isinstance(observation_input, Mapping)
        and not observation_input
    )


class _CustomApiRecoveryRuntimePort:
    """Keep private loop state behind five high-depth recovery operations."""

    def __init__(
        self,
        *,
        owner: RuntimeCustomApiAgentLoop,
        model_config: Mapping[str, Any] | Callable[[], Mapping[str, Any]],
        allowed_tools: list[str],
        broker: Any,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        budget: Any,
        run_id: str,
        source_tool_call_id: str = "",
        source_tool_name: str = "",
        recovery_action: str = "",
        recovery_scope_id: str = "",
        recovery_goal_identity: Mapping[str, Any] | None = None,
        observation_only: bool = False,
        goal_bounded_effectful_tools: Iterable[str] = (),
    ) -> None:
        self._owner = owner
        self._model_config = model_config
        self._resolved_model_config: Mapping[str, Any] | None = None
        self._root_allowed_tools = frozenset(
            str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()
        )
        self._broker = broker
        self._messages = messages
        self._timeline = timeline
        self._artifacts = artifacts
        self._budget = budget
        self._run_id = run_id
        self._source_tool_call_id = str(source_tool_call_id or "").strip()
        self._source_tool_name = str(source_tool_name or "").strip()
        self._recovery_action = str(recovery_action or "").strip()
        self._recovery_scope_id = str(recovery_scope_id or "").strip()
        self._observation_only = bool(observation_only)
        private_tools = tuple(
            dict.fromkeys(
                tool
                for value in goal_bounded_effectful_tools
                if (tool := str(value or "").strip())
            )
        )
        if private_tools and private_tools != (
            _RUNTIME_GOAL_BOUNDED_BACKGROUND_WINDOW_TOOLS
        ):
            raise ValueError(
                "Goal-bounded recovery requires the exact background-window tool pair"
            )
        if private_tools and self._observation_only:
            raise ValueError(
                "Goal-bounded effectful recovery cannot use observation-only authority"
            )
        self._goal_bounded_effectful_tools = private_tools
        raw_goal_identity = (
            recovery_goal_identity
            if isinstance(recovery_goal_identity, Mapping)
            else {}
        )
        goal_identity = {
            key: str(raw_goal_identity.get(key) or "").strip()
            for key in (
                "goal_contract_id",
                "goal_criterion_id",
                "goal_subgoal_id",
            )
        }
        self._recovery_goal_identity = (
            {**goal_identity, "root_goal_unchanged": True}
            if all(goal_identity.values())
            and raw_goal_identity.get("root_goal_unchanged") is True
            else {}
        )
        root_identity = {
            key: str(raw_goal_identity.get(key) or "").strip()
            for key in ("root_plan_id", "root_source_step_id")
        }
        self._recovery_root_identity = (
            root_identity
            if self._recovery_goal_identity and all(root_identity.values())
            else {}
        )
        self._resources_released = False

    def _narrowed_allowlist(self, allowed_tools: Iterable[str]) -> list[str]:
        narrowed = list(
            dict.fromkeys(
                tool
                for value in allowed_tools
                if (tool := str(value or "").strip())
            )
        )
        if not set(narrowed).issubset(self._root_allowed_tools):
            raise ValueError("Recovery action attempted to expand the runtime allowlist")
        return narrowed

    def _narrowed_execution_allowlist(
        self,
        allowed_tools: Iterable[str],
    ) -> tuple[list[str], bool]:
        narrowed = list(
            dict.fromkeys(
                tool
                for value in allowed_tools
                if (tool := str(value or "").strip())
            )
        )
        if set(narrowed).issubset(self._root_allowed_tools):
            return narrowed, False
        if tuple(narrowed) != self._goal_bounded_effectful_tools:
            raise ValueError("Recovery action attempted to expand the runtime allowlist")
        return narrowed, True

    def execute_tools(
        self,
        tool_requests: Iterable[Mapping[str, Any]],
        *,
        allowed_tools: Iterable[str],
        next_iteration: int,
    ) -> RecoveryToolBatch:
        narrowed, uses_goal_bounded_effectful_grant = (
            self._narrowed_execution_allowlist(allowed_tools)
        )
        requests = tuple(
            self._request_with_recovery_identity(request)
            for request in tool_requests
        )
        if uses_goal_bounded_effectful_grant and not _is_exact_background_window_batch(
            requests
        ):
            raise ValueError(
                "Goal-bounded recovery may execute only the exact background-window batch"
            )
        request_tools_by_call_id: dict[str, str] = {}
        for request in requests:
            tool_name = str(request.get("tool") or "").strip()
            tool_call_id = str(request.get("tool_call_id") or "").strip()
            if not tool_name or tool_name not in narrowed:
                raise ValueError(
                    "Recovery action requested a tool outside its narrowed allowlist"
                )
            if not tool_call_id:
                raise ValueError("Recovery action tool_call_id is required")
            if (
                self._observation_only
                and tool_name not in _RUNTIME_REPLAN_GROUNDING_OBSERVATION_TOOLS
            ):
                raise ValueError(
                    "Goal-loop recovery may execute only trusted observation tools"
                )
            if tool_call_id in request_tools_by_call_id:
                raise ValueError("Recovery action tool_call_id values must be unique")
            request_tools_by_call_id[tool_call_id] = tool_name
        timeline_start = len(self._timeline)
        self._owner._run_tool_requests(
            list(requests),
            narrowed,
            self._broker,
            self._messages,
            self._timeline,
            self._artifacts,
            next_iteration=int(next_iteration),
            run_id=self._run_id,
            budget=self._budget,
        )
        results: list[RecoveryToolResult] = []
        for request in requests:
            tool_call_id = str(request.get("tool_call_id") or "").strip()
            expected_tool = request_tools_by_call_id[tool_call_id]
            raw_result: Any = None
            terminal_event_type = ""
            for event in reversed(self._timeline[timeline_start:]):
                event_type = str(
                    event.get("event") or event.get("event_type") or ""
                ).strip()
                if event_type not in {
                    "agent.tool.call",
                    "agent.tool.failed",
                    "agent.tool.skipped",
                }:
                    continue
                event_payload = (
                    event.get("payload")
                    if isinstance(event.get("payload"), Mapping)
                    else event
                )
                if (
                    str(event_payload.get("tool_call_id") or "").strip()
                    != tool_call_id
                ):
                    continue
                event_tool = str(
                    event_payload.get("tool")
                    or event_payload.get("detail")
                    or event.get("detail")
                    or ""
                ).strip()
                if event_tool != expected_tool:
                    continue
                raw_result = event_payload.get("result")
                terminal_event_type = event_type
                break
            results.append(
                RecoveryToolResult(
                    tool_call_id=tool_call_id,
                    result=raw_result,
                    event_type=terminal_event_type,
                )
            )
        return RecoveryToolBatch(
            requests=requests,
            results=tuple(results),
            completion_token=timeline_start,
        )

    def _request_with_recovery_identity(
        self,
        request: Mapping[str, Any],
    ) -> dict[str, Any]:
        item = dict(request)
        item["source"] = "runtime_internal_recovery"
        if not self._source_tool_call_id:
            return item
        item["source_tool_call_id"] = self._source_tool_call_id
        item["recovery_link_kind"] = "coordinator_action"
        if self._source_tool_name:
            item["recovery_source_tool"] = self._source_tool_name
        if self._recovery_action:
            item["recovery_action"] = self._recovery_action
        if self._recovery_scope_id:
            item["recovery_scope_id"] = self._recovery_scope_id
            item["replan_recovery_identity"] = self._recovery_scope_id
        item.update(self._recovery_goal_identity)
        request_tool = str(item.get("tool") or "").strip()
        tool_call_id = str(item.get("tool_call_id") or "").strip()
        if request_tool and tool_call_id:
            # Recovery adapters and recovery-model output are suggestions, not
            # Runtime identity authority.  Mint the request generation here so
            # the executor-owned terminal fact can be admitted by GoalRuntime
            # without trusting a caller-supplied request_id.
            digest = hashlib.sha256(
                (
                    f"{self._run_id}\0{self._recovery_scope_id}\0"
                    f"{tool_call_id}\0{request_tool}"
                ).encode("utf-8")
            ).hexdigest()[:24]
            item["request_id"] = f"runtime-recovery-request-{digest}"
            if self._observation_only:
                item["step_id"] = f"runtime-recovery-observation-step-{digest}"
                item["planner_step_id"] = item["step_id"]
                item["observation_only"] = True
                item["goal_completion_authority"] = False
                for key in (
                    "action_target",
                    "postcondition_verified",
                    "verification_passed",
                    "verified",
                    "state",
                    "target",
                ):
                    item.pop(key, None)
        if self._recovery_root_identity:
            item["plan_id"] = self._recovery_root_identity["root_plan_id"]
            item["source_step_id"] = self._recovery_root_identity[
                "root_source_step_id"
            ]
            # The retry is a Runtime-owned attempt of the immutable root
            # criterion.  Give its terminal fact an executor step identity as
            # well as a source link; otherwise GoalRuntime must correctly
            # quarantine it as an unscoped effectful event.
            if not self._observation_only:
                item["step_id"] = self._recovery_root_identity[
                    "root_source_step_id"
                ]
                item["planner_step_id"] = self._recovery_root_identity[
                    "root_source_step_id"
                ]
        if (
            self._recovery_goal_identity
            and request_tool
            and request_tool == self._source_tool_name
            and tool_call_id
            and self._recovery_scope_id
            and self._recovery_root_identity
        ):
            item["recovery_suggested_tool"] = request_tool
            item[RUNTIME_PRIVATE_RECOVERY_CONTEXT_KEY] = {
                "version": RUNTIME_PRIVATE_RECOVERY_CONTEXT_VERSION,
                "_authority": RUNTIME_PRIVATE_RECOVERY_AUTHORITY,
                "run_id": self._run_id,
                "return_to_root": True,
                "source_tool_call_id": self._source_tool_call_id,
                "recovery_source_tool": self._source_tool_name,
                "recovery_action": self._recovery_action,
                "recovery_scope_id": self._recovery_scope_id,
                "tool_call_id": tool_call_id,
                **self._recovery_goal_identity,
                "plan_id": self._recovery_root_identity["root_plan_id"],
                "source_step_id": self._recovery_root_identity[
                    "root_source_step_id"
                ],
                "recovery_suggested_tool": request_tool,
            }
        return item

    def select_tool(
        self,
        *,
        system_prompt: str,
        user_prompt: str,
        allowed_tools: Iterable[str],
    ) -> RecoveryModelTurn:
        narrowed = self._narrowed_allowlist(allowed_tools)
        if self._resolved_model_config is None:
            resolved_config = (
                self._model_config()
                if callable(self._model_config)
                else self._model_config
            )
            if not isinstance(resolved_config, Mapping):
                raise TypeError("Recovery model configuration must be a mapping")
            self._resolved_model_config = resolved_config
        model_config = self._resolved_model_config
        recovery_messages = [
            {"role": "system", "content": str(system_prompt)},
            {"role": "user", "content": str(user_prompt)},
        ]
        self._owner._check_context_budget(self._budget, recovery_messages)
        self._budget.claim_model_call()
        message = self._owner._coalesce_model_message(
            self._owner._call_model(
                str(model_config.get("base_url") or "").rstrip("/"),
                str(model_config.get("model") or "").strip(),
                str(model_config.get("api_key") or "").strip(),
                recovery_messages,
                tools=self._owner._tool_schemas(narrowed),
                stream=True,
            )
        )
        visible_content = self._owner._message_visible_content_text(message)
        requests = self._owner._tool_requests_from_message(message, visible_content)
        return RecoveryModelTurn(
            message=message,
            visible_content=visible_content,
            tool_requests=tuple(dict(request) for request in requests),
        )

    def commit_model_turn(
        self,
        *,
        user_prompt: str,
        turn: RecoveryModelTurn,
    ) -> None:
        self._messages.append({"role": "user", "content": str(user_prompt)})
        if turn.tool_requests and turn.tool_requests[0].get("protocol") == "tool_calls":
            self._messages.append(
                self._owner._tool_loop_projection.assistant_message_for_history(
                    dict(turn.message)
                )
            )
            stage_tool_result_messages(self._messages)
            return
        self._messages.append(
            {"role": "assistant", "content": turn.visible_content}
        )

    def project_completion(self, batch: RecoveryToolBatch) -> str:
        if not isinstance(batch.completion_token, int):
            return ""
        return self._owner._direct_daily_desktop_sequence_result(
            [dict(request) for request in batch.requests],
            self._timeline,
            tool_timeline_start=batch.completion_token,
            run_id=self._run_id,
        )

    def release_owned_resources(self) -> None:
        if self._resources_released:
            return
        self._resources_released = True
        close_target = getattr(self._broker, "close_owned_browser_target", None)
        if not callable(close_target):
            return
        try:
            close_target()
        except Exception:
            return


class RuntimeCustomApiAgentLoop:
    """Runs the model/tool loop for native-profile and custom API Agents."""

    def __init__(
        self,
        *,
        agent_model_config_private: Callable[[dict[str, Any]], dict[str, Any]],
        compile_agent_runtime: Callable[[dict[str, Any]], dict[str, Any]],
        run_budget: Callable[[str, list[dict[str, Any]]], Any],
        check_context_budget: Callable[[Any, list[dict[str, Any]]], None],
        tool_schemas: Callable[[list[str]], list[dict[str, Any]]],
        normalize_tool_iteration: Callable[[Any], int],
        max_tool_iterations: int,
        operating_doctrine: str,
        memory_tool_names: set[str] | frozenset[str] | tuple[str, ...],
        future_task_tool_names: set[str] | frozenset[str] | tuple[str, ...],
        call_model: Callable[..., Any],
        coalesce_model_message: Callable[[Any], dict[str, Any]],
        message_visible_content_text: Callable[[dict[str, Any]], str],
        model_message_metadata: Callable[[dict[str, Any]], dict[str, Any]],
        tool_requests_from_message: Callable[[dict[str, Any], str], list[dict[str, Any]]],
        timeline_factory: Callable[..., dict[str, Any]],
        limit_model_output: Callable[[Any], tuple[str, bool]],
        model_output_text_factory: Callable[..., str],
        tool_loop_projection: Any,
        run_tool_requests: Callable[..., None],
        error_type: type[Exception],
        append_run_event: Callable[[str, str, dict[str, Any]], Any] | None = None,
        recovery_action_registry: RecoveryActionRegistry | None = None,
    ) -> None:
        self._agent_model_config_private = agent_model_config_private
        self._compile_agent_runtime = compile_agent_runtime
        self._run_budget = run_budget
        self._check_context_budget = check_context_budget
        self._tool_schemas = tool_schemas
        self._normalize_tool_iteration = normalize_tool_iteration
        self._max_tool_iterations = max_tool_iterations
        self._operating_doctrine = operating_doctrine
        self._memory_tool_names = set(memory_tool_names)
        self._future_task_tool_names = set(future_task_tool_names)
        self._call_model = call_model
        self._coalesce_model_message = coalesce_model_message
        self._message_visible_content_text = message_visible_content_text
        self._model_message_metadata = model_message_metadata
        self._tool_requests_from_message = tool_requests_from_message
        self._timeline = timeline_factory
        self._limit_model_output = limit_model_output
        self._model_output_text_factory = model_output_text_factory
        self._tool_loop_projection = tool_loop_projection
        self._run_tool_requests = run_tool_requests
        self._error_type = error_type
        self._append_run_event = append_run_event
        self._recovery_action_registry = (
            recovery_action_registry
            if recovery_action_registry is not None
            else RecoveryActionRegistry()
        )

    def resolve_initial_model_plan(
        self,
        *,
        agent: dict[str, Any],
        original_goal: str,
        allowed_tools: list[str],
        runtime_execution_metadata: Mapping[str, Any] | None = None,
        runtime_execution_envelope: Mapping[str, Any] | None = None,
        run_id: str = "",
        timeline: list[dict[str, Any]],
        budget: Any,
    ) -> DirectToolSelection | ModelIntentClarificationResolution | None:
        """Resolve only ambiguous first-turn intent; execution stays Runtime-owned.

        The model may normalize the user's intent category, but it cannot name
        an executable adapter, author approval/risk metadata, mint plan IDs, or
        define completion evidence.  The deterministic RuntimePlanner validates
        the proposal and creates every authoritative snapshot afterwards.
        """

        immutable_goal = str(original_goal or "").strip()
        if not immutable_goal or not allowed_tools:
            return None
        planner_metadata = _runtime_planner_execution_metadata(
            runtime_execution_metadata,
        )
        deterministic_selection = planner_first_direct_tool_selection(
            immutable_goal,
            allowed_tools,
            metadata=planner_metadata,
        )
        clarification_authority = clarification_authority_for_goal(
            planner_metadata,
            immutable_goal,
        )
        needs_model_assistance = planner_selection_needs_model_assistance(
            deterministic_selection,
            immutable_goal,
        )
        if clarification_authority is None and not needs_model_assistance:
            return None
        if not _model_intent_plan_may_replace_execution_envelope(
            runtime_execution_envelope,
            deterministic_selection,
        ):
            raise ValueError("model_intent_plan_envelope_conflict")

        if clarification_authority is not None:
            clarification_selection = (
                direct_tool_selection_from_user_clarification(
                    immutable_goal,
                    allowed_tools,
                    metadata=planner_metadata,
                )
            )
            if clarification_selection is not None:
                timeline.append(
                    self._timeline(
                        "agent.plan.clarification_resumed",
                        str(
                            getattr(
                                clarification_selection.decision.selected_intent,
                                "kind",
                                "",
                            )
                            or ""
                        ),
                        decision_id=str(
                            getattr(
                                clarification_selection.decision,
                                "decision_id",
                                "",
                            )
                            or ""
                        ),
                        visibility="internal",
                        source="runtime_validated_user_clarification",
                    )
                )
                return clarification_selection

        model_config = self._agent_model_config_private(agent)
        base_url = str(model_config.get("base_url") or "").rstrip("/")
        model = str(model_config.get("model") or "").strip()
        api_key = str(model_config.get("api_key") or "").strip()
        if not base_url or not model or not api_key:
            if clarification_authority is not None:
                timeline.append(
                    self._timeline(
                        "agent.plan.clarification_resume_unresolved",
                        "complete_goal_required",
                        visibility="internal",
                        source="runtime_validated_user_clarification",
                    )
                )
                return ModelIntentClarificationResolution(
                    original_goal=immutable_goal,
                    question="请在一条消息里完整说明动作和对象，我会按新目标继续。",
                )
            raise self._error_type(
                "native_agent_not_ready:chat_model_profile_required"
            )

        planning_messages = _initial_model_intent_planning_messages(
            immutable_goal,
            deterministic_selection,
            allowed_tools,
            clarification_authority=clarification_authority,
        )
        self._check_context_budget(budget, planning_messages)
        budget.claim_model_call()
        timeline.append(
            self._timeline(
                "agent.plan.model_assistance_requested",
                "intent_disambiguation",
                visibility="internal",
                source="runtime_model_intent_planner",
            )
        )
        message = self._coalesce_model_message(
            self._call_model(
                base_url,
                model,
                api_key,
                planning_messages,
                tools=[model_intent_planning_tool_schema()],
                stream=True,
            )
        )
        content = self._message_visible_content_text(message)
        proposals = self._tool_requests_from_message(message, content)
        proposal = None
        validation_error: ModelIntentPlanningError | None = None
        try:
            proposal = model_intent_proposal_from_tool_requests(proposals)
        except ModelIntentPlanningError as exc:
            validation_error = exc
        if validation_error is None:
            if proposal is None:
                raise self._error_type(
                    "Agent 无法形成安全的候选计划，请补充目标、对象或期望结果。"
                )
            try:
                resolution = model_intent_resolution_from_proposal(
                    proposal,
                    original_goal=immutable_goal,
                    allowed_tools=allowed_tools,
                    metadata=planner_metadata,
                )
            except ModelIntentPlanningError as exc:
                validation_error = exc
        validation_reason = ""
        if validation_error is not None:
            candidate_reason = str(validation_error).partition(":")[0].strip()
            validation_reason = (
                candidate_reason
                if re.fullmatch(r"[a-z][a-z0-9_]{0,127}", candidate_reason)
                else "model_intent_proposal_rejected"
            )
            # Any model proposal that Runtime cannot safely promote is a
            # request for user authority, not an execution failure.  Keep the
            # question generic so rejected model content never leaks back as
            # trusted task detail.
            resolution = ModelIntentClarificationResolution(
                original_goal=immutable_goal,
                question="请补充要操作的具体对象、范围、时间或期望结果。",
            )
        if isinstance(resolution, ModelIntentClarificationResolution):
            event_payload = {
                "intent_kind": proposal.intent_kind if proposal is not None else "",
                "resolution": "awaiting_user",
                "visibility": "internal",
                "source": "runtime_model_intent_planner",
            }
            if validation_reason:
                event_payload["validation_reason"] = validation_reason
            timeline.append(
                self._timeline(
                    "agent.plan.model_assistance_resolved",
                    "clarification_required",
                    **event_payload,
                )
            )
            return resolution

        selection = resolution
        event_payload = {
            **dict(selection.event_payload),
            "selected_reason": "runtime_model_assisted_intent",
            "model_assisted_from_decision_id": str(
                getattr(deterministic_selection.decision, "decision_id", "") or ""
            ).strip(),
            "visibility": "internal",
        }
        selection = replace(selection, event_payload=event_payload)
        timeline.append(
            self._timeline(
                "agent.plan.model_assistance_resolved",
                str(getattr(selection.decision.selected_intent, "kind", "") or ""),
                decision_id=str(
                    getattr(selection.decision, "decision_id", "") or ""
                ),
                visibility="internal",
                source="runtime_model_intent_planner",
            )
        )
        return selection

    def run(
        self,
        agent: dict[str, Any],
        context: str,
        broker: Any,
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        *,
        messages: list[dict[str, Any]] | None = None,
        direct_tool_request: dict[str, Any] | None = None,
        direct_tool_requests: list[dict[str, Any]] | None = None,
        runtime_execution_envelope: dict[str, Any] | None = None,
        runtime_execution_metadata: Mapping[str, Any] | None = None,
        daily_desktop_planning_context: str | None = None,
        original_goal: str | None = None,
        start_iteration: int = 0,
        run_id: str = "",
        budget: Any | None = None,
        resume_after_approved_tool: bool = False,
    ) -> str:
        runtime = self._compile_agent_runtime(agent)
        allowed_tools = runtime["tool_policy"].get("allowed_tools") or []
        desktop_execution_policy = _desktop_execution_policy_from_context(
            agent,
            runtime_execution_metadata,
        )
        if (
            isinstance(runtime_execution_envelope, Mapping)
            and desktop_execution_policy
        ):
            runtime_execution_envelope = (
                runtime_execution_envelope_with_desktop_execution_policy(
                    runtime_execution_envelope,
                    desktop_execution_policy,
                )
            )
        runtime_execution_envelope_owned = _runtime_execution_envelope_has_requests(
            runtime_execution_envelope
        )
        if runtime_execution_envelope_owned:
            # A caller-supplied envelope owns both execution and blocking.
            # Conflicting legacy direct fields must never outrank it.
            direct_tool_request = None
        else:
            direct_tool_request = _tool_request_with_desktop_execution_policy(
                direct_tool_request,
                desktop_execution_policy,
            )
        blocked_runtime_execution_requests = (
            runtime_execution_blocked_requests_from_envelope_payload(
                runtime_execution_envelope,
                allowed_tools=allowed_tools,
            )
            if runtime_execution_envelope_owned
            else []
        )
        if runtime_execution_envelope_owned:
            direct_tool_requests = _runtime_execution_context_tool_requests(
                runtime_execution_envelope,
                runtime_execution_metadata,
                allowed_tools,
            )
        elif direct_tool_requests is None:
            direct_tool_requests = _runtime_execution_context_tool_requests(
                runtime_execution_envelope,
                runtime_execution_metadata,
                allowed_tools,
            )
        direct_tool_requests = _tool_requests_with_desktop_execution_policy(
            direct_tool_requests,
            desktop_execution_policy,
        )
        runtime_execution_selection_payload = _runtime_execution_context_selection_payload(
            runtime_execution_envelope,
            runtime_execution_metadata,
            direct_tool_requests
            or ([direct_tool_request] if isinstance(direct_tool_request, dict) else None),
        )
        default_messages = messages is None
        normalized_start_iteration = self._normalize_tool_iteration(start_iteration)
        goal_contract = None
        if original_goal is None:
            # Restore before any planner or prompt construction. Continuations
            # must not invoke a new planner merely to rebuild system guidance.
            goal_contract = runtime_goal_contract(
                run_id=run_id,
                original_goal=None,
                runtime_execution_envelope=(
                    runtime_execution_envelope
                    if isinstance(runtime_execution_envelope, Mapping)
                    else None
                ),
                runtime_execution_metadata=runtime_execution_metadata,
                messages=(messages if messages is not None else []),
                timeline=timeline,
            )
        immutable_original_goal = (
            goal_contract.original_goal
            if goal_contract is not None
            else (
                str(original_goal).strip()
                if original_goal is not None
                else (
                    self._original_user_intent_text(messages or [])
                    or str(context or "").strip()
                )
            )
        )
        fresh_runtime_planner_selection: DirectToolSelection | None = None
        fresh_goal_contract_template: dict[str, Any] | None = None
        if (
            goal_contract is None
            and immutable_original_goal
            and not runtime_execution_envelope_owned
            and not resume_after_approved_tool
            and normalized_start_iteration == 0
            and allowed_tools
        ):
            planner_metadata = _runtime_planner_execution_metadata(
                runtime_execution_metadata,
            )
            try:
                fresh_runtime_planner_selection = planner_first_direct_tool_selection(
                    immutable_original_goal,
                    allowed_tools,
                    metadata=planner_metadata,
                )
            except Exception as exc:
                reason = (
                    "planner_authority_conflict"
                    if str(exc or "").strip()
                    in _RUNTIME_PLANNER_AUTHORITY_CONFLICT_CODES
                    else "planner_internal_failure"
                )
                self._record_runtime_planner_failure(
                    reason,
                    timeline=timeline,
                    run_id=run_id,
                )
                raise self._error_type(
                    "Agent 规划暂时无法安全执行，任务已停止。请重试或重新描述目标。"
                ) from exc
            fresh_goal_contract_template = _fresh_planner_goal_contract_template(
                fresh_runtime_planner_selection,
                immutable_original_goal,
            )
        planner_guidance_decision = (
            fresh_runtime_planner_selection.decision
            if fresh_runtime_planner_selection is not None
            else None
        )
        if messages is None:
            messages = self._initial_messages(
                context,
                allowed_tools,
                planner_decision=planner_guidance_decision,
                plan_guidance_if_missing=False,
            )
        else:
            self._ensure_runtime_system_message(
                messages,
                allowed_tools,
                planner_decision=planner_guidance_decision,
                plan_guidance_if_missing=False,
            )
        goal_contract_base_kwargs = {
            "run_id": run_id,
            "runtime_execution_envelope": (
                runtime_execution_envelope
                if isinstance(runtime_execution_envelope, Mapping)
                else None
            ),
            "runtime_execution_metadata": runtime_execution_metadata,
            "messages": messages,
            "timeline": timeline,
        }
        if goal_contract is None:
            goal_contract_kwargs = {
                **goal_contract_base_kwargs,
                "original_goal": immutable_original_goal or None,
            }
            try:
                goal_contract = runtime_goal_contract(
                    **goal_contract_kwargs,
                    goal_contract_template=fresh_goal_contract_template,
                )
            except ValueError as exc:
                if str(exc) != "goal_contract_missing" or not immutable_original_goal:
                    raise
                goal_contract = runtime_goal_contract(
                    **goal_contract_kwargs,
                    goal_contract_template=planned_goal_contract_payload(
                        immutable_original_goal,
                        allowed_tools=allowed_tools,
                    ),
                )
        if goal_contract is not None:
            self._ensure_goal_contract_context(
                goal_contract,
                messages=messages,
                timeline=timeline,
                run_id=run_id,
            )
            direct_tool_requests = _runtime_bind_unscoped_tools_to_goal_contract(
                list(direct_tool_requests or []),
                contract=goal_contract,
                timeline=timeline,
                run_id=run_id,
                iteration=normalized_start_iteration,
                # Direct requests at this layer are projections from the
                # deterministic planner / execution envelope. Public or model
                # requests must enter through ToolRequestParser or the
                # safe-default binder boundary below.
                trusted_runtime_metadata=True,
            )
            if isinstance(direct_tool_request, dict):
                bound_direct = _runtime_bind_unscoped_tools_to_goal_contract(
                    [direct_tool_request],
                    contract=goal_contract,
                    timeline=timeline,
                    run_id=run_id,
                    iteration=normalized_start_iteration,
                    trusted_runtime_metadata=True,
                )
                direct_tool_request = bound_direct[0] if bound_direct else None
        budget = budget or self._run_budget(run_id, timeline)
        self._check_context_budget(budget, messages)
        start_iteration = normalized_start_iteration
        runtime_planner_decision = None
        planned_tool_requests: list[dict[str, Any]] = []
        direct_tool_selection_payload: dict[str, Any] = {}
        if not default_messages:
            resumed_result = self._direct_existing_daily_desktop_result(
                agent,
                timeline,
                run_id=run_id,
            )
            if resumed_result:
                return resumed_result
            if resume_after_approved_tool:
                approved_verification_request = (
                    _approved_tool_post_action_verification_request(
                        timeline,
                        allowed_tools=allowed_tools,
                    )
                )
                if approved_verification_request:
                    self._record_auto_model_followup_app_write_plan(
                        [approved_verification_request],
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._run_tool_requests(
                        [approved_verification_request],
                        allowed_tools,
                        broker,
                        messages,
                        timeline,
                        artifacts,
                        next_iteration=start_iteration,
                        run_id=run_id,
                        budget=budget,
                    )
                    resumed_result = self._direct_existing_daily_desktop_result(
                        agent,
                        timeline,
                        run_id=run_id,
                    )
                    if resumed_result:
                        return resumed_result
            # An approval resume already carries the model-selected recovery
            # action and its fresh tool result. Let the model consume that
            # result before considering any older pending replan request.
            if start_iteration > 0 and not resume_after_approved_tool:
                replan_recovery_result = self._run_pending_runtime_replan_recovery(
                    allowed_tools,
                    broker,
                    messages,
                    timeline,
                    artifacts,
                    agent=agent,
                    run_id=run_id,
                    budget=budget,
                    next_iteration=start_iteration,
                    goal_contract=goal_contract,
                )
                if replan_recovery_result:
                    return replan_recovery_result
        if default_messages or (start_iteration == 0 and not resume_after_approved_tool):
            planning_context = (
                str(daily_desktop_planning_context or "").strip()
                if daily_desktop_planning_context is not None
                else context if default_messages else self._latest_user_intent_text(messages)
            )
            direct_planned_tool_request = self._direct_daily_desktop_tool_request(
                direct_tool_request,
                allowed_tools,
            )
            direct_planned_tool_requests = self._direct_daily_desktop_tool_requests(
                direct_tool_requests,
                allowed_tools,
            )
            planner_replan_only = False
            if direct_planned_tool_requests:
                planned_tool_requests = direct_planned_tool_requests
                if not direct_tool_selection_payload:
                    direct_tool_selection_payload = runtime_execution_selection_payload
                if any(
                    bool(request.get("continue_to_model"))
                    and str(request.get("source") or "").strip() == "runtime_planner"
                    for request in direct_planned_tool_requests
                    if isinstance(request, dict)
                ) and not runtime_execution_envelope_owned:
                    (
                        runtime_planner_decision,
                        planner_execution_requests,
                        direct_tool_selection_payload,
                    ) = self._runtime_planner_tool_requests_for_run(
                        planning_context,
                        allowed_tools,
                        runtime_execution_metadata=runtime_execution_metadata,
                        preplanned_selection=fresh_runtime_planner_selection,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    if planner_execution_requests:
                        planned_tool_requests = planner_execution_requests
            elif direct_planned_tool_request:
                planned_tool_requests = [direct_planned_tool_request]
                if not direct_tool_selection_payload:
                    direct_tool_selection_payload = runtime_execution_selection_payload
            elif runtime_execution_envelope_owned:
                self._record_direct_tool_selection_event(
                    runtime_execution_selection_payload,
                    timeline=timeline,
                    run_id=run_id,
                    scope_context=_runtime_planner_scope_context(
                        runtime_execution_selection_payload,
                        timeline=timeline,
                    ),
                )
                return self._record_blocked_runtime_execution_envelope(
                    runtime_execution_envelope,
                    blocked_runtime_execution_requests,
                    timeline=timeline,
                    run_id=run_id,
                )
            elif messages_require_model_first(messages) or bool(
                isinstance(runtime_execution_metadata, Mapping)
                and runtime_execution_metadata.get("yachiyo_attachment_input")
            ):
                pass
            else:
                (
                    runtime_planner_decision,
                    planned_tool_requests,
                    direct_tool_selection_payload,
                ) = self._runtime_planner_tool_requests_for_run(
                    planning_context,
                    allowed_tools,
                    runtime_execution_metadata=runtime_execution_metadata,
                    preplanned_selection=fresh_runtime_planner_selection,
                    timeline=timeline,
                    run_id=run_id,
                )
                if not planned_tool_requests:
                    planner_unavailable_payloads = (
                        _runtime_planner_unavailable_failure_payloads(
                            runtime_planner_decision
                        )
                        if runtime_planner_decision is not None
                        else []
                    )
                    if planner_unavailable_payloads:
                        self._record_runtime_planner_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            run_id=run_id,
                            scope_context=_runtime_planner_scope_context(
                                direct_tool_selection_payload,
                                timeline=timeline,
                            ),
                        )
                        self._record_direct_tool_selection_event(
                            direct_tool_selection_payload,
                            timeline=timeline,
                            run_id=run_id,
                            scope_context=_runtime_planner_scope_context(
                                direct_tool_selection_payload,
                                timeline=timeline,
                            ),
                        )
                        replan_payloads = self._record_runtime_planner_replan_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            tool_timeline_start=len(timeline),
                            run_id=run_id,
                        )
                        if replan_payloads and _planner_replan_context_requested(
                            default_messages=default_messages,
                            runtime_execution_metadata=runtime_execution_metadata,
                        ):
                            self._append_replan_followup_context(
                                replan_payloads,
                                allowed_tools=allowed_tools,
                                messages=messages,
                                timeline=timeline,
                                run_id=run_id,
                            )
                        if _goal_contract_requires_effectful_execution(goal_contract):
                            return self._record_runtime_planner_unavailable_goal(
                                runtime_planner_decision,
                                planner_unavailable_payloads,
                                replan_payloads,
                                goal_contract=goal_contract,
                                timeline=timeline,
                                run_id=run_id,
                            )
                        planner_replan_only = True
            if planned_tool_requests:
                planned_tool_requests = _runtime_bind_unscoped_tools_to_goal_contract(
                    list(planned_tool_requests),
                    contract=goal_contract,
                    timeline=timeline,
                    run_id=run_id,
                    iteration=normalized_start_iteration,
                    trusted_runtime_metadata=True,
                )
                planned_tool_requests = _tool_requests_with_desktop_execution_policy(
                    planned_tool_requests,
                    desktop_execution_policy,
                )
                if runtime_planner_decision is not None:
                    planner_scope_context = _runtime_planner_scope_context(
                        planned_tool_requests,
                        direct_tool_selection_payload,
                        timeline=timeline,
                    )
                    self._record_runtime_planner_events(
                        runtime_planner_decision,
                        timeline=timeline,
                        run_id=run_id,
                        scope_context=planner_scope_context,
                    )
                else:
                    planner_scope_context = _runtime_planner_scope_context(
                        planned_tool_requests,
                        direct_tool_selection_payload,
                        timeline=timeline,
                    )
                self._record_direct_tool_selection_event(
                    direct_tool_selection_payload,
                    timeline=timeline,
                    run_id=run_id,
                    scope_context=planner_scope_context,
                )
                for planned_tool_request in planned_tool_requests:
                    planned_tool = str(planned_tool_request.get("tool") or "")
                    planned_input = planned_tool_request.get("input") or {}
                    planned_payload = {
                        "tool": planned_tool,
                        "status": "planned",
                        "source": str(planned_tool_request.get("source") or "daily_desktop_intent"),
                        "planning_reason": str(
                            planned_tool_request.get("planning_reason") or "clear_daily_desktop_intent"
                        ),
                        "input_preview": planned_input,
                    }
                    planned_payload.update(
                        _desktop_execution_mode_payload(
                            planned_tool,
                            planned_input if isinstance(planned_input, Mapping) else {},
                        )
                    )
                    presentation = str(planned_tool_request.get("presentation") or "").strip()
                    if presentation:
                        planned_payload["presentation"] = presentation
                    if planned_tool_request.get("continue_to_model"):
                        planned_payload["continue_to_model"] = True
                    for key in (
                        "step_id",
                        "capability_id",
                        "replan_request_id",
                        "replan_trigger",
                    ):
                        value = str(planned_tool_request.get(key) or "").strip()
                        if value:
                            planned_payload[key] = value
                    planned_payload.update(
                        _request_observability_metadata(planned_tool_request)
                    )
                    timeline.append(
                        self._timeline(
                            "agent.desktop.intent_planned",
                            planned_tool,
                            **planned_payload,
                        )
                    )
                    if run_id and self._append_run_event is not None:
                        self._append_run_event(
                            run_id,
                            "agent.desktop.intent_planned",
                            planned_payload,
                        )
                execution_tool_requests = planned_tool_requests
                if self._has_approval_plan_tool(planned_tool_requests):
                    approval_has_dependencies = any(
                        bool(request.get("approval_required"))
                        and any(
                            str(step_id or "").strip()
                            for step_id in request.get("depends_on", [])
                        )
                        for request in planned_tool_requests
                        if isinstance(request, Mapping)
                        and isinstance(request.get("depends_on"), list)
                    )
                    if not approval_has_dependencies:
                        execution_tool_requests = _visible_daily_desktop_completed_steps(
                            planned_tool_requests
                        )
                        if not execution_tool_requests:
                            execution_tool_requests = planned_tool_requests
                execution_tool_requests = _split_model_materialization_tool_requests(
                    execution_tool_requests
                )[0]
                execution_tool_requests = _split_combined_foreground_app_requests(
                    execution_tool_requests,
                    allowed_tools,
                )
                first_approval_index = next(
                    (
                        index
                        for index, request in enumerate(execution_tool_requests)
                        if isinstance(request, dict)
                        and bool(request.get("approval_required"))
                    ),
                    -1,
                )
                if first_approval_index >= 0:
                    approval_candidate = execution_tool_requests[first_approval_index]
                    approval_dependencies = approval_candidate.get("depends_on")
                    if isinstance(approval_dependencies, list) and any(
                        str(step_id or "").strip()
                        for step_id in approval_dependencies
                    ):
                        # Dependency-aware planner approvals must stay in the
                        # authoritative Runner sequence.  Splitting the prefix
                        # here would let this compatibility projection bypass
                        # the Runner's prerequisite-success gate.
                        first_approval_index = -1
                if first_approval_index == 0:
                    approval_candidate = execution_tool_requests[0]
                    approval_tool = str(approval_candidate.get("tool") or "").strip()
                    if _broker_tool_precondition_failure(broker, approval_tool) is not None:
                        # Project the precondition failure through the Runner
                        # instead of requesting approval for an impossible action.
                        first_approval_index = -1
                if first_approval_index == 0:
                    approval_request = execution_tool_requests[0]
                    approval_tool = str(approval_request.get("tool") or "").strip()
                    approval_input = (
                        approval_request.get("input")
                        if isinstance(approval_request.get("input"), dict)
                        else {}
                    )
                    approval_id_source = (
                        f"{run_id}:{approval_tool}:{repr(sorted(approval_input.items()))}"
                    )
                    approval_preview = dict(approval_input)
                    if approval_preview.get("app_name"):
                        approval_preview.pop("query", None)
                        approval_preview.pop("selection_source", None)
                    pending_approval = {
                        "approval_id": (
                            "approval-"
                            + hashlib.sha1(approval_id_source.encode("utf-8")).hexdigest()[:12]
                        ),
                        "tool": approval_tool,
                        "input": dict(approval_input),
                        "input_preview": approval_preview,
                        "messages": list(messages),
                        "tool_request": dict(approval_request),
                        "remaining_tool_requests": [
                            dict(item)
                            for item in execution_tool_requests[1:]
                            if isinstance(item, dict)
                        ],
                        "next_iteration": self._normalize_tool_iteration(
                            start_iteration + 1
                        ),
                    }
                    self._record_desktop_intent_approval_required(
                        approval_tool,
                        dict(approval_input),
                        pending_approval=pending_approval,
                        timeline=timeline,
                        run_id=run_id,
                        planned_request=approval_request,
                        source=self._approval_event_source(
                            approval_request,
                            approval_tool,
                        ),
                        planning_reason=self._approval_event_planning_reason(
                            approval_request,
                            approval_tool,
                        ),
                    )
                    raise AgentApprovalRequired(pending_approval)
                if first_approval_index > 0:
                    (
                        preapproval_requests,
                        approval_request,
                        approval_gate_requests,
                    ) = _legacy_approval_gate_requests(
                        execution_tool_requests,
                        first_approval_index,
                    )
                    self._record_desktop_permission_preflight(
                        preapproval_requests,
                        broker,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._record_desktop_tool_policy_decisions(
                        preapproval_requests,
                        allowed_tools=allowed_tools,
                        agent=agent,
                        run_id=run_id,
                    )
                    preapproval_timeline_start = len(timeline)
                    self._run_tool_requests(
                        preapproval_requests,
                        allowed_tools,
                        broker,
                        messages,
                        timeline,
                        artifacts,
                        next_iteration=start_iteration,
                        run_id=run_id,
                        budget=budget,
                    )
                    self._record_runtime_planner_task_progress_events(
                        runtime_planner_decision,
                        timeline=timeline,
                        tool_timeline_start=preapproval_timeline_start,
                        run_id=run_id,
                    )
                    approval_tool = str(approval_request.get("tool") or "").strip()
                    approval_input = (
                        approval_request.get("input")
                        if isinstance(approval_request.get("input"), dict)
                        else {}
                    )
                    approval_dependency_block = _approval_dependency_block_result(
                        approval_gate_requests[-1],
                        approval_gate_requests,
                        _legacy_approval_gate_timeline(
                            timeline,
                            preapproval_requests,
                            tool_timeline_start=preapproval_timeline_start,
                        ),
                    )
                    if approval_dependency_block:
                        blocked_payload = {
                            "tool": approval_tool,
                            "input_preview": dict(approval_input),
                            "result": approval_dependency_block,
                            "status": "blocked",
                            **_request_observability_metadata(approval_request),
                        }
                        timeline.append(
                            self._timeline(
                                "agent.tool.skipped",
                                approval_tool,
                                **blocked_payload,
                            )
                        )
                        if run_id and self._append_run_event is not None:
                            self._append_run_event(
                                run_id,
                                "agent.tool.skipped",
                                blocked_payload,
                            )
                        raise AgentDirectOutcomeUnverified(
                            str(
                                approval_dependency_block.get("summary")
                                or "approval dependency unverified"
                            ),
                            reason="approval_dependency_unverified",
                            tool_name=approval_tool,
                            input_preview=dict(approval_input),
                            tool_call_id=str(
                                approval_request.get("tool_call_id") or ""
                            ),
                        )
                    approval_id_source = (
                        f"{run_id}:{approval_tool}:{repr(sorted(approval_input.items()))}"
                    )
                    approval_preview = dict(approval_input)
                    if approval_preview.get("app_name"):
                        approval_preview.pop("query", None)
                        approval_preview.pop("selection_source", None)
                    pending_approval = {
                        "approval_id": (
                            "approval-"
                            + hashlib.sha1(approval_id_source.encode("utf-8")).hexdigest()[:12]
                        ),
                        "tool": approval_tool,
                        "input": dict(approval_input),
                        "input_preview": approval_preview,
                        "messages": list(messages),
                        "tool_request": dict(approval_request),
                        "completed_tool_requests": [
                            dict(item)
                            for item in preapproval_requests
                            if isinstance(item, dict)
                        ],
                        "remaining_tool_requests": [
                            dict(item)
                            for item in execution_tool_requests[first_approval_index + 1 :]
                            if isinstance(item, dict)
                        ],
                        "next_iteration": self._normalize_tool_iteration(
                            start_iteration + 1
                        ),
                    }
                    self._record_desktop_intent_approval_required(
                        approval_tool,
                        dict(approval_input),
                        pending_approval=pending_approval,
                        timeline=timeline,
                        run_id=run_id,
                        planned_request=approval_request,
                        source=self._approval_event_source(
                            approval_request,
                            approval_tool,
                        ),
                        planning_reason=self._approval_event_planning_reason(
                            approval_request,
                            approval_tool,
                        ),
                    )
                    raise AgentApprovalRequired(pending_approval)
                self._record_desktop_permission_preflight(
                    execution_tool_requests,
                    broker,
                    timeline=timeline,
                    run_id=run_id,
                )
                self._record_desktop_tool_policy_decisions(
                    execution_tool_requests,
                    allowed_tools=allowed_tools,
                    agent=agent,
                    run_id=run_id,
                )
                explicit_model_followup_requested = any(
                    bool(request.get("continue_to_model"))
                    for request in planned_tool_requests
                    if isinstance(request, dict)
                )
                immutable_execution_tool_requests = [
                    deepcopy(request)
                    for request in execution_tool_requests
                    if isinstance(request, Mapping)
                ]
                tool_timeline_start = len(timeline)
                try:
                    if execution_tool_requests:
                        self._run_tool_requests(
                            execution_tool_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            next_iteration=start_iteration,
                            run_id=run_id,
                            budget=budget,
                        )
                except AgentApprovalRequired as exc:
                    self._record_runtime_planner_task_progress_events(
                        runtime_planner_decision,
                        timeline=timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    pending_approval = (
                        exc.pending_approval if isinstance(exc.pending_approval, dict) else {}
                    )
                    planned_tool = str(
                        pending_approval.get("tool")
                        or execution_tool_requests[0].get("tool")
                        or ""
                    )
                    approval_request = self._planned_request_for_tool(
                        execution_tool_requests,
                        planned_tool,
                    )
                    planned_input = self._pending_approval_input_preview(
                        pending_approval,
                        approval_request,
                        execution_tool_requests[0] if execution_tool_requests else {},
                    )
                    self._record_desktop_intent_approval_required(
                        planned_tool,
                        planned_input,
                        pending_approval=exc.pending_approval,
                        timeline=timeline,
                        run_id=run_id,
                        planned_request=approval_request,
                        source=self._approval_event_source(approval_request, planned_tool),
                        planning_reason=self._approval_event_planning_reason(
                            approval_request,
                            planned_tool,
                        ),
                    )
                    raise
                replan_payloads = self._record_runtime_planner_replan_events(
                    runtime_planner_decision,
                    timeline=timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                )
                direct_outcome_results = OutcomeLoopCoordinator().decide_terminal_batch(
                    timeline=timeline,
                    start_index=tool_timeline_start,
                    run_id=run_id,
                    allowed_tools=allowed_tools,
                    planned_tool_call_ids=[
                        str(request.get("tool_call_id") or "")
                        for request in execution_tool_requests
                    ],
                    recovery_action=lambda assessment: (
                        self._execute_runtime_recovery_plan(
                            assessment,
                            model_config=lambda: self._agent_model_config_private(agent),
                            allowed_tools=allowed_tools,
                            broker=broker,
                            messages=messages,
                            timeline=timeline,
                            artifacts=artifacts,
                            budget=budget,
                            iteration=start_iteration,
                            run_id=run_id,
                        )
                    ),
                )
                (
                    recovered_source_tool_call_id,
                    recovered_downstream_requests,
                ) = _runtime_background_window_resume_contract(
                    direct_outcome_results,
                    immutable_execution_tool_requests,
                    timeline=timeline,
                    run_id=run_id,
                )
                if recovered_downstream_requests:
                    recovered_timeline_start = len(timeline)
                    try:
                        self._run_tool_requests(
                            recovered_downstream_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            next_iteration=start_iteration,
                            run_id=run_id,
                            budget=budget,
                        )
                    except AgentApprovalRequired as exc:
                        self._record_runtime_planner_task_progress_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            tool_timeline_start=recovered_timeline_start,
                            run_id=run_id,
                        )
                        self._record_tool_request_task_progress_events(
                            recovered_downstream_requests,
                            timeline=timeline,
                            tool_timeline_start=recovered_timeline_start,
                            run_id=run_id,
                        )
                        pending_approval = (
                            exc.pending_approval
                            if isinstance(exc.pending_approval, dict)
                            else {}
                        )
                        planned_tool = str(
                            pending_approval.get("tool")
                            or recovered_downstream_requests[0].get("tool")
                            or ""
                        )
                        approval_request = self._planned_request_for_tool(
                            recovered_downstream_requests,
                            planned_tool,
                        )
                        planned_input = self._pending_approval_input_preview(
                            pending_approval,
                            approval_request,
                            recovered_downstream_requests[0],
                        )
                        self._record_desktop_intent_approval_required(
                            planned_tool,
                            planned_input,
                            pending_approval=exc.pending_approval,
                            timeline=timeline,
                            run_id=run_id,
                            planned_request=approval_request,
                            source=self._approval_event_source(
                                approval_request,
                                planned_tool,
                            ),
                            planning_reason=self._approval_event_planning_reason(
                                approval_request,
                                planned_tool,
                            ),
                        )
                        raise
                    recovered_outcomes = OutcomeLoopCoordinator().decide_terminal_batch(
                        timeline=timeline,
                        start_index=recovered_timeline_start,
                        run_id=run_id,
                        allowed_tools=allowed_tools,
                        planned_tool_call_ids=[
                            str(request.get("tool_call_id") or "")
                            for request in recovered_downstream_requests
                        ],
                        recovery_action=lambda assessment: (
                            self._execute_runtime_recovery_plan(
                                assessment,
                                model_config=lambda: self._agent_model_config_private(
                                    agent
                                ),
                                allowed_tools=allowed_tools,
                                broker=broker,
                                messages=messages,
                                timeline=timeline,
                                artifacts=artifacts,
                                budget=budget,
                                iteration=start_iteration,
                                run_id=run_id,
                            )
                        ),
                    )
                    direct_outcome_results = [
                        result
                        for result in direct_outcome_results
                        if result.source_tool_call_id
                        != recovered_source_tool_call_id
                    ]
                    direct_outcome_results.extend(recovered_outcomes)
                clipboard_transfer_failure = (
                    _runtime_clipboard_transfer_verification_failure(
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                )
                if clipboard_transfer_failure is not None:
                    raise AgentDirectOutcomeUnverified(
                        str(clipboard_transfer_failure["summary"]),
                        reason="desktop_verification_failed",
                        tool_name=str(clipboard_transfer_failure["tool"]),
                        input_preview=dict(
                            clipboard_transfer_failure["input_preview"]
                        ),
                        tool_call_id=str(
                            clipboard_transfer_failure["tool_call_id"]
                        ),
                    )
                await_user_result = next(
                    (
                        result
                        for result in direct_outcome_results
                        if result.disposition is OutcomeLoopDisposition.AWAIT_USER
                    ),
                    None,
                )
                if await_user_result is not None:
                    if _timeline_has_permission_recovery_signal(
                        timeline,
                        tool_timeline_start,
                    ):
                        self._record_timeline_desktop_permission_recovery_events(
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                    source_request = next(
                        (
                            request
                            for request in execution_tool_requests
                            if str(request.get("tool_call_id") or "").strip()
                            == await_user_result.source_tool_call_id
                        ),
                        {},
                    )
                    message = self._consumer_tool_outcome_message(
                        await_user_result,
                        source_request,
                        default="此操作需要用户授权后才能继续。",
                    )
                    raise AgentDirectOutcomeUnverified(
                        message,
                        reason=await_user_result.reason or "action_required",
                        tool_name=await_user_result.outcome.tool_name,
                        input_preview=(
                            dict(source_request.get("input"))
                            if isinstance(source_request.get("input"), Mapping)
                            else {}
                        ),
                        tool_call_id=await_user_result.source_tool_call_id,
                    )
                failed_outcome_result = next(
                    (
                        result
                        for result in direct_outcome_results
                        if result.disposition is OutcomeLoopDisposition.FAILED
                    ),
                    None,
                )
                if failed_outcome_result is not None and not replan_payloads:
                    source_request = next(
                        (
                            request
                            for request in execution_tool_requests
                            if str(request.get("tool_call_id") or "").strip()
                            == failed_outcome_result.source_tool_call_id
                        ),
                        {},
                    )
                    message = self._consumer_tool_outcome_message(
                        failed_outcome_result,
                        source_request,
                        default="工具执行未完成。",
                    )
                    raise AgentDirectOutcomeUnverified(
                        message,
                        reason=failed_outcome_result.reason or "tool_error",
                        tool_name=failed_outcome_result.outcome.tool_name,
                        input_preview=(
                            dict(source_request.get("input"))
                            if isinstance(source_request.get("input"), Mapping)
                            else {}
                        ),
                        tool_call_id=failed_outcome_result.source_tool_call_id,
                    )
                continuation_replan_payloads = (
                    self._record_recovery_continuation_replan_events(
                        direct_outcome_results,
                        timeline=timeline,
                        run_id=run_id,
                    )
                )
                if continuation_replan_payloads:
                    replan_payloads = [
                        *replan_payloads,
                        *(
                            payload
                            for payload in continuation_replan_payloads
                            if _runtime_replan_payload_identity(payload)
                            not in {
                                _runtime_replan_payload_identity(existing)
                                for existing in replan_payloads
                            }
                        ),
                    ]
                terminal_recovery_output = self._terminal_recovery_output(
                    direct_outcome_results,
                    goal_contract=goal_contract,
                    timeline=timeline,
                )
                blocking_outcome_dispositions = {
                    OutcomeLoopDisposition.AWAIT_USER,
                    OutcomeLoopDisposition.REPLAN_MODEL,
                    OutcomeLoopDisposition.PARTIAL,
                    OutcomeLoopDisposition.FAILED,
                }
                if terminal_recovery_output and not any(
                    result.disposition in blocking_outcome_dispositions
                    for result in direct_outcome_results
                ):
                    return terminal_recovery_output
                if any(
                    result.disposition
                    in {
                        OutcomeLoopDisposition.REPLAN_MODEL,
                        OutcomeLoopDisposition.PARTIAL,
                    }
                    for result in direct_outcome_results
                ) and not replan_payloads:
                    # One intermediate action can be intentionally partial
                    # (for example, opening an app's find UI) while a later,
                    # provenance-bound verifier already satisfies the root
                    # Goal.  Do not discard that completed plan and fall into
                    # an unnecessary model turn.  The Goal evaluator remains
                    # the authority here; provider summaries and aggregate
                    # success flags cannot enter this branch on their own.
                    goal_completed = bool(
                        goal_contract is not None
                        and runtime_goal_assessment(
                            goal_contract,
                            timeline,
                        ).completed
                    )
                    if goal_completed:
                        direct_result = self._direct_daily_desktop_sequence_result(
                            execution_tool_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if direct_result:
                            self._record_goal_contract_assessment(
                                goal_contract,
                                timeline=timeline,
                                run_id=run_id,
                            )
                            return direct_result
                    if _tool_requests_require_media_playback(
                        execution_tool_requests
                    ) and not _tool_requests_include_media_tool(
                        execution_tool_requests
                    ):
                        # A preparatory UI action may be honestly partial even
                        # when the later search/read steps succeeded.  With no
                        # recovery plan and no playback receipt, handing that
                        # state to the model only invites an unsupported
                        # "played" claim.  Close the capability-level goal
                        # through the same verified aggregate used by every
                        # direct desktop sequence.
                        direct_result = self._direct_daily_desktop_sequence_result(
                            execution_tool_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if direct_result:
                            return direct_result
                    execution_tool_requests = []
                    planned_tool_requests = []
                    replan_payloads = []
                deferred_verification_requests = [
                    request
                    for request in execution_tool_requests
                    if isinstance(request, Mapping)
                    and str(request.get("tool") or "").strip()
                    in _DAILY_DESKTOP_VERIFY_TOOLS
                ]
                completed_primary = (
                    _runtime_planner_verified_app_open_before_unavailable_deferred_verification(
                        deferred_verification_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=len(timeline),
                    )
                )
                if completed_primary is not None:
                    completed_requests, completed_timeline_start = completed_primary
                    direct_result = self._direct_daily_desktop_sequence_result(
                        completed_requests,
                        timeline,
                        tool_timeline_start=completed_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
                permission_recovery_detected = _timeline_has_permission_recovery_signal(
                    timeline,
                    tool_timeline_start,
                )
                permission_recovery_requests: list[dict[str, Any]] = []
                if permission_recovery_detected:
                    self._record_timeline_desktop_permission_recovery_events(
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    permission_recovery_requests = (
                        _permission_recovery_executed_requests(
                            execution_tool_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                        )
                    )
                terminal_media_failure_detected = (
                    _timeline_has_terminal_media_failure_without_permission_recovery(
                        timeline,
                        tool_timeline_start,
                    )
                )
                if replan_payloads and permission_recovery_detected:
                    replan_payloads = []
                self._record_runtime_planner_task_progress_events(
                    runtime_planner_decision,
                    timeline=timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                )
                self._record_tool_request_task_progress_events(
                    execution_tool_requests,
                    timeline=timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                )
                verification_failure = (
                    _runtime_planner_trailing_verification_failure_outcome(
                        execution_tool_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                    if replan_payloads and not permission_recovery_detected
                    else None
                )
                if verification_failure is not None:
                    # The primary action is not proven complete, but the
                    # Runtime already minted a trusted replan request for the
                    # failed verifier.  Keep that failure as observation
                    # evidence and let the bounded recovery loop select
                    # another verifier or model-planned recovery action. Tool
                    # policy and approval gates still prevent an unverified
                    # side effect from being replayed directly.
                    timeline.append(
                        self._timeline(
                            "agent.goal.verification_replan_continued",
                            str(verification_failure["tool"]),
                            run_id=run_id,
                            status="replanning",
                            source_tool_call_id=str(
                                verification_failure["tool_call_id"]
                            ),
                            visibility="internal",
                        )
                    )
                if terminal_media_failure_detected and not replan_payloads:
                    # A retryable media miss is a failed attempt, not a failed
                    # root goal.  When Runtime has already minted a replan
                    # request, preserve the failure as model-visible evidence
                    # and let the generic loop choose another capability (for
                    # example research/observation) before retrying playback.
                    # Only project the terminal consumer failure when no safe
                    # replan path exists.
                    direct_result = self._direct_daily_desktop_sequence_result(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
                if _direct_runtime_verification_only_followups(
                    execution_tool_requests
                ):
                    direct_result = self._direct_daily_desktop_sequence_result(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
                explicit_model_followup = explicit_model_followup_requested
                if permission_recovery_detected:
                    direct_result = self._direct_daily_desktop_sequence_result(
                        permission_recovery_requests or execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
                    auto_permission_recovery_requests = []
                else:
                    auto_permission_recovery_requests = _auto_direct_permission_recovery_requests(
                        execution_tool_requests,
                        allowed_tools,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                if auto_permission_recovery_requests:
                    recovery_timeline_start = self._run_auto_runtime_planner_requests(
                        auto_permission_recovery_requests,
                        allowed_tools,
                        broker,
                        messages,
                        timeline,
                        artifacts,
                        agent=agent,
                        runtime_planner_decision=runtime_planner_decision,
                        run_id=run_id,
                        budget=budget,
                        next_iteration=start_iteration,
                    )
                    auto_permission_retry_requests = _auto_direct_permission_retry_requests(
                        auto_permission_recovery_requests,
                        allowed_tools,
                        timeline,
                        tool_timeline_start=recovery_timeline_start,
                    )
                    retry_timeline_start = len(timeline)
                    if auto_permission_retry_requests:
                        retry_timeline_start = self._run_auto_runtime_planner_requests(
                            auto_permission_retry_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            agent=agent,
                            runtime_planner_decision=runtime_planner_decision,
                            run_id=run_id,
                            budget=budget,
                            next_iteration=start_iteration,
                        )
                    permission_retry_completed = bool(
                        auto_permission_retry_requests
                        and _auto_direct_permission_retry_completed(
                            auto_permission_retry_requests,
                            timeline,
                            tool_timeline_start=retry_timeline_start,
                        )
                    )
                    if permission_recovery_detected and not permission_retry_completed:
                        direct_result = self._direct_daily_desktop_sequence_result(
                            execution_tool_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if direct_result:
                            return direct_result
                    planned_tool_requests = [
                        *planned_tool_requests,
                        *auto_permission_recovery_requests,
                        *auto_permission_retry_requests,
                    ]
                    explicit_model_followup = (
                        explicit_model_followup_requested
                        if permission_retry_completed
                        else True
                    )
                if (
                    runtime_execution_envelope_owned
                    and blocked_runtime_execution_requests
                    and not _runtime_execution_envelope_has_only_materialization_blockers(
                        runtime_execution_envelope,
                        blocked_runtime_execution_requests,
                    )
                ):
                    return self._record_blocked_runtime_execution_envelope(
                        runtime_execution_envelope,
                        blocked_runtime_execution_requests,
                        timeline=timeline,
                        run_id=run_id,
                        completed_requests=execution_tool_requests,
                    )
                if (
                    explicit_model_followup
                    and not replan_payloads
                    and not _selection_payload_has_model_followup_target(
                        direct_tool_selection_payload
                    )
                    and _runtime_planner_completed_discovered_app_direct_action(
                        execution_tool_requests,
                        direct_tool_selection_payload,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    explicit_model_followup = False
                if (
                    explicit_model_followup
                    and not replan_payloads
                    and _runtime_planner_followup_requests_are_only_verification(
                        planned_tool_requests
                    )
                    and _runtime_planner_completed_direct_requests_with_successful_verification(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    explicit_model_followup = False
                if (
                    explicit_model_followup
                    and _runtime_planner_followup_requests_are_only_verification(
                        planned_tool_requests
                    )
                    and _runtime_planner_completed_terminal_direct_requests_with_trailing_verification(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    explicit_model_followup = False
                if (
                    replan_payloads
                    and _runtime_planner_completed_terminal_direct_requests_with_verification_replan(
                        execution_tool_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    replan_payloads = []
                    explicit_model_followup = False
                if (
                    replan_payloads
                    and not explicit_model_followup
                    and _runtime_planner_completed_direct_requests_with_unavailable_replan(
                        execution_tool_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    replan_payloads = []
                if (
                    replan_payloads
                    and not explicit_model_followup
                    and _runtime_planner_completed_direct_requests_with_verification_replan(
                        execution_tool_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    replan_payloads = []
                if (
                    replan_payloads
                    and _legacy_chat_direct_local_completed_with_verification_replan(
                        execution_tool_requests,
                        replan_payloads,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    replan_payloads = []
                    explicit_model_followup = False
                    direct_result = self._direct_daily_desktop_sequence_result(
                        _legacy_chat_direct_local_result_requests(
                            execution_tool_requests
                        ),
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
                continue_to_model = bool(replan_payloads) or explicit_model_followup
                if continue_to_model:
                    followup_selection_payload = _selection_payload_with_timeline_fallback(
                        direct_tool_selection_payload,
                        timeline,
                    )
                    auto_followup_request = self._auto_data_analysis_request_from_discovery(
                        planned_tool_requests,
                        allowed_tools,
                        followup_selection_payload,
                        timeline,
                    )
                    auto_replan_recovery_requests = (
                        _auto_replan_recovery_requests_with_task_context(
                            replan_payloads,
                            allowed_tools,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                        )
                    )
                    auto_replan_recovery_requests = (
                        _bind_trusted_runtime_replan_goal_requests(
                            auto_replan_recovery_requests,
                            replan_payloads,
                            contract=goal_contract,
                            timeline=timeline,
                            run_id=run_id,
                        )
                    )
                    if auto_replan_recovery_requests:
                        self._record_auto_model_followup_app_write_plan(
                            auto_replan_recovery_requests,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_permission_preflight(
                            auto_replan_recovery_requests,
                            broker,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_tool_policy_decisions(
                            auto_replan_recovery_requests,
                            allowed_tools=allowed_tools,
                            agent=agent,
                            run_id=run_id,
                        )
                        auto_tool_timeline_start = len(timeline)
                        auto_replan_approval_requests = auto_replan_recovery_requests
                        try:
                            self._run_tool_requests(
                                auto_replan_recovery_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=start_iteration,
                                run_id=run_id,
                                budget=budget,
                            )
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            generated_replan_deferred_continuation_requests = (
                                _auto_replan_recovery_deferred_continuation_requests(
                                    auto_replan_recovery_requests,
                                    allowed_tools,
                                    timeline,
                                    tool_timeline_start=auto_tool_timeline_start,
                                )
                            )
                            auto_replan_deferred_continuation_requests = (
                                _drop_replayed_approved_replan_operate_requests(
                                    generated_replan_deferred_continuation_requests,
                                    timeline,
                                )
                            )
                            if (
                                generated_replan_deferred_continuation_requests
                                and not auto_replan_deferred_continuation_requests
                                and _replayed_approved_replan_operate_requests_verified(
                                    generated_replan_deferred_continuation_requests,
                                    timeline,
                                )
                            ):
                                verified_operate_request = next(
                                    request
                                    for request in generated_replan_deferred_continuation_requests
                                    if _replan_observed_action_operate_request(request)
                                )
                                direct_result = self._direct_daily_desktop_result(
                                    agent,
                                    str(verified_operate_request.get("tool") or ""),
                                    (
                                        dict(verified_operate_request.get("input"))
                                        if isinstance(
                                            verified_operate_request.get("input"),
                                            Mapping,
                                        )
                                        else {}
                                    ),
                                    timeline,
                                    run_id=run_id,
                                    source=str(
                                        verified_operate_request.get("source")
                                        or "runtime_planner"
                                    ),
                                    planning_reason=(
                                        "planner_verified_approved_replan_action"
                                    ),
                                )
                                if direct_result:
                                    return direct_result
                            if auto_replan_deferred_continuation_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_approval_requests,
                                    *auto_replan_deferred_continuation_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_deferred_continuation_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_deferred_continuation_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_deferred_continuation_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                deferred_continuation_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_deferred_continuation_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=deferred_continuation_timeline_start,
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup_and_deferred_continuation(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_deferred_continuation_requests,
                                ]
                            auto_replan_continuation_requests = (
                                _auto_replan_discovered_app_continuation_requests(
                                    replan_payloads,
                                    execution_tool_requests,
                                    allowed_tools,
                                    timeline,
                                )
                            )
                            if auto_replan_continuation_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_recovery_requests,
                                    *auto_replan_continuation_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_continuation_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_continuation_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_continuation_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                continuation_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_continuation_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=continuation_timeline_start,
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_continuation_requests,
                                ]
                            auto_replan_verification_continuation_requests = (
                                _auto_replan_verification_continuation_requests(
                                    replan_payloads,
                                    execution_tool_requests,
                                    allowed_tools,
                                    timeline,
                                    tool_timeline_start=tool_timeline_start,
                                    planning_reason=(
                                        "planner_replan_verification_continuation"
                                    ),
                                )
                            )
                            if auto_replan_verification_continuation_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_approval_requests,
                                    *auto_replan_verification_continuation_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_verification_continuation_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_verification_continuation_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_verification_continuation_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                verification_continuation_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_verification_continuation_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=(
                                        verification_continuation_timeline_start
                                    ),
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_verification_continuation_requests,
                                ]
                            auto_replan_verification_observed_action_requests = (
                                _auto_replan_verification_observed_action_requests(
                                    replan_payloads,
                                    execution_tool_requests,
                                    allowed_tools,
                                    timeline,
                                    planning_reason=(
                                        "planner_replan_verification_observed_action"
                                    ),
                                )
                            )
                            if auto_replan_verification_observed_action_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_approval_requests,
                                    *auto_replan_verification_observed_action_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_verification_observed_action_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_verification_observed_action_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_verification_observed_action_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                verification_observed_action_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_verification_observed_action_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=(
                                        verification_observed_action_timeline_start
                                    ),
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_verification_observed_action_requests,
                                ]
                            auto_replan_ui_observed_action_requests = (
                                _auto_replan_ui_observed_action_requests(
                                    replan_payloads,
                                    allowed_tools,
                                    timeline,
                                    planning_reason="planner_replan_ui_observed_action",
                                )
                            )
                            if auto_replan_ui_observed_action_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_approval_requests,
                                    *auto_replan_ui_observed_action_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_ui_observed_action_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_ui_observed_action_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_ui_observed_action_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                observed_ui_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_ui_observed_action_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=observed_ui_timeline_start,
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_ui_observed_action_requests,
                                ]
                                auto_replan_ui_continuation_requests = (
                                    _auto_replan_ui_continuation_requests(
                                        replan_payloads,
                                        execution_tool_requests,
                                        allowed_tools,
                                        timeline,
                                        tool_timeline_start=tool_timeline_start,
                                        planning_reason="planner_replan_ui_continuation",
                                    )
                                )
                                if auto_replan_ui_continuation_requests:
                                    auto_replan_approval_requests = [
                                        *auto_replan_approval_requests,
                                        *auto_replan_ui_continuation_requests,
                                    ]
                                    self._record_auto_model_followup_app_write_plan(
                                        auto_replan_ui_continuation_requests,
                                        timeline=timeline,
                                        run_id=run_id,
                                    )
                                    self._record_desktop_permission_preflight(
                                        auto_replan_ui_continuation_requests,
                                        broker,
                                        timeline=timeline,
                                        run_id=run_id,
                                    )
                                    self._record_desktop_tool_policy_decisions(
                                        auto_replan_ui_continuation_requests,
                                        allowed_tools=allowed_tools,
                                        agent=agent,
                                        run_id=run_id,
                                    )
                                    ui_continuation_timeline_start = len(timeline)
                                    self._run_tool_requests(
                                        auto_replan_ui_continuation_requests,
                                        allowed_tools,
                                        broker,
                                        messages,
                                        timeline,
                                        artifacts,
                                        next_iteration=start_iteration,
                                        run_id=run_id,
                                        budget=budget,
                                    )
                                    self._record_runtime_planner_task_progress_events(
                                        runtime_planner_decision,
                                        timeline=timeline,
                                        tool_timeline_start=ui_continuation_timeline_start,
                                        run_id=run_id,
                                    )
                                    auto_replan_recovery_requests = [
                                        *auto_replan_recovery_requests,
                                        *auto_replan_ui_continuation_requests,
                                    ]
                                auto_replan_ui_observed_result_requests = (
                                    _auto_replan_ui_search_observed_result_requests(
                                        replan_payloads,
                                        execution_tool_requests,
                                        allowed_tools,
                                        timeline,
                                        planning_reason=(
                                            "planner_replan_ui_search_observed_result"
                                        ),
                                    )
                                )
                                if auto_replan_ui_observed_result_requests:
                                    auto_replan_approval_requests = [
                                        *auto_replan_approval_requests,
                                        *auto_replan_ui_observed_result_requests,
                                    ]
                                    self._record_auto_model_followup_app_write_plan(
                                        auto_replan_ui_observed_result_requests,
                                        timeline=timeline,
                                        run_id=run_id,
                                    )
                                    self._record_desktop_permission_preflight(
                                        auto_replan_ui_observed_result_requests,
                                        broker,
                                        timeline=timeline,
                                        run_id=run_id,
                                    )
                                    self._record_desktop_tool_policy_decisions(
                                        auto_replan_ui_observed_result_requests,
                                        allowed_tools=allowed_tools,
                                        agent=agent,
                                        run_id=run_id,
                                    )
                                    ui_observed_result_timeline_start = len(timeline)
                                    self._run_tool_requests(
                                        auto_replan_ui_observed_result_requests,
                                        allowed_tools,
                                        broker,
                                        messages,
                                        timeline,
                                        artifacts,
                                        next_iteration=start_iteration,
                                        run_id=run_id,
                                        budget=budget,
                                    )
                                    self._record_runtime_planner_task_progress_events(
                                        runtime_planner_decision,
                                        timeline=timeline,
                                        tool_timeline_start=(
                                            ui_observed_result_timeline_start
                                        ),
                                        run_id=run_id,
                                    )
                                    auto_replan_recovery_requests = [
                                        *auto_replan_recovery_requests,
                                        *auto_replan_ui_observed_result_requests,
                                    ]
                            else:
                                auto_replan_ui_observed_result_requests = []
                            auto_replan_observed_result_requests = []
                            if not auto_replan_ui_observed_result_requests:
                                auto_replan_observed_result_requests = (
                                    _auto_replan_app_search_observed_result_requests(
                                        replan_payloads,
                                        followup_selection_payload,
                                        allowed_tools,
                                        timeline,
                                        planning_reason=(
                                            "planner_replan_app_search_observed_result"
                                        ),
                                    )
                                )
                            if auto_replan_observed_result_requests:
                                auto_replan_approval_requests = [
                                    *auto_replan_approval_requests,
                                    *auto_replan_observed_result_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_replan_observed_result_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_replan_observed_result_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_replan_observed_result_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                observed_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_replan_observed_result_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=observed_timeline_start,
                                    run_id=run_id,
                                )
                                auto_replan_recovery_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_replan_recovery_requests
                                    ),
                                    *auto_replan_observed_result_requests,
                                ]
                        except AgentApprovalRequired as exc:
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            pending_approval = (
                                exc.pending_approval
                                if isinstance(exc.pending_approval, dict)
                                else {}
                            )
                            planned_tool = str(
                                pending_approval.get("tool")
                                or auto_replan_approval_requests[0].get("tool")
                                or ""
                            )
                            approval_request = self._planned_request_for_tool(
                                auto_replan_approval_requests,
                                planned_tool,
                            )
                            planned_input = self._pending_approval_input_preview(
                                pending_approval,
                                approval_request,
                                (
                                    auto_replan_approval_requests[0]
                                    if auto_replan_approval_requests
                                    else {}
                                ),
                            )
                            self._record_desktop_intent_approval_required(
                                planned_tool,
                                planned_input,
                                pending_approval=exc.pending_approval,
                                timeline=timeline,
                                run_id=run_id,
                                planned_request=approval_request,
                                source=self._approval_event_source(
                                    approval_request,
                                    planned_tool,
                                ),
                                planning_reason=self._approval_event_planning_reason(
                                    approval_request,
                                    planned_tool,
                                ),
                            )
                            raise
                        planned_tool_requests = [
                            *planned_tool_requests,
                            *auto_replan_recovery_requests,
                        ]
                        if not _replan_recovery_requests_need_model_followup(
                            auto_replan_recovery_requests
                        ):
                            direct_result = self._direct_daily_desktop_sequence_result(
                                auto_replan_recovery_requests,
                                timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            if direct_result:
                                assessment = self._record_goal_contract_assessment(
                                    goal_contract,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                if assessment is None or assessment.completed:
                                    return direct_result
                    if replan_payloads and not _auto_deferred_observed_ui_can_complete_without_model(
                        planned_tool_requests,
                        allowed_tools,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    ):
                        self._append_replan_followup_context(
                            replan_payloads,
                            allowed_tools=allowed_tools,
                            messages=messages,
                            timeline=timeline,
                            run_id=run_id,
                        )
                    if auto_followup_request:
                        auto_followup_request = (
                            _tool_request_with_desktop_execution_policy(
                                auto_followup_request,
                                desktop_execution_policy,
                            )
                            or {}
                        )
                        auto_payload = {
                            "tool": str(auto_followup_request.get("tool") or ""),
                            "status": "planned",
                            "source": str(auto_followup_request.get("source") or "runtime_planner"),
                            "planning_reason": str(
                                auto_followup_request.get("planning_reason")
                                or "planner_builtin_data_analysis"
                            ),
                            "input_preview": (
                                auto_followup_request.get("input")
                                if isinstance(auto_followup_request.get("input"), dict)
                                else {}
                            ),
                        }
                        auto_payload.update(
                            _desktop_execution_mode_payload(
                                str(auto_followup_request.get("tool") or ""),
                                auto_followup_request.get("input")
                                if isinstance(auto_followup_request.get("input"), Mapping)
                                else {},
                            )
                        )
                        for key in (
                            "step_id",
                            "capability_id",
                            "replan_request_id",
                            "replan_trigger",
                        ):
                            value = str(auto_followup_request.get(key) or "").strip()
                            if value:
                                auto_payload[key] = value
                        auto_payload.update(
                            _request_observability_metadata(auto_followup_request)
                        )
                        timeline.append(
                            self._timeline(
                                "agent.desktop.intent_planned",
                                str(auto_followup_request.get("tool") or ""),
                                **auto_payload,
                            )
                        )
                        if run_id and self._append_run_event is not None:
                            self._append_run_event(
                                run_id,
                                "agent.desktop.intent_planned",
                                auto_payload,
                            )
                        self._record_desktop_tool_policy_decisions(
                            [auto_followup_request],
                            allowed_tools=allowed_tools,
                            agent=agent,
                            run_id=run_id,
                        )
                        auto_tool_timeline_start = len(timeline)
                        try:
                            self._run_tool_requests(
                                [auto_followup_request],
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=start_iteration,
                                run_id=run_id,
                                budget=budget,
                            )
                        finally:
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                        followup_target = (
                            followup_selection_payload.get("followup_target")
                            if isinstance(
                                followup_selection_payload.get("followup_target"),
                                Mapping,
                            )
                            else {}
                        )
                        if (
                            _selection_payload_has_model_followup_target(
                                followup_selection_payload
                            )
                            and not _model_followup_artifact_target_completed(
                                followup_target,
                                timeline,
                            )
                        ):
                            auto_followup_request = {
                                **auto_followup_request,
                                "continue_to_model": True,
                            }
                            planned_tool_requests = [
                                *planned_tool_requests,
                                auto_followup_request,
                            ]
                        else:
                            direct_result = self._direct_daily_desktop_result(
                                agent,
                                "data.analyze",
                                (
                                    auto_followup_request.get("input")
                                    if isinstance(auto_followup_request.get("input"), dict)
                                    else {}
                                ),
                                timeline,
                                run_id=run_id,
                                source=str(auto_followup_request.get("source") or "runtime_planner"),
                                planning_reason=str(
                                    auto_followup_request.get("planning_reason")
                                    or "planner_builtin_data_analysis"
                                ),
                            )
                            if direct_result:
                                return direct_result
                    generated_deferred_observed_ui_requests = (
                        _auto_deferred_observed_ui_followup_requests(
                            planned_tool_requests,
                            allowed_tools,
                            timeline,
                        )
                    )
                    generated_deferred_observed_ui_requests = _drop_completed_auto_followup_prefix(
                        generated_deferred_observed_ui_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                    auto_deferred_observed_ui_requests = (
                        _drop_replayed_approved_replan_operate_requests(
                            generated_deferred_observed_ui_requests,
                            timeline,
                        )
                    )
                    if (
                        generated_deferred_observed_ui_requests
                        and _replayed_approved_replan_operate_requests_verified(
                            generated_deferred_observed_ui_requests,
                            timeline,
                        )
                        and all(
                            _replan_observed_action_verification_request(request)
                            for request in auto_deferred_observed_ui_requests
                        )
                    ):
                        verified_operate_request = next(
                            request
                            for request in generated_deferred_observed_ui_requests
                            if _replan_observed_action_operate_request(request)
                        )
                        direct_result = self._direct_daily_desktop_result(
                            agent,
                            str(verified_operate_request.get("tool") or ""),
                            (
                                dict(verified_operate_request.get("input"))
                                if isinstance(
                                    verified_operate_request.get("input"),
                                    Mapping,
                                )
                                else {}
                            ),
                            timeline,
                            run_id=run_id,
                            source=str(
                                verified_operate_request.get("source")
                                or "runtime_planner"
                            ),
                            planning_reason="planner_verified_approved_replan_action",
                        )
                        if direct_result:
                            return direct_result
                    auto_deferred_observed_ui_requests = (
                        _tool_requests_with_desktop_execution_policy(
                            auto_deferred_observed_ui_requests,
                            desktop_execution_policy,
                        )
                    )
                    if auto_deferred_observed_ui_requests:
                        self._record_auto_model_followup_app_write_plan(
                            auto_deferred_observed_ui_requests,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_permission_preflight(
                            auto_deferred_observed_ui_requests,
                            broker,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_tool_policy_decisions(
                            auto_deferred_observed_ui_requests,
                            allowed_tools=allowed_tools,
                            agent=agent,
                            run_id=run_id,
                        )
                        auto_tool_timeline_start = len(timeline)
                        try:
                            self._run_tool_requests(
                                auto_deferred_observed_ui_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=start_iteration,
                                run_id=run_id,
                                budget=budget,
                            )
                        except AgentApprovalRequired as exc:
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            pending_approval = (
                                exc.pending_approval
                                if isinstance(exc.pending_approval, dict)
                                else {}
                            )
                            planned_tool = str(
                                pending_approval.get("tool")
                                or auto_deferred_observed_ui_requests[0].get("tool")
                                or ""
                            )
                            approval_request = self._planned_request_for_tool(
                                auto_deferred_observed_ui_requests,
                                planned_tool,
                            )
                            planned_input = self._pending_approval_input_preview(
                                pending_approval,
                                approval_request,
                                (
                                    auto_deferred_observed_ui_requests[0]
                                    if auto_deferred_observed_ui_requests
                                    else {}
                                ),
                            )
                            self._record_desktop_intent_approval_required(
                                planned_tool,
                                planned_input,
                                pending_approval=exc.pending_approval,
                                timeline=timeline,
                                run_id=run_id,
                                planned_request=approval_request,
                                source=self._approval_event_source(
                                    approval_request,
                                    planned_tool,
                                ),
                                planning_reason=self._approval_event_planning_reason(
                                    approval_request,
                                    planned_tool,
                                ),
                            )
                            raise
                        self._record_runtime_planner_task_progress_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            tool_timeline_start=auto_tool_timeline_start,
                            run_id=run_id,
                        )
                        deferred_direct_result = (
                            self._run_deferred_observed_ui_replan_recovery(
                                runtime_planner_decision,
                                auto_deferred_observed_ui_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                agent=agent,
                                run_id=run_id,
                                budget=budget,
                                next_iteration=start_iteration,
                                tool_timeline_start=auto_tool_timeline_start,
                            )
                        )
                        if deferred_direct_result:
                            return deferred_direct_result
                        nested_deferred_observed_ui_requests = (
                            _auto_deferred_observed_ui_followup_requests(
                                auto_deferred_observed_ui_requests,
                                allowed_tools,
                                timeline,
                            )
                        )
                        nested_deferred_observed_ui_requests = _drop_completed_auto_followup_prefix(
                            nested_deferred_observed_ui_requests,
                            timeline,
                            tool_timeline_start=auto_tool_timeline_start,
                        )
                        nested_deferred_observed_ui_requests = (
                            _drop_replayed_approved_replan_operate_requests(
                                nested_deferred_observed_ui_requests,
                                timeline,
                            )
                        )
                        if nested_deferred_observed_ui_requests:
                            self._record_auto_model_followup_app_write_plan(
                                nested_deferred_observed_ui_requests,
                                timeline=timeline,
                                run_id=run_id,
                            )
                            self._record_desktop_permission_preflight(
                                nested_deferred_observed_ui_requests,
                                broker,
                                timeline=timeline,
                                run_id=run_id,
                            )
                            self._record_desktop_tool_policy_decisions(
                                nested_deferred_observed_ui_requests,
                                allowed_tools=allowed_tools,
                                agent=agent,
                                run_id=run_id,
                            )
                            nested_tool_timeline_start = len(timeline)
                            try:
                                self._run_tool_requests(
                                    nested_deferred_observed_ui_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                            except AgentApprovalRequired as exc:
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=nested_tool_timeline_start,
                                    run_id=run_id,
                                )
                                pending_approval = (
                                    exc.pending_approval
                                    if isinstance(exc.pending_approval, dict)
                                    else {}
                                )
                                planned_tool = str(
                                    pending_approval.get("tool")
                                    or nested_deferred_observed_ui_requests[0].get("tool")
                                    or ""
                                )
                                approval_request = self._planned_request_for_tool(
                                    nested_deferred_observed_ui_requests,
                                    planned_tool,
                                )
                                planned_input = self._pending_approval_input_preview(
                                    pending_approval,
                                    approval_request,
                                    (
                                        nested_deferred_observed_ui_requests[0]
                                        if nested_deferred_observed_ui_requests
                                        else {}
                                    ),
                                )
                                self._record_desktop_intent_approval_required(
                                    planned_tool,
                                    planned_input,
                                    pending_approval=exc.pending_approval,
                                    timeline=timeline,
                                    run_id=run_id,
                                    planned_request=approval_request,
                                    source=self._approval_event_source(
                                        approval_request,
                                        planned_tool,
                                    ),
                                    planning_reason=self._approval_event_planning_reason(
                                        approval_request,
                                        planned_tool,
                                    ),
                                )
                                raise
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=nested_tool_timeline_start,
                                run_id=run_id,
                            )
                            nested_deferred_direct_result = (
                                self._run_deferred_observed_ui_replan_recovery(
                                    runtime_planner_decision,
                                    nested_deferred_observed_ui_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    agent=agent,
                                    run_id=run_id,
                                    budget=budget,
                                    next_iteration=start_iteration,
                                    tool_timeline_start=nested_tool_timeline_start,
                                )
                            )
                            if nested_deferred_direct_result:
                                return nested_deferred_direct_result
                            direct_result = self._direct_daily_desktop_sequence_result(
                                nested_deferred_observed_ui_requests,
                                timeline,
                                tool_timeline_start=nested_tool_timeline_start,
                                run_id=run_id,
                            )
                            if direct_result:
                                return direct_result
                        if not _runtime_planner_scoped_observation_deferral(
                            planned_tool_requests
                        ):
                            direct_result = self._direct_daily_desktop_sequence_result(
                                auto_deferred_observed_ui_requests,
                                timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            if direct_result:
                                return direct_result
                    auto_discovered_app_candidates = _auto_discovered_followup_requests(
                        followup_selection_payload,
                        allowed_tools,
                        timeline,
                    )
                    auto_discovered_app_requests = _drop_completed_auto_followup_prefix(
                        auto_discovered_app_candidates,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                    if auto_discovered_app_candidates and not auto_discovered_app_requests:
                        direct_result = self._direct_daily_desktop_sequence_result(
                            auto_discovered_app_candidates,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if direct_result:
                            return direct_result
                    if auto_discovered_app_requests:
                        self._record_auto_model_followup_app_write_plan(
                            auto_discovered_app_requests,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_permission_preflight(
                            auto_discovered_app_requests,
                            broker,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_tool_policy_decisions(
                            auto_discovered_app_requests,
                            allowed_tools=allowed_tools,
                            agent=agent,
                            run_id=run_id,
                        )
                        auto_tool_timeline_start = len(timeline)
                        auto_discovered_app_approval_requests = auto_discovered_app_requests
                        try:
                            first_auto_approval_index = next(
                                (
                                    index
                                    for index, request in enumerate(
                                        auto_discovered_app_requests
                                    )
                                    if isinstance(request, Mapping)
                                    and bool(request.get("approval_required"))
                                ),
                                -1,
                            )
                            if first_auto_approval_index == 0:
                                approval_request = auto_discovered_app_requests[0]
                                approval_tool = str(
                                    approval_request.get("tool") or ""
                                ).strip()
                                approval_input = (
                                    dict(approval_request.get("input"))
                                    if isinstance(approval_request.get("input"), Mapping)
                                    else {}
                                )
                                approval_preview = dict(approval_input)
                                if approval_preview.get("app_name"):
                                    approval_preview.pop("query", None)
                                    approval_preview.pop("selection_source", None)
                                approval_id_source = (
                                    f"{run_id}:{approval_tool}:"
                                    f"{repr(sorted(approval_input.items()))}"
                                )
                                raise AgentApprovalRequired(
                                    {
                                        "approval_id": (
                                            "approval-"
                                            + hashlib.sha1(
                                                approval_id_source.encode("utf-8")
                                            ).hexdigest()[:12]
                                        ),
                                        "tool": approval_tool,
                                        "input": approval_input,
                                        "input_preview": approval_preview,
                                        "messages": list(messages),
                                        "tool_request": dict(approval_request),
                                        "remaining_tool_requests": [
                                            dict(item)
                                            for item in auto_discovered_app_requests[1:]
                                            if isinstance(item, Mapping)
                                        ],
                                        "next_iteration": self._normalize_tool_iteration(
                                            start_iteration + 1
                                        ),
                                    }
                                )
                            self._run_tool_requests(
                                auto_discovered_app_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=start_iteration,
                                run_id=run_id,
                                budget=budget,
                            )
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            auto_observed_result_requests = (
                                _auto_discovered_app_search_observed_result_requests(
                                    followup_selection_payload,
                                    allowed_tools,
                                    timeline,
                                )
                            )
                            if auto_observed_result_requests:
                                auto_discovered_app_approval_requests = [
                                    *auto_discovered_app_requests,
                                    *auto_observed_result_requests,
                                ]
                                self._record_auto_model_followup_app_write_plan(
                                    auto_observed_result_requests,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_permission_preflight(
                                    auto_observed_result_requests,
                                    broker,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                self._record_desktop_tool_policy_decisions(
                                    auto_observed_result_requests,
                                    allowed_tools=allowed_tools,
                                    agent=agent,
                                    run_id=run_id,
                                )
                                observed_tool_timeline_start = len(timeline)
                                self._run_tool_requests(
                                    auto_observed_result_requests,
                                    allowed_tools,
                                    broker,
                                    messages,
                                    timeline,
                                    artifacts,
                                    next_iteration=start_iteration,
                                    run_id=run_id,
                                    budget=budget,
                                )
                                self._record_runtime_planner_task_progress_events(
                                    runtime_planner_decision,
                                    timeline=timeline,
                                    tool_timeline_start=observed_tool_timeline_start,
                                    run_id=run_id,
                                )
                                auto_discovered_app_requests = [
                                    *_tool_requests_without_model_followup(
                                        auto_discovered_app_requests
                                    ),
                                    *auto_observed_result_requests,
                                ]
                        except AgentApprovalRequired as exc:
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            pending_approval = (
                                exc.pending_approval
                                if isinstance(exc.pending_approval, dict)
                                else {}
                            )
                            planned_tool = str(
                                pending_approval.get("tool")
                                or auto_discovered_app_approval_requests[0].get("tool")
                                or ""
                            )
                            approval_request = self._planned_request_for_tool(
                                auto_discovered_app_approval_requests,
                                planned_tool,
                            )
                            planned_input = self._pending_approval_input_preview(
                                pending_approval,
                                approval_request,
                                (
                                    auto_discovered_app_approval_requests[0]
                                    if auto_discovered_app_approval_requests
                                    else {}
                                ),
                            )
                            self._record_desktop_intent_approval_required(
                                planned_tool,
                                planned_input,
                                pending_approval=exc.pending_approval,
                                timeline=timeline,
                                run_id=run_id,
                                planned_request=approval_request,
                                source=self._approval_event_source(
                                    approval_request,
                                    planned_tool,
                                ),
                                planning_reason=self._approval_event_planning_reason(
                                    approval_request,
                                    planned_tool,
                                ),
                            )
                            raise
                        if any(
                            bool(request.get("continue_to_model"))
                            for request in auto_discovered_app_requests
                            if isinstance(request, dict)
                        ):
                            planned_tool_requests = [
                                *planned_tool_requests,
                                *auto_discovered_app_requests,
                            ]
                        else:
                            direct_result = self._direct_daily_desktop_sequence_result(
                                auto_discovered_app_requests,
                                timeline,
                                tool_timeline_start=auto_tool_timeline_start,
                                run_id=run_id,
                            )
                            if direct_result:
                                return direct_result
                    auto_code_context_requests = _auto_code_context_read_requests(
                        followup_selection_payload,
                        allowed_tools,
                        timeline,
                    )
                    auto_code_context_requests = _drop_completed_auto_followup_prefix(
                        auto_code_context_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                    if auto_code_context_requests:
                        auto_tool_timeline_start = len(timeline)
                        self._run_tool_requests(
                            auto_code_context_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            next_iteration=start_iteration,
                            run_id=run_id,
                            budget=budget,
                        )
                        self._record_runtime_planner_task_progress_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            tool_timeline_start=auto_tool_timeline_start,
                            run_id=run_id,
                        )
                        planned_tool_requests = [
                            *planned_tool_requests,
                            *auto_code_context_requests,
                        ]
                    if not replan_payloads:
                        if _runtime_planner_completed_exact_verified_discovery_action_chain(
                            execution_tool_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                        ):
                            direct_result = self._direct_daily_desktop_sequence_result(
                                execution_tool_requests,
                                timeline,
                                tool_timeline_start=tool_timeline_start,
                                run_id=run_id,
                            )
                            if direct_result:
                                return direct_result
                        self._append_model_followup_context(
                            planned_tool_requests,
                            followup_selection_payload,
                            allowed_tools=allowed_tools,
                            messages=messages,
                            timeline=timeline,
                            run_id=run_id,
                        )
                    direct_result = ""
                elif len(_tool_requests_without_model_followup(execution_tool_requests)) == 1:
                    completed_execution_tool_requests = _tool_requests_without_model_followup(
                        execution_tool_requests
                    )
                    planned_tool = str(completed_execution_tool_requests[0].get("tool") or "")
                    planned_input = completed_execution_tool_requests[0].get("input") or {}
                    presentation = str(
                        completed_execution_tool_requests[0].get("presentation") or ""
                    ).strip()
                    direct_result = self._direct_daily_desktop_result(
                        agent,
                        planned_tool,
                        planned_input,
                        timeline,
                        run_id=run_id,
                        presentation=presentation,
                        source=str(
                            completed_execution_tool_requests[0].get("source")
                            or "daily_desktop_intent"
                        ),
                        planning_reason=str(
                            completed_execution_tool_requests[0].get("planning_reason") or ""
                        ),
                    )
                else:
                    completed_execution_tool_requests = _tool_requests_without_model_followup(
                        execution_tool_requests
                    )
                    direct_result = self._direct_daily_desktop_sequence_result(
                        completed_execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                if (
                    not direct_result
                    and _runtime_planner_completed_exact_verified_discovery_action_chain(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    direct_result = self._direct_daily_desktop_sequence_result(
                        execution_tool_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                if (
                    not direct_result
                    and _tool_requests_have_unresolved_discovered_app(
                        execution_tool_requests
                    )
                    and not _timeline_has_model_followup_context(
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                ):
                    self._append_model_followup_context(
                        planned_tool_requests,
                        _selection_payload_with_timeline_fallback(
                            direct_tool_selection_payload,
                            timeline,
                        ),
                        allowed_tools=allowed_tools,
                        messages=messages,
                        timeline=timeline,
                        run_id=run_id,
                    )
                if direct_result:
                    return direct_result
            elif not planner_replan_only:
                direct_candidate = (
                    direct_tool_request
                    if isinstance(direct_tool_request, dict)
                    and str(direct_tool_request.get("tool") or "").strip()
                    else None
                )
                if direct_candidate:
                    unavailable_summary = self._record_unavailable_desktop_intent(
                        direct_candidate,
                        allowed_tools=allowed_tools,
                        messages=messages,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    if unavailable_summary:
                        return unavailable_summary
        if (
            planned_tool_requests
            and _tool_requests_have_unresolved_discovered_app(planned_tool_requests)
            and not _latest_message_is_model_followup_context(messages)
        ):
            self._append_model_followup_context(
                planned_tool_requests,
                _selection_payload_with_timeline_fallback(
                    direct_tool_selection_payload,
                    timeline,
                ),
                allowed_tools=allowed_tools,
                messages=messages,
                timeline=timeline,
                run_id=run_id,
            )
        model_config = self._agent_model_config_private(agent)
        base_url = str(model_config.get("base_url") or "").rstrip("/")
        model = str(model_config.get("model") or "").strip()
        api_key = str(model_config.get("api_key") or "").strip()
        if not base_url or not model or not api_key:
            raise self._error_type("Agent 模型 Profile 缺少 base_url、model 或 API Key")
        tools = self._tool_schemas(allowed_tools)
        for iteration in range(start_iteration, self._max_tool_iterations):
            self._check_context_budget(budget, messages)
            budget.claim_model_call()
            message = self._coalesce_model_message(
                self._call_model(base_url, model, api_key, messages, tools=tools, stream=True)
            )
            content = self._message_visible_content_text(message)
            tool_requests = [
                _runtime_model_tool_selection_request(request)
                for request in self._tool_requests_from_message(message, content)
                if isinstance(request, Mapping)
            ]
            tool_requests = _tool_requests_with_pending_plan_metadata(
                tool_requests,
                timeline,
                run_id=run_id,
            )
            tool_requests = runtime_execution_verified_tool_requests(
                tool_requests,
                allowed_tools,
                include_model_app_foreground=not (
                    _model_tool_requests_are_verification_recovery_actions(
                        tool_requests
                    )
                ),
            )
            tool_requests = _runtime_bind_unscoped_tools_to_goal_contract(
                tool_requests,
                contract=goal_contract,
                timeline=timeline,
                run_id=run_id,
                iteration=iteration,
                # The immediately preceding helper reconstructs requests from
                # the model protocol whitelist before attaching Runtime trace
                # metadata; model-authored authority fields are already gone.
                trusted_runtime_metadata=True,
            )
            tool_requests = self._bind_runtime_goal_recovery_lineage(
                tool_requests,
                followup_context=_latest_model_followup_context(
                    timeline,
                    include_pending_execution_only=True,
                ),
                contract=goal_contract,
                timeline=timeline,
                run_id=run_id,
            )
            tool_requests = _tool_requests_with_desktop_execution_policy(
                tool_requests,
                desktop_execution_policy,
            )
            detail = content[:500] if content else ", ".join(
                request["tool"] for request in tool_requests
            )[:500]
            timeline.append(
                self._timeline(
                    "agent.model.response",
                    detail,
                    run_id=run_id,
                )
            )
            if (
                runtime_execution_envelope_owned
                and blocked_runtime_execution_requests
                and not _runtime_execution_envelope_has_only_materialization_blockers(
                    runtime_execution_envelope,
                    blocked_runtime_execution_requests,
                )
            ):
                # Approval continuation may legitimately ask the model to
                # consume the approved result, but that model turn cannot
                # replace a still-blocked authoritative execution request
                # with a different tool or target.
                return self._record_blocked_runtime_execution_envelope(
                    runtime_execution_envelope,
                    blocked_runtime_execution_requests,
                    timeline=timeline,
                    run_id=run_id,
                )
            if not tool_requests:
                if not content.strip():
                    raise self._error_type("Native Agent 模型返回了空回复")
                followup_context = _latest_model_followup_context(
                    timeline,
                    include_pending_execution_only=True,
                )
                followup_target = _model_followup_context_target(followup_context)
                semantic_artifact_retry = _trusted_semantic_artifact_retry_context(
                    goal_contract,
                    followup_context,
                    timeline,
                )
                if not semantic_artifact_retry and _model_followup_artifact_target_completed(
                    followup_target,
                    timeline,
                ):
                    gated_output = self._goal_gated_model_output(
                        content,
                        message,
                        contract=goal_contract,
                        messages=messages,
                        timeline=timeline,
                        run_id=run_id,
                        base_url=base_url,
                        model=model,
                        api_key=api_key,
                        budget=budget,
                    )
                    if gated_output is not None:
                        return gated_output
                    continue
                auto_app_write_requests = _model_followup_app_write_requests(
                    content,
                    followup_target,
                    allowed_tools,
                    followup_context=followup_context,
                )
                canonical_materialized_requests = _model_followup_pending_plan_requests(
                    followup_context,
                    allowed_tools,
                    generated_content=content,
                    timeline=timeline,
                    allow_materialization_rebind=True,
                    semantic_artifact_retry=semantic_artifact_retry,
                )
                fresh_materialized_requests = _fresh_materialized_execution_bindings(
                    canonical_materialized_requests,
                    followup_context,
                    generated_content=content,
                    goal_contract=goal_contract,
                    run_id=run_id,
                )
                if fresh_materialized_requests:
                    auto_app_write_requests = fresh_materialized_requests
                else:
                    auto_app_write_requests = (
                        _model_followup_requests_with_pending_plan_metadata(
                            auto_app_write_requests,
                            followup_context,
                            trusted_request_metadata=True,
                        )
                    )
                if not semantic_artifact_retry:
                    auto_app_write_requests = _drop_completed_auto_followup_prefix(
                        auto_app_write_requests,
                        timeline,
                        tool_timeline_start=0,
                    )
                    auto_app_write_requests = _drop_completed_artifact_write_requests(
                        auto_app_write_requests,
                        artifacts,
                    )
                auto_app_write_requests = _tool_requests_with_desktop_execution_policy(
                    auto_app_write_requests,
                    desktop_execution_policy,
                )
                if auto_app_write_requests:
                    messages.append({"role": "assistant", "content": content})
                    self._record_auto_model_followup_app_write_plan(
                        auto_app_write_requests,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._record_desktop_permission_preflight(
                        auto_app_write_requests,
                        broker,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._record_desktop_tool_policy_decisions(
                        auto_app_write_requests,
                        allowed_tools=allowed_tools,
                        agent=agent,
                        run_id=run_id,
                    )
                    tool_timeline_start = len(timeline)
                    try:
                        self._run_tool_requests(
                            auto_app_write_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            next_iteration=iteration + 1,
                            run_id=run_id,
                            budget=budget,
                        )
                        discovered_app_write_requests = (
                            _model_followup_discovered_app_write_requests_after_discovery(
                                content,
                                followup_target,
                                allowed_tools,
                                timeline,
                            )
                        )
                        discovered_app_write_requests = (
                            _model_followup_requests_with_pending_plan_metadata(
                                discovered_app_write_requests,
                                followup_context,
                                trusted_request_metadata=True,
                            )
                        )
                        if discovered_app_write_requests:
                            discovered_app_write_requests = (
                                _tool_requests_with_desktop_execution_policy(
                                    discovered_app_write_requests,
                                    desktop_execution_policy,
                                )
                            )
                            self._record_auto_model_followup_app_write_plan(
                                discovered_app_write_requests,
                                timeline=timeline,
                                run_id=run_id,
                            )
                            self._record_desktop_permission_preflight(
                                discovered_app_write_requests,
                                broker,
                                timeline=timeline,
                                run_id=run_id,
                            )
                            self._record_desktop_tool_policy_decisions(
                                discovered_app_write_requests,
                                allowed_tools=allowed_tools,
                                agent=agent,
                                run_id=run_id,
                            )
                            self._run_tool_requests(
                                discovered_app_write_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=iteration + 1,
                                run_id=run_id,
                                budget=budget,
                            )
                            auto_app_write_requests = [
                                *auto_app_write_requests,
                                *discovered_app_write_requests,
                            ]
                    except AgentApprovalRequired as exc:
                        pending_approval = (
                            exc.pending_approval if isinstance(exc.pending_approval, dict) else {}
                        )
                        planned_tool = str(
                            pending_approval.get("tool")
                            or auto_app_write_requests[0].get("tool")
                            or ""
                        )
                        approval_request = self._planned_request_for_tool(
                            auto_app_write_requests,
                            planned_tool,
                        )
                        planned_input = self._pending_approval_input_preview(
                            pending_approval,
                            approval_request,
                            auto_app_write_requests[0] if auto_app_write_requests else {},
                        )
                        self._record_desktop_intent_approval_required(
                            planned_tool,
                            planned_input,
                            pending_approval=exc.pending_approval,
                            timeline=timeline,
                            run_id=run_id,
                            planned_request=approval_request,
                            source=self._approval_event_source(approval_request, planned_tool),
                            planning_reason=self._approval_event_planning_reason(
                                approval_request,
                                planned_tool,
                            ),
                        )
                        raise
                    auto_app_write_outcomes = self._tool_batch_outcome_results(
                        auto_app_write_requests,
                        timeline=timeline,
                        tool_timeline_start=tool_timeline_start,
                        model_config=model_config,
                        allowed_tools=allowed_tools,
                        broker=broker,
                        messages=messages,
                        artifacts=artifacts,
                        budget=budget,
                        iteration=iteration,
                        run_id=run_id,
                    )
                    self._raise_terminal_outcome_blocker(
                        auto_app_write_outcomes,
                        auto_app_write_requests,
                        include_failed=False,
                    )
                    terminal_recovery_output = self._terminal_recovery_output(
                        auto_app_write_outcomes,
                        goal_contract=goal_contract,
                        timeline=timeline,
                    )
                    if terminal_recovery_output and not (
                        fresh_materialized_requests
                        and _goal_contract_requires_semantic_artifact_assessment(
                            goal_contract
                        )
                    ):
                        return terminal_recovery_output
                    if self._outcome_results_require_model_turn(
                        auto_app_write_outcomes
                    ):
                        continue
                    if (
                        fresh_materialized_requests
                        and _goal_contract_requires_semantic_artifact_assessment(
                            goal_contract
                        )
                    ):
                        gated_output = self._goal_gated_model_output(
                            content,
                            message,
                            contract=goal_contract,
                            messages=messages,
                            timeline=timeline,
                            run_id=run_id,
                            base_url=base_url,
                            model=model,
                            api_key=api_key,
                            budget=budget,
                        )
                        if gated_output is not None:
                            return gated_output
                        continue
                    direct_result = self._direct_daily_desktop_sequence_result(
                        auto_app_write_requests,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        if _only_persists_model_generated_artifacts(
                            auto_app_write_requests
                        ):
                            gated_output = self._goal_gated_model_output(
                                content,
                                message,
                                contract=goal_contract,
                                messages=messages,
                                timeline=timeline,
                                run_id=run_id,
                                base_url=base_url,
                                model=model,
                                api_key=api_key,
                                budget=budget,
                            )
                        else:
                            gated_output = self._goal_gated_model_output(
                                str(direct_result),
                                message,
                                contract=goal_contract,
                                messages=messages,
                                timeline=timeline,
                                run_id=run_id,
                                base_url=base_url,
                                model=model,
                                api_key=api_key,
                                budget=budget,
                            )
                        if gated_output is not None:
                            return gated_output
                        continue
                else:
                    auto_pending_plan_requests = _model_followup_pending_plan_requests(
                        followup_context,
                        allowed_tools,
                        generated_content=content,
                        timeline=timeline,
                        allow_materialization_rebind=True,
                        semantic_artifact_retry=semantic_artifact_retry,
                    )
                    auto_pending_plan_requests = (
                        _fresh_materialized_execution_bindings(
                            auto_pending_plan_requests,
                            followup_context,
                            generated_content=content,
                            goal_contract=goal_contract,
                            run_id=run_id,
                        )
                    )
                    auto_pending_plan_requests = (
                        self._bind_runtime_goal_recovery_lineage(
                            auto_pending_plan_requests,
                            followup_context=followup_context,
                            contract=goal_contract,
                            timeline=timeline,
                            run_id=run_id,
                        )
                    )
                    if not semantic_artifact_retry:
                        auto_pending_plan_requests = _drop_completed_auto_followup_prefix(
                            auto_pending_plan_requests,
                            timeline,
                            tool_timeline_start=0,
                        )
                        auto_pending_plan_requests = _drop_completed_artifact_write_requests(
                            auto_pending_plan_requests,
                            artifacts,
                        )
                    auto_pending_plan_requests = _tool_requests_with_desktop_execution_policy(
                        auto_pending_plan_requests,
                        desktop_execution_policy,
                    )
                    if auto_pending_plan_requests:
                        messages.append({"role": "assistant", "content": content})
                        self._record_auto_model_followup_app_write_plan(
                            auto_pending_plan_requests,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_permission_preflight(
                            auto_pending_plan_requests,
                            broker,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        self._record_desktop_tool_policy_decisions(
                            auto_pending_plan_requests,
                            allowed_tools=allowed_tools,
                            agent=agent,
                            run_id=run_id,
                        )
                        tool_timeline_start = len(timeline)
                        try:
                            self._run_tool_requests(
                                auto_pending_plan_requests,
                                allowed_tools,
                                broker,
                                messages,
                                timeline,
                                artifacts,
                                next_iteration=iteration + 1,
                                run_id=run_id,
                                budget=budget,
                            )
                        except AgentApprovalRequired as exc:
                            self._record_runtime_planner_task_progress_events(
                                runtime_planner_decision,
                                timeline=timeline,
                                tool_timeline_start=tool_timeline_start,
                                run_id=run_id,
                            )
                            pending_approval = (
                                exc.pending_approval if isinstance(exc.pending_approval, dict) else {}
                            )
                            planned_tool = str(
                                pending_approval.get("tool")
                                or auto_pending_plan_requests[0].get("tool")
                                or ""
                            )
                            approval_request = self._planned_request_for_tool(
                                auto_pending_plan_requests,
                                planned_tool,
                            )
                            planned_input = self._pending_approval_input_preview(
                                pending_approval,
                                approval_request,
                                (
                                    auto_pending_plan_requests[0]
                                    if auto_pending_plan_requests
                                    else {}
                                ),
                            )
                            self._record_desktop_intent_approval_required(
                                planned_tool,
                                planned_input,
                                pending_approval=exc.pending_approval,
                                timeline=timeline,
                                run_id=run_id,
                                planned_request=approval_request,
                                source=self._approval_event_source(
                                    approval_request,
                                    planned_tool,
                                ),
                                planning_reason=self._approval_event_planning_reason(
                                    approval_request,
                                    planned_tool,
                                ),
                            )
                            raise
                        auto_pending_plan_outcomes = self._tool_batch_outcome_results(
                            auto_pending_plan_requests,
                            timeline=timeline,
                            tool_timeline_start=tool_timeline_start,
                            model_config=model_config,
                            allowed_tools=allowed_tools,
                            broker=broker,
                            messages=messages,
                            artifacts=artifacts,
                            budget=budget,
                            iteration=iteration,
                            run_id=run_id,
                        )
                        terminal_recovery_output = self._terminal_recovery_output(
                            auto_pending_plan_outcomes,
                            goal_contract=goal_contract,
                            timeline=timeline,
                        )
                        if terminal_recovery_output:
                            return terminal_recovery_output
                        self._record_runtime_planner_task_progress_events(
                            runtime_planner_decision,
                            timeline=timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if runtime_planner_decision is None:
                            self._record_model_followup_pending_plan_progress_events(
                                followup_context,
                                auto_pending_plan_requests,
                                timeline=timeline,
                                tool_timeline_start=tool_timeline_start,
                                run_id=run_id,
                            )
                            replan_payloads = (
                                self._record_model_followup_pending_plan_replan_events(
                                    followup_context,
                                    auto_pending_plan_requests,
                                    timeline=timeline,
                                    tool_timeline_start=tool_timeline_start,
                                    run_id=run_id,
                                )
                            )
                            if replan_payloads:
                                self._append_replan_followup_context(
                                    replan_payloads,
                                    allowed_tools=allowed_tools,
                                    messages=messages,
                                    timeline=timeline,
                                    run_id=run_id,
                                )
                                continue
                        self._raise_terminal_outcome_blocker(
                            auto_pending_plan_outcomes,
                            auto_pending_plan_requests,
                            include_failed=False,
                        )
                        if self._outcome_results_require_model_turn(
                            auto_pending_plan_outcomes
                        ):
                            continue
                        direct_result = self._direct_daily_desktop_sequence_result(
                            auto_pending_plan_requests,
                            timeline,
                            tool_timeline_start=tool_timeline_start,
                            run_id=run_id,
                        )
                        if direct_result:
                            if _only_persists_model_generated_artifacts(
                                auto_pending_plan_requests
                            ):
                                gated_output = self._goal_gated_model_output(
                                    content,
                                    message,
                                    contract=goal_contract,
                                    messages=messages,
                                    timeline=timeline,
                                    run_id=run_id,
                                    base_url=base_url,
                                    model=model,
                                    api_key=api_key,
                                    budget=budget,
                                )
                            else:
                                gated_output = self._goal_gated_model_output(
                                    str(direct_result),
                                    message,
                                    contract=goal_contract,
                                    messages=messages,
                                    timeline=timeline,
                                    run_id=run_id,
                                    base_url=base_url,
                                    model=model,
                                    api_key=api_key,
                                    budget=budget,
                                )
                            if gated_output is not None:
                                return gated_output
                            continue
                gated_output = self._goal_gated_model_output(
                    content,
                    message,
                    contract=goal_contract,
                    messages=messages,
                    timeline=timeline,
                    run_id=run_id,
                    base_url=base_url,
                    model=model,
                    api_key=api_key,
                    budget=budget,
                )
                if gated_output is not None:
                    return gated_output
                continue

            if tool_requests[0].get("protocol") == "tool_calls":
                messages.append(self._tool_loop_projection.assistant_message_for_history(message))
                stage_tool_result_messages(messages)
            else:
                messages.append({"role": "assistant", "content": content})
            followup_context = _latest_model_followup_context(timeline)
            tool_timeline_start = len(timeline)
            self._run_tool_requests(
                tool_requests,
                allowed_tools,
                broker,
                messages,
                timeline,
                artifacts,
                next_iteration=iteration + 1,
                run_id=run_id,
                budget=budget,
            )
            latest_recovery_assessment = assess_latest_tool_recovery(
                timeline,
                start_index=tool_timeline_start,
                allowed_tools=allowed_tools,
                attempt_lineage=recovery_attempt_lineage_from_timeline(timeline),
            )
            direct_partial_result = (
                self._direct_daily_desktop_sequence_result(
                    tool_requests,
                    timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                    allow_honest_partial=True,
                )
                if latest_recovery_assessment is not None
                and latest_recovery_assessment.preserves_partial_result
                else ""
            )
            model_tool_outcomes = self._tool_batch_outcome_results(
                tool_requests,
                timeline=timeline,
                tool_timeline_start=tool_timeline_start,
                model_config=model_config,
                allowed_tools=allowed_tools,
                broker=broker,
                messages=messages,
                artifacts=artifacts,
                budget=budget,
                iteration=iteration,
                run_id=run_id,
            )
            terminal_recovery_output = self._terminal_recovery_output(
                model_tool_outcomes,
                goal_contract=goal_contract,
                timeline=timeline,
            )
            if terminal_recovery_output:
                return terminal_recovery_output
            continuation_replan_payloads = (
                self._record_recovery_continuation_replan_events(
                    model_tool_outcomes,
                    timeline=timeline,
                    run_id=run_id,
                )
            )
            if continuation_replan_payloads and not any(
                result.disposition
                in {
                    OutcomeLoopDisposition.AWAIT_USER,
                    OutcomeLoopDisposition.FAILED,
                }
                for result in model_tool_outcomes
            ):
                if not _timeline_has_replan_followup_context(
                    timeline,
                    continuation_replan_payloads,
                ):
                    self._append_replan_followup_context(
                        continuation_replan_payloads,
                        allowed_tools=allowed_tools,
                        messages=messages,
                        timeline=timeline,
                        run_id=run_id,
                    )
                if not any(
                    result.disposition
                    in {
                        OutcomeLoopDisposition.REPLAN_MODEL,
                        OutcomeLoopDisposition.PARTIAL,
                    }
                    for result in model_tool_outcomes
                ):
                    continue
            partial_completion_candidates = [
                result
                for result in model_tool_outcomes
                if result.outcome.status is OutcomeStatus.PARTIAL
                and not (
                    result.recovery_action_result is not None
                    and result.recovery_action_result.reason
                    == "effectful_recovery_deferred_to_goal_loop"
                )
                and (
                    result.disposition is OutcomeLoopDisposition.PARTIAL
                    or (
                        result.disposition is OutcomeLoopDisposition.REPLAN_MODEL
                        and result.recovery_plan is not None
                        and result.recovery_plan.action == "resolve_entity_alias"
                    )
                )
            ]
            other_unresolved_outcomes = [
                result
                for result in model_tool_outcomes
                if (
                    self._outcome_results_require_model_turn((result,))
                    or result.disposition
                    in {
                        OutcomeLoopDisposition.AWAIT_USER,
                        OutcomeLoopDisposition.FAILED,
                    }
                )
                and result not in partial_completion_candidates
            ]
            if partial_completion_candidates and not other_unresolved_outcomes:
                if direct_partial_result:
                    # Entity-alias recovery is an optional enrichment of an
                    # already honest partial result only for legacy/unstructured
                    # calls. Structured Goal runs defer effectful recovery to
                    # the canonical model loop and are excluded above.
                    return direct_partial_result
            if followup_context:
                self._record_model_followup_pending_plan_progress_events(
                    followup_context,
                    tool_requests,
                    timeline=timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                )
                replan_payloads = self._record_model_followup_pending_plan_replan_events(
                    followup_context,
                    tool_requests,
                    timeline=timeline,
                    tool_timeline_start=tool_timeline_start,
                    run_id=run_id,
                )
                if replan_payloads:
                    self._append_replan_followup_context(
                        replan_payloads,
                        allowed_tools=allowed_tools,
                        messages=messages,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    continue
                self._raise_terminal_outcome_blocker(
                    model_tool_outcomes,
                    tool_requests,
                    include_failed=False,
                )
                auto_pending_continuation_requests = (
                    _model_followup_pending_plan_continuation_requests(
                        followup_context,
                        tool_requests,
                        allowed_tools,
                        timeline,
                        tool_timeline_start=tool_timeline_start,
                    )
                )
                auto_pending_continuation_requests = (
                    self._bind_runtime_goal_recovery_lineage(
                        auto_pending_continuation_requests,
                        followup_context=followup_context,
                        contract=goal_contract,
                        timeline=timeline,
                        run_id=run_id,
                    )
                )
                auto_pending_continuation_requests = (
                    _tool_requests_with_desktop_execution_policy(
                        auto_pending_continuation_requests,
                        desktop_execution_policy,
                    )
                )
                if auto_pending_continuation_requests:
                    self._record_auto_model_followup_app_write_plan(
                        auto_pending_continuation_requests,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._record_desktop_permission_preflight(
                        auto_pending_continuation_requests,
                        broker,
                        timeline=timeline,
                        run_id=run_id,
                    )
                    self._record_desktop_tool_policy_decisions(
                        auto_pending_continuation_requests,
                        allowed_tools=allowed_tools,
                        agent=agent,
                        run_id=run_id,
                    )
                    continuation_timeline_start = len(timeline)
                    try:
                        self._run_tool_requests(
                            auto_pending_continuation_requests,
                            allowed_tools,
                            broker,
                            messages,
                            timeline,
                            artifacts,
                            next_iteration=iteration + 1,
                            run_id=run_id,
                            budget=budget,
                        )
                    except AgentApprovalRequired as exc:
                        self._record_model_followup_pending_plan_progress_events(
                            followup_context,
                            auto_pending_continuation_requests,
                            timeline=timeline,
                            tool_timeline_start=continuation_timeline_start,
                            run_id=run_id,
                        )
                        pending_approval = (
                            exc.pending_approval if isinstance(exc.pending_approval, dict) else {}
                        )
                        planned_tool = str(
                            pending_approval.get("tool")
                            or auto_pending_continuation_requests[0].get("tool")
                            or ""
                        )
                        approval_request = self._planned_request_for_tool(
                            auto_pending_continuation_requests,
                            planned_tool,
                        )
                        planned_input = self._pending_approval_input_preview(
                            pending_approval,
                            approval_request,
                            (
                                auto_pending_continuation_requests[0]
                                if auto_pending_continuation_requests
                                else {}
                            ),
                        )
                        self._record_desktop_intent_approval_required(
                            planned_tool,
                            planned_input,
                            pending_approval=exc.pending_approval,
                            timeline=timeline,
                            run_id=run_id,
                            planned_request=approval_request,
                            source=self._approval_event_source(
                                approval_request,
                                planned_tool,
                            ),
                            planning_reason=self._approval_event_planning_reason(
                                approval_request,
                                planned_tool,
                            ),
                        )
                        raise
                    continuation_outcomes = self._tool_batch_outcome_results(
                        auto_pending_continuation_requests,
                        timeline=timeline,
                        tool_timeline_start=continuation_timeline_start,
                        model_config=model_config,
                        allowed_tools=allowed_tools,
                        broker=broker,
                        messages=messages,
                        artifacts=artifacts,
                        budget=budget,
                        iteration=iteration,
                        run_id=run_id,
                    )
                    terminal_recovery_output = self._terminal_recovery_output(
                        continuation_outcomes,
                        goal_contract=goal_contract,
                        timeline=timeline,
                    )
                    if terminal_recovery_output:
                        return terminal_recovery_output
                    self._record_model_followup_pending_plan_progress_events(
                        followup_context,
                        auto_pending_continuation_requests,
                        timeline=timeline,
                        tool_timeline_start=continuation_timeline_start,
                        run_id=run_id,
                    )
                    continuation_replan_payloads = (
                        self._record_model_followup_pending_plan_replan_events(
                            followup_context,
                            auto_pending_continuation_requests,
                            timeline=timeline,
                            tool_timeline_start=continuation_timeline_start,
                            run_id=run_id,
                        )
                    )
                    if continuation_replan_payloads:
                        self._append_replan_followup_context(
                            continuation_replan_payloads,
                            allowed_tools=allowed_tools,
                            messages=messages,
                            timeline=timeline,
                            run_id=run_id,
                        )
                        continue
                    self._raise_terminal_outcome_blocker(
                        continuation_outcomes,
                        auto_pending_continuation_requests,
                        include_failed=False,
                    )
                    if self._outcome_results_require_model_turn(
                        continuation_outcomes
                    ):
                        continue
                    direct_result = self._direct_daily_desktop_sequence_result(
                        auto_pending_continuation_requests,
                        timeline,
                        tool_timeline_start=continuation_timeline_start,
                        run_id=run_id,
                    )
                    if direct_result:
                        return direct_result
            else:
                self._raise_terminal_outcome_blocker(
                    model_tool_outcomes,
                    tool_requests,
                    include_failed=False,
                )
        artifact_completion = self._tool_loop_projection.artifact_completion(timeline, artifacts)
        if artifact_completion:
            goal_assessment = self._record_goal_contract_assessment(
                goal_contract,
                timeline=timeline,
                run_id=run_id,
            )
            if goal_contract is not None and not (
                goal_assessment is not None and goal_assessment.completed
            ):
                raise self._error_type(
                    "custom_api Agent 工具循环超过上限；"
                    f"{self._tool_loop_projection.loop_limit_detail(timeline)}"
                )
            timeline.append(
                self._timeline(
                    "agent.tool.loop_limit_completed",
                    "artifact.write completed before model final output",
                    artifact_paths=[
                        str(artifact.get("path") or "")
                        for artifact in artifacts
                        if artifact.get("kind") != "context" and str(artifact.get("path") or "").strip()
                    ],
                    loop_limit_detail=self._tool_loop_projection.loop_limit_detail(timeline),
                )
            )
            return artifact_completion
        raise self._error_type(
            "custom_api Agent 工具循环超过上限；"
            f"{self._tool_loop_projection.loop_limit_detail(timeline)}"
        )

    def _tool_batch_outcome_results(
        self,
        tool_requests: Sequence[Mapping[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        model_config: Mapping[str, Any],
        allowed_tools: list[str],
        broker: Any,
        messages: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        budget: Any,
        iteration: int,
        run_id: str,
    ) -> tuple[OutcomeLoopResult, ...]:
        """Route every terminal result in a model-owned batch through one loop."""

        return OutcomeLoopCoordinator().decide_terminal_batch(
            timeline=timeline,
            start_index=tool_timeline_start,
            run_id=run_id,
            allowed_tools=allowed_tools,
            planned_tool_call_ids=[
                str(request.get("tool_call_id") or "")
                for request in tool_requests
                if isinstance(request, Mapping)
            ],
            recovery_action=lambda assessment: self._execute_runtime_recovery_plan(
                assessment,
                model_config=model_config,
                allowed_tools=allowed_tools,
                broker=broker,
                messages=messages,
                timeline=timeline,
                artifacts=artifacts,
                budget=budget,
                iteration=iteration,
                run_id=run_id,
            ),
        )

    def _record_recovery_continuation_replan_events(
        self,
        results: Sequence[OutcomeLoopResult],
        *,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> list[dict[str, Any]]:
        """Promote one grounded recovery observation into a canonical replan.

        Recovery adapters can deliberately return ``CONTINUE_PLAN`` after a
        bounded discovery/inspection call.  That disposition is not success
        for the immutable root goal: the observation must be handed back to
        the planner/model loop.  Only an executor-owned observation that can
        be replayed against the adapter's attempt is admitted here.
        """

        existing_by_id = {
            str(payload.get("request_id") or "").strip(): copy_live_replan_payload(
                payload
            )
            for payload in _timeline_replan_request_payloads(timeline)
            if str(payload.get("request_id") or "").strip()
        }
        recorded: list[dict[str, Any]] = []
        seen_request_ids: set[str] = set()
        for result in results:
            payload = _runtime_recovery_continuation_replan_payload(
                result,
                timeline=timeline,
                run_id=run_id,
            )
            request_id = str(payload.get("request_id") or "").strip()
            if not request_id or request_id in seen_request_ids:
                continue
            seen_request_ids.add(request_id)
            existing = existing_by_id.get(request_id)
            if existing is not None:
                recorded.append(existing)
                continue
            event_payload = {**payload, "visibility": "internal"}
            timeline.append(
                self._timeline(
                    "agent.replan.requested",
                    str(payload.get("reason") or "recovery_observation_ready"),
                    payload=event_payload,
                    visibility="internal",
                )
            )
            self._append_internal_run_event(
                run_id,
                "agent.replan.requested",
                event_payload,
            )
            existing_by_id[request_id] = event_payload
            recorded.append(event_payload)
        return recorded

    @staticmethod
    def _terminal_recovery_output(
        results: Sequence[OutcomeLoopResult],
        *,
        goal_contract: GoalContract | None = None,
        timeline: Sequence[Mapping[str, Any]] = (),
    ) -> str:
        blocking_dispositions = {
            OutcomeLoopDisposition.AWAIT_USER,
            OutcomeLoopDisposition.REPLAN_MODEL,
            OutcomeLoopDisposition.PARTIAL,
            OutcomeLoopDisposition.FAILED,
        }
        if any(result.disposition in blocking_dispositions for result in results):
            return ""
        if any(
            result.disposition is OutcomeLoopDisposition.CONTINUE_PLAN
            and result.outcome.status is not OutcomeStatus.SUCCESS
            for result in results
        ):
            return ""
        terminal_output = next(
            (
                result.terminal_output
                for result in results
                if result.disposition is OutcomeLoopDisposition.COMPLETED
                and result.terminal_output
            ),
            "",
        )
        if (
            terminal_output
            and goal_contract is not None
            and not runtime_goal_assessment(goal_contract, timeline).completed
        ):
            return ""
        return terminal_output

    def _consumer_tool_outcome_message(
        self,
        result: OutcomeLoopResult,
        source_request: Mapping[str, Any],
        *,
        default: str,
    ) -> str:
        """Project structured tool failure facts instead of raw provider copy."""

        raw_outcome = result.outcome.raw
        raw_payload = dict(raw_outcome) if isinstance(raw_outcome, Mapping) else {}
        input_preview = (
            dict(source_request.get("input"))
            if isinstance(source_request.get("input"), Mapping)
            else {}
        )
        consumer_summary = self._daily_desktop_summary(
            result.outcome.tool_name,
            input_preview,
            raw_payload,
        )
        if consumer_summary:
            return consumer_summary
        raw_data = (
            raw_payload.get("data")
            if isinstance(raw_payload.get("data"), Mapping)
            else {}
        )
        return str(
            raw_data.get("error")
            or raw_payload.get("error")
            or default
        ).strip()

    @staticmethod
    def _outcome_results_require_model_turn(
        results: Sequence[OutcomeLoopResult],
    ) -> bool:
        return any(
            result.disposition
            in {
                OutcomeLoopDisposition.REPLAN_MODEL,
                OutcomeLoopDisposition.PARTIAL,
                OutcomeLoopDisposition.FAILED,
            }
            or (
                result.disposition is OutcomeLoopDisposition.CONTINUE_PLAN
                and result.outcome.status is not OutcomeStatus.SUCCESS
            )
            for result in results
        )

    @staticmethod
    def _raise_terminal_outcome_blocker(
        results: Sequence[OutcomeLoopResult],
        tool_requests: Sequence[Mapping[str, Any]],
        *,
        include_failed: bool = True,
    ) -> None:
        dispositions = [OutcomeLoopDisposition.AWAIT_USER]
        if include_failed:
            dispositions.append(OutcomeLoopDisposition.FAILED)
        blocker = next(
            (
                result
                for disposition in dispositions
                for result in results
                if result.disposition is disposition
            ),
            None,
        )
        if blocker is None:
            return
        source_request = next(
            (
                request
                for request in tool_requests
                if str(request.get("tool_call_id") or "").strip()
                == blocker.source_tool_call_id
            ),
            {},
        )
        if blocker.disposition is OutcomeLoopDisposition.AWAIT_USER:
            message = "此操作需要用户授权或补充操作后才能继续。"
        else:
            message = "工具执行未完成，请检查输入或稍后重试。"
        raise AgentDirectOutcomeUnverified(
            message,
            reason=blocker.reason or "tool_error",
            tool_name=blocker.outcome.tool_name,
            input_preview=(
                dict(source_request.get("input"))
                if isinstance(source_request.get("input"), Mapping)
                else {}
            ),
            tool_call_id=blocker.source_tool_call_id,
        )

    def _execute_runtime_recovery_plan(
        self,
        assessment: RecoveryAssessment,
        *,
        model_config: Mapping[str, Any] | Callable[[], Mapping[str, Any]],
        allowed_tools: list[str],
        broker: Any,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        budget: Any,
        iteration: int,
        run_id: str,
    ) -> RecoveryActionResult:
        """Resolve and execute one bounded plan through the adapter registry."""

        plan = assessment.plan
        if plan is None:
            return RecoveryActionResult.not_handled(reason="recovery_plan_unavailable")
        goal_contract = runtime_goal_contract(
            run_id=run_id,
            runtime_execution_envelope=None,
            runtime_execution_metadata=None,
            messages=messages,
            timeline=timeline,
        )
        goal_subgoal = None
        goal_root_identity: dict[str, str] = {}
        goal_bounded_effectful_tools: tuple[str, ...] = ()
        structured_goal = bool(
            goal_contract is not None
            and any(
                criterion.required and not criterion.response_satisfiable
                for criterion in goal_contract.criteria
            )
        )
        if structured_goal and goal_contract is not None:
            goal_assessment = runtime_goal_assessment(goal_contract, timeline)
            outcome_capabilities = set(assessment.outcome.capabilities)
            unsatisfied_criterion_ids = set(
                goal_assessment.unsatisfied_criterion_ids
            )
            bound_criterion = next(
                (
                    criterion
                    for criterion in goal_contract.criteria
                    if criterion.criterion_id
                    in unsatisfied_criterion_ids
                    and bool(criterion.required_capabilities)
                    and set(criterion.required_capabilities).issubset(
                        outcome_capabilities
                    )
                    and any(
                        evidence.kind == "tool_outcome"
                        and evidence.criterion_id == criterion.criterion_id
                        and evidence.source_tool_call_id == assessment.tool_call_id
                        and evidence.status == assessment.outcome.status.value
                        for evidence in goal_assessment.evidence
                    )
                ),
                None,
            )
            goal_root_identity = (
                _runtime_direct_recovery_root_identity(
                    timeline,
                    run_id=run_id,
                    source_tool_call_id=assessment.tool_call_id,
                    source_tool_name=assessment.outcome.tool_name,
                    source_result=assessment.outcome.raw,
                    source_step_ids=bound_criterion.source_step_ids,
                )
                if bound_criterion is not None
                else {}
            )
            exact_background_tools = background_window_runtime_recovery_tools(
                assessment.outcome
            )
            if not goal_root_identity and tuple(exact_background_tools) == (
                _RUNTIME_GOAL_BOUNDED_BACKGROUND_WINDOW_TOOLS
            ):
                dependency_bindings: list[
                    tuple[Any, dict[str, str]]
                ] = []
                recovery_capabilities = set(plan.required_capabilities)
                for criterion in goal_contract.criteria:
                    if (
                        criterion.criterion_id not in unsatisfied_criterion_ids
                        or not criterion.required_capabilities
                        or not set(criterion.required_capabilities).issubset(
                            recovery_capabilities
                        )
                    ):
                        continue
                    root_identity = _runtime_direct_recovery_root_identity(
                        timeline,
                        run_id=run_id,
                        source_tool_call_id=assessment.tool_call_id,
                        source_tool_name=assessment.outcome.tool_name,
                        source_result=assessment.outcome.raw,
                        source_step_ids=criterion.source_step_ids,
                        allow_planner_dependency_ancestor=True,
                        required_target_capabilities=criterion.required_capabilities,
                    )
                    if root_identity:
                        dependency_bindings.append((criterion, root_identity))
                if len(dependency_bindings) == 1:
                    bound_criterion, goal_root_identity = dependency_bindings[0]
            if bound_criterion is None:
                return RecoveryActionResult.not_handled(
                    reason="goal_criterion_unmatched"
                )
            if not goal_root_identity:
                return RecoveryActionResult.not_handled(
                    reason="goal_root_lineage_unavailable"
                )
            goal_assessment, goal_subgoal = GoalCoordinator().open_subgoal(
                goal_contract,
                goal_assessment,
                criterion_id=bound_criterion.criterion_id,
                action=plan.action,
                description=(
                    f"{plan.action}: {plan.recovery_hint}; then return to the root criterion"
                ),
                source_tool_call_id=assessment.tool_call_id,
            )
            if goal_subgoal is None:
                return RecoveryActionResult.not_handled(
                    reason="goal_subgoal_budget_exhausted"
                )
            plan = replace(
                plan,
                metadata={
                    **dict(plan.metadata),
                    "goal_contract_id": goal_contract.contract_id,
                    "goal_criterion_id": bound_criterion.criterion_id,
                    "goal_subgoal_id": goal_subgoal.subgoal_id,
                    "root_goal_unchanged": True,
                    **goal_root_identity,
                },
            )
            assessment = replace(assessment, plan=plan)
            if (
                plan.action == "materialize_background_window"
                and plan.strategy_id == "materialize-background-window"
                and plan.recovery_hint == "materialize_background_window"
                and tuple(exact_background_tools)
                == _RUNTIME_GOAL_BOUNDED_BACKGROUND_WINDOW_TOOLS
                and goal_subgoal.action == plan.action
                and str(plan.metadata.get("goal_contract_id") or "").strip()
                == goal_contract.contract_id
                and str(plan.metadata.get("goal_criterion_id") or "").strip()
                == bound_criterion.criterion_id
                and str(plan.metadata.get("goal_subgoal_id") or "").strip()
                == goal_subgoal.subgoal_id
                and plan.metadata.get("root_goal_unchanged") is True
                and all(goal_root_identity.values())
            ):
                goal_bounded_effectful_tools = tuple(exact_background_tools)
        recovery_scope_tools = frozenset(
            (*allowed_tools, *goal_bounded_effectful_tools)
        )
        context = RecoveryActionContext(
            plan=plan,
            source_outcome=assessment.outcome,
            source_tool_call_id=assessment.tool_call_id,
            scope=RecoveryActionScope(
                allowed_tools=recovery_scope_tools,
                iteration=iteration,
            ),
            runtime=_CustomApiRecoveryRuntimePort(
                owner=self,
                model_config=model_config,
                allowed_tools=allowed_tools,
                broker=broker,
                messages=messages,
                timeline=timeline,
                artifacts=artifacts,
                budget=budget,
                run_id=run_id,
                source_tool_call_id=assessment.tool_call_id,
                source_tool_name=assessment.outcome.tool_name,
                recovery_action=plan.action,
                recovery_scope_id=plan.scope_id,
                recovery_goal_identity={
                    key: plan.metadata.get(key)
                    for key in (
                        "goal_contract_id",
                        "goal_criterion_id",
                        "goal_subgoal_id",
                        "root_goal_unchanged",
                        "root_plan_id",
                        "root_source_step_id",
                    )
                },
                observation_only=(
                    structured_goal and not goal_bounded_effectful_tools
                ),
                goal_bounded_effectful_tools=goal_bounded_effectful_tools,
            ),
        )
        resolved = self._recovery_action_registry.resolve(context)
        if resolved is None:
            return RecoveryActionResult.not_handled(reason="no_unique_adapter")
        goal_bounded_effectful_adapter = bool(
            goal_bounded_effectful_tools
            and resolved.execution_mode
            is RecoveryActionExecutionMode.GOAL_BOUNDED_EFFECTFUL
            and resolved.adapter_type is BackgroundWindowRecoveryAdapter
        )
        if structured_goal and not (
            resolved.execution_mode is RecoveryActionExecutionMode.OBSERVATION_ONLY
            or goal_bounded_effectful_adapter
        ):
            # Recovery may gather bounded evidence for the canonical loop, but
            # it may not run a second effectful executor or model-selected
            # retry beneath an immutable GoalContract.  The ordinary outcome
            # path maps NOT_HANDLED to REPLAN_MODEL and the main loop owns the
            # next tool choice, target binding, execution, and verification.
            return RecoveryActionResult.not_handled(
                reason="effectful_recovery_deferred_to_goal_loop"
            )
        ownership = resolve_legacy_replan_ownership(
            timeline,
            source_tool_call_id=assessment.tool_call_id,
            source_tool_name=assessment.outcome.tool_name,
            plan=plan,
        )
        if ownership.status is RecoveryOwnershipStatus.ALREADY_HANDLED:
            completed_batch = _completed_legacy_recovery_batch(
                timeline,
                request_id=ownership.request_id,
                action_id=ownership.action_id,
            )
            if completed_batch is not None:
                reconciled = resolved.reconcile_completed_attempt(completed_batch)
                if reconciled.disposition is not RecoveryActionDisposition.NOT_HANDLED:
                    return reconciled
        if not ownership.coordinator_may_execute:
            return RecoveryActionResult.not_handled(reason="legacy_replan_owns_source")
        if goal_subgoal is not None and goal_contract is not None:
            serialized_subgoal = goal_subgoal.to_payload()
            subgoal_payload = {
                "subgoal": serialized_subgoal,
                "subgoal_json": json.dumps(
                    serialized_subgoal,
                    ensure_ascii=False,
                    separators=(",", ":"),
                    sort_keys=True,
                ),
                "contract_id": goal_contract.contract_id,
                "criterion_id": goal_subgoal.criterion_id,
                "source_tool_call_id": assessment.tool_call_id,
                **goal_root_identity,
                "run_id": run_id,
                "status": "opened",
                "source": "runtime_goal_coordinator",
                "actor": "native_runtime",
                "execution_authority": "runtime_goal_coordinator",
                "visibility": "internal",
            }
            timeline.append(
                self._timeline(
                    "agent.goal.subgoal.opened",
                    goal_subgoal.action,
                    **subgoal_payload,
                )
            )
            self._append_internal_run_event(
                run_id,
                "agent.goal.subgoal.opened",
                subgoal_payload,
            )
        payload = {
            "strategy_id": plan.strategy_id,
            "action": plan.action,
            "recovery_hint": plan.recovery_hint,
            "required_capabilities": list(plan.required_capabilities),
            "source_status": plan.source_status.value,
            "source_reason": plan.source_reason,
            "source_tool_name": assessment.outcome.tool_name,
            "source_tool_call_id": assessment.tool_call_id,
            "scope_id": plan.scope_id,
            "recovery_owner": "coordinator",
            "visibility": "internal",
            **(
                {
                    "goal_contract_id": goal_contract.contract_id,
                    "goal_criterion_id": goal_subgoal.criterion_id,
                    "goal_subgoal_id": goal_subgoal.subgoal_id,
                    "root_goal_unchanged": True,
                }
                if goal_subgoal is not None and goal_contract is not None
                else {}
            ),
            **ownership.event_fields(),
        }

        def append_lifecycle_event(
            event_type: str,
            status: str,
            **extra: Any,
        ) -> None:
            event_payload = {**payload, "status": status, **extra}
            timeline.append(
                self._timeline(
                    event_type,
                    plan.action,
                    **event_payload,
                )
            )
            if run_id:
                self._append_internal_run_event(
                    run_id,
                    event_type,
                    event_payload,
                )

        append_lifecycle_event("agent.recovery.planned", "claimed")
        try:
            result = resolved.execute()
        except BaseException as exc:
            try:
                append_lifecycle_event(
                    "agent.recovery.failed",
                    "failed",
                    error_type=type(exc).__name__,
                )
            except BaseException:
                # Recovery journaling must never replace the original control/error signal.
                pass
            raise
        result_fields = {
            "result_disposition": result.disposition.value,
            "result_reason": result.reason,
            "attempt_count": len(result.attempts),
        }
        if result.disposition is RecoveryActionDisposition.EXECUTION_FAILED:
            append_lifecycle_event(
                "agent.recovery.failed",
                "failed",
                **result_fields,
            )
        elif result.disposition is RecoveryActionDisposition.NOT_HANDLED:
            append_lifecycle_event(
                "agent.recovery.skipped",
                "not_handled",
                **result_fields,
            )
        else:
            append_lifecycle_event(
                "agent.recovery.completed",
                "completed",
                **result_fields,
            )
        return result

    @staticmethod
    def _direct_daily_desktop_tool_request(
        tool_request: dict[str, Any] | None,
        allowed_tools: list[str],
    ) -> dict[str, Any] | None:
        if not isinstance(tool_request, dict):
            return None
        tool_name = str(tool_request.get("tool") or "").strip()
        if not tool_name:
            return None
        allowed = {str(tool or "").strip() for tool in allowed_tools}
        if tool_name not in allowed:
            return None
        payload = tool_request.get("input") if isinstance(tool_request.get("input"), dict) else {}
        return {
            **tool_request,
            "tool": tool_name,
            "input": dict(payload),
        }

    @classmethod
    def _direct_daily_desktop_tool_requests(
        cls,
        tool_requests: list[dict[str, Any]] | None,
        allowed_tools: list[str],
    ) -> list[dict[str, Any]]:
        if not isinstance(tool_requests, list):
            return []
        cleaned: list[dict[str, Any]] = []
        for tool_request in tool_requests:
            cleaned_request = cls._direct_daily_desktop_tool_request(
                tool_request,
                allowed_tools,
            )
            if cleaned_request:
                cleaned.append(cleaned_request)
        if any(
            str(request.get("planning_reason") or "").strip() == "explicit_full_plan"
            for request in cleaned
        ):
            return cleaned
        if _preserve_direct_daily_desktop_tool_requests(cleaned):
            return cleaned
        requests = planner_execution_tool_requests(cleaned, allowed_tools)
        if any(
            str(request.get("planning_reason") or "").strip() == "explicit_full_plan"
            for request in requests
        ):
            return _drop_trailing_daily_desktop_verify_requests(requests) or requests
        return requests

    @staticmethod
    def _auto_data_analysis_request_from_discovery(
        planned_tool_requests: list[dict[str, Any]],
        allowed_tools: list[str],
        selection_payload: dict[str, Any],
        timeline: list[dict[str, Any]],
    ) -> dict[str, Any] | None:
        allowed = {str(tool or "").strip() for tool in allowed_tools}
        if "data.analyze" not in allowed:
            return None
        if not any(
            str(request.get("planning_reason") or "").strip() == "planner_prefetch_data_source"
            and bool(request.get("continue_to_model"))
            for request in planned_tool_requests
            if isinstance(request, dict)
        ):
            return None
        prefetch_tools = {
            str(request.get("tool") or "").strip()
            for request in planned_tool_requests
            if isinstance(request, dict) and bool(request.get("continue_to_model"))
        }
        if "workspace.list" in prefetch_tools:
            latest_list = _latest_workspace_list_event(timeline)
            if latest_list is None:
                return None
            result = latest_list.get("result") if isinstance(latest_list.get("result"), dict) else {}
            entries = result.get("entries")
            data_entries: list[Mapping[str, Any]] = []
            if isinstance(entries, list):
                data_entries = [
                    entry
                    for entry in entries
                    if isinstance(entry, dict)
                    and str(entry.get("type") or "").strip() == "file"
                    and _data_analysis_file_kind(
                        str(entry.get("name") or entry.get("path") or "")
                    )
                    != ""
                ]
            if result.get("ok") is not True or not data_entries:
                return None
            list_input = (
                latest_list.get("input_preview")
                if isinstance(latest_list.get("input_preview"), dict)
                else {}
            )
            base_path = str(list_input.get("path") or result.get("path") or "").strip()
            artifact_paths = _string_list(selection_payload.get("artifacts_expected"))
            if not artifact_paths:
                artifact_paths = ["analysis-report.md"]
            selection = str(list_input.get("selection") or "").strip().lower()
            input_payload = _data_analysis_input_from_workspace_entries(
                base_path,
                data_entries,
                selection,
                artifact_paths,
            )
            if not input_payload:
                return None
            if len(artifact_paths) > 1:
                input_payload["artifact_paths"] = artifact_paths
            return _first_annotated_auto_followup_request(
                {
                    "protocol": "json_fallback",
                    "tool": "data.analyze",
                    "input": input_payload,
                    "source": "runtime_planner",
                    "planning_reason": "planner_builtin_data_analysis",
                },
                selection_payload,
            )
        return _auto_data_analysis_request_from_captured_content(
            planned_tool_requests,
            selection_payload,
            timeline,
        )

    def _append_model_followup_context(
        self,
        planned_tool_requests: list[dict[str, Any]],
        selection_payload: dict[str, Any],
        *,
        allowed_tools: list[str],
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        run_id: str = "",
    ) -> None:
        payload = _model_followup_context_payload(
            planned_tool_requests,
            selection_payload,
            allowed_tools=allowed_tools,
            timeline=timeline,
        )
        if not payload:
            return
        payload = {**payload, "visibility": "internal"}
        messages.append(
            {
                "role": "user",
                "content": _model_followup_context_message(payload),
            }
        )
        timeline.append(
            self._timeline(
                "agent.model.followup_context",
                str(payload.get("planning_reason") or "model_followup"),
                **payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_internal_run_event(
                run_id,
                "agent.model.followup_context",
                payload,
            )

    def _append_replan_followup_context(
        self,
        replan_payloads: list[dict[str, Any]],
        *,
        allowed_tools: list[str],
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        run_id: str = "",
    ) -> None:
        payload = _model_replan_followup_context_payload(
            replan_payloads,
            allowed_tools=allowed_tools,
            timeline=timeline,
        )
        if not payload:
            return
        payload = {**payload, "visibility": "internal"}
        messages.append(
            {
                "role": "user",
                "content": _model_replan_followup_context_message(payload),
            }
        )
        timeline.append(
            self._timeline(
                "agent.model.followup_context",
                str(payload.get("planning_reason") or "planner_replan_after_tool_failure"),
                **payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_internal_run_event(
                run_id,
                "agent.model.followup_context",
                payload,
            )

    def _append_internal_run_event(
        self,
        run_id: str,
        event_type: str,
        payload: dict[str, Any],
    ) -> None:
        if not run_id or self._append_run_event is None:
            return
        internal_payload = {**payload, "visibility": "internal"}
        try:
            self._append_run_event(
                run_id,
                event_type,
                internal_payload,
                visibility="internal",
            )
        except TypeError as exc:
            # Lightweight test/legacy adapters may still expose the old
            # three-argument callback. Keep the payload fail-closed there too.
            if "visibility" not in str(exc) or "keyword" not in str(exc):
                raise
            self._append_run_event(run_id, event_type, internal_payload)

    def _bind_runtime_goal_recovery_lineage(
        self,
        requests: list[dict[str, Any]],
        *,
        followup_context: Mapping[str, Any],
        contract: GoalContract | None,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> list[dict[str, Any]]:
        """Bind a verification-repair tail back to one immutable criterion."""

        if not requests or contract is None or contract.run_id != str(run_id or "").strip():
            return requests
        lineage = _runtime_goal_verification_recovery_lineage(
            followup_context,
            contract=contract,
            timeline=timeline,
            run_id=run_id,
        )
        if not lineage:
            return requests
        assessment = runtime_goal_assessment(contract, timeline)
        coordinator = GoalCoordinator()
        subgoal = next(
            (
                item
                for item in assessment.subgoals
                if item.criterion_id == lineage["criterion_id"]
                and item.source_tool_call_id
                == lineage["failed_verifier_tool_call_id"]
                and item.action == lineage["recovery_action"]
            ),
            None,
        )
        if subgoal is None:
            _next_assessment, subgoal = coordinator.open_subgoal(
                contract,
                assessment,
                criterion_id=lineage["criterion_id"],
                action=lineage["recovery_action"],
                description="Repair the root action after its declared verifier failed.",
                source_tool_call_id=lineage["failed_verifier_tool_call_id"],
            )
            if subgoal is None:
                return requests
            subgoal_payload = subgoal.to_payload()
            opened_payload = {
                "subgoal": subgoal_payload,
                "subgoal_json": json.dumps(
                    subgoal_payload,
                    ensure_ascii=False,
                    separators=(",", ":"),
                    sort_keys=True,
                ),
                "contract_id": contract.contract_id,
                "criterion_id": lineage["criterion_id"],
                "source_tool_call_id": lineage["failed_verifier_tool_call_id"],
                "recovery_origin_tool_call_id": lineage[
                    "failed_verifier_tool_call_id"
                ],
                "root_source_tool_call_id": lineage["root_source_tool_call_id"],
                "root_source_step_id": lineage["root_source_step_id"],
                "root_verifier_step_id": lineage["root_verifier_step_id"],
                "root_plan_id": lineage["root_plan_id"],
                "root_provider_kind": lineage["root_provider_kind"],
                "root_provider_id": lineage["root_provider_id"],
                "run_id": run_id,
                "status": "opened",
                "source": "runtime_goal_coordinator",
                "actor": "native_runtime",
                "execution_authority": "runtime_goal_coordinator",
                "visibility": "internal",
            }
            timeline.append(
                self._timeline(
                    "agent.goal.subgoal.opened",
                    subgoal.subgoal_id,
                    **opened_payload,
                )
            )
            self._append_internal_run_event(
                run_id,
                "agent.goal.subgoal.opened",
                opened_payload,
            )
        recovery_scope_id = "recovery-scope-" + hashlib.sha256(
            (
                f"{contract.contract_id}|{subgoal.subgoal_id}|"
                f"{lineage['root_source_tool_call_id']}|"
                f"{lineage['failed_verifier_tool_call_id']}"
            ).encode("utf-8")
        ).hexdigest()[:24]
        common = {
            "recovery_link_kind": "coordinator_action",
            "root_goal_unchanged": True,
            "goal_contract_id": contract.contract_id,
            "goal_criterion_id": lineage["criterion_id"],
            "goal_subgoal_id": subgoal.subgoal_id,
            "recovery_action": subgoal.action,
            "recovery_scope_id": recovery_scope_id,
            "replan_recovery_identity": recovery_scope_id,
            "recovery_source_tool": lineage["failed_verifier_tool"],
            "recovery_origin_tool_call_id": lineage[
                "failed_verifier_tool_call_id"
            ],
            "root_source_tool_call_id": lineage["root_source_tool_call_id"],
            "root_source_step_id": lineage["root_source_step_id"],
            "root_verifier_step_id": lineage["root_verifier_step_id"],
            "root_plan_id": lineage["root_plan_id"],
            "source": "runtime_internal_recovery",
        }
        prepared = [dict(request) for request in requests]
        for item in prepared:
            item.pop("recovery_context_trusted", None)
            item.pop(RUNTIME_PRIVATE_RECOVERY_CONTEXT_KEY, None)
            if _request_matches_runtime_goal_recovery_lineage(item, lineage):
                ensure_tool_call_id(item)
        call_id_by_step = {
            str(item.get("step_id") or item.get("planner_step_id") or "").strip(): str(
                item.get("tool_call_id") or ""
            ).strip()
            for item in prepared
            if str(item.get("step_id") or item.get("planner_step_id") or "").strip()
            and str(item.get("tool_call_id") or "").strip()
        }
        bound: list[dict[str, Any]] = []
        for item in prepared:
            if not _request_matches_runtime_goal_recovery_lineage(item, lineage):
                bound.append(item)
                continue
            tool_name = str(item.get("tool") or item.get("tool_name") or "").strip()
            item.update(common)
            item["plan_id"] = lineage["root_plan_id"]
            item["source_step_id"] = lineage["root_source_step_id"]
            item["recovery_suggested_tool"] = tool_name
            dependency_call_id = next(
                (
                    call_id_by_step.get(str(step_id or "").strip(), "")
                    for step_id in item.get("depends_on", [])
                    if call_id_by_step.get(str(step_id or "").strip(), "")
                ),
                "",
            )
            item["source_tool_call_id"] = (
                dependency_call_id or lineage["failed_verifier_tool_call_id"]
            )
            if item.get("approval_required") is not True:
                item[RUNTIME_PRIVATE_RECOVERY_CONTEXT_KEY] = {
                    "version": RUNTIME_PRIVATE_RECOVERY_CONTEXT_VERSION,
                    "_authority": RUNTIME_PRIVATE_RECOVERY_AUTHORITY,
                    "run_id": run_id,
                    "return_to_root": True,
                    "tool_call_id": str(item.get("tool_call_id") or "").strip(),
                    "source_tool_call_id": item["source_tool_call_id"],
                    "recovery_source_tool": lineage["failed_verifier_tool"],
                    "recovery_action": subgoal.action,
                    "recovery_scope_id": recovery_scope_id,
                    "goal_contract_id": contract.contract_id,
                    "goal_criterion_id": lineage["criterion_id"],
                    "goal_subgoal_id": subgoal.subgoal_id,
                    "root_goal_unchanged": True,
                    "plan_id": lineage["root_plan_id"],
                    "source_step_id": lineage["root_source_step_id"],
                    "recovery_suggested_tool": tool_name,
                    "root_source_tool_call_id": lineage[
                        "root_source_tool_call_id"
                    ],
                    "root_source_step_id": lineage["root_source_step_id"],
                    "root_verifier_step_id": lineage["root_verifier_step_id"],
                    "root_plan_id": lineage["root_plan_id"],
                    "recovery_origin_tool_call_id": lineage[
                        "failed_verifier_tool_call_id"
                    ],
                }
            bound.append(item)
        return bound

    def _ensure_goal_contract_context(
        self,
        contract: GoalContract,
        *,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> None:
        marker = f"Contract id: {contract.contract_id}"
        if not any(marker in str(message.get("content") or "") for message in messages):
            context_message = goal_contract_context_message(contract)
            if messages and str(messages[0].get("role") or "").strip() == "system":
                messages[0] = {
                    **messages[0],
                    "content": f"{str(messages[0].get('content') or '').rstrip()}\n\n{context_message}",
                }
            else:
                messages.insert(0, {"role": "system", "content": context_message})
        already_recorded = any(
            str(event.get("event") or event.get("event_type") or "").strip()
            == "agent.goal.contract"
            and str(event.get("contract_id") or "").strip() == contract.contract_id
            for event in timeline
            if isinstance(event, Mapping)
        )
        if already_recorded:
            return
        payload = goal_contract_event_payload(contract)
        timeline.append(
            self._timeline(
                "agent.goal.contract",
                contract.contract_id,
                **payload,
            )
        )
        self._append_internal_run_event(run_id, "agent.goal.contract", payload)

    def _record_goal_contract_assessment(
        self,
        contract: GoalContract | None,
        *,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> Any | None:
        if contract is None:
            return None
        assessment = runtime_goal_assessment(contract, timeline)
        assessment_payload = goal_assessment_event_payload(assessment)
        timeline.append(
            self._timeline(
                "agent.goal.assessed",
                contract.contract_id,
                **assessment_payload,
            )
        )
        self._append_internal_run_event(
            run_id,
            "agent.goal.assessed",
            assessment_payload,
        )
        return assessment

    def _goal_gated_model_output(
        self,
        content: str,
        message: Mapping[str, Any],
        *,
        contract: GoalContract | None,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        run_id: str,
        base_url: str = "",
        model: str = "",
        api_key: str = "",
        budget: Any = None,
    ) -> Any | None:
        result_text, truncated = self._limit_model_output(content)
        if contract is None:
            return self._model_output_text_factory(
                result_text,
                metadata=self._model_message_metadata(dict(message)),
                truncated=truncated,
            )
        semantic_assessments = self._assess_pending_semantic_artifacts(
            contract,
            timeline=timeline,
            run_id=run_id,
            base_url=base_url,
            model=model,
            api_key=api_key,
            budget=budget,
        )
        assessment = runtime_goal_assessment(contract, timeline)
        assessment = complete_response_only_goal(
            contract,
            assessment,
            str(result_text),
        )
        assessment_payload = goal_assessment_event_payload(assessment)
        timeline.append(
            self._timeline(
                "agent.goal.assessed",
                contract.contract_id,
                **assessment_payload,
            )
        )
        self._append_internal_run_event(
            run_id,
            "agent.goal.assessed",
            assessment_payload,
        )
        if assessment.completed:
            return self._model_output_text_factory(
                result_text,
                metadata=self._model_message_metadata(dict(message)),
                truncated=truncated,
            )
        followup = goal_replan_context_message(contract, assessment)
        semantic_hint = _semantic_artifact_replan_hint(semantic_assessments)
        if semantic_hint:
            followup = f"{followup}\n\n{semantic_hint}"
        if not (
            messages
            and str(messages[-1].get("role") or "").strip() == "user"
            and str(messages[-1].get("content") or "").strip() == followup
        ):
            messages.append({"role": "user", "content": followup})
        replan_payload = {
            **assessment_payload,
            "planning_reason": "goal_contract_unsatisfied",
            "original_goal_unchanged": True,
        }
        timeline.append(
            self._timeline(
                "agent.goal.replan_required",
                contract.contract_id,
                **replan_payload,
            )
        )
        self._append_internal_run_event(
            run_id,
            "agent.goal.replan_required",
            replan_payload,
        )
        return None

    def _assess_pending_semantic_artifacts(
        self,
        contract: GoalContract,
        *,
        timeline: list[dict[str, Any]],
        run_id: str,
        base_url: str,
        model: str,
        api_key: str,
        budget: Any,
    ) -> tuple[dict[str, Any], ...]:
        """Mint Runtime events around proposal-only semantic model verdicts."""

        if (
            contract.run_id != str(run_id or "").strip()
            or not str(base_url or "").strip()
            or not str(model or "").strip()
            or not str(api_key or "").strip()
            or budget is None
        ):
            return ()
        candidates = pending_semantic_artifact_assessment_candidates(
            contract,
            timeline,
        )
        recorded: list[dict[str, Any]] = []
        for candidate in candidates:
            budget_errors: list[Exception] = []

            def verifier_model_call(
                *,
                messages: list[dict[str, Any]],
                tools: list[dict[str, Any]],
                tool_choice: Mapping[str, Any],
                config: Mapping[str, Any],
            ) -> Any:
                # The verifier owns no independent allowance. Its prompt and
                # model call consume the same caller-owned run budget/deadline.
                del config
                try:
                    self._check_context_budget(budget, messages)
                    budget.claim_model_call()
                except Exception as exc:
                    budget_errors.append(exc)
                    raise
                return self._coalesce_model_message(
                    self._call_model(
                        base_url,
                        model,
                        api_key,
                        messages,
                        tools=tools,
                        tool_choice=tool_choice,
                        stream=True,
                    )
                )

            proposal = verify_semantic_artifact(
                original_user_goal=str(candidate.get("original_goal") or ""),
                criterion_id=str(candidate.get("criterion_id") or ""),
                criterion_description=str(
                    candidate.get("criterion_description") or ""
                ),
                expected_target=candidate.get("criterion_expected"),
                artifact_path=str(candidate.get("observed_path") or ""),
                artifact_content=candidate.get("content"),
                artifact_sha256=str(candidate.get("content_sha256") or ""),
                artifact_byte_length=candidate.get("content_length"),
                model_call=verifier_model_call,
                model_config={"purpose": "semantic_artifact_verification"},
            )
            if budget_errors:
                # verify_semantic_artifact deliberately converts callback
                # exceptions to uncertain proposals. Budget/context failures
                # are control-flow authority and must remain fatal to this run.
                raise budget_errors[0]
            payload = _runtime_semantic_artifact_assessment_payload(
                candidate,
                proposal,
            )
            timeline.append(
                self._timeline(
                    SEMANTIC_ARTIFACT_ASSESSED_EVENT,
                    str(candidate.get("criterion_id") or ""),
                    **payload,
                )
            )
            self._append_internal_run_event(
                run_id,
                SEMANTIC_ARTIFACT_ASSESSED_EVENT,
                payload,
            )
            recorded.append(payload)
        return tuple(recorded)

    def _record_auto_model_followup_app_write_plan(
        self,
        tool_requests: list[dict[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        run_id: str = "",
    ) -> None:
        for request in tool_requests:
            tool_name = str(request.get("tool") or "").strip()
            if not tool_name:
                continue
            step_id = str(request.get("step_id") or "").strip()
            payload = {
                "tool": tool_name,
                "status": "planned",
                "source": str(request.get("source") or "runtime_planner"),
                "planning_reason": str(
                    request.get("planning_reason") or "planner_followup_app_write"
                ),
                "input_preview": (
                    request.get("input") if isinstance(request.get("input"), dict) else {}
                ),
            }
            presentation = str(request.get("presentation") or "").strip()
            if presentation:
                payload["presentation"] = presentation
            if request.get("continue_to_model"):
                payload["continue_to_model"] = True
            for key in ("step_id", "capability_id", "replan_request_id", "replan_trigger"):
                value = str(request.get(key) or "").strip()
                if value:
                    payload[key] = value
            payload.update(_request_observability_metadata(request))
            runtime_stage = str(request.get("runtime_stage") or "").strip()
            is_replan_attempt = bool(
                str(request.get("replan_request_id") or "").strip()
            )
            already_planned = bool(step_id) and not is_replan_attempt and any(
                str(event.get("event") or "").strip()
                == "agent.desktop.intent_planned"
                and str(event.get("tool") or event.get("detail") or "").strip()
                == tool_name
                and str(event.get("step_id") or "").strip() == step_id
                and str(event.get("runtime_stage") or "").strip()
                == runtime_stage
                for event in timeline
                if isinstance(event, Mapping)
            )
            event_type = (
                "agent.tool.input_resolved"
                if already_planned
                else "agent.desktop.intent_planned"
            )
            if already_planned:
                payload.pop("status", None)
                payload.pop("continue_to_model", None)
                payload["resolution_source"] = "model_followup"
            timeline.append(
                self._timeline(
                    event_type,
                    tool_name,
                    **payload,
                )
            )
            if run_id and self._append_run_event is not None:
                self._append_run_event(run_id, event_type, payload)

    @staticmethod
    def _planned_request_for_tool(
        planned_tool_requests: list[dict[str, Any]],
        tool_name: str,
    ) -> dict[str, Any]:
        clean_tool = str(tool_name or "").strip()
        for request in planned_tool_requests:
            if str(request.get("tool") or "").strip() == clean_tool:
                return request
        return planned_tool_requests[0] if planned_tool_requests else {}

    @staticmethod
    def _pending_approval_input_preview(
        pending_approval: dict[str, Any],
        planned_request: dict[str, Any],
        fallback_request: dict[str, Any],
    ) -> dict[str, Any]:
        for candidate in (
            pending_approval.get("input_preview"),
            pending_approval.get("input"),
            planned_request.get("input") if isinstance(planned_request, dict) else None,
            fallback_request.get("input") if isinstance(fallback_request, dict) else None,
        ):
            if isinstance(candidate, dict):
                preview = dict(candidate)
                if isinstance(planned_request, Mapping):
                    for key, value in _request_observability_metadata(
                        planned_request
                    ).items():
                        preview.setdefault(key, value)
                return preview
        return {}

    @staticmethod
    def _approval_event_source(
        planned_request: dict[str, Any],
        tool_name: str,
    ) -> str:
        planning_reason = str(planned_request.get("planning_reason") or "").strip()
        if planning_reason == "planner_desktop_hotkey" or "hotkey" in str(tool_name or ""):
            return "daily_desktop_intent"
        return str(planned_request.get("source") or "daily_desktop_intent")

    @staticmethod
    def _approval_event_planning_reason(
        planned_request: dict[str, Any],
        tool_name: str,
    ) -> str:
        planning_reason = str(planned_request.get("planning_reason") or "").strip()
        if planning_reason == "planner_desktop_hotkey" or "hotkey" in str(tool_name or ""):
            return ""
        if (
            planning_reason == "planner_desktop_operation"
            and str(tool_name or "").strip().endswith("_click_ui_element")
        ):
            return "planner_fallback_desktop_operation"
        return planning_reason

    def _runtime_planner_tool_requests_for_run(
        self,
        planning_context: str,
        allowed_tools: list[str],
        *,
        runtime_execution_metadata: Mapping[str, Any] | None,
        preplanned_selection: DirectToolSelection | None = None,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> tuple[Any | None, list[dict[str, Any]], dict[str, Any]]:
        try:
            return self._runtime_planner_tool_requests(
                planning_context,
                allowed_tools,
                runtime_execution_metadata=runtime_execution_metadata,
                preplanned_selection=preplanned_selection,
            )
        except _RuntimePlannerFailure as exc:
            self._record_runtime_planner_failure(
                exc.reason,
                timeline=timeline,
                run_id=run_id,
            )
            raise self._error_type(
                "Agent 规划暂时无法安全执行，任务已停止。请重试或重新描述目标。"
            ) from exc

    def _record_runtime_planner_failure(
        self,
        reason: str,
        *,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> None:
        diagnostic_reason = (
            "planner_authority_conflict"
            if str(reason or "").strip() == "planner_authority_conflict"
            else "planner_internal_failure"
        )
        request_seed = f"{run_id}:{len(timeline)}:{diagnostic_reason}"
        request_id = (
            "runtime-planner-diagnostic:"
            f"{hashlib.sha256(request_seed.encode('utf-8')).hexdigest()[:16]}"
        )
        payload = {
            "request_id": request_id,
            "trigger": diagnostic_reason,
            "source": "runtime_planner_integrity_guard",
            "failure_detail": "planner_execution_blocked",
            "replan_allowed": False,
            "requires_new_plan": True,
            "visibility": "internal",
        }
        timeline.append(
            self._timeline(
                "agent.replan.requested",
                "planner_execution_blocked",
                payload=payload,
                visibility="internal",
            )
        )
        self._append_internal_run_event(
            run_id,
            "agent.replan.requested",
            payload,
        )

    def _runtime_planner_tool_requests(
        self,
        planning_context: str,
        allowed_tools: list[str],
        *,
        runtime_execution_metadata: Mapping[str, Any] | None = None,
        preplanned_selection: DirectToolSelection | None = None,
    ) -> tuple[Any | None, list[dict[str, Any]], dict[str, Any]]:
        try:
            planner_metadata = _runtime_planner_execution_metadata(
                runtime_execution_metadata,
            )
            selection: DirectToolSelection = (
                preplanned_selection
                if preplanned_selection is not None
                else planner_first_direct_tool_selection(
                    planning_context,
                    allowed_tools,
                    metadata=planner_metadata,
                )
            )
            if selection.selected_source == "runtime_planner" and selection.decision is not None:
                runtime_request_trace_metadata = _runtime_planner_request_trace_metadata(
                    selection.decision,
                )
                runtime_trace_metadata = _runtime_planner_execution_metadata(
                    runtime_execution_metadata,
                    runtime_request_trace_metadata,
                )
                full_plan_requests = _runtime_planner_full_plan_tool_requests(
                    selection.decision,
                    allowed_tools,
                    metadata=runtime_trace_metadata,
                ) or planner_tool_requests(
                    planning_context,
                    allowed_tools,
                    metadata=runtime_trace_metadata,
                )
                execution_requests = planner_execution_tool_requests(
                    full_plan_requests,
                    allowed_tools,
                )
                execution_requests = self._legacy_return_hotkey_projection(
                    planning_context,
                    execution_requests,
                    allowed_tools,
                )
                execution_requests = _drop_trailing_daily_desktop_verify_requests(execution_requests)
                execution_requests = _discovered_app_resolution_probe_requests(
                    execution_requests,
                    selection.event_payload,
                )
                execution_requests = _dedupe_runtime_planner_full_plan_requests(
                    execution_requests
                )
                execution_requests = _runtime_planner_execution_requests_with_selection_inputs(
                    execution_requests,
                    selection.requests,
                )
                planner_goal_contract = _runtime_planner_goal_contract_payload(
                    selection.decision
                )
                execution_requests = _bind_runtime_planner_declared_verifiers(
                    execution_requests,
                    planner_goal_contract,
                )
                execution_requests = _defer_runtime_planner_desktop_type_until_observed(
                    execution_requests,
                    goal_contract=planner_goal_contract,
                )
                has_approval_plan_tool = self._has_approval_plan_tool(execution_requests)
                if execution_requests and (
                    not has_approval_plan_tool
                    or _direct_action_with_active_window_verification(execution_requests)
                    or runtime_request_trace_metadata is not None
                ):
                    selection_payload = planner_selection_payload(
                        decision=selection.decision,
                        planner_requests=full_plan_requests,
                        legacy_requests=[],
                        selected_requests=execution_requests,
                        selected_source="runtime_planner",
                        selected_reason="runtime_planner_full_plan_execution",
                        metadata=runtime_trace_metadata,
                    )
                    selection_payload = _selection_payload_with_runtime_execution_envelope(
                        selection_payload,
                        selection.decision,
                        allowed_tools,
                        full_plan=True,
                        execution_requests=execution_requests,
                        metadata=runtime_trace_metadata,
                    )
                    return (
                        selection.decision,
                        execution_requests,
                        selection_payload,
                    )
            execution_requests = planner_execution_tool_requests(
                selection.requests,
                allowed_tools,
            )
            execution_requests = self._legacy_return_hotkey_projection(
                planning_context,
                execution_requests,
                allowed_tools,
            )
            execution_requests = _drop_trailing_daily_desktop_verify_requests(execution_requests)
            execution_requests = _discovered_app_resolution_probe_requests(
                execution_requests,
                selection.event_payload,
            )
            planner_goal_contract = _runtime_planner_goal_contract_payload(
                selection.decision
            )
            execution_requests = _bind_runtime_planner_declared_verifiers(
                execution_requests,
                planner_goal_contract,
            )
            execution_requests = _defer_runtime_planner_desktop_type_until_observed(
                execution_requests,
                goal_contract=planner_goal_contract,
            )
            if not execution_requests and not selection.requests:
                unavailable_decision = (
                    selection.decision
                    if selection.decision is not None
                    else runtime_planner_decision(
                        planning_context,
                        allowed_tools=allowed_tools,
                        metadata=planner_metadata,
                    )
                )
                if (
                    unavailable_decision is not None
                    and _runtime_planner_unavailable_failure_payloads(unavailable_decision)
                ):
                    return (
                        unavailable_decision,
                        [],
                        planner_selection_payload(
                            decision=unavailable_decision,
                            planner_requests=[],
                            legacy_requests=[],
                            selected_requests=[],
                            selected_source="runtime_planner",
                            selected_reason="runtime_planner_unavailable_plan",
                            metadata=planner_metadata,
                        ),
                    )
            event_payload = selection.event_payload
            if execution_requests != selection.requests:
                execution_tools = [
                    str(request.get("tool") or "").strip()
                    for request in execution_requests
                    if str(request.get("tool") or "").strip()
                ]
                event_payload = {
                    **selection.event_payload,
                    "selected_tools": execution_tools,
                    "selected_request_count": len(execution_requests),
                    "execution_tools": execution_tools,
                    "execution_request_count": len(execution_requests),
                }
            if (
                selection.selected_source == "runtime_planner"
                and selection.decision is not None
                and _tool_requests_include_model_followup(execution_requests)
            ):
                event_payload = _selection_payload_with_runtime_execution_envelope(
                    event_payload,
                    selection.decision,
                    allowed_tools,
                    full_plan=True,
                    execution_requests=execution_requests,
                    metadata=planner_metadata,
                )
            return selection.decision, execution_requests, event_payload
        except Exception as exc:
            reason = (
                "planner_authority_conflict"
                if str(exc or "").strip()
                in _RUNTIME_PLANNER_AUTHORITY_CONFLICT_CODES
                else "planner_internal_failure"
            )
            raise _RuntimePlannerFailure(reason) from exc

    @staticmethod
    def _legacy_return_hotkey_projection(
        planning_context: str,
        requests: list[dict[str, Any]],
        allowed_tools: list[str],
    ) -> list[dict[str, Any]]:
        if not requests or "desktop.hotkey" not in {str(tool or "").strip() for tool in allowed_tools}:
            return requests
        if not RuntimeCustomApiAgentLoop._legacy_explicit_return_key_prompt(planning_context):
            return requests
        updated: list[dict[str, Any]] = []
        converted = False
        for request in requests:
            tool_name = str(request.get("tool") or "").strip()
            payload = request.get("input") if isinstance(request.get("input"), dict) else {}
            if (
                not converted
                and tool_name == "desktop.submit_foreground"
                and str(payload.get("action") or "").strip() == "confirm"
            ):
                updated.append(
                    {
                        **request,
                        "tool": "desktop.hotkey",
                        "input": {"key": "return", "modifiers": []},
                        "planning_reason": "planner_desktop_hotkey",
                    }
                )
                converted = True
                continue
            updated.append(request)
        return updated

    @staticmethod
    def _legacy_explicit_return_key_prompt(planning_context: str) -> bool:
        value = str(planning_context or "").strip()
        if re.search(r"(?:发送|提交|send|submit).{0,8}(?:回车|enter|return)", value, flags=re.IGNORECASE):
            return False
        return bool(
            re.search(
                r"(?:并|再|然后|接着|之后|后|and\s+then|then)?.{0,8}"
                r"(?:按|敲|触发|press|hit|tap)?\s*(?:回车键?|enter|return)(?:\s|$|[。！？!?，,])",
                value,
                flags=re.IGNORECASE,
            )
        )

    @staticmethod
    def _has_approval_plan_tool(tool_requests: list[dict[str, Any]]) -> bool:
        return any(
            isinstance(request, dict)
            and str(request.get("tool") or "").strip() in _DAILY_DESKTOP_APPROVAL_PLAN_TOOLS
            for request in tool_requests
        )

    def _record_runtime_planner_events(
        self,
        decision: Any,
        *,
        timeline: list[dict[str, Any]],
        run_id: str = "",
        scope_context: Mapping[str, Any] | None = None,
    ) -> None:
        decision_id = str(getattr(decision, "decision_id", "") or "").strip()
        if decision_id and _runtime_planner_decision_exists(
            timeline,
            decision_id=decision_id,
            scope_context=scope_context,
        ):
            return
        try:
            from apps.shell.yachiyo_agent.planner_projection import (
                planner_run_event_payloads,
                planner_timeline_events,
            )
        except Exception:
            return
        for event in planner_timeline_events(decision):
            event_type = str(event.get("event") or "").strip()
            payload = (
                event.get("payload")
                if isinstance(event.get("payload"), Mapping)
                else {}
            )
            if _runtime_planner_is_progress_event(event_type):
                _append_runtime_task_progress_event(
                    event_type,
                    str(event.get("detail") or ""),
                    {**dict(payload), **dict(scope_context or {})},
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=None,
                    run_id="",
                )
                continue
            timeline.append(_runtime_planner_timeline_event(event, scope_context))
        if run_id and self._append_run_event is not None:
            for event_type, payload in planner_run_event_payloads(decision):
                if _runtime_planner_is_progress_event(event_type):
                    scoped_payload = {**dict(payload), **dict(scope_context or {})}
                    scoped_event_type = _runtime_progress_event_type(
                        event_type,
                        scoped_payload,
                    )
                    event_payload = _runtime_progress_event_payload(
                        scoped_payload,
                        event_type,
                        scoped_event_type,
                    )
                else:
                    scoped_event_type = _runtime_planner_event_type(
                        event_type,
                        scope_context,
                    )
                    event_payload = _runtime_planner_event_payload(
                        payload,
                        event_type,
                        scoped_event_type,
                        scope_context,
                    )
                self._append_run_event(run_id, scoped_event_type, event_payload)

    def _record_runtime_planner_initial_task_events(
        self,
        decision: Any,
        *,
        timeline: list[dict[str, Any]],
        run_id: str = "",
        scope_context: Mapping[str, Any] | None = None,
    ) -> None:
        for event_type, detail, payload in _runtime_planner_initial_task_updates(decision):
            event_payload = {**dict(scope_context or {}), **dict(payload)}
            if _runtime_task_update_exists(timeline, event_type, event_payload):
                continue
            _append_runtime_task_progress_event(
                event_type,
                detail,
                event_payload,
                timeline=timeline,
                timeline_factory=self._timeline,
                append_run_event=self._append_run_event,
                run_id=run_id,
            )

    def _record_direct_tool_selection_event(
        self,
        payload: dict[str, Any],
        *,
        timeline: list[dict[str, Any]],
        run_id: str = "",
        scope_context: Mapping[str, Any] | None = None,
    ) -> None:
        if not payload:
            return
        event_payload = {**dict(scope_context or {}), **dict(payload)}
        if run_id:
            # Run identity is Runtime authority.  Selection snapshots without
            # it cannot later authorize dependency recovery or continuation.
            event_payload["run_id"] = run_id
        detail = str(
            event_payload.get("selection_source")
            or event_payload.get("selection_reason")
            or "direct_tool_selection"
        )
        event_type = "agent.plan.selection"
        scoped_event_type = _runtime_planner_event_type(event_type, event_payload)
        event_payload = _runtime_planner_event_payload(
            event_payload,
            event_type,
            scoped_event_type,
            event_payload,
        )
        timeline.append(
            self._timeline(
                scoped_event_type,
                detail,
                **event_payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_run_event(run_id, scoped_event_type, event_payload)

    def _run_auto_runtime_planner_requests(
        self,
        requests: list[dict[str, Any]],
        allowed_tools: Iterable[str],
        broker: Any,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        *,
        agent: dict[str, Any],
        runtime_planner_decision: Any,
        run_id: str,
        budget: Any,
        next_iteration: int,
    ) -> int:
        if not requests:
            return len(timeline)
        goal_contract_payload = _runtime_planner_goal_contract_payload(
            runtime_planner_decision
        )
        requests[:] = _bind_runtime_planner_declared_verifiers(
            requests,
            goal_contract_payload,
        )
        self._record_auto_model_followup_app_write_plan(
            requests,
            timeline=timeline,
            run_id=run_id,
        )
        self._record_desktop_permission_preflight(
            requests,
            broker,
            timeline=timeline,
            run_id=run_id,
        )
        self._record_desktop_tool_policy_decisions(
            requests,
            allowed_tools=allowed_tools,
            agent=agent,
            run_id=run_id,
        )
        tool_timeline_start = len(timeline)
        try:
            completed_requests: list[dict[str, Any]] = list(requests)
            self._run_tool_requests(
                requests,
                allowed_tools,
                broker,
                messages,
                timeline,
                artifacts,
                next_iteration=next_iteration,
                run_id=run_id,
                budget=budget,
            )
            continuation_requests = _auto_replan_recovery_deferred_continuation_requests(
                requests,
                allowed_tools,
                timeline,
                tool_timeline_start=tool_timeline_start,
            )
            if continuation_requests:
                continuation_requests = _bind_runtime_planner_declared_verifiers(
                    continuation_requests,
                    goal_contract_payload,
                )
                completed_requests = [*completed_requests, *continuation_requests]
                self._record_auto_model_followup_app_write_plan(
                    continuation_requests,
                    timeline=timeline,
                    run_id=run_id,
                )
                self._record_desktop_permission_preflight(
                    continuation_requests,
                    broker,
                    timeline=timeline,
                    run_id=run_id,
                )
                self._record_desktop_tool_policy_decisions(
                    continuation_requests,
                    allowed_tools=allowed_tools,
                    agent=agent,
                    run_id=run_id,
                )
                self._run_tool_requests(
                    continuation_requests,
                    allowed_tools,
                    broker,
                    messages,
                    timeline,
                    artifacts,
                    next_iteration=next_iteration,
                    run_id=run_id,
                    budget=budget,
                )
        except AgentApprovalRequired as exc:
            self._record_runtime_planner_task_progress_events(
                runtime_planner_decision,
                timeline=timeline,
                tool_timeline_start=tool_timeline_start,
                run_id=run_id,
            )
            self._record_tool_request_task_progress_events(
                requests,
                timeline=timeline,
                tool_timeline_start=tool_timeline_start,
                run_id=run_id,
            )
            pending_approval = (
                exc.pending_approval if isinstance(exc.pending_approval, dict) else {}
            )
            planned_tool = str(
                pending_approval.get("tool") or requests[0].get("tool") or ""
            )
            approval_request = self._planned_request_for_tool(requests, planned_tool)
            planned_input = self._pending_approval_input_preview(
                pending_approval,
                approval_request,
                requests[0] if requests else {},
            )
            self._record_desktop_intent_approval_required(
                planned_tool,
                planned_input,
                pending_approval=exc.pending_approval,
                timeline=timeline,
                run_id=run_id,
                planned_request=approval_request,
                source=self._approval_event_source(approval_request, planned_tool),
                planning_reason=self._approval_event_planning_reason(
                    approval_request,
                    planned_tool,
                ),
            )
            raise
        self._record_runtime_planner_task_progress_events(
            runtime_planner_decision,
            timeline=timeline,
            tool_timeline_start=tool_timeline_start,
            run_id=run_id,
        )
        self._record_tool_request_task_progress_events(
            completed_requests,
            timeline=timeline,
            tool_timeline_start=tool_timeline_start,
            run_id=run_id,
        )
        return tool_timeline_start

    def _run_pending_runtime_replan_recovery(
        self,
        allowed_tools: Iterable[str],
        broker: Any,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        *,
        agent: dict[str, Any],
        run_id: str,
        budget: Any,
        next_iteration: int,
        goal_contract: GoalContract | None,
    ) -> str:
        replan_payloads = _pending_runtime_replan_payloads(timeline)
        if not replan_payloads:
            return ""
        allowed_tool_list = list(allowed_tools)
        recovery_requests = _pending_auto_replan_recovery_requests_with_task_context(
            replan_payloads,
            allowed_tool_list,
            timeline,
        )
        recovery_requests = _bind_trusted_runtime_replan_goal_requests(
            recovery_requests,
            replan_payloads,
            contract=goal_contract,
            timeline=timeline,
            run_id=run_id,
        )
        if not recovery_requests:
            if not _timeline_has_replan_followup_context(timeline, replan_payloads):
                self._append_replan_followup_context(
                    replan_payloads,
                    allowed_tools=allowed_tool_list,
                    messages=messages,
                    timeline=timeline,
                    run_id=run_id,
                )
            return ""

        recovery_timeline_start = self._run_auto_runtime_planner_requests(
            recovery_requests,
            allowed_tool_list,
            broker,
            messages,
            timeline,
            artifacts,
            agent=agent,
            runtime_planner_decision=None,
            run_id=run_id,
            budget=budget,
            next_iteration=next_iteration,
        )
        if not _replan_recovery_requests_need_model_followup(recovery_requests):
            direct_result = self._direct_daily_desktop_sequence_result(
                _tool_requests_without_model_followup(recovery_requests),
                timeline,
                tool_timeline_start=recovery_timeline_start,
                run_id=run_id,
            )
            if direct_result:
                return direct_result
        if not _timeline_has_replan_followup_context(timeline, replan_payloads):
            self._append_replan_followup_context(
                replan_payloads,
                allowed_tools=allowed_tool_list,
                messages=messages,
                timeline=timeline,
                run_id=run_id,
            )
        return ""

    def _run_deferred_observed_ui_replan_recovery(
        self,
        runtime_planner_decision: Any,
        planned_tool_requests: list[dict[str, Any]],
        allowed_tools: Iterable[str],
        broker: Any,
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        artifacts: list[dict[str, Any]],
        *,
        agent: dict[str, Any],
        run_id: str,
        budget: Any,
        next_iteration: int,
        tool_timeline_start: int,
    ) -> str:
        replan_payloads = self._record_runtime_planner_replan_events(
            runtime_planner_decision,
            timeline=timeline,
            tool_timeline_start=tool_timeline_start,
            run_id=run_id,
        )
        if replan_payloads and _timeline_has_permission_recovery_signal(
            timeline,
            tool_timeline_start,
        ):
            replan_payloads = []
        if not replan_payloads:
            return ""
        completed_primary = (
            _runtime_planner_verified_app_open_before_unavailable_deferred_verification(
                planned_tool_requests,
                replan_payloads,
                timeline,
                tool_timeline_start=tool_timeline_start,
            )
        )
        if completed_primary is not None:
            completed_requests, completed_timeline_start = completed_primary
            direct_result = self._direct_daily_desktop_sequence_result(
                completed_requests,
                timeline,
                tool_timeline_start=completed_timeline_start,
                run_id=run_id,
            )
            if direct_result:
                return direct_result

        recovery_requests: list[dict[str, Any]] = []
        recovery_timeline_start = len(timeline)
        observation_requests = _auto_replan_ui_observation_recovery_requests(
            replan_payloads,
            allowed_tools,
        )
        if observation_requests:
            self._run_auto_runtime_planner_requests(
                observation_requests,
                allowed_tools,
                broker,
                messages,
                timeline,
                artifacts,
                agent=agent,
                runtime_planner_decision=runtime_planner_decision,
                run_id=run_id,
                budget=budget,
                next_iteration=next_iteration,
            )
            recovery_requests.extend(observation_requests)

        continuation_requests = _auto_replan_ui_continuation_requests(
            replan_payloads,
            planned_tool_requests,
            allowed_tools,
            timeline,
            tool_timeline_start=tool_timeline_start,
            planning_reason="planner_replan_ui_continuation",
        )
        if continuation_requests:
            self._run_auto_runtime_planner_requests(
                continuation_requests,
                allowed_tools,
                broker,
                messages,
                timeline,
                artifacts,
                agent=agent,
                runtime_planner_decision=runtime_planner_decision,
                run_id=run_id,
                budget=budget,
                next_iteration=next_iteration,
            )
            recovery_requests.extend(continuation_requests)

        observed_action_requests = (
            []
            if continuation_requests
            else _auto_replan_ui_observed_action_requests(
                replan_payloads,
                allowed_tools,
                timeline,
                planning_reason="planner_replan_ui_observed_action",
            )
        )
        if observed_action_requests:
            self._run_auto_runtime_planner_requests(
                observed_action_requests,
                allowed_tools,
                broker,
                messages,
                timeline,
                artifacts,
                agent=agent,
                runtime_planner_decision=runtime_planner_decision,
                run_id=run_id,
                budget=budget,
                next_iteration=next_iteration,
            )
            recovery_requests.extend(observed_action_requests)

            continuation_requests = _auto_replan_ui_continuation_requests(
                replan_payloads,
                planned_tool_requests,
                allowed_tools,
                timeline,
                tool_timeline_start=tool_timeline_start,
                planning_reason="planner_replan_ui_continuation",
            )
            if continuation_requests:
                self._run_auto_runtime_planner_requests(
                    continuation_requests,
                    allowed_tools,
                    broker,
                    messages,
                    timeline,
                    artifacts,
                    agent=agent,
                    runtime_planner_decision=runtime_planner_decision,
                    run_id=run_id,
                    budget=budget,
                    next_iteration=next_iteration,
                )
                recovery_requests.extend(continuation_requests)

            observed_result_requests = _auto_replan_ui_search_observed_result_requests(
                replan_payloads,
                planned_tool_requests,
                allowed_tools,
                timeline,
                planning_reason="planner_replan_ui_search_observed_result",
            )
            if observed_result_requests:
                self._run_auto_runtime_planner_requests(
                    observed_result_requests,
                    allowed_tools,
                    broker,
                    messages,
                    timeline,
                    artifacts,
                    agent=agent,
                    runtime_planner_decision=runtime_planner_decision,
                    run_id=run_id,
                    budget=budget,
                    next_iteration=next_iteration,
                )
                recovery_requests.extend(observed_result_requests)

        if not recovery_requests:
            return ""
        return self._direct_daily_desktop_sequence_result(
            _tool_requests_without_model_followup(recovery_requests),
            timeline,
            tool_timeline_start=recovery_timeline_start,
            run_id=run_id,
        )

    def _record_runtime_planner_replan_events(
        self,
        decision: Any,
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        run_id: str = "",
    ) -> list[dict[str, Any]]:
        existing_payloads = _timeline_replan_request_payloads(
            timeline,
            start=tool_timeline_start,
        )
        live_existing_payloads: list[dict[str, Any]] = []
        for existing_payload in existing_payloads:
            live_payload = _runtime_replan_payload_with_private_authority(
                existing_payload,
                timeline=timeline,
                run_id=run_id,
            )
            live_existing_payloads.append(live_payload)
            dedupe_key = _replan_payload_dedupe_key(live_payload)
            if dedupe_key:
                _replace_in_memory_runtime_replan_payload(
                    timeline,
                    dedupe_key=dedupe_key,
                    payload=live_payload,
                )
        existing_payloads = live_existing_payloads
        if decision is None:
            return existing_payloads
        try:
            from apps.shell.yachiyo_agent.planner_projection import (
                planner_replan_timeline_event,
            )
        except Exception:
            return existing_payloads
        existing_request_ids = {
            str(
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), dict)
                    else {}
                ).get("request_id")
                or ""
            )
            for event in timeline
            if isinstance(event, dict)
            and _runtime_replan_event_type(
                str(event.get("event") or event.get("event_type") or "").strip()
            )
            == "agent.replan.requested"
        }
        existing_payload_by_key = {
            key: index
            for index, payload in enumerate(existing_payloads)
            for key in [_replan_payload_dedupe_key(payload)]
            if key
        }
        tool_events = [
            event
            for event in list(timeline[tool_timeline_start:])
            if isinstance(event, dict)
            and str(event.get("event") or "").strip()
            in {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
        ]
        step_payloads = _runtime_planner_tool_event_step_payloads(decision, tool_events)
        failure_payloads: list[dict[str, Any]] = []
        for event_index, event in enumerate(tool_events):
            event_payload = (
                event.get("payload")
                if isinstance(event.get("payload"), Mapping)
                else {}
            )
            terminal_event = {**dict(event_payload), **dict(event)}
            result = (
                terminal_event.get("result")
                if isinstance(terminal_event.get("result"), dict)
                else {}
            )
            if terminal_event.get("verification_failed") is True:
                result = {**result, "verification_failed": True}
            if not _tool_result_requests_replan(result):
                continue
            tool_name = str(
                terminal_event.get("tool") or terminal_event.get("detail") or ""
            ).strip()
            step_payload = step_payloads.get(event_index, {})
            step_metadata = (
                step_payload.get("metadata")
                if isinstance(step_payload.get("metadata"), Mapping)
                else {}
            )
            input_preview = (
                terminal_event.get("input_preview")
                if isinstance(terminal_event.get("input_preview"), dict)
                else {}
            )
            failure_payload = {
                "event_type": str(
                    terminal_event.get("event")
                    or terminal_event.get("event_type")
                    or "agent.tool.call"
                ).strip(),
                "tool_name": tool_name,
                "input_preview": input_preview,
                "result": result,
                **{
                    key: value
                    for key, value in step_payload.items()
                    if key != "metadata"
                },
            }
            for key in ("plan_id", "goal_contract_id", "goal_criterion_id"):
                value = str(terminal_event.get(key) or "").strip()
                if value:
                    failure_payload[key] = value
            if result.get("verification_failed") is True:
                failure_payload["trigger"] = "verification_failed"
                failure_payload["status"] = "verification_failed"
            event_trace_metadata = _runtime_trace_metadata_from_mapping(terminal_event)
            recovery_metadata = _runtime_replan_failure_metadata(result)
            source_identity = {
                key: value
                for key, value in {
                    "source_tool_call_id": str(
                        terminal_event.get("tool_call_id") or ""
                    ).strip(),
                    "source_request_id": str(
                        terminal_event.get("request_id") or ""
                    ).strip(),
                    "source_plan_id": str(
                        terminal_event.get("plan_id") or ""
                    ).strip(),
                    "source_step_id": str(
                        terminal_event.get("step_id")
                        or terminal_event.get("planner_step_id")
                        or step_payload.get("source_step_id")
                        or step_payload.get("step_id")
                        or ""
                    ).strip(),
                    "source_provider_kind": str(
                        _runtime_execution_provider_identity(result).get(
                            "provider_kind"
                        )
                        or ""
                    ).strip(),
                    "source_provider_id": str(
                        _runtime_execution_provider_identity(result).get(
                            "provider_id"
                        )
                        or ""
                    ).strip(),
                }.items()
                if value
            }
            if (
                step_metadata
                or input_preview
                or event_trace_metadata
                or recovery_metadata
                or source_identity
            ):
                metadata = {
                    **dict(step_metadata),
                    **event_trace_metadata,
                    **recovery_metadata,
                    **source_identity,
                }
                if input_preview:
                    metadata.setdefault("input_preview", dict(input_preview))
                failure_payload["metadata"] = metadata
            failure_payloads.append(failure_payload)
        failure_payloads.extend(
            _runtime_planner_verification_failure_payloads(
                decision,
                list(timeline[tool_timeline_start:]),
            )
        )
        if not failure_payloads:
            failure_payloads.extend(_runtime_planner_unavailable_failure_payloads(decision))
        payloads: list[dict[str, Any]] = list(existing_payloads)
        for failure_payload in failure_payloads:
            replan_metadata = (
                failure_payload.get("metadata")
                if isinstance(failure_payload.get("metadata"), Mapping)
                else {}
            )
            replan_event = planner_replan_timeline_event(
                decision,
                failure_payload,
                run_id=run_id,
                metadata=replan_metadata,
            )
            if not replan_event:
                continue
            payload = (
                replan_event.get("payload")
                if isinstance(replan_event.get("payload"), dict)
                else {}
            )
            payload_dict = dict(payload)
            goal_identity = {
                key: str(failure_payload.get(key) or "").strip()
                for key in ("goal_contract_id", "goal_criterion_id")
            }
            if all(
                str(replan_metadata.get(key) or "").strip()
                for key in (
                    "source_tool_call_id",
                    "source_request_id",
                    "source_plan_id",
                    "source_step_id",
                    "source_provider_kind",
                    "source_provider_id",
                )
            ) and all(goal_identity.values()):
                # The planner describes the bounded fallback, but this replan
                # exists only because Runtime correlated an executor-owned,
                # provider-attested terminal failure above. Preserve that
                # authority boundary for the later recovery binder.
                payload_dict["source"] = "runtime_tool_request_runner"
                payload_dict["plan_id"] = str(
                    replan_metadata.get("source_plan_id") or ""
                ).strip()
                payload_dict.update(goal_identity)
            dedupe_key = _replan_payload_dedupe_key(payload_dict)
            if dedupe_key and dedupe_key in existing_payload_by_key:
                private_payload = _runtime_replan_payload_with_private_authority(
                    payload_dict,
                    timeline=timeline,
                    run_id=run_id,
                )
                payloads[existing_payload_by_key[dedupe_key]] = private_payload
                _replace_in_memory_runtime_replan_payload(
                    timeline,
                    dedupe_key=dedupe_key,
                    payload=private_payload,
                )
                continue
            request_id = str(payload.get("request_id") or "").strip()
            if request_id and request_id in existing_request_ids:
                continue
            if request_id:
                existing_request_ids.add(request_id)
            # Persist only the public replay projection. The process-private
            # execution capability is attached outside the live payload's
            # mapping entries and therefore disappears across serialization.
            if run_id and self._append_run_event is not None:
                self._append_run_event(run_id, "agent.replan.requested", payload_dict)
            private_payload = _runtime_replan_payload_with_private_authority(
                payload_dict,
                timeline=timeline,
                run_id=run_id,
            )
            replan_event["payload"] = private_payload
            timeline.append(replan_event)
            payloads.append(private_payload)
        return payloads

    def _record_model_followup_pending_plan_replan_events(
        self,
        followup_context: Mapping[str, Any],
        planned_tool_requests: list[dict[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        run_id: str = "",
    ) -> list[dict[str, Any]]:
        payloads = _model_followup_pending_plan_replan_payloads(
            followup_context,
            planned_tool_requests,
            timeline,
            tool_timeline_start=tool_timeline_start,
            run_id=run_id,
        )
        if not payloads:
            return []
        existing_request_ids = {
            str(
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), Mapping)
                    else {}
                ).get("request_id")
                or event.get("request_id")
                or ""
            ).strip()
            for event in timeline
            if isinstance(event, Mapping)
            and _runtime_replan_event_type(
                str(event.get("event") or event.get("event_type") or "").strip()
            )
            == "agent.replan.requested"
        }
        existing_keys = {
            key
            for event in timeline
            if isinstance(event, Mapping)
            and _runtime_replan_event_type(
                str(event.get("event") or event.get("event_type") or "").strip()
            )
            == "agent.replan.requested"
            for payload in [
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), Mapping)
                    else event
                )
            ]
            for key in [_replan_payload_dedupe_key(payload)]
            if key
        }
        recorded: list[dict[str, Any]] = []
        for payload in payloads:
            request_id = str(payload.get("request_id") or "").strip()
            if request_id and request_id in existing_request_ids:
                continue
            dedupe_key = _replan_payload_dedupe_key(payload)
            if dedupe_key and dedupe_key in existing_keys:
                continue
            if request_id:
                existing_request_ids.add(request_id)
            if dedupe_key:
                existing_keys.add(dedupe_key)
            event_type = _runtime_replan_request_event_type(payload)
            event_payload = _runtime_replan_request_event_payload(payload, event_type)
            detail = str(
                payload.get("reason")
                or payload.get("failure_detail")
                or payload.get("trigger")
                or "Runtime requested a replan."
            ).strip()
            event_fields = {
                "status": str(payload.get("status") or "requested"),
                "source": str(payload.get("source") or "runtime_planner"),
                "decision_id": str(payload.get("decision_id") or ""),
                "plan_id": str(payload.get("plan_id") or ""),
                "payload": event_payload,
            }
            for key in (
                "task_id",
                "run_id",
                "core_id",
                "group_run_id",
                "run_group_id",
                "group_id",
                "workflow_run_id",
                "workflow_id",
                "workflow_node_id",
                "workflow_node_label",
            ):
                value = str(event_payload.get(key) or payload.get(key) or "").strip()
                if value:
                    event_fields[key] = value
            timeline.append(self._timeline(event_type, detail, **event_fields))
            recorded.append(dict(payload))
            if run_id and self._append_run_event is not None:
                self._append_run_event(run_id, event_type, event_payload)
        return recorded

    def _record_tool_request_task_progress_events(
        self,
        planned_tool_requests: list[dict[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        run_id: str = "",
    ) -> None:
        followup_context = _tool_request_task_progress_followup_context(
            planned_tool_requests,
        )
        if not followup_context:
            return
        self._record_model_followup_pending_plan_progress_events(
            followup_context,
            planned_tool_requests,
            timeline=timeline,
            tool_timeline_start=tool_timeline_start,
            run_id=run_id,
        )

    def _record_runtime_planner_task_progress_events(
        self,
        decision: Any,
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        run_id: str = "",
    ) -> None:
        plan = getattr(decision, "plan", None)
        if plan is None:
            return
        task_core = getattr(plan, "task_core", None)
        tool_plan = getattr(plan, "tool_plan", None)
        if task_core is None or tool_plan is None:
            return
        steps = list(getattr(tool_plan, "steps", []) or [])
        if not steps:
            return
        workspace = getattr(task_core, "workspace", None)
        todo_by_step = {
            str(getattr(todo, "step_id", "") or "").strip(): todo
            for todo in list(getattr(task_core, "todos", []) or [])
            if str(getattr(todo, "step_id", "") or "").strip()
        }
        checkpoints_by_step: dict[str, list[Any]] = {}
        for checkpoint in list(getattr(task_core, "checkpoints", []) or []):
            step_id = str(getattr(checkpoint, "after_step_id", "") or "").strip()
            if not step_id:
                continue
            checkpoints_by_step.setdefault(step_id, []).append(checkpoint)
        workspace_items_by_step: dict[str, list[Any]] = {}
        for item in list(getattr(workspace, "items", []) if workspace is not None else []):
            step_id = str(getattr(item, "source_step_id", "") or "").strip()
            if not step_id:
                continue
            workspace_items_by_step.setdefault(step_id, []).append(item)
        tool_events = [
            event
            for event in timeline[tool_timeline_start:]
            if isinstance(event, dict)
            and str(event.get("event") or "").strip()
            in {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
        ]
        event_index = 0
        core_id = str(getattr(task_core, "core_id", "") or "").strip()
        workspace_id = str(getattr(workspace, "workspace_id", "") or "").strip()
        plan_id = str(getattr(plan, "plan_id", "") or "").strip()
        decision_id = str(getattr(decision, "decision_id", "") or "").strip()
        for step in steps:
            tool_name = str(getattr(step, "tool_name", "") or "").strip()
            step_id = str(getattr(step, "step_id", "") or "").strip()
            if not tool_name or not step_id:
                continue
            tool_event: dict[str, Any] | None = None
            scan_index = event_index
            while scan_index < len(tool_events):
                candidate = tool_events[scan_index]
                candidate_step_id = str(
                    candidate.get("step_id") or candidate.get("planner_step_id") or ""
                ).strip()
                if (
                    candidate_step_id == step_id
                    or (
                        not candidate_step_id
                        and str(candidate.get("detail") or "").strip() == tool_name
                    )
                ):
                    tool_event = candidate
                    event_index = scan_index + 1
                    break
                scan_index += 1
            if tool_event is None:
                continue
            result = tool_event.get("result") if isinstance(tool_event.get("result"), dict) else {}
            todo = todo_by_step.get(step_id)
            checkpoints = checkpoints_by_step.get(step_id, [])
            requires_post_action_verification = (
                _task_progress_requires_post_action_verification(todo, *checkpoints)
            )
            todo_status = _task_todo_status_for_tool_result(
                str(tool_event.get("event") or ""),
                result,
                requires_post_action_verification=requires_post_action_verification,
            )
            checkpoint_status = _task_checkpoint_status_for_todo_status(
                todo_status,
                result,
                requires_post_action_verification=requires_post_action_verification,
            )
            if (
                todo_status == "in_progress"
                and _runtime_planner_step_has_following_ui_verification(
                    step_id,
                    steps,
                    tool_events[event_index:],
                    todos_by_step=todo_by_step,
                    checkpoints_by_step=checkpoints_by_step,
                )
            ):
                todo_status = "completed"
                checkpoint_status = "completed"
            skip_statuses = {todo_status}
            if todo_status != "completed":
                skip_statuses.add("completed")
            if _runtime_planner_step_has_status(
                timeline,
                decision_id=decision_id,
                step_id=step_id,
                statuses=skip_statuses,
            ):
                continue
            source_event = {
                "event": str(tool_event.get("event") or ""),
                "detail": str(tool_event.get("detail") or ""),
                "request_id": str(tool_event.get("request_id") or "").strip(),
                "tool_call_id": str(tool_event.get("tool_call_id") or "").strip(),
            }
            base_payload = {
                "source": "runtime_planner",
                "core_id": core_id,
                "workspace_id": workspace_id,
                "decision_id": decision_id,
                "plan_id": plan_id,
                "step_id": step_id,
                "tool": tool_name,
                "request_id": str(tool_event.get("request_id") or "").strip(),
                "tool_call_id": str(tool_event.get("tool_call_id") or "").strip(),
                "source_event": source_event,
                "result_preview": _task_progress_result_preview(result),
                "actor": "native_runtime",
                "visibility": "internal",
            }
            base_payload.update(
                _runtime_task_progress_scope_context(
                    timeline,
                    tool_event,
                    decision_id=decision_id,
                    plan_id=plan_id,
                )
            )
            for workspace_item in workspace_items_by_step.get(step_id, []):
                item_payload = _snapshot_payload(workspace_item)
                item_payload["status"] = todo_status
                payload = {
                    **base_payload,
                    "workspace_item_id": str(
                        getattr(workspace_item, "item_id", "") or ""
                    ).strip(),
                    "status": todo_status,
                    "previous_status": str(
                        getattr(workspace_item, "status", "") or "planned"
                    ),
                    "workspace_item": item_payload,
                }
                _append_runtime_task_progress_event(
                    "agent.task.workspace_item.updated",
                    str(getattr(workspace_item, "title", "") or step_id),
                    payload,
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=self._append_run_event,
                    run_id=run_id,
                )
            todo = todo_by_step.get(step_id)
            if todo is not None:
                todo_payload = _snapshot_payload(todo)
                todo_payload["status"] = todo_status
                payload = {
                    **base_payload,
                    "todo_id": str(getattr(todo, "todo_id", "") or "").strip(),
                    "status": todo_status,
                    "previous_status": str(getattr(todo, "status", "") or "pending"),
                    "todo": todo_payload,
                }
                _append_runtime_task_progress_event(
                    "agent.task.todo.updated",
                    str(getattr(todo, "title", "") or step_id),
                    payload,
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=self._append_run_event,
                    run_id=run_id,
                )
            for checkpoint in checkpoints:
                checkpoint_payload = _snapshot_payload(checkpoint)
                checkpoint_payload["status"] = checkpoint_status
                payload = {
                    **base_payload,
                    "checkpoint_id": str(
                        getattr(checkpoint, "checkpoint_id", "") or ""
                    ).strip(),
                    "status": checkpoint_status,
                    "previous_status": str(
                        getattr(checkpoint, "status", "") or "planned"
                    ),
                    "checkpoint": checkpoint_payload,
                }
                _append_runtime_task_progress_event(
                    "agent.task.checkpoint.updated",
                    str(getattr(checkpoint, "title", "") or step_id),
                    payload,
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=self._append_run_event,
                    run_id=run_id,
                )
            _append_runtime_task_verification_target_progress_events(
                _runtime_planner_verification_targets(
                    step,
                    checkpoints,
                    todo_by_step,
                    checkpoints_by_step,
                ),
                base_payload,
                result,
                timeline=timeline,
                timeline_factory=self._timeline,
                append_run_event=self._append_run_event,
                run_id=run_id,
            )

    def _record_model_followup_pending_plan_progress_events(
        self,
        followup_context: Mapping[str, Any],
        planned_tool_requests: list[dict[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        tool_timeline_start: int,
        run_id: str = "",
    ) -> None:
        task_core = (
            followup_context.get("task_core")
            if isinstance(followup_context.get("task_core"), Mapping)
            else {}
        )
        if not task_core:
            return
        workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
        workspace_items = (
            workspace.get("items")
            if isinstance(workspace.get("items"), list)
            else []
        )
        workspace_items_by_step: dict[str, list[Mapping[str, Any]]] = {}
        for item in workspace_items:
            if not isinstance(item, Mapping):
                continue
            step_id = str(item.get("source_step_id") or "").strip()
            if step_id:
                workspace_items_by_step.setdefault(step_id, []).append(item)
        todos_by_step = {
            str(todo.get("step_id") or "").strip(): todo
            for todo in task_core.get("todos", [])
            if isinstance(todo, Mapping) and str(todo.get("step_id") or "").strip()
        }
        for request in planned_tool_requests:
            if not isinstance(request, Mapping):
                continue
            task_todo = (
                request.get("task_todo")
                if isinstance(request.get("task_todo"), Mapping)
                else {}
            )
            step_id = str(task_todo.get("step_id") or request.get("step_id") or "").strip()
            if step_id and task_todo:
                todos_by_step.setdefault(step_id, task_todo)
        checkpoints_by_step: dict[str, list[Mapping[str, Any]]] = {}
        for checkpoint in task_core.get("checkpoints", []):
            if not isinstance(checkpoint, Mapping):
                continue
            step_id = str(checkpoint.get("after_step_id") or "").strip()
            if step_id:
                checkpoints_by_step.setdefault(step_id, []).append(checkpoint)
        for request in planned_tool_requests:
            if not isinstance(request, Mapping):
                continue
            for checkpoint in request.get("task_checkpoints", []):
                if not isinstance(checkpoint, Mapping):
                    continue
                step_id = str(
                    checkpoint.get("after_step_id")
                    or checkpoint.get("step_id")
                    or request.get("step_id")
                    or ""
                ).strip()
                if step_id:
                    checkpoints_by_step.setdefault(step_id, []).append(checkpoint)
        tool_events = [
            event
            for event in timeline[tool_timeline_start:]
            if isinstance(event, dict)
            and str(event.get("event") or "").strip()
            in {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
        ]
        if not tool_events:
            return
        core_id = str(task_core.get("core_id") or "").strip()
        workspace_id = str(workspace.get("workspace_id") or "").strip()
        decision_id = str(followup_context.get("decision_id") or "").strip()
        plan_id = str(followup_context.get("plan_id") or "").strip()
        event_index = 0
        for request in planned_tool_requests:
            if not isinstance(request, Mapping):
                continue
            step_id = str(request.get("step_id") or "").strip()
            tool_name = str(request.get("tool") or "").strip()
            if not step_id or not tool_name:
                continue
            tool_event: dict[str, Any] | None = None
            scan_index = event_index
            while scan_index < len(tool_events):
                candidate = tool_events[scan_index]
                candidate_step_id = str(
                    candidate.get("step_id") or candidate.get("planner_step_id") or ""
                ).strip()
                if (
                    candidate_step_id == step_id
                    or (
                        not candidate_step_id
                        and str(candidate.get("detail") or "").strip() == tool_name
                    )
                ):
                    tool_event = candidate
                    event_index = scan_index + 1
                    break
                scan_index += 1
            if tool_event is None:
                continue
            result = tool_event.get("result") if isinstance(tool_event.get("result"), dict) else {}
            todo = todos_by_step.get(step_id)
            if todo is not None and not _task_core_todo_matches_request(todo, request):
                continue
            checkpoints = checkpoints_by_step.get(step_id, [])
            requires_post_action_verification = (
                _task_progress_requires_post_action_verification(
                    request,
                    todo,
                    *checkpoints,
                )
            )
            todo_status = _task_todo_status_for_tool_result(
                str(tool_event.get("event") or ""),
                result,
                requires_post_action_verification=requires_post_action_verification,
            )
            checkpoint_status = _task_checkpoint_status_for_todo_status(
                todo_status,
                result,
                requires_post_action_verification=requires_post_action_verification,
            )
            skip_statuses = {todo_status}
            if todo_status != "completed":
                skip_statuses.add("completed")
            if _runtime_planner_step_has_status(
                timeline,
                decision_id=decision_id,
                step_id=step_id,
                statuses=skip_statuses,
            ):
                continue
            source_event = {
                "event": str(tool_event.get("event") or ""),
                "detail": str(tool_event.get("detail") or ""),
                "request_id": str(tool_event.get("request_id") or "").strip(),
                "tool_call_id": str(tool_event.get("tool_call_id") or "").strip(),
            }
            base_payload = {
                "source": "runtime_planner",
                "core_id": core_id,
                "workspace_id": workspace_id,
                "decision_id": decision_id,
                "plan_id": plan_id,
                "step_id": step_id,
                "tool": tool_name,
                "request_id": str(tool_event.get("request_id") or "").strip(),
                "tool_call_id": str(tool_event.get("tool_call_id") or "").strip(),
                "source_event": source_event,
                "result_preview": _task_progress_result_preview(result),
                "actor": "native_runtime",
                "visibility": "internal",
            }
            base_payload.update(
                _runtime_task_progress_scope_context(
                    timeline,
                    tool_event,
                    decision_id=decision_id,
                    plan_id=plan_id,
                )
                or _runtime_task_progress_context_from_mapping(followup_context)
            )
            for workspace_item in workspace_items_by_step.get(step_id, []):
                item_payload = dict(workspace_item)
                item_payload["status"] = todo_status
                payload = {
                    **base_payload,
                    "workspace_item_id": str(workspace_item.get("item_id") or "").strip(),
                    "status": todo_status,
                    "previous_status": str(workspace_item.get("status") or "planned"),
                    "workspace_item": item_payload,
                }
                if _runtime_task_update_exists(
                    timeline,
                    "agent.task.workspace_item.updated",
                    payload,
                ):
                    continue
                _append_runtime_task_progress_event(
                    "agent.task.workspace_item.updated",
                    str(workspace_item.get("title") or step_id),
                    payload,
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=self._append_run_event,
                    run_id=run_id,
                )
            if todo is not None:
                todo_payload = dict(todo)
                todo_payload["status"] = todo_status
                payload = {
                    **base_payload,
                    "todo_id": str(todo.get("todo_id") or "").strip(),
                    "status": todo_status,
                    "previous_status": str(todo.get("status") or "pending"),
                    "todo": todo_payload,
                }
                if not _runtime_task_update_exists(
                    timeline,
                    "agent.task.todo.updated",
                    payload,
                ):
                    _append_runtime_task_progress_event(
                        "agent.task.todo.updated",
                        str(todo.get("title") or step_id),
                        payload,
                        timeline=timeline,
                        timeline_factory=self._timeline,
                        append_run_event=self._append_run_event,
                        run_id=run_id,
                    )
            for checkpoint in checkpoints:
                checkpoint_payload = dict(checkpoint)
                checkpoint_payload["status"] = checkpoint_status
                payload = {
                    **base_payload,
                    "checkpoint_id": str(checkpoint.get("checkpoint_id") or "").strip(),
                    "status": checkpoint_status,
                    "previous_status": str(checkpoint.get("status") or "planned"),
                    "checkpoint": checkpoint_payload,
                }
                if _runtime_task_update_exists(
                    timeline,
                    "agent.task.checkpoint.updated",
                    payload,
                ):
                    continue
                _append_runtime_task_progress_event(
                    "agent.task.checkpoint.updated",
                    str(checkpoint.get("title") or step_id),
                    payload,
                    timeline=timeline,
                    timeline_factory=self._timeline,
                    append_run_event=self._append_run_event,
                    run_id=run_id,
                )
            _append_runtime_task_verification_target_progress_events(
                _model_followup_verification_targets(
                    request,
                    checkpoints,
                    todos_by_step,
                    checkpoints_by_step,
                ),
                base_payload,
                result,
                timeline=timeline,
                timeline_factory=self._timeline,
                append_run_event=self._append_run_event,
                run_id=run_id,
            )

    def _record_unavailable_desktop_intent(
        self,
        candidate: dict[str, Any],
        *,
        allowed_tools: list[str],
        messages: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> str:
        tool_name = str(candidate.get("tool") or "").strip()
        payload = candidate.get("input") if isinstance(candidate.get("input"), dict) else {}
        summary = self._unavailable_desktop_intent_summary(tool_name, allowed_tools)
        event_payload = {
            "tool": tool_name,
            "status": "unavailable",
            "source": "daily_desktop_intent",
            "reason": "tool_not_allowed",
            "blocked_by": "agent_tool_policy",
            "blocked_summary": summary,
            "recovery_actions": [
                "改用八千代日常入口执行这个桌面指令。",
                "在 Agent Studio 为该 Agent 开启桌面执行能力。",
            ],
            "input_preview": payload,
            "allowed_tools": [str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()],
        }
        timeline.append(
            self._timeline(
                "agent.desktop.intent_unavailable",
                tool_name,
                **event_payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_run_event(run_id, "agent.desktop.intent_unavailable", event_payload)
        messages.append(
            {
                "role": "user",
                "content": (
                    f"Desktop intent for {tool_name} was not executed because this Agent "
                    f"does not allow {tool_name}. Allowed tools: "
                    f"{', '.join(event_payload['allowed_tools']) or 'none'}."
                ),
            }
        )
        return summary

    def _record_runtime_planner_unavailable_goal(
        self,
        decision: Any,
        unavailable_payloads: Iterable[Mapping[str, Any]],
        replan_payloads: Iterable[Mapping[str, Any]],
        *,
        goal_contract: GoalContract | None,
        timeline: list[dict[str, Any]],
        run_id: str,
    ) -> str:
        payload = _runtime_planner_unavailable_goal_payload(
            decision,
            unavailable_payloads,
            replan_payloads,
            goal_contract=goal_contract,
            run_id=run_id,
        )
        timeline.append(
            self._timeline(
                "agent.plan.blocked",
                str(payload.get("plan_id") or "runtime_planner_unavailable"),
                **payload,
            )
        )
        self._append_internal_run_event(
            run_id,
            "agent.plan.blocked",
            payload,
        )
        return self._model_output_text_factory(
            _runtime_planner_unavailable_user_summary(payload),
            metadata={
                "runtime_status": "awaiting_user",
                "blocked_reason": str(payload.get("blocked_reason") or ""),
            },
            truncated=False,
        )

    def _record_blocked_runtime_execution_envelope(
        self,
        envelope: Mapping[str, Any] | None,
        blocked_requests: list[dict[str, Any]],
        *,
        timeline: list[dict[str, Any]],
        run_id: str,
        completed_requests: Iterable[Mapping[str, Any]] = (),
    ) -> str:
        envelope_payload = dict(envelope or {})
        raw_requests = (
            envelope_payload.get("requests")
            if isinstance(envelope_payload.get("requests"), list)
            else []
        )
        candidate: Mapping[str, Any] = (
            blocked_requests[0]
            if blocked_requests
            else next(
                (request for request in raw_requests if isinstance(request, Mapping)),
                {},
            )
        )
        route = (
            candidate.get("desktop_execution_route")
            if isinstance(candidate.get("desktop_execution_route"), Mapping)
            else {}
        )
        tool_name = str(
            candidate.get("tool") or candidate.get("tool_name") or ""
        ).strip()
        blocked_by = str(
            candidate.get("blocked_by")
            or route.get("status")
            or candidate.get("status")
            or "runtime_execution_not_ready"
        ).strip()
        reason = str(
            candidate.get("policy_reason")
            or route.get("reason")
            or "Runtime execution requirements are not currently satisfied."
        ).strip()
        completed_tools = _tool_names_from_requests(completed_requests)
        if completed_tools and "real_virtual_desktop_provider" in blocked_by:
            summary = (
                "Runtime 已完成可执行的前置步骤，但后续步骤需要真实隔离桌面 Provider，"
                "因此没有继续执行。请启动或配置 Provider 后重试。"
            )
        elif "real_virtual_desktop_provider" in blocked_by:
            summary = (
                "当前没有可用的真实隔离桌面 Provider，Runtime 已保留执行计划但没有执行。"
                "请启动或配置真实隔离桌面 Provider 后重试。"
            )
        elif blocked_by == "provider_required" or "cua driver" in reason.casefold():
            planned_input = (
                candidate.get("input")
                if isinstance(candidate.get("input"), Mapping)
                else {}
            )
            app_name = str(
                planned_input.get("app_name")
                or planned_input.get("expected_app_name")
                or ""
            ).strip()
            target = _display_target_name(app_name) or "目标应用"
            summary = (
                f"后台桌面控制尚未就绪，因此没有打开或操作{target}，"
                "也没有接管你正在使用的鼠标和键盘。"
                "请先安装或授权后台控制组件后重试。"
            )
        else:
            summary = f"Runtime 已保留执行计划，但当前执行条件未满足，因此没有执行：{reason}"
        event_payload = {
            **_runtime_planner_scope_context(
                envelope_payload,
                candidate,
                timeline=timeline,
            ),
            "tool": tool_name,
            "status": "blocked",
            "source": "runtime_execution_envelope",
            "reason": "runtime_execution_not_ready",
            "blocked_by": blocked_by,
            "blocked_summary": summary,
            "policy_reason": reason,
            "input_preview": dict(candidate.get("input") or {}),
            "runtime_execution_envelope_id": str(
                envelope_payload.get("envelope_id")
                or envelope_payload.get("execution_id")
                or ""
            ),
            "decision_id": str(envelope_payload.get("decision_id") or ""),
            "plan_id": str(envelope_payload.get("plan_id") or ""),
            "request_id": str(candidate.get("request_id") or ""),
            "step_id": str(
                candidate.get("step_id")
                or candidate.get("planner_step_id")
                or ""
            ),
            "blocking_conditions": _string_list(
                route.get("blocking_conditions")
                or candidate.get("blocking_conditions")
            ),
            "completed_tools": completed_tools,
        }
        detail = tool_name or "runtime_execution_envelope"
        timeline.append(
            self._timeline(
                "agent.desktop.intent_unavailable",
                detail,
                **event_payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_run_event(
                run_id,
                "agent.desktop.intent_unavailable",
                event_payload,
            )
        return summary

    @staticmethod
    def _unavailable_desktop_intent_summary(tool_name: str, allowed_tools: list[str]) -> str:
        label = _DAILY_DESKTOP_TOOL_LABELS.get(tool_name) or tool_name or "桌面动作"
        allowed = ", ".join(str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip())
        allowed_suffix = f"当前允许的工具：{allowed}。" if allowed else "当前没有开启可执行工具。"
        return (
            f"这个 Agent 当前没有开启 {tool_name}，所以不能直接执行「{label}」。"
            "请改用八千代日常入口，或在 Agent Studio 给该 Agent 开启桌面执行能力。"
            f"{allowed_suffix}"
        )

    def _record_desktop_permission_preflight(
        self,
        planned_tool_requests: list[dict[str, Any]],
        broker: Any,
        *,
        timeline: list[dict[str, Any]],
        run_id: str = "",
    ) -> None:
        payload = _desktop_permission_preflight_event_payload(
            planned_tool_requests,
            broker,
        )
        if not payload:
            return
        detail = str(payload.get("tool") or "desktop.permissions")
        timeline.append(
            self._timeline(
                "agent.desktop.permission_preflight",
                detail,
                **payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_run_event(
                run_id,
                "agent.desktop.permission_preflight",
                payload,
            )

    def _record_desktop_tool_policy_decisions(
        self,
        planned_tool_requests: list[dict[str, Any]],
        *,
        allowed_tools: list[str],
        agent: dict[str, Any],
        run_id: str = "",
    ) -> None:
        if not run_id or self._append_run_event is None:
            return
        allowed = [
            str(tool or "").strip()
            for tool in allowed_tools
            if str(tool or "").strip()
        ]
        policy_overlay = agent.get("_daily_desktop_policy_overlay") is True
        reason = "daily_desktop_policy_overlay" if policy_overlay else "agent_tool_policy"
        for request in planned_tool_requests:
            tool_name = str(request.get("tool") or "").strip()
            if not tool_name:
                continue
            payload = {
                "tool": tool_name,
                "status": "allowed",
                "decision": "allow",
                "source": str(request.get("source") or "daily_desktop_intent"),
                "reason": reason,
                "policy_scope": "daily_desktop",
                "policy_overlay": policy_overlay,
                "input_preview": request.get("input") if isinstance(request.get("input"), dict) else {},
                "allowed_tools": allowed,
            }
            payload.update(
                _desktop_execution_mode_payload(
                    tool_name,
                    payload.get("input_preview")
                    if isinstance(payload.get("input_preview"), Mapping)
                    else {},
                )
            )
            planning_reason = str(
                request.get("planning_reason") or "clear_daily_desktop_intent"
            ).strip()
            if planning_reason:
                payload["planning_reason"] = planning_reason
            payload.update(_request_observability_metadata(request))
            self._append_run_event(run_id, "agent.tool.policy_decision", payload)

    def _record_desktop_intent_approval_required(
        self,
        tool_name: str,
        tool_input: dict[str, Any],
        *,
        pending_approval: dict[str, Any],
        timeline: list[dict[str, Any]],
        run_id: str,
        planned_request: dict[str, Any] | None = None,
        source: str = "daily_desktop_intent",
        planning_reason: str = "",
    ) -> None:
        event_payload = {
            "tool": tool_name,
            "status": "approval_required",
            "source": str(source or "daily_desktop_intent"),
            "reason": "tool_policy_requires_approval",
            "input_preview": tool_input,
        }
        event_payload.update(_desktop_execution_mode_payload(tool_name, tool_input))
        clean_planning_reason = str(planning_reason or "").strip()
        if clean_planning_reason:
            event_payload["planning_reason"] = clean_planning_reason
        for key in ("approval_id", "risk_level", "policy_reason"):
            value = str(pending_approval.get(key) or "").strip()
            if value:
                event_payload[key] = value
        event_payload.update(
            _approval_required_planner_trace_payload(
                pending_approval,
                planned_request or {},
            )
        )
        timeline.append(
            self._timeline(
                "agent.desktop.intent_approval_required",
                tool_name,
                **event_payload,
            )
        )
        if run_id and self._append_run_event is not None:
            self._append_run_event(run_id, "agent.desktop.intent_approval_required", event_payload)

    def _direct_daily_desktop_result(
        self,
        _agent: dict[str, Any],
        planned_tool: str,
        planned_input: dict[str, Any],
        timeline: list[dict[str, Any]],
        run_id: str = "",
        presentation: str = "",
        source: str = "daily_desktop_intent",
        planning_reason: str = "",
    ) -> str:
        if planned_tool not in _DIRECT_DAILY_DESKTOP_TOOLS:
            return ""
        tool_event = self._latest_tool_call_event(timeline, planned_tool)
        if not tool_event:
            return ""
        result = tool_event.get("result") if isinstance(tool_event.get("result"), dict) else {}
        if result.get("approval_required"):
            return ""
        result = _with_retry_recovery_action(planned_tool, planned_input, result)
        tool_event["result"] = result
        executed_input = (
            tool_event.get("input_preview")
            if isinstance(tool_event.get("input_preview"), dict)
            else planned_input
        )
        clean_presentation = str(presentation or "").strip()
        summary = self._daily_desktop_summary(
            planned_tool,
            executed_input,
            result,
            presentation=clean_presentation,
        )
        if not summary:
            return ""
        event_payload = {
            "tool": planned_tool,
            "source": str(source or "daily_desktop_intent"),
            "input_preview": executed_input,
            "result": result,
            "summary": summary,
        }
        tool_call_id = str(
            tool_event.get("tool_call_id") or tool_event.get("id") or ""
        ).strip()
        if tool_call_id:
            event_payload["tool_call_id"] = tool_call_id
        clean_planning_reason = str(planning_reason or "").strip()
        if clean_planning_reason:
            event_payload["planning_reason"] = clean_planning_reason
        if clean_presentation:
            event_payload["presentation"] = clean_presentation
        completed_event = self._timeline(
            "agent.desktop.intent_completed",
            planned_tool,
            **event_payload,
        )
        goal_gate = evaluate_goal_contract_outcome(
            {"run_id": run_id},
            [*timeline, completed_event],
        )
        aggregate_outcome = evaluate_main_chat_outcome(
            {},
            [completed_event],
        )
        completion_outcome = goal_gate or aggregate_outcome
        if not completion_outcome.allows_completion:
            try:
                self._raise_unverified_media_completion(
                    (
                        aggregate_outcome
                        if planned_tool.startswith("media.")
                        and not aggregate_outcome.allows_completion
                        else completion_outcome
                    ),
                    [event_payload],
                    summary,
                    media_intent=planned_tool.startswith("media."),
                )
            except AgentDirectOutcomeUnverified:
                self._record_desktop_permission_recovery_events(
                    [event_payload],
                    timeline,
                    run_id=run_id,
                )
                raise
            return ""
        timeline.append(completed_event)
        if run_id and self._append_run_event is not None:
            self._append_run_event(run_id, "agent.desktop.intent_completed", event_payload)
        self._record_desktop_permission_recovery_events(
            [event_payload],
            timeline,
            run_id=run_id,
        )
        return summary

    def _direct_daily_desktop_sequence_result(
        self,
        planned_tool_requests: list[dict[str, Any]],
        timeline: list[dict[str, Any]],
        *,
        tool_timeline_start: int,
        run_id: str = "",
        allow_honest_partial: bool = False,
    ) -> str:
        tool_events = [
            event
            for event in timeline[tool_timeline_start:]
            if event.get("event") in {"agent.tool.call", "agent.tool.skipped"}
        ]
        planned_tool_requests = _direct_sequence_requests_with_safe_deferred_continuations(
            planned_tool_requests
        )
        sequence_media_intent = _tool_requests_require_media_playback(
            planned_tool_requests
        )
        if any(
            bool(request.get("continue_to_model"))
            and str(request.get("tool") or "").strip()
            not in {*_DAILY_DESKTOP_DISCOVERY_TOOLS, *_DAILY_DESKTOP_VERIFY_TOOLS}
            and not _direct_daily_runtime_verification_request(request)
            for request in planned_tool_requests
            if isinstance(request, dict)
        ):
            return ""
        event_index = 0
        completed_steps: list[dict[str, Any]] = []
        stopped_after_recovery = False
        for request_index, planned_tool_request in enumerate(planned_tool_requests):
            planned_tool = str(planned_tool_request.get("tool") or "")
            if (
                planned_tool not in _DIRECT_DAILY_DESKTOP_TOOLS
                and planned_tool not in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
            ):
                return ""
            planned_input = planned_tool_request.get("input")
            if not isinstance(planned_input, dict):
                planned_input = {}
            direct_runtime_verification = _direct_daily_runtime_verification_request(
                planned_tool_request
            )
            tool_event: dict[str, Any] | None = None
            while event_index < len(tool_events):
                candidate = tool_events[event_index]
                event_index += 1
                candidate_tool = str(candidate.get("detail") or "")
                if not (
                    candidate_tool == planned_tool
                    or _followup_plan_tools_match(candidate_tool, planned_tool)
                ):
                    continue
                planned_plan_id = str(planned_tool_request.get("plan_id") or "").strip()
                candidate_plan_id = str(candidate.get("plan_id") or "").strip()
                if (
                    planned_plan_id
                    and candidate_plan_id
                    and candidate_plan_id != planned_plan_id
                ):
                    continue
                planned_step_id = str(
                    planned_tool_request.get("step_id")
                    or planned_tool_request.get("planner_step_id")
                    or ""
                ).strip()
                candidate_step_id = str(
                    candidate.get("step_id") or candidate.get("planner_step_id") or ""
                ).strip()
                if (
                    planned_step_id
                    and candidate_step_id
                    and candidate_step_id != planned_step_id
                ):
                    continue
                candidate_result = (
                    candidate.get("result") if isinstance(candidate.get("result"), dict) else {}
                )
                if candidate_result.get("approval_required"):
                    continue
                tool_event = candidate
                break
            if tool_event is None:
                remaining_requests = planned_tool_requests[request_index:]
                if completed_steps and _all_noncritical_daily_desktop_observations(
                    remaining_requests
                ):
                    break
                return ""
            result = tool_event.get("result") if isinstance(tool_event.get("result"), dict) else {}
            if result.get("approval_required"):
                return ""
            if planned_tool_request.get("replan_request_id") and (
                result.get("ok") is False or result.get("verification_failed") is True
            ):
                return ""
            if (
                result.get("ok") is False
                and planned_tool not in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
                and planned_tool not in _DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS
                and not _has_permission_recovery_signal(result)
            ):
                remaining_requests = planned_tool_requests[request_index:]
                if completed_steps and _all_noncritical_daily_desktop_observations(
                    remaining_requests
                ):
                    stopped_after_recovery = True
                    break
                return ""
            result = _with_retry_recovery_action(planned_tool, planned_input, result)
            tool_event["result"] = result
            if (
                planned_tool in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
                and result.get("ok") is False
            ):
                return ""
            executed_input = (
                tool_event.get("input_preview")
                if isinstance(tool_event.get("input_preview"), dict)
                else planned_input
            )
            if planned_tool in {
                "app.open_and_safe_type_text",
                "app.focus_and_safe_type_text",
            }:
                executed_input = {**planned_input, **executed_input}
            if (
                not direct_runtime_verification
                and _daily_desktop_step_has_placeholder_app(
                    planned_input,
                    executed_input,
                    result,
                )
            ):
                return ""
            presentation = str(planned_tool_request.get("presentation") or "").strip()
            summary = (
                ""
                if (
                    planned_tool in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
                    or direct_runtime_verification
                )
                else self._daily_desktop_summary(
                    planned_tool,
                    executed_input,
                    result,
                    presentation=presentation,
                )
            )
            if (
                not summary
                and planned_tool not in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
                and not direct_runtime_verification
            ):
                return ""
            completed_step = {
                "tool": planned_tool,
                "input_preview": executed_input,
                "result": result,
                "summary": summary,
            }
            execution_source = str(
                tool_event.get("source") or planned_tool_request.get("source") or ""
            ).strip()
            if execution_source:
                # Keep the authoritative execution source on aggregate steps.
                # Native-receipt verifiers are projected after the action and
                # the terminal outcome evaluator deliberately requires this
                # provenance before it trusts the receipt.
                completed_step["source"] = execution_source
            tool_call_id = str(
                tool_event.get("tool_call_id") or tool_event.get("id") or ""
            ).strip()
            if tool_call_id:
                completed_step["tool_call_id"] = tool_call_id
            request_id = str(
                tool_event.get("request_id")
                or planned_tool_request.get("request_id")
                or ""
            ).strip()
            if request_id:
                # The aggregate step is a lifecycle projection of this exact
                # execution, not a retry. Preserve the authoritative request
                # identity so the terminal evaluator can distinguish it from
                # a later same-step action without weakening fail-closed
                # receipt correlation.
                completed_step["request_id"] = request_id
            completed_step.update(_request_observability_metadata(planned_tool_request))
            if presentation:
                completed_step["presentation"] = presentation
            completed_steps.append(completed_step)
            if result.get("ok") is False and _has_permission_recovery_signal(result):
                stopped_after_recovery = True
                break
        visible_steps = _visible_daily_desktop_completed_steps(completed_steps)
        summary_steps = _daily_desktop_sequence_summary_steps(visible_steps)
        summary = _combine_daily_desktop_summaries(
            [
                str(step.get("summary") or "")
                for step in summary_steps
                if isinstance(step, dict)
            ]
        )
        if not summary:
            return ""
        last_step = summary_steps[-1] if summary_steps else visible_steps[-1]
        completed_tools_steps = (
            visible_steps
            if any(
                isinstance(event.get("result"), dict)
                and event["result"].get("approval_required")
                for event in tool_events
            )
            else completed_steps
        )
        clean_source = str(
            (planned_tool_requests[0].get("source") if planned_tool_requests else "")
            or "daily_desktop_intent"
        )
        planning_reasons = {
            str(request.get("planning_reason") or "").strip()
            for request in planned_tool_requests
            if str(request.get("planning_reason") or "").strip()
        }
        event_payload = {
            "tool": str(last_step.get("tool") or ""),
            "tools": (
                [str(step.get("tool") or "") for step in completed_tools_steps]
                if stopped_after_recovery
                else _planned_daily_desktop_tools(planned_tool_requests)
                or [str(step.get("tool") or "") for step in completed_tools_steps]
            ),
            "input_preview": (
                last_step.get("input_preview") if isinstance(last_step.get("input_preview"), dict) else {}
            ),
            "result": last_step.get("result") if isinstance(last_step.get("result"), dict) else {},
            "source": clean_source,
            "steps": completed_steps,
            "summary": summary,
        }
        verification_evidence = _desktop_intent_verification_evidence(completed_steps)
        if verification_evidence:
            event_payload.update(verification_evidence)
            event_payload["verification_evidence"] = verification_evidence
        if len(planning_reasons) == 1:
            event_payload["planning_reason"] = next(iter(planning_reasons))
        event_payload.update(_step_observability_metadata(last_step))
        # The aggregate completion is not a second execution of the visible
        # action. Nested action facts keep their strict verification contract.
        event_payload["requires_post_action_verification"] = False
        presentation = str(last_step.get("presentation") or "").strip()
        if presentation:
            event_payload["presentation"] = presentation
        completed_event = self._timeline(
            "agent.desktop.intent_completed",
            str(last_step.get("tool") or ""),
            **event_payload,
        )
        goal_gate = evaluate_goal_contract_outcome(
            {"run_id": run_id},
            [*timeline, completed_event],
        )
        aggregate_outcome = evaluate_main_chat_outcome(
            {},
            [*timeline[tool_timeline_start:], completed_event],
        )
        completion_outcome = goal_gate or aggregate_outcome
        if not completion_outcome.allows_completion:
            if allow_honest_partial:
                # Recovery enrichment may fail while the first tool attempt
                # still produced a truthful, useful partial result (for
                # example, a bounded media-library miss).  Preserve that text
                # without projecting a completion event or weakening the Goal
                # contract; the caller returns it only after the recovery loop
                # has exhausted its bounded alternatives.
                return summary
            try:
                self._raise_unverified_media_completion(
                    (
                        aggregate_outcome
                        if sequence_media_intent
                        and not aggregate_outcome.allows_completion
                        else completion_outcome
                    ),
                    completed_steps,
                    summary,
                    media_intent=sequence_media_intent,
                )
            except AgentDirectOutcomeUnverified:
                self._record_desktop_permission_recovery_events(
                    summary_steps,
                    timeline,
                    run_id=run_id,
                )
                raise
            return ""
        timeline.append(completed_event)
        if run_id and self._append_run_event is not None:
            self._append_run_event(run_id, "agent.desktop.intent_completed", event_payload)
        self._record_desktop_permission_recovery_events(
            summary_steps,
            timeline,
            run_id=run_id,
        )
        return summary

    def _record_desktop_permission_recovery_events(
        self,
        steps: Iterable[Mapping[str, Any]],
        timeline: list[dict[str, Any]],
        *,
        run_id: str = "",
    ) -> None:
        for step in steps:
            tool_call_id = str(step.get("tool_call_id") or "").strip()
            if tool_call_id and any(
                event.get("event") == "agent.desktop.permission_recovery"
                and str(event.get("tool_call_id") or "").strip() == tool_call_id
                for event in timeline
            ):
                continue
            recovery_payload = _desktop_permission_recovery_event_payload(
                str(step.get("tool") or ""),
                step.get("input_preview") if isinstance(step.get("input_preview"), dict) else {},
                step.get("result") if isinstance(step.get("result"), dict) else {},
            )
            if recovery_payload:
                if tool_call_id:
                    recovery_payload["tool_call_id"] = tool_call_id
                timeline.append(
                    self._timeline(
                        "agent.desktop.permission_recovery",
                        str(step.get("tool") or ""),
                        **recovery_payload,
                    )
                )
                if run_id and self._append_run_event is not None:
                    self._append_run_event(
                        run_id,
                        "agent.desktop.permission_recovery",
                        recovery_payload,
                    )

    def _record_timeline_desktop_permission_recovery_events(
        self,
        timeline: list[dict[str, Any]],
        *,
        tool_timeline_start: int,
        run_id: str = "",
    ) -> None:
        steps: list[dict[str, Any]] = []
        for event in timeline[tool_timeline_start:]:
            if event.get("event") not in {"agent.tool.call", "agent.tool.skipped"}:
                continue
            result = event.get("result") if isinstance(event.get("result"), dict) else {}
            if not _has_permission_recovery_signal(result):
                continue
            tool_name = str(event.get("detail") or "").strip()
            input_preview = (
                event.get("input_preview")
                if isinstance(event.get("input_preview"), dict)
                else {}
            )
            enriched_result = _with_retry_recovery_action(
                tool_name,
                input_preview,
                result,
            )
            event["result"] = enriched_result
            steps.append(
                {
                    "tool": tool_name,
                    "input_preview": input_preview,
                    "result": enriched_result,
                    "tool_call_id": str(event.get("tool_call_id") or "").strip(),
                }
            )
        self._record_desktop_permission_recovery_events(
            steps,
            timeline,
            run_id=run_id,
        )

    def _raise_unverified_media_completion(
        self,
        completion_outcome: Any,
        completed_steps: Iterable[Mapping[str, Any]],
        summary: str,
        *,
        media_intent: bool = False,
    ) -> None:
        completion_reason = str(
            getattr(completion_outcome, "reason", "") or ""
        ).strip()
        unverified_reasons = {
            "desktop_permission_required",
            "desktop_tool_failed",
            "desktop_verification_missing",
            "desktop_verification_failed",
            "goal_contract_incomplete",
        }
        if completion_reason not in unverified_reasons:
            return
        completed_step_list = list(completed_steps)
        if not media_intent and not any(
            _direct_step_requires_honest_unverified_outcome(step)
            for step in completed_step_list
        ):
            return
        executed_direct_steps = [
            step
            for step in completed_step_list
            if str(step.get("tool") or "").strip()
            and isinstance(step.get("result"), Mapping)
            and _direct_step_requires_honest_unverified_outcome(step)
        ]
        unverified_direct_steps = [
            step
            for step in executed_direct_steps
            if (
                completion_reason == "desktop_permission_required"
                and isinstance(step.get("result"), Mapping)
                and _has_permission_recovery_signal(dict(step["result"]))
            )
            or (
                completion_reason == "goal_contract_incomplete"
                and _direct_step_requires_honest_unverified_outcome(step)
            )
            or _direct_step_unverified_outcome_reason(step)
            in unverified_reasons
        ]
        if not unverified_direct_steps:
            return
        unverified_step = unverified_direct_steps[-1]
        unverified_result = (
            unverified_step.get("result")
            if isinstance(unverified_step.get("result"), Mapping)
            else {}
        )
        preserve_failure_summary = completion_reason == "desktop_tool_failed" or (
            completion_reason == "desktop_permission_required"
            and unverified_result.get("ok") is False
        )
        if preserve_failure_summary:
            failure_summary = summary
        elif media_intent or str(unverified_step.get("tool") or "").strip().startswith(
            "media."
        ):
            failure_summary = _unverified_media_action_failure_summary(
                str(unverified_step.get("summary") or summary)
            )
        else:
            failure_summary = _unverified_desktop_action_failure_summary(
                str(unverified_step.get("summary") or summary)
            )
        reported_reason = (
            "desktop_verification_missing"
            if completion_reason == "goal_contract_incomplete"
            else completion_reason
        )
        raise AgentDirectOutcomeUnverified(
            failure_summary,
            reason=reported_reason,
            tool_name=str(unverified_step.get("tool") or "").strip(),
            tool_call_id=str(unverified_step.get("tool_call_id") or "").strip(),
            input_preview=(
                dict(unverified_step.get("input_preview"))
                if isinstance(unverified_step.get("input_preview"), Mapping)
                else {}
            ),
        )

    @staticmethod
    def _latest_tool_call_event(
        timeline: list[dict[str, Any]],
        planned_tool: str,
    ) -> dict[str, Any] | None:
        for event in reversed(timeline):
            if event.get("event") != "agent.tool.call":
                continue
            if str(event.get("detail") or "") == planned_tool:
                return event
        return None

    @staticmethod
    def _daily_desktop_summary(
        tool_name: str,
        planned_input: dict[str, Any],
        result: dict[str, Any],
        *,
        presentation: str = "",
    ) -> str:
        result_summary = str(result.get("summary") or "").strip()
        if result.get("ok"):
            if tool_name in {"app.open", "desktop.open_app"}:
                app_name = _payload_text(result, planned_input, "app_name")
                data = result.get("data") if isinstance(result.get("data"), dict) else {}
                if data.get("launch_verified") is False and app_name:
                    return f"已向 macOS 发送打开{_display_target_name(app_name, '的请求')}，但未能确认它已启动。"
                return f"已打开{_display_target_name(app_name)}。" if app_name else (result_summary or "已打开应用。")
            if tool_name in {"app.focus", "desktop.focus_app"}:
                app_name = _payload_text(result, planned_input, "app_name")
                return f"已切换到{_display_target_name(app_name)}。" if app_name else (result_summary or "已切换到应用。")
            if tool_name == "app.focus_window":
                app_name = _payload_text(result, planned_input, "app_name")
                title = _payload_text(result, planned_input, "window_title") or _payload_text(
                    result,
                    planned_input,
                    "title_contains",
                )
                if app_name and title:
                    return f"已切换到{_display_target_name(app_name, f'的 {title} 窗口')}。"
                return result_summary or "已切换到指定窗口。"
            if tool_name in {"app.open_and_safe_type_text", "app.focus_and_safe_type_text"}:
                app_name = _payload_text(result, planned_input, "app_name")
                text = _payload_text(result, planned_input, "text")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                if text:
                    detail = f"并输入文字（{len(text)} 个字符）"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并输入文字")
                return result_summary or f"已{action}{target or '并输入文字'}。"
            if tool_name in {"app.open_and_safe_shortcut", "app.focus_and_safe_shortcut"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                shortcut = _safe_shortcut_summary(result, planned_input)
                if shortcut:
                    detail = f"并{shortcut.removeprefix('已').removesuffix('。')}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并执行快捷动作")
                return result_summary or f"已{action}{target or '并执行快捷动作'}。"
            if tool_name in {"app.open_and_safe_key", "app.focus_and_safe_key"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                safe_key = _safe_key_summary(result, planned_input)
                if safe_key:
                    detail = f"并{safe_key.removeprefix('已').removesuffix('。')}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并按前台导航键")
                return result_summary or f"已{action}{target or '并按前台导航键'}。"
            if tool_name in {"app.open_and_hotkey", "app.focus_and_hotkey"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                hotkey = _hotkey_text(result, planned_input)
                if hotkey:
                    detail = f"并发送快捷键：{hotkey}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并发送快捷键")
                return result_summary or f"已{action}{target or '并发送快捷键'}。"
            if tool_name in {"app.open_and_safe_scroll", "app.focus_and_safe_scroll"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                safe_scroll = _safe_scroll_summary(result, planned_input)
                if safe_scroll:
                    detail = f"并{safe_scroll.removeprefix('已').removesuffix('。')}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并滚动前台界面")
                return result_summary or f"已{action}{target or '并滚动前台界面'}。"
            if tool_name in {"app.open_and_safe_click", "app.focus_and_safe_click"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                click = _click_text(result, planned_input)
                if click:
                    detail = f"并点击前台位置：{click}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并点击前台界面")
                return result_summary or f"已{action}{target or '并点击前台界面'}。"
            if tool_name in {"app.open_and_click_ui_element", "app.focus_and_click_ui_element"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                click_ui = _click_ui_element_summary(result, planned_input)
                if click_ui:
                    detail = f"并{click_ui.removeprefix('已').removesuffix('。')}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并点击前台控件")
                return result_summary or f"已{action}{target or '并点击前台控件'}。"
            if tool_name in {"app.open_and_type_into_ui_element", "app.focus_and_type_into_ui_element"}:
                app_name = _payload_text(result, planned_input, "app_name")
                action = "打开" if tool_name.startswith("app.open") else "切到"
                type_into = _type_into_ui_element_summary(result, planned_input)
                if type_into:
                    detail = f"并{type_into.removeprefix('已').removesuffix('。')}"
                    target = _display_target_name(app_name, detail)
                    return f"已{action}{target or detail}。"
                target = _display_target_name(app_name, "并填写前台控件")
                return result_summary or f"已{action}{target or '并填写前台控件'}。"
            if tool_name == "app.show":
                app_name = _payload_text(result, planned_input, "app_name")
                data = result.get("data") if isinstance(result.get("data"), dict) else {}
                if data.get("show_status") == "launched" and app_name:
                    return f"已打开并显示{_display_target_name(app_name)}。"
                return f"已显示{_display_target_name(app_name)}。" if app_name else (result_summary or "已显示应用。")
            if tool_name == "app.hide":
                app_name = _payload_text(result, planned_input, "app_name")
                return f"已隐藏{_display_target_name(app_name)}。" if app_name else (result_summary or "已隐藏应用。")
            if tool_name == "app.minimize":
                app_name = _payload_text(result, planned_input, "app_name")
                return f"已最小化{_display_target_name(app_name)}。" if app_name else (result_summary or "已最小化应用。")
            if tool_name == "app.quit":
                app_name = _payload_text(result, planned_input, "app_name")
                data = result.get("data") if isinstance(result.get("data"), dict) else {}
                if data.get("running") is True and app_name:
                    return f"已向{_display_target_name(app_name, '发送退出请求')}，但它可能仍在运行。"
                return f"已退出{_display_target_name(app_name)}。" if app_name else (result_summary or "已退出应用。")
            if tool_name == "media.apple_music_play":
                data = result.get("data") if isinstance(result.get("data"), dict) else {}
                track = str(data.get("track") or "").strip()
                artist = str(data.get("artist") or "").strip()
                query = _payload_text(result, planned_input, "query")
                if (
                    data.get("status") == "not_found"
                    and data.get("outcome") == "partial"
                    and data.get("background_safe") is True
                    and data.get("library_search_completed") is True
                    and data.get("foreground_action_taken") is False
                    and data.get("playback_started") is False
                    and data.get("search_opened") is False
                    and data.get("user_action_required") is False
                ):
                    target = f"「{query}」" if query else "目标音乐"
                    if data.get("catalog_lookup_completed") is True:
                        return (
                            f"已在后台检查 Apple Music 本地曲库和官方目录，"
                            f"但没有找到{target}的精确匹配。"
                            "为避免抢占你的桌面，没有打开前台搜索，也没有开始播放。"
                        )
                    if data.get("catalog_lookup_status"):
                        return (
                            f"Apple Music 本地曲库中没有找到{target}，"
                            "官方目录查询也未能完成。"
                            "为避免误报或抢占桌面，本次没有开始播放。"
                        )
                    return (
                        f"已在后台检查 Apple Music 本地曲库，但没有找到{target}。"
                        "为避免抢占你的桌面，没有打开前台搜索，也没有开始播放。"
                    )
                if (
                    data.get("outcome") == "partial"
                    and data.get("user_action_required") is False
                    and data.get("playback_started") is False
                    and data.get("status")
                    in {"playback_unverified", "catalog_playback_unverified"}
                ):
                    target = f"「{track or query}」" if track or query else "目标音乐"
                    if data.get("foreground_action_taken") is True:
                        return (
                            f"Apple Music 找到了{target}，但播放状态未能安全确认，"
                            "且 Music 意外切到了前台，因此没有宣称任务成功。"
                        )
                    return (
                        f"Apple Music 找到了{target}，但没有验证到它正在播放。"
                        "为避免误报，本次按未播放处理。"
                    )
                if (
                    data.get("status") == "not_found"
                    and data.get("search_opened") is True
                    and data.get("playback_started") is False
                    and data.get("outcome") == "partial"
                    and data.get("user_action_required") is True
                ):
                    target = f"「{query}」" if query else "这首音乐"
                    return (
                        f"已打开 Apple Music，但没有找到可直接播放的{target}。"
                        "你可以换个关键词，或在 Music 中选择结果。"
                    )
                if track:
                    return f"已在 Apple Music 播放：{track}{f' - {artist}' if artist else ''}。"
                return f"已尝试在 Apple Music 播放：{query}。" if query else (result_summary or "已尝试播放。")
            if tool_name == "media.apple_music_status":
                return _apple_music_status_summary(result) or result_summary or "已读取 Apple Music 播放状态。"
            if tool_name == "media.apple_music_open_and_play":
                data = result.get("data") if isinstance(result.get("data"), dict) else {}
                track = str(data.get("track") or "").strip()
                artist = str(data.get("artist") or "").strip()
                track_text = f"当前：{track}{f' - {artist}' if artist else ''}。" if track else ""
                if data.get("playback_state_unverified"):
                    return f"已打开 Apple Music，并用媒体键尝试开始播放。{track_text}"
                return f"已打开 Apple Music 并开始播放。{track_text}"
            if tool_name == "media.apple_music_control":
                return _apple_music_control_summary(result, planned_input) or result_summary or "已控制 Apple Music。"
            if tool_name == "media.music_app_open_and_play":
                return _music_app_open_and_play_summary(result, planned_input) or result_summary or "已尝试播放音乐。"
            if tool_name == "media.music_app_control":
                return _music_app_control_summary(result, planned_input) or result_summary or "已尝试控制音乐应用。"
            if tool_name == "media.system_control":
                return _system_media_control_summary(result, planned_input) or result_summary or "已尝试控制当前媒体。"
            if tool_name == "system.settings_open":
                return _system_settings_open_summary(result, planned_input) or result_summary or "已打开系统设置。"
            if tool_name == "system.volume":
                return _system_volume_summary(result, planned_input) or result_summary or "已处理系统音量。"
            if tool_name == "system.brightness":
                return _system_brightness_summary(result, planned_input) or result_summary or "已调整屏幕亮度。"
            if tool_name == "system.display_sleep":
                return "已让显示器睡眠。"
            if tool_name == "system.screen_saver_start":
                return "已启动屏幕保护程序。"
            if tool_name == "clipboard.write":
                return _clipboard_write_summary(result, planned_input) or result_summary or "已写入剪贴板。"
            if tool_name == "clipboard.read":
                return _clipboard_read_summary(result) or result_summary or "已读取剪贴板。"
            if tool_name == "notes.create":
                return _notes_create_summary(result, planned_input) or result_summary or "已创建备忘录。"
            if tool_name == "artifact.write":
                path = str(result.get("path") or planned_input.get("path") or "").strip()
                return f"已生成文件：{path}。" if path else (result_summary or "已写入 Artifact。")
            if tool_name == "reminders.create":
                return _reminders_create_summary(result, planned_input) or result_summary or "已创建提醒事项。"
            if tool_name == "calendar.create_event":
                return _calendar_create_event_summary(result, planned_input) or result_summary or "已创建日历事件。"
            if tool_name == "screen.capture":
                return result_summary or "已截取当前屏幕。"
            if tool_name == "desktop.permissions":
                return _desktop_permissions_summary(result) or result_summary or "已检查桌面权限。"
            if tool_name == "desktop.active_window":
                return _active_window_summary(result) or result_summary or "已读取当前前台窗口。"
            if tool_name == "desktop.list_apps":
                return _installed_apps_summary(result) or result_summary or "已发现已安装应用。"
            if tool_name == "desktop.running_apps":
                return _running_apps_summary(result) or result_summary or "已读取运行中的应用。"
            if tool_name in {"desktop.windows", "desktop.list_windows"}:
                return _windows_summary(result, planned_input) or result_summary or "已读取窗口列表。"
            if tool_name == "desktop.ui_elements":
                return _ui_elements_summary(result) or result_summary or "已读取当前界面控件。"
            if tool_name == "desktop.inspect_app":
                return _inspect_app_result_summary(result) or result_summary or "已检查应用。"
            if tool_name == "app.status":
                return _app_status_summary(result, planned_input) or result_summary or "已检查应用状态。"
            if tool_name == "desktop.reveal_path":
                path = _desktop_path_summary_path(result, planned_input)
                return f"已在 Finder 中显示：{path}。" if path else (result_summary or "已在 Finder 中显示。")
            if tool_name == "desktop.open_path":
                return _desktop_open_path_summary(result, planned_input) or result_summary or "已打开本地路径。"
            if tool_name in _OPEN_PATH_WITH_APP_TOOLS:
                return (
                    _desktop_open_path_with_app_summary(result, planned_input)
                    or result_summary
                    or "已用应用打开本地路径。"
                )
            if tool_name == "desktop.hide_app":
                return "已发送隐藏当前应用指令。"
            if tool_name == "desktop.show_all_apps":
                return "已显示所有隐藏应用。"
            if tool_name == "desktop.minimize_window":
                return "已发送最小化当前窗口指令。"
            if tool_name == "desktop.close_window":
                return "已发送关闭当前窗口指令。"
            if tool_name == "desktop.quit_app":
                return "已发送退出当前应用请求。"
            if tool_name == "data.analyze":
                return _data_analyze_summary(result, planned_input) or result_summary or "已分析数据。"
            if tool_name == "terminal.run":
                return _terminal_run_summary(result, planned_input)
            if tool_name == "browser.open_url":
                url = _payload_text(result, planned_input, "url")
                if result.get("fallback_used") and result.get("fallback") == "system_browser":
                    return f"已用系统浏览器打开网页：{url}。" if url else (result_summary or "已用系统浏览器打开网页。")
                return f"已打开网页：{url}。" if url else (result_summary or "已打开网页。")
            if tool_name == "browser.open_url_and_extract_text":
                if result.get("ok") is True:
                    if presentation == "summary":
                        return _browser_text_digest_summary(result)
                    return _browser_text_summary(result)
                return result_summary or _browser_text_summary(result)
            if tool_name == "browser.open_url_and_screenshot":
                if result.get("ok") is True:
                    return "已打开网页并截取当前网页。"
                return result_summary or "已打开网页，但没能截取当前网页。"
            if tool_name == "browser.current_page":
                if result.get("ok") is True:
                    return _browser_page_summary(result)
                return result_summary or _browser_page_summary(result)
            if tool_name == "browser.click":
                if result.get("ok") is True:
                    return _browser_click_summary(result, planned_input)
                return result_summary or "没能点击网页元素。"
            if tool_name == "browser.type_text":
                if result.get("ok") is True:
                    return _browser_type_text_summary(result, planned_input)
                return result_summary or "没能填写网页输入。"
            if tool_name == "browser.extract_text":
                if result.get("ok") is True:
                    if presentation == "summary":
                        return _browser_text_digest_summary(result)
                    return _browser_text_summary(result)
                return result_summary or _browser_text_summary(result)
            if tool_name == "browser.screenshot":
                if result.get("ok") is True:
                    return "已截取当前网页。"
                return result_summary or "已截取当前网页。"
            if tool_name == "desktop.safe_shortcut":
                return _safe_shortcut_summary(result, planned_input) or result_summary or "已执行快捷动作。"
            if tool_name == "desktop.safe_key":
                return _safe_key_summary(result, planned_input) or result_summary or "已按前台导航键。"
            if tool_name == "desktop.safe_type_text":
                text = _payload_text(result, planned_input, "text")
                if text:
                    return f"已向前台输入文字（{len(text)} 个字符）。"
                return result_summary or "已向前台输入文字。"
            if tool_name == "desktop.safe_click":
                click = _click_text(result, planned_input)
                return f"已点击前台位置：{click}。" if click else (result_summary or "已点击前台界面。")
            if tool_name == "desktop.safe_scroll":
                return _safe_scroll_summary(result, planned_input) or result_summary or "已滚动前台界面。"
            if tool_name == "desktop.search_submit":
                return "已提交前台搜索。" if result.get("ok") is True else (result_summary or "没能提交前台搜索。")
            if tool_name == "desktop.click_ui_element":
                return _click_ui_element_summary(result, planned_input) or result_summary or "已点击前台控件。"
            if tool_name == "desktop.type_into_ui_element":
                return _type_into_ui_element_summary(result, planned_input) or result_summary or "已填写前台控件。"
            if tool_name in {"desktop.hotkey", "desktop.shortcut"}:
                hotkey = _hotkey_text(result, planned_input)
                return f"已发送快捷键：{hotkey}。" if hotkey else (result_summary or "已发送快捷键。")
            if tool_name == "desktop.submit_foreground":
                action = _payload_text(result, planned_input, "submit_action") or _payload_text(
                    result,
                    planned_input,
                    "action",
                )
                return {
                    "send": "已向前台发送“发送”指令。",
                    "submit": "已向前台发送提交指令。",
                    "confirm": "已向前台发送确认指令。",
                }.get(action, result_summary or "已向前台发送提交按键。")
            if tool_name == "desktop.type_text":
                text = _payload_text(result, planned_input, "text")
                if text:
                    return f"已向前台输入文字（{len(text)} 个字符）。"
                return result_summary or "已向前台输入文字。"
            if tool_name == "desktop.click":
                click = _click_text(result, planned_input)
                return f"已点击前台位置：{click}。" if click else (result_summary or "已点击前台界面。")
            return result_summary or "已执行桌面操作。"

        error = str(result.get("error") or result_summary or "工具返回失败").strip()
        permission_targets = result.get("permission_targets")
        fallback = result.get("fallback_result") if isinstance(result.get("fallback_result"), dict) else {}
        if result.get("blocked_by_runtime_readiness"):
            conditions = _text_list(result.get("blocking_conditions")) or ([error] if error else [])
            condition_text = ", ".join(conditions)
            source_summary = str(result.get("source_summary") or "").strip()
            detail = (
                f"前置检查未确认目标应用可接收前台输入：{condition_text}。"
                if condition_text
                else "前置检查未确认目标应用可接收前台输入。"
            )
            if source_summary:
                detail = f"{detail}{source_summary}。"
            return _append_recovery_action_summary(f"桌面操作已暂停：{detail}", result)
        if tool_name == "app.open" and str(result.get("error_code") or "") == "app_not_found":
            app_name = _payload_text(result, planned_input, "app_name")
            target = _display_target_name(app_name)
            diagnostics = _permission_diagnostics(result)
            return _append_recovery_action_summary(
                f"已尝试启动{target}，但 macOS 没找到这个应用。{diagnostics}".strip(),
                result,
            )
        if tool_name in {"browser.open_url_and_extract_text", "browser.open_url_and_screenshot"}:
            opened = fallback.get("open") if isinstance(fallback.get("open"), dict) else {}
            if opened.get("ok"):
                targets = ", ".join(str(item) for item in permission_targets or [] if str(item))
                diagnostics = _permission_diagnostics(result)
                suffix = f" 缺少权限：{targets}。" if targets else ""
                partial_summary = (
                    "已打开网页，但没能读取网页文本。"
                    if tool_name == "browser.open_url_and_extract_text"
                    else "已打开网页，但没能截取当前网页。"
                )
                return _append_recovery_action_summary(
                    f"{partial_summary}{suffix}{diagnostics}".strip(),
                    result,
                )
        if tool_name in _APP_FOREGROUND_ACTION_TOOLS:
            foreground_summary = _app_foreground_action_failed_summary(
                tool_name,
                planned_input,
                result,
            )
            if foreground_summary:
                return foreground_summary
        if result.get("permission_error") or permission_targets:
            targets = ", ".join(str(item) for item in permission_targets or [] if str(item))
            diagnostics = _permission_diagnostics(result)
            suffix = f" 缺少权限：{targets}。" if targets else ""
            return _append_recovery_action_summary(
                f"桌面操作未完成：{_sentence(error)}{suffix}{diagnostics}".strip(),
                result,
            )
        if tool_name == "media.apple_music_play" and fallback.get("ok"):
            query = _payload_text(result, planned_input, "query")
            if str(fallback.get("action") or "") == "media.apple_music.search":
                return (
                    f"没能直接播放 {query}，但已打开 Apple Music 搜索。"
                    if query
                    else "没能直接播放，但已打开 Apple Music 搜索。"
                )
            return (
                f"没能直接播放 {query}，但已打开 Apple Music。"
                if query
                else "没能直接播放，但已打开 Apple Music。"
            )
        if tool_name == "media.apple_music_open_and_play":
            control = fallback.get("control") if isinstance(fallback.get("control"), dict) else {}
            opened = fallback.get("open") if isinstance(fallback.get("open"), dict) else {}
            if control.get("fallback_used") or opened.get("ok"):
                return "已打开 Apple Music，但没能直接开始播放。"
        if tool_name == "media.apple_music_control" and fallback.get("ok"):
            action = _payload_text(result, planned_input, "action")
            label = _apple_music_control_label(action)
            return f"没能直接{label}，但已打开 Apple Music。" if label else "没能直接控制，但已打开 Apple Music。"
        if tool_name == "terminal.run":
            return _terminal_run_summary(result, planned_input)
        diagnostics = _permission_diagnostics(result)
        return f"桌面操作未完成：{_sentence(error)}{diagnostics}".strip()

    def _direct_existing_daily_desktop_result(
        self,
        agent: dict[str, Any],
        timeline: list[dict[str, Any]],
        *,
        run_id: str = "",
    ) -> str:
        sequence_result = self._direct_existing_daily_desktop_sequence_result(
            timeline,
            run_id=run_id,
        )
        if sequence_result:
            return sequence_result
        sequence = self._latest_uncompleted_daily_desktop_sequence(timeline)
        if sequence is not None:
            # A verifier/observation is not a substitute for the action whose
            # postcondition it was meant to prove. If sequence aggregation
            # cannot establish completion, do not summarize the last read-only
            # tool as though it satisfied the user's original side effect.
            return ""
        tool_event = self._latest_tool_call_event_for_daily_desktop_intent(timeline)
        if not tool_event:
            return ""
        tool_name = str(tool_event.get("detail") or tool_event.get("tool") or "").strip()
        planned_input = self._latest_daily_desktop_input(timeline, tool_name)
        if planned_input is None:
            return ""
        return self._direct_daily_desktop_result(
            agent,
            tool_name,
            planned_input,
            timeline,
            run_id=run_id,
        )

    def _direct_existing_daily_desktop_sequence_result(
        self,
        timeline: list[dict[str, Any]],
        *,
        run_id: str = "",
    ) -> str:
        sequence = self._latest_uncompleted_daily_desktop_sequence(timeline)
        if sequence is None:
            return ""
        requests = _visible_daily_desktop_completed_steps(sequence["requests"])
        if not requests:
            requests = sequence["requests"]
        requests = _drop_resolved_deferred_observation_requests(requests)
        return self._direct_daily_desktop_sequence_result(
            requests,
            timeline,
            tool_timeline_start=sequence["start_index"] + 1,
            run_id=run_id,
        )

    @staticmethod
    def _latest_uncompleted_daily_desktop_sequence(
        timeline: list[dict[str, Any]],
    ) -> dict[str, Any] | None:
        requests: list[dict[str, Any]] = []
        start_index = -1
        for index, event in enumerate(timeline):
            event_type = str(event.get("event") or "").strip()
            if event_type == "agent.desktop.intent_completed":
                requests = []
                start_index = -1
                continue
            if event_type != "agent.desktop.intent_planned":
                continue
            source = str(event.get("source") or "").strip()
            if source not in _DAILY_DESKTOP_PLAN_SOURCES:
                continue
            tool_name = str(event.get("tool") or event.get("detail") or "").strip()
            if not tool_name:
                continue
            input_preview = event.get("input_preview")
            request = {
                "protocol": "json_fallback",
                "tool": tool_name,
                "input": dict(input_preview) if isinstance(input_preview, dict) else {},
                "source": source,
            }
            planning_reason = str(event.get("planning_reason") or "").strip()
            if planning_reason:
                request["planning_reason"] = planning_reason
            presentation = str(event.get("presentation") or "").strip()
            if presentation:
                request["presentation"] = presentation
            if bool(event.get("continue_to_model")):
                request["continue_to_model"] = True
            for key in ("step_id", "capability_id", "replan_request_id", "replan_trigger"):
                value = str(event.get(key) or "").strip()
                if value:
                    request[key] = value
            request.update(_request_observability_metadata(event))
            if start_index < 0:
                start_index = index
            requests.append(request)
        if start_index < 0 or len(requests) < 2:
            return None
        return {"start_index": start_index, "requests": requests}

    @staticmethod
    def _latest_tool_call_event_for_daily_desktop_intent(
        timeline: list[dict[str, Any]],
    ) -> dict[str, Any] | None:
        for event in reversed(timeline):
            if event.get("event") == "agent.desktop.intent_completed":
                return None
            if event.get("event") != "agent.tool.call":
                continue
            tool_name = str(event.get("detail") or event.get("tool") or "").strip()
            if tool_name in _DIRECT_DAILY_DESKTOP_TOOLS:
                return event
        return None

    @staticmethod
    def _latest_daily_desktop_input(
        timeline: list[dict[str, Any]],
        tool_name: str,
    ) -> dict[str, Any] | None:
        for event in reversed(timeline):
            event_type = str(event.get("event") or "").strip()
            if event_type not in {
                "agent.desktop.intent_planned",
                "agent.desktop.intent_approval_required",
            }:
                continue
            event_tool = str(event.get("tool") or event.get("detail") or "").strip()
            if event_tool != tool_name:
                continue
            if str(event.get("source") or "").strip() not in _DAILY_DESKTOP_PLAN_SOURCES:
                continue
            input_preview = event.get("input_preview")
            return dict(input_preview) if isinstance(input_preview, dict) else {}
        return None

    def _latest_user_intent_text(self, messages: list[dict[str, Any]]) -> str:
        for message in reversed(messages):
            if str(message.get("role") or "") != "user":
                continue
            content = self._message_visible_content_text(message).strip()
            if (
                content.startswith("Tool result for ")
                or content.startswith("Desktop intent for ")
                or content.startswith("Runtime follow-up context:")
            ):
                continue
            if content:
                return content
        return ""

    def _original_user_intent_text(self, messages: list[dict[str, Any]]) -> str:
        """Return the first authentic run goal, never a Runtime pseudo-user turn."""

        runtime_prefixes = (
            "Tool result for ",
            "Desktop intent for ",
            "Runtime follow-up context:",
            "Runtime replan context:",
            "Runtime goal verification:",
            "Runtime root-goal contract ",
        )
        for message in messages:
            if str(message.get("role") or "").strip() != "user":
                continue
            if message.get("runtime_generated") is True or str(
                message.get("visibility") or ""
            ).strip() == "internal":
                continue
            content = self._message_visible_content_text(message).strip()
            if not content or content.startswith(runtime_prefixes):
                continue
            return content
        return ""

    def _ensure_runtime_system_message(
        self,
        messages: list[dict[str, Any]],
        allowed_tools: list[str],
        *,
        planner_decision: Any | None = None,
        plan_guidance_if_missing: bool = True,
    ) -> None:
        if messages and str(messages[0].get("role") or "") == "system":
            content = str(messages[0].get("content") or "")
            if "Oha-Yachiyo Agent Runtime" in content:
                return
        runtime_message = self._system_message(
            allowed_tools,
            planner_context=self._latest_user_intent_text(messages),
            planner_decision=planner_decision,
            plan_guidance_if_missing=plan_guidance_if_missing,
        )
        if messages and str(messages[0].get("role") or "") == "system":
            content = str(messages[0].get("content") or "")
            messages[0] = {
                **messages[0],
                "content": f"{runtime_message['content']}\n\n{content}",
            }
            return
        messages.insert(0, runtime_message)

    def _initial_messages(
        self,
        context: str,
        allowed_tools: list[str],
        *,
        planner_decision: Any | None = None,
        plan_guidance_if_missing: bool = True,
    ) -> list[dict[str, Any]]:
        return [
            self._system_message(
                allowed_tools,
                planner_context=context,
                planner_decision=planner_decision,
                plan_guidance_if_missing=plan_guidance_if_missing,
            ),
            {"role": "user", "content": context},
        ]

    def _system_message(
        self,
        allowed_tools: list[str],
        *,
        planner_context: str = "",
        planner_decision: Any | None = None,
        plan_guidance_if_missing: bool = True,
    ) -> dict[str, Any]:
        allowed_tool_text = ", ".join(allowed_tools) or "none"
        memory_tool_guidance = (
            "Use memory.add, memory.replace, and memory.remove only for stable user preferences, durable facts, "
            "task commitments, reusable summaries, or explicit forget/correction requests; never store secrets. "
            if any(tool in allowed_tools for tool in self._memory_tool_names)
            else ""
        )
        future_task_guidance = (
            "Use future_task.schedule/list/cancel for explicit reminders, follow-up commitments, standing orders, "
            "or recurring summaries; do not schedule hidden future work without user intent. "
            if any(tool in allowed_tools for tool in self._future_task_tool_names)
            else ""
        )
        desktop_tool_guidance = (
            "For desktop requests, prefer structured desktop tools such as screen.capture, "
            "desktop.permissions, desktop.active_window, desktop.list_apps, desktop.running_apps, desktop.windows, desktop.ui_elements, app.status, app.open/app.focus/app.focus_window/app.open_and_safe_type_text/app.focus_and_safe_type_text/app.open_and_safe_shortcut/app.focus_and_safe_shortcut/app.open_and_safe_key/app.focus_and_safe_key/app.open_and_hotkey/app.focus_and_hotkey/app.open_and_safe_scroll/app.focus_and_safe_scroll/app.open_and_safe_click/app.focus_and_safe_click/app.open_and_click_ui_element/app.focus_and_click_ui_element/app.open_and_type_into_ui_element/app.focus_and_type_into_ui_element/app.show/app.hide/app.minimize/app.quit, desktop.reveal_path, desktop.open_path, media.apple_music_play, "
            "media.apple_music_open_and_play, media.apple_music_control, media.music_app_open_and_play, media.music_app_control, media.system_control, system.settings_open, system.volume, system.brightness, system.display_sleep, system.screen_saver_start, clipboard.write, clipboard.read, notes.create, reminders.create, calendar.create_event, desktop.safe_shortcut, desktop.safe_key, desktop.safe_type_text, desktop.search_submit, desktop.safe_click, desktop.safe_scroll, desktop.click_ui_element, desktop.type_into_ui_element, desktop.hide_app, desktop.show_all_apps, desktop.minimize_window, desktop.close_window, desktop.quit_app, desktop.click, desktop.hotkey, desktop.submit_foreground, and desktop.type_text "
            "when they are allowed. When the user names an app or discovery resolves an app, prefer app-scoped "
            "foreground tools such as app.open_and_click_ui_element, app.focus_and_click_ui_element, "
            "app.open_and_type_into_ui_element, app.focus_and_type_into_ui_element, app.*_and_safe_shortcut, "
            "app.*_and_safe_key, app.*_and_safe_scroll, and app.*_and_safe_click so Runtime can bind the action "
            "to the target app. Use bare desktop.click_ui_element, desktop.type_into_ui_element, desktop.safe_* "
            "foreground tools only for explicit current-foreground requests or when the app-scoped tool is not "
            "available after discovery/focus. For explicit media requests, treat the media app as a discoverable "
            "desktop resource instead of an Apple Music-only branch: resolve named or uncertain media apps "
            "through desktop.list_apps when available, open or focus the app, search/type the requested query "
            "with app/desktop tools, submit, and verify with UI/window observations. Do not default song search "
            "or playback queries to media.apple_music_play when generic app discovery/search tools are allowed; "
            "use media.apple_music_play, media.apple_music_open_and_play, media.apple_music_control, "
            "media.music_app_open_and_play, media.music_app_control, or media.system_control only as allowed "
            "runtime media execution/fallback tools. Map named app pause/resume/next/previous commands to "
            "media.music_app_control or media.apple_music_control when direct control is requested; map generic "
            "current media pause/resume/next/previous commands to media.system_control; map macOS System Settings pane or permission page open requests to system.settings_open; map volume status/set/up/down/mute/unmute "
            "commands to system.volume; map explicit relative brightness up/down commands to system.brightness; map explicit display sleep or turn-off-screen commands to system.display_sleep; map explicit start-screen-saver commands to system.screen_saver_start; map explicit 'copy/write to clipboard' requests to "
            "clipboard.write without reading clipboard contents; map explicit clipboard content read/status questions to clipboard.read; map explicit selected text read requests to desktop.safe_shortcut(copy) followed by clipboard.read; map explicit create/new note requests with user-provided body text to notes.create; map explicit reminder creation requests with a clear title to reminders.create, adding due_at only when the local date/time is deterministic; map explicit calendar event creation requests with a clear title and deterministic local start time to calendar.create_event; map screen capture requests to "
            "screen.capture, and current or foreground window questions to desktop.active_window "
            "before answering; map installed/available app discovery or uncertain app names to desktop.list_apps before app.open; map running/open app list questions to desktop.running_apps; "
            "map open window list questions to desktop.windows; "
            "map explicit current-page reading/extraction requests to browser.extract_text; "
            "map foreground UI control/button/input field list questions to desktop.ui_elements; "
            "map explicit foreground UI control/button clicks by visible label to desktop.click_ui_element; "
            "map explicit foreground text input by visible input field label to desktop.type_into_ui_element; "
            "map single app running/open status questions to app.status; "
            "map explicit app window focus requests with a title substring to app.focus_window; "
            "map Chrome/Edge/Brave downloads, bookmarks, and extensions page requests to app open/focus address bar, desktop.safe_type_text with the whitelisted internal URL, then desktop.search_submit; "
            "map common whitelisted foreground shortcuts such as copy/paste/copy current page link/select all/undo/redo/find/focus address bar/new tab/new private window/close tab/next tab/previous tab/next window/previous window/app switching/hide other apps/toggle current-window full screen/Mission Control/Application Windows/Spotlight/Emoji picker/screenshot selection/screenshot or recording toolbar/Lock Screen/Force Quit dialog/new window/new document/new note/new reminder/new event/refresh/bookmark page/history/DevTools/page zoom/browser back/browser forward/reopen closed tab to desktop.safe_shortcut; "
            "map Finder or app open/focus followed by new-folder, new-document, new-message, new-reminder, new-event, "
            "rename, info, parent-folder, command-palette, preferences, or similar whitelisted app shortcuts to "
            "app.open_and_safe_shortcut or app.focus_and_safe_shortcut when app-scoped tools are allowed; use "
            "app.open/app.focus followed by desktop.safe_shortcut only as a compatibility fallback. "
            "map app open/focus followed by explicit safe navigation keys, scroll, single-click coordinates, "
            "visible-label clicks, or visible-field typing to app.*_and_safe_key, app.*_and_safe_scroll, "
            "app.*_and_safe_click, app.*_and_click_ui_element, or app.*_and_type_into_ui_element when available; "
            "fall back to app.open/app.focus followed by desktop.safe_key, desktop.safe_scroll, desktop.safe_click, "
            "desktop.click_ui_element, or desktop.type_into_ui_element only when app-scoped tools are unavailable. "
            "map explicit foreground navigation keys such as Escape, Tab, Shift+Tab, arrow keys, Home, End, Page Up, and Page Down to desktop.safe_key; "
            "map explicit user-provided foreground typing requests to desktop.safe_type_text; "
            "map explicit foreground search/find query submission to desktop.search_submit; "
            "map explicit user-provided single-click coordinates to desktop.safe_click; "
            "map explicit current foreground scroll/page up/page down requests to desktop.safe_scroll; "
            "when the user explicitly chains multiple low-risk desktop actions, execute them in order and let Runtime pause for approval if a later action requires it; "
            "map explicit named app show/unhide/restore requests to app.show, named app hide requests to app.hide, named app minimize requests to app.minimize, and app quit/close/exit requests to app.quit; "
            "map desktop permission diagnostics and 'why can't you control/open/click/play' "
            "questions to desktop.permissions; "
            "map 'show/reveal in Finder' requests to desktop.reveal_path and safe local "
            "file or folder open requests to desktop.open_path. "
            "Map explicit current/foreground app hide requests to desktop.hide_app. "
            "Map explicit show all hidden apps requests to desktop.show_all_apps. "
            "Map explicit current/foreground window minimize requests to desktop.minimize_window. "
            "Map explicit current/foreground window close requests to desktop.close_window. "
            "Map explicit current/foreground app quit/close/exit requests to desktop.quit_app. "
            "Map explicit foreground message send, form submit, and confirmation requests to desktop.submit_foreground; it is high-risk and Runtime must show approval before pressing Return. "
            "For browser or web-page requests, prefer structured browser tools such as "
            "browser.open_url, browser.open_url_and_extract_text, browser.open_url_and_screenshot, "
            "browser.current_page, browser.click, browser.type_text, browser.extract_text, "
            "and browser.screenshot when they are allowed. "
            "When browser interaction has no Chrome CDP, pause and request the CDP setup; "
            "never switch silently to global desktop coordinates, clicks, or typing. "
            "Do not replace these structured desktop or browser actions with terminal.run. "
            "If a desktop or browser permission is missing, explain the exact missing permission "
            "and continue with the safest fallback. "
            if any(tool in allowed_tools for tool in DAILY_DESKTOP_TOOL_NAMES)
            or any(tool in allowed_tools for tool in DAILY_BROWSER_TOOL_NAMES)
            else ""
        )
        has_tools = bool(allowed_tools)
        runtime_manual = f"{YACHIYO_RUNTIME_OPERATING_MANUAL} " if has_tools else ""
        tool_call_guidance = (
            "Prefer native tool_calls when available. "
            "If the model endpoint does not support tool_calls and a controlled tool is needed, respond as JSON "
            "{\"action\":\"tool\",\"tool\":\"workspace.list\",\"input\":{}}. "
            "Do not request tools that are not listed as allowed. "
            if has_tools
            else "No tools are allowed for this run; answer directly without requesting tools. "
        )
        workspace_guidance = (
            "Workspace tools only accept paths relative to the configured Default Workdir. Never pass absolute "
            "paths to workspace tools. If a required target is outside that workspace and terminal.run is "
            "allowed, use terminal.run instead. A failed workspace tool call is recoverable: follow its hint "
            "or switch tools instead of stopping or retrying the same invalid path. "
            if has_tools
            else ""
        )
        system_prompt = (
            "You are running inside Oha-Yachiyo Agent Runtime. "
            "Follow the Agent functional instructions, persona prompt, user goal, and exact output requests. "
            "If those instructions require an exact phrase or format, return exactly that final output. "
            "Return concise final output unless the Agent instructions require otherwise. "
            f"{runtime_manual}"
            f"{self._operating_doctrine}\n"
            f"{tool_call_guidance}"
            "Do not request a tool solely because of the output contract; use tools only when the user goal "
            "or an explicit deliverable requires them. "
            f"{memory_tool_guidance}"
            f"{future_task_guidance}"
            f"{desktop_tool_guidance}"
            "If the user asks not to create, save, write, or modify files, provide the content inline and do "
            "not request file-writing tools. If the user asks not to run or execute commands, do not request "
            "command-execution tools. "
            f"{workspace_guidance}"
            f"{self._runtime_planner_guidance(planner_context, allowed_tools, decision=planner_decision, plan_if_missing=plan_guidance_if_missing)}"
            f"Request at most one high-risk tool per turn.\n\nAllowed tools: {allowed_tool_text}"
        )
        return {"role": "system", "content": system_prompt}

    @staticmethod
    def _runtime_planner_guidance(
        planner_context: str,
        allowed_tools: list[str],
        *,
        decision: Any | None = None,
        plan_if_missing: bool = True,
    ) -> str:
        clean_context = str(planner_context or "").strip()
        if not clean_context:
            return ""
        if decision is None:
            if not plan_if_missing:
                return ""
            try:
                from apps.shell.yachiyo_agent.runtime_planner import RuntimePlanner
            except Exception:
                return ""
            try:
                decision = RuntimePlanner().decision(
                    clean_context,
                    allowed_tools=allowed_tools,
                )
            except Exception:
                return ""
        if str(getattr(decision, "prompt", "") or "").strip() != clean_context:
            return ""
        if decision.selected_intent.kind == "general":
            return ""
        steps = list(decision.plan.tool_plan.steps)
        tool_path = [
            str(step.tool_name or step.capability_id or "").strip()
            for step in steps
            if str(step.tool_name or step.capability_id or "").strip()
        ]
        if not tool_path:
            return ""
        intent_kind = str(decision.selected_intent.kind or "").strip()
        missing = ", ".join(decision.plan.tool_plan.missing_capabilities) or "none"
        outputs = ", ".join(decision.selected_intent.expected_outputs) or "unspecified"
        artifacts = ", ".join(decision.plan.tool_plan.artifacts_expected) or "none"
        route_to_studio = "yes" if decision.plan.route_to_studio else "no"
        studio_handoff_guidance = ""
        if intent_kind in {"workflow_orchestration", "multi_agent"}:
            studio_surface = (
                "Workflow"
                if intent_kind == "workflow_orchestration"
                else "GroupRun"
            )
            studio_handoff_guidance = (
                f"Studio orchestration handoff: this is an Agent Studio {studio_surface} plan, "
                "not a normal model-only recipe. Preserve the planner intent, target, approvals, "
                "artifacts, and timeline context; do not claim the workflow or group run completed "
                "unless Runtime or Agent Studio returns a concrete run snapshot/run id. "
            )
        step_guidance = []
        for index, step in enumerate(steps, start=1):
            tool_or_capability = str(step.tool_name or step.capability_id or "").strip()
            if not tool_or_capability:
                continue
            approval = "approval required" if step.approval_required else "approval not preflagged"
            action = str(getattr(step, "action", "") or "").strip()
            action_text = f"action={action}; " if action else ""
            reason = str(step.reason or "").strip()
            reason_text = f"; reason={reason}" if reason else ""
            step_guidance.append(
                f"{index}. {step.title}: {tool_or_capability} "
                f"({action_text}capability={step.capability_id}; status={step.status}; "
                f"risk={step.risk_level}; {approval}{reason_text})"
            )
        steps_text = " ".join(step_guidance)
        task_workspace_guidance = _runtime_planner_task_workspace_guidance(decision)
        return (
            "Runtime planner guidance: "
            f"selected intent={decision.selected_intent.kind}; "
            f"expected outputs={outputs}; "
            f"planned tool path={' -> '.join(tool_path)}; "
            f"missing required capabilities={missing}; "
            f"artifact expected={artifacts}; "
            f"route to Studio={route_to_studio}. "
            f"Plan steps: {steps_text}. "
            f"{task_workspace_guidance}"
            f"{studio_handoff_guidance}"
            "Follow the planned tool path in order when the named tools are allowed; do not replace app-scoped "
            "app.*_and_* foreground tools with a looser app.open/app.focus plus desktop.* sequence unless the "
            "app-scoped tool is unavailable. "
            "Use available tools to execute the request when the plan names an allowed tool; do not provide "
            "only manual instructions unless required capabilities are missing, user constraints forbid tools, "
            "or policy blocks execution. If a required step is unavailable, explain the missing capability "
            "instead of fabricating execution. Existing tool policy and approval gates still apply. "
        )


def _runtime_semantic_artifact_assessment_payload(
    candidate: Mapping[str, Any],
    proposal: SemanticArtifactVerificationResult,
) -> dict[str, Any]:
    """Bind one untrusted verdict to Runtime-owned exact-readback lineage."""

    verdict = str(getattr(proposal.verdict, "value", "uncertain") or "uncertain")
    if verdict not in {"fulfilled", "insufficient", "uncertain"}:
        verdict = "uncertain"
    reason = redact_sensitive_text(proposal.reason, limit=500) or "semantic_verdict_unavailable"
    missing_requirements = [
        clean
        for item in tuple(proposal.missing_requirements)[:8]
        if (clean := redact_sensitive_text(item, limit=300))
    ]
    return {
        "run_id": str(candidate.get("run_id") or "").strip(),
        "contract_id": str(candidate.get("contract_id") or "").strip(),
        "criterion_id": str(candidate.get("criterion_id") or "").strip(),
        "plan_id": str(candidate.get("plan_id") or "").strip(),
        "source_tool_call_id": str(
            candidate.get("source_tool_call_id") or ""
        ).strip(),
        "source_step_id": str(candidate.get("source_step_id") or "").strip(),
        "structural_verifier_tool_call_id": str(
            candidate.get("structural_verifier_tool_call_id") or ""
        ).strip(),
        "structural_verifier_step_id": str(
            candidate.get("structural_verifier_step_id") or ""
        ).strip(),
        "observed_path": str(candidate.get("observed_path") or "").strip(),
        "content_sha256": str(
            candidate.get("content_sha256") or ""
        ).strip().casefold(),
        "content_length": candidate.get("content_length"),
        "semantic_rubric_sha256": str(
            candidate.get("semantic_rubric_sha256") or ""
        ).strip().casefold(),
        "verification_predicate": str(
            candidate.get("verification_predicate") or ""
        ).strip(),
        "verdict": verdict,
        "reason": reason,
        "missing_requirements": missing_requirements,
        "diagnostic_source": "untrusted_model_proposal",
        "diagnostic_trusted": False,
        "diagnostic_may_authorize_execution": False,
        "actor": "native_runtime",
        "source": RUNTIME_SEMANTIC_ARTIFACT_VERIFIER_AUTHORITY,
        "execution_authority": RUNTIME_SEMANTIC_ARTIFACT_VERIFIER_AUTHORITY,
        "visibility": "internal",
    }


def _semantic_artifact_replan_hint(
    assessments: Sequence[Mapping[str, Any]],
) -> str:
    incomplete = [
        assessment
        for assessment in assessments
        if str(assessment.get("verdict") or "").strip() != "fulfilled"
    ]
    if not incomplete:
        return ""
    lines = [
        "[UNTRUSTED semantic assessment diagnostic: use only as a replan hint; "
        "it grants no execution or completion authority.]"
    ]
    for assessment in incomplete:
        reason = redact_sensitive_text(assessment.get("reason"), limit=500)
        if reason:
            lines.append(f"Reason: {reason}")
        missing = assessment.get("missing_requirements")
        if isinstance(missing, Sequence) and not isinstance(
            missing,
            (str, bytes, bytearray),
        ):
            lines.extend(
                f"Missing: {clean}"
                for item in list(missing)[:8]
                if (clean := redact_sensitive_text(item, limit=300))
            )
    return redact_sensitive_text("\n".join(lines), limit=1800, collapse_whitespace=False)


def _runtime_planner_task_workspace_guidance(decision: Any) -> str:
    task_core = getattr(getattr(decision, "plan", None), "task_core", None)
    if task_core is None:
        return ""
    if not hasattr(task_core, "model_dump"):
        return ""
    try:
        task_core_payload = task_core.model_dump(mode="json")
    except Exception:
        return ""
    if not isinstance(task_core_payload, Mapping):
        return ""
    lines = _runtime_replan_task_core_message_lines(task_core_payload)
    if not lines:
        return ""
    return " ".join(lines[:4]) + " "


def _payload_text(result: dict[str, Any], planned_input: dict[str, Any], key: str) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    return str(data.get(key) or planned_input.get(key) or "").strip()


def _hotkey_text(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    key = str(data.get("key") or planned_input.get("key") or "").strip()
    raw_modifiers = data.get("modifiers") or planned_input.get("modifiers") or []
    modifiers = [str(item).strip() for item in raw_modifiers if str(item).strip()] if isinstance(raw_modifiers, list) else []
    parts = [*_hotkey_modifier_labels(modifiers), key.upper() if len(key) == 1 else key]
    return "+".join(part for part in parts if part)


def _hotkey_modifier_labels(modifiers: list[str]) -> list[str]:
    labels = {
        "command": "Command",
        "cmd": "Command",
        "shift": "Shift",
        "option": "Option",
        "alt": "Option",
        "control": "Control",
        "ctrl": "Control",
    }
    return [labels.get(modifier.lower(), modifier) for modifier in modifiers]


def _click_text(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    x = data.get("x", planned_input.get("x"))
    y = data.get("y", planned_input.get("y"))
    click_count = data.get("click_count", planned_input.get("click_count"))
    if x in (None, "") or y in (None, ""):
        return ""
    count_text = "双击 " if str(click_count or "") == "2" else ""
    return f"{count_text}{x}, {y}"


def _click_ui_element_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    label = str(data.get("matched_label") or data.get("target") or planned_input.get("target") or "").strip()
    point = _click_text(result, planned_input)
    if label and point:
        return f"已点击前台控件：{label}（{point}）。"
    if label:
        return f"已点击前台控件：{label}。"
    if point:
        return f"已点击前台控件位置：{point}。"
    return ""


def _type_into_ui_element_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    label = str(data.get("matched_label") or data.get("target") or planned_input.get("target") or "").strip()
    count = data.get("character_count")
    if not isinstance(count, int):
        text = str(planned_input.get("text") or "").strip()
        count = len(text) if text else 0
    count_text = f"（{count} 个字符）" if count else ""
    if label:
        return f"已在前台控件 {label} 输入文字{count_text}。"
    if count_text:
        return f"已在前台控件输入文字{count_text}。"
    return ""


def _app_foreground_action_failed_summary(
    tool_name: str,
    planned_input: dict[str, Any],
    result: dict[str, Any],
) -> str:
    policy = (
        result.get("desktop_execution_policy")
        if isinstance(result.get("desktop_execution_policy"), Mapping)
        else {}
    )
    if (
        result.get("blocked_by_desktop_execution_policy")
        and policy.get("prefer_background_desktop") is True
    ):
        app_name = _payload_text(result, planned_input, "app_name")
        target = _display_target_name(app_name)
        return _append_recovery_action_summary(
            (
                f"后台桌面控制尚未就绪，因此没有打开或操作{target}，"
                "也没有接管你正在使用的鼠标和键盘。"
                "请先安装或授权后台控制组件后重试。"
            ),
            result,
        )
    fallback = result.get("fallback_result") if isinstance(result.get("fallback_result"), dict) else {}
    setup = fallback.get("focus") if isinstance(fallback.get("focus"), dict) else {}
    if not setup.get("ok"):
        return ""
    app_name = _payload_text(result, planned_input, "app_name")
    action = "打开" if tool_name.startswith("app.open") else "切到"
    target = _display_target_name(app_name)
    failed_action = _app_foreground_action_phrase(tool_name, result, planned_input)
    targets = ", ".join(str(item) for item in result.get("permission_targets") or [] if str(item))
    diagnostics = _permission_diagnostics(result)
    suffix = f" 缺少权限：{targets}。" if targets else ""
    return _append_recovery_action_summary(
        f"已{action}{target}，但没能{failed_action}。{suffix}{diagnostics}".strip(),
        result,
    )


def _app_foreground_action_phrase(
    tool_name: str,
    result: dict[str, Any],
    planned_input: dict[str, Any],
) -> str:
    if tool_name.endswith("safe_type_text"):
        return "输入文字"
    if tool_name.endswith("safe_shortcut"):
        return _action_phrase_from_done_summary(
            _safe_shortcut_summary(result, planned_input),
            "执行快捷动作",
        )
    if tool_name.endswith("safe_key"):
        return _action_phrase_from_done_summary(
            _safe_key_summary(result, planned_input),
            "按前台导航键",
        )
    if tool_name.endswith("safe_scroll"):
        return _action_phrase_from_done_summary(
            _safe_scroll_summary(result, planned_input),
            "滚动前台界面",
        )
    if tool_name.endswith("safe_click"):
        point = _click_text(result, planned_input)
        return f"点击前台位置：{point}" if point else "点击前台位置"
    if tool_name.endswith("click_ui_element"):
        return _action_phrase_from_done_summary(
            _click_ui_element_summary(result, planned_input),
            "点击前台控件",
        )
    if tool_name.endswith("type_into_ui_element"):
        return _action_phrase_from_done_summary(
            _type_into_ui_element_summary(result, planned_input),
            "填写前台控件",
        )
    if tool_name.endswith("hotkey"):
        hotkey = _hotkey_text(result, planned_input)
        return f"发送快捷键：{hotkey}" if hotkey else "发送快捷键"
    return "执行前台动作"


def _action_phrase_from_done_summary(summary: str, fallback: str) -> str:
    text = str(summary or "").strip()
    if not text:
        return fallback
    if text.startswith("已"):
        text = text[1:]
    return text.removesuffix("。") or fallback


def _active_window_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    app_name = str(data.get("app_name") or "").strip()
    title = str(data.get("title") or "").strip()
    if app_name and title:
        return f"当前前台窗口是 {app_name}：{title}。"
    if app_name:
        return f"当前前台应用是 {app_name}。"
    return "已读取当前前台窗口。"


def _desktop_permissions_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_targets = result.get("permission_targets") or data.get("permission_targets") or []
    targets = _text_list(raw_targets)
    if not targets:
        return "桌面执行权限已就绪。"
    raw_tools = result.get("affected_tools") or data.get("affected_tools") or []
    affected_tools = _text_list(raw_tools)
    target_text = ", ".join(targets[:6])
    target_suffix = " 等" if len(targets) > 6 else ""
    if not affected_tools:
        return _append_recovery_action_summary(
            f"桌面执行权限还缺少：{target_text}{target_suffix}。",
            result,
        )
    legacy_apple_music_tools = [
        "media.apple_music_play",
        "media.apple_music_open_and_play",
        "media.apple_music_control",
    ]
    tool_text = ", ".join(affected_tools[:6])
    tool_suffix = " 等" if len(affected_tools) > 6 else ""
    if all(tool in affected_tools for tool in legacy_apple_music_tools) and affected_tools != legacy_apple_music_tools:
        legacy_tool_text = ", ".join(legacy_apple_music_tools)
        return _append_recovery_action_summary(
            (
                f"桌面执行权限还缺少：{target_text}{target_suffix}。"
                f"受影响工具：{legacy_tool_text}。完整受影响工具：{tool_text}{tool_suffix}。"
            ),
            result,
        )
    return _append_recovery_action_summary(
        f"桌面执行权限还缺少：{target_text}{target_suffix}。受影响工具：{tool_text}{tool_suffix}。",
        result,
    )


def _desktop_permission_recovery_event_payload(
    tool_name: str,
    planned_input: dict[str, Any],
    result: dict[str, Any],
) -> dict[str, Any]:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    permission_targets = _ordered_text_list(
        [
            *_string_list(result.get("permission_targets")),
            *_string_list(result.get("missing_permissions")),
            *_string_list(data.get("permission_targets")),
            *_string_list(data.get("missing_permissions")),
        ]
    )
    recovery_hints = _ordered_text_list(
        [
            *_string_list(result.get("recovery_hints")),
            *_string_list(data.get("recovery_hints")),
            *_permission_target_hints(permission_targets),
        ]
    )
    recovery_actions = _recovery_actions(result)
    affected_tools = _ordered_text_list(
        [
            *_string_list(result.get("affected_tools")),
            *_string_list(data.get("affected_tools")),
            tool_name,
        ]
    )
    legacy_apple_music_tools = [
        "media.apple_music_play",
        "media.apple_music_open_and_play",
        "media.apple_music_control",
    ]
    if (
        {"music_app", "automation"}.issubset(set(permission_targets))
        and set(legacy_apple_music_tools).issubset(set(affected_tools))
    ):
        affected_tools = _ordered_text_list([*legacy_apple_music_tools, tool_name])
    has_recovery_signal = (
        bool(permission_targets)
        or bool(recovery_hints)
        or bool(recovery_actions)
        or bool(result.get("permission_error"))
    )
    if not has_recovery_signal:
        return {}
    return {
        "tool": tool_name,
        "source": "daily_desktop_intent",
        "status": "permission_recovery_available",
        "input_preview": planned_input,
        "permission_targets": permission_targets,
        "affected_tools": affected_tools,
        "recovery_hints": recovery_hints,
        "recovery_actions": recovery_actions,
    }


def _desktop_permission_preflight_event_payload(
    planned_tool_requests: list[dict[str, Any]],
    broker: Any,
) -> dict[str, Any]:
    planned_tools = _ordered_text_list(
        [str(request.get("tool") or "") for request in planned_tool_requests]
    )
    planned_tools = [tool for tool in planned_tools if tool and tool != "desktop.permissions"]
    if not planned_tools:
        return {}
    desktop_permission_preflight = getattr(broker, "desktop_permission_preflight", None)
    if not callable(desktop_permission_preflight):
        return {}
    try:
        result = desktop_permission_preflight()
    except Exception:
        return {}
    if not isinstance(result, dict):
        return {}
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    permission_targets = _ordered_text_list(
        [
            *_string_list(result.get("permission_targets")),
            *_string_list(result.get("missing_permissions")),
            *_string_list(data.get("permission_targets")),
            *_string_list(data.get("missing_permissions")),
        ]
    )
    if not permission_targets and not result.get("permission_error"):
        return {}
    affected_tools = _ordered_text_list(
        [
            *_string_list(result.get("affected_tools")),
            *_string_list(data.get("affected_tools")),
        ]
    )
    impacted_tools = _permission_preflight_impacted_tools(
        planned_tools,
        affected_tools,
    )
    if affected_tools and not impacted_tools:
        return {}
    recovery_hints = _ordered_text_list(
        [
            *_string_list(result.get("recovery_hints")),
            *_string_list(data.get("recovery_hints")),
            *_permission_target_hints(permission_targets),
        ]
    )
    recovery_actions = _recovery_actions(result)
    if not permission_targets and not recovery_hints and not recovery_actions:
        return {}
    return {
        "tool": impacted_tools[0] if impacted_tools else planned_tools[0],
        "tools": planned_tools,
        "source": "daily_desktop_intent",
        "status": "permission_preflight_available",
        "permission_targets": permission_targets,
        "affected_tools": impacted_tools or affected_tools,
        "recovery_hints": recovery_hints,
        "recovery_actions": recovery_actions,
        "diagnostic_route": str(
            result.get("diagnostic_route")
            or data.get("diagnostic_route")
            or "/yachiyo/readiness"
        ),
    }


def _permission_preflight_impacted_tools(
    planned_tools: list[str],
    affected_tools: list[str],
) -> list[str]:
    if not affected_tools:
        return planned_tools
    planned = {tool for tool in planned_tools if tool}
    impacted: list[str] = []
    for tool in affected_tools:
        if tool in planned or _affected_tool_matches_split_desktop_plan(tool, planned):
            impacted.append(tool)
    return _ordered_text_list(impacted)


def _affected_tool_matches_split_desktop_plan(
    affected_tool: str,
    planned_tools: set[str],
) -> bool:
    open_tools = {"app.open", "desktop.open_app"}
    focus_tools = {"app.focus", "desktop.focus_app"}
    if affected_tool == "app.open_and_safe_type_text":
        return bool(planned_tools & open_tools) and "desktop.safe_type_text" in planned_tools
    if affected_tool == "app.focus_and_safe_type_text":
        return bool(planned_tools & focus_tools) and "desktop.safe_type_text" in planned_tools
    if affected_tool == "app.open_and_safe_shortcut":
        return bool(planned_tools & open_tools) and "desktop.safe_shortcut" in planned_tools
    if affected_tool == "app.focus_and_safe_shortcut":
        return bool(planned_tools & focus_tools) and "desktop.safe_shortcut" in planned_tools
    return False


def _append_recovery_action_summary(text: str, result: dict[str, Any]) -> str:
    labels = _recovery_action_labels(result)
    if not labels:
        return text
    return f"{text}可直接打开：{'、'.join(labels[:4])}。"


def _with_retry_recovery_action(
    tool_name: str,
    planned_input: dict[str, Any],
    result: dict[str, Any],
) -> dict[str, Any]:
    if not _has_permission_recovery_signal(result):
        return result
    if not isinstance(planned_input, dict):
        return result
    retry_request = daily_desktop_metadata_tool_request(
        {
            "desktop_permission_recovery": True,
            "desktop_permission_retry": True,
            "recovery_action_kind": "retry_original",
            "recovery_tool": tool_name,
            "recovery_input": planned_input,
            "recovery_retry_tool": tool_name,
            "recovery_retry_input": planned_input,
        },
        [tool_name],
    )
    if retry_request is None:
        return result
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_actions = result.get("recovery_actions") or data.get("recovery_actions") or []
    actions = [dict(action) for action in raw_actions if isinstance(action, dict)]
    if not actions:
        return result
    retry_prompt = _daily_desktop_retry_prompt(tool_name, planned_input)
    enriched_actions: list[dict[str, Any]] = []
    changed = False
    for action in actions:
        if str(action.get("tool") or "").strip() not in {"app.open", "system.settings_open"}:
            enriched_actions.append(action)
            continue
        enriched_action = dict(action)
        if not str(enriched_action.get("retry_tool") or "").strip():
            enriched_action["retry_tool"] = tool_name
            changed = True
        if not str(enriched_action.get("recovery_retry_tool") or "").strip():
            enriched_action["recovery_retry_tool"] = tool_name
            changed = True
        if not isinstance(enriched_action.get("retry_input"), dict):
            enriched_action["retry_input"] = dict(planned_input)
            changed = True
        if not isinstance(enriched_action.get("recovery_retry_input"), dict):
            enriched_action["recovery_retry_input"] = dict(planned_input)
            changed = True
        if retry_prompt and not str(enriched_action.get("retry_prompt") or "").strip():
            enriched_action["retry_prompt"] = retry_prompt
            changed = True
        if retry_prompt and not str(enriched_action.get("recovery_retry_prompt") or "").strip():
            enriched_action["recovery_retry_prompt"] = retry_prompt
            changed = True
        enriched_actions.append(enriched_action)
    if not changed:
        return result
    enriched = dict(result)
    enriched["recovery_actions"] = enriched_actions
    return enriched


def _has_permission_recovery_signal(result: dict[str, Any]) -> bool:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    return bool(
        result.get("permission_error")
        or result.get("user_handoff_required")
        or data.get("user_handoff_required")
        or _string_list(result.get("permission_targets"))
        or _string_list(result.get("missing_permissions"))
        or _string_list(data.get("permission_targets"))
        or _string_list(data.get("missing_permissions"))
        or _recovery_actions(result)
    )


def _timeline_has_permission_recovery_signal(
    timeline: list[dict[str, Any]],
    start_index: int,
) -> bool:
    for event in timeline[start_index:]:
        if event.get("event") not in {"agent.tool.call", "agent.tool.skipped"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), dict) else {}
        if result.get("blocked_by_app_resolution") or str(
            result.get("error") or ""
        ).strip() == "app_resolution_failed":
            continue
        if result and _has_permission_recovery_signal(result):
            return True
    return False


def _permission_recovery_executed_requests(
    requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    """Project only recovery-bearing requests that actually reached a tool call."""

    request_list = [dict(request) for request in requests if isinstance(request, Mapping)]
    selected: list[dict[str, Any]] = []
    selected_indexes: set[int] = set()
    for event in timeline[tool_timeline_start:]:
        if event.get("event") not in {"agent.tool.call", "agent.tool.skipped"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), dict) else {}
        if not _has_permission_recovery_signal(result):
            continue
        event_tool = str(event.get("detail") or "").strip()
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        for index, request in enumerate(request_list):
            if index in selected_indexes:
                continue
            request_tool = str(request.get("tool") or request.get("tool_name") or "").strip()
            request_step_id = str(
                request.get("step_id") or request.get("planner_step_id") or ""
            ).strip()
            step_matches = bool(
                event_step_id and request_step_id and event_step_id == request_step_id
            )
            tool_matches = bool(
                event_tool
                and request_tool
                and (
                    event_tool == request_tool
                    or _followup_plan_tools_match(event_tool, request_tool)
                )
            )
            if not step_matches and not tool_matches:
                continue
            selected_indexes.add(index)
            selected.append(request)
            break
    return selected




def _timeline_has_terminal_media_failure_without_permission_recovery(
    timeline: list[dict[str, Any]],
    start_index: int,
) -> bool:
    for event in timeline[start_index:]:
        if event.get("event") not in {"agent.tool.call", "agent.tool.skipped"}:
            continue
        tool_name = str(event.get("detail") or event.get("tool") or "").strip()
        if tool_name not in _DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS:
            continue
        result = event.get("result") if isinstance(event.get("result"), dict) else {}
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        if any(
            source.get("playback_state_unverified") is True
            for source in (result, data)
        ):
            # Provider dispatch success is still a terminal, honest partial
            # outcome when playback cannot be observed.  Route it through the
            # media completion evaluator now; falling into model/profile
            # lookup cannot manufacture the missing postcondition evidence.
            return True
        if (
            result.get("ok") is False
            and not _has_permission_recovery_signal(result)
        ):
            return True
    return False


def _direct_daily_runtime_verification_request(
    request: Mapping[str, Any],
) -> bool:
    runtime_stage = str(request.get("runtime_stage") or "").strip()
    runtime_role = str(request.get("runtime_role") or "").strip()
    if runtime_stage != "verify" and runtime_role != "verify_result":
        return False
    tool_name = str(request.get("tool") or "").strip()
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    if (
        tool_name in _DAILY_DESKTOP_VERIFY_TOOLS
        and not bool(request.get("approval_required"))
        and request.get("requires_post_action_verification") is not True
    ):
        return True
    return bool(
        tool_name == "system.volume"
        and str(payload.get("action") or "").strip() == "status"
        and not bool(request.get("approval_required"))
    )


def _direct_runtime_verification_only_followups(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    followups = [request for request in requests if bool(request.get("continue_to_model"))]
    return bool(followups) and all(
        _direct_daily_runtime_verification_request(request)
        for request in followups
    )


def _daily_desktop_retry_prompt(tool_name: str, planned_input: dict[str, Any]) -> str:
    if tool_name == "media.apple_music_play":
        query = str(planned_input.get("query") or "").strip()
        return f"用 Apple Music 播放{query}" if query else "用 Apple Music 播放音乐"
    if tool_name == "media.apple_music_open_and_play":
        return "打开Apple Music并播放"
    if tool_name == "media.music_app_open_and_play":
        app_name = str(planned_input.get("app_name") or "").strip()
        return f"打开{app_name}并播放" if app_name else "播放音乐"
    if tool_name == "system.settings_open":
        target = str(planned_input.get("target") or "").strip()
        return f"打开{target}" if target else "打开系统设置"
    if tool_name == "browser.extract_text":
        return "读取当前网页正文"
    if tool_name == "browser.screenshot":
        return "截取当前网页"
    if tool_name == "browser.current_page":
        return "查看当前网页"
    if tool_name == "screen.capture":
        return "截图当前屏幕"
    if tool_name == "desktop.active_window":
        return "查看当前窗口"
    if tool_name == "desktop.permissions":
        return "检查桌面权限"
    if tool_name == "desktop.safe_click":
        x = planned_input.get("x")
        y = planned_input.get("y")
        if x is not None and y is not None:
            return f"点击 {x}, {y}"
        return ""
    if tool_name == "app.open":
        app_name = str(planned_input.get("app_name") or "").strip()
        return f"打开{app_name}" if app_name else ""
    if tool_name == "app.focus":
        app_name = str(planned_input.get("app_name") or "").strip()
        return f"切到{app_name}" if app_name else ""
    if tool_name in _APP_FOREGROUND_ACTION_TOOLS:
        app_name = str(planned_input.get("app_name") or "").strip()
        if not app_name:
            return ""
        action = "打开" if tool_name.startswith("app.open") else "切到"
        phrase = _app_foreground_action_phrase(tool_name, {}, planned_input)
        return f"{action}{app_name}并{phrase}" if phrase else f"{action}{app_name}"
    return ""


def _preserve_direct_daily_desktop_tool_requests(
    requests: list[dict[str, Any]],
) -> bool:
    if not requests:
        return False
    tools = {
        str(request.get("tool") or "").strip()
        for request in requests
        if isinstance(request, dict)
    }
    if not tools:
        return False
    app_ui_approval_tools = {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "app.open_and_type_into_ui_element",
        "app.focus_and_type_into_ui_element",
    }
    if tools & app_ui_approval_tools:
        return True
    if any(
        isinstance(request, dict) and bool(request.get("approval_required"))
        for request in requests
    ):
        return True
    if tools & _DAILY_DESKTOP_DISCOVERY_PREFIX_TOOLS:
        return False
    app_shortcut_tools = {
        "app.open_and_safe_shortcut",
        "app.focus_and_safe_shortcut",
    }
    return "desktop.submit_foreground" in tools and bool(tools & app_shortcut_tools)


def _recovery_actions(result: dict[str, Any]) -> list[dict[str, Any]]:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_actions = result.get("recovery_actions") or data.get("recovery_actions") or []
    if not isinstance(raw_actions, list):
        return []
    actions: list[dict[str, Any]] = []
    seen: set[tuple[str, str, str]] = set()
    for raw_action in raw_actions:
        if not isinstance(raw_action, dict):
            continue
        tool = str(raw_action.get("tool") or "").strip()
        label = str(raw_action.get("label") or tool).strip()
        permission_target = str(raw_action.get("permission_target") or "").strip()
        action_input = raw_action.get("input") if isinstance(raw_action.get("input"), dict) else {}
        key = (tool, repr(sorted(action_input.items())), permission_target)
        if not tool or key in seen:
            continue
        seen.add(key)
        action = {
            "label": label or tool,
            "tool": tool,
            "input": dict(action_input),
            "permission_target": permission_target,
            "risk_level": str(raw_action.get("risk_level") or "low"),
        }
        for text_key in (
            "planning_reason",
            "action_kind",
            "recovery_action_kind",
            "runtime_stage",
            "runtime_role",
            "retry_prompt",
            "recovery_retry_prompt",
            "retry_source_event_type",
            "recovery_retry_source_event_type",
            "retry_source_tool_call_id",
            "recovery_retry_source_tool_call_id",
            "retry_input_source",
            "recovery_retry_input_source",
            "retry_artifact_tool",
            "recovery_retry_artifact_tool",
            "retry_artifact_kind",
            "recovery_retry_artifact_kind",
            "retry_tool",
            "recovery_retry_tool",
            "followup_tool",
            "recovery_followup_tool",
        ):
            value = str(raw_action.get(text_key) or "").strip()
            if value:
                action[text_key] = value
        for dict_key in (
            "action_target",
            "desktop_loop",
            "metadata",
            "observation_evidence",
            "observation_retry",
            "retry_input",
            "recovery_retry_input",
            "retry_input_schema",
            "recovery_retry_input_schema",
            "followup_input",
            "recovery_followup_input",
            "desktop_execution_policy",
            "desktop_execution_route",
            "sandbox_provider",
        ):
            value = raw_action.get(dict_key)
            if isinstance(value, dict):
                action[dict_key] = dict(value)
        for list_key in ("required_retry_fields", "recommended_tools"):
            value = raw_action.get(list_key)
            if isinstance(value, list):
                normalized = [str(item).strip() for item in value if str(item or "").strip()]
                if normalized:
                    action[list_key] = normalized
        for list_key in ("verification_targets",):
            value = raw_action.get(list_key)
            if isinstance(value, list):
                normalized_targets = [
                    dict(item) for item in value if isinstance(item, dict) and item
                ]
                if normalized_targets:
                    action[list_key] = normalized_targets
        if raw_action.get("desktop_permission_retry") is True:
            action["desktop_permission_retry"] = True
        if raw_action.get("approval_required") is True:
            action["approval_required"] = True
        actions.append(action)
    return actions


def _recovery_action_labels(result: dict[str, Any]) -> list[str]:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_actions = result.get("recovery_actions") or data.get("recovery_actions") or []
    if not isinstance(raw_actions, list):
        return []
    labels: list[str] = []
    for action in raw_actions:
        if isinstance(action, dict):
            label = str(action.get("label") or action.get("tool") or "").strip()
        else:
            label = str(action or "").strip()
        if label and label not in labels:
            labels.append(label)
    return labels


def _running_apps_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_apps = data.get("apps")
    if not isinstance(raw_apps, list):
        return ""
    names = []
    for item in raw_apps:
        if not isinstance(item, dict):
            continue
        name = str(item.get("name") or "").strip()
        if name:
            names.append(name)
    if not names:
        return "当前没有读取到正在运行的前台应用。"
    visible = names[:8]
    suffix = f" 等 {len(names)} 个应用" if len(names) > len(visible) else ""
    frontmost = str(data.get("frontmost") or "").strip()
    frontmost_text = f"前台是 {frontmost}。" if frontmost else ""
    return f"正在运行的应用：{', '.join(visible)}{suffix}。{frontmost_text}"


def _installed_apps_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_apps = data.get("apps")
    if not isinstance(raw_apps, list):
        raw_apps = data.get("matches")
    if not isinstance(raw_apps, list):
        return ""
    query = str(data.get("query") or "").strip()
    names = []
    for item in raw_apps:
        if not isinstance(item, dict):
            continue
        name = str(item.get("name") or "").strip()
        if name:
            names.append(name)
    if not names:
        return f"没有找到匹配「{query}」的已安装应用。" if query else "没有读取到已安装应用。"
    visible = names[:8]
    total_count = data.get("total_count")
    try:
        total = int(total_count)
    except (TypeError, ValueError):
        total = len(names)
    suffix = f" 等 {total} 个应用" if total > len(visible) else ""
    prefix = f"匹配「{query}」的已安装应用" if query else "已安装应用"
    return f"{prefix}：{', '.join(visible)}{suffix}。"


def _windows_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_windows = data.get("windows")
    if not isinstance(raw_windows, list):
        return ""
    app_filter = str(data.get("app_name") or planned_input.get("app_name") or "").strip()
    if not raw_windows:
        return f"没有读取到 {app_filter} 的窗口。" if app_filter else "没有读取到打开的窗口。"
    items = []
    for item in raw_windows[:8]:
        if not isinstance(item, dict):
            continue
        app_name = str(item.get("app_name") or "").strip()
        title = str(item.get("title") or "").strip()
        if title and app_name:
            items.append(f"{app_name}: {title}")
        elif app_name:
            items.append(app_name)
        elif title:
            items.append(title)
    if not items:
        return ""
    suffix = f" 等 {len(raw_windows)} 个窗口" if len(raw_windows) > len(items) else ""
    return f"当前窗口：{'; '.join(items)}{suffix}。"


def _ui_elements_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    raw_elements = data.get("elements")
    if not isinstance(raw_elements, list):
        return ""
    app_name = str(data.get("app_name") or "").strip()
    title = str(data.get("title") or "").strip()
    if not raw_elements:
        scope = f"{app_name} {title}".strip()
        return f"没有读取到 {scope} 的界面控件。" if scope else "没有读取到当前界面控件。"
    items = []
    for item in raw_elements[:8]:
        if not isinstance(item, dict):
            continue
        role = str(item.get("role") or "元素").replace("AX", "").strip()
        label = (
            str(item.get("name") or "").strip()
            or str(item.get("description") or "").strip()
            or str(item.get("value") or "").strip()
        )
        center = item.get("center") if isinstance(item.get("center"), dict) else {}
        point = ""
        if center.get("x") not in (None, "") and center.get("y") not in (None, ""):
            point = f"（{center['x']}, {center['y']}）"
        items.append(f"{role}{f' {label}' if label else ''}{point}")
    if not items:
        return ""
    suffix = f" 等 {len(raw_elements)} 个控件" if len(raw_elements) > len(items) else ""
    prefix = f"当前 {app_name} 界面控件" if app_name else "当前界面控件"
    return f"{prefix}：{'; '.join(items)}{suffix}。"


def _inspect_app_result_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    if not data:
        return ""
    app_name = str(
        data.get("app_name")
        or data.get("discovered_app_name")
        or data.get("requested_app_name")
        or ""
    ).strip()
    ui_result = data.get("ui_elements") if isinstance(data.get("ui_elements"), dict) else {}
    ui_summary = _ui_elements_summary(ui_result) if ui_result else ""
    open_result = data.get("open_result") if isinstance(data.get("open_result"), dict) else {}
    focus_result = data.get("focus_result") if isinstance(data.get("focus_result"), dict) else {}
    if open_result.get("ok") is True and app_name:
        prefix = f"已打开 {app_name}。"
    elif (
        focus_result.get("ok") is True
        or data.get("focus_verified") is True
    ) and app_name:
        prefix = f"已切换到 {app_name}。"
    elif app_name:
        prefix = f"已检查 {app_name}。"
    else:
        prefix = "已检查应用。"
    return f"{prefix} {ui_summary}".strip() if ui_summary else prefix


def _app_status_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    app_name = str(data.get("app_name") or planned_input.get("app_name") or "").strip()
    running = data.get("running")
    if not app_name or not isinstance(running, bool):
        return ""
    return f"{app_name} 当前{'正在运行' if running else '没有运行'}。"


def _text_list(value: Any) -> list[str]:
    if isinstance(value, (list, tuple, set)):
        raw_items = value
    elif value in (None, ""):
        raw_items = []
    else:
        raw_items = [value]
    items: list[str] = []
    for item in raw_items:
        text = str(item or "").strip()
        if text and text not in items:
            items.append(text)
    return items


def _browser_page_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    title = str(data.get("title") or "").strip()
    url = str(data.get("url") or "").strip()
    if title and url:
        return f"当前网页是 {title}：{url}。"
    if url:
        return f"当前网页是 {url}。"
    return "已读取当前网页信息。"


def _browser_click_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    selector = str(data.get("selector") or planned_input.get("selector") or "").strip()
    point = _browser_point_selector_label(selector)
    if point:
        return f"已点击网页位置：{point}。"
    label = str(data.get("label") or "").strip()
    target = label or _browser_selector_label(selector)
    return f"已点击网页元素：{target}。" if target else "已点击网页元素。"


def _browser_type_text_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    selector = str(data.get("selector") or planned_input.get("selector") or "").strip()
    length = data.get("length") or data.get("character_count")
    if not isinstance(length, int):
        text = _payload_text(result, planned_input, "text")
        length = len(text) if text else 0
    point = _browser_point_selector_label(selector)
    if point:
        return f"已在网页位置：{point} 输入文字（{length} 个字符）。"
    target = _browser_selector_label(selector)
    if target:
        return f"已在网页{target}输入文字（{length} 个字符）。"
    return f"已向网页输入文字（{length} 个字符）。"


def _browser_selector_label(selector: str) -> str:
    clean = str(selector or "").strip()
    if clean.startswith("text="):
        return clean.removeprefix("text=").strip()
    point = _browser_point_selector_label(clean)
    if point:
        return f"位置 {point}"
    if clean in {
        'input[type="search"], input[name="q"], textarea[name="q"], input[aria-label*="搜索" i], input[placeholder*="搜索" i], input[aria-label*="search" i], input[placeholder*="search" i]',
    }:
        return "搜索框"
    if clean:
        return f"元素 {clean}"
    return ""


def _browser_point_selector_label(selector: str) -> str:
    clean = str(selector or "").strip()
    if not clean.startswith("point="):
        return ""
    parts = [part.strip() for part in clean.removeprefix("point=").split(",")]
    if len(parts) != 2 or not all(parts):
        return ""
    try:
        x = _browser_point_number_label(parts[0])
        y = _browser_point_number_label(parts[1])
    except ValueError:
        return ""
    return f"{x}, {y}"


def _browser_point_number_label(value: str) -> str:
    number = float(value)
    if number.is_integer():
        return str(int(number))
    return str(number)


def _browser_text_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    text = str(data.get("text") or result.get("text") or "").strip()
    if not text:
        return "已读取当前网页文本。"
    if len(text) > 1200:
        text = f"{text[:1200]}..."
    return text


def _browser_text_digest_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    text = str(data.get("text") or result.get("text") or "").strip()
    if not text:
        return "已读取当前网页文本，但没有可见正文可总结。"
    points = _browser_text_digest_points(text)
    if not points:
        return "已读取当前网页文本，但没有足够正文可总结。"
    return "网页内容摘要：\n" + "\n".join(f"- {point}" for point in points)


def _browser_text_digest_points(text: str) -> list[str]:
    paragraphs = [
        _clean_browser_digest_line(line)
        for line in str(text or "").replace("\r", "\n").split("\n")
    ]
    candidates = [line for line in paragraphs if line]
    if len(candidates) < 2:
        candidates.extend(
            _clean_browser_digest_line(sentence)
            for sentence in _split_browser_digest_sentences(" ".join(candidates or [text]))
        )
    points: list[str] = []
    seen: set[str] = set()
    for candidate in candidates:
        if not candidate:
            continue
        normalized = candidate.casefold()
        if normalized in seen:
            continue
        seen.add(normalized)
        points.append(_truncate_browser_digest_point(candidate))
        if len(points) >= 4:
            break
    return points


def _split_browser_digest_sentences(text: str) -> list[str]:
    sentences: list[str] = []
    current: list[str] = []
    for char in str(text or ""):
        current.append(char)
        if char in "。！？.!?":
            sentence = "".join(current).strip()
            if sentence:
                sentences.append(sentence)
            current = []
    tail = "".join(current).strip()
    if tail:
        sentences.append(tail)
    return sentences


def _clean_browser_digest_line(value: str) -> str:
    text = " ".join(str(value or "").strip().split())
    text = text.strip("-*•·#> ")
    if len(text) < 8:
        return ""
    return text


def _truncate_browser_digest_point(value: str, limit: int = 180) -> str:
    text = str(value or "").strip()
    if len(text) <= limit:
        return text
    return f"{text[:limit].rstrip()}..."


def _apple_music_control_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("control") or planned_input.get("action") or "").strip()
    label = _apple_music_control_label(action)
    if not label:
        return ""
    track = str(data.get("track") or "").strip()
    artist = str(data.get("artist") or "").strip()
    track_text = f"当前：{track}{f' - {artist}' if artist else ''}。" if track else ""
    if data.get("playback_state_unverified"):
        return f"已用媒体键尝试{label} Apple Music。{track_text}"
    return f"已{label} Apple Music。{track_text}"


def _apple_music_status_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    if data.get("running") is False or str(data.get("player_state") or "").strip() == "not_running":
        return "Apple Music 当前没有运行。"
    state = str(data.get("player_state") or "").strip().lower()
    track = str(data.get("track") or "").strip()
    artist = str(data.get("artist") or "").strip()
    track_text = f"{track}{f' - {artist}' if artist else ''}" if track else ""
    if state == "playing" and track_text:
        return f"Apple Music 当前播放：{track_text}。"
    labels = {
        "playing": "正在播放",
        "paused": "已暂停",
        "stopped": "已停止",
    }
    label = labels.get(state, state or "状态未知")
    if track_text:
        return f"Apple Music 当前{label}：{track_text}。"
    return f"Apple Music 当前{label}，没有可读取的曲目。"


def _music_app_display_name(app_name: str, result: dict[str, Any]) -> str:
    clean_name = str(app_name or "").strip()
    action = str(result.get("action") or "").strip()
    if action.startswith("media.apple_music") or clean_name.casefold() in {
        "music",
        "apple music",
    }:
        return _display_target_name("Apple Music")
    return _display_target_name(clean_name)


def _music_app_open_and_play_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    app_name = str(data.get("app_name") or planned_input.get("app_name") or "").strip()
    if not app_name:
        return ""
    track = str(data.get("track") or "").strip()
    artist = str(data.get("artist") or "").strip()
    track_text = f"{track}{f' - {artist}' if artist else ''}" if track else ""
    if data.get("playback_state_unverified"):
        return f"已打开{_music_app_display_name(app_name, result)}，并用媒体键尝试开始播放。"
    suffix = f"当前：{track_text}。" if track_text else ""
    return f"已打开{_music_app_display_name(app_name, result)}，并开始播放。{suffix}"


def _music_app_control_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    app_name = str(data.get("app_name") or planned_input.get("app_name") or "").strip()
    action = str(data.get("control") or planned_input.get("action") or "").strip()
    label = _music_app_control_label(action)
    if not app_name or not label:
        return ""
    if data.get("running") is False:
        return f"{_display_target_name(app_name).strip() or app_name} 当前没有运行。"
    return f"已向{_display_target_name(app_name, '发送媒体键')}尝试{label}。"


def _system_media_control_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("control") or planned_input.get("action") or "").strip()
    label = _music_app_control_label(action)
    if not label:
        return ""
    return f"已发送媒体键尝试{label}当前媒体。"


def _unverified_media_action_failure_summary(summary: str) -> str:
    first_sentence = str(summary or "").strip().split("。", 1)[0].strip()
    if "尝试" not in first_sentence and "请求" not in first_sentence:
        first_sentence = "已发送媒体控制请求"
    return f"{first_sentence}，但无法确认播放状态；请在播放器中确认后重试。"


def _unverified_desktop_action_failure_summary(summary: str) -> str:
    first_sentence = str(summary or "").strip().split("。", 1)[0].strip()
    if not first_sentence:
        first_sentence = "已发送桌面操作指令"
    return f"{first_sentence}，但未能确认界面已按预期变化；请确认后重试。"


def _tool_requests_require_media_playback(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    """Return whether the immutable plan requires a playback effect."""

    return any(
        str(request.get("tool") or "").strip().startswith("media.")
        or str(request.get("intent_kind") or "").strip().casefold()
        == "media_playback"
        or "media_playback"
        in str(request.get("planning_reason") or "").strip().casefold()
        for request in requests
        if isinstance(request, Mapping)
    )


def _tool_requests_include_media_tool(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    """Return whether a plan already attempted a native media capability."""

    return any(
        str(request.get("tool") or "").strip().startswith("media.")
        for request in requests
        if isinstance(request, Mapping)
    )


def _direct_step_requires_honest_unverified_outcome(
    step: Mapping[str, Any],
) -> bool:
    tool_name = str(step.get("tool") or "").strip()
    result = step.get("result") if isinstance(step.get("result"), Mapping) else {}
    return bool(
        tool_name.startswith("media.")
        or _has_permission_recovery_signal(dict(result))
        or step.get("requires_post_action_verification") is True
        or (
            tool_name in DAILY_DESKTOP_TOOL_NAMES
            and tool_name not in READ_ONLY_OBSERVATION_TOOLS
            and tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS
            and tool_name not in _DIRECT_DAILY_SEQUENCE_CONTEXT_TOOLS
        )
    )


def _direct_step_unverified_outcome_reason(step: Mapping[str, Any]) -> str:
    return str(
        evaluate_main_chat_outcome(
            {},
            [
                {
                    "event_type": "agent.tool.call",
                    "payload": dict(step),
                }
            ],
        ).reason
        or ""
    ).strip()


def _music_app_control_label(action: str) -> str:
    return {
        "toggle": "切换播放/暂停",
        "play": "开始播放",
        "pause": "暂停",
        "next": "切到下一首",
        "previous": "切到上一首",
    }.get(str(action or "").strip(), "")


def _system_settings_open_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    label = str(data.get("settings_label") or "").strip()
    target = str(data.get("target") or planned_input.get("target") or "").strip()
    if target and target not in {"System Settings", "系统设置", "设置"}:
        return f"已打开系统设置：{target}。"
    if label and label != "System Settings":
        return f"已打开系统设置：{label}。"
    return "已打开系统设置。"


def _apple_music_control_label(action: str) -> str:
    return {
        "toggle": "切换播放/暂停",
        "play": "继续播放",
        "pause": "暂停",
        "next": "切到下一首",
        "previous": "切到上一首",
    }.get(str(action or "").strip(), "")


def _safe_shortcut_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("shortcut_action") or planned_input.get("action") or "").strip()
    label = {
        "copy": "复制选中内容",
        "copy_current_page_link": "复制当前网页链接",
        "paste": "粘贴",
        "select_all": "全选",
        "undo": "撤销",
        "redo": "重做",
        "find": "打开查找",
        "focus_address_bar": "聚焦地址栏",
        "new_tab": "新建标签页",
        "new_private_window": "新建私密窗口",
        "close_tab": "关闭标签页",
        "next_tab": "切到下一个标签页",
        "previous_tab": "切到上一个标签页",
        "next_window": "切到下一个窗口",
        "previous_window": "切到上一个窗口",
        "switch_previous_app": "切到上一个应用",
        "switch_next_app": "切到下一个应用",
        "hide_other_apps": "隐藏其他应用",
        "toggle_full_screen": "切换当前窗口全屏",
        "mission_control": "打开任务控制中心",
        "application_windows": "显示当前应用窗口",
        "spotlight_search": "打开 Spotlight",
        "emoji_picker": "打开 Emoji 面板",
        "screenshot_selection": "截取选区截图",
        "screenshot_toolbar": "打开截图/录屏工具",
        "lock_screen": "锁屏",
        "force_quit_dialog": "打开强制退出窗口",
        "new_window": "新建窗口",
        "new_document": "新建文档",
        "new_task": "新建任务",
        "new_message": "新建消息",
        "new_folder": "新建文件夹",
        "rename_selected": "重命名 Finder 选中项",
        "finder_get_info": "显示 Finder 选中项简介",
        "parent_folder": "打开上一级文件夹",
        "new_note": "新建笔记",
        "new_reminder": "新建提醒事项",
        "new_event": "新建日程",
        "refresh": "刷新",
        "bookmark_page": "加入书签",
        "show_history": "打开历史记录",
        "open_devtools": "打开开发者工具",
        "zoom_in": "放大页面",
        "zoom_out": "缩小页面",
        "reset_zoom": "重置页面缩放",
        "browser_back": "返回上一页",
        "browser_forward": "前进一页",
        "reopen_closed_tab": "重新打开关闭的标签页",
        "finder_quick_look": "快速查看选中项",
    }.get(action, "")
    return f"已发送“{label}”快捷键。" if label else ""


def _safe_key_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("key_action") or planned_input.get("action") or "").strip()
    label = {
        "escape": "Escape",
        "tab": "Tab",
        "shift_tab": "Shift+Tab",
        "arrow_up": "上箭头",
        "arrow_down": "下箭头",
        "arrow_left": "左箭头",
        "arrow_right": "右箭头",
        "home": "Home",
        "end": "End",
        "page_up": "Page Up",
        "page_down": "Page Down",
        "show_desktop": "显示桌面",
    }.get(action, "")
    try:
        repeat_count = int(data.get("repeat_count") or planned_input.get("repeat_count") or 1)
    except (TypeError, ValueError):
        repeat_count = 1
    if not label:
        return ""
    if action == "show_desktop":
        return "已显示桌面。"
    suffix = "" if repeat_count == 1 else f"（{repeat_count} 次）"
    return f"已按{label}{suffix}。"


def _safe_scroll_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    direction = str(data.get("direction") or planned_input.get("direction") or "").strip().lower()
    label = {"down": "向下", "up": "向上"}.get(direction, "")
    try:
        pages = int(data.get("pages") or planned_input.get("pages") or 1)
    except (TypeError, ValueError):
        pages = 1
    if not label:
        return ""
    return f"已{label}滚动前台界面（{pages} 页）。"


def _system_volume_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("requested_action") or planned_input.get("action") or "").strip()
    level = data.get("level")
    muted = data.get("muted")
    old_level = data.get("old_level")
    try:
        level_text = f"{int(level)}%"
    except (TypeError, ValueError):
        level_text = ""
    try:
        old_level_text = f"{int(old_level)}%"
    except (TypeError, ValueError):
        old_level_text = ""
    if action == "status":
        if level_text:
            return f"当前系统音量是 {level_text}{'，已静音' if muted else ''}。"
        return ""
    if action == "set" and level_text:
        return f"已把系统音量调到 {level_text}。"
    if action == "up" and old_level_text and level_text:
        return f"已把系统音量从 {old_level_text} 调高到 {level_text}。"
    if action == "down" and old_level_text and level_text:
        return f"已把系统音量从 {old_level_text} 调低到 {level_text}。"
    if action == "mute":
        return "已将系统音量静音。"
    if action == "unmute":
        return f"已取消系统静音{f'，当前音量 {level_text}' if level_text else ''}。"
    return ""


def _system_brightness_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    action = str(data.get("requested_action") or planned_input.get("action") or "").strip()
    try:
        step = int(data.get("step") or planned_input.get("step") or 2)
    except (TypeError, ValueError):
        step = 2
    suffix = "" if step <= 1 else f"（{step} 格）"
    if action == "up":
        return f"已调高屏幕亮度{suffix}。"
    if action == "down":
        return f"已调低屏幕亮度{suffix}。"
    return ""


def _clipboard_write_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    length = data.get("text_length")
    if not isinstance(length, int):
        text = str(planned_input.get("text") or "")
        length = len(text) if text else 0
    return f"已复制 {length} 个字符到剪贴板。" if length else "已写入剪贴板。"


def _clipboard_read_summary(result: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    text = str(data.get("text") or "")
    text_length = data.get("text_length")
    truncated = bool(data.get("truncated"))
    if not text:
        return "剪贴板是空的。" if text_length == 0 else ""
    suffix = "（内容较长，已截断预览）" if truncated else ""
    return f"剪贴板内容：{text}{suffix}"


def _notes_create_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    title = str(data.get("title") or planned_input.get("title") or "").strip()
    length = data.get("body_length")
    if not isinstance(length, int):
        body = str(planned_input.get("body") or "")
        length = len(body) if body else 0
    detail = f"：{title}" if title else ""
    suffix = f"（{length} 个字符）" if length else ""
    return f"已创建备忘录{detail}{suffix}。"


def _reminders_create_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    title = str(data.get("title") or planned_input.get("title") or "").strip()
    due_at = str(data.get("due_at") or planned_input.get("due_at") or "").strip()
    if not title:
        return ""
    return f"已创建提醒事项：{title}{f'（{due_at}）' if due_at else ''}。"


def _calendar_create_event_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    title = str(data.get("title") or planned_input.get("title") or "").strip()
    start_at = str(data.get("start_at") or planned_input.get("start_at") or "").strip()
    end_at = str(data.get("end_at") or planned_input.get("end_at") or "").strip()
    if not title:
        return ""
    time_text = ""
    if start_at and end_at:
        time_text = f"（{start_at} - {end_at}）"
    elif start_at:
        time_text = f"（{start_at}）"
    return f"已创建日历事件：{title}{time_text}。"


def _desktop_open_path_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    path = _desktop_path_summary_path(result, planned_input)
    if not path:
        return ""
    if data.get("is_dir") is True:
        return f"已打开文件夹：{path}。"
    return f"已打开文件：{path}。"


def _desktop_open_path_with_app_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    path = _desktop_path_summary_path(result, planned_input)
    app_name = str(data.get("app_name") or planned_input.get("app_name") or "").strip()
    if not path:
        return ""
    app_text = f"用 {app_name} " if app_name else "用应用"
    if data.get("is_dir") is True:
        return f"已{app_text}打开文件夹：{path}。"
    return f"已{app_text}打开文件：{path}。"


def _desktop_path_summary_path(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    data = result.get("data") if isinstance(result.get("data"), dict) else {}
    if data.get("desktop_object"):
        return str(
            data.get("display_path")
            or data.get("resolved_path")
            or data.get("expanded_path")
            or data.get("path")
            or planned_input.get("path")
            or ""
        ).strip()
    return str(data.get("path") or planned_input.get("path") or "").strip()


def _terminal_run_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    command = str(planned_input.get("command") or "").strip()
    command_text = f"：{command}" if command else ""
    stdout = str(result.get("stdout") or "").strip()
    stderr = str(result.get("stderr") or "").strip()
    if result.get("ok"):
        if stdout:
            return f"已运行命令{command_text}。\n输出：{_terminal_output_preview(stdout)}"
        return f"已运行命令{command_text}。"
    parts = [f"命令执行失败{command_text}。"]
    returncode = result.get("returncode")
    if returncode not in (None, ""):
        parts.append(f"退出码：{returncode}。")
    if stderr:
        parts.append(f"stderr：{_terminal_output_preview(stderr)}")
    elif stdout:
        parts.append(f"stdout：{_terminal_output_preview(stdout)}")
    return " ".join(parts)


def _data_analyze_summary(result: dict[str, Any], planned_input: dict[str, Any]) -> str:
    path = str(result.get("path") or planned_input.get("path") or "").strip()
    artifact = result.get("artifact") if isinstance(result.get("artifact"), dict) else {}
    artifact_path = str(
        artifact.get("path")
        or result.get("artifact_path")
        or planned_input.get("artifact_path")
        or ""
    ).strip()
    rows = result.get("rows")
    columns = result.get("columns")
    column_count = len(columns) if isinstance(columns, list) else None
    source = f"「{path}」" if path else "数据文件"
    facts = []
    if rows not in (None, ""):
        facts.append(f"{rows} 行")
    if column_count is not None:
        facts.append(f"{column_count} 列")
    fact_text = f"（{ '、'.join(facts) }）" if facts else ""
    artifact_text = f"报告已写入 {artifact_path}。" if artifact_path else ""
    return f"已分析{source}{fact_text}。{artifact_text}".strip()


def _terminal_output_preview(value: str) -> str:
    text = str(value or "").strip()
    if len(text) > 600:
        return f"{text[:600]}..."
    return text


def _sentence(value: str) -> str:
    text = str(value or "").strip()
    if not text:
        return ""
    if text.endswith(("。", ".", "!", "！", "?", "？")):
        return text
    return f"{text}。"


def _combine_daily_desktop_summaries(summaries: list[str]) -> str:
    sentences = [_sentence(summary) for summary in summaries if str(summary or "").strip()]
    return " ".join(sentences)


def _model_followup_context_payload(
    planned_tool_requests: list[dict[str, Any]],
    selection_payload: dict[str, Any],
    *,
    allowed_tools: list[str],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    content_requests = [
        request
        for request in planned_tool_requests
        if isinstance(request, dict)
        and bool(request.get("continue_to_model"))
    ]
    if not content_requests:
        content_requests = _model_followup_context_requests_from_selection_target(
            planned_tool_requests,
            selection_payload,
        )
    if not content_requests:
        return {}
    context_only_followup = any(
        bool(request.get("_context_only_followup"))
        for request in content_requests
        if isinstance(request, Mapping)
    )
    observation_content_requests = [
        request
        for request in content_requests
        if not _tool_request_requires_model_materialization(request)
    ]
    materialization_content_requests = [
        request
        for request in content_requests
        if _tool_request_requires_model_materialization(request)
    ]
    if materialization_content_requests and not observation_content_requests:
        observation_content_requests = _model_followup_pre_materialization_requests(
            planned_tool_requests,
            materialization_content_requests,
        )
    allowed = {
        str(tool or "").strip()
        for tool in allowed_tools
        if str(tool or "").strip()
    }
    artifacts_expected = _string_list(selection_payload.get("artifacts_expected"))
    planning_reason_requests = [
        request
        for request in content_requests
        if str(request.get("replan_recovery_identity") or "").strip()
    ] or content_requests
    planning_reasons = _ordered_text_list(
        [
            str(request.get("planning_reason") or "").strip()
            for request in planning_reason_requests
            if str(request.get("planning_reason") or "").strip()
        ]
    )
    planning_reason = (
        planning_reasons[0]
        if len(planning_reasons) == 1
        else "planner_model_followup_context"
    )
    observation_tools = _ordered_text_list(
        [
            str(request.get("tool") or "").strip()
            for request in observation_content_requests
            if str(request.get("tool") or "").strip()
        ]
    )
    payload: dict[str, Any] = {
        "source": "runtime_planner",
        "status": "ready",
        "planning_reason": planning_reason,
        "observation_tools": observation_tools,
        "artifact_write_allowed": "artifact.write" in allowed,
    }
    followup_target = _model_followup_target_payload(selection_payload, allowed)
    if followup_target:
        payload["followup_target"] = followup_target
    if artifacts_expected:
        payload["artifacts_expected"] = artifacts_expected
    materialization_requests_for_pending = (
        []
        if followup_target
        and _materialization_requests_are_artifact_writes(
            materialization_content_requests
        )
        else materialization_content_requests
    )
    pending_plan_steps = (
        []
        if context_only_followup
        else _model_followup_pending_plan_steps(
            selection_payload,
            observation_content_requests,
        )
    )
    if pending_plan_steps:
        payload["pending_plan_steps"] = pending_plan_steps
    pending_execution_requests = (
        []
        if context_only_followup
        else _model_followup_pending_execution_requests(
            selection_payload,
            observation_content_requests,
            allowed,
        )
    )
    materialization_execution_requests = (
        []
        if context_only_followup
        else _model_followup_materialization_execution_requests(
            selection_payload,
            materialization_requests_for_pending,
            allowed,
        )
    )
    if materialization_execution_requests:
        pending_execution_requests = _merged_pending_execution_requests(
            pending_execution_requests,
            materialization_execution_requests,
        )
    deferred_execution_requests = (
        []
        if context_only_followup
        else _model_followup_deferred_execution_requests(
            content_requests,
            allowed,
        )
    )
    if deferred_execution_requests:
        pending_execution_requests = _merged_pending_execution_requests(
            pending_execution_requests,
            deferred_execution_requests,
        )
    if pending_execution_requests:
        payload["pending_execution_requests"] = pending_execution_requests
    task_core_payload = _model_followup_task_core_payload(selection_payload)
    if task_core_payload:
        payload["task_core"] = task_core_payload
    content_snapshots = followup_content_snapshots(timeline, observation_tools)
    content_snapshot = content_snapshots[-1] if content_snapshots else latest_followup_content_snapshot(timeline, observation_tools)
    if content_snapshot:
        payload["content_snapshot"] = content_snapshot
    if content_snapshots:
        payload["content_snapshots"] = content_snapshots
    for key in ("decision_id", "plan_id", "intent_kind"):
        value = str(selection_payload.get(key) or "").strip()
        if value:
            payload[key] = value
    return payload


def _model_followup_pre_materialization_requests(
    planned_tool_requests: list[dict[str, Any]],
    materialization_requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    materialization_ids = {id(request) for request in materialization_requests}
    observed: list[dict[str, Any]] = []
    for request in planned_tool_requests:
        if not isinstance(request, dict):
            continue
        if id(request) in materialization_ids:
            break
        if str(request.get("tool") or "").strip() and not (
            _tool_request_requires_model_materialization(request)
        ):
            observed.append(request)
    preferred = [
        request
        for request in observed
        if str(request.get("tool") or "").strip()
        in _MODEL_FOLLOWUP_PRIMARY_OBSERVATION_TOOLS
    ]
    return preferred[-1:] if preferred else observed


def _materialization_requests_are_artifact_writes(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    tools = [
        str(request.get("tool") or request.get("tool_name") or "").strip()
        for request in requests
        if isinstance(request, Mapping)
    ]
    return bool(tools) and set(tools) == {"artifact.write"}


_MODEL_FOLLOWUP_PRIMARY_OBSERVATION_TOOLS = frozenset(
    {
        "browser.extract_text",
        "browser.open_url_and_extract_text",
        "clipboard.read",
        "data.analyze",
        "desktop.inspect_app",
        "desktop.read_ui",
        "desktop.ui_elements",
        "file.read",
        "file.search",
        "screen.capture",
        "terminal.run",
        "workspace.list",
        "workspace.read",
    }
)


def _model_followup_context_requests_from_selection_target(
    planned_tool_requests: list[dict[str, Any]],
    selection_payload: Mapping[str, Any],
) -> list[dict[str, Any]]:
    if not _selection_payload_has_model_followup_target(selection_payload):
        return []
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    post_action_observation = (
        target.get("post_action_observation")
        if isinstance(target.get("post_action_observation"), Mapping)
        else {}
    )
    preferred_tools = _ordered_text_list(
        [
            str(post_action_observation.get("tool") or "").strip(),
            "desktop.ui_elements",
            "desktop.read_ui",
            "screen.capture",
        ]
    )
    for request in reversed(planned_tool_requests):
        if not isinstance(request, dict):
            continue
        tool_name = str(request.get("tool") or "").strip()
        if preferred_tools and tool_name not in preferred_tools:
            continue
        return [{**request, "continue_to_model": True, "_context_only_followup": True}]
    return []


def _model_followup_task_core_payload(selection_payload: Mapping[str, Any]) -> dict[str, Any]:
    for key in ("task_core", "yachiyo_task_core"):
        value = selection_payload.get(key)
        if isinstance(value, Mapping) and value:
            return dict(value)
    runtime_plan = (
        selection_payload.get("runtime_plan")
        if isinstance(selection_payload.get("runtime_plan"), Mapping)
        else {}
    )
    value = runtime_plan.get("task_core") if isinstance(runtime_plan, Mapping) else {}
    return dict(value) if isinstance(value, Mapping) and value else {}


def _model_followup_pending_plan_steps(
    selection_payload: Mapping[str, Any],
    content_requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    steps = _selection_tool_plan_steps(selection_payload)
    observation_tools = [
        str(request.get("tool") or "").strip()
        for request in content_requests
        if str(request.get("tool") or "").strip()
    ]
    if not steps or not observation_tools:
        return []

    start_index = _pending_plan_step_start_index(steps, observation_tools)
    if start_index <= 0:
        return []

    pending_steps: list[dict[str, Any]] = []
    task_core = _model_followup_task_core_payload(selection_payload)
    for step in steps[start_index:]:
        step_payload = _model_followup_plan_step_payload(step)
        if not step_payload:
            continue
        step_payload.update(
            _model_followup_task_core_step_runtime_trace(
                task_core,
                str(step_payload.get("step_id") or ""),
            )
        )
        pending_steps.append(step_payload)
        if len(pending_steps) >= 5:
            break
    return pending_steps


def _model_followup_pending_execution_requests(
    selection_payload: Mapping[str, Any],
    content_requests: list[dict[str, Any]],
    allowed: set[str],
) -> list[dict[str, Any]]:
    requests = _selection_execution_envelope_requests(selection_payload)
    observation_tools = [
        str(request.get("tool") or "").strip()
        for request in content_requests
        if str(request.get("tool") or "").strip()
    ]
    if not requests or not observation_tools:
        return []

    start_index = _pending_execution_request_start_index(requests, observation_tools)
    if start_index <= 0:
        return []

    pending_requests: list[dict[str, Any]] = []
    for request in requests[start_index:]:
        request_payload = _model_followup_execution_request_payload(request, allowed)
        if not request_payload:
            continue
        pending_requests.append(request_payload)
        if len(pending_requests) >= 5:
            break
    return pending_requests


def _model_followup_materialization_execution_requests(
    selection_payload: Mapping[str, Any],
    content_requests: Iterable[Mapping[str, Any]],
    allowed: set[str],
) -> list[dict[str, Any]]:
    materialization_requests = [
        request
        for request in content_requests
        if isinstance(request, Mapping)
        and _tool_request_requires_model_materialization(request)
    ]
    if not materialization_requests:
        return []
    envelope_requests = _selection_execution_envelope_requests(selection_payload)
    start_indexes = [
        index
        for request in materialization_requests
        for index in [
            _matching_execution_envelope_request_index(envelope_requests, request)
        ]
        if index >= 0
    ]
    raw_pending_requests: Iterable[Mapping[str, Any]]
    if start_indexes:
        raw_pending_requests = envelope_requests[min(start_indexes) :]
    else:
        raw_pending_requests = materialization_requests

    pending: list[dict[str, Any]] = []
    for request in raw_pending_requests:
        if not isinstance(request, Mapping):
            continue
        request_payload = _model_followup_execution_request_payload(request, allowed)
        if request_payload:
            pending.append(request_payload)
    return pending


def _matching_execution_envelope_request_index(
    envelope_requests: list[Mapping[str, Any]],
    request: Mapping[str, Any],
) -> int:
    if not envelope_requests:
        return -1
    request_id = str(request.get("request_id") or "").strip()
    step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    for index, candidate in enumerate(envelope_requests):
        candidate_request_id = str(candidate.get("request_id") or "").strip()
        if request_id and candidate_request_id == request_id:
            return index
        candidate_step_id = str(candidate.get("step_id") or "").strip()
        if step_id and candidate_step_id == step_id:
            return index
    request_tool = str(request.get("tool") or request.get("tool_name") or "").strip()
    request_input = (
        request.get("input")
        if isinstance(request.get("input"), Mapping)
        else request.get("input_preview")
        if isinstance(request.get("input_preview"), Mapping)
        else {}
    )
    for index, candidate in enumerate(envelope_requests):
        candidate_tool = str(
            candidate.get("tool_name") or candidate.get("tool") or ""
        ).strip()
        if candidate_tool != request_tool:
            continue
        candidate_input = (
            candidate.get("input") if isinstance(candidate.get("input"), Mapping) else {}
        )
        if _model_materialization_inputs_match(candidate_input, request_input):
            return index
    return -1


def _model_materialization_inputs_match(
    candidate_input: Mapping[str, Any],
    request_input: Mapping[str, Any],
) -> bool:
    for key in (
        "app_name",
        "path",
        "target_path",
        "body_source",
        "selection_source",
        "app_selection_source",
        "action",
    ):
        request_value = request_input.get(key)
        if request_value in (None, "", [], {}):
            continue
        if candidate_input.get(key) != request_value:
            return False
    return True


def _model_followup_deferred_execution_requests(
    content_requests: list[dict[str, Any]],
    allowed: set[str],
) -> list[dict[str, Any]]:
    pending: list[dict[str, Any]] = []
    for index, request in enumerate(content_requests, start=1):
        if not isinstance(request, Mapping):
            continue
        tool_name = str(request.get("deferred_tool") or "").strip()
        if not tool_name or (allowed and tool_name not in allowed):
            continue
        raw_input = (
            dict(request.get("deferred_input"))
            if isinstance(request.get("deferred_input"), Mapping)
            else {}
        )
        if not raw_input:
            continue
        pending.append(
            {
                "request_id": _deferred_execution_request_id(
                    request,
                    tool_name=tool_name,
                    index=index,
                ),
                "tool_name": tool_name,
                "capability_id": "desktop.ui_operation",
                "input_preview": raw_input,
                "planning_reason": str(
                    request.get("planning_reason") or "planner_followup_deferred_ui_action"
                ).strip(),
                "status": "planned",
                "runtime_doctrine": str(
                    request.get("runtime_doctrine") or "discover_operate_verify"
                ).strip(),
                "runtime_stage": "operate",
                "runtime_role": _deferred_ui_runtime_role(tool_name),
                "approval_required": _deferred_ui_tool_requires_approval(tool_name),
                "requires_post_action_verification": True,
                "source": str(request.get("source") or "runtime_planner").strip(),
            }
        )
    return pending


def _auto_deferred_observed_ui_followup_requests(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    for index, request in enumerate(planned_tool_requests, start=1):
        if not isinstance(request, Mapping) or not bool(request.get("continue_to_model")):
            continue
        target = _deferred_observed_ui_target_from_request(request)
        if not target:
            continue
        target = _deferred_observed_ui_target_with_timeline_context(
            target,
            request,
            timeline,
        )
        if not _runtime_planner_deferred_observation_scope_matches(
            request,
            target,
            timeline,
        ):
            continue
        tool_name = str(request.get("deferred_tool") or "").strip()
        scoped_allowed = _deferred_observed_ui_scoped_allowed_tools(
            tool_name,
            target,
            allowed,
        )
        if not scoped_allowed:
            continue
        target_label = str(target.get("target") or "").strip()
        role_filter = str(target.get("role_filter") or "").strip()
        if not _latest_desktop_observation_has_target_match(
            timeline,
            target_label,
            role_filter,
        ):
            retry_request = _deferred_observed_ui_observation_retry_request(
                request,
                target,
                allowed,
                timeline,
            )
            if retry_request:
                return [retry_request]
            continue
        observation_source = _latest_desktop_observation_tool(timeline)
        if observation_source:
            target["observation_source"] = observation_source
        requests = _auto_desktop_observed_action_followup_requests(
            {"followup_target": target},
            scoped_allowed,
            timeline,
            planning_reason=_deferred_observed_ui_planning_reason(request),
        )
        if requests:
            annotated = _annotate_deferred_observed_ui_followup_requests(
                requests,
                request,
                tool_name=tool_name,
                index=index,
            )
            continuation = _deferred_observed_ui_continuation_requests(request, allowed)
            if not continuation:
                continuation = _deferred_media_playback_continuation_requests(
                    request,
                    target,
                    allowed,
                    planning_reason=_deferred_observed_ui_planning_reason(request),
                )
            return [*annotated, *continuation]
    return []


def _auto_deferred_observed_ui_can_complete_without_model(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = _auto_deferred_observed_ui_followup_requests(
        planned_tool_requests,
        allowed_tools,
        timeline,
    )
    requests = _drop_completed_auto_followup_prefix(
        requests,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    return bool(requests) and not _replan_recovery_requests_need_model_followup(requests)


def _deferred_observed_ui_planning_reason(request: Mapping[str, Any]) -> str:
    if _string_list(request.get("replan_triggers")) or _string_list(
        request.get("replan_signal_ids")
    ):
        return "planner_replan_app_search_observed_result"
    return str(
        request.get("planning_reason") or "planner_followup_deferred_ui_action"
    ).strip() or "planner_followup_deferred_ui_action"


def _deferred_observed_ui_scoped_allowed_tools(
    tool_name: str,
    target: Mapping[str, Any],
    allowed: set[str],
) -> list[str]:
    clean_tool = str(tool_name or "").strip()
    if not clean_tool:
        return []
    observation_tools = (
        "desktop.read_ui",
        "desktop.ui_elements",
        "desktop.active_window",
    )
    app_name = _observed_action_app_name(target)
    tools: list[str] = []
    if clean_tool in {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "desktop.click_ui_element",
        "desktop.safe_click",
        "desktop.click",
    }:
        if clean_tool in allowed:
            tools.append(clean_tool)
        if app_name:
            tools.extend(
                tool
                for tool in (
                    "app.focus_and_click_ui_element",
                    "app.open_and_click_ui_element",
                )
                if tool in allowed
            )
        tools.extend(
            tool
            for tool in (
                "desktop.click_ui_element",
                "desktop.safe_click",
                "desktop.click",
            )
            if tool in allowed
        )
    elif clean_tool in {
        "app.open_and_type_into_ui_element",
        "app.focus_and_type_into_ui_element",
        "desktop.type_into_ui_element",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
    }:
        if clean_tool in allowed:
            tools.append(clean_tool)
        if app_name:
            tools.extend(
                tool
                for tool in (
                    "app.focus_and_type_into_ui_element",
                    "app.open_and_type_into_ui_element",
                    "app.focus_and_click_ui_element",
                    "app.open_and_click_ui_element",
                )
                if tool in allowed
            )
        tools.extend(
            tool
            for tool in (
                "desktop.type_into_ui_element",
                "desktop.click_ui_element",
                "desktop.safe_click",
                "desktop.click",
                "desktop.safe_type_text",
                "desktop.type_text",
                "desktop.type",
            )
            if tool in allowed
        )
    else:
        return []
    tools.extend(tool for tool in observation_tools if tool in allowed)
    return _ordered_text_list(tools)


def _deferred_observed_ui_continuation_requests(
    request: Mapping[str, Any],
    allowed: set[str],
) -> list[dict[str, Any]]:
    raw_continuation = request.get("deferred_continuation")
    if not isinstance(raw_continuation, list):
        return []
    continuation: list[dict[str, Any]] = []
    for item in raw_continuation:
        if not isinstance(item, Mapping):
            continue
        next_request = dict(item)
        tool_name = str(next_request.get("tool") or "").strip()
        if not tool_name or (allowed and tool_name not in allowed):
            continue
        continuation.append(next_request)
    return continuation


def _deferred_media_playback_continuation_requests(
    request: Mapping[str, Any],
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    if str(request.get("intent_kind") or "").strip() != "media_playback":
        return []
    if str(target.get("target_action") or "").strip() != "type_text":
        return []
    app_name = _observed_action_app_name(target)
    if not app_name:
        return []
    source = str(request.get("source") or "runtime_planner").strip() or "runtime_planner"
    submit_request = _media_search_submit_request(
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if not submit_request:
        return []
    result_selection = {
        "target": "first result",
        "role_filter": "",
        "limit": 80,
        "click_count": 1,
    }
    result_observation = _discovered_media_result_observation_request(
        app_name,
        {"result_selection": result_selection},
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if not result_observation:
        return [submit_request]
    return [submit_request, result_observation]


def _deferred_observed_ui_observation_retry_request(
    request: Mapping[str, Any],
    target: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    if "desktop.read_ui" not in allowed:
        return {}
    if str(request.get("tool") or "").strip() == "desktop.read_ui":
        return {}
    latest_observation = _latest_desktop_observation_event(timeline)
    if str(latest_observation.get("tool") or "").strip() != "desktop.ui_elements":
        return {}
    payload = {
        key: value
        for key, value in {
            "app_name": target.get("app_name"),
            "role_filter": target.get("role_filter"),
            "limit": target.get("limit"),
        }.items()
        if value not in (None, "", [], {})
    }
    if "limit" not in payload:
        payload["limit"] = 80
    retry = _request_like(
        "desktop.read_ui",
        payload,
        source=str(request.get("source") or "runtime_planner").strip(),
        planning_reason=str(
            request.get("planning_reason") or "planner_retry_deferred_ui_observation"
        ).strip()
        or "planner_retry_deferred_ui_observation",
    )
    retry["continue_to_model"] = True
    retry["deferred_tool"] = str(request.get("deferred_tool") or "").strip()
    raw_input = request.get("deferred_input")
    if isinstance(raw_input, Mapping):
        retry["deferred_input"] = dict(raw_input)
    raw_context = request.get("deferred_context")
    if isinstance(raw_context, Mapping):
        retry["deferred_context"] = dict(raw_context)
    raw_verifier_context = request.get("deferred_verifier_context")
    if isinstance(raw_verifier_context, Mapping):
        retry["deferred_verifier_context"] = dict(raw_verifier_context)
    raw_continuation = request.get("deferred_continuation")
    if isinstance(raw_continuation, list):
        retry["deferred_continuation"] = [
            dict(item) for item in raw_continuation if isinstance(item, Mapping)
        ]
    for key, value in _request_observability_metadata(request).items():
        retry.setdefault(key, value)
    # This request only re-observes state after an already-approved action.
    # Never inherit the action's approval/risk/stage contract onto the verifier.
    retry.update(
        {
            "approval_required": False,
            "risk_level": "low",
            "requires_observation": True,
            "requires_post_action_verification": False,
            "runtime_stage": "verify",
            "runtime_role": "verify_result",
        }
    )
    request_id = str(request.get("request_id") or "").strip()
    if request_id:
        retry["request_id"] = f"{request_id}:retry:desktop.read_ui"
    retry["observation_retry"] = {
        "from_tool": "desktop.ui_elements",
        "reason": "target_not_found",
        "target": str(target.get("target") or "").strip(),
    }
    return retry


def _deferred_observed_ui_target_from_request(
    request: Mapping[str, Any],
) -> dict[str, Any]:
    tool_name = str(request.get("deferred_tool") or "").strip()
    raw_input = (
        request.get("deferred_input")
        if isinstance(request.get("deferred_input"), Mapping)
        else {}
    )
    if not tool_name or not raw_input:
        return {}
    click_tools = {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "desktop.click_ui_element",
        "desktop.safe_click",
        "desktop.click",
    }
    type_tools = {
        "app.open_and_type_into_ui_element",
        "app.focus_and_type_into_ui_element",
        "desktop.type_into_ui_element",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
    }
    if tool_name in click_tools:
        target_action = "click"
    elif tool_name in type_tools:
        target_action = "type_text"
    else:
        return {}
    target_label = str(raw_input.get("target") or "").strip()
    if not target_label:
        return {}
    target: dict[str, Any] = {
        "kind": "desktop_observed_action",
        "target_action": target_action,
        "target": target_label,
        "role_filter": str(raw_input.get("role_filter") or "").strip(),
        "limit": _clean_model_followup_int(raw_input.get("limit"), default=80),
    }
    app_name = str(raw_input.get("app_name") or request.get("target_app_name") or "").strip()
    if app_name:
        target["app_name"] = app_name
    app_query = str(
        raw_input.get("app_query")
        or request.get("target_app_query")
        or request.get("app_query")
        or ""
    ).strip()
    if app_query:
        target["app_query"] = app_query
    if target_action == "click":
        target["click_count"] = _clean_model_followup_int(
            raw_input.get("click_count"),
            default=1,
        )
    else:
        text = str(raw_input.get("text") or "")
        if not text:
            return {}
        target["text"] = text
        submit_action = str(raw_input.get("submit_action") or "").strip()
        if submit_action:
            target["submit_action"] = submit_action
    return {key: value for key, value in target.items() if value not in ("", None, [], {})}


def _deferred_observed_ui_target_with_timeline_context(
    target: Mapping[str, Any],
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    resolved = dict(target)
    deferred_input = (
        request.get("deferred_input")
        if isinstance(request.get("deferred_input"), Mapping)
        else {}
    )
    app_query = str(
        resolved.get("app_query")
        or resolved.get("target_app_query")
        or deferred_input.get("query")
        or deferred_input.get("app_query")
        or request.get("target_app_query")
        or ""
    ).strip()
    if app_query:
        resolved["app_query"] = app_query
        resolved["target_app_query"] = app_query
    app_name = str(resolved.get("app_name") or resolved.get("target_app_name") or "").strip()
    if app_name and not _runtime_planner_placeholder_app_name(app_name):
        return resolved
    discovered = _discovered_app_name_for_query(timeline, app_query) if app_query else ""
    if not discovered:
        discovered = _latest_replan_context_app_name(timeline, request_id="")
    if discovered and not _runtime_planner_placeholder_app_name(discovered):
        resolved["app_name"] = discovered
        resolved["target_app_name"] = discovered
    return resolved


def _annotate_deferred_observed_ui_followup_requests(
    requests: list[dict[str, Any]],
    source_request: Mapping[str, Any],
    *,
    tool_name: str,
    index: int,
) -> list[dict[str, Any]]:
    deferred_context = (
        source_request.get("deferred_context")
        if isinstance(source_request.get("deferred_context"), Mapping)
        else {}
    )
    request_id = (
        _runtime_planner_deferred_root_request_id(
            source_request,
            deferred_context,
            tool_name=tool_name,
        )
        or _deferred_execution_request_id(
            source_request,
            tool_name=tool_name,
            index=index,
        )
    )
    deferred_verifier_context = (
        source_request.get("deferred_verifier_context")
        if isinstance(source_request.get("deferred_verifier_context"), Mapping)
        else {}
    )
    annotated: list[dict[str, Any]] = []
    for request_index, request in enumerate(requests):
        item = dict(request)
        is_verification_request = _replan_observed_action_verification_request(
            item
        )
        if request_id:
            item["request_id"] = (
                request_id if request_index == 0 else f"{request_id}:verify:{request_index}"
            )
        for key in (
            "decision_id",
            "plan_id",
            "tool_plan_id",
            "intent_kind",
            "core_id",
            "workspace_id",
            "task_id",
            "run_id",
            "runtime_doctrine",
            "requires_post_action_verification",
            "replan_signal_ids",
            "replan_triggers",
        ):
            value = source_request.get(key)
            if value in (None, "", [], {}) or key in item:
                continue
            item[key] = value
        for key in (
            "step_id",
            "planner_step_id",
            "capability_id",
            "task_todo",
            "task_checkpoints",
            "task_workspace_items",
            "approval_required",
            "approval_status",
            "risk_level",
            "depends_on",
            "runtime_stage",
            "runtime_role",
            "requires_post_action_verification",
            "action_target",
            "verification_targets",
        ):
            value = deferred_context.get(key)
            if value in (None, "", [], {}) or key in item:
                continue
            item[key] = value
        if item.get("step_id") and not item.get("planner_step_id"):
            item["planner_step_id"] = str(item.get("step_id") or "").strip()
        if is_verification_request and deferred_verifier_context:
            for key in (
                "step_id",
                "planner_step_id",
                "capability_id",
                "depends_on",
                "task_todo",
                "task_checkpoints",
                "task_workspace_items",
                "verification_targets",
                "task_verification_targets",
                "verification_predicate_kind",
            ):
                value = deferred_verifier_context.get(key)
                if value not in (None, "", [], {}):
                    item[key] = value
            if item.get("step_id"):
                # The generated verifier initially inherits the deferred
                # action's planner_step_id.  Replace it with the dedicated
                # verifier identity; otherwise task progress opens a false
                # replan even though GoalContract correlation succeeds.
                item["planner_step_id"] = str(
                    deferred_verifier_context.get("planner_step_id")
                    or item.get("step_id")
                    or ""
                ).strip()
            verifier_action_target = deferred_verifier_context.get("action_target")
            if isinstance(verifier_action_target, Mapping):
                item["action_target"] = dict(verifier_action_target)
            item["source"] = str(
                deferred_verifier_context.get("source") or "runtime_verification"
            ).strip()
            verifier_reason = str(
                deferred_verifier_context.get("planning_reason") or ""
            ).strip()
            if verifier_reason:
                item["planning_reason"] = verifier_reason
            item["approval_required"] = False
            item["risk_level"] = "low"
            item["runtime_stage"] = "verify"
            item["runtime_role"] = "verify_result"
        else:
            action_target = deferred_context.get("action_target")
            if request_index == 0 and isinstance(action_target, Mapping):
                # The follow-up helper uses an internal observed-action target
                # while the immutable GoalContract is bound to the planner's
                # canonical action target.  Preserve that exact target across
                # the observation -> action transformation so verifier
                # evidence remains contract-correlated.
                item["action_target"] = dict(action_target)
            item.setdefault("runtime_stage", "operate" if request_index == 0 else "verify")
            item.setdefault("runtime_role", _deferred_ui_runtime_role(tool_name))
        if is_verification_request:
            verifier_tool = str(item.get("tool") or "").strip()
            if verifier_tool not in READ_ONLY_OBSERVATION_TOOLS:
                return []
            action_step_id = str(
                deferred_context.get("step_id")
                or deferred_context.get("planner_step_id")
                or ""
            ).strip()
            if not action_step_id:
                return []
            item["approval_required"] = False
            item["risk_level"] = "low"
            item["runtime_stage"] = "verify"
            item["runtime_role"] = "verify_result"
            item["requires_observation"] = True
            item["requires_post_action_verification"] = False
            item["depends_on"] = [action_step_id]
            item["verification_target_step_ids"] = [action_step_id]
            if request_id:
                item["source_request_id"] = request_id
        annotated.append(item)
    return annotated


def _runtime_planner_deferred_root_request_id(
    source_request: Mapping[str, Any],
    deferred_context: Mapping[str, Any],
    *,
    tool_name: str,
) -> str:
    scope = (
        source_request.get("runtime_observation_scope")
        if isinstance(source_request.get("runtime_observation_scope"), Mapping)
        else {}
    )
    if not scope or str(source_request.get("deferred_tool") or "").strip() != str(
        tool_name or ""
    ).strip():
        return ""
    plan_id = str(deferred_context.get("plan_id") or "").strip()
    step_id = str(
        deferred_context.get("step_id")
        or deferred_context.get("planner_step_id")
        or ""
    ).strip()
    request_id = str(deferred_context.get("request_id") or "").strip()
    if (
        not plan_id
        or not step_id
        or str(scope.get("plan_id") or "").strip() != plan_id
        or not request_id.startswith(f"{plan_id}:request:")
    ):
        return ""
    return request_id


def _deferred_execution_request_id(
    request: Mapping[str, Any],
    *,
    tool_name: str,
    index: int,
) -> str:
    request_id = str(request.get("request_id") or "").strip()
    if request_id:
        return f"{request_id}:deferred:{tool_name}"
    plan_id = str(request.get("plan_id") or "").strip()
    if plan_id:
        return f"{plan_id}:deferred:{index}:{tool_name}"
    return f"deferred:{index}:{tool_name}"


def _deferred_ui_runtime_role(tool_name: str) -> str:
    clean_tool = str(tool_name or "").strip()
    if clean_tool.endswith("_type_into_ui_element") or clean_tool in {
        "desktop.type_into_ui_element",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
    }:
        return "type_ui"
    if clean_tool.endswith("_click_ui_element") or clean_tool in {
        "desktop.click_ui_element",
        "desktop.safe_click",
        "desktop.click",
    }:
        return "click_ui"
    return "operate_ui"


def _deferred_ui_tool_requires_approval(tool_name: str) -> bool:
    clean_tool = str(tool_name or "").strip()
    return clean_tool in {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "app.open_and_type_into_ui_element",
        "app.focus_and_type_into_ui_element",
        "desktop.click_ui_element",
        "desktop.type_into_ui_element",
        "desktop.safe_click",
        "desktop.click",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
    }


def _merged_pending_execution_requests(
    first: list[dict[str, Any]],
    second: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    merged: list[dict[str, Any]] = []
    seen: set[tuple[str, str]] = set()
    for request in [*first, *second]:
        tool_name = str(request.get("tool_name") or request.get("tool") or "").strip()
        input_preview = (
            request.get("input_preview")
            if isinstance(request.get("input_preview"), Mapping)
            else request.get("input")
            if isinstance(request.get("input"), Mapping)
            else {}
        )
        key = (tool_name, repr(sorted(dict(input_preview).items())))
        if key in seen:
            continue
        seen.add(key)
        merged.append(request)
    return merged


def _selection_execution_envelope_requests(
    selection_payload: Mapping[str, Any],
) -> list[Mapping[str, Any]]:
    for key in (
        "yachiyo_execution_envelope",
        "execution_envelope",
        "runtime_execution_envelope",
    ):
        envelope = selection_payload.get(key)
        if not isinstance(envelope, Mapping):
            continue
        requests = envelope.get("requests")
        if not isinstance(requests, list):
            continue
        return [request for request in requests if isinstance(request, Mapping)]
    return []


def _pending_execution_request_start_index(
    requests: list[Mapping[str, Any]],
    observation_tools: list[str],
) -> int:
    cursor = 0
    last_match = -1
    for observed_tool in observation_tools:
        for index in range(cursor, len(requests)):
            tool_name = str(
                requests[index].get("tool_name")
                or requests[index].get("tool")
                or ""
            ).strip()
            if tool_name != observed_tool:
                continue
            last_match = index
            cursor = index + 1
            break
    return last_match + 1 if last_match >= 0 else 0


def _model_followup_execution_request_payload(
    request: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    status = str(request.get("status") or "planned").strip()
    tool_name = str(request.get("tool_name") or request.get("tool") or "").strip()
    request_input = (
        dict(request.get("input"))
        if isinstance(request.get("input"), Mapping)
        else {}
    )
    materialization_blocker = bool(
        status in {"blocked", "waiting_approval"}
        and _tool_request_requires_model_materialization(
            {
                "tool": tool_name,
                "input": request_input,
                "continue_to_model": True,
            }
        )
    )
    if status not in {"", "planned"} and not materialization_blocker:
        return {}
    if not tool_name or (allowed and tool_name not in allowed):
        return {}
    payload: dict[str, Any] = {
        "request_id": str(request.get("request_id") or "").strip(),
        "step_id": str(request.get("step_id") or "").strip(),
        "tool_name": tool_name,
        "capability_id": str(request.get("capability_id") or "").strip(),
        "input_preview": request_input,
        "planning_reason": str(request.get("planning_reason") or "").strip(),
        "status": status or "planned",
    }
    for key in (
        "runtime_doctrine",
        "runtime_stage",
        "runtime_role",
        "source",
        *_RUNTIME_ORCHESTRATION_SCOPE_KEYS,
    ):
        value = str(request.get(key) or "").strip()
        if value:
            payload[key] = value
    for key in (
        "approval_required",
        "continue_to_model",
        "requires_observation",
        "requires_post_action_verification",
    ):
        if bool(request.get(key)):
            payload[key] = True
    for key in (
        "depends_on",
        "fallback_tools",
        "replan_triggers",
        "replan_signal_ids",
    ):
        values = _string_list(request.get(key))
        if values:
            payload[key] = values
    raw_input_bindings = request.get("input_bindings")
    input_bindings = (
        [
            dict(binding)
            for binding in raw_input_bindings
            if isinstance(binding, Mapping)
        ]
        if isinstance(raw_input_bindings, list)
        else []
    )
    if input_bindings:
        payload["input_bindings"] = input_bindings
    action_target = request.get("action_target")
    if isinstance(action_target, Mapping) and action_target:
        payload["action_target"] = dict(action_target)
    task_todo = request.get("task_todo")
    if isinstance(task_todo, Mapping) and task_todo:
        payload["task_todo"] = dict(task_todo)
    for key in (
        "task_checkpoints",
        "task_workspace_items",
        "task_verification_targets",
    ):
        items = [
            dict(item)
            for item in request.get(key, [])
            if isinstance(item, Mapping)
        ]
        if items:
            payload[key] = items
    return {key: value for key, value in payload.items() if value not in ("", [], {})}


def _selection_tool_plan_steps(selection_payload: Mapping[str, Any]) -> list[Mapping[str, Any]]:
    tool_plan = (
        selection_payload.get("tool_plan")
        if isinstance(selection_payload.get("tool_plan"), Mapping)
        else {}
    )
    steps = tool_plan.get("steps") if isinstance(tool_plan, Mapping) else []
    if not isinstance(steps, list):
        return []
    return [step for step in steps if isinstance(step, Mapping)]


def _pending_plan_step_start_index(
    steps: list[Mapping[str, Any]],
    observation_tools: list[str],
) -> int:
    cursor = 0
    last_match = -1
    for observed_tool in observation_tools:
        for index in range(cursor, len(steps)):
            tool_name = str(steps[index].get("tool_name") or "").strip()
            if tool_name != observed_tool:
                continue
            last_match = index
            cursor = index + 1
            break
    return last_match + 1 if last_match >= 0 else 0


def _model_followup_plan_step_payload(step: Mapping[str, Any]) -> dict[str, Any]:
    if str(step.get("status") or "planned").strip() not in {"", "planned"}:
        return {}
    step_id = str(step.get("step_id") or "").strip()
    tool_name = str(step.get("tool_name") or "").strip()
    if not step_id and not tool_name:
        return {}
    payload: dict[str, Any] = {
        "step_id": step_id,
        "title": str(step.get("title") or "").strip(),
        "tool_name": tool_name,
        "capability_id": str(step.get("capability_id") or "").strip(),
        "action": str(step.get("action") or "").strip(),
        "input_preview": (
            dict(step.get("input_preview"))
            if isinstance(step.get("input_preview"), Mapping)
            else {}
        ),
    }
    risk_level = str(step.get("risk_level") or "").strip()
    if risk_level:
        payload["risk_level"] = risk_level
    if bool(step.get("approval_required")):
        payload["approval_required"] = True
    depends_on = _string_list(step.get("depends_on"))
    if depends_on:
        payload["depends_on"] = depends_on
    raw_input_bindings = step.get("input_bindings")
    input_bindings = (
        [
            dict(binding)
            for binding in raw_input_bindings
            if isinstance(binding, Mapping)
        ]
        if isinstance(raw_input_bindings, list)
        else []
    )
    if input_bindings:
        payload["input_bindings"] = input_bindings
    action_target = step.get("action_target")
    if isinstance(action_target, Mapping) and action_target:
        payload["action_target"] = dict(action_target)
    for key in (
        "runtime_doctrine",
        "runtime_stage",
        "runtime_role",
        "requires_observation",
        "requires_post_action_verification",
    ):
        value = step.get(key)
        if value not in (None, "", [], {}):
            payload[key] = value
    return {key: value for key, value in payload.items() if value not in ("", [], {})}


def _model_followup_task_core_step_runtime_trace(
    task_core: Mapping[str, Any],
    step_id: str,
) -> dict[str, Any]:
    clean_step_id = str(step_id or "").strip()
    if not clean_step_id or not isinstance(task_core, Mapping):
        return {}
    for todo in _mapping_list(task_core.get("todos")):
        if str(todo.get("step_id") or "").strip() != clean_step_id:
            continue
        metadata = todo.get("metadata") if isinstance(todo.get("metadata"), Mapping) else {}
        trace = _runtime_trace_metadata_from_mapping(metadata)
        if trace:
            return trace
    for checkpoint in _mapping_list(task_core.get("checkpoints")):
        if str(checkpoint.get("after_step_id") or "").strip() != clean_step_id:
            continue
        payload = checkpoint.get("payload") if isinstance(checkpoint.get("payload"), Mapping) else {}
        trace = _runtime_trace_metadata_from_mapping(payload)
        if trace:
            return trace
    return {}


def _model_replan_followup_context_payload(
    replan_payloads: list[dict[str, Any]],
    *,
    allowed_tools: list[str],
    timeline: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    requests: list[dict[str, Any]] = []
    fallback_candidates: list[str] = []
    failed_tools: list[str] = []
    triggers: list[str] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        metadata = (
            dict(payload.get("metadata"))
            if isinstance(payload.get("metadata"), Mapping)
            else {}
        )
        request = {
            "request_id": str(payload.get("request_id") or "").strip(),
            "trigger": str(payload.get("trigger") or "").strip(),
            "source": str(payload.get("source") or "").strip(),
            "run_id": str(payload.get("run_id") or "").strip(),
            "decision_id": str(payload.get("decision_id") or "").strip(),
            "plan_id": str(payload.get("plan_id") or "").strip(),
            "source_step_id": str(payload.get("source_step_id") or "").strip(),
            "source_tool_name": str(payload.get("source_tool_name") or "").strip(),
            "source_tool_call_id": str(
                payload.get("source_tool_call_id") or ""
            ).strip(),
            "recovery_observation_tool_call_id": str(
                payload.get("recovery_observation_tool_call_id") or ""
            ).strip(),
            "target_capability_id": str(payload.get("target_capability_id") or "").strip(),
            "condition": str(payload.get("condition") or "").strip(),
            "reason": str(payload.get("reason") or "").strip(),
            "failure_event_type": str(payload.get("failure_event_type") or "").strip(),
            "failure_detail": str(payload.get("failure_detail") or "").strip(),
            "fallback_tools": _string_list(payload.get("fallback_tools")),
            "recovery_actions": _mapping_list(metadata.get("recovery_actions")),
            "recommended_tools": _string_list(metadata.get("recommended_tools")),
            "replan_prompt": str(payload.get("replan_prompt") or "").strip(),
            "metadata": metadata,
            "grounded_observation": (
                dict(payload.get("grounded_observation"))
                if isinstance(payload.get("grounded_observation"), Mapping)
                else {}
            ),
            "grounded_observations": [
                dict(item)
                for item in _mapping_list(payload.get("grounded_observations"))
            ],
            **_replan_recovery_target(payload),
        }
        requests.append(request)
        failed_tools.append(request["source_tool_name"])
        fallback_candidates.extend(request["fallback_tools"])
        triggers.append(request["trigger"])
    if not requests:
        return {}
    fallback_candidates = _ordered_text_list(fallback_candidates)
    allowed_fallback_tools = [
        tool for tool in fallback_candidates if not allowed or tool in allowed
    ]
    triggers = _ordered_text_list(triggers)
    first = requests[0]
    payload = {
        "source": "runtime_planner",
        "status": "ready",
        "planning_reason": _runtime_replan_planning_reason(triggers),
        "replan_request_count": len(requests),
        "replan_requests": requests,
        "fallback_tools": allowed_fallback_tools,
        "fallback_tool_candidates": fallback_candidates,
        "failed_tools": _ordered_text_list(failed_tools),
        "triggers": triggers,
        "trigger": first.get("trigger", ""),
        "failure_detail": first.get("failure_detail", ""),
        "source_tool_name": first.get("source_tool_name", ""),
        "target_capability_id": first.get("target_capability_id", ""),
    }
    recovery_targets = _runtime_replan_recovery_targets(requests)
    if recovery_targets:
        payload["recovery_targets"] = recovery_targets
        for key in ("target_app_name", "target_app_query", "target_search_text"):
            value = str(recovery_targets[0].get(key) or "").strip()
            if value:
                payload[key] = value
    recovery_observations = _runtime_replan_recovery_observations(
        requests,
        timeline or [],
    )
    if recovery_observations:
        payload["recovery_observations"] = recovery_observations
        payload["content_snapshots"] = recovery_observations
        payload["content_snapshot"] = recovery_observations[-1]
        payload["recovery_observation_tools"] = _ordered_text_list(
            [
                str(observation.get("source_tool") or "").strip()
                for observation in recovery_observations
                if str(observation.get("source_tool") or "").strip()
            ]
        )
    for key in ("request_id", "decision_id", "plan_id", "core_id", "run_id", "task_id"):
        value = str((replan_payloads[0] if replan_payloads else {}).get(key) or "").strip()
        if value:
            payload[key] = value
    task_progress = _runtime_replan_task_progress_summary(
        replan_payloads,
        timeline or [],
    )
    if task_progress:
        payload["task_progress"] = task_progress
    task_core = _runtime_replan_task_core_payload(replan_payloads, timeline or [])
    if not task_core:
        task_core = _runtime_replan_task_core_context_payload(replan_payloads)
    if task_core:
        payload["task_core"] = task_core
        followup_target = _model_followup_target_from_task_core_context(payload)
        if followup_target:
            normalized_target = _model_followup_target_payload(
                {"followup_target": followup_target},
                allowed,
            )
            payload["followup_target"] = normalized_target or followup_target
    capability_recovery = _runtime_replan_capability_recovery(
        requests,
        allowed_tools=allowed_tools,
    )
    if capability_recovery:
        payload["capability_recovery"] = capability_recovery
    recovery_actions = _runtime_replan_recovery_actions(
        requests,
        allowed_tools=allowed_tools,
    )
    if recovery_actions:
        payload["recovery_actions"] = recovery_actions
    pending_plan_steps = _runtime_replan_pending_code_repair_steps(
        requests,
        allowed_tools=allowed,
    )
    if pending_plan_steps:
        payload["pending_plan_steps"] = pending_plan_steps
    return payload


def _runtime_replan_task_core_context_payload(
    replan_payloads: list[dict[str, Any]],
) -> dict[str, Any]:
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
        context = (
            metadata.get("task_core_context")
            if isinstance(metadata.get("task_core_context"), Mapping)
            else {}
        )
        if not context:
            continue
        workspace_items = _mapping_list(context.get("workspace_items"))
        workspace = {
            "workspace_id": str(context.get("workspace_id") or "").strip(),
            "title": str(context.get("workspace_title") or "").strip(),
            "items": [dict(item) for item in workspace_items],
        }
        return {
            "core_id": str(context.get("core_id") or "").strip(),
            "workspace": {
                key: value
                for key, value in workspace.items()
                if value not in ("", [], {})
            },
            "todos": [dict(todo) for todo in _mapping_list(context.get("todos"))],
            "checkpoints": [
                dict(checkpoint)
                for checkpoint in _mapping_list(context.get("checkpoints"))
            ],
            "replan_signals": [
                dict(signal)
                for signal in _mapping_list(context.get("replan_signals"))
            ],
        }
    return {}


def _runtime_replan_pending_code_repair_steps(
    requests: Iterable[Mapping[str, Any]],
    *,
    allowed_tools: set[str],
) -> list[dict[str, Any]]:
    steps: list[dict[str, Any]] = []
    for request in requests:
        if not _runtime_replan_request_needs_code_repair(request):
            continue
        source_step = str(request.get("source_step_id") or "verification").strip()
        suffix = _runtime_replan_safe_step_suffix(source_step)
        request_id = str(request.get("request_id") or "").strip()
        trigger = str(request.get("trigger") or "").strip()
        repair_step_id = f"repair-after-{suffix}"
        verify_step_id = f"verify-after-{suffix}"
        command = _runtime_replan_verification_command(request)
        common = {
            "replan_request_id": request_id,
            "replan_trigger": trigger,
            "decision_id": str(request.get("decision_id") or "").strip(),
            "plan_id": str(request.get("plan_id") or "").strip(),
            "runtime_doctrine": "discover_operate_verify",
        }
        if "workspace.write_patch" in allowed_tools:
            steps.append(
                {
                    **common,
                    "step_id": repair_step_id,
                    "title": "Repair code after failed verification",
                    "tool_name": "workspace.write_patch",
                    "capability_id": "file.workspace_write",
                    "action": "apply_patch",
                    "input_preview": {
                        "mode": "fix",
                        "patch_source": "model_after_failed_verification",
                        "failure_detail": str(request.get("failure_detail") or "").strip(),
                    },
                    "risk_level": "high",
                    "approval_required": True,
                    "runtime_stage": "operate",
                    "runtime_role": "apply_patch",
                    "requires_observation": False,
                    "requires_post_action_verification": True,
                    "task_todo": {
                        "todo_id": f"todo-{repair_step_id}",
                        "step_id": repair_step_id,
                        "title": "Repair code after failed verification",
                        "status": "pending",
                        "tool_name": "workspace.write_patch",
                        "capability_id": "file.workspace_write",
                        "approval_required": True,
                    },
                }
            )
        if command and "terminal.run" in allowed_tools:
            verify_step: dict[str, Any] = {
                **common,
                "step_id": verify_step_id,
                "title": "Verify repaired code",
                "tool_name": "terminal.run",
                "capability_id": "terminal.execution",
                "action": "run_verification",
                "input_preview": {"command": command},
                "risk_level": "high",
                "approval_required": True,
                "runtime_stage": "verify",
                "runtime_role": "verify_result",
                "requires_observation": True,
                "requires_post_action_verification": False,
                "task_todo": {
                    "todo_id": f"todo-{verify_step_id}",
                    "step_id": verify_step_id,
                    "title": "Verify repaired code",
                    "status": "pending",
                    "tool_name": "terminal.run",
                    "capability_id": "terminal.execution",
                    "approval_required": True,
                },
            }
            if any(step.get("step_id") == repair_step_id for step in steps):
                verify_step["depends_on"] = [repair_step_id]
            steps.append(verify_step)
        if steps:
            break
    return [
        {key: value for key, value in step.items() if value not in ("", [], {})}
        for step in steps
    ]


def _runtime_direct_recovery_root_identity(
    timeline: Sequence[Mapping[str, Any]],
    *,
    run_id: str,
    source_tool_call_id: str,
    source_tool_name: str,
    source_result: Mapping[str, Any],
    source_step_ids: Iterable[str],
    allow_planner_dependency_ancestor: bool = False,
    required_target_capabilities: Iterable[str] = (),
) -> dict[str, str]:
    """Return one exact, provider-attested planner source for a direct retry."""

    clean_run_id = str(run_id or "").strip()
    clean_call_id = str(source_tool_call_id or "").strip()
    clean_tool = str(source_tool_name or "").strip()
    declared_steps = {
        str(step_id or "").strip()
        for step_id in source_step_ids
        if str(step_id or "").strip()
    }
    if (
        not clean_run_id
        or not clean_call_id
        or not clean_tool
        or not declared_steps
        or not _runtime_execution_provider_identity(source_result)
    ):
        return {}
    winner: Mapping[str, Any] | None = None
    winner_index = -1
    for index, raw_event in enumerate(timeline):
        if not isinstance(raw_event, Mapping):
            continue
        payload = raw_event.get("payload")
        event = (
            {**dict(payload), **dict(raw_event)}
            if isinstance(payload, Mapping)
            else dict(raw_event)
        )
        if str(event.get("event") or event.get("event_type") or "").strip() not in {
            "agent.tool.call",
            "agent.tool.failed",
            "agent.tool.skipped",
        }:
            continue
        if str(event.get("run_id") or "").strip() != clean_run_id:
            continue
        if str(event.get("tool_call_id") or "").strip() != clean_call_id:
            continue
        winner = event
        winner_index = index
        break
    if winner is None:
        return {}
    result = winner.get("result")
    plan_id = str(winner.get("plan_id") or "").strip()
    step_id = str(
        winner.get("step_id") or winner.get("planner_step_id") or ""
    ).strip()
    source_is_declared = step_id in declared_steps
    if (
        not source_is_declared
        and allow_planner_dependency_ancestor
        and winner_index >= 0
    ):
        source_is_declared = _runtime_planner_dependency_ancestor_matches(
            timeline[:winner_index],
            run_id=clean_run_id,
            plan_id=plan_id,
            source_step_id=step_id,
            source_tool_name=clean_tool,
            target_step_ids=declared_steps,
            required_target_capabilities=required_target_capabilities,
        )
    if (
        str(winner.get("tool") or winner.get("detail") or "").strip()
        != clean_tool
        or not isinstance(result, Mapping)
        or result != source_result
        or not source_is_declared
        or not _runtime_owned_terminal_event(
            winner,
            result,
            run_id=clean_run_id,
            plan_id=plan_id,
        )
    ):
        return {}
    provider = _runtime_execution_provider_identity(result)
    return {
        "root_plan_id": plan_id,
        "root_source_step_id": step_id,
        "root_provider_kind": provider["provider_kind"],
        "root_provider_id": provider["provider_id"],
    }


_RUNTIME_PLANNER_GRAPH_BOOL_FIELDS = (
    "continue_to_model",
    "requires_observation",
    "requires_post_action_verification",
)

_RUNTIME_PLANNER_GRAPH_AUTHORITY_BOOL_FIELDS = (
    "approved",
    "approval_resume_result_canonical",
    "approved_by_replan_recovery_action",
    "goal_completion_authority",
    "observation_only",
)

_RUNTIME_PLANNER_GRAPH_APPROVAL_TEXT_FIELDS = (
    "approval_id",
    "approval_generation_id",
    "approval_claim_id",
    "approval_request_fingerprint",
    "approval_status",
    "source_approval_id",
    "expected_approval_id",
    "parent_approval_id",
    "approval_source",
    "approval_authority",
    "approval_reason",
    "approval_policy",
    "approval_mode",
    "approval_scope",
)

_RUNTIME_PLANNER_GRAPH_APPROVAL_MAPPING_FIELDS = (
    "approval",
    "approval_context",
    "approval_lineage",
    "approval_metadata",
)

_RUNTIME_PLANNER_ROUTE_AUTHORITY_TEXT_FIELDS = (
    "route_id",
    "tool_name",
    "requested_mode",
    "desktop_execution_session_policy",
    "desktop_session_kind",
    "desktop_backend_kind",
    "fallback_mode",
    "source",
    "delivery_mode",
    "transport",
    "transport_kind",
    "selected_transport",
    "selected_transport_kind",
    "selected_transport_id",
    "mcp_tool",
    "endpoint_origin",
    "endpoint_path",
)

_RUNTIME_PLANNER_ROUTE_AUTHORITY_BOOL_FIELDS = (
    "can_execute",
    "can_auto_start",
    "provider_execution_required",
    "sandbox_required",
    "background_desktop_preferred",
    "isolated_desktop_preferred",
    "foreground_takeover_allowed",
    "user_foreground_takeover_risk",
    "requires_user_foreground_session",
    "foreground_mutation_supported",
    "keyboard_mouse_capture_supported",
    "desktop_session_isolated",
    "foreground_takeover_required",
    "desktop_backend_is_loopback",
    "desktop_backend_ready_for_public_release",
    "requires_real_virtual_desktop_backend",
    "simulated_desktop_provider",
    "background_only",
    "background_execution_required",
    "foreground_execution_required",
)

_RUNTIME_PLANNER_ROUTE_AUTHORITY_MAPPING_FIELDS = (
    "provider_contract",
    "provider_transport",
    "desktop_execution_provider_transport",
    "transport_authority",
)

_RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_FIELDS = (
    "status",
    "provider_readiness_status",
    "reason",
)

_RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_LIST_FIELDS = (
    "blocking_conditions",
    "provider_contract_blocking_conditions",
)

_RUNTIME_PLANNER_CANONICAL_INVALID = object()


def _canonical_runtime_planner_json_value(value: Any) -> Any:
    if value is None or isinstance(value, (bool, int, float, str)):
        return value
    if isinstance(value, Mapping):
        if any(not isinstance(key, str) for key in value):
            return _RUNTIME_PLANNER_CANONICAL_INVALID
        payload: dict[str, Any] = {}
        for key in sorted(value):
            normalized = _canonical_runtime_planner_json_value(value[key])
            if normalized is _RUNTIME_PLANNER_CANONICAL_INVALID:
                return _RUNTIME_PLANNER_CANONICAL_INVALID
            payload[key] = normalized
        return payload
    if isinstance(value, (list, tuple)):
        payload = []
        for item in value:
            normalized = _canonical_runtime_planner_json_value(item)
            if normalized is _RUNTIME_PLANNER_CANONICAL_INVALID:
                return _RUNTIME_PLANNER_CANONICAL_INVALID
            payload.append(normalized)
        return payload
    return _RUNTIME_PLANNER_CANONICAL_INVALID


def _canonical_runtime_planner_desktop_execution_route(
    value: Any,
) -> dict[str, Any] | None:
    if value is None:
        route: Mapping[str, Any] = {}
    elif isinstance(value, Mapping):
        route = value
    else:
        return None

    known_fields = {
        *_RUNTIME_PLANNER_ROUTE_AUTHORITY_TEXT_FIELDS,
        *_RUNTIME_PLANNER_ROUTE_AUTHORITY_BOOL_FIELDS,
        *_RUNTIME_PLANNER_ROUTE_AUTHORITY_MAPPING_FIELDS,
        *_RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_FIELDS,
        *_RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_LIST_FIELDS,
        "selected_provider_kind",
        "selected_provider_id",
        "provider_kind",
        "provider_id",
    }
    if any(not isinstance(key, str) or key not in known_fields for key in route):
        # A new route field may carry provider or foreground authority. Do not
        # silently dedupe it until its immutable/volatile semantics are known.
        return None

    for key in _RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_FIELDS:
        if key in route and not isinstance(route.get(key), str):
            return None
    for key in _RUNTIME_PLANNER_ROUTE_VOLATILE_TEXT_LIST_FIELDS:
        if key not in route:
            continue
        values = route.get(key)
        if not isinstance(values, (list, tuple)) or any(
            not isinstance(item, str) for item in values
        ):
            return None

    canonical: dict[str, Any] = {}
    for key in _RUNTIME_PLANNER_ROUTE_AUTHORITY_TEXT_FIELDS:
        raw = route.get(key)
        if key in route and not isinstance(raw, str):
            return None
        canonical[key] = raw.strip() if isinstance(raw, str) else ""
    for key in _RUNTIME_PLANNER_ROUTE_AUTHORITY_BOOL_FIELDS:
        raw = route.get(key)
        if key in route and raw is not None and not isinstance(raw, bool):
            return None
        canonical[key] = raw if isinstance(raw, bool) else None
    for key in _RUNTIME_PLANNER_ROUTE_AUTHORITY_MAPPING_FIELDS:
        raw = route.get(key)
        if key in route and not isinstance(raw, Mapping):
            return None
        normalized = _canonical_runtime_planner_json_value(raw or {})
        if normalized is _RUNTIME_PLANNER_CANONICAL_INVALID:
            return None
        canonical[key] = normalized

    for canonical_key, aliases in (
        ("selected_provider_kind", ("selected_provider_kind", "provider_kind")),
        ("selected_provider_id", ("selected_provider_id", "provider_id")),
    ):
        values: set[str] = set()
        for key in aliases:
            raw = route.get(key)
            if key in route and not isinstance(raw, str):
                return None
            if isinstance(raw, str) and raw.strip():
                values.add(raw.strip())
        if len(values) > 1:
            return None
        canonical[canonical_key] = next(iter(values), "")
    return canonical


def _canonical_runtime_planner_request_graph(
    requests: Sequence[Mapping[str, Any]],
    *,
    plan_id: str,
    decision_id: str,
) -> list[dict[str, Any]] | None:
    """Canonicalize immutable execution semantics, excluding live overlays."""

    clean_plan_id = str(plan_id or "").strip()
    clean_decision_id = str(decision_id or "").strip()
    if not clean_plan_id or not clean_decision_id or not requests:
        return None
    canonical: list[dict[str, Any]] = []
    seen_step_ids: set[str] = set()
    for request in requests:
        if not isinstance(request, Mapping):
            return None
        step_values = {
            str(request.get(key) or "").strip()
            for key in ("step_id", "planner_step_id")
            if str(request.get(key) or "").strip()
        }
        tool_values = {
            str(request.get(key) or "").strip()
            for key in ("tool_name", "tool")
            if str(request.get(key) or "").strip()
        }
        step_id = next(iter(step_values), "")
        tool_name = next(iter(tool_values), "")
        request_id = str(request.get("request_id") or "").strip()
        request_plan_id = str(request.get("plan_id") or "").strip()
        request_decision_id = str(request.get("decision_id") or "").strip()
        tool_plan_id = str(request.get("tool_plan_id") or "").strip()
        capability_id = str(request.get("capability_id") or "").strip()
        source = str(request.get("source") or "").strip()
        request_input = request.get("input")
        dependencies = request.get("depends_on") or []
        desktop_execution_route = (
            _canonical_runtime_planner_desktop_execution_route(
                request.get("desktop_execution_route")
            )
        )
        approval_required_values: set[bool] = set()
        for key in ("approval_required", "requires_approval"):
            raw = request.get(key)
            if key in request and not isinstance(raw, bool):
                return None
            if isinstance(raw, bool):
                approval_required_values.add(raw)
        if len(approval_required_values) > 1:
            return None
        approval_required = next(iter(approval_required_values), False)
        known_approval_fields = {
            "approval_required",
            "requires_approval",
            "approval_ids",
            *_RUNTIME_PLANNER_GRAPH_AUTHORITY_BOOL_FIELDS,
            *_RUNTIME_PLANNER_GRAPH_APPROVAL_TEXT_FIELDS,
            *_RUNTIME_PLANNER_GRAPH_APPROVAL_MAPPING_FIELDS,
        }
        if (
            len(step_values) != 1
            or len(tool_values) != 1
            or not request_id
            or not tool_plan_id
            or not capability_id
            or request_plan_id != clean_plan_id
            or request_decision_id != clean_decision_id
            or source not in {"runtime_planner", "runtime_verification"}
            or not isinstance(request_input, Mapping)
            or not isinstance(dependencies, (list, tuple))
            or any(not str(value or "").strip() for value in dependencies)
            or step_id in seen_step_ids
            or any(
                key in request and not isinstance(request.get(key), bool)
                for key in _RUNTIME_PLANNER_GRAPH_BOOL_FIELDS
            )
            or desktop_execution_route is None
            or any(
                key in request
                and request.get(key) is not None
                and not isinstance(request.get(key), bool)
                for key in _RUNTIME_PLANNER_GRAPH_AUTHORITY_BOOL_FIELDS
            )
            or any(
                key in request
                and request.get(key) is not None
                and not isinstance(request.get(key), str)
                for key in _RUNTIME_PLANNER_GRAPH_APPROVAL_TEXT_FIELDS
            )
            or any(
                isinstance(key, str)
                and "approval" in key
                and key not in known_approval_fields
                for key in request
            )
        ):
            return None
        approval_ids = request.get("approval_ids") or []
        if not isinstance(approval_ids, (list, tuple)) or any(
            not isinstance(value, str) or not value.strip()
            for value in approval_ids
        ):
            return None
        approval_mappings: dict[str, Any] = {}
        for key in _RUNTIME_PLANNER_GRAPH_APPROVAL_MAPPING_FIELDS:
            raw = request.get(key)
            if key in request and raw is not None and not isinstance(raw, Mapping):
                return None
            normalized = _canonical_runtime_planner_json_value(raw or {})
            if normalized is _RUNTIME_PLANNER_CANONICAL_INVALID:
                return None
            approval_mappings[key] = normalized
        clean_dependencies = sorted(
            {str(value or "").strip() for value in dependencies}
        )
        if step_id in clean_dependencies:
            return None
        seen_step_ids.add(step_id)
        item: dict[str, Any] = {
            "request_id": request_id,
            "decision_id": request_decision_id,
            "plan_id": request_plan_id,
            "tool_plan_id": tool_plan_id,
            "step_id": step_id,
            "tool_name": tool_name,
            "input": dict(request_input),
            "depends_on": clean_dependencies,
            "source": source,
            "capability_id": capability_id,
            "protocol": str(request.get("protocol") or "json_fallback").strip(),
            "planning_reason": str(request.get("planning_reason") or "").strip(),
            "intent_kind": str(request.get("intent_kind") or "").strip(),
            "core_id": str(request.get("core_id") or "").strip(),
            "workspace_id": str(request.get("workspace_id") or "").strip(),
            "runtime_stage": str(request.get("runtime_stage") or "").strip(),
            "runtime_role": str(request.get("runtime_role") or "").strip(),
            "risk_level": str(request.get("risk_level") or "").strip(),
            "policy_reason": str(request.get("policy_reason") or "").strip(),
            "presentation": str(request.get("presentation") or "").strip(),
            "approval_required": approval_required,
            "approval_ids": [value.strip() for value in approval_ids],
            "desktop_execution_route": desktop_execution_route,
        }
        for key in _RUNTIME_PLANNER_GRAPH_BOOL_FIELDS:
            item[key] = request.get(key) is True
        for key in _RUNTIME_PLANNER_GRAPH_AUTHORITY_BOOL_FIELDS:
            value = request.get(key)
            item[key] = value if isinstance(value, bool) else None
        for key in _RUNTIME_PLANNER_GRAPH_APPROVAL_TEXT_FIELDS:
            value = request.get(key)
            item[key] = value.strip() if isinstance(value, str) else ""
        item.update(approval_mappings)
        for key in (
            "fallback_tools",
            "input_bindings",
            "replan_triggers",
            "task_verification_targets",
            "verification_targets",
        ):
            value = request.get(key) or []
            if not isinstance(value, (list, tuple)):
                return None
            item[key] = list(value)
        for key in (
            "action_target",
            "desktop_execution_policy",
            "desktop_loop",
            "observation_evidence",
            "observation_retry",
        ):
            value = request.get(key) or {}
            if not isinstance(value, Mapping):
                return None
            item[key] = dict(value)
        canonical.append(item)

    if any(
        dependency not in seen_step_ids
        for request in canonical
        for dependency in request["depends_on"]
    ):
        return None
    by_step_id = {request["step_id"]: request for request in canonical}
    visiting: set[str] = set()
    visited: set[str] = set()

    def visit(step_id: str) -> bool:
        if step_id in visited:
            return True
        if step_id in visiting:
            return False
        visiting.add(step_id)
        if not all(visit(dependency) for dependency in by_step_id[step_id]["depends_on"]):
            return False
        visiting.remove(step_id)
        visited.add(step_id)
        return True

    if not all(visit(step_id) for step_id in by_step_id):
        return None
    return sorted(canonical, key=lambda request: request["step_id"])


def _runtime_planner_selection_request_graphs(
    timeline: Sequence[Mapping[str, Any]],
    *,
    run_id: str,
    plan_id: str,
) -> dict[str, list[dict[str, Any]]]:
    clean_run_id = str(run_id or "").strip()
    clean_plan_id = str(plan_id or "").strip()
    if not clean_run_id or not clean_plan_id:
        return {}
    matching: dict[str, list[dict[str, Any]]] = {}
    for raw_event in timeline:
        if not isinstance(raw_event, Mapping):
            continue
        payload = (
            raw_event.get("payload")
            if isinstance(raw_event.get("payload"), Mapping)
            else {}
        )
        event = {**dict(payload), **dict(raw_event)}
        if _runtime_planner_base_event_type(
            str(event.get("event") or event.get("event_type") or "").strip()
        ) != "agent.plan.selection":
            continue
        run_ids = {
            str(source.get("run_id") or "").strip()
            for source in (payload, raw_event)
            if str(source.get("run_id") or "").strip()
        }
        if run_ids != {clean_run_id}:
            continue
        selection_sources = {
            str(source.get(key) or "").strip()
            for source in (payload, raw_event)
            for key in ("selected_source", "selection_source", "source")
            if str(source.get(key) or "").strip()
        }
        if not selection_sources or not selection_sources.issubset(
            {"runtime_planner", "runtime_execution_envelope"}
        ):
            continue
        envelope_candidates = [
            source.get(key)
            for source in (payload, raw_event)
            for key in (
                "yachiyo_execution_envelope",
                "execution_envelope",
                "runtime_execution_envelope",
            )
            if isinstance(source.get(key), Mapping)
        ]
        if not envelope_candidates:
            continue
        envelope_keys = {
            json.dumps(
                dict(envelope),
                ensure_ascii=False,
                separators=(",", ":"),
                sort_keys=True,
            )
            for envelope in envelope_candidates
        }
        if len(envelope_keys) != 1:
            continue
        envelope = envelope_candidates[0]
        envelope_id = str(envelope.get("envelope_id") or "").strip()
        envelope_decision_id = str(envelope.get("decision_id") or "").strip()
        envelope_plan_id = str(envelope.get("plan_id") or "").strip()
        envelope_source = str(envelope.get("source") or "").strip()
        event_plan_id = str(event.get("plan_id") or "").strip()
        event_decision_id = str(event.get("decision_id") or "").strip()
        requests = envelope.get("requests")
        if (
            not envelope_id
            or not envelope_decision_id
            or envelope_plan_id != clean_plan_id
            or envelope_source != "runtime_planner"
            or (event_plan_id and event_plan_id != clean_plan_id)
            or (event_decision_id and event_decision_id != envelope_decision_id)
            or not isinstance(requests, list)
            or not requests
            or any(not isinstance(request, Mapping) for request in requests)
        ):
            continue
        graph = _canonical_runtime_planner_request_graph(
            requests,
            plan_id=clean_plan_id,
            decision_id=envelope_decision_id,
        )
        if graph is None:
            # A matching trusted selection snapshot with malformed or unknown
            # execution authority cannot be ignored in favour of an older
            # valid snapshot. The graph proof is now ambiguous, so fail closed.
            return {}
        canonical = json.dumps(
            {
                "envelope_id": envelope_id,
                "decision_id": envelope_decision_id,
                "plan_id": envelope_plan_id,
                "source": envelope_source,
                "requests": graph,
            },
            ensure_ascii=False,
            separators=(",", ":"),
            sort_keys=True,
        )
        matching[canonical] = graph
    return matching


def _runtime_planner_dependency_ancestor_matches(
    timeline: Sequence[Mapping[str, Any]],
    *,
    run_id: str,
    plan_id: str,
    source_step_id: str,
    source_tool_name: str,
    target_step_ids: Iterable[str],
    required_target_capabilities: Iterable[str],
) -> bool:
    """Prove that a failed step is an immutable planner dependency.

    Goal criteria name the step that fulfils the user-visible effect, not
    every prerequisite needed to reach it. An owned-window launch may recover
    only when the Runtime's recorded plan proves that the launch step is an
    ancestor of that exact criterion source step. Public/model text and tool
    results cannot mint this dependency edge.
    """

    clean_run_id = str(run_id or "").strip()
    clean_plan_id = str(plan_id or "").strip()
    clean_source_step_id = str(source_step_id or "").strip()
    clean_source_tool_name = str(source_tool_name or "").strip()
    clean_targets = {
        str(step_id or "").strip()
        for step_id in target_step_ids
        if str(step_id or "").strip()
    }
    target_capabilities = {
        str(capability or "").strip()
        for capability in required_target_capabilities
        if str(capability or "").strip()
    }
    if not all(
        (
            clean_run_id,
            clean_plan_id,
            clean_source_step_id,
            clean_source_tool_name,
            clean_targets,
            target_capabilities,
        )
    ):
        return False

    matching_request_sets = _runtime_planner_selection_request_graphs(
        timeline,
        run_id=clean_run_id,
        plan_id=clean_plan_id,
    )
    if len(matching_request_sets) != 1:
        return False

    requests = next(iter(matching_request_sets.values()))
    by_step_id: dict[str, Mapping[str, Any]] = {}
    for request in requests:
        step_id = str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        tool_name = str(
            request.get("tool_name") or request.get("tool") or ""
        ).strip()
        request_plan_id = str(request.get("plan_id") or "").strip()
        source = str(request.get("source") or "").strip()
        dependencies = request.get("depends_on") or []
        if (
            not step_id
            or not tool_name
            or step_id in by_step_id
            or (request_plan_id and request_plan_id != clean_plan_id)
            or source not in {"runtime_planner", "runtime_verification"}
            or not isinstance(dependencies, (list, tuple))
            or any(not str(value or "").strip() for value in dependencies)
        ):
            return False
        by_step_id[step_id] = request

    source_request = by_step_id.get(clean_source_step_id)
    if not isinstance(source_request, Mapping) or str(
        source_request.get("tool_name") or source_request.get("tool") or ""
    ).strip() != clean_source_tool_name:
        return False

    eligible_targets = {
        target
        for target in clean_targets
        if target in by_step_id
        and target_capabilities.issubset(
            set(
                capability_ids_for_tool(
                    str(
                        by_step_id[target].get("tool_name")
                        or by_step_id[target].get("tool")
                        or ""
                    ).strip()
                )
            )
        )
    }
    if not eligible_targets or clean_source_step_id in eligible_targets:
        return False

    def depends_on_source(step_id: str, visiting: frozenset[str]) -> bool:
        if step_id == clean_source_step_id:
            return True
        if step_id in visiting:
            return False
        request = by_step_id.get(step_id)
        if not isinstance(request, Mapping):
            return False
        dependencies = {
            str(value or "").strip()
            for value in request.get("depends_on") or []
            if str(value or "").strip()
        }
        if any(dependency not in by_step_id for dependency in dependencies):
            return False
        next_visiting = frozenset((*visiting, step_id))
        return any(
            depends_on_source(dependency, next_visiting)
            for dependency in dependencies
        )

    return any(
        depends_on_source(target, frozenset()) for target in eligible_targets
    )


def _runtime_background_window_resume_contract(
    results: Sequence[OutcomeLoopResult],
    planned_requests: Sequence[Mapping[str, Any]],
    *,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> tuple[str, list[dict[str, Any]]]:
    """Return the exact recovered source and its transitive plan descendants."""

    candidates = []
    for result in results:
        recovery = result.recovery_action_result
        plan = result.recovery_plan
        if (
            result.disposition is OutcomeLoopDisposition.CONTINUE_PLAN
            and result.outcome.status is OutcomeStatus.FAILED
            and result.outcome.tool_name == "app.open"
            and plan is not None
            and plan.action == "materialize_background_window"
            and plan.strategy_id == "materialize-background-window"
            and plan.recovery_hint == "materialize_background_window"
            and recovery is not None
            and recovery.disposition is RecoveryActionDisposition.CONTINUE_PLAN
            and recovery.reason == "background_window_materialized"
            and recovery.attempts
        ):
            candidates.append(result)
    if len(candidates) != 1:
        return "", []
    result = candidates[0]
    plan = result.recovery_plan
    assert plan is not None
    source_tool_call_id = str(result.source_tool_call_id or "").strip()
    subgoal_candidates: list[Mapping[str, Any]] = []
    for raw_event in timeline:
        if not isinstance(raw_event, Mapping):
            continue
        payload = (
            raw_event.get("payload")
            if isinstance(raw_event.get("payload"), Mapping)
            else {}
        )
        event = {**dict(payload), **dict(raw_event)}
        subgoal = event.get("subgoal")
        if (
            str(event.get("event") or event.get("event_type") or "").strip()
            == "agent.goal.subgoal.opened"
            and str(event.get("detail") or "").strip()
            == "materialize_background_window"
            and str(event.get("run_id") or "").strip()
            == str(run_id or "").strip()
            and str(event.get("source_tool_call_id") or "").strip()
            == source_tool_call_id
            and str(event.get("source") or "").strip()
            == "runtime_goal_coordinator"
            and str(event.get("actor") or "").strip() == "native_runtime"
            and str(event.get("execution_authority") or "").strip()
            == "runtime_goal_coordinator"
            and str(event.get("visibility") or "").strip() == "internal"
            and str(event.get("status") or "").strip() == "opened"
            and isinstance(subgoal, Mapping)
            and str(subgoal.get("action") or "").strip()
            == "materialize_background_window"
            and str(subgoal.get("source_tool_call_id") or "").strip()
            == source_tool_call_id
            and str(subgoal.get("contract_id") or "").strip()
            == str(event.get("contract_id") or "").strip()
            and str(subgoal.get("criterion_id") or "").strip()
            == str(event.get("criterion_id") or "").strip()
            and str(subgoal.get("subgoal_id") or "").strip()
        ):
            subgoal_candidates.append(event)
    if len(subgoal_candidates) != 1:
        return "", []
    goal_identity = subgoal_candidates[0]
    subgoal = goal_identity["subgoal"]
    plan_id = str(goal_identity.get("root_plan_id") or "").strip()
    source_step_id = str(
        goal_identity.get("root_source_step_id") or ""
    ).strip()
    if not all(
        (
            plan_id,
            source_step_id,
            str(goal_identity.get("root_provider_kind") or "").strip(),
            str(goal_identity.get("root_provider_id") or "").strip(),
        )
    ):
        return "", []
    completed_recovery_events = [
        event
        for raw_event in timeline
        if isinstance(raw_event, Mapping)
        for event in (
            {
                **(
                    dict(raw_event.get("payload"))
                    if isinstance(raw_event.get("payload"), Mapping)
                    else {}
                ),
                **dict(raw_event),
            },
        )
        if str(event.get("event") or event.get("event_type") or "").strip()
        == "agent.recovery.completed"
        and str(event.get("source_tool_call_id") or "").strip()
        == source_tool_call_id
        and str(event.get("scope_id") or "").strip() == plan.scope_id
        and str(event.get("strategy_id") or "").strip() == plan.strategy_id
        and str(event.get("action") or "").strip() == plan.action
        and str(event.get("goal_contract_id") or "").strip()
        == str(subgoal.get("contract_id") or "").strip()
        and str(event.get("goal_criterion_id") or "").strip()
        == str(subgoal.get("criterion_id") or "").strip()
        and str(event.get("goal_subgoal_id") or "").strip()
        == str(subgoal.get("subgoal_id") or "").strip()
        and event.get("root_goal_unchanged") is True
        and str(event.get("recovery_owner") or "").strip() == "coordinator"
        and str(event.get("visibility") or "").strip() == "internal"
        and str(event.get("status") or "").strip() == "completed"
        and str(event.get("result_disposition") or "").strip()
        == RecoveryActionDisposition.CONTINUE_PLAN.value
        and str(event.get("result_reason") or "").strip()
        == "background_window_materialized"
        and int(event.get("attempt_count") or 0) == len(recovery.attempts)
    ]
    if len(completed_recovery_events) != 1:
        return "", []
    source_match = _runtime_recovery_executor_terminal_event(
        timeline,
        run_id=str(run_id or "").strip(),
        tool_call_id=source_tool_call_id,
        tool_name="app.open",
    )
    if source_match is None:
        return "", []
    _source_index, source_event = source_match
    source_result = source_event.get("result")
    if (
        not isinstance(source_result, Mapping)
        or source_result != result.outcome.raw
        or str(source_event.get("plan_id") or "").strip() != plan_id
        or str(source_event.get("step_id") or "").strip() != source_step_id
        or str(source_event.get("source") or "").strip() != "runtime_planner"
    ):
        return "", []

    source_request_id = str(source_event.get("request_id") or "").strip()
    source_candidates = [
        (index, request)
        for index, request in enumerate(planned_requests)
        if isinstance(request, Mapping)
        and str(request.get("request_id") or "").strip() == source_request_id
        and str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        == source_step_id
        and str(request.get("tool") or request.get("tool_name") or "").strip()
        == "app.open"
        and str(request.get("plan_id") or "").strip() == plan_id
        and str(request.get("source") or "").strip() == "runtime_planner"
    ]
    if len(source_candidates) != 1:
        return "", []
    source_request_index, source_request = source_candidates[0]
    decision_id = str(source_request.get("decision_id") or "").strip()
    planned_graph = _canonical_runtime_planner_request_graph(
        planned_requests,
        plan_id=plan_id,
        decision_id=decision_id,
    )
    selection_graphs = _runtime_planner_selection_request_graphs(
        timeline,
        run_id=str(run_id or "").strip(),
        plan_id=plan_id,
    )
    if planned_graph is None or len(selection_graphs) != 1:
        return "", []
    selection_graph = next(iter(selection_graphs.values()))
    if json.dumps(
        planned_graph,
        ensure_ascii=False,
        separators=(",", ":"),
        sort_keys=True,
    ) != json.dumps(
        selection_graph,
        ensure_ascii=False,
        separators=(",", ":"),
        sort_keys=True,
    ):
        return "", []

    by_step_id = {request["step_id"]: request for request in selection_graph}
    descendant_step_ids = {source_step_id}
    changed = True
    while changed:
        changed = False
        for step_id, request in by_step_id.items():
            if step_id in descendant_step_ids:
                continue
            if descendant_step_ids.intersection(request["depends_on"]):
                descendant_step_ids.add(step_id)
                changed = True
    descendant_step_ids.discard(source_step_id)
    if not descendant_step_ids:
        return "", []
    request_indexes = {
        str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip(): index
        for index, request in enumerate(planned_requests)
        if isinstance(request, Mapping)
    }
    if (
        set(request_indexes) != set(by_step_id)
        or any(
            request_indexes[dependency] >= request_indexes[step_id]
            for step_id, request in by_step_id.items()
            for dependency in request["depends_on"]
        )
        or any(
            request_indexes[step_id] <= source_request_index
            for step_id in descendant_step_ids
        )
        or any(
            dependency not in descendant_step_ids
            and request_indexes[dependency] > source_request_index
            for step_id in descendant_step_ids
            for dependency in by_step_id[step_id]["depends_on"]
        )
    ):
        return "", []
    resume_requests = [
        deepcopy(dict(request))
        for request in planned_requests[source_request_index + 1 :]
        if str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        in descendant_step_ids
    ]
    if len(resume_requests) != len(descendant_step_ids):
        return "", []
    return source_tool_call_id, resume_requests


def _runtime_terminal_winners_for_run(
    timeline: Sequence[Mapping[str, Any]],
    *,
    run_id: str,
) -> list[tuple[int, dict[str, Any]]]:
    winners: list[tuple[int, dict[str, Any]]] = []
    seen_call_ids: set[str] = set()
    clean_run_id = str(run_id or "").strip()
    if not clean_run_id:
        return winners
    for index, raw_event in enumerate(timeline):
        if not isinstance(raw_event, Mapping):
            continue
        payload = raw_event.get("payload")
        event = (
            {**dict(payload), **dict(raw_event)}
            if isinstance(payload, Mapping)
            else dict(raw_event)
        )
        if str(event.get("event") or event.get("event_type") or "").strip() not in {
            "agent.tool.call",
            "agent.tool.failed",
            "agent.tool.skipped",
        }:
            continue
        if str(event.get("run_id") or "").strip() != clean_run_id:
            continue
        tool_call_id = str(event.get("tool_call_id") or "").strip()
        if not tool_call_id or tool_call_id in seen_call_ids:
            continue
        seen_call_ids.add(tool_call_id)
        winners.append((index, event))
    return winners


def _runtime_goal_verification_recovery_lineage(
    followup_context: Mapping[str, Any],
    *,
    contract: GoalContract,
    timeline: list[dict[str, Any]],
    run_id: str,
) -> dict[str, str]:
    if str(followup_context.get("planning_reason") or "").strip() != (
        "planner_replan_after_verification_failed"
    ):
        return {}
    replan_requests = _mapping_list(followup_context.get("replan_requests"))
    candidates = [
        request
        for request in replan_requests
        if str(request.get("trigger") or "").strip() == "verification_failed"
    ]
    if len(candidates) != 1:
        return {}
    replan_request = candidates[0]
    root_verifier_step_id = str(
        replan_request.get("source_step_id") or ""
    ).strip()
    failed_verifier_tool = str(
        replan_request.get("source_tool_name") or ""
    ).strip()
    root_plan_id = str(
        replan_request.get("plan_id")
        or followup_context.get("plan_id")
        or ""
    ).strip()
    replan_request_id = str(replan_request.get("request_id") or "").strip()
    matching_criteria = [
        criterion
        for criterion in contract.criteria
        if root_verifier_step_id in criterion.verifier_step_ids
        and criterion.criterion_id
        in runtime_goal_assessment(contract, timeline).unsatisfied_criterion_ids
    ]
    if (
        len(matching_criteria) != 1
        or not root_verifier_step_id
        or not failed_verifier_tool
        or not root_plan_id
        or not replan_request_id
        or str(replan_request.get("run_id") or "").strip() != run_id
        or str(replan_request.get("plan_id") or "").strip() != root_plan_id
        or str(replan_request.get("source") or "").strip()
        != "runtime_tool_request_runner"
    ):
        return {}
    criterion = matching_criteria[0]
    failed_verifier_event: Mapping[str, Any] | None = None
    failed_index = -1
    winners = _runtime_terminal_winners_for_run(timeline, run_id=run_id)
    for index, event in reversed(winners):
        if str(event.get("detail") or event.get("tool") or "").strip() != (
            failed_verifier_tool
        ):
            continue
        if str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip() != root_verifier_step_id:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not False or not _runtime_owned_terminal_event(
            event,
            result,
            run_id=run_id,
            plan_id=root_plan_id,
        ):
            continue
        tool_call_id = str(event.get("tool_call_id") or "").strip()
        if not tool_call_id:
            continue
        failed_verifier_event = event
        failed_index = index
        break
    if failed_verifier_event is None:
        return {}
    root_source_candidates: list[Mapping[str, Any]] = []
    for index, event in winners:
        if index >= failed_index:
            break
        source_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if source_step_id not in criterion.source_step_ids:
            continue
        tool_name = str(event.get("detail") or event.get("tool") or "").strip()
        if not set(criterion.required_capabilities).issubset(
            set(capability_ids_for_tool(tool_name))
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            result.get("ok") is not True
            or not str(event.get("tool_call_id") or "").strip()
            or not _runtime_owned_terminal_event(
                event,
                result,
                run_id=run_id,
                plan_id=root_plan_id,
            )
        ):
            continue
        root_source_candidates.append(event)
    if not root_source_candidates:
        return {}
    root_source_event = root_source_candidates[-1]
    failed_result = (
        failed_verifier_event.get("result")
        if isinstance(failed_verifier_event.get("result"), Mapping)
        else {}
    )
    root_result = (
        root_source_event.get("result")
        if isinstance(root_source_event.get("result"), Mapping)
        else {}
    )
    failed_provider = _runtime_execution_provider_identity(failed_result)
    root_provider = _runtime_execution_provider_identity(root_result)
    if not root_provider or failed_provider != root_provider:
        return {}
    root_source_step_id = str(
        root_source_event.get("step_id")
        or root_source_event.get("planner_step_id")
        or ""
    ).strip()
    return {
        "criterion_id": criterion.criterion_id,
        "root_source_tool_call_id": str(
            root_source_event.get("tool_call_id") or ""
        ).strip(),
        "root_source_step_id": root_source_step_id,
        "root_verifier_step_id": root_verifier_step_id,
        "root_plan_id": root_plan_id,
        "root_provider_kind": root_provider["provider_kind"],
        "root_provider_id": root_provider["provider_id"],
        "failed_verifier_tool_call_id": str(
            failed_verifier_event.get("tool_call_id") or ""
        ).strip(),
        "failed_verifier_tool": failed_verifier_tool,
        "replan_request_id": replan_request_id,
        "recovery_action": "repair_after_failed_verification",
    }


def _request_matches_runtime_goal_recovery_lineage(
    request: Mapping[str, Any],
    lineage: Mapping[str, str],
) -> bool:
    return bool(
        str(request.get("replan_trigger") or "").strip()
        == "verification_failed"
        and str(request.get("replan_request_id") or "").strip()
        == str(lineage.get("replan_request_id") or "").strip()
        and str(request.get("plan_id") or "").strip()
        == str(lineage.get("root_plan_id") or "").strip()
        and str(request.get("tool") or request.get("tool_name") or "").strip()
    )


def _runtime_replan_request_needs_code_repair(request: Mapping[str, Any]) -> bool:
    if str(request.get("trigger") or "").strip() != "verification_failed":
        return False
    source_tool = str(request.get("source_tool_name") or "").strip()
    capability_id = str(request.get("target_capability_id") or "").strip()
    metadata = request.get("metadata") if isinstance(request.get("metadata"), Mapping) else {}
    intent_kind = str(metadata.get("original_intent_kind") or "").strip()
    fallback_tools = set(_string_list(request.get("fallback_tools")))
    return bool(
        intent_kind == "code_task"
        or source_tool == "terminal.run"
        or capability_id == "terminal.execution"
        or "workspace.write_patch" in fallback_tools
    )


def _runtime_replan_verification_command(request: Mapping[str, Any]) -> str:
    metadata = request.get("metadata") if isinstance(request.get("metadata"), Mapping) else {}
    input_preview = (
        metadata.get("input_preview")
        if isinstance(metadata.get("input_preview"), Mapping)
        else {}
    )
    command = str(input_preview.get("command") or "").strip()
    if command:
        return command
    result_preview = (
        metadata.get("result_preview")
        if isinstance(metadata.get("result_preview"), Mapping)
        else {}
    )
    return str(result_preview.get("command") or "").strip()


def _runtime_replan_safe_step_suffix(value: str) -> str:
    clean = re.sub(r"[^a-zA-Z0-9_-]+", "-", str(value or "").strip()).strip("-")
    return clean[:60] or "verification"


def _timeline_replan_request_payloads(
    timeline: list[dict[str, Any]],
    *,
    start: int = 0,
) -> list[dict[str, Any]]:
    payloads: list[dict[str, Any]] = []
    for event in list(timeline[max(0, int(start or 0)):]):
        if not isinstance(event, Mapping):
            continue
        if (
            _runtime_replan_event_type(
                str(event.get("event") or event.get("event_type") or "").strip()
            )
            != "agent.replan.requested"
        ):
            continue
        payload = event.get("payload")
        if isinstance(payload, Mapping):
            source = str(payload.get("source") or "").strip()
            if source not in _RUNTIME_REPLAN_INGESTION_SOURCES:
                continue
            if (
                source == _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
                and not _runtime_recovery_continuation_payload_is_trusted(
                    payload,
                    timeline=timeline,
                )
            ):
                continue
            payloads.append(copy_live_replan_payload(payload))
    return payloads


def _runtime_recovery_continuation_replan_payload(
    result: OutcomeLoopResult,
    *,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> dict[str, Any]:
    """Build one replan request from an executor-grounded recovery batch."""

    recovery = result.recovery_action_result
    clean_run_id = str(run_id or "").strip()
    source_tool_call_id = str(result.source_tool_call_id or "").strip()
    if (
        not clean_run_id
        or not source_tool_call_id
        or result.disposition is not OutcomeLoopDisposition.CONTINUE_PLAN
        or result.outcome.status is OutcomeStatus.SUCCESS
        or recovery is None
        or recovery.disposition is not RecoveryActionDisposition.CONTINUE_PLAN
        or not recovery.attempts
    ):
        return {}
    source_match = _runtime_recovery_executor_terminal_event(
        timeline,
        run_id=clean_run_id,
        tool_call_id=source_tool_call_id,
        tool_name=result.outcome.tool_name,
    )
    if source_match is None:
        return {}
    source_index, source_event = source_match
    source_result = source_event.get("result")
    if not isinstance(source_result, Mapping) or source_result != result.outcome.raw:
        return {}

    plan = result.recovery_plan
    plan_metadata = (
        plan.metadata
        if plan is not None and isinstance(plan.metadata, Mapping)
        else {}
    )
    source_goal_contract_id = str(
        source_event.get("goal_contract_id") or ""
    ).strip()
    source_goal_criterion_id = str(
        source_event.get("goal_criterion_id") or ""
    ).strip()
    if (
        not source_goal_contract_id
        or not source_goal_criterion_id
        or (
            str(plan_metadata.get("goal_contract_id") or "").strip()
            and str(plan_metadata.get("goal_contract_id") or "").strip()
            != source_goal_contract_id
        )
        or (
            str(plan_metadata.get("goal_criterion_id") or "").strip()
            and str(plan_metadata.get("goal_criterion_id") or "").strip()
            != source_goal_criterion_id
        )
    ):
        # A continuation may refine only the target of the already-bound
        # root criterion.  Recovery-plan metadata is advisory unless it
        # exactly agrees with the executor-owned source receipt.
        return {}
    recovery_action = str(getattr(plan, "action", "") or "").strip()
    recovery_scope_id = str(getattr(plan, "scope_id", "") or "").strip()
    observations: list[dict[str, Any]] = []
    observation_identities: list[tuple[str, str, str]] = []
    seen_observation_call_ids: set[str] = set()
    for batch in recovery.attempts:
        for request in batch.requests:
            if not isinstance(request, Mapping):
                continue
            observation_tool = str(request.get("tool") or "").strip()
            observation_call_id = str(request.get("tool_call_id") or "").strip()
            if (
                not observation_tool
                or not observation_call_id
                or observation_call_id in seen_observation_call_ids
                or str(request.get("source") or "").strip()
                != "runtime_internal_recovery"
                or str(request.get("source_tool_call_id") or "").strip()
                != source_tool_call_id
                or str(request.get("recovery_link_kind") or "").strip()
                != "coordinator_action"
                or (
                    recovery_action
                    and str(request.get("recovery_action") or "").strip()
                    != recovery_action
                )
                or (
                    recovery_scope_id
                    and str(request.get("recovery_scope_id") or "").strip()
                    != recovery_scope_id
                )
            ):
                continue
            batch_result = batch.tool_result_for(observation_call_id)
            if (
                batch_result is None
                or batch_result.event_type != "agent.tool.call"
                or not isinstance(batch_result.result, Mapping)
                or batch_result.result.get("ok") is not True
            ):
                continue
            observation_match = _runtime_recovery_executor_terminal_event(
                timeline,
                run_id=clean_run_id,
                tool_call_id=observation_call_id,
                tool_name=observation_tool,
            )
            if observation_match is None:
                continue
            observation_index, observation_event = observation_match
            if observation_index <= source_index:
                continue
            observed_result = observation_event.get("result")
            if (
                not isinstance(observed_result, Mapping)
                or observed_result != batch_result.result
                or str(observation_event.get("source") or "").strip()
                != "runtime_internal_recovery"
                or str(
                    observation_event.get("source_tool_call_id") or ""
                ).strip()
                != source_tool_call_id
                or str(
                    observation_event.get("recovery_link_kind") or ""
                ).strip()
                != "coordinator_action"
                or (
                    recovery_action
                    and str(
                        observation_event.get("recovery_action") or ""
                    ).strip()
                    != recovery_action
                )
            ):
                continue
            request_input = (
                request.get("input")
                if isinstance(request.get("input"), Mapping)
                else {}
            )
            observed_input = (
                observation_event.get("input_preview")
                if isinstance(observation_event.get("input_preview"), Mapping)
                else {}
            )
            if dict(observed_input) != dict(request_input):
                continue
            snapshot = _runtime_grounded_recovery_observation(
                observation_event,
                source_tool_call_id=source_tool_call_id,
                recovery_action=recovery_action,
            )
            if not snapshot:
                continue
            seen_observation_call_ids.add(observation_call_id)
            observations.append(snapshot)
            observation_identities.append(
                (
                    observation_call_id,
                    observation_tool,
                    str(observation_event.get("request_id") or "").strip(),
                )
            )
    if not observations:
        return {}

    request_id = _runtime_recovery_continuation_request_id(
        run_id=clean_run_id,
        source_tool_call_id=source_tool_call_id,
        recovery_action=recovery_action,
        observation_identities=observation_identities,
    )
    source_provider = _runtime_execution_provider_identity(
        source_result,
        expected_tool=result.outcome.tool_name,
    )
    last_observation = observations[-1]
    metadata = {
        "source_tool_call_id": source_tool_call_id,
        "source_request_id": str(source_event.get("request_id") or "").strip(),
        "source_plan_id": str(source_event.get("plan_id") or "").strip(),
        "source_step_id": str(
            source_event.get("step_id")
            or source_event.get("planner_step_id")
            or ""
        ).strip(),
        "source_provider_kind": str(source_provider.get("provider_kind") or ""),
        "source_provider_id": str(source_provider.get("provider_id") or ""),
        "recovery_action": recovery_action,
        "recovery_scope_id": recovery_scope_id,
        "recovery_observation_tool_call_id": str(
            last_observation.get("source_tool_call_id") or ""
        ),
        "recovery_observation_tool": str(
            last_observation.get("source_tool") or ""
        ),
    }
    payload: dict[str, Any] = {
        "request_id": request_id,
        "trigger": "recovery_observation_ready",
        "status": "requested",
        "source": _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE,
        "run_id": clean_run_id,
        "plan_id": str(source_event.get("plan_id") or "").strip(),
        "source_step_id": str(
            source_event.get("step_id")
            or source_event.get("planner_step_id")
            or ""
        ).strip(),
        "source_tool_name": result.outcome.tool_name,
        "source_tool_call_id": source_tool_call_id,
        "goal_contract_id": source_goal_contract_id,
        "goal_criterion_id": source_goal_criterion_id,
        "source_outcome_status": result.outcome.status.value,
        "source_outcome_reason": result.outcome.reason,
        "target_capability_id": str(
            (plan.required_capabilities[0] if plan and plan.required_capabilities else "")
        ).strip(),
        "condition": "grounded_recovery_observation_available",
        "reason": recovery.reason or "recovery_observation_ready",
        "failure_detail": result.outcome.reason,
        "recovery_action": recovery_action,
        "recovery_scope_id": recovery_scope_id,
        "recovery_observation_tool_call_id": str(
            last_observation.get("source_tool_call_id") or ""
        ),
        "grounded_observation": last_observation,
        "grounded_observations": observations,
        "metadata": {
            key: value for key, value in metadata.items() if value not in (None, "")
        },
    }
    if plan is not None and isinstance(plan.metadata, Mapping):
        for key in (
            "goal_subgoal_id",
            "root_plan_id",
            "root_source_step_id",
        ):
            value = str(plan.metadata.get(key) or "").strip()
            if value:
                payload[key] = value
    compact = {
        key: value
        for key, value in payload.items()
        if value not in (None, "", [], {})
    }
    if not _runtime_recovery_continuation_payload_is_trusted(
        compact,
        timeline=timeline,
    ):
        return {}
    return compact


def _runtime_recovery_continuation_payload_is_trusted(
    payload: Mapping[str, Any],
    *,
    timeline: Sequence[Mapping[str, Any]],
) -> bool:
    """Replay a persisted continuation against both executor-owned calls."""

    if str(payload.get("source") or "").strip() != (
        _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
    ):
        return False
    run_id = str(payload.get("run_id") or "").strip()
    source_tool_call_id = str(payload.get("source_tool_call_id") or "").strip()
    source_tool = str(payload.get("source_tool_name") or "").strip()
    goal_contract_id = str(payload.get("goal_contract_id") or "").strip()
    goal_criterion_id = str(payload.get("goal_criterion_id") or "").strip()
    recovery_action = str(payload.get("recovery_action") or "").strip()
    recovery_scope_id = str(payload.get("recovery_scope_id") or "").strip()
    metadata = (
        payload.get("metadata")
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    if (
        _string_list(payload.get("fallback_tools"))
        or _mapping_list(payload.get("recovery_actions"))
        or _mapping_list(metadata.get("recovery_actions"))
        or _string_list(payload.get("recommended_tools"))
        or str(payload.get("replan_prompt") or "").strip()
        or payload.get(RUNTIME_PRIVATE_REPLAN_CONTEXT_KEY) is not None
    ):
        # This source reports an already-completed observation and grants no
        # auto-execution authority.  A persisted payload cannot be upgraded
        # into a fallback plan by adding public recovery fields.
        return False
    observations = [
        dict(item)
        for item in _mapping_list(payload.get("grounded_observations"))
    ]
    grounded_observation = payload.get("grounded_observation")
    if (
        not run_id
        or not source_tool_call_id
        or not source_tool
        or not goal_contract_id
        or not goal_criterion_id
        or not recovery_action
        or not recovery_scope_id
        or not observations
        or not isinstance(grounded_observation, Mapping)
        or dict(grounded_observation) != observations[-1]
    ):
        return False
    source_match = _runtime_recovery_executor_terminal_event(
        timeline,
        run_id=run_id,
        tool_call_id=source_tool_call_id,
        tool_name=source_tool,
    )
    if source_match is None:
        return False
    source_index, source_event = source_match
    source_result = source_event.get("result")
    if not isinstance(source_result, Mapping):
        return False
    source_step_id = str(
        source_event.get("step_id")
        or source_event.get("planner_step_id")
        or ""
    ).strip()
    source_plan_id = str(source_event.get("plan_id") or "").strip()
    source_request_id = str(source_event.get("request_id") or "").strip()
    source_provider = _runtime_execution_provider_identity(
        source_result,
        expected_tool=source_tool,
    )
    if (
        str(source_event.get("goal_contract_id") or "").strip()
        != goal_contract_id
        or str(source_event.get("goal_criterion_id") or "").strip()
        != goal_criterion_id
        or not source_step_id
        or not source_plan_id
        or not source_request_id
        or str(payload.get("plan_id") or "").strip() != source_plan_id
        or str(payload.get("source_step_id") or "").strip() != source_step_id
        or str(metadata.get("source_tool_call_id") or "").strip()
        != source_tool_call_id
        or str(metadata.get("source_request_id") or "").strip()
        != source_request_id
        or str(metadata.get("source_plan_id") or "").strip()
        != source_plan_id
        or str(metadata.get("source_step_id") or "").strip()
        != source_step_id
        or str(metadata.get("source_provider_kind") or "").strip()
        != str(source_provider.get("provider_kind") or "").strip()
        or str(metadata.get("source_provider_id") or "").strip()
        != str(source_provider.get("provider_id") or "").strip()
        or str(metadata.get("recovery_action") or "").strip()
        != recovery_action
        or str(metadata.get("recovery_scope_id") or "").strip()
        != recovery_scope_id
    ):
        return False
    normalized_source = from_tool_result(
        source_tool,
        source_result,
        capability_ids_for_tool(source_tool),
    )
    if (
        normalized_source.status is OutcomeStatus.SUCCESS
        or normalized_source.status.value
        != str(payload.get("source_outcome_status") or "").strip()
        or normalized_source.reason
        != str(payload.get("source_outcome_reason") or "").strip()
    ):
        return False

    identities: list[tuple[str, str, str]] = []
    seen_call_ids: set[str] = set()
    for observation in observations:
        observation_call_id = str(
            observation.get("source_tool_call_id") or ""
        ).strip()
        observation_tool = str(observation.get("source_tool") or "").strip()
        if (
            not observation_call_id
            or not observation_tool
            or observation_tool
            not in _RUNTIME_REPLAN_GROUNDING_OBSERVATION_TOOLS
            or observation_call_id in seen_call_ids
            or str(observation.get("recovery_source_tool_call_id") or "").strip()
            != source_tool_call_id
            or str(observation.get("grounded_by") or "").strip()
            != _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
        ):
            return False
        observation_match = _runtime_recovery_executor_terminal_event(
            timeline,
            run_id=run_id,
            tool_call_id=observation_call_id,
            tool_name=observation_tool,
        )
        if observation_match is None:
            return False
        observation_index, observation_event = observation_match
        if (
            observation_index <= source_index
            or str(observation_event.get("source") or "").strip()
            != "runtime_internal_recovery"
            or str(observation_event.get("source_tool_call_id") or "").strip()
            != source_tool_call_id
            or str(observation_event.get("recovery_link_kind") or "").strip()
            != "coordinator_action"
            or str(observation_event.get("recovery_action") or "").strip()
            != recovery_action
            or str(observation_event.get("recovery_scope_id") or "").strip()
            != recovery_scope_id
            or str(observation_event.get("goal_contract_id") or "").strip()
            != goal_contract_id
            or str(observation_event.get("goal_criterion_id") or "").strip()
            != goal_criterion_id
            or _runtime_grounded_recovery_observation(
                observation_event,
                source_tool_call_id=source_tool_call_id,
                recovery_action=recovery_action,
            )
            != observation
        ):
            return False
        seen_call_ids.add(observation_call_id)
        identities.append(
            (
                observation_call_id,
                observation_tool,
                str(observation_event.get("request_id") or "").strip(),
            )
        )
    expected_request_id = _runtime_recovery_continuation_request_id(
        run_id=run_id,
        source_tool_call_id=source_tool_call_id,
        recovery_action=recovery_action,
        observation_identities=identities,
    )
    return str(payload.get("request_id") or "").strip() == expected_request_id


def _runtime_recovery_executor_terminal_event(
    timeline: Sequence[Mapping[str, Any]],
    *,
    run_id: str,
    tool_call_id: str,
    tool_name: str,
) -> tuple[int, dict[str, Any]] | None:
    """Return the first provider-attested terminal owned by Runtime."""

    expected_run_id = str(run_id or "").strip()
    expected_call_id = str(tool_call_id or "").strip()
    expected_tool = str(tool_name or "").strip()
    if not expected_run_id or not expected_call_id or not expected_tool:
        return None
    for index, raw_event in enumerate(timeline):
        if not isinstance(raw_event, Mapping):
            continue
        nested = (
            raw_event.get("payload")
            if isinstance(raw_event.get("payload"), Mapping)
            else {}
        )
        event = {**dict(nested), **dict(raw_event)}
        event_types = {
            str(source.get(key) or "").strip()
            for source in (raw_event, nested)
            for key in ("event", "event_type")
            if str(source.get(key) or "").strip()
        }
        run_ids = {
            str(source.get("run_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("run_id") or "").strip()
        }
        actors = {
            str(source.get("actor") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("actor") or "").strip()
        }
        authorities = {
            str(source.get("execution_authority") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("execution_authority") or "").strip()
        }
        call_ids = {
            str(source.get("tool_call_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("tool_call_id") or "").strip()
        }
        request_ids = {
            str(source.get("request_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("request_id") or "").strip()
        }
        plan_ids = {
            str(source.get("plan_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("plan_id") or "").strip()
        }
        step_ids = {
            str(source.get(key) or "").strip()
            for source in (raw_event, nested)
            for key in ("step_id", "planner_step_id")
            if str(source.get(key) or "").strip()
        }
        tool_names = {
            str(source.get(key) or "").strip()
            for source in (raw_event, nested)
            for key in ("tool", "detail")
            if str(source.get(key) or "").strip()
        }
        goal_contract_ids = {
            str(source.get("goal_contract_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("goal_contract_id") or "").strip()
        }
        goal_criterion_ids = {
            str(source.get("goal_criterion_id") or "").strip()
            for source in (raw_event, nested)
            if str(source.get("goal_criterion_id") or "").strip()
        }
        result = event.get("result")
        if (
            len(event_types) != 1
            or not event_types.issubset(
                {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
            )
            or run_ids != {expected_run_id}
            or actors != {"native_runtime"}
            or authorities != {"runtime_tool_executor"}
            or call_ids != {expected_call_id}
            or len(request_ids) > 1
            or len(plan_ids) > 1
            or len(step_ids) > 1
            or tool_names != {expected_tool}
            or len(goal_contract_ids) > 1
            or len(goal_criterion_ids) > 1
            or not isinstance(result, Mapping)
            or not _runtime_execution_provider_identity(
                result,
                expected_tool=expected_tool,
            )
        ):
            continue
        return index, event
    return None


def _runtime_grounded_recovery_observation(
    event: Mapping[str, Any],
    *,
    source_tool_call_id: str,
    recovery_action: str,
) -> dict[str, Any]:
    result = event.get("result")
    if not isinstance(result, Mapping) or result.get("ok") is not True:
        return {}
    tool_name = str(event.get("tool") or event.get("detail") or "").strip()
    input_preview = (
        event.get("input_preview")
        if isinstance(event.get("input_preview"), Mapping)
        else {}
    )
    snapshot = followup_content_snapshot_for_tool_call(
        tool_name,
        dict(result),
        dict(input_preview),
    )
    observation_call_id = str(event.get("tool_call_id") or "").strip()
    if not snapshot or not observation_call_id:
        return {}
    return {
        **snapshot,
        "source_tool_call_id": observation_call_id,
        "recovery_source_tool_call_id": str(source_tool_call_id or "").strip(),
        "recovery_action": str(recovery_action or "").strip(),
        "grounded_by": _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE,
    }


def _runtime_recovery_continuation_request_id(
    *,
    run_id: str,
    source_tool_call_id: str,
    recovery_action: str,
    observation_identities: Sequence[tuple[str, str, str]],
) -> str:
    seed = json.dumps(
        {
            "run_id": str(run_id or "").strip(),
            "source_tool_call_id": str(source_tool_call_id or "").strip(),
            "recovery_action": str(recovery_action or "").strip(),
            "observations": [list(identity) for identity in observation_identities],
        },
        ensure_ascii=False,
        separators=(",", ":"),
        sort_keys=True,
    )
    return (
        "runtime-recovery-continuation-"
        f"{hashlib.sha256(seed.encode('utf-8')).hexdigest()[:24]}"
    )


def _pending_runtime_replan_payloads(
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    handled = _handled_runtime_replan_request_identities(timeline)
    payloads: list[dict[str, Any]] = []
    for payload in _timeline_replan_request_payloads(timeline):
        identity = _runtime_replan_payload_identity(payload)
        if identity and identity in handled:
            continue
        payloads.append(payload)
    return payloads


def _pending_auto_replan_recovery_requests_with_task_context(
    replan_payloads: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    allowed_tool_list = list(allowed_tools)
    consumed_identities = recovery_action_identities_from_timeline(timeline)
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        raw_start = payload.get("runtime_tool_timeline_start")
        tool_timeline_start = int(raw_start) if isinstance(raw_start, int) else 0
        candidates = _auto_replan_recovery_requests_with_task_context(
            [dict(payload)],
            allowed_tool_list,
            timeline,
            tool_timeline_start=min(
                len(timeline),
                max(0, tool_timeline_start),
            ),
        )
        for request in candidates:
            if recovery_request_repeats_stalled_discovery(
                request,
                timeline,
                tool_timeline_start=min(
                    len(timeline),
                    max(0, tool_timeline_start),
                ),
            ):
                continue
            identity = ensure_recovery_action_identity(request)
            if identity and identity in consumed_identities:
                continue
            requests.append(request)
    return requests


def _runtime_replan_request_action_id(request: Mapping[str, Any]) -> str:
    return str(
        request.get("replan_recovery_action_id")
        or request.get("action_id")
        or ""
    ).strip()


def _runtime_replan_effectful_action_signatures(
    payload: Mapping[str, Any],
) -> tuple[str, ...]:
    metadata = (
        payload.get("metadata")
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    authorized_tools = {
        *_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS,
        *_string_list(payload.get("fallback_tools")),
    }
    actions = [
        *_mapping_list(metadata.get("recovery_actions")),
        *_mapping_list(payload.get("recovery_actions")),
    ]
    for action in actions:
        tool_name = str(action.get("tool") or action.get("tool_name") or "").strip()
        if tool_name:
            authorized_tools.add(tool_name)
        for deferred in materialized_deferred_items(action):
            deferred_tool = str(
                deferred.get("tool") or deferred.get("tool_name") or ""
            ).strip()
            if deferred_tool:
                authorized_tools.add(deferred_tool)
    try:
        candidates = _auto_replan_recovery_requests(
            [dict(payload)],
            authorized_tools,
        )
    except (TypeError, ValueError):
        return ()
    signatures: list[str] = []
    for request in candidates:
        tool_name = str(request.get("tool") or "").strip()
        if not tool_name or tool_name in READ_ONLY_OBSERVATION_TOOLS:
            continue
        request_input = (
            request.get("input")
            if isinstance(request.get("input"), Mapping)
            else {}
        )
        signature = canonical_replan_action_signature(
            tool_name,
            request_input,
            action_id=_runtime_replan_request_action_id(request),
        )
        if signature and signature not in signatures:
            signatures.append(signature)
    return tuple(signatures)


def _runtime_replan_private_binding_from_payload(
    payload: Mapping[str, Any],
) -> dict[str, str]:
    metadata = (
        payload.get("metadata")
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    binding = {
        "run_id": str(payload.get("run_id") or "").strip(),
        "replan_request_id": str(payload.get("request_id") or "").strip(),
        "source_tool_call_id": str(
            metadata.get("source_tool_call_id") or ""
        ).strip(),
        "source_request_id": str(
            metadata.get("source_request_id") or ""
        ).strip(),
        "source_plan_id": str(metadata.get("source_plan_id") or "").strip(),
        "source_step_id": str(metadata.get("source_step_id") or "").strip(),
        "source_tool": str(payload.get("source_tool_name") or "").strip(),
        "source_provider_kind": str(
            metadata.get("source_provider_kind") or ""
        ).strip(),
        "source_provider_id": str(
            metadata.get("source_provider_id") or ""
        ).strip(),
        "goal_contract_id": str(payload.get("goal_contract_id") or "").strip(),
        "goal_criterion_id": str(payload.get("goal_criterion_id") or "").strip(),
    }
    if (
        str(payload.get("source") or "").strip()
        != "runtime_tool_request_runner"
        or any(not value for value in binding.values())
        or str(payload.get("plan_id") or "").strip()
        != binding["source_plan_id"]
        or str(payload.get("source_step_id") or "").strip()
        != binding["source_step_id"]
    ):
        return {}
    return binding


def _runtime_replan_payload_with_private_authority(
    payload: Mapping[str, Any],
    *,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> dict[str, Any]:
    public_payload = dict(payload)
    public_payload.pop(RUNTIME_PRIVATE_REPLAN_CONTEXT_KEY, None)
    binding = _runtime_replan_private_binding_from_payload(public_payload)
    if (
        not binding
        or binding["run_id"] != str(run_id or "").strip()
        or not _runtime_replan_private_source_matches(
            binding,
            timeline=timeline,
        )
    ):
        return public_payload
    context = mint_private_replan_context(
        binding=binding,
        action_signatures=_runtime_replan_effectful_action_signatures(public_payload),
    )
    if context:
        return RuntimeAuthorizedReplanPayload(
            public_payload,
            private_context=context,
        )
    return public_payload


def _runtime_replan_private_source_matches(
    binding: Mapping[str, str],
    *,
    timeline: Sequence[Mapping[str, Any]],
) -> bool:
    matches = 0
    for raw_event in timeline:
        if not isinstance(raw_event, Mapping):
            continue
        raw_payload = (
            raw_event.get("payload")
            if isinstance(raw_event.get("payload"), Mapping)
            else {}
        )
        event = {**dict(raw_payload), **dict(raw_event)}
        event_type = str(
            event.get("event") or event.get("event_type") or ""
        ).strip()
        result = event.get("result")
        if (
            event_type
            not in {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
            or not isinstance(result, Mapping)
            or str(event.get("run_id") or "").strip() != binding["run_id"]
            or str(event.get("tool_call_id") or "").strip()
            != binding["source_tool_call_id"]
            or str(event.get("request_id") or "").strip()
            != binding["source_request_id"]
            or str(event.get("plan_id") or "").strip()
            != binding["source_plan_id"]
            or str(
                event.get("step_id") or event.get("planner_step_id") or ""
            ).strip()
            != binding["source_step_id"]
            or str(event.get("tool") or event.get("detail") or "").strip()
            != binding["source_tool"]
            or str(event.get("goal_contract_id") or "").strip()
            != binding["goal_contract_id"]
            or str(event.get("goal_criterion_id") or "").strip()
            != binding["goal_criterion_id"]
            or not _runtime_owned_terminal_event(
                event,
                result,
                run_id=binding["run_id"],
                plan_id=binding["source_plan_id"],
            )
            or _runtime_execution_provider_identity(result)
            != {
                "provider_kind": binding["source_provider_kind"],
                "provider_id": binding["source_provider_id"],
            }
        ):
            continue
        matches += 1
        if matches > 1:
            return False
    return matches == 1


def _replace_in_memory_runtime_replan_payload(
    timeline: list[dict[str, Any]],
    *,
    dedupe_key: tuple[str, str, str],
    payload: Mapping[str, Any],
) -> None:
    for event in reversed(timeline):
        if not isinstance(event, dict):
            continue
        if _runtime_replan_event_type(
            str(event.get("event") or event.get("event_type") or "").strip()
        ) != "agent.replan.requested":
            continue
        current = (
            event.get("payload")
            if isinstance(event.get("payload"), Mapping)
            else {}
        )
        if _replan_payload_dedupe_key(current) != dedupe_key:
            continue
        event["payload"] = copy_live_replan_payload(payload)
        return


def _bind_trusted_runtime_replan_goal_requests(
    requests: list[dict[str, Any]],
    replan_payloads: Sequence[Mapping[str, Any]],
    *,
    contract: GoalContract | None,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> list[dict[str, Any]]:
    """Authorize fallback attempts only from an exact failed Runtime fact.

    Replan events are persisted projections and are not execution authority by
    themselves.  This gate replays their source identity against the immutable
    Goal contract and the provider-attested first terminal event, then mints a
    fresh request identity for one bounded effectful fallback plus its safe
    observation batch.  Stale/public replans remain visible for diagnosis but
    cannot execute a side effect.
    """

    clean_run_id = str(run_id or "").strip()
    if (
        not requests
        or contract is None
        or not clean_run_id
        or contract.run_id != clean_run_id
    ):
        return []
    payload_by_id = {
        str(payload.get("request_id") or "").strip(): payload
        for payload in replan_payloads
        if isinstance(payload, Mapping)
        and str(payload.get("request_id") or "").strip()
    }
    assessment = runtime_goal_assessment(contract, timeline)
    effectful_source_calls: set[str] = set()
    seen_request_signatures: set[tuple[str, str]] = set()
    bound: list[dict[str, Any]] = []
    for request in requests:
        replan_request_id = str(
            request.get("replan_request_id") or ""
        ).strip()
        payload = payload_by_id.get(replan_request_id)
        if payload is None:
            continue
        source = _trusted_runtime_replan_source(
            payload,
            contract=contract,
            assessment=assessment,
            timeline=timeline,
            run_id=clean_run_id,
        )
        if not source:
            continue
        source_call_id = str(source["source_tool_call_id"])
        criterion = contract.criterion(str(source["criterion_id"]))
        tool_name = str(request.get("tool") or "").strip()
        tool_capabilities = set(capability_ids_for_tool(tool_name))
        is_observation = tool_name in READ_ONLY_OBSERVATION_TOOLS
        capability_compatible = bool(
            criterion is not None
            and set(criterion.required_capabilities).issubset(tool_capabilities)
        )
        if criterion is None or not tool_name:
            continue
        source_is_observation_only = (
            str(source.get("source_observation_only") or "").strip() == "true"
        )
        # The exact failed source may authorize several policy-filtered reads
        # needed to decide the next action.  It may never authorize an
        # unrelated write, and only one capability-compatible effectful retry
        # is minted for a source terminal.
        if source_is_observation_only and not is_observation:
            continue
        if not is_observation and (
            not capability_compatible
            or not criterion.required_capabilities
            or str(source.get("source_retryable") or "").strip() != "true"
            or str(source.get("source_step_id") or "").strip()
            not in {*criterion.source_step_ids, *criterion.verifier_step_ids}
            or source_call_id in effectful_source_calls
        ):
            continue
        if not is_observation:
            trusted_signatures = trusted_private_replan_action_signatures(
                payload,
                binding={
                    "run_id": clean_run_id,
                    "replan_request_id": replan_request_id,
                    "source_tool_call_id": source_call_id,
                    "source_request_id": str(source["source_request_id"]),
                    "source_plan_id": str(source["source_plan_id"]),
                    "source_step_id": str(source["source_step_id"]),
                    "source_tool": str(source["source_tool"]),
                    "source_provider_kind": str(source["source_provider_kind"]),
                    "source_provider_id": str(source["source_provider_id"]),
                    "goal_contract_id": contract.contract_id,
                    "goal_criterion_id": criterion.criterion_id,
                },
            )
            request_input = (
                request.get("input")
                if isinstance(request.get("input"), Mapping)
                else {}
            )
            candidate_signature = canonical_replan_action_signature(
                tool_name,
                request_input,
                action_id=_runtime_replan_request_action_id(request),
            )
            if not candidate_signature or candidate_signature not in trusted_signatures:
                continue
        request_signature = _trusted_runtime_replan_request_signature(request)
        if request_signature in seen_request_signatures:
            continue
        item = dict(request)
        ensure_tool_call_id(item)
        # Persisted/model requests do not choose planner authority.  Bind every
        # fallback to the exact Goal step that produced the trusted terminal.
        bound_step_id = str(source["source_step_id"])
        item.update(
            {
                "source": "runtime_planner",
                "planning_reason": "planner_replan_fallback_recovery",
                "plan_id": str(source["source_plan_id"]),
                "step_id": bound_step_id,
                "planner_step_id": bound_step_id,
                "goal_contract_id": contract.contract_id,
                "goal_criterion_id": criterion.criterion_id,
                "root_goal_unchanged": True,
                "replan_source_tool_call_id": source_call_id,
                "replan_source_request_id": str(source["source_request_id"]),
                "replan_source_step_id": str(source["source_step_id"]),
            }
        )
        expected_target = criterion.expected.get("target")
        if is_observation:
            # Observation receipts can guide the loop, but they are not the
            # root action and must not carry synthetic completion evidence.
            item["observation_only"] = True
            item["goal_completion_authority"] = False
            for key in (
                "action_target",
                "postcondition_verified",
                "verification_passed",
                "verified",
                "state",
                "target",
            ):
                item.pop(key, None)
            capability_id = str(item.get("capability_id") or "").strip()
            if capability_id not in tool_capabilities:
                if tool_capabilities:
                    item["capability_id"] = sorted(tool_capabilities)[0]
                else:
                    item.pop("capability_id", None)
        else:
            for key in (
                "postcondition_verified",
                "verification_passed",
                "verified",
                "state",
                "target",
                "goal_completion_authority",
                "observation_only",
            ):
                item.pop(key, None)
            if isinstance(expected_target, Mapping) and expected_target:
                item["action_target"] = dict(expected_target)
            else:
                item.pop("action_target", None)
            effectful_source_calls.add(source_call_id)
        if not str(item.get("request_id") or "").strip():
            digest = hashlib.sha256(
                (
                    f"{replan_request_id}\0{source_call_id}\0"
                    f"{item['tool_call_id']}\0{tool_name}"
                ).encode("utf-8")
            ).hexdigest()[:16]
            item["request_id"] = f"runtime-replan-request-{digest}"
        seen_request_signatures.add(request_signature)
        bound.append(item)
        if len(bound) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return bound


def _trusted_runtime_replan_request_signature(
    request: Mapping[str, Any],
) -> tuple[str, str]:
    tool_name = str(request.get("tool") or "").strip()
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    try:
        normalized_input = json.dumps(
            dict(request_input),
            ensure_ascii=False,
            sort_keys=True,
            separators=(",", ":"),
            default=str,
        )
    except (TypeError, ValueError):
        normalized_input = repr(
            sorted(
                dict(request_input).items(),
                key=lambda item: str(item[0]),
            )
        )
    return tool_name, normalized_input


def _trusted_runtime_replan_source(
    payload: Mapping[str, Any],
    *,
    contract: GoalContract,
    assessment: Any,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> dict[str, str]:
    metadata = (
        payload.get("metadata")
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    identity = {
        "source_tool_call_id": str(
            metadata.get("source_tool_call_id") or ""
        ).strip(),
        "source_request_id": str(
            metadata.get("source_request_id") or ""
        ).strip(),
        "source_plan_id": str(metadata.get("source_plan_id") or "").strip(),
        "source_step_id": str(metadata.get("source_step_id") or "").strip(),
        "source_tool": str(payload.get("source_tool_name") or "").strip(),
        "source_provider_kind": str(
            metadata.get("source_provider_kind") or ""
        ).strip(),
        "source_provider_id": str(
            metadata.get("source_provider_id") or ""
        ).strip(),
    }
    payload_contract_id = str(payload.get("goal_contract_id") or "").strip()
    payload_criterion_id = str(payload.get("goal_criterion_id") or "").strip()
    payload_source = str(payload.get("source") or "").strip()
    if payload_source == _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE:
        if not _runtime_recovery_continuation_payload_is_trusted(
            payload,
            timeline=timeline,
        ):
            return {}
    elif payload_source != "runtime_tool_request_runner":
        return {}
    if (
        str(payload.get("run_id") or "").strip() != run_id
        or not str(payload.get("request_id") or "").strip()
        or payload_contract_id != contract.contract_id
        or not payload_criterion_id
        or any(not value for value in identity.values())
        or str(payload.get("plan_id") or "").strip()
        != identity["source_plan_id"]
        or str(payload.get("source_step_id") or "").strip()
        != identity["source_step_id"]
    ):
        return {}
    matches: list[
        tuple[Mapping[str, Any], Mapping[str, Any], Any]
    ] = []
    for raw_event in timeline:
        if not isinstance(raw_event, Mapping):
            continue
        raw_payload = (
            raw_event.get("payload")
            if isinstance(raw_event.get("payload"), Mapping)
            else {}
        )
        event = {**dict(raw_payload), **dict(raw_event)}
        event_type = str(
            event.get("event") or event.get("event_type") or ""
        ).strip()
        result = event.get("result")
        if (
            event_type not in {
                "agent.tool.call",
                "agent.tool.failed",
                "agent.tool.skipped",
            }
            or not isinstance(result, Mapping)
            or str(event.get("run_id") or "").strip() != run_id
            or str(event.get("tool_call_id") or "").strip()
            != identity["source_tool_call_id"]
            or str(event.get("request_id") or "").strip()
            != identity["source_request_id"]
            or str(event.get("plan_id") or "").strip()
            != identity["source_plan_id"]
            or str(
                event.get("step_id") or event.get("planner_step_id") or ""
            ).strip()
            != identity["source_step_id"]
            or str(event.get("tool") or event.get("detail") or "").strip()
            != identity["source_tool"]
            or str(event.get("goal_contract_id") or "").strip()
            != payload_contract_id
            or str(event.get("goal_criterion_id") or "").strip()
            != payload_criterion_id
        ):
            continue
        if not _runtime_owned_terminal_event(
            event,
            result,
            run_id=run_id,
            plan_id=identity["source_plan_id"],
        ):
            continue
        provider = _runtime_execution_provider_identity(result)
        if provider != {
            "provider_kind": identity["source_provider_kind"],
            "provider_id": identity["source_provider_id"],
        }:
            continue
        outcome = from_tool_result(
            identity["source_tool"],
            result,
            capabilities=capability_ids_for_tool(identity["source_tool"]),
        )
        if outcome.status not in {
            OutcomeStatus.FAILED,
            OutcomeStatus.PARTIAL,
            OutcomeStatus.SKIPPED,
        }:
            continue
        matches.append((event, result, outcome))
    if len(matches) != 1:
        return {}
    source_event, _source_result, source_outcome = matches[0]
    criterion_ids = {
        evidence.criterion_id
        for evidence in assessment.evidence
        if evidence.kind == "tool_outcome"
        and evidence.source_tool_call_id == identity["source_tool_call_id"]
        and evidence.status in {
            OutcomeStatus.FAILED.value,
            OutcomeStatus.PARTIAL.value,
            OutcomeStatus.SKIPPED.value,
        }
    }
    source_observation_only = bool(
        source_outcome.status is OutcomeStatus.SKIPPED
        or identity["source_tool"] in READ_ONLY_OBSERVATION_TOOLS
    )
    if len(criterion_ids) == 1:
        criterion_id = next(iter(criterion_ids))
        if payload_criterion_id != criterion_id:
            return {}
        return {
            **identity,
            "criterion_id": criterion_id,
            "source_kind": "goal_failure_evidence",
            "source_observation_only": (
                "true" if source_observation_only else "false"
            ),
            "source_retryable": "true" if source_outcome.retryable else "false",
        }
    if criterion_ids or identity["source_tool"] not in READ_ONLY_OBSERVATION_TOOLS:
        return {}
    criterion_id = _goal_bound_runtime_observation_criterion_id(
        contract,
        source_event,
        payload,
    )
    if not criterion_id:
        return {}
    return {
        **identity,
        "criterion_id": criterion_id,
        "source_kind": "goal_bound_observation_failure",
        "source_observation_only": "true",
        "source_retryable": "true" if source_outcome.retryable else "false",
    }


def _goal_bound_runtime_observation_criterion_id(
    contract: GoalContract,
    event: Mapping[str, Any],
    payload: Mapping[str, Any],
) -> str:
    if str(event.get("source") or "").strip() != "runtime_planner":
        return ""
    step_id = str(
        event.get("step_id") or event.get("planner_step_id") or ""
    ).strip()
    capability_id = str(event.get("capability_id") or "").strip()
    payload_contract_id = str(payload.get("goal_contract_id") or "").strip()
    payload_criterion_id = str(payload.get("goal_criterion_id") or "").strip()
    event_contract_id = str(event.get("goal_contract_id") or "").strip()
    event_criterion_id = str(event.get("goal_criterion_id") or "").strip()
    if (
        payload_contract_id != contract.contract_id
        or event_contract_id != contract.contract_id
        or not payload_criterion_id
        or event_criterion_id != payload_criterion_id
    ):
        return ""
    candidates = [
        criterion
        for criterion in contract.criteria
        if step_id in {*criterion.source_step_ids, *criterion.verifier_step_ids}
        and set(criterion.required_capabilities).issubset({capability_id})
    ]
    if len(candidates) != 1:
        return ""
    criterion_id = candidates[0].criterion_id
    if payload_criterion_id != criterion_id or event_criterion_id != criterion_id:
        return ""
    return criterion_id


def _handled_runtime_replan_request_identities(
    timeline: list[dict[str, Any]],
) -> set[str]:
    handled: set[str] = set()
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        event_type = str(event.get("event") or event.get("event_type") or "").strip()
        base_event_type = _runtime_progress_base_event_type(event_type)
        payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
        if base_event_type == "agent.recovery.planned" and str(
            event.get("recovery_owner") or payload.get("recovery_owner") or ""
        ).strip() == "coordinator":
            identity = str(
                event.get("replan_request_id")
                or payload.get("replan_request_id")
                or ""
            ).strip()
            if identity:
                handled.add(identity)
            continue
        if base_event_type != "agent.replan.recovery.updated":
            continue
        if recovery_action_identities_from_timeline([event]):
            continue
        identity = str(
            event.get("request_id")
            or payload.get("request_id")
            or event.get("replan_request_id")
            or payload.get("replan_request_id")
            or ""
        ).strip()
        if identity:
            handled.add(identity)
    return handled


def _timeline_has_replan_followup_context(
    timeline: list[dict[str, Any]],
    replan_payloads: list[dict[str, Any]],
) -> bool:
    target = {
        identity
        for identity in (
            _runtime_replan_payload_identity(payload)
            for payload in replan_payloads
            if isinstance(payload, Mapping)
        )
        if identity
    }
    if not target:
        return False
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        if str(event.get("event") or "").strip() != "agent.model.followup_context":
            continue
        event_payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
        requests = event.get("replan_requests")
        if not isinstance(requests, list):
            requests = event_payload.get("replan_requests")
        if not isinstance(requests, list):
            continue
        existing = {
            identity
            for identity in (
                _runtime_replan_payload_identity(request)
                for request in requests
                if isinstance(request, Mapping)
            )
            if identity
        }
        if target.issubset(existing):
            return True
    return False


def _runtime_replan_payload_identity(payload: Mapping[str, Any]) -> str:
    request_id = str(payload.get("request_id") or "").strip()
    if request_id:
        return request_id
    dedupe_key = _replan_payload_dedupe_key(payload)
    if not dedupe_key:
        return ""
    return "|".join(dedupe_key)


def _replan_payload_dedupe_key(payload: Mapping[str, Any]) -> tuple[str, str, str] | None:
    step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    tool_name = str(
        payload.get("source_tool_name") or payload.get("tool_name") or ""
    ).strip()
    trigger = str(payload.get("trigger") or payload.get("replan_trigger") or "").strip()
    if not step_id and not tool_name:
        return None
    return (step_id, tool_name, trigger)


def _runtime_replan_planning_reason(triggers: list[str]) -> str:
    clean = {str(trigger or "").strip() for trigger in triggers if str(trigger or "").strip()}
    if clean == {"verification_failed"}:
        return "planner_replan_after_verification_failed"
    if clean == {"tool_unavailable"}:
        return "planner_replan_after_tool_unavailable"
    if "verification_failed" in clean:
        return "planner_replan_after_mixed_runtime_failure"
    return "planner_replan_after_tool_failure"


def _runtime_replan_capability_recovery(
    requests: list[dict[str, Any]],
    *,
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    capability_ids = _ordered_text_list(
        [
            str(request.get("target_capability_id") or "").strip()
            for request in requests
            if str(request.get("trigger") or "").strip() == "tool_unavailable"
            and str(request.get("target_capability_id") or "").strip()
        ]
    )
    if not capability_ids:
        return []
    snapshots = {
        str(snapshot.capability_id or "").strip(): snapshot
        for snapshot in capability_snapshots(
            allowed_tools=allowed_tools,
            capability_ids=capability_ids,
        )
    }
    recoveries: dict[str, dict[str, Any]] = {}
    for request in requests:
        if str(request.get("trigger") or "").strip() != "tool_unavailable":
            continue
        capability_id = str(request.get("target_capability_id") or "").strip()
        if not capability_id:
            continue
        snapshot = snapshots.get(capability_id)
        recovery = recoveries.setdefault(
            capability_id,
            {
                "capability_id": capability_id,
                "title": str(getattr(snapshot, "title", "") or "").strip(),
                "category": str(getattr(snapshot, "category", "") or "").strip(),
                "description": str(getattr(snapshot, "description", "") or "").strip(),
                "risk_level": str(getattr(snapshot, "risk_level", "") or "").strip(),
                "approval_required": bool(getattr(snapshot, "approval_required", False)),
                "tools": list(getattr(snapshot, "tools", []) or []),
                "available_tools": list(getattr(snapshot, "available_tools", []) or []),
                "missing_tools": list(getattr(snapshot, "missing_tools", []) or []),
                "fallback_tools": [],
                "source_step_ids": [],
                "missing_permissions": [],
                "blocking_conditions": [],
            },
        )
        step_id = str(request.get("source_step_id") or "").strip()
        if step_id and step_id not in recovery["source_step_ids"]:
            recovery["source_step_ids"].append(step_id)
        for key in ("fallback_tools",):
            recovery[key] = _ordered_text_list(
                [*recovery.get(key, []), *_string_list(request.get(key))]
            )
        metadata = request.get("metadata") if isinstance(request.get("metadata"), Mapping) else {}
        for key in ("missing_permissions", "blocking_conditions"):
            recovery[key] = _ordered_text_list(
                [*recovery.get(key, []), *_string_list(metadata.get(key))]
            )
    result: list[dict[str, Any]] = []
    for capability_id in capability_ids:
        recovery = recoveries.get(capability_id)
        if not recovery:
            continue
        missing_tools = _string_list(recovery.get("missing_tools"))
        missing_permissions = _string_list(recovery.get("missing_permissions"))
        blocking_conditions = _string_list(recovery.get("blocking_conditions"))
        if missing_permissions:
            suggested_action = "resolve_permissions"
        elif blocking_conditions:
            suggested_action = "clear_runtime_blockers"
        elif missing_tools:
            suggested_action = "enable_tools"
        else:
            suggested_action = "inspect_capability"
        recovery["suggested_action"] = suggested_action
        recovery["recommended_enable_tools"] = missing_tools[:6]
        result.append(
            {key: value for key, value in recovery.items() if value not in ("", [], {})}
        )
    return result


def _runtime_replan_recovery_actions(
    requests: Iterable[Mapping[str, Any]],
    *,
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    actions: list[dict[str, Any]] = []
    seen: set[tuple[str, str]] = set()
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        for action in _mapping_list(request.get("recovery_actions")):
            tool_name = str(action.get("tool") or "").strip()
            if allowed and tool_name not in allowed:
                continue
            action_input = (
                action.get("input")
                if isinstance(action.get("input"), Mapping)
                else {}
            )
            signature = (tool_name, repr(sorted(dict(action_input).items())))
            if not tool_name or signature in seen:
                continue
            seen.add(signature)
            actions.append(dict(action))
    return actions


def _runtime_replan_recovery_targets(
    requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, str]]:
    targets: list[dict[str, str]] = []
    seen: set[tuple[str, str, str]] = set()
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        target = {
            key: str(request.get(key) or "").strip()
            for key in ("target_app_name", "target_app_query", "target_search_text")
        }
        signature = (
            target["target_app_name"],
            target["target_app_query"],
            target["target_search_text"],
        )
        if not any(signature) or signature in seen:
            continue
        seen.add(signature)
        targets.append({key: value for key, value in target.items() if value})
    return targets


def _runtime_replan_recovery_observations(
    requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    request_list = [request for request in requests if isinstance(request, Mapping)]
    request_ids = {
        str(request.get("request_id") or "").strip()
        for request in request_list
        if str(request.get("request_id") or "").strip()
    }
    observations: list[dict[str, Any]] = []
    seen: set[tuple[str, str, str]] = set()
    for request in request_list:
        if str(request.get("source") or "").strip() != (
            _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
        ):
            continue
        embedded = _mapping_list(request.get("grounded_observations"))
        if not embedded and isinstance(request.get("grounded_observation"), Mapping):
            embedded = [dict(request["grounded_observation"])]
        for observation in embedded:
            if str(observation.get("grounded_by") or "").strip() != (
                _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
            ):
                continue
            signature = (
                str(observation.get("source_tool") or "").strip(),
                str(observation.get("source_tool_call_id") or "").strip(),
                str(
                    observation.get("text")
                    or observation.get("summary")
                    or observation.get("path")
                    or ""
                ).strip(),
            )
            if not all(signature[:2]) or signature in seen:
                continue
            seen.add(signature)
            observations.append(dict(observation))
    if not request_ids or not timeline:
        return observations[-6:]
    for event in timeline:
        if not _runtime_replan_recovery_tool_event(event, request_ids):
            continue
        tool_name = str(event.get("detail") or event.get("tool") or "").strip()
        result = event.get("result") if isinstance(event.get("result"), dict) else {}
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), dict)
            else {}
        )
        snapshot = followup_content_snapshot_for_tool_call(
            tool_name,
            result,
            input_preview,
        )
        if not snapshot:
            continue
        _attach_replan_observation_trace(snapshot, event)
        signature = (
            str(snapshot.get("source_tool") or "").strip(),
            str(snapshot.get("replan_request_id") or "").strip(),
            str(snapshot.get("text") or snapshot.get("summary") or snapshot.get("path") or "").strip(),
        )
        if signature in seen:
            continue
        seen.add(signature)
        observations.append(snapshot)
    return observations[-6:]


def _runtime_replan_recovery_tool_event(
    event: Mapping[str, Any],
    request_ids: set[str],
) -> bool:
    if not isinstance(event, Mapping):
        return False
    if str(event.get("event") or "").strip() != "agent.tool.call":
        return False
    replan_request_id = str(event.get("replan_request_id") or "").strip()
    if replan_request_id:
        return replan_request_id in request_ids
    return str(event.get("planning_reason") or "").strip() == (
        "planner_verification_recovery_observation"
    )


def _attach_replan_observation_trace(
    snapshot: dict[str, Any],
    event: Mapping[str, Any],
) -> None:
    for key in (
        "replan_request_id",
        "replan_trigger",
        "target_app_name",
        "target_app_query",
        "target_search_text",
        "materialized_from_request_id",
        "materialization_source_request_id",
        "materialization_source_step_id",
        "materialization_binding_id",
        "materialized_content_sha256",
        "materialization_authority",
        "goal_contract_id",
        "goal_criterion_id",
        "runtime_stage",
        "runtime_role",
    ):
        value = str(event.get(key) or "").strip()
        if value:
            snapshot[key] = value
    for key in ("replan_triggers", "replan_signal_ids"):
        values = _string_list(event.get(key))
        if values:
            snapshot[key] = values


def _model_replan_followup_context_message(payload: dict[str, Any]) -> str:
    requests = [
        request
        for request in payload.get("replan_requests", [])
        if isinstance(request, dict)
    ]
    fallback_tools = _string_list(payload.get("fallback_tools"))
    failed_tools = _string_list(payload.get("failed_tools"))
    planning_reason = str(payload.get("planning_reason") or "").strip()
    lines = _runtime_replan_message_preamble(planning_reason)
    followup_target = (
        payload.get("followup_target")
        if isinstance(payload.get("followup_target"), Mapping)
        else {}
    )
    target_instruction = _model_followup_target_instruction(followup_target)
    if target_instruction:
        lines.append(target_instruction.strip())
    pending_plan_instruction = _model_followup_pending_plan_instruction(payload)
    if pending_plan_instruction:
        lines.append(pending_plan_instruction)
    if failed_tools:
        lines.append(f"Failed tools: {', '.join(failed_tools)}.")
    if fallback_tools:
        lines.append(f"Preferred fallback tools: {', '.join(fallback_tools)}.")
    recovery_actions = [
        item
        for item in payload.get("recovery_actions", [])
        if isinstance(item, dict)
    ]
    if recovery_actions:
        lines.append("Runtime recovery actions:")
        for item in recovery_actions[:5]:
            tool_name = str(item.get("tool") or "").strip()
            label = str(item.get("label") or tool_name).strip()
            action_input = (
                item.get("input")
                if isinstance(item.get("input"), dict)
                else {}
            )
            parts = [part for part in (label, tool_name) if part]
            if action_input:
                parts.append(f"input={_model_followup_input_preview_text(action_input)}")
            if parts:
                lines.append(f"- {'; '.join(parts)}")
    recovery_targets = [
        item
        for item in payload.get("recovery_targets", [])
        if isinstance(item, dict)
    ]
    if recovery_targets:
        lines.append("Recovery targets:")
        for item in recovery_targets[:3]:
            parts = [
                part
                for part in (
                    _runtime_replan_target_part("app", item.get("target_app_name")),
                    _runtime_replan_target_part("query", item.get("target_app_query")),
                    _runtime_replan_target_part("text", item.get("target_search_text")),
                )
                if part
            ]
            if parts:
                lines.append(f"- {'; '.join(parts)}")
    snapshot_text = _followup_content_snapshots_message(payload)
    if snapshot_text:
        lines.append(snapshot_text.strip())
    capability_recovery = [
        item
        for item in payload.get("capability_recovery", [])
        if isinstance(item, dict)
    ]
    if capability_recovery:
        lines.append("Capability recovery:")
        for item in capability_recovery[:5]:
            capability_id = str(item.get("capability_id") or "").strip()
            missing_tools = _string_list(item.get("missing_tools"))
            available_tools = _string_list(item.get("available_tools"))
            missing_permissions = _string_list(item.get("missing_permissions"))
            blocking_conditions = _string_list(item.get("blocking_conditions"))
            parts = [capability_id] if capability_id else []
            if missing_tools:
                parts.append(f"enable_tools={', '.join(missing_tools[:6])}")
            if available_tools:
                parts.append(f"available_tools={', '.join(available_tools[:6])}")
            if missing_permissions:
                parts.append(f"permissions={', '.join(missing_permissions[:6])}")
            if blocking_conditions:
                parts.append(f"blockers={', '.join(blocking_conditions[:6])}")
            if parts:
                lines.append(f"- {'; '.join(parts)}")
    task_progress = (
        payload.get("task_progress")
        if isinstance(payload.get("task_progress"), dict)
        else {}
    )
    if task_progress:
        lines.append("Task progress:")
        for key, label in (
            ("completed_steps", "completed_steps"),
            ("blocked_steps", "blocked_steps"),
            ("pending_steps", "pending_steps"),
            ("waiting_approval_steps", "waiting_approval_steps"),
        ):
            values = _string_list(task_progress.get(key))
            if values:
                lines.append(f"- {label}: {', '.join(values)}")
        workspace_items = [
            item
            for item in task_progress.get("workspace_items", [])
            if isinstance(item, dict)
        ]
        if workspace_items:
            rendered_items = []
            for item in workspace_items[:8]:
                kind = str(item.get("kind") or "").strip()
                path = str(item.get("path") or item.get("title") or "").strip()
                if path:
                    rendered_items.append(f"{kind}:{path}" if kind else path)
            if rendered_items:
                lines.append(f"- workspace_items: {', '.join(rendered_items)}")
    task_core = payload.get("task_core") if isinstance(payload.get("task_core"), dict) else {}
    if task_core:
        lines.extend(_runtime_replan_task_core_message_lines(task_core))
    for index, request in enumerate(requests[:3], start=1):
        detail = str(request.get("failure_detail") or "").strip()
        reason = str(request.get("reason") or "").strip()
        prompt = str(request.get("replan_prompt") or "").strip()
        lines.append(f"Replan request {index}:")
        if request.get("request_id"):
            lines.append(f"- request_id: {request['request_id']}")
        if request.get("source_step_id"):
            lines.append(f"- failed_step: {request['source_step_id']}")
        if request.get("source_tool_name"):
            lines.append(f"- failed_tool: {request['source_tool_name']}")
        if request.get("target_capability_id"):
            lines.append(f"- target_capability: {request['target_capability_id']}")
        if detail:
            lines.append(f"- failure_detail: {detail}")
        if reason:
            lines.append(f"- replan_reason: {reason}")
        if prompt:
            lines.append(f"- planner_replan_prompt: {prompt}")
    return "\n".join(lines)


def _runtime_replan_target_part(label: str, value: Any) -> str:
    clean = str(value or "").strip()
    return f"{label}={clean}" if clean else ""


def _runtime_replan_task_core_message_lines(task_core: Mapping[str, Any]) -> list[str]:
    lines = ["Task workspace:"]
    workspace = (
        task_core.get("workspace")
        if isinstance(task_core.get("workspace"), Mapping)
        else {}
    )
    workspace_title = str(
        workspace.get("title") or workspace.get("workspace_id") or ""
    ).strip()
    if workspace_title:
        lines.append(f"- workspace: {workspace_title}")
    workspace_items = [
        item
        for item in workspace.get("items", [])
        if isinstance(item, Mapping)
    ]
    if workspace_items:
        rendered = []
        for item in workspace_items[:8]:
            kind = str(item.get("kind") or "").strip()
            status = str(item.get("status") or "planned").strip()
            path = str(item.get("path") or item.get("title") or "").strip()
            source_step_id = str(item.get("source_step_id") or "").strip()
            metadata = item.get("metadata") if isinstance(item.get("metadata"), Mapping) else {}
            pattern = str(metadata.get("pattern") or "").strip()
            parts = [part for part in (kind, status, path, source_step_id, pattern) if part]
            if parts:
                rendered.append(" · ".join(parts))
        if rendered:
            lines.append(f"- workspace_items: {'; '.join(rendered)}")
    todos = [todo for todo in task_core.get("todos", []) if isinstance(todo, Mapping)]
    if todos:
        rendered = []
        for todo in todos[:8]:
            step_id = str(todo.get("step_id") or todo.get("todo_id") or "").strip()
            status = str(todo.get("status") or "planned").strip()
            tool = str(todo.get("tool_name") or "").strip()
            parts = [part for part in (step_id, status, tool) if part]
            if parts:
                rendered.append(" · ".join(parts))
        if rendered:
            lines.append(f"- todos: {'; '.join(rendered)}")
    checkpoints = [
        checkpoint
        for checkpoint in task_core.get("checkpoints", [])
        if isinstance(checkpoint, Mapping)
    ]
    if checkpoints:
        rendered = []
        for checkpoint in checkpoints[:5]:
            step_id = str(
                checkpoint.get("after_step_id") or checkpoint.get("step_id") or ""
            ).strip()
            status = str(checkpoint.get("status") or "planned").strip()
            title = str(checkpoint.get("title") or checkpoint.get("checkpoint_id") or "").strip()
            parts = [part for part in (step_id, status, title) if part]
            if parts:
                rendered.append(" · ".join(parts))
        if rendered:
            lines.append(f"- checkpoints: {'; '.join(rendered)}")
    return lines


def _runtime_replan_message_preamble(planning_reason: str) -> list[str]:
    if planning_reason == "planner_replan_after_verification_failed":
        return [
            (
                "Runtime replan context: post-action verification did not confirm "
                "the requested desktop state."
            ),
            (
                "Continue the existing task workspace. Do not ask the user to perform "
                "the desktop-capable action manually."
            ),
            (
                "Rediscover or inspect the visible app/window state, choose the next "
                "safe observable action, and keep all approval and policy gates."
            ),
        ]
    if planning_reason == "planner_replan_after_tool_unavailable":
        return [
            "Runtime replan context: a planned tool is unavailable.",
            (
                "Continue the existing task workspace. Select another available "
                "capability or ask only for the missing permission/tool."
            ),
            "Keep all approval and policy gates.",
        ]
    return [
        "Runtime replan context: a planned tool failed or could not be verified.",
        (
            "Continue the existing task workspace. Do not ask the user to perform the "
            "tool-capable action manually."
        ),
        (
            "Inspect the failure, choose the next safe observable action, and keep all "
            "approval and policy gates."
        ),
    ]


def _runtime_replan_task_progress_summary(
    replan_payloads: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    if not timeline:
        return {}
    core_ids = {
        str(payload.get("core_id") or "").strip()
        for payload in replan_payloads
        if isinstance(payload, Mapping) and str(payload.get("core_id") or "").strip()
    }
    plan_ids = {
        str(payload.get("plan_id") or "").strip()
        for payload in replan_payloads
        if isinstance(payload, Mapping) and str(payload.get("plan_id") or "").strip()
    }
    todos_by_step: dict[str, dict[str, Any]] = {}
    checkpoints_by_step: dict[str, dict[str, Any]] = {}
    workspace_items_by_id: dict[str, dict[str, Any]] = {}
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
        task_context = (
            metadata.get("task_core_context")
            if isinstance(metadata.get("task_core_context"), Mapping)
            else {}
        )
        if not task_context:
            continue
        workspace_items_by_id.update(
            {
                _runtime_workspace_item_key(item): item
                for item in _runtime_workspace_item_summaries(
                    task_context.get("workspace_items")
                )
                if _runtime_workspace_item_key(item)
            }
        )
        for todo in task_context.get("todos", []):
            if not isinstance(todo, Mapping):
                continue
            step_id = str(todo.get("step_id") or "").strip()
            if not step_id:
                continue
            todos_by_step[step_id] = {
                "step_id": step_id,
                "title": str(todo.get("title") or step_id).strip(),
                "status": str(todo.get("status") or "").strip(),
                "tool": str(todo.get("tool_name") or "").strip(),
                "approval_required": bool(todo.get("approval_required")),
            }
        for checkpoint in task_context.get("checkpoints", []):
            if not isinstance(checkpoint, Mapping):
                continue
            step_id = str(
                checkpoint.get("after_step_id") or checkpoint.get("step_id") or ""
            ).strip()
            if not step_id:
                continue
            checkpoints_by_step[step_id] = {
                "step_id": step_id,
                "title": str(checkpoint.get("title") or step_id).strip(),
                "status": str(checkpoint.get("status") or "").strip(),
                "checkpoint_id": str(checkpoint.get("checkpoint_id") or "").strip(),
            }
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        event_name = str(event.get("event") or "").strip()
        base_event_name = _runtime_progress_base_event_type(event_name)
        event_payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
        core_id = str(event.get("core_id") or event_payload.get("core_id") or "").strip()
        plan_id = str(event.get("plan_id") or event_payload.get("plan_id") or "").strip()
        if core_ids and core_id and core_id not in core_ids:
            continue
        if plan_ids and plan_id and plan_id not in plan_ids:
            continue
        if _runtime_planner_base_event_type(event_name) == "agent.task_core.created":
            task_core = (
                event_payload.get("task_core")
                if isinstance(event_payload.get("task_core"), Mapping)
                else {}
            )
            workspace = (
                task_core.get("workspace")
                if isinstance(task_core.get("workspace"), Mapping)
                else {}
            )
            workspace_items_by_id = {
                _runtime_workspace_item_key(item): item
                for item in _runtime_workspace_item_summaries(workspace.get("items"))
                if _runtime_workspace_item_key(item)
            }
            continue
        if base_event_name == "agent.task.workspace_item.updated":
            item = (
                event.get("workspace_item")
                if isinstance(event.get("workspace_item"), Mapping)
                else {}
            )
            summaries = _runtime_workspace_item_summaries([item])
            if summaries:
                summary = {
                    **summaries[0],
                    "status": str(
                        event.get("status") or summaries[0].get("status") or ""
                    ).strip(),
                }
                key = _runtime_workspace_item_key(summary)
                if key:
                    workspace_items_by_id[key] = summary
            continue
        if base_event_name == "agent.task.todo.updated":
            step_id = str(event.get("step_id") or "").strip()
            if not step_id:
                continue
            todo = event.get("todo") if isinstance(event.get("todo"), Mapping) else {}
            todos_by_step[step_id] = {
                "step_id": step_id,
                "title": str(todo.get("title") or event.get("detail") or step_id).strip(),
                "status": str(event.get("status") or todo.get("status") or "").strip(),
                "tool": str(event.get("tool") or todo.get("tool_name") or "").strip(),
                "approval_required": bool(todo.get("approval_required")),
            }
            continue
        if base_event_name == "agent.task.checkpoint.updated":
            step_id = str(event.get("step_id") or "").strip()
            if not step_id:
                continue
            checkpoint = (
                event.get("checkpoint")
                if isinstance(event.get("checkpoint"), Mapping)
                else {}
            )
            checkpoints_by_step[step_id] = {
                "step_id": step_id,
                "title": str(checkpoint.get("title") or event.get("detail") or step_id).strip(),
                "status": str(event.get("status") or checkpoint.get("status") or "").strip(),
                "checkpoint_id": str(
                    event.get("checkpoint_id") or checkpoint.get("checkpoint_id") or ""
                ).strip(),
            }

    todos = list(todos_by_step.values())
    checkpoints = list(checkpoints_by_step.values())
    workspace_items = list(workspace_items_by_id.values())
    if not todos and not checkpoints and not workspace_items:
        return {}
    summary: dict[str, Any] = {
        "todo_count": len(todos),
        "checkpoint_count": len(checkpoints),
        "todos": todos[:20],
        "checkpoints": checkpoints[:20],
    }
    for status, key in (
        ("completed", "completed_steps"),
        ("blocked", "blocked_steps"),
        ("pending", "pending_steps"),
        ("waiting_approval", "waiting_approval_steps"),
        ("skipped", "skipped_steps"),
    ):
        values = [
            str(todo.get("step_id") or "").strip()
            for todo in todos
            if str(todo.get("status") or "").strip() == status
            and str(todo.get("step_id") or "").strip()
        ]
        if values:
            summary[key] = values
    if workspace_items:
        summary["workspace_items"] = workspace_items
    return summary


def _runtime_replan_task_core_payload(
    replan_payloads: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    if not timeline:
        return {}
    core_ids = {
        str(payload.get("core_id") or "").strip()
        for payload in replan_payloads
        if isinstance(payload, Mapping) and str(payload.get("core_id") or "").strip()
    }
    plan_ids = {
        str(payload.get("plan_id") or "").strip()
        for payload in replan_payloads
        if isinstance(payload, Mapping) and str(payload.get("plan_id") or "").strip()
    }
    task_core_event = _runtime_replan_latest_task_core_event(
        timeline,
        core_ids=core_ids,
        plan_ids=plan_ids,
    )
    if not task_core_event:
        return {}
    event_payload = (
        task_core_event.get("payload")
        if isinstance(task_core_event.get("payload"), Mapping)
        else {}
    )
    task_core = (
        event_payload.get("task_core")
        if isinstance(event_payload.get("task_core"), Mapping)
        else {}
    )
    if not task_core:
        return {}
    replay_events = _runtime_replan_task_core_replay_events(
        timeline,
        core_ids=core_ids,
        plan_ids=plan_ids,
    )
    replayed = _runtime_replan_replayed_task_core_payload(
        task_core,
        replay_events,
    )
    if replayed:
        return replayed
    return dict(task_core)


def _runtime_replan_latest_task_core_event(
    timeline: list[dict[str, Any]],
    *,
    core_ids: set[str],
    plan_ids: set[str],
) -> Mapping[str, Any]:
    for event in reversed(timeline):
        if not isinstance(event, Mapping):
            continue
        event_name = str(event.get("event") or "").strip()
        if not event_name.endswith(".task_core.created"):
            continue
        if not _runtime_replan_event_matches_ids(event, core_ids, plan_ids):
            continue
        return event
    return {}


def _runtime_replan_task_core_replay_events(
    timeline: list[dict[str, Any]],
    *,
    core_ids: set[str],
    plan_ids: set[str],
) -> list[Mapping[str, Any]]:
    events: list[Mapping[str, Any]] = []
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        event_name = str(event.get("event") or "").strip()
        if not (
            event_name.endswith(".task_core.created")
            or event_name.endswith(".task.workspace_item.updated")
            or event_name.endswith(".task.todo.updated")
            or event_name.endswith(".task.checkpoint.updated")
        ):
            continue
        if not _runtime_replan_event_matches_ids(event, core_ids, plan_ids):
            continue
        events.append(event)
    return events


def _runtime_replan_event_matches_ids(
    event: Mapping[str, Any],
    core_ids: set[str],
    plan_ids: set[str],
) -> bool:
    event_payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
    core_id = str(event.get("core_id") or event_payload.get("core_id") or "").strip()
    plan_id = str(event.get("plan_id") or event_payload.get("plan_id") or "").strip()
    if core_ids:
        return bool(core_id and core_id in core_ids)
    if plan_ids:
        return bool(plan_id and plan_id in plan_ids)
    return True


def _runtime_replan_replayed_task_core_payload(
    task_core: Mapping[str, Any],
    replay_events: list[Mapping[str, Any]],
) -> dict[str, Any]:
    try:
        from apps.shell.yachiyo_agent.events import (
            public_run_event_from_payload,
            run_event_parent_context,
        )
        from apps.shell.yachiyo_agent.task_core_snapshots import (
            task_core_snapshot_from_payload,
        )
    except Exception:
        return {}
    normalized_task_core = _runtime_replan_normalized_task_core_payload(task_core)
    context = run_event_parent_context({"task_core": normalized_task_core})
    public_events = [
        public_run_event_from_payload(event, sequence=index + 1, context=context)
        for index, event in enumerate(replay_events)
    ]
    snapshot = task_core_snapshot_from_payload(
        {"task_core": normalized_task_core},
        events=public_events,
    )
    if snapshot is None:
        return {}
    return snapshot.model_dump(mode="json")


def _runtime_replan_normalized_task_core_payload(
    task_core: Mapping[str, Any],
) -> dict[str, Any]:
    normalized = dict(task_core)
    workspace = (
        dict(normalized.get("workspace"))
        if isinstance(normalized.get("workspace"), Mapping)
        else {}
    )
    if workspace:
        workspace.setdefault(
            "title",
            str(workspace.get("workspace_id") or "Task Workspace").strip(),
        )
        workspace["items"] = [
            _runtime_replan_normalized_workspace_item(item)
            for item in workspace.get("items", [])
            if isinstance(item, Mapping)
        ]
        normalized["workspace"] = workspace
    normalized["todos"] = [
        _runtime_replan_normalized_todo_item(todo)
        for todo in normalized.get("todos", [])
        if isinstance(todo, Mapping)
    ]
    normalized["checkpoints"] = [
        _runtime_replan_normalized_checkpoint(checkpoint)
        for checkpoint in normalized.get("checkpoints", [])
        if isinstance(checkpoint, Mapping)
    ]
    return normalized


def _runtime_replan_normalized_workspace_item(item: Mapping[str, Any]) -> dict[str, Any]:
    normalized = dict(item)
    title = str(
        normalized.get("title")
        or normalized.get("path")
        or normalized.get("item_id")
        or "workspace item"
    ).strip()
    normalized["title"] = title
    return normalized


def _runtime_replan_normalized_todo_item(todo: Mapping[str, Any]) -> dict[str, Any]:
    normalized = dict(todo)
    title = str(
        normalized.get("title")
        or normalized.get("step_id")
        or normalized.get("tool_name")
        or normalized.get("todo_id")
        or "task todo"
    ).strip()
    normalized["title"] = title
    return normalized


def _runtime_replan_normalized_checkpoint(
    checkpoint: Mapping[str, Any],
) -> dict[str, Any]:
    normalized = dict(checkpoint)
    title = str(
        normalized.get("title")
        or normalized.get("after_step_id")
        or normalized.get("checkpoint_id")
        or "task checkpoint"
    ).strip()
    normalized["title"] = title
    return normalized


def _runtime_workspace_item_summaries(items: Any) -> list[dict[str, Any]]:
    if not isinstance(items, list):
        return []
    summaries: list[dict[str, Any]] = []
    for item in items:
        if not isinstance(item, Mapping):
            continue
        summary = {
            "item_id": str(item.get("item_id") or "").strip(),
            "kind": str(item.get("kind") or "").strip(),
            "path": str(item.get("path") or "").strip(),
            "title": str(item.get("title") or "").strip(),
            "status": str(item.get("status") or "").strip(),
        }
        clean_summary = {
            key: value
            for key, value in summary.items()
            if value
        }
        if clean_summary:
            summaries.append(clean_summary)
    return summaries[:20]


def _runtime_workspace_item_key(item: Mapping[str, Any]) -> str:
    for key in ("item_id", "path", "title"):
        value = str(item.get(key) or "").strip()
        if value:
            return value
    return ""


def _selection_payload_with_timeline_fallback(
    selection_payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    payload = dict(selection_payload) if isinstance(selection_payload, Mapping) else {}
    if isinstance(payload.get("followup_target"), Mapping):
        return payload
    for event in reversed(timeline):
        if not isinstance(event, dict):
            continue
        if (
            _runtime_planner_base_event_type(str(event.get("event") or "").strip())
            != "agent.plan.selection"
        ):
            continue
        target = event.get("followup_target")
        if not isinstance(target, Mapping):
            continue
        event_payload = {
            str(key): value
            for key, value in event.items()
            if key not in {"event", "detail", "timestamp"}
        }
        return {**event_payload, **payload, "followup_target": dict(target)}
    return payload


def _tool_result_requests_replan(result: Mapping[str, Any]) -> bool:
    if not isinstance(result, Mapping):
        return False
    if result.get("approval_required") or result.get("blocked_by_user_goal"):
        return False
    if result.get("verification_failed") is True:
        return True
    if result.get("ok") is True:
        return False
    if result.get("ok") is False:
        return True
    if str(result.get("error") or "").strip():
        return True
    returncode = result.get("returncode")
    if returncode not in (None, "", 0, "0"):
        return True
    exit_code = result.get("exit_code")
    if exit_code not in (None, "", 0, "0"):
        return True
    return False


def _runtime_planner_completed_direct_requests_with_unavailable_replan(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    if not _runtime_planner_replan_payloads_only_tool_unavailable(replan_payloads):
        return False
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    primary_requests: list[Mapping[str, Any]] = []
    last_primary_index = -1
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if not tool_name or tool_name not in _DIRECT_DAILY_DESKTOP_TOOLS:
            continue
        if tool_name in _DAILY_DESKTOP_DISCOVERY_TOOLS or tool_name in _DAILY_DESKTOP_VERIFY_TOOLS:
            continue
        primary_requests.append(request)
        last_primary_index = index
    if not primary_requests:
        return False
    if not all(
        _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in primary_requests
    ):
        return False
    trailing_observation_tools = {
        str(request.get("tool") or "").strip()
        for request in requests[last_primary_index + 1 :]
        if str(request.get("tool") or "").strip() in {
            *_DAILY_DESKTOP_DISCOVERY_TOOLS,
            *_DAILY_DESKTOP_VERIFY_TOOLS,
        }
    }
    payloads = [payload for payload in replan_payloads if isinstance(payload, Mapping)]
    if not trailing_observation_tools:
        return bool(payloads) and all(
            _runtime_replan_payload_is_missing_desktop_observation(payload)
            and not _runtime_replan_payload_requires_continuation(payload)
            for payload in payloads
        )
    if not payloads:
        return False
    return all(
        any(tool in trailing_observation_tools for tool in _runtime_replan_payload_tool_candidates(payload))
        for payload in payloads
    )


def _runtime_planner_verified_app_open_before_unavailable_deferred_verification(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> tuple[list[dict[str, Any]], int] | None:
    raw_requests = list(planned_tool_requests)
    if not raw_requests or any(
        not isinstance(request, Mapping) for request in raw_requests
    ):
        return None
    requests = [request for request in raw_requests if isinstance(request, Mapping)]
    raw_payloads = list(replan_payloads)
    if (
        len(raw_payloads) != len(requests)
        or any(not isinstance(payload, Mapping) for payload in raw_payloads)
    ):
        return None
    payloads = [payload for payload in raw_payloads if isinstance(payload, Mapping)]
    if not payloads or any(
        str(payload.get("trigger") or "").strip() != "tool_unavailable"
        for payload in payloads
    ):
        return None

    expected_app_identities: list[str] = []
    for request in requests:
        tool_name = str(request.get("tool") or "").strip()
        request_input = (
            request.get("input")
            if isinstance(request.get("input"), Mapping)
            else None
        )
        app_name = (
            str(request_input.get("app_name") or "").strip()
            if isinstance(request_input, Mapping)
            else ""
        )
        app_identity = _strict_canonical_app_identity(app_name)
        if (
            request.get("continue_to_model") is not True
            or tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS
            or not app_name
            or not app_identity
        ):
            return None
        expected_app_identities.append(app_identity)
    if not _runtime_planner_deferred_verifiers_have_distinct_unavailable_replans(
        requests,
        payloads,
    ):
        return None

    matched_primary_events: list[tuple[int, str, dict[str, Any]]] = []
    used_event_indexes: set[int] = set()
    timeline_end = min(tool_timeline_start, len(timeline))
    for expected_app_identity in expected_app_identities:
        matched_event: tuple[int, str, dict[str, Any], Mapping[str, Any]] | None = None
        for event_index in range(timeline_end - 1, -1, -1):
            if event_index in used_event_indexes:
                continue
            event = timeline[event_index]
            if str(event.get("event") or "").strip() != "agent.tool.call":
                continue
            event_tool = str(event.get("detail") or "").strip()
            if event_tool not in _RUNTIME_REPLAN_NO_REPEAT_AFTER_SUCCESS_TOOLS:
                continue
            event_input = (
                event.get("input_preview")
                if isinstance(event.get("input_preview"), Mapping)
                else {}
            )
            expected_event_app = str(event_input.get("app_name") or "").strip()
            if (
                not expected_event_app
                or _strict_canonical_app_identity(expected_event_app)
                != expected_app_identity
            ):
                continue
            result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
            matched_event = (event_index, event_tool, dict(event_input), result)
            break
        if matched_event is None:
            return None
        event_index, event_tool, event_input, result = matched_event
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        observed_app = str(data.get("app_name") or "").strip()
        if (
            result.get("ok") is not True
            or data.get("launch_verified") is not True
            or not observed_app
            or _strict_canonical_app_identity(observed_app) != expected_app_identity
            or _strict_canonical_app_identity(
                str(event_input.get("app_name") or "")
            )
            != _strict_canonical_app_identity(observed_app)
        ):
            return None
        used_event_indexes.add(event_index)
        matched_primary_events.append((event_index, event_tool, event_input))
    matched_primary_events.sort(key=lambda item: item[0])
    return (
        [
            {
                "tool": event_tool,
                "input": event_input,
                "source": "runtime_planner",
                "planning_reason": "planner_verified_primary_postcondition",
            }
            for _event_index, event_tool, event_input in matched_primary_events
        ],
        matched_primary_events[0][0],
    )


def _strict_canonical_app_identity(value: str) -> str:
    compact = compact_app_alias(value)
    if not compact:
        return ""
    return _STRICT_CANONICAL_APP_IDENTITIES.get(compact, compact)


def _runtime_planner_deferred_verifiers_have_distinct_unavailable_replans(
    requests: list[Mapping[str, Any]],
    payloads: list[Mapping[str, Any]],
) -> bool:
    payload_to_request: dict[int, int] = {}

    def assign(request_index: int, visited_payloads: set[int]) -> bool:
        request = requests[request_index]
        for payload_index, payload in enumerate(payloads):
            if (
                payload_index in visited_payloads
                or not _runtime_planner_replan_matches_deferred_verifier(
                    request,
                    payload,
                )
            ):
                continue
            visited_payloads.add(payload_index)
            previous_request = payload_to_request.get(payload_index)
            if previous_request is None or assign(previous_request, visited_payloads):
                payload_to_request[payload_index] = request_index
                return True
        return False

    return all(assign(index, set()) for index in range(len(requests)))


def _runtime_planner_replan_matches_deferred_verifier(
    request: Mapping[str, Any],
    payload: Mapping[str, Any],
) -> bool:
    metadata = (
        payload.get("metadata")
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    source_tool = str(
        payload.get("source_tool_name")
        or metadata.get("source_tool_name")
        or ""
    ).strip()
    if source_tool != str(request.get("tool") or "").strip():
        return False
    request_step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    payload_step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    if request_step_id and payload_step_id and request_step_id != payload_step_id:
        return False
    input_preview = (
        metadata.get("input_preview")
        if isinstance(metadata.get("input_preview"), Mapping)
        else payload.get("input_preview")
        if isinstance(payload.get("input_preview"), Mapping)
        else {}
    )
    payload_app_name = str(input_preview.get("app_name") or "").strip()
    if not payload_app_name:
        return True
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    return (
        _strict_canonical_app_identity(payload_app_name)
        == _strict_canonical_app_identity(
            str(request_input.get("app_name") or "")
        )
    )


def _runtime_replan_payload_is_missing_desktop_observation(payload: Mapping[str, Any]) -> bool:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    capability_id = str(
        payload.get("target_capability_id")
        or payload.get("capability_id")
        or metadata.get("target_capability_id")
        or metadata.get("capability_id")
        or ""
    ).strip()
    if capability_id == "desktop.app_discovery":
        return True
    return any(
        tool in _DAILY_DESKTOP_DISCOVERY_TOOLS or tool in _DAILY_DESKTOP_VERIFY_TOOLS
        for tool in _runtime_replan_payload_tool_candidates(payload)
    )


def _runtime_planner_completed_direct_requests_with_verification_replan(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    payloads: list[Mapping[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        nested_requests = (
            payload.get("replan_requests")
            if isinstance(payload.get("replan_requests"), list)
            else []
        )
        payloads.extend(item for item in nested_requests if isinstance(item, Mapping))
        if _runtime_replan_payload_tool_candidates(payload):
            payloads.append(payload)
    if not payloads:
        return False
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if any(bool(request.get("continue_to_model")) for request in requests):
        return False
    primary_requests: list[Mapping[str, Any]] = []
    last_primary_index = -1
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if not tool_name or tool_name not in _DIRECT_DAILY_DESKTOP_TOOLS:
            continue
        if tool_name in _DAILY_DESKTOP_DISCOVERY_TOOLS or tool_name in _DAILY_DESKTOP_VERIFY_TOOLS:
            continue
        primary_requests.append(request)
        last_primary_index = index
    if last_primary_index < 0:
        return False
    if not any(
        _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in primary_requests
    ):
        return False

    trailing_verification_tools = {
        str(request.get("tool") or "").strip()
        for request in requests[last_primary_index + 1 :]
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
    }
    if not trailing_verification_tools:
        return False
    if all(
        _runtime_replan_payload_is_trailing_verification_failure(
            payload,
            trailing_verification_tools,
        )
        for payload in payloads
    ):
        return True
    if any(_runtime_replan_payload_requires_continuation(payload) for payload in payloads):
        return False
    return False


def _legacy_chat_direct_local_completed_with_verification_replan(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not any(_request_uses_legacy_chat_direct_local_policy(request) for request in requests):
        return False
    primary_requests = [
        request
        for request in requests
        if str(request.get("tool") or "").strip() in _DIRECT_DAILY_DESKTOP_TOOLS
        and str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_DISCOVERY_TOOLS
        and str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    if not primary_requests:
        return False
    if not all(
        _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in primary_requests
    ):
        return False
    payloads = _flatten_runtime_replan_payloads(replan_payloads)
    if not payloads:
        return False
    return all(
        str(payload.get("trigger") or "").strip() == "verification_failed"
        for payload in payloads
    )


def _legacy_chat_direct_local_result_requests(
    planned_tool_requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    result: list[dict[str, Any]] = []
    for request in planned_tool_requests:
        if not isinstance(request, Mapping):
            continue
        if not _request_uses_legacy_chat_direct_local_policy(request):
            continue
        tool_name = str(request.get("tool") or "").strip()
        if tool_name in _DAILY_DESKTOP_DISCOVERY_TOOLS:
            continue
        if tool_name in _DAILY_DESKTOP_VERIFY_TOOLS:
            continue
        result.append(dict(request))
    return _coalesced_legacy_chat_direct_local_result_requests(result)


def _coalesced_legacy_chat_direct_local_result_requests(
    planned_tool_requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    coalesced: list[dict[str, Any]] = []
    index = 0
    while index < len(planned_tool_requests):
        if index + 2 >= len(planned_tool_requests):
            coalesced.append(planned_tool_requests[index])
            index += 1
            continue
        open_request = planned_tool_requests[index]
        focus_request = planned_tool_requests[index + 1]
        type_request = planned_tool_requests[index + 2]
        open_input = open_request.get("input") if isinstance(open_request.get("input"), Mapping) else {}
        focus_input = focus_request.get("input") if isinstance(focus_request.get("input"), Mapping) else {}
        type_input = type_request.get("input") if isinstance(type_request.get("input"), Mapping) else {}
        app_name = str(open_input.get("app_name") or "").strip()
        text = str(type_input.get("text") or "").strip()
        if (
            str(open_request.get("tool") or "") in {"app.open", "desktop.open_app"}
            and str(focus_request.get("tool") or "") in {"app.focus", "desktop.focus_app"}
            and str(type_request.get("tool") or "") == "desktop.safe_type_text"
            and app_name
            and str(focus_input.get("app_name") or "").strip() == app_name
            and text
        ):
            coalesced.append(
                {
                    **type_request,
                    "tool": "app.open_and_safe_type_text",
                    "input": {"app_name": app_name, "text": text},
                }
            )
            index += 3
            continue
        coalesced.append(planned_tool_requests[index])
        index += 1
    return coalesced


def _request_uses_legacy_chat_direct_local_policy(request: Mapping[str, Any]) -> bool:
    policy = request.get("desktop_execution_policy")
    return (
        isinstance(policy, Mapping)
        and str(policy.get("source") or "").strip() == "legacy_chat_direct_local"
    )


def _flatten_runtime_replan_payloads(
    replan_payloads: Iterable[Mapping[str, Any]],
) -> list[Mapping[str, Any]]:
    payloads: list[Mapping[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        nested_requests = (
            payload.get("replan_requests")
            if isinstance(payload.get("replan_requests"), list)
            else []
        )
        payloads.extend(item for item in nested_requests if isinstance(item, Mapping))
        if _runtime_replan_payload_tool_candidates(payload):
            payloads.append(payload)
    return payloads


_APPROVED_DISPATCH_RECEIPT_ACTIONS = frozenset(
    {
        "dispatch_management",
        "dispatch_shortcut",
        "dispatch_submit",
    }
)

_APPROVAL_DISPATCH_INPUT_CONTEXT_KEYS = frozenset(
    {
        "agent_id",
        "agent_name",
        "capability_id",
        "core_id",
        "decision_id",
        "group_id",
        "group_run_id",
        "intent_kind",
        "member_agent_id",
        "member_agent_name",
        "plan_id",
        "planner_step_id",
        "planning_reason",
        "replan_request_id",
        "replan_signal_ids",
        "replan_trigger",
        "replan_triggers",
        "requires_observation",
        "requires_post_action_verification",
        "run_group_id",
        "runtime_doctrine",
        "runtime_execution_envelope",
        "runtime_execution_metadata",
        "runtime_role",
        "runtime_stage",
        "source",
        "source_run_id",
        "source_runnable_id",
        "source_runnable_name",
        "step_id",
        "task_id",
        "tool_plan_id",
        "workflow_id",
        "workflow_node_id",
        "workflow_node_label",
        "workflow_run_id",
        "workspace_id",
        "yachiyo_execution_envelope",
    }
)


def _approved_tool_post_action_verification_request(
    timeline: list[dict[str, Any]],
    *,
    allowed_tools: list[str],
) -> dict[str, Any]:
    approved_event_index = -1
    approved_event: Mapping[str, Any] | None = None
    for index in range(len(timeline) - 1, -1, -1):
        event = timeline[index]
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            event.get("approval_resume_result_canonical") is True
            and event.get("approved") is True
            and result.get("ok") is True
            and not result.get("approval_required")
        ):
            approved_event_index = index
            approved_event = event
            break
    if approved_event is None:
        return {}

    tool_name = str(
        approved_event.get("detail")
        or approved_event.get("tool")
        or approved_event.get("tool_name")
        or ""
    ).strip()
    plan_id = str(approved_event.get("plan_id") or "").strip()
    step_id = str(
        approved_event.get("step_id") or approved_event.get("planner_step_id") or ""
    ).strip()
    request_id = str(approved_event.get("request_id") or "").strip()
    tool_call_id = str(approved_event.get("tool_call_id") or "").strip()
    approval_id = str(approved_event.get("approval_id") or "").strip()
    decision_id = str(approved_event.get("decision_id") or "").strip()
    if (
        not tool_name
        or not decision_id
        or not plan_id
        or not step_id
        or not request_id
        or not tool_call_id
        or not approval_id
    ):
        # A canonical approval fact without the full production identity can
        # never establish a dispatch receipt.  It may still be observed by a
        # generic verifier below, but is deliberately ineligible for the
        # no-verifier fast path.
        approval_chain_identity_complete = False
    else:
        approval_chain_identity_complete = request_id.startswith(
            f"{plan_id}:request:"
        )

    approval_required_event = next(
        (
            event
            for event in reversed(timeline[:approved_event_index])
            if str(event.get("event") or event.get("event_type") or "").strip()
            in {
                "agent.desktop.intent_approval_required",
                "agent.tool.approval_required",
            }
            and str(
                event.get("detail") or event.get("tool") or event.get("tool_name") or ""
            ).strip()
            == tool_name
            and str(event.get("decision_id") or "").strip() == decision_id
            and str(event.get("plan_id") or "").strip() == plan_id
            and str(event.get("step_id") or event.get("planner_step_id") or "").strip()
            == step_id
            and str(event.get("approval_id") or "").strip() == approval_id
        ),
        None,
    ) if approval_chain_identity_complete else None
    pending_tool_event = next(
        (
            event
            for event in reversed(timeline[:approved_event_index])
            if str(event.get("event") or event.get("event_type") or "").strip()
            == "agent.tool.call"
            and str(event.get("detail") or event.get("tool") or "").strip()
            == tool_name
            and str(event.get("decision_id") or "").strip() == decision_id
            and str(event.get("plan_id") or "").strip() == plan_id
            and str(event.get("step_id") or event.get("planner_step_id") or "").strip()
            == step_id
            and str(event.get("request_id") or "").strip() == request_id
            and str(event.get("tool_call_id") or "").strip() == tool_call_id
            and isinstance(event.get("result"), Mapping)
            and event["result"].get("approval_required") is True
        ),
        None,
    ) if approval_chain_identity_complete else None
    if _runtime_native_receipt_satisfied_action(
        timeline,
        tool_timeline_start=approved_event_index + 1,
        plan_id=plan_id,
        source_step_id=step_id,
        source_request_id=request_id,
        source_tool=tool_name,
    ):
        return {}

    input_preview = (
        approved_event.get("input_preview")
        if isinstance(approved_event.get("input_preview"), Mapping)
        else {}
    )
    result = (
        approved_event.get("result")
        if isinstance(approved_event.get("result"), Mapping)
        else {}
    )
    execution_event = _approved_dispatch_successful_execution_event(
        timeline,
        canonical_index=approved_event_index,
        approval_event=approval_required_event,
        pending_event=pending_tool_event,
        decision_id=decision_id,
        plan_id=plan_id,
        step_id=step_id,
        request_id=request_id,
        tool_call_id=tool_call_id,
        tool_name=tool_name,
        canonical_input=input_preview,
        canonical_result=result,
    )
    if execution_event is not None and _approved_tool_has_exact_dispatch_receipt(
        execution_event,
        tool_name=tool_name,
        input_preview=(
            execution_event.get("input_preview")
            if isinstance(execution_event.get("input_preview"), Mapping)
            else {}
        ),
        result=(
            execution_event.get("result")
            if isinstance(execution_event.get("result"), Mapping)
            else {}
        ),
    ):
        return {}
    source_request = {
        **dict(execution_event or approved_event),
        "tool": tool_name,
        "input": dict(
            execution_event.get("input_preview")
            if isinstance((execution_event or {}).get("input_preview"), Mapping)
            else input_preview
        ),
        "decision_id": decision_id,
        "plan_id": plan_id,
        "step_id": step_id,
        "planner_step_id": step_id,
        "request_id": request_id,
        "tool_call_id": tool_call_id,
        "approval_id": approval_id,
        "requires_post_action_verification": True,
    }
    return _post_action_verification_request(
        tool_name,
        source_request,
        result,
        allowed_tools=allowed_tools,
        remaining_requests=[],
        active_window_target=None,
    )


def _approved_dispatch_successful_execution_event(
    timeline: list[dict[str, Any]],
    *,
    canonical_index: int,
    approval_event: Mapping[str, Any] | None,
    pending_event: Mapping[str, Any] | None,
    decision_id: str,
    plan_id: str,
    step_id: str,
    request_id: str,
    tool_call_id: str,
    tool_name: str,
    canonical_input: Mapping[str, Any],
    canonical_result: Mapping[str, Any],
) -> Mapping[str, Any] | None:
    """Return the real executor event that closes an approval dispatch chain."""

    if approval_event is None or pending_event is None:
        return None
    approval_index = next(
        (index for index, event in enumerate(timeline) if event is approval_event),
        -1,
    )
    pending_index = next(
        (index for index, event in enumerate(timeline) if event is pending_event),
        -1,
    )
    if not (0 <= pending_index < approval_index < canonical_index):
        return None
    canonical_input_signature = _dispatch_tool_input_signature(
        tool_name,
        canonical_input,
    )
    pending_input = (
        pending_event.get("input_preview")
        if isinstance(pending_event.get("input_preview"), Mapping)
        else {}
    )
    approval_input = (
        approval_event.get("input_preview")
        if isinstance(approval_event.get("input_preview"), Mapping)
        else {}
    )
    if (
        not canonical_input_signature
        or _dispatch_tool_input_signature(tool_name, pending_input)
        != canonical_input_signature
        or _dispatch_tool_input_signature(tool_name, approval_input)
        != canonical_input_signature
    ):
        return None
    expected_identity = {
        "decision_id": decision_id,
        "plan_id": plan_id,
        "step_id": step_id,
        "request_id": request_id,
        "tool_call_id": tool_call_id,
    }
    for event in reversed(timeline[approval_index + 1 : canonical_index]):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if event.get("approval_resume_result_canonical") is True:
            continue
        event_tool = str(
            event.get("detail") or event.get("tool") or event.get("tool_name") or ""
        ).strip()
        if event_tool != tool_name:
            continue
        if any(
            str(event.get(key) or "").strip() != value
            for key, value in expected_identity.items()
        ):
            continue
        event_input = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        event_result = (
            event.get("result")
            if isinstance(event.get("result"), Mapping)
            else {}
        )
        if _dispatch_tool_input_signature(tool_name, event_input) != (
            canonical_input_signature
        ):
            continue
        if dict(event_result) != dict(canonical_result):
            continue
        return event
    return None


def _approved_tool_has_exact_dispatch_receipt(
    execution_event: Mapping[str, Any],
    *,
    tool_name: str,
    input_preview: Mapping[str, Any],
    result: Mapping[str, Any],
) -> bool:
    action_target = (
        execution_event.get("action_target")
        if isinstance(execution_event.get("action_target"), Mapping)
        else {}
    )
    dispatch_action = str(action_target.get("action") or "").strip()
    if dispatch_action not in _APPROVED_DISPATCH_RECEIPT_ACTIONS:
        return False
    execution_step_id = str(
        execution_event.get("step_id")
        or execution_event.get("planner_step_id")
        or ""
    ).strip()
    if dispatch_action == "dispatch_management":
        if dict(action_target) != {"action": "dispatch_management"}:
            return False
    elif (
        not execution_step_id
        or str(action_target.get("step_id") or "").strip() != execution_step_id
    ):
        return False
    if dispatch_action != _dispatch_action_for_tool_input(tool_name, input_preview):
        return False
    if (
        result.get("ok") is not True
        or result.get("approval_required")
        or str(result.get("action") or "").strip() != tool_name
    ):
        return False

    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    if dispatch_action == "dispatch_shortcut":
        if tool_name == "desktop.safe_key":
            requested_action = str(input_preview.get("action") or "").strip().lower()
            return bool(
                str(data.get("key_action") or "").strip().lower() == requested_action
                and data.get("repeat_count") == input_preview.get("repeat_count", 1)
            )
        if tool_name == "desktop.safe_scroll":
            return bool(
                str(data.get("direction") or "").strip().lower()
                == str(input_preview.get("direction") or "").strip().lower()
                and data.get("pages") == input_preview.get("pages", 1)
            )
        if tool_name == "desktop.safe_click":
            return bool(
                data.get("x") == input_preview.get("x")
                and data.get("y") == input_preview.get("y")
                and data.get("click_count") == 1
                and data.get("explicit_user_coordinates") is True
            )
        requested_action = str(input_preview.get("action") or "").strip()
        if is_semantic_safe_shortcut(tool_name, input_preview):
            return False
        if requested_action:
            receipt_action = str(
                data.get("shortcut_action") or data.get("requested_action") or ""
            ).strip()
            return bool(receipt_action and receipt_action == requested_action)
        requested_key = str(input_preview.get("key") or "").strip().casefold()
        receipt_key = str(data.get("key") or "").strip().casefold()
        if not requested_key or receipt_key != requested_key:
            return False
        return _dispatch_receipt_modifiers(data.get("modifiers")) == (
            _dispatch_receipt_modifiers(input_preview.get("modifiers"))
        )
    if dispatch_action == "dispatch_submit":
        if tool_name == "desktop.search_submit":
            return _dispatch_key_receipt(data, key="return", modifiers=())
        requested_action = str(input_preview.get("action") or "submit").strip().lower()
        receipt_action = str(data.get("submit_action") or "").strip().lower()
        return bool(
            receipt_action == requested_action
            and _dispatch_key_receipt(data, key="return", modifiers=())
        )
    expected_management = {
        "desktop.hide_app": ("h", ("command",)),
        "desktop.minimize_window": ("m", ("command",)),
        "desktop.close_window": ("w", ("command",)),
        "desktop.quit_app": ("q", ("command",)),
    }.get(tool_name)
    return bool(
        expected_management
        and _dispatch_key_receipt(
            data,
            key=expected_management[0],
            modifiers=expected_management[1],
        )
    )


def _dispatch_action_for_tool_input(
    tool_name: str,
    input_preview: Mapping[str, Any],
) -> str:
    clean_tool = str(tool_name or "").strip()
    if clean_tool in {
        "desktop.hide_app",
        "desktop.minimize_window",
        "desktop.close_window",
        "desktop.quit_app",
    }:
        return (
            "dispatch_management"
            if _dispatch_tool_input_signature(clean_tool, input_preview)
            else ""
        )
    if clean_tool in {"desktop.search_submit", "desktop.submit_foreground"}:
        return (
            "dispatch_submit"
            if _dispatch_tool_input_signature(clean_tool, input_preview)
            else ""
        )
    return "dispatch_shortcut" if _dispatch_tool_input_signature(clean_tool, input_preview) else ""


def _dispatch_tool_input_signature(
    tool_name: str,
    input_preview: Mapping[str, Any],
) -> tuple[Any, ...]:
    clean_tool = str(tool_name or "").strip()
    if clean_tool in {"desktop.hotkey", "desktop.shortcut"}:
        if not _dispatch_input_has_only(input_preview, {"key", "modifiers"}):
            return ()
        key = str(input_preview.get("key") or "").strip().casefold()
        return (
            clean_tool,
            key,
            _dispatch_receipt_modifiers(input_preview.get("modifiers")),
        ) if key else ()
    if clean_tool == "desktop.safe_shortcut":
        if is_semantic_safe_shortcut(clean_tool, input_preview):
            return ()
        if not _dispatch_input_has_only(input_preview, {"action"}):
            return ()
        action = str(input_preview.get("action") or "").strip().lower()
        return (clean_tool, action) if action else ()
    if clean_tool == "desktop.safe_key":
        if not _dispatch_input_has_only(input_preview, {"action", "repeat_count"}):
            return ()
        action = str(input_preview.get("action") or "").strip().lower()
        repeat_count = input_preview.get("repeat_count", 1)
        return (clean_tool, action, repeat_count) if action else ()
    if clean_tool == "desktop.safe_scroll":
        if not _dispatch_input_has_only(input_preview, {"direction", "pages"}):
            return ()
        direction = str(input_preview.get("direction") or "").strip().lower()
        pages = input_preview.get("pages", 1)
        return (clean_tool, direction, pages) if direction in {"up", "down"} else ()
    if clean_tool == "desktop.safe_click":
        if not _dispatch_input_has_only(input_preview, {"x", "y"}):
            return ()
        x = input_preview.get("x")
        y = input_preview.get("y")
        return (clean_tool, x, y) if x is not None and y is not None else ()
    if clean_tool in {
        "desktop.hide_app",
        "desktop.minimize_window",
        "desktop.close_window",
        "desktop.quit_app",
        "desktop.search_submit",
    }:
        return (
            (clean_tool,)
            if _dispatch_input_has_only(input_preview, set())
            else ()
        )
    if clean_tool == "desktop.submit_foreground":
        if not _dispatch_input_has_only(input_preview, {"action"}):
            return ()
        action = str(input_preview.get("action") or "submit").strip().lower()
        return (clean_tool, action) if action in {"send", "submit", "confirm"} else ()
    return ()


def _dispatch_input_has_only(
    input_preview: Mapping[str, Any],
    executable_keys: set[str],
) -> bool:
    return set(input_preview).issubset(
        executable_keys | _APPROVAL_DISPATCH_INPUT_CONTEXT_KEYS
    )


def _dispatch_key_receipt(
    data: Mapping[str, Any],
    *,
    key: str,
    modifiers: Iterable[str],
) -> bool:
    return bool(
        str(data.get("key") or "").strip().casefold() == str(key).casefold()
        and _dispatch_receipt_modifiers(data.get("modifiers"))
        == _dispatch_receipt_modifiers(list(modifiers))
    )


def _dispatch_receipt_modifiers(value: Any) -> tuple[str, ...]:
    return tuple(sorted(item.casefold() for item in _string_list(value)))


def _runtime_planner_completed_exact_verified_discovery_action_chain(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not requests:
        return False
    discovery_requests = [
        request
        for request in requests
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_DISCOVERY_TOOLS
        and str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    verification_requests = [
        request
        for request in requests
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    action_requests = [
        request
        for request in requests
        if request not in discovery_requests and request not in verification_requests
    ]
    if not discovery_requests or len(action_requests) != 1:
        return False
    if len(discovery_requests) + len(verification_requests) + len(action_requests) != len(
        requests
    ):
        return False
    action_request = action_requests[0]
    action_tool = str(action_request.get("tool") or "").strip()
    if action_tool not in _DIRECT_DAILY_DESKTOP_TOOLS:
        return False
    if (
        str(action_request.get("source") or "").strip() != "runtime_planner"
        or str(action_request.get("risk_level") or "").strip() != "low"
        or bool(action_request.get("approval_required"))
        or action_request.get("requires_post_action_verification") is not True
    ):
        return False

    plan_ids = {str(request.get("plan_id") or "").strip() for request in requests}
    if len(plan_ids) != 1 or not next(iter(plan_ids)):
        return False
    plan_id = next(iter(plan_ids))
    for request in requests:
        tool_name = str(request.get("tool") or "").strip()
        if tool_name in _DAILY_DESKTOP_VERIFY_TOOLS:
            continue
        if _exact_runtime_planner_tool_event(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        ) is None:
            return False

    action_step_id = str(
        action_request.get("step_id") or action_request.get("planner_step_id") or ""
    ).strip()
    action_request_id = str(action_request.get("request_id") or "").strip()
    if not action_step_id or not action_request_id:
        return False
    return _runtime_native_receipt_satisfied_action(
        timeline,
        tool_timeline_start=tool_timeline_start,
        plan_id=plan_id,
        source_step_id=action_step_id,
        source_request_id=action_request_id,
        source_tool=action_tool,
    )


def _exact_runtime_planner_tool_event(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> Mapping[str, Any] | None:
    tool_name = str(request.get("tool") or "").strip()
    plan_id = str(request.get("plan_id") or "").strip()
    step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    request_id = str(request.get("request_id") or "").strip()
    if not tool_name or not plan_id or not step_id or not request_id:
        return None
    if not request_id.startswith(f"{plan_id}:request:"):
        return None
    for event in reversed(timeline[max(0, int(tool_timeline_start or 0)) :]):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        if str(event.get("step_id") or event.get("planner_step_id") or "").strip() != step_id:
            continue
        if str(event.get("request_id") or "").strip() != request_id:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is True and not result.get("approval_required"):
            return event
    return None


def _latest_exact_runtime_planner_tool_event(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> Mapping[str, Any] | None:
    tool_name = str(request.get("tool") or "").strip()
    plan_id = str(request.get("plan_id") or "").strip()
    step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    request_id = str(request.get("request_id") or "").strip()
    if not tool_name or not plan_id or not step_id or not request_id:
        return None
    if not request_id.startswith(f"{plan_id}:request:"):
        return None
    for event in reversed(timeline[max(0, int(tool_timeline_start or 0)) :]):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        if str(event.get("step_id") or event.get("planner_step_id") or "").strip() != step_id:
            continue
        if str(event.get("request_id") or "").strip() != request_id:
            continue
        return event
    return None


def _latest_scoped_runtime_tool_event(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> Mapping[str, Any] | None:
    exact_event = _latest_exact_runtime_planner_tool_event(
        request,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    if exact_event is not None:
        return exact_event
    if any(
        str(request.get(key) or "").strip()
        for key in ("plan_id", "step_id", "planner_step_id", "request_id")
    ):
        return None
    tool_name = str(request.get("tool") or "").strip()
    source = str(request.get("source") or "").strip()
    planning_reason = str(request.get("planning_reason") or "").strip()
    if not tool_name or not source or not planning_reason:
        return None
    if not (
        (source == "daily_desktop_metadata" and planning_reason == "structured_recovery_metadata")
        or (
            source == "runtime_verification"
            and planning_reason.startswith("runtime_")
            and planning_reason.endswith("_verification")
        )
    ):
        return None
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    target_app_name = str(request.get("target_app_name") or "").strip()
    for event in reversed(timeline[max(0, int(tool_timeline_start or 0)) :]):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        if str(event.get("source") or "").strip() != source:
            continue
        if str(event.get("planning_reason") or "").strip() != planning_reason:
            continue
        event_input = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        if any(
            str(event_input.get(key) or "").strip()
            != str(request_input.get(key) or "").strip()
            for key in ("app_name", "action", "target", "path", "url")
            if str(request_input.get(key) or "").strip()
        ):
            continue
        if target_app_name and str(event.get("target_app_name") or "").strip() != target_app_name:
            continue
        return event
    return None


def _runtime_native_receipt_satisfied_action(
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
    plan_id: str,
    source_step_id: str,
    source_request_id: str,
    source_tool: str,
) -> bool:
    for event in timeline[max(0, int(tool_timeline_start or 0)) :]:
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.post_action_verification.satisfied"
        ):
            continue
        if str(event.get("source") or "").strip() != "runtime_native_postcondition_receipt":
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        verifier_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        verifier_request_id = str(event.get("request_id") or "").strip()
        if not verifier_step_id or not verifier_request_id:
            continue
        if (
            not verifier_request_id.startswith(f"{plan_id}:request:")
            or verifier_request_id == source_request_id
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        event_source_request_id = str(
            event.get("source_request_id") or result.get("source_request_id") or ""
        ).strip()
        if event_source_request_id and event_source_request_id != source_request_id:
            continue
        if (
            result.get("ok") is True
            and result.get("postcondition_verified") is True
            and result.get("verification_satisfied_by_native_receipt") is True
            and str(result.get("source_step_id") or "").strip() == source_step_id
            and str(result.get("source_tool") or "").strip() == source_tool
        ):
            return True
    return False


def _runtime_planner_completed_terminal_direct_requests_with_trailing_verification(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    terminal_primary_index = -1
    terminal_primary_request: Mapping[str, Any] | None = None
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if tool_name in _DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS:
            terminal_primary_index = index
            terminal_primary_request = request
    if terminal_primary_index < 0 or terminal_primary_request is None:
        return False
    if not _runtime_planner_tool_request_completed(
        terminal_primary_request,
        timeline,
        tool_timeline_start=tool_timeline_start,
    ):
        return False

    allowed_trailing_tools = {*_DAILY_DESKTOP_DISCOVERY_TOOLS, *_DAILY_DESKTOP_VERIFY_TOOLS}
    trailing_requests = requests[terminal_primary_index + 1 :]
    if any(
        str(request.get("tool") or "").strip() not in allowed_trailing_tools
        for request in trailing_requests
    ):
        return False
    return any(
        str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
        for request in trailing_requests
    )


def _runtime_planner_completed_terminal_direct_requests_with_verification_replan(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    payloads: list[Mapping[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        nested_requests = (
            payload.get("replan_requests")
            if isinstance(payload.get("replan_requests"), list)
            else []
        )
        payloads.extend(item for item in nested_requests if isinstance(item, Mapping))
        if _runtime_replan_payload_tool_candidates(payload):
            payloads.append(payload)
    if not payloads:
        return False

    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    terminal_primary_index = -1
    terminal_primary_request: Mapping[str, Any] | None = None
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if tool_name in _DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS:
            terminal_primary_index = index
            terminal_primary_request = request
    if terminal_primary_index < 0 or terminal_primary_request is None:
        return False
    if not _runtime_planner_tool_request_completed(
        terminal_primary_request,
        timeline,
        tool_timeline_start=tool_timeline_start,
    ):
        return False

    trailing_requests = requests[terminal_primary_index + 1 :]
    if any(
        str(request.get("tool") or "").strip()
        not in {*_DAILY_DESKTOP_DISCOVERY_TOOLS, *_DAILY_DESKTOP_VERIFY_TOOLS}
        for request in trailing_requests
    ):
        return False
    trailing_verification_tools = {
        str(request.get("tool") or "").strip()
        for request in trailing_requests
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
    }
    if not trailing_verification_tools:
        return False
    return all(
        _runtime_replan_payload_is_trailing_verification_failure(
            payload,
            trailing_verification_tools,
        )
        for payload in payloads
    )


def _runtime_planner_trailing_verification_failure_outcome(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> dict[str, Any] | None:
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not requests:
        return None
    plan_ids = {str(request.get("plan_id") or "").strip() for request in requests}
    if len(plan_ids) != 1:
        return None

    primary_index = -1
    primary_request: Mapping[str, Any] | None = None
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if (
            tool_name in _DIRECT_DAILY_DESKTOP_TOOLS
            and tool_name not in _DAILY_DESKTOP_DISCOVERY_TOOLS
            and tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS
        ):
            primary_index = index
            primary_request = request
    if primary_index < 0 or primary_request is None:
        return None
    primary_tool = str(primary_request.get("tool") or "").strip()
    if primary_tool in _DIRECT_DAILY_DESKTOP_TERMINAL_ACTION_TOOLS:
        return None
    primary_event = _latest_scoped_runtime_tool_event(
        primary_request,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    if primary_event is None:
        return None

    trailing_requests = requests[primary_index + 1 :]
    if not trailing_requests or any(
        str(request.get("tool") or "").strip()
        not in {*_DAILY_DESKTOP_DISCOVERY_TOOLS, *_DAILY_DESKTOP_VERIFY_TOOLS}
        for request in trailing_requests
    ):
        return None
    verification_requests = [
        request
        for request in trailing_requests
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    if not verification_requests:
        return None
    if not next(iter(plan_ids)):
        if not (
            str(primary_request.get("source") or "").strip() == "daily_desktop_metadata"
            and str(primary_request.get("planning_reason") or "").strip()
            == "structured_recovery_metadata"
            and all(
                str(request.get("source") or "").strip() == "runtime_verification"
                for request in verification_requests
            )
        ):
            return None
    verification_tools = {
        str(request.get("tool") or "").strip() for request in verification_requests
    }

    payloads: list[Mapping[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        nested_requests = (
            payload.get("replan_requests")
            if isinstance(payload.get("replan_requests"), list)
            else []
        )
        payloads.extend(item for item in nested_requests if isinstance(item, Mapping))
        if _runtime_replan_payload_tool_candidates(payload):
            payloads.append(payload)
    if not payloads or not all(
        _runtime_replan_payload_is_trailing_verification_failure(
            payload,
            verification_tools,
        )
        for payload in payloads
    ):
        return None

    primary_step_id = str(
        primary_request.get("step_id") or primary_request.get("planner_step_id") or ""
    ).strip()
    failed_verifier_event: Mapping[str, Any] | None = None
    for verification_request in verification_requests:
        dependency_ids = {
            str(step_id or "").strip()
            for step_id in verification_request.get("depends_on", [])
            if str(step_id or "").strip()
        }
        desktop_loop = (
            verification_request.get("desktop_loop")
            if isinstance(verification_request.get("desktop_loop"), Mapping)
            else {}
        )
        dependency_ids.update(
            str(step_id or "").strip()
            for step_id in desktop_loop.get("verification_target_step_ids", [])
            if str(step_id or "").strip()
        )
        if primary_step_id and dependency_ids and primary_step_id not in dependency_ids:
            continue
        verifier_event = _latest_scoped_runtime_tool_event(
            verification_request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        if verifier_event is None:
            continue
        verifier_result = (
            verifier_event.get("result")
            if isinstance(verifier_event.get("result"), Mapping)
            else {}
        )
        if verifier_result.get("ok") is False or verifier_result.get(
            "verification_failed"
        ) is True:
            failed_verifier_event = verifier_event
            break
    if failed_verifier_event is None:
        return None

    primary_input = (
        primary_event.get("input_preview")
        if isinstance(primary_event.get("input_preview"), Mapping)
        else primary_request.get("input")
        if isinstance(primary_request.get("input"), Mapping)
        else {}
    )
    primary_result = (
        primary_event.get("result")
        if isinstance(primary_event.get("result"), Mapping)
        else {}
    )
    if primary_tool in {"app.open", "desktop.open_app"}:
        app_name = _payload_text(primary_result, dict(primary_input), "app_name")
        target = _display_target_name(app_name, "的请求")
        summary = (
            f"已向 macOS 发送打开{target}，但未能确认它已成为前台应用；请确认后重试。"
            if target
            else "已发送打开应用的请求，但未能确认它已成为前台应用；请确认后重试。"
        )
    else:
        action_summary = str(primary_result.get("summary") or "已执行桌面操作").strip().rstrip(
            "。"
        )
        summary = f"{action_summary}，但后续验证失败；请确认后重试。"
    return {
        "summary": summary,
        "tool": primary_tool,
        "input_preview": dict(primary_input),
        "tool_call_id": str(
            primary_event.get("tool_call_id") or primary_event.get("id") or ""
        ).strip(),
    }


def _runtime_clipboard_transfer_verification_failure(
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> dict[str, Any] | None:
    """Project an exact copy/paste readback failure as an honest direct result."""

    failure_reasons = {
        "clipboard_copy_source_unbound",
        "clipboard_copy_source_unverified",
        "clipboard_paste_source_or_readback_unverified",
    }
    verifier_event: Mapping[str, Any] | None = None
    verifier_result: Mapping[str, Any] = {}
    for event in reversed(timeline[max(0, int(tool_timeline_start or 0)) :]):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            result.get("verification_failed") is True
            and str(result.get("reason") or "").strip() in failure_reasons
        ):
            verifier_event = event
            verifier_result = result
            break
    if verifier_event is None:
        return None
    source_tool_call_id = str(
        verifier_result.get("source_tool_call_id")
        or verifier_event.get("source_tool_call_id")
        or ""
    ).strip()
    if not source_tool_call_id:
        return None
    source_event = next(
        (
            event
            for event in reversed(
                timeline[max(0, int(tool_timeline_start or 0)) :]
            )
            if str(event.get("event") or event.get("event_type") or "").strip()
            == "agent.tool.call"
            and str(event.get("tool_call_id") or "").strip()
            == source_tool_call_id
        ),
        None,
    )
    if source_event is None:
        return None
    source_tool = str(
        source_event.get("detail") or source_event.get("tool") or ""
    ).strip()
    source_input = (
        source_event.get("input_preview")
        if isinstance(source_event.get("input_preview"), Mapping)
        else {}
    )
    action = str(source_input.get("action") or "").strip().lower()
    if source_tool not in {
        "desktop.safe_shortcut",
        "desktop.shortcut",
        "app.open_and_safe_shortcut",
        "app.focus_and_safe_shortcut",
    } or action not in {"copy", "paste"}:
        return None
    summary = (
        "已执行复制，但无法确认剪贴板内容来自当前选区；任务已停止。"
        if action == "copy"
        else "已执行粘贴，但无法确认剪贴板内容与目标输入框回读一致；任务已停止。"
    )
    return {
        "summary": summary,
        "tool": source_tool,
        "input_preview": dict(source_input),
        "tool_call_id": source_tool_call_id,
    }


def _runtime_planner_followup_requests_are_only_verification(
    planned_tool_requests: Iterable[Mapping[str, Any]],
) -> bool:
    followup_requests = [
        request
        for request in planned_tool_requests
        if isinstance(request, Mapping) and bool(request.get("continue_to_model"))
    ]
    if not followup_requests:
        return False
    for request in followup_requests:
        tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
        if tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS:
            return False
        if str(request.get("deferred_tool") or "").strip():
            return False
        if isinstance(request.get("deferred_input"), Mapping) and request.get(
            "deferred_input"
        ):
            return False
        if _mapping_list(request.get("deferred_continuation")):
            return False
        if _tool_request_requires_model_materialization(request):
            return False
    return True


def _runtime_planner_completed_direct_requests_with_successful_verification(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not requests:
        return False
    primary_indexes = [
        index
        for index, request in enumerate(requests)
        if (
            str(request.get("tool") or "").strip() in _DIRECT_DAILY_DESKTOP_TOOLS
            and str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_DISCOVERY_TOOLS
            and str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_VERIFY_TOOLS
        )
    ]
    if not primary_indexes:
        # A full direct plan can be executed in multiple Runner batches.  The
        # operate batch may already have produced a strictly correlated native
        # receipt, allowing the later verifier batch to project a satisfied
        # verification without repeating the OS read.  That projection is the
        # terminal evidence; it must not trigger a model call merely because
        # the primary request lives in an earlier batch.
        return _runtime_planner_native_receipt_satisfied_verifications(
            requests,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
    last_primary_index = primary_indexes[-1]
    verification_requests = [
        request
        for request in requests[last_primary_index + 1 :]
        if str(request.get("tool") or "").strip() in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    if verification_requests and _runtime_planner_native_receipt_satisfied_verifications(
        verification_requests,
        timeline,
        tool_timeline_start=tool_timeline_start,
    ):
        return True
    if not all(
        _runtime_planner_tool_request_completed(
            requests[index],
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for index in primary_indexes
    ):
        return False
    if not verification_requests:
        return False
    return all(
        _runtime_planner_direct_verification_request_succeeded(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in verification_requests
    )


def _runtime_planner_native_receipt_satisfied_verifications(
    requests: list[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    verification_requests = [
        request
        for request in requests
        if str(request.get("tool") or request.get("tool_name") or "").strip()
        in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    if not verification_requests:
        return False
    if any(
        str(request.get("tool") or request.get("tool_name") or "").strip()
        not in {*_DAILY_DESKTOP_DISCOVERY_TOOLS, *_DAILY_DESKTOP_VERIFY_TOOLS}
        for request in requests
    ):
        return False
    events = timeline[max(0, int(tool_timeline_start or 0)) :]
    for request in verification_requests:
        tool_name = str(
            request.get("tool") or request.get("tool_name") or ""
        ).strip()
        plan_id = str(request.get("plan_id") or "").strip()
        step_id = str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        if not plan_id or not step_id:
            return False
        matched = False
        for event in events:
            if str(event.get("event") or event.get("event_type") or "").strip() != (
                "agent.post_action_verification.satisfied"
            ):
                continue
            event_tool = str(
                event.get("detail") or event.get("tool") or event.get("tool_name") or ""
            ).strip()
            if event_tool != tool_name:
                continue
            if str(event.get("plan_id") or "").strip() != plan_id:
                continue
            if str(
                event.get("step_id") or event.get("planner_step_id") or ""
            ).strip() != step_id:
                continue
            result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
            if (
                result.get("ok") is True
                and result.get("postcondition_verified") is True
                and result.get("verification_satisfied_by_native_receipt") is True
            ):
                matched = True
                break
        if not matched:
            return False
    return True


def _runtime_planner_direct_verification_request_succeeded(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    tool_name = str(request.get("tool") or "").strip()
    if tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS:
        return False
    for event_index, event in enumerate(timeline[tool_timeline_start:]):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        event_tool = str(event.get("detail") or "").strip()
        if event_tool != tool_name and not _followup_plan_tools_match(event_tool, tool_name):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True or result.get("approval_required"):
            return False
        if tool_name not in {"desktop.ui_elements", "desktop.read_ui"}:
            return True
        prior_tool_events = [
            item
            for item in timeline[tool_timeline_start : tool_timeline_start + event_index]
            if isinstance(item, dict)
            and str(item.get("event") or "").strip() == "agent.tool.call"
        ]
        context = _runtime_planner_verification_context(prior_tool_events, event)
        return not _tool_result_verification_weak(
            tool_name,
            result,
        ) and not _tool_result_verification_target_missing(
            tool_name,
            result,
            target_text=str(context.get("target_search_text") or ""),
            role_filter=str(context.get("ui_role_filter") or ""),
        )
    return False


def _runtime_replan_payload_requires_continuation(payload: Mapping[str, Any]) -> bool:
    if _replan_payload_is_focus_mismatch(payload):
        return True
    if _replan_ui_observed_action_target(payload):
        return True
    if isinstance(payload.get("followup_target"), Mapping) and payload.get(
        "followup_target"
    ):
        return True
    if isinstance(payload.get("action_target"), Mapping) and payload.get(
        "action_target"
    ):
        return True
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    trigger = str(payload.get("trigger") or metadata.get("signal_trigger") or "").strip()
    if trigger == "verification_failed" and any(
        str(payload.get(key) or metadata.get(key) or "").strip()
        for key in (
            "target_app_name",
            "target_app_query",
            "target_search_text",
            "recovery_observation_goal",
        )
    ):
        return True
    return bool(
        isinstance(metadata.get("followup_target"), Mapping)
        and metadata.get("followup_target")
    ) or bool(
        isinstance(metadata.get("action_target"), Mapping)
        and metadata.get("action_target")
    )


def _runtime_replan_payload_is_trailing_verification_failure(
    payload: Mapping[str, Any],
    trailing_verification_tools: set[str],
) -> bool:
    source_tool_candidates = _runtime_replan_payload_tool_candidates(payload)
    source_tool = next(
        (tool for tool in source_tool_candidates if tool in trailing_verification_tools),
        source_tool_candidates[0] if source_tool_candidates else "",
    )
    if source_tool not in trailing_verification_tools:
        return False
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    trigger = str(payload.get("trigger") or metadata.get("signal_trigger") or "").strip()
    if trigger and trigger not in {"tool_failure", "verification_failed"}:
        return False
    source_step = str(payload.get("source_step_id") or metadata.get("step_id") or "").strip()
    if source_step and "verify" not in source_step and trigger != "verification_failed":
        return False
    return True


def _runtime_replan_payload_tool_candidates(payload: Mapping[str, Any]) -> list[str]:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    action_target = (
        payload.get("action_target") if isinstance(payload.get("action_target"), Mapping) else {}
    )
    observation_retry = (
        payload.get("observation_retry")
        if isinstance(payload.get("observation_retry"), Mapping)
        else {}
    )
    return [
        str(value or "").strip()
        for value in (
            payload.get("source_tool_name"),
            payload.get("tool_name"),
            payload.get("tool"),
            metadata.get("source_tool_name"),
            metadata.get("tool_name"),
            metadata.get("tool"),
            action_target.get("verification_tool"),
            action_target.get("tool_name"),
            observation_retry.get("tool"),
        )
        if str(value or "").strip()
    ]


def _runtime_planner_completed_discovered_app_direct_action(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    selection_payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return False
    if str(target.get("target_action") or "").strip() not in {
        "open_app",
        "open",
        "focus_app",
        "focus",
        "open_path_with_app",
        "open_path_with_selected_app",
    }:
        return False
    if _discovered_app_target_requires_model_followup(target):
        return False

    requests = [
        request
        for request in planned_tool_requests
        if isinstance(request, Mapping)
    ]
    followup_requests = [
        request
        for request in requests
        if bool(request.get("continue_to_model"))
    ]
    if not followup_requests:
        return False
    if any(
        str(request.get("tool") or "").strip() not in _DISCOVERED_APP_SELECTION_SOURCES
        for request in followup_requests
    ):
        return False

    indexed_action_requests = [
        (index, request)
        for index, request in enumerate(requests)
        if str(request.get("tool") or "").strip() in _DISCOVERED_APP_DIRECT_COMPLETION_TOOLS
        and _request_uses_discovered_app_resolution(request)
    ]
    completed_action_indexes = [
        index
        for index, request in indexed_action_requests
        if _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
    ]
    if not completed_action_indexes:
        return False
    first_completed_action_index = min(completed_action_indexes)
    completed_action_request = next(
        request
        for index, request in indexed_action_requests
        if index == first_completed_action_index
    )
    expected_app_name = _expected_app_name_for_discovered_app_direct_action(
        completed_action_request,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    verification_requests = [
        request
        for index, request in enumerate(requests)
        if index > first_completed_action_index
        and str(request.get("tool") or "").strip()
        in _DISCOVERED_APP_DIRECT_VERIFICATION_TOOLS
    ]
    if not verification_requests:
        return True
    return all(
        _runtime_planner_verification_request_completed(
            request,
            expected_app_name,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in verification_requests
    )


def _discovered_app_target_requires_model_followup(target: Mapping[str, Any]) -> bool:
    if isinstance(target.get("post_action_observation"), Mapping):
        return True
    if isinstance(target.get("creative_canvas"), Mapping):
        return True
    if isinstance(target.get("communication_compose"), Mapping):
        return True
    if isinstance(target.get("app_search"), Mapping):
        return True
    if str(target.get("compose_text") or "").strip():
        return True
    if str(target.get("pending_user_action") or "").strip():
        return True
    return str(target.get("body_source") or "").strip() == "model_generated_content"


def _discovered_app_selection_source(value: Any) -> str:
    clean = str(value or "").strip()
    if clean in _DISCOVERED_APP_SELECTION_SOURCES:
        return clean
    return ""


def _discovered_app_placeholder_source(value: Any) -> str:
    clean = str(value or "").strip()
    for source, placeholder in _DISCOVERED_APP_PLACEHOLDERS.items():
        if clean == placeholder:
            return source
    return ""


def _request_uses_discovered_app_resolution(request: Mapping[str, Any]) -> bool:
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    resolution = (
        request.get("input_resolution")
        if isinstance(request.get("input_resolution"), Mapping)
        else {}
    )
    return (
        bool(_discovered_app_placeholder_source(payload.get("app_name")))
        or bool(_discovered_app_selection_source(payload.get("selection_source")))
        or bool(_discovered_app_selection_source(resolution.get("source_tool")))
    )


def _runtime_planner_tool_request_completed(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    tool_name = str(request.get("tool") or "").strip()
    if not tool_name:
        return False
    for event in timeline[tool_timeline_start:]:
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        event_tool = str(event.get("detail") or "").strip()
        if event_tool != tool_name and not _followup_plan_tools_match(
            event_tool,
            tool_name,
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is True and not result.get("approval_required"):
            return True
    return False


def _expected_app_name_for_discovered_app_direct_action(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> str:
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    resolution = (
        request.get("input_resolution")
        if isinstance(request.get("input_resolution"), Mapping)
        else {}
    )
    resolved_app = str(resolution.get("resolved_app_name") or "").strip()
    if resolved_app:
        return resolved_app
    raw_app = str(payload.get("app_name") or "").strip()
    selection_source = (
        _discovered_app_selection_source(payload.get("selection_source"))
        or _discovered_app_placeholder_source(raw_app)
    )
    query = str(payload.get("query") or "").strip()
    if selection_source and query:
        discovered = _discovered_app_name_for_query(
            timeline[tool_timeline_start:],
            query,
            source_tool=selection_source,
        )
        if discovered:
            return discovered
    if raw_app and not _discovered_app_placeholder_source(raw_app):
        return raw_app
    tool_name = str(request.get("tool") or "").strip()
    for event in timeline[tool_timeline_start:]:
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("detail") or "").strip() != tool_name:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        event_app = str(data.get("app_name") or "").strip()
        if event_app:
            return event_app
    return ""


def _runtime_planner_verification_request_completed(
    request: Mapping[str, Any],
    expected_app_name: str,
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    tool_name = str(request.get("tool") or "").strip()
    if tool_name != "desktop.active_window" or not str(expected_app_name or "").strip():
        return _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
    expected_compact = compact_app_name_hint(expected_app_name)
    if not expected_compact:
        return _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
    for event in timeline[tool_timeline_start:]:
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("detail") or "").strip() != tool_name:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True or result.get("approval_required"):
            continue
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        active_app = str(data.get("app_name") or "").strip()
        if active_app and compact_app_name_hint(active_app) == expected_compact:
            return True
    return False


def _runtime_planner_replan_payloads_only_tool_unavailable(
    replan_payloads: Iterable[Mapping[str, Any]],
) -> bool:
    payloads = [payload for payload in replan_payloads if isinstance(payload, Mapping)]
    if not payloads:
        return False
    return all(str(payload.get("trigger") or "").strip() == "tool_unavailable" for payload in payloads)


def _runtime_replan_failure_metadata(result: Mapping[str, Any]) -> dict[str, Any]:
    if not isinstance(result, Mapping):
        return {}
    result_dict = dict(result)
    metadata: dict[str, Any] = {}
    recovery_actions = _recovery_actions(result_dict)
    if recovery_actions:
        metadata["recovery_actions"] = recovery_actions
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    for key in (
        "recommended_tools",
        "blocking_conditions",
        "permission_targets",
        "missing_permissions",
    ):
        values = _string_list(result.get(key)) or _string_list(data.get(key))
        if values:
            metadata[key] = values
    blocking_condition = str(
        result.get("blocking_condition") or data.get("blocking_condition") or ""
    ).strip()
    if blocking_condition:
        blocking_conditions = _string_list(metadata.get("blocking_conditions"))
        if blocking_condition not in blocking_conditions:
            blocking_conditions.append(blocking_condition)
        metadata["blocking_conditions"] = blocking_conditions
    expected_app_name = str(
        result.get("expected_app_name") or data.get("expected_app_name") or ""
    ).strip()
    if expected_app_name:
        metadata["expected_app_name"] = expected_app_name
        metadata.setdefault("target_app_name", expected_app_name)
    active_app_name = str(
        result.get("active_app_name") or data.get("active_app_name") or ""
    ).strip()
    if active_app_name:
        metadata["active_app_name"] = active_app_name
    return metadata


def _runtime_planner_tool_event_step_payloads(
    decision: Any,
    tool_events: list[dict[str, Any]],
) -> dict[int, dict[str, Any]]:
    plan = getattr(decision, "plan", None)
    tool_plan = getattr(plan, "tool_plan", None)
    steps = list(getattr(tool_plan, "steps", []) or [])
    if not steps or not tool_events:
        return {}
    by_event_index: dict[int, dict[str, Any]] = {}
    step_cursor = 0
    for event_index, event in enumerate(tool_events):
        tool_name = str(event.get("detail") or "").strip()
        if not tool_name:
            continue
        explicit_step_id = str(event.get("step_id") or event.get("planner_step_id") or "").strip()
        match_index, step = _runtime_planner_matching_tool_event_step(
            steps,
            tool_name=tool_name,
            step_cursor=step_cursor,
            explicit_step_id=explicit_step_id,
        )
        if step is None:
            continue
        if match_index >= step_cursor:
            step_cursor = match_index + 1
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        step_payload = _runtime_planner_step_failure_trace_payload(step)
        _merge_runtime_planner_failure_context(
            step_payload,
            _runtime_planner_step_verification_failure_context(
                decision,
                step,
                event,
                result,
            ),
        )
        by_event_index[event_index] = step_payload
    return by_event_index


def _runtime_planner_matching_tool_event_step(
    steps: list[Any],
    *,
    tool_name: str,
    step_cursor: int,
    explicit_step_id: str = "",
) -> tuple[int, Any | None]:
    if explicit_step_id:
        for index, step in enumerate(steps):
            if str(getattr(step, "step_id", "") or "").strip() == explicit_step_id:
                return index, step
    for index in range(max(0, step_cursor), len(steps)):
        step = steps[index]
        if str(getattr(step, "tool_name", "") or "").strip() == tool_name:
            return index, step
    for index, step in enumerate(steps):
        if index >= step_cursor:
            continue
        if str(getattr(step, "tool_name", "") or "").strip() == tool_name:
            return index, step
    return -1, None


def _runtime_planner_step_failure_trace_payload(step: Any) -> dict[str, Any]:
    step_id = str(getattr(step, "step_id", "") or "").strip()
    capability_id = str(getattr(step, "capability_id", "") or "").strip()
    tool_name = str(getattr(step, "tool_name", "") or "").strip()
    payload: dict[str, Any] = {}
    if step_id:
        payload["source_step_id"] = step_id
        payload["planner_step_id"] = step_id
    if tool_name:
        payload["planned_tool_name"] = tool_name
    if capability_id:
        payload["capability_id"] = capability_id
    metadata = {
        key: value
        for key, value in {
            "step_id": step_id,
            "capability_id": capability_id,
            "planned_tool_name": tool_name,
            "step_title": str(getattr(step, "title", "") or "").strip(),
            "step_action": str(getattr(step, "action", "") or "").strip(),
            "risk_level": str(getattr(step, "risk_level", "") or "").strip(),
            "approval_required": bool(getattr(step, "approval_required", False)),
        }.items()
        if value not in ("", None)
    }
    fallback_tools = [
        str(tool or "").strip()
        for tool in list(getattr(step, "fallback_tools", []) or [])
        if str(tool or "").strip()
    ]
    if fallback_tools:
        metadata["fallback_tools"] = fallback_tools
    if metadata:
        payload["metadata"] = metadata
    return payload


def _merge_runtime_planner_failure_context(
    payload: dict[str, Any],
    context: Mapping[str, Any],
) -> None:
    if not context:
        return
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    merged_metadata = dict(metadata)
    for key, value in context.items():
        if value in (None, "", [], {}):
            continue
        if key == "task_verification_targets":
            payload.setdefault(key, [dict(item) for item in _mapping_list(value)])
            merged_metadata.setdefault(key, [dict(item) for item in _mapping_list(value)])
            continue
        if key == "verification_targets":
            payload.setdefault(key, [dict(item) for item in _mapping_list(value)])
            merged_metadata.setdefault(key, [dict(item) for item in _mapping_list(value)])
            continue
        if isinstance(value, Mapping):
            payload.setdefault(key, dict(value))
            merged_metadata.setdefault(key, dict(value))
        else:
            payload.setdefault(key, value)
            merged_metadata.setdefault(key, value)
    if merged_metadata:
        payload["metadata"] = merged_metadata


def _runtime_planner_step_verification_failure_context(
    decision: Any,
    step: Any,
    event: Mapping[str, Any],
    result: Mapping[str, Any],
) -> dict[str, Any]:
    targets = _runtime_planner_task_verification_targets_for_step(decision, step)
    if not targets:
        return {}
    step_id = str(getattr(step, "step_id", "") or "").strip()
    tool_name = str(getattr(step, "tool_name", "") or "").strip()
    input_preview = event.get("input_preview") if isinstance(event.get("input_preview"), Mapping) else {}
    result_preview = _task_progress_result_preview(result)
    compact_targets = _runtime_planner_compact_task_verification_targets(targets)
    action_target = _runtime_planner_verification_action_target(targets, input_preview)
    observation_evidence = {
        "source_tool": tool_name,
        "source_step_id": step_id,
        "verification_failed": True,
        "input_preview": dict(input_preview),
        "result_preview": result_preview,
    }
    failure_detail = _runtime_planner_failure_detail(result)
    if failure_detail:
        observation_evidence["failure_detail"] = failure_detail
    observation_retry = {
        "tool": tool_name,
        "input": dict(input_preview),
        "source_step_id": step_id,
        "reason": "verification_failed",
    }
    context = {
        "verification_targets": compact_targets,
        "task_verification_targets": targets,
        "action_target": action_target,
        "observation_evidence": _runtime_planner_non_empty_mapping(observation_evidence),
        "observation_retry": _runtime_planner_non_empty_mapping(observation_retry),
        "recovery_action_label": "Re-observe failed verification target",
    }
    target_app_name = _runtime_planner_first_text(
        ("target_app_name", "app_name", "expected_app_name"),
        action_target,
        input_preview,
        *compact_targets,
    )
    target_app_query = _runtime_planner_first_text(
        ("target_app_query", "app_query", "query"),
        action_target,
        input_preview,
        *compact_targets,
    )
    target_search_text = _runtime_planner_first_text(
        ("target_search_text", "target", "selector", "text", "value"),
        action_target,
        input_preview,
        *compact_targets,
    )
    if target_app_name:
        context["target_app_name"] = target_app_name
    if target_app_query:
        context["target_app_query"] = target_app_query
    if target_search_text:
        context["target_search_text"] = target_search_text
    return {key: value for key, value in context.items() if value not in ("", [], {})}


def _runtime_planner_task_verification_targets_for_step(
    decision: Any,
    step: Any,
) -> list[dict[str, Any]]:
    plan = getattr(decision, "plan", None)
    task_core = getattr(plan, "task_core", None)
    if task_core is None:
        return []
    step_id = str(getattr(step, "step_id", "") or "").strip()
    todos_by_step = {
        str(getattr(todo, "step_id", "") or "").strip(): todo
        for todo in list(getattr(task_core, "todos", []) or [])
        if str(getattr(todo, "step_id", "") or "").strip()
    }
    checkpoints_by_step: dict[str, list[Any]] = {}
    for checkpoint in list(getattr(task_core, "checkpoints", []) or []):
        after_step_id = str(getattr(checkpoint, "after_step_id", "") or "").strip()
        if after_step_id:
            checkpoints_by_step.setdefault(after_step_id, []).append(checkpoint)
    workspace = getattr(task_core, "workspace", None)
    workspace_items_by_step: dict[str, list[Any]] = {}
    for item in list(getattr(workspace, "items", []) if workspace is not None else []):
        source_step_id = str(getattr(item, "source_step_id", "") or "").strip()
        if source_step_id:
            workspace_items_by_step.setdefault(source_step_id, []).append(item)
    target_ids = _task_progress_verified_step_ids(
        step,
        *checkpoints_by_step.get(step_id, []),
    )
    targets: list[dict[str, Any]] = []
    for target_step_id in target_ids:
        target: dict[str, Any] = {"step_id": target_step_id}
        todo = todos_by_step.get(target_step_id)
        if _task_progress_has_value(todo):
            target["todo"] = _snapshot_payload(todo)
        checkpoints = [
            _snapshot_payload(checkpoint)
            for checkpoint in checkpoints_by_step.get(target_step_id, [])
            if _task_progress_has_value(checkpoint)
        ]
        if checkpoints:
            target["checkpoints"] = checkpoints
        workspace_items = [
            _snapshot_payload(item)
            for item in workspace_items_by_step.get(target_step_id, [])
            if _task_progress_has_value(item)
        ]
        if workspace_items:
            target["workspace_items"] = workspace_items
        if len(target) > 1:
            targets.append(target)
    return targets


def _runtime_planner_compact_task_verification_targets(
    targets: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    compact_targets: list[dict[str, Any]] = []
    for target in targets:
        if not isinstance(target, Mapping):
            continue
        step_id = str(target.get("step_id") or "").strip()
        if not step_id:
            continue
        todo = target.get("todo") if isinstance(target.get("todo"), Mapping) else {}
        checkpoints = _mapping_list(target.get("checkpoints"))
        compact: dict[str, Any] = {"step_id": step_id}
        todo_id = str(todo.get("todo_id") or "").strip()
        if todo_id:
            compact["todo_id"] = todo_id
        todo_title = str(todo.get("title") or "").strip()
        if todo_title:
            compact["todo_title"] = todo_title
        tool_name = str(todo.get("tool_name") or todo.get("tool") or "").strip()
        if tool_name:
            compact["tool_name"] = tool_name
        checkpoint_ids = [
            str(checkpoint.get("checkpoint_id") or "").strip()
            for checkpoint in checkpoints
            if str(checkpoint.get("checkpoint_id") or "").strip()
        ]
        if checkpoint_ids:
            compact["checkpoint_ids"] = checkpoint_ids
        checkpoint_titles = [
            str(checkpoint.get("title") or "").strip()
            for checkpoint in checkpoints
            if str(checkpoint.get("title") or "").strip()
        ]
        if checkpoint_titles:
            compact["checkpoint_titles"] = checkpoint_titles
        action_target = _runtime_planner_verification_action_target([target], {})
        if action_target:
            compact["action_target"] = action_target
            for key in (
                "app_name",
                "target_app_name",
                "target_app_query",
                "target_search_text",
                "target",
                "selector",
            ):
                value = str(action_target.get(key) or "").strip()
                if value:
                    compact[key] = value
        compact_targets.append(compact)
    return compact_targets


def _runtime_planner_verification_action_target(
    targets: Iterable[Mapping[str, Any]],
    input_preview: Mapping[str, Any],
) -> dict[str, Any]:
    for target in targets:
        if not isinstance(target, Mapping):
            continue
        for checkpoint in _mapping_list(target.get("checkpoints")):
            payload = checkpoint.get("payload") if isinstance(checkpoint.get("payload"), Mapping) else {}
            action_target = (
                payload.get("action_target")
                if isinstance(payload.get("action_target"), Mapping)
                else {}
            )
            if action_target:
                return dict(action_target)
        todo = target.get("todo") if isinstance(target.get("todo"), Mapping) else {}
        metadata = todo.get("metadata") if isinstance(todo.get("metadata"), Mapping) else {}
        action = str(metadata.get("action") or "").strip()
        if action:
            target_payload = {
                "kind": "task_step",
                "action": action,
                "step_id": str(target.get("step_id") or "").strip(),
                "tool_name": str(todo.get("tool_name") or "").strip(),
                **{
                    key: input_preview.get(key)
                    for key in ("app_name", "target", "selector", "target_search_text")
                    if input_preview.get(key)
                },
            }
            return _runtime_planner_non_empty_mapping(target_payload)
    return _runtime_planner_non_empty_mapping(
        {
            key: input_preview.get(key)
            for key in ("app_name", "target", "selector", "target_search_text")
            if input_preview.get(key)
        }
    )


def _runtime_planner_failure_detail(result: Mapping[str, Any]) -> str:
    for key in ("error", "hint", "summary", "status"):
        value = str(result.get(key) or "").strip() if isinstance(result, Mapping) else ""
        if value:
            return value[:500]
    return ""


def _runtime_planner_non_empty_mapping(value: Mapping[str, Any]) -> dict[str, Any]:
    return {key: item for key, item in value.items() if item not in (None, "", [], {})}


def _runtime_planner_first_text(
    keys: Iterable[str],
    *values: Mapping[str, Any],
) -> str:
    for value in values:
        if not isinstance(value, Mapping):
            continue
        for key in keys:
            clean = str(value.get(key) or "").strip()
            if clean:
                return clean
    return ""


def _runtime_planner_verification_failure_payloads(
    decision: Any,
    events: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    plan = getattr(decision, "plan", None)
    tool_plan = getattr(plan, "tool_plan", None)
    steps = list(getattr(tool_plan, "steps", []) or [])
    if not steps:
        return []
    tool_events = [
        event
        for event in events
        if isinstance(event, dict)
        and str(event.get("event") or "").strip() == "agent.tool.call"
    ]
    event_index = 0
    payloads: list[dict[str, Any]] = []
    for step in steps:
        tool_name = str(getattr(step, "tool_name", "") or "").strip()
        step_id = str(getattr(step, "step_id", "") or "").strip()
        if not tool_name or not step_id:
            continue
        tool_event: dict[str, Any] | None = None
        scan_index = event_index
        while scan_index < len(tool_events):
            candidate = tool_events[scan_index]
            if str(candidate.get("detail") or "").strip() == tool_name:
                tool_event = candidate
                event_index = scan_index + 1
                break
            scan_index += 1
        if tool_event is None:
            continue
        if not _runtime_planner_step_is_ui_verification(step_id, tool_name):
            continue
        result = (
            tool_event.get("result")
            if isinstance(tool_event.get("result"), Mapping)
            else {}
        )
        verification_context = _runtime_planner_verification_context(
            tool_events[: max(0, event_index - 1)],
            tool_event,
        )
        if _runtime_planner_native_receipt_satisfies_verification(
            tool_events[: max(0, event_index - 1)],
            tool_event,
            result,
        ):
            continue
        target_missing = False
        if _runtime_planner_verification_requires_exact_target_match(
            tool_events[: max(0, event_index - 1)],
            tool_event,
        ):
            target_missing = _tool_result_verification_target_missing(
                tool_name,
                result,
                target_text=str(verification_context.get("target_search_text") or ""),
                role_filter=str(verification_context.get("ui_role_filter") or ""),
            )
        weak_observation = _tool_result_verification_weak(tool_name, result)
        if not weak_observation and not target_missing:
            continue
        metadata = {
            **_runtime_trace_metadata_from_mapping(tool_event),
            **verification_context,
            **_ui_verification_failure_metadata(
                tool_name,
                result,
                target_text=str(verification_context.get("target_search_text") or ""),
                role_filter=str(verification_context.get("ui_role_filter") or ""),
                target_missing=target_missing,
            ),
        }
        detail = "verification observation returned no UI elements or readable text"
        if target_missing:
            detail = (
                "verification observation did not include target UI element"
                f": {verification_context.get('target_search_text')}"
            )
        payload = {
            "event_type": "agent.tool.call",
            "trigger": "verification_failed",
            "status": "verification_failed",
            "source_step_id": step_id,
            "planner_step_id": step_id,
            "tool_name": tool_name,
            "input_preview": (
                tool_event.get("input_preview")
                if isinstance(tool_event.get("input_preview"), Mapping)
                else {}
            ),
            "detail": detail,
            "result": result,
            **verification_context,
            **({"metadata": metadata} if metadata else {}),
        }
        _merge_runtime_planner_failure_context(
            payload,
            _runtime_planner_step_verification_failure_context(
                decision,
                step,
                tool_event,
                result,
            ),
        )
        payloads.append(payload)
    return payloads


def _runtime_planner_native_receipt_satisfies_verification(
    prior_events: Iterable[Mapping[str, Any]],
    verifier_event: Mapping[str, Any],
    result: Mapping[str, Any],
) -> bool:
    if (
        str(verifier_event.get("source") or "").strip()
        != "runtime_native_postcondition_receipt"
        or result.get("ok") is not True
        or result.get("postcondition_verified") is not True
        or result.get("verification_satisfied_by_native_receipt") is not True
    ):
        return False
    source_call_id = str(result.get("source_tool_call_id") or "").strip()
    source_tool = str(result.get("source_tool") or "").strip()
    source_step_id = str(result.get("source_step_id") or "").strip()
    if not source_call_id or not source_tool or not source_step_id:
        return False
    for key, expected in (
        ("source_tool_call_id", source_call_id),
        ("source_step_id", source_step_id),
    ):
        claimed = str(verifier_event.get(key) or "").strip()
        if claimed != expected:
            return False
    verifier_plan_id = str(verifier_event.get("plan_id") or "").strip()
    for event in reversed(list(prior_events)):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("tool_call_id") or "").strip() != source_call_id:
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != source_tool:
            return False
        if str(event.get("step_id") or event.get("planner_step_id") or "").strip() != (
            source_step_id
        ):
            return False
        source_plan_id = str(event.get("plan_id") or "").strip()
        if (verifier_plan_id or source_plan_id) and (
            not verifier_plan_id or verifier_plan_id != source_plan_id
        ):
            return False
        source_result = (
            event.get("result") if isinstance(event.get("result"), Mapping) else {}
        )
        return source_result.get("ok") is True
    return False


def _runtime_planner_verification_context(
    events: list[dict[str, Any]],
    current_event: Mapping[str, Any],
) -> dict[str, Any]:
    app_name = _runtime_planner_verification_app_name(events, current_event)
    app_query = _runtime_planner_verification_app_query(events)
    search_text = _runtime_planner_verification_target_text(events, current_event)
    role_filter = _runtime_planner_verification_role_filter(events, current_event)
    context = {
        "target_app_name": app_name,
        "target_app_query": app_query,
        "target_search_text": search_text,
        "ui_role_filter": role_filter,
        "recovery_observation_goal": "inspect_current_target_after_verification_gap",
    }
    return {key: value for key, value in context.items() if value not in ("", [], {})}


def _runtime_planner_verification_requires_exact_target_match(
    events: list[dict[str, Any]],
    current_event: Mapping[str, Any],
) -> bool:
    current_input = (
        current_event.get("input_preview")
        if isinstance(current_event.get("input_preview"), Mapping)
        else {}
    )
    if _first_runtime_planner_event_text(
        current_input,
        keys=("target_search_text", "target", "selector"),
    ):
        return True
    if _first_runtime_planner_event_text(current_input, keys=("role_filter", "role")):
        return True
    for event in reversed(events):
        tool_name = str(event.get("detail") or "").strip()
        if tool_name in {
            "app.open_and_click_ui_element",
            "app.focus_and_click_ui_element",
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.click_ui_element",
            "desktop.type_into_ui_element",
        }:
            return True
        if tool_name in {"desktop.safe_type_text", "desktop.search_submit"}:
            return False
    return False


def _runtime_planner_verification_app_name(
    events: list[dict[str, Any]],
    current_event: Mapping[str, Any],
) -> str:
    candidates: list[str] = []
    for event in [current_event, *reversed(events)]:
        if not isinstance(event, Mapping):
            continue
        candidates.extend(_runtime_planner_event_app_name_candidates(event))
    for candidate in candidates:
        if candidate and not _runtime_planner_placeholder_app_name(candidate):
            return candidate
    return candidates[0] if candidates else ""


def _runtime_planner_event_app_name_candidates(event: Mapping[str, Any]) -> list[str]:
    result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    input_preview = (
        event.get("input_preview") if isinstance(event.get("input_preview"), Mapping) else {}
    )
    candidates = _runtime_planner_text_candidates(
        (
            "app_name",
            "resolved_app_name",
            "discovered_app_name",
            "requested_app_name",
            "active_app",
            "bundle_name",
        ),
        input_preview,
        data,
        result,
    )
    detail = str(event.get("detail") or "").strip()
    if detail in {"desktop.list_apps", "desktop.running_apps"}:
        candidates.extend(_runtime_planner_list_apps_result_candidates(result, data))
    return candidates


def _runtime_planner_list_apps_result_candidates(
    result: Mapping[str, Any],
    data: Mapping[str, Any],
) -> list[str]:
    candidates: list[str] = []
    for source in (data, result):
        best_match = source.get("best_match")
        if isinstance(best_match, Mapping):
            candidates.extend(
                _runtime_planner_text_candidates(
                    ("name", "app_name", "resolved_app_name"),
                    best_match,
                )
            )
        apps = source.get("apps")
        if isinstance(apps, list):
            for item in apps[:3]:
                if isinstance(item, Mapping):
                    candidates.extend(
                        _runtime_planner_text_candidates(
                            ("name", "app_name", "display_name"),
                            item,
                        )
                    )
    return candidates


def _runtime_planner_verification_app_query(events: list[dict[str, Any]]) -> str:
    for event in reversed(events):
        if str(event.get("detail") or "").strip() != "desktop.list_apps":
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        query = str(input_preview.get("query") or "").strip()
        if query:
            return query
    return ""


def _runtime_planner_verification_search_text(events: list[dict[str, Any]]) -> str:
    for event in reversed(events):
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {
            "desktop.safe_type_text",
            "desktop.type",
            "desktop.type_into_ui_element",
            "app.open_and_safe_type_text",
            "app.focus_and_safe_type_text",
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
        }:
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        text = str(input_preview.get("text") or input_preview.get("value") or "").strip()
        if text:
            return text
    return ""


def _runtime_planner_verification_target_text(
    events: list[dict[str, Any]],
    current_event: Mapping[str, Any],
) -> str:
    current_input = (
        current_event.get("input_preview")
        if isinstance(current_event.get("input_preview"), Mapping)
        else {}
    )
    current_target = _first_runtime_planner_event_text(
        current_input,
        keys=("target_search_text", "target", "text", "value"),
    )
    if current_target:
        return current_target
    for event in reversed(events):
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        }:
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        if tool_name in {
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        }:
            target = _first_runtime_planner_event_text(
                input_preview,
                data,
                result,
                keys=("text", "value", "target_search_text", "matched_label", "target"),
            )
            if target:
                return target
            continue
    return _runtime_planner_verification_search_text(events)


def _runtime_planner_verification_role_filter(
    events: list[dict[str, Any]],
    current_event: Mapping[str, Any],
) -> str:
    current_input = (
        current_event.get("input_preview")
        if isinstance(current_event.get("input_preview"), Mapping)
        else {}
    )
    role_filter = _first_runtime_planner_event_text(
        current_input,
        keys=("role_filter", "role"),
    )
    if role_filter:
        return role_filter
    for event in reversed(events):
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {
            "app.open_and_click_ui_element",
            "app.focus_and_click_ui_element",
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.click_ui_element",
            "desktop.type_into_ui_element",
        }:
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        role_filter = _first_runtime_planner_event_text(
            input_preview,
            keys=("role_filter", "role"),
        )
        if role_filter:
            return role_filter
    return ""


def _first_runtime_planner_event_text(
    *sources: Mapping[str, Any],
    keys: tuple[str, ...],
) -> str:
    for source in sources:
        if not isinstance(source, Mapping):
            continue
        for key in keys:
            value = str(source.get(key) or "").strip()
            if value:
                return value
    return ""


def _runtime_planner_text_candidates(
    keys: tuple[str, ...],
    *sources: Mapping[str, Any],
) -> list[str]:
    candidates: list[str] = []
    for source in sources:
        if not isinstance(source, Mapping):
            continue
        for key in keys:
            value = str(source.get(key) or "").strip()
            if value:
                candidates.append(value)
    return candidates


def _runtime_planner_placeholder_app_name(value: str) -> bool:
    clean = str(value or "").strip().lower()
    return clean.startswith("<") and "selected app" in clean


def _runtime_planner_unavailable_failure_payloads(decision: Any) -> list[dict[str, Any]]:
    plan = getattr(decision, "plan", None)
    tool_plan = getattr(plan, "tool_plan", None)
    steps = list(getattr(tool_plan, "steps", []) or [])
    payloads: list[dict[str, Any]] = []
    for step in steps:
        status = str(getattr(step, "status", "") or "").strip()
        tool_name = str(getattr(step, "tool_name", "") or "").strip()
        if status != "unavailable" and (tool_name or status not in {"", "planned"}):
            continue
        step_id = str(getattr(step, "step_id", "") or "").strip()
        capability_id = str(getattr(step, "capability_id", "") or "").strip()
        if not step_id and not capability_id:
            continue
        reason = str(getattr(step, "reason", "") or "").strip()
        title = str(getattr(step, "title", "") or "").strip()
        input_preview = (
            dict(getattr(step, "input_preview", {}) or {})
            if isinstance(getattr(step, "input_preview", {}), Mapping)
            else {}
        )
        metadata = {
            "capability_id": capability_id,
            "planned_tool_name": tool_name,
            "step_title": title,
            "step_status": status or "unavailable",
            "input_preview": input_preview,
        }
        missing_permissions = _string_list(input_preview.get("missing_permissions"))
        if missing_permissions:
            metadata["missing_permissions"] = missing_permissions
        blocking_conditions = _string_list(input_preview.get("blocking_conditions"))
        if blocking_conditions:
            metadata["blocking_conditions"] = blocking_conditions
        detail_parts = [
            "planned tool is missing or unavailable",
            f"step={step_id}" if step_id else "",
            f"capability={capability_id}" if capability_id else "",
            reason,
        ]
        payloads.append(
            {
                "event_type": "agent.plan.step",
                "trigger": "tool_unavailable",
                "status": "tool_unavailable",
                "source_step_id": step_id,
                "planner_step_id": step_id,
                "tool_name": tool_name,
                "capability_id": capability_id,
                "title": title,
                "input_preview": input_preview,
                "detail": "; ".join(part for part in detail_parts if part),
                "result": {
                    "ok": False,
                    "error": "planned tool is missing or unavailable",
                    "step_status": status or "unavailable",
                    "capability_id": capability_id,
                },
                "metadata": metadata,
            }
        )
    return payloads


def _goal_contract_requires_effectful_execution(
    contract: GoalContract | None,
) -> bool:
    return bool(
        contract is not None
        and any(
            criterion.required
            and (criterion.effectful or not criterion.response_satisfiable)
            for criterion in contract.criteria
        )
    )


def _runtime_planner_unavailable_goal_payload(
    decision: Any,
    unavailable_payloads: Iterable[Mapping[str, Any]],
    replan_payloads: Iterable[Mapping[str, Any]],
    *,
    goal_contract: GoalContract | None,
    run_id: str,
) -> dict[str, Any]:
    failures = [
        dict(payload)
        for payload in unavailable_payloads
        if isinstance(payload, Mapping)
    ]
    replans = [
        dict(payload)
        for payload in replan_payloads
        if isinstance(payload, Mapping)
    ]
    plan = getattr(decision, "plan", None)
    missing_capabilities = _ordered_text_list(
        str(payload.get("capability_id") or "").strip()
        for payload in failures
    )
    missing_tools = _ordered_text_list(
        str(
            payload.get("tool_name")
            or (
                payload.get("metadata", {}).get("planned_tool_name")
                if isinstance(payload.get("metadata"), Mapping)
                else ""
            )
            or ""
        ).strip()
        for payload in failures
    )
    missing_permissions = _ordered_text_list(
        permission
        for payload in failures
        for permission in _string_list(
            (
                payload.get("metadata", {}).get("missing_permissions")
                if isinstance(payload.get("metadata"), Mapping)
                else []
            )
        )
    )
    blocking_conditions = _ordered_text_list(
        condition
        for payload in failures
        for condition in _string_list(
            (
                payload.get("metadata", {}).get("blocking_conditions")
                if isinstance(payload.get("metadata"), Mapping)
                else []
            )
        )
    )
    blocked_reason = _runtime_planner_unavailable_blocked_reason(
        missing_permissions=missing_permissions,
        blocking_conditions=blocking_conditions,
        missing_tools=missing_tools,
        missing_capabilities=missing_capabilities,
    )
    payload = {
        "status": "awaiting_user",
        "completion_impact": "await_user",
        "retryable": True,
        "blocked_reason": blocked_reason,
        "recommended_action": {
            "permission": "grant_permission",
            "install": "install_component",
            "configuration": "configure_capability",
            "capability_not_enabled": "enable_capability",
            "unsupported": "use_supported_workflow",
        }[blocked_reason],
        "run_id": str(run_id or "").strip(),
        "decision_id": str(getattr(decision, "decision_id", "") or "").strip(),
        "plan_id": str(getattr(plan, "plan_id", "") or "").strip(),
        "goal_contract_id": str(
            goal_contract.contract_id if goal_contract is not None else ""
        ).strip(),
        "missing_capabilities": missing_capabilities,
        "missing_tools": missing_tools,
        "missing_permissions": missing_permissions,
        "blocking_conditions": blocking_conditions,
        "blocked_step_ids": _ordered_text_list(
            str(payload.get("source_step_id") or "").strip()
            for payload in failures
        ),
        "replan_request_ids": _ordered_text_list(
            str(payload.get("request_id") or "").strip()
            for payload in replans
        ),
        "root_goal_unchanged": True,
        "actor": "native_runtime",
        "visibility": "internal",
        "source": "runtime_planner",
    }
    return {
        key: value
        for key, value in payload.items()
        if value not in (None, "", [], {})
    }


def _runtime_planner_unavailable_blocked_reason(
    *,
    missing_permissions: list[str],
    blocking_conditions: list[str],
    missing_tools: list[str],
    missing_capabilities: list[str],
) -> str:
    normalized_conditions = " ".join(blocking_conditions).casefold()
    if missing_permissions or "permission" in normalized_conditions:
        return "permission"
    if any(token in normalized_conditions for token in ("install", "not_installed")):
        return "install"
    if any(
        token in normalized_conditions
        for token in ("config", "credential", "provider_required", "not_configured")
    ):
        return "configuration"
    if missing_tools or missing_capabilities:
        return "capability_not_enabled"
    return "unsupported"


def _runtime_planner_unavailable_user_summary(payload: Mapping[str, Any]) -> str:
    return {
        "permission": "任务暂时无法继续：请先授予所需权限，然后重试。",
        "install": "任务暂时无法继续：请先安装所需组件，然后重试。",
        "configuration": "任务暂时无法继续：请先完成相关配置，然后重试。",
        "capability_not_enabled": (
            "任务暂时无法继续：请先在 Agent 设置中开启所需能力，然后重试。"
        ),
        "unsupported": "当前版本暂不支持完成这个任务。",
    }.get(
        str(payload.get("blocked_reason") or "").strip(),
        "任务暂时无法继续，请检查 Agent 设置后重试。",
    )


def _runtime_planner_step_is_ui_verification(step_id: str, tool_name: str) -> bool:
    return (
        str(step_id or "").strip()
        in {"verify-desktop-result", "observe-selected-discovered-app"}
        and str(tool_name or "").strip() in {"desktop.ui_elements", "desktop.read_ui"}
    )


def _tool_result_verification_weak(tool_name: str, result: Mapping[str, Any]) -> bool:
    if str(tool_name or "").strip() not in {"desktop.ui_elements", "desktop.read_ui"}:
        return False
    if not isinstance(result, Mapping):
        return False
    if result.get("ok") is not True:
        return False
    if result.get("approval_required") or result.get("blocked_by_user_goal"):
        return False
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    elements = _ui_elements_from_result(result, data)
    if _ui_element_count(result, data, elements) > 0:
        return False
    return not _ui_observation_has_readable_text(result, data, elements)


def _tool_result_verification_target_missing(
    tool_name: str,
    result: Mapping[str, Any],
    *,
    target_text: str,
    role_filter: str = "",
) -> bool:
    if str(tool_name or "").strip() not in {"desktop.ui_elements", "desktop.read_ui"}:
        return False
    if not str(target_text or "").strip():
        return False
    if not isinstance(result, Mapping):
        return False
    if result.get("ok") is not True:
        return False
    if result.get("approval_required") or result.get("blocked_by_user_goal"):
        return False
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    elements = _ui_elements_from_result(result, data)
    if not elements and not _ui_observation_has_readable_text(result, data, elements):
        return False
    if _ui_observation_text_matches_target(result, data, target_text):
        return False
    return not _ui_observation_target_matches(elements, target_text, role_filter)


def _ui_verification_failure_metadata(
    tool_name: str,
    result: Mapping[str, Any],
    *,
    target_text: str,
    role_filter: str,
    target_missing: bool,
) -> dict[str, Any]:
    if str(tool_name or "").strip() not in {"desktop.ui_elements", "desktop.read_ui"}:
        return {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    elements = _ui_elements_from_result(result, data)
    metadata: dict[str, Any] = {
        "ui_element_count": _ui_element_count(result, data, elements),
    }
    if target_text:
        metadata["target_search_text"] = str(target_text).strip()
        metadata["ui_target"] = str(target_text).strip()
    if role_filter:
        metadata["ui_role_filter"] = str(role_filter).strip()
    if target_missing and target_text:
        metadata["ui_target_found"] = False
        metadata["ui_match_count"] = 0
        metadata["blocking_conditions"] = ["ui_target_not_found"]
    return {key: value for key, value in metadata.items() if value not in ("", [], {})}


def _ui_elements_from_result(
    result: Mapping[str, Any],
    data: Mapping[str, Any],
) -> list[Any]:
    for source in (data, result):
        elements = source.get("elements")
        if isinstance(elements, list):
            return list(elements)
    return []


def _ui_element_count(
    result: Mapping[str, Any],
    data: Mapping[str, Any],
    elements: list[Any],
) -> int:
    counts: list[int] = []
    for source in (data, result):
        for key in ("element_count", "text_item_count", "count"):
            count = _non_negative_int(source.get(key))
            if count is not None:
                counts.append(count)
    if elements:
        counts.append(len(elements))
    return max(counts) if counts else 0


def _ui_observation_has_readable_text(
    result: Mapping[str, Any],
    data: Mapping[str, Any],
    elements: list[Any],
) -> bool:
    for source in (data, result):
        for key in (
            "text",
            "content",
            "ocr_text",
            "selected_text",
            "value",
            "title",
            "window_title",
            "active_app",
            "app_name",
        ):
            if str(source.get(key) or "").strip():
                return True
    for element in elements:
        if not isinstance(element, Mapping):
            continue
        for key in ("name", "value", "title", "label", "description", "text"):
            if str(element.get(key) or "").strip():
                return True
    return False


def _ui_observation_target_matches(
    elements: list[Any],
    target_text: str,
    role_filter: str = "",
) -> list[dict[str, Any]]:
    normalized_target = _normalize_observed_desktop_text(target_text)
    if not normalized_target:
        return []
    ordinal = _observed_desktop_target_ordinal(target_text)
    if ordinal:
        return _ordinal_observed_desktop_elements(elements, ordinal, role_filter)
    normalized_filter = _normalize_observed_desktop_text(role_filter)
    matches: list[dict[str, Any]] = []
    for raw_element in elements:
        if not isinstance(raw_element, Mapping):
            continue
        element = dict(raw_element)
        if element.get("enabled") is False:
            continue
        searchable = _normalize_observed_desktop_text(
            " ".join(
                str(element.get(key) or "")
                for key in ("role", "subrole", "name", "description", "value", "label")
            )
        )
        if normalized_filter and not _observed_desktop_text_matches(
            searchable,
            normalized_filter,
        ):
            continue
        if _observed_desktop_element_match_score(
            element,
            normalized_target,
            role_filter,
        ) > 0:
            matches.append(element)
    return matches


def _ui_observation_text_matches_target(
    result: Mapping[str, Any],
    data: Mapping[str, Any],
    target_text: str,
) -> bool:
    normalized_target = _normalize_observed_desktop_text(target_text)
    if not normalized_target:
        return False
    for source in (data, result):
        for key in (
            "text",
            "content",
            "ocr_text",
            "selected_text",
            "value",
            "title",
            "window_title",
        ):
            searchable = _normalize_observed_desktop_text(source.get(key))
            if searchable and _observed_desktop_text_matches(
                searchable,
                normalized_target,
            ):
                return True
    return False


def _non_negative_int(value: Any) -> int | None:
    if isinstance(value, bool):
        return None
    try:
        parsed = int(value)
    except (TypeError, ValueError):
        return None
    return parsed if parsed >= 0 else None


def _task_todo_status_for_tool_result(
    event_type: str,
    result: Mapping[str, Any],
    *,
    requires_post_action_verification: bool = False,
) -> str:
    if not isinstance(result, Mapping):
        return "blocked"
    if result.get("approval_required"):
        return "blocked"
    if str(event_type or "").strip() == "agent.tool.skipped":
        return "skipped" if result.get("blocked_by_user_goal") else "blocked"
    if result.get("ok") is True and requires_post_action_verification:
        return "in_progress"
    return "blocked" if _tool_result_requests_replan(result) else "completed"


def _task_checkpoint_status_for_todo_status(
    todo_status: str,
    result: Mapping[str, Any],
    *,
    requires_post_action_verification: bool = False,
) -> str:
    if isinstance(result, Mapping) and result.get("approval_required"):
        return "waiting_approval"
    if todo_status == "in_progress" and requires_post_action_verification:
        return "ready"
    if todo_status == "completed":
        return "completed"
    return "blocked"


def _task_progress_requires_post_action_verification(*values: Any) -> bool:
    for value in values:
        if value in (None, "", [], {}):
            continue
        if _task_progress_mapping_requires_post_action_verification(value):
            return True
        for attr in ("metadata", "payload", "checkpoint_policy", "desktop_loop"):
            nested = getattr(value, attr, None)
            if _task_progress_mapping_requires_post_action_verification(nested):
                return True
    return False


def _runtime_planner_step_has_following_ui_verification(
    step_id: str,
    steps: Iterable[Any],
    following_tool_events: Iterable[Mapping[str, Any]],
    *,
    todos_by_step: Mapping[str, Any] | None = None,
    checkpoints_by_step: Mapping[str, list[Any]] | None = None,
) -> bool:
    for candidate_step in steps:
        depends_on = _string_list(getattr(candidate_step, "depends_on", None))
        tool_name = str(getattr(candidate_step, "tool_name", "") or "").strip()
        if step_id not in depends_on or tool_name not in {
            "desktop.ui_elements",
            "desktop.read_ui",
        }:
            continue
        candidate_step_id = str(getattr(candidate_step, "step_id", "") or "").strip()
        if step_id in _task_progress_verified_step_ids(
            candidate_step,
            (todos_by_step or {}).get(candidate_step_id),
            *(checkpoints_by_step or {}).get(candidate_step_id, []),
        ):
            # A declared verifier owns completion of its source step.  Let
            # its verification-target projection record verifier completion
            # first and source completion second; look-ahead must not promote
            # the effectful action from readable UI text alone.
            return False
        for event in following_tool_events:
            event_step_id = str(
                event.get("step_id") or event.get("planner_step_id") or ""
            ).strip()
            if event_step_id and event_step_id != candidate_step_id:
                continue
            if not event_step_id and str(event.get("detail") or "").strip() != tool_name:
                continue
            result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
            data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
            elements = _ui_elements_from_result(result, data)
            return bool(
                result.get("ok") is True
                and not result.get("approval_required")
                and result.get("verification_failed") is not True
                and _ui_observation_has_readable_text(result, data, elements)
            )
    return False


def _task_progress_mapping_requires_post_action_verification(value: Any) -> bool:
    if not isinstance(value, Mapping):
        return False
    if value.get("requires_post_action_verification") is True:
        return True
    for key in ("metadata", "payload", "checkpoint_policy", "desktop_loop"):
        nested = value.get(key)
        if isinstance(nested, Mapping) and nested.get("requires_post_action_verification") is True:
            return True
    return False


def _runtime_planner_verification_targets(
    step: Any,
    checkpoints: Iterable[Any],
    todos_by_step: Mapping[str, Any],
    checkpoints_by_step: Mapping[str, list[Any]],
) -> list[dict[str, Any]]:
    target_step_ids = _task_progress_verified_step_ids(step, *list(checkpoints))
    targets: list[dict[str, Any]] = []
    for target_step_id in target_step_ids:
        target: dict[str, Any] = {"step_id": target_step_id}
        todo = todos_by_step.get(target_step_id)
        if _task_progress_has_value(todo):
            target["todo"] = todo
        target_checkpoints = checkpoints_by_step.get(target_step_id, [])
        if target_checkpoints:
            target["checkpoints"] = list(target_checkpoints)
        if len(target) > 1:
            targets.append(target)
    return targets


def _model_followup_verification_targets(
    request: Mapping[str, Any],
    checkpoints: Iterable[Mapping[str, Any]],
    todos_by_step: Mapping[str, Mapping[str, Any]],
    checkpoints_by_step: Mapping[str, list[Mapping[str, Any]]],
) -> list[dict[str, Any]]:
    explicit_targets = _mapping_list(request.get("task_verification_targets"))
    if not explicit_targets:
        explicit_targets = _mapping_list(request.get("verification_targets"))
    target_step_ids = [
        str(target.get("step_id") or "").strip()
        for target in explicit_targets
        if str(target.get("step_id") or "").strip()
    ]
    if not target_step_ids:
        target_step_ids = _task_progress_verified_step_ids(request, *list(checkpoints))
    targets: list[dict[str, Any]] = []
    explicit_by_step = {
        str(target.get("step_id") or "").strip(): target
        for target in explicit_targets
        if str(target.get("step_id") or "").strip()
    }
    for target_step_id in target_step_ids:
        explicit_target = explicit_by_step.get(target_step_id, {})
        target: dict[str, Any] = {"step_id": target_step_id}
        todo = (
            explicit_target.get("todo")
            if isinstance(explicit_target.get("todo"), Mapping)
            else todos_by_step.get(target_step_id)
            or _task_progress_todo_from_target(explicit_target)
        )
        if _task_progress_has_value(todo):
            target["todo"] = todo
        target_checkpoints = (
            _mapping_list(explicit_target.get("checkpoints"))
            or checkpoints_by_step.get(target_step_id, [])
            or _task_progress_checkpoints_from_target(explicit_target)
        )
        if target_checkpoints:
            target["checkpoints"] = list(target_checkpoints)
        if len(target) > 1:
            targets.append(target)
    return targets


def _task_progress_todo_from_target(target: Mapping[str, Any]) -> dict[str, Any]:
    todo_id = str(target.get("todo_id") or "").strip()
    step_id = str(target.get("step_id") or "").strip()
    if not todo_id or not step_id:
        return {}
    return {
        key: value
        for key, value in {
            "todo_id": todo_id,
            "step_id": step_id,
            "title": str(target.get("todo_title") or target.get("title") or step_id).strip(),
            "status": str(target.get("status") or "in_progress").strip(),
            "tool_name": str(target.get("tool_name") or target.get("tool") or "").strip(),
        }.items()
        if value
    }


def _task_progress_checkpoints_from_target(target: Mapping[str, Any]) -> list[dict[str, Any]]:
    checkpoint_ids = _string_list(target.get("checkpoint_ids"))
    if not checkpoint_ids:
        return []
    checkpoint_titles = _string_list(target.get("checkpoint_titles"))
    step_id = str(target.get("step_id") or "").strip()
    return [
        {
            "checkpoint_id": checkpoint_id,
            "after_step_id": step_id,
            "title": (
                checkpoint_titles[index]
                if index < len(checkpoint_titles)
                else checkpoint_id
            ),
            "status": "ready",
        }
        for index, checkpoint_id in enumerate(checkpoint_ids)
    ]


def _task_progress_verified_step_ids(*values: Any) -> list[str]:
    ids: list[str] = []
    for value in values:
        for step_id in _string_list(_task_progress_nested_value(value, "verified_step_ids")):
            ids.append(step_id)
    if ids:
        return _ordered_text_list(ids)
    runtime_stage = _task_progress_runtime_stage(*values)
    runtime_role = str(
        next(
            (
                _task_progress_nested_value(value, "runtime_role")
                for value in values
                if _task_progress_nested_value(value, "runtime_role")
            ),
            "",
        )
        or ""
    ).strip()
    if runtime_stage != "verify" and runtime_role != "inspect_ui":
        return []
    for value in values:
        depends_on = (
            value.get("depends_on")
            if isinstance(value, Mapping)
            else getattr(value, "depends_on", None)
        )
        for step_id in _string_list(depends_on):
            ids.append(step_id)
    return _ordered_text_list(ids)


def _task_progress_runtime_stage(*values: Any) -> str:
    for value in values:
        stage = str(_task_progress_nested_value(value, "runtime_stage") or "").strip()
        if stage:
            return stage
    return ""


def _task_progress_nested_value(value: Any, key: str) -> Any:
    if isinstance(value, Mapping):
        if value.get(key) not in (None, "", [], {}):
            return value.get(key)
        for nested_key in ("metadata", "payload", "checkpoint_policy", "desktop_loop"):
            nested = value.get(nested_key)
            if isinstance(nested, Mapping) and nested.get(key) not in (None, "", [], {}):
                return nested.get(key)
        return None
    raw_value = getattr(value, key, None)
    if raw_value not in (None, "", [], {}):
        return raw_value
    for attr in ("metadata", "payload", "checkpoint_policy", "desktop_loop"):
        nested = getattr(value, attr, None)
        if isinstance(nested, Mapping) and nested.get(key) not in (None, "", [], {}):
            return nested.get(key)
    return None


def _append_runtime_task_verification_target_progress_events(
    targets: Iterable[Mapping[str, Any]],
    base_payload: Mapping[str, Any],
    result: Mapping[str, Any],
    *,
    timeline: list[dict[str, Any]],
    timeline_factory: Callable[..., dict[str, Any]],
    append_run_event: Callable[[str, str, dict[str, Any]], Any] | None,
    run_id: str,
) -> None:
    target_todo_status = _task_verification_target_todo_status(result)
    target_checkpoint_status = _task_verification_target_checkpoint_status(result)
    verification_status = _task_verification_target_status(result)
    verify_step_id = str(base_payload.get("step_id") or "").strip()
    verify_tool = str(base_payload.get("tool") or "").strip()
    for target in targets:
        target_step_id = str(target.get("step_id") or "").strip()
        if not target_step_id:
            continue
        target_base_payload = {
            **dict(base_payload),
            "step_id": target_step_id,
            "verified_by_step_id": verify_step_id,
            "verification_tool": verify_tool,
            "verification_status": verification_status,
            "verification_result": _task_progress_result_preview(result),
        }
        todo = target.get("todo")
        if _task_progress_has_value(todo):
            todo_payload = _snapshot_payload(todo)
            todo_payload["status"] = target_todo_status
            _task_progress_apply_nested_context(
                todo_payload,
                "metadata",
                target_base_payload,
            )
            payload = {
                **target_base_payload,
                "todo_id": _task_progress_text(todo, "todo_id"),
                "status": target_todo_status,
                "previous_status": _task_progress_text(todo, "status") or "pending",
                "todo": todo_payload,
            }
            _append_runtime_task_progress_event(
                "agent.task.todo.updated",
                _task_progress_text(todo, "title") or target_step_id,
                payload,
                timeline=timeline,
                timeline_factory=timeline_factory,
                append_run_event=append_run_event,
                run_id=run_id,
            )
        for checkpoint in list(target.get("checkpoints") or []):
            if not _task_progress_has_value(checkpoint):
                continue
            checkpoint_payload = _snapshot_payload(checkpoint)
            checkpoint_payload["status"] = target_checkpoint_status
            _task_progress_apply_nested_context(
                checkpoint_payload,
                "payload",
                target_base_payload,
            )
            payload = {
                **target_base_payload,
                "checkpoint_id": _task_progress_text(checkpoint, "checkpoint_id"),
                "status": target_checkpoint_status,
                "previous_status": _task_progress_text(checkpoint, "status") or "planned",
                "checkpoint": checkpoint_payload,
            }
            _append_runtime_task_progress_event(
                "agent.task.checkpoint.updated",
                _task_progress_text(checkpoint, "title") or target_step_id,
                payload,
                timeline=timeline,
                timeline_factory=timeline_factory,
                append_run_event=append_run_event,
                run_id=run_id,
            )


def _task_verification_target_todo_status(result: Mapping[str, Any]) -> str:
    if not isinstance(result, Mapping):
        return "blocked"
    if result.get("approval_required") or result.get("verification_failed") is True:
        return "blocked"
    return "completed" if result.get("ok") is True else "blocked"


def _task_verification_target_checkpoint_status(result: Mapping[str, Any]) -> str:
    if isinstance(result, Mapping) and result.get("approval_required"):
        return "waiting_approval"
    if isinstance(result, Mapping) and result.get("verification_failed") is True:
        return "blocked"
    return "completed" if isinstance(result, Mapping) and result.get("ok") is True else "blocked"


def _task_verification_target_status(result: Mapping[str, Any]) -> str:
    if isinstance(result, Mapping) and result.get("approval_required"):
        return "waiting_approval"
    if isinstance(result, Mapping) and result.get("verification_failed") is True:
        return "verification_failed"
    return "verified" if isinstance(result, Mapping) and result.get("ok") is True else "verification_failed"


def _task_progress_apply_nested_context(
    payload: dict[str, Any],
    key: str,
    source: Mapping[str, Any],
) -> None:
    nested = payload.get(key) if isinstance(payload.get(key), Mapping) else {}
    payload[key] = {**dict(nested), **_task_progress_nested_context(source)}


def _task_progress_nested_context(source: Mapping[str, Any]) -> dict[str, Any]:
    context: dict[str, Any] = {}
    for key in (
        "verification_status",
        "verified_by_step_id",
        "verification_tool",
        "verification_result",
    ):
        value = source.get(key)
        if value not in (None, "", [], {}):
            context[key] = dict(value) if isinstance(value, Mapping) else value
    return context


def _task_progress_text(value: Any, key: str) -> str:
    if isinstance(value, Mapping):
        return str(value.get(key) or "").strip()
    return str(getattr(value, key, "") or "").strip()


def _task_progress_has_value(value: Any) -> bool:
    return value not in (None, "", [], {})


def _task_progress_result_preview(result: Mapping[str, Any]) -> dict[str, Any]:
    if not isinstance(result, Mapping):
        return {}
    preview: dict[str, Any] = {}
    for key in (
        "ok",
        "action",
        "summary",
        "error",
        "hint",
        "verification_failed",
        "returncode",
        "exit_code",
        "blocked_by_user_goal",
        "approval_required",
        "content_verified",
        "launch_verified",
        "postcondition_verified",
        "verification_passed",
        "verified",
    ):
        if key in result:
            preview[key] = result.get(key)
    stderr = str(result.get("stderr") or "").strip()
    if stderr:
        preview["stderr"] = stderr[:500]
    return preview


_RUNTIME_PLANNER_PROGRESS_EVENT_TYPES = {
    "agent.task.workspace_item.updated",
    "agent.task.todo.updated",
    "agent.task.checkpoint.updated",
    "agent.replan.recovery.updated",
}


def _runtime_planner_is_progress_event(event_type: str) -> bool:
    return _runtime_progress_base_event_type(str(event_type or "").strip()) in (
        _RUNTIME_PLANNER_PROGRESS_EVENT_TYPES
    )


def _runtime_planner_decision_exists(
    timeline: list[dict[str, Any]],
    *,
    decision_id: str,
    scope_context: Mapping[str, Any] | None,
) -> bool:
    expected_scope = _runtime_planner_scope_context(scope_context or {})
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        payload = (
            event.get("payload")
            if isinstance(event.get("payload"), Mapping)
            else {}
        )
        event_decision_id = str(
            event.get("decision_id") or payload.get("decision_id") or ""
        ).strip()
        if event_decision_id != decision_id:
            continue
        if _runtime_planner_scope_context(event) == expected_scope:
            return True
    return False


def _runtime_planner_initial_task_updates(
    decision: Any,
) -> list[tuple[str, str, dict[str, Any]]]:
    plan = getattr(decision, "plan", None)
    task_core = getattr(plan, "task_core", None)
    tool_plan = getattr(plan, "tool_plan", None)
    if task_core is None or tool_plan is None:
        return []

    steps_by_id = {
        str(getattr(step, "step_id", "") or "").strip(): step
        for step in list(getattr(tool_plan, "steps", []) or [])
        if str(getattr(step, "step_id", "") or "").strip()
    }
    core_id = str(getattr(task_core, "core_id", "") or "").strip()
    workspace = getattr(task_core, "workspace", None)
    workspace_id = str(getattr(workspace, "workspace_id", "") or "").strip()
    plan_id = str(getattr(plan, "plan_id", "") or "").strip()
    decision_id = str(getattr(decision, "decision_id", "") or "").strip()
    source_event = {"event": "agent.plan.created", "detail": "runtime_plan_created"}
    events: list[tuple[str, str, dict[str, Any]]] = []

    for item in list(getattr(workspace, "items", []) if workspace is not None else []):
        step_id = str(getattr(item, "source_step_id", "") or "").strip()
        step = steps_by_id.get(step_id)
        tool_name = str(getattr(step, "tool_name", "") or "").strip()
        status = str(getattr(item, "status", "") or "planned").strip()
        item_payload = _snapshot_payload(item)
        item_payload["status"] = status
        payload = {
            "source": "runtime_planner",
            "core_id": core_id,
            "workspace_id": workspace_id,
            "decision_id": decision_id,
            "plan_id": plan_id,
            "step_id": step_id,
            "tool": tool_name,
            "workspace_item_id": str(getattr(item, "item_id", "") or "").strip(),
            "status": status,
            "previous_status": "",
            "source_event": source_event,
            "result_preview": {},
            "workspace_item": item_payload,
        }
        events.append(
            (
                "agent.task.workspace_item.updated",
                str(getattr(item, "title", "") or step_id or "workspace item"),
                payload,
            )
        )

    for todo in list(getattr(task_core, "todos", []) or []):
        step_id = str(getattr(todo, "step_id", "") or "").strip()
        step = steps_by_id.get(step_id)
        tool_name = str(
            getattr(step, "tool_name", "")
            or getattr(todo, "tool_name", "")
            or ""
        ).strip()
        status = str(getattr(todo, "status", "") or "pending").strip()
        todo_payload = _snapshot_payload(todo)
        todo_payload["status"] = status
        payload = {
            "source": "runtime_planner",
            "core_id": core_id,
            "workspace_id": workspace_id,
            "decision_id": decision_id,
            "plan_id": plan_id,
            "step_id": step_id,
            "tool": tool_name,
            "todo_id": str(getattr(todo, "todo_id", "") or "").strip(),
            "status": status,
            "previous_status": "",
            "source_event": source_event,
            "result_preview": {},
            "todo": todo_payload,
        }
        events.append(
            (
                "agent.task.todo.updated",
                str(getattr(todo, "title", "") or step_id),
                payload,
            )
        )

    for checkpoint in list(getattr(task_core, "checkpoints", []) or []):
        step_id = str(getattr(checkpoint, "after_step_id", "") or "").strip()
        step = steps_by_id.get(step_id)
        tool_name = str(getattr(step, "tool_name", "") or "").strip()
        status = str(getattr(checkpoint, "status", "") or "planned").strip()
        checkpoint_payload = _snapshot_payload(checkpoint)
        checkpoint_payload["status"] = status
        payload = {
            "source": "runtime_planner",
            "core_id": core_id,
            "workspace_id": workspace_id,
            "decision_id": decision_id,
            "plan_id": plan_id,
            "step_id": step_id,
            "tool": tool_name,
            "checkpoint_id": str(getattr(checkpoint, "checkpoint_id", "") or "").strip(),
            "status": status,
            "previous_status": "",
            "source_event": source_event,
            "result_preview": {},
            "checkpoint": checkpoint_payload,
        }
        events.append(
            (
                "agent.task.checkpoint.updated",
                str(getattr(checkpoint, "title", "") or step_id),
                payload,
            )
        )
    return events


def _append_runtime_task_progress_event(
    event_type: str,
    detail: str,
    payload: dict[str, Any],
    *,
    timeline: list[dict[str, Any]],
    timeline_factory: Callable[..., dict[str, Any]],
    append_run_event: Callable[[str, str, dict[str, Any]], Any] | None,
    run_id: str,
) -> None:
    if _runtime_task_update_exists(timeline, event_type, payload):
        return
    scoped_event_type = _runtime_progress_event_type(event_type, payload)
    event_payload = _runtime_progress_event_payload(payload, event_type, scoped_event_type)
    if run_id:
        event_payload["run_id"] = str(run_id).strip()
    timeline.append(timeline_factory(scoped_event_type, detail, **event_payload))
    if run_id and append_run_event is not None:
        append_run_event(run_id, scoped_event_type, event_payload)


def _runtime_task_progress_scope_context(
    timeline: list[dict[str, Any]],
    tool_event: Mapping[str, Any],
    *,
    decision_id: str,
    plan_id: str,
) -> dict[str, str]:
    context = _runtime_task_progress_context_from_mapping(tool_event)
    if context:
        return context
    clean_decision_id = str(decision_id or "").strip()
    clean_plan_id = str(plan_id or "").strip()
    for event in reversed(timeline):
        if not isinstance(event, Mapping):
            continue
        event_type = str(event.get("event") or event.get("event_type") or "").strip()
        if event_type not in {
            "agent.task_core.created",
            "group.run.task_core.created",
            "workflow.task_core.created",
            "workflow.run.task_core.created",
        }:
            continue
        payload = _runtime_task_progress_event_payload(event)
        event_decision_id = str(payload.get("decision_id") or "").strip()
        event_plan_id = str(payload.get("plan_id") or "").strip()
        if clean_decision_id and event_decision_id and event_decision_id != clean_decision_id:
            continue
        if clean_plan_id and event_plan_id and event_plan_id != clean_plan_id:
            continue
        context = _runtime_task_progress_context_from_mapping(payload)
        if context:
            return context
    return {}


def _runtime_task_progress_context_from_mapping(value: Mapping[str, Any]) -> dict[str, str]:
    payload = _runtime_task_progress_event_payload(value)
    context: dict[str, str] = {}
    for key in (
        "task_id",
        "run_group_id",
        "group_run_id",
        "group_id",
        "workflow_id",
        "workflow_run_id",
        "workflow_node_id",
        "workflow_node_label",
    ):
        clean_value = str(payload.get(key) or "").strip()
        if clean_value:
            context[key] = clean_value
    return context


def _runtime_task_update_exists(
    timeline: list[dict[str, Any]],
    event_type: str,
    payload: Mapping[str, Any],
) -> bool:
    identity_key = (
        "todo_id"
        if event_type == "agent.task.todo.updated"
        else "workspace_item_id"
        if event_type == "agent.task.workspace_item.updated"
        else "checkpoint_id"
        if event_type == "agent.task.checkpoint.updated"
        else ""
    )
    identity = str(payload.get(identity_key) or "").strip() if identity_key else ""
    status = str(payload.get("status") or "").strip()
    decision_id = str(payload.get("decision_id") or "").strip()
    expected_scope = _runtime_planner_scope_context(payload)
    return any(
        isinstance(event, dict)
        and _runtime_progress_base_event_type(str(event.get("event") or "").strip())
        == event_type
        and (
            not decision_id
            or str(event.get("decision_id") or "").strip() == decision_id
            or str(
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), dict)
                    else {}
                ).get("decision_id")
                or ""
            ).strip()
            == decision_id
        )
        and (
            not identity
            or str(event.get(identity_key) or "").strip() == identity
            or str(
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), dict)
                    else {}
                ).get(identity_key)
                or ""
            ).strip()
            == identity
        )
        and (
            not status
            or str(event.get("status") or "").strip() == status
            or str(
                (
                    event.get("payload")
                    if isinstance(event.get("payload"), dict)
                    else {}
                ).get("status")
                or ""
            ).strip()
            == status
        )
        and _runtime_planner_scope_context(event) == expected_scope
        for event in timeline
    )


def _runtime_planner_step_has_status(
    timeline: list[dict[str, Any]],
    *,
    decision_id: str,
    step_id: str,
    statuses: set[str],
) -> bool:
    clean_step_id = str(step_id or "").strip()
    if not clean_step_id:
        return False
    clean_decision_id = str(decision_id or "").strip()
    expected_statuses = {
        str(status or "").strip()
        for status in statuses
        if str(status or "").strip()
    }
    if not expected_statuses:
        return False
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        if (
            _runtime_progress_base_event_type(str(event.get("event") or "").strip())
            != "agent.task.todo.updated"
        ):
            continue
        if str(event.get("step_id") or "").strip() != clean_step_id:
            continue
        if clean_decision_id and str(event.get("decision_id") or "").strip() != clean_decision_id:
            continue
        if str(event.get("status") or "").strip() in expected_statuses:
            return True
    return False


def _snapshot_payload(snapshot: Any) -> dict[str, Any]:
    if hasattr(snapshot, "model_dump"):
        return snapshot.model_dump(mode="json")
    if isinstance(snapshot, Mapping):
        return dict(snapshot)
    return {}


def _model_followup_context_message(payload: dict[str, Any]) -> str:
    artifacts = _string_list(payload.get("artifacts_expected"))
    artifact_write_allowed = bool(payload.get("artifact_write_allowed"))
    observation_tools = _string_list(payload.get("observation_tools"))
    observation_text = ", ".join(observation_tools) or "runtime observation tools"
    followup_target = payload.get("followup_target") if isinstance(payload.get("followup_target"), dict) else {}
    target_instruction = _model_followup_target_instruction(followup_target)
    pending_plan_instruction = _model_followup_pending_plan_instruction(payload)
    if target_instruction:
        pending_suffix = f"{pending_plan_instruction} " if pending_plan_instruction else ""
        artifact_instruction = (
            f"{_model_followup_chained_artifact_instruction(followup_target)}"
            f"{target_instruction}"
            f"{pending_suffix}"
        )
    elif pending_plan_instruction:
        artifact_instruction = pending_plan_instruction
    elif artifact_write_allowed and artifacts:
        artifact_instruction = (
            "The user requested a durable output. Call artifact.write next with "
            f"path {artifacts[0]!r} and content derived from the observed context. "
            "Do not write an empty or placeholder artifact."
        )
    elif artifact_write_allowed:
        artifact_instruction = (
            "If the user requested a durable output, call artifact.write next with an appropriate "
            "path and content derived from the observed context. Do not write an empty artifact."
        )
    else:
        artifact_instruction = (
            "artifact.write is not allowed, so provide the requested summary or report inline."
        )
    snapshot_text = _followup_content_snapshots_message(payload)
    return (
        "Runtime follow-up context: source material has just been observed through "
        f"{observation_text}. Use the latest Tool result messages above as source material. "
        f"{snapshot_text}"
        f"{artifact_instruction} If the observation failed or lacks readable content, explain the "
        "missing permission, capability, or fallback without asking the user to manually repeat a "
        "tool-capable action."
    )


def _model_followup_pending_plan_instruction(payload: Mapping[str, Any]) -> str:
    execution_instruction = _model_followup_pending_execution_request_instruction(payload)
    if execution_instruction:
        return execution_instruction
    steps = payload.get("pending_plan_steps")
    if not isinstance(steps, list):
        return ""
    normalized_steps = [
        step
        for step in steps
        if isinstance(step, Mapping)
        and (
            str(step.get("step_id") or "").strip()
            or str(step.get("tool_name") or "").strip()
        )
    ]
    if not normalized_steps:
        return ""
    items = []
    for index, step in enumerate(normalized_steps, start=1):
        tool_name = str(step.get("tool_name") or "").strip() or "available tool"
        step_id = str(step.get("step_id") or "").strip() or f"step-{index}"
        action = str(step.get("action") or "").strip()
        risk_level = str(step.get("risk_level") or "").strip()
        approval = " approval required" if bool(step.get("approval_required")) else ""
        preview = (
            step.get("input_preview")
            if isinstance(step.get("input_preview"), Mapping)
            else {}
        )
        preview_text = _model_followup_input_preview_text(preview)
        detail = f"[{index}] {step_id} via {tool_name}"
        if action:
            detail = f"{detail} action={action!r}"
        if risk_level or approval:
            detail = f"{detail} ({risk_level or 'low'} risk{approval})"
        if preview_text:
            detail = f"{detail} input_preview={preview_text}"
        items.append(detail)
    patch_instruction = ""
    if any(
        str(step.get("tool_name") or "").strip() == "workspace.write_patch"
        or (
            str(step.get("capability_id") or "").strip() == "file.workspace_write"
            and str(step.get("action") or "").strip() == "apply_patch"
        )
        for step in normalized_steps
    ):
        patch_instruction = (
            " For workspace.write_patch steps, generate a concrete single-file UTF-8 "
            "unified diff from the observed workspace and diagnostic results, then call "
            "workspace.write_patch with path and patch. The tool remains approval-gated; "
            "do not replace this pending patch step with a prose-only summary."
        )
    return (
        "Continue the pending Runtime Plan steps in order before giving a final answer: "
        + "; ".join(items)
        + ". If a pending terminal.execution step only has an abstract operation, synthesize a concrete, "
        "safe command from the observed files and request approval through the normal tool/policy gate. "
        "Do not skip directly to final prose while an available tool step is still pending."
        + patch_instruction
    )


def _model_followup_pending_execution_request_instruction(
    payload: Mapping[str, Any],
) -> str:
    requests = payload.get("pending_execution_requests")
    if not isinstance(requests, list):
        return ""
    normalized_requests = [
        request
        for request in requests
        if isinstance(request, Mapping)
        and (
            str(request.get("request_id") or "").strip()
            or str(request.get("step_id") or "").strip()
            or str(request.get("tool_name") or "").strip()
        )
    ]
    if not normalized_requests:
        return ""
    items = []
    for index, request in enumerate(normalized_requests, start=1):
        request_id = str(request.get("request_id") or "").strip() or f"request-{index}"
        tool_name = str(request.get("tool_name") or "").strip() or "available tool"
        step_id = str(request.get("step_id") or "").strip()
        stage = str(request.get("runtime_stage") or "").strip()
        role = str(request.get("runtime_role") or "").strip()
        approval = " approval required" if bool(request.get("approval_required")) else ""
        preview = (
            request.get("input_preview")
            if isinstance(request.get("input_preview"), Mapping)
            else {}
        )
        preview_text = _model_followup_input_preview_text(preview)
        detail = f"[{index}] {request_id} via {tool_name}"
        if step_id:
            detail = f"{detail} step={step_id!r}"
        if stage or role or approval:
            stage_text = stage or "planned"
            role_text = f" role={role}" if role else ""
            detail = f"{detail} ({stage_text}{role_text}{approval})"
        if preview_text:
            detail = f"{detail} input_preview={preview_text}"
        items.append(detail)
    patch_instruction = ""
    if any(
        str(request.get("tool_name") or "").strip() == "workspace.write_patch"
        or str(request.get("capability_id") or "").strip() == "file.workspace_write"
        for request in normalized_requests
    ):
        patch_instruction = (
            " For workspace.write_patch requests, generate a concrete single-file UTF-8 "
            "unified diff from the observed workspace and diagnostic results, then call "
            "workspace.write_patch with path and patch."
        )
    return (
        "Continue the pending Runtime execution requests in order before giving a final answer: "
        + "; ".join(items)
        + ". Respect approval_required through the normal tool/policy gate. If a pending "
        "terminal.run request only has an abstract operation, synthesize a concrete, safe command "
        "from the observed files and request approval through the normal gate. Do not skip directly "
        "to final prose while an available execution request is still pending."
        + patch_instruction
    )


def _model_followup_input_preview_text(preview: Mapping[str, Any]) -> str:
    if not preview:
        return ""
    text = repr(dict(preview))
    if len(text) > 360:
        return f"{text[:357]}..."
    return text


def _model_followup_target_payload(
    selection_payload: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    app_name = str(target.get("app_name") or "").strip()
    kind = str(target.get("kind") or "").strip()
    if kind == "communication_message":
        return _model_followup_communication_target_payload(target, allowed)
    if kind == "desktop_discovered_app_action":
        return _model_followup_desktop_discovered_app_target_payload(target, allowed)
    if kind == "desktop_discovered_media_playback":
        return _model_followup_discovered_media_playback_target_payload(target, allowed)
    if kind == "artifact_write":
        return _model_followup_artifact_write_target_payload(target, allowed)
    if kind == "note_write":
        return _model_followup_note_write_target_payload(target, allowed)
    if kind == "desktop_observed_action":
        return _model_followup_desktop_observed_action_target_payload(target, allowed)
    if kind == "current_input_write":
        return _model_followup_current_input_write_target_payload(target, allowed)
    if kind != "app_write" or not app_name:
        return {}
    container_action = _model_followup_container_action(target)
    write_tools = _model_followup_app_write_tool_names(allowed, container_action)
    verify_tools = [
        tool
        for tool in ("desktop.ui_elements", "desktop.active_window", "screen.capture")
        if tool in allowed
    ]
    payload: dict[str, Any] = {
        "kind": "app_write",
        "app_name": app_name,
        "target_action": str(target.get("target_action") or "app_paste").strip(),
        "body_source": "model_generated_content",
        "write_allowed": bool(write_tools),
        "recommended_tools": write_tools,
        "verify_tools": verify_tools,
    }
    if container_action:
        payload["container_action"] = container_action
    context_source = str(target.get("context_source") or "").strip()
    if context_source:
        payload["context_source"] = context_source
    artifact_write = _model_followup_chained_artifact_write_payload(target, allowed)
    if artifact_write:
        payload["artifact_write"] = artifact_write
    return payload


def _model_followup_artifact_write_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    path = str(target.get("path") or "").strip()
    if not path:
        return {}
    write_allowed = "artifact.write" in allowed
    payload: dict[str, Any] = {
        "kind": "artifact_write",
        "target_action": str(target.get("target_action") or "write_artifact").strip(),
        "path": path,
        "body_source": "model_generated_content",
        "write_allowed": write_allowed,
        "recommended_tools": ["artifact.write"] if write_allowed else [],
    }
    context_source = str(target.get("context_source") or "").strip()
    if context_source:
        payload["context_source"] = context_source
    intent_kind = str(target.get("intent_kind") or "").strip()
    if intent_kind:
        payload["intent_kind"] = intent_kind
    return payload


def _model_followup_current_input_write_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    write_tools = _model_followup_current_input_write_tool_names(allowed)
    verify_tools = [
        tool
        for tool in ("desktop.ui_elements", "desktop.active_window", "screen.capture")
        if tool in allowed
    ]
    payload: dict[str, Any] = {
        "kind": "current_input_write",
        "target_action": str(target.get("target_action") or "current_input_write").strip(),
        "body_source": "model_generated_content",
        "write_allowed": bool(write_tools),
        "recommended_tools": write_tools,
        "verify_tools": verify_tools,
    }
    context_source = str(target.get("context_source") or "").strip()
    if context_source:
        payload["context_source"] = context_source
    artifact_write = _model_followup_chained_artifact_write_payload(target, allowed)
    if artifact_write:
        payload["artifact_write"] = artifact_write
    return payload


def _model_followup_note_write_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    write_allowed = "notes.create" in allowed
    payload: dict[str, Any] = {
        "kind": "note_write",
        "target_action": str(target.get("target_action") or "create_note").strip(),
        "body_source": "model_generated_content",
        "write_allowed": write_allowed,
        "recommended_tools": ["notes.create"] if write_allowed else [],
    }
    context_source = str(target.get("context_source") or "").strip()
    if context_source:
        payload["context_source"] = context_source
    title = str(target.get("title") or "").strip()
    if title:
        payload["title"] = title
    folder_name = str(target.get("folder_name") or "").strip()
    if folder_name:
        payload["folder_name"] = folder_name
    return payload


def _model_followup_desktop_observed_action_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    target_action = str(target.get("target_action") or "").strip()
    if target_action not in {"click", "type_text"}:
        return {}
    target_label = str(target.get("target") or "").strip()
    if not target_label:
        return {}
    recommended_tools = _model_followup_desktop_observed_action_tools(
        target_action,
        allowed,
        app_scoped=bool(_observed_action_app_name(target)),
    )
    verify_tools = [
        tool
        for tool in (
            "desktop.ui_elements",
            "desktop.read_ui",
            "desktop.active_window",
            "screen.capture",
        )
        if tool in allowed
    ]
    payload: dict[str, Any] = {
        "kind": "desktop_observed_action",
        "target_action": target_action,
        "target": target_label,
        "role_filter": str(target.get("role_filter") or "").strip(),
        "limit": _clean_model_followup_int(target.get("limit"), default=80),
        "action_allowed": bool(recommended_tools),
        "recommended_tools": recommended_tools,
        "verify_tools": verify_tools,
        "observation_source": str(
            target.get("observation_source") or "desktop.read_ui"
        ).strip(),
    }
    app_name = _observed_action_app_name(target)
    if app_name:
        payload["app_name"] = app_name
    app_query = _observed_action_app_query(target)
    if app_query:
        payload["app_query"] = app_query
    if target_action == "click":
        payload["click_count"] = _clean_model_followup_int(
            target.get("click_count"),
            default=1,
        )
    else:
        text = str(target.get("text") or "")
        if not text:
            return {}
        payload["text"] = text
        payload["body_source"] = str(target.get("body_source") or "explicit_user_text").strip()
    return payload


def _model_followup_desktop_observed_action_tools(
    target_action: str,
    allowed: set[str],
    *,
    app_scoped: bool = False,
) -> list[str]:
    app_candidates = (
        (
            "app.focus_and_click_ui_element",
            "app.open_and_click_ui_element",
        )
        if target_action == "click"
        else (
            "app.focus_and_type_into_ui_element",
            "app.open_and_type_into_ui_element",
            "app.focus_and_click_ui_element",
            "app.open_and_click_ui_element",
        )
    )
    candidates = (
        (
            "desktop.click_ui_element",
            "desktop.click",
        )
        if target_action == "click"
        else (
            "desktop.type_into_ui_element",
            "desktop.click_ui_element",
            "desktop.click",
            "desktop.type_text",
            "desktop.type",
        )
    )
    if app_scoped:
        candidates = (*app_candidates, *candidates)
    return [tool for tool in candidates if tool in allowed]


def _clean_model_followup_int(value: Any, *, default: int) -> int:
    try:
        return int(value)
    except (TypeError, ValueError):
        return default


def _model_followup_desktop_discovered_app_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    app_query = str(target.get("app_query") or "").strip()
    if not app_query:
        return {}
    recommended_tools = [
        tool
        for tool in (
            "desktop.list_apps",
            "app.open_and_safe_shortcut",
            "app.focus_and_safe_shortcut",
            "app.open",
            "app.focus",
            "desktop.open_path_with_app",
            "app.open_path_with_app",
            "desktop.safe_shortcut",
            "desktop.safe_type_text",
            "desktop.click_ui_element",
            "desktop.type_into_ui_element",
            "desktop.inspect_app",
            "app.focus_and_type_into_ui_element",
            "desktop.search_submit",
            "desktop.submit_foreground",
        )
        if tool in allowed
    ]
    verify_tools = [
        tool
        for tool in ("desktop.ui_elements", "desktop.active_window", "screen.capture")
        if tool in allowed
    ]
    payload: dict[str, Any] = {
        "kind": "desktop_discovered_app_action",
        "app_query": app_query,
        "app_name_source": str(target.get("app_name_source") or "desktop.list_apps").strip(),
        "target_action": str(target.get("target_action") or "").strip(),
        "recommended_tools": recommended_tools,
        "verify_tools": verify_tools,
    }
    safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
    if safe_shortcut_action:
        payload["safe_shortcut_action"] = safe_shortcut_action
    target_path = str(target.get("target_path") or "").strip()
    if target_path:
        payload["target_path"] = target_path
    compose_text = str(target.get("compose_text") or "").strip()
    if compose_text:
        payload["compose_text"] = compose_text
        payload["body_source"] = str(target.get("body_source") or "explicit_user_text").strip()
    body_source = str(target.get("body_source") or "").strip()
    if body_source and "body_source" not in payload:
        payload["body_source"] = body_source
    context_source = str(target.get("context_source") or "").strip()
    if context_source:
        payload["context_source"] = context_source
    source_action = str(target.get("source_action") or "").strip()
    if source_action:
        payload["source_action"] = source_action
    dynamic_context_transfer = (
        target.get("dynamic_context_transfer")
        if isinstance(target.get("dynamic_context_transfer"), Mapping)
        else {}
    )
    if dynamic_context_transfer:
        payload["dynamic_context_transfer"] = dict(dynamic_context_transfer)
    app_search = _discovered_app_search_payload(target)
    if app_search:
        payload["app_search"] = app_search
    communication_compose = _discovered_app_communication_compose_payload(target)
    if communication_compose:
        payload["communication_compose"] = communication_compose
        payload["send_allowed"] = (
            communication_compose.get("send_action") == "send"
            and "desktop.submit_foreground" in allowed
        )
        transform = str(
            target.get("content_transform_hint")
            or target.get("transform")
            or ""
        ).strip()
        if transform:
            payload["transform"] = transform
    artifact_write = _model_followup_chained_artifact_write_payload(target, allowed)
    if artifact_write:
        payload["artifact_write"] = artifact_write
    creative_canvas = (
        target.get("creative_canvas")
        if isinstance(target.get("creative_canvas"), Mapping)
        else {}
    )
    if creative_canvas:
        payload["creative_canvas"] = dict(creative_canvas)
    post_action_observation = (
        target.get("post_action_observation")
        if isinstance(target.get("post_action_observation"), Mapping)
        else {}
    )
    if post_action_observation:
        payload["post_action_observation"] = dict(post_action_observation)
    pending_user_action = str(target.get("pending_user_action") or "").strip()
    if pending_user_action:
        payload["pending_user_action"] = pending_user_action
    return payload


def _model_followup_discovered_media_playback_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    app_query = str(target.get("app_query") or "").strip()
    media_query = str(target.get("media_playback_query") or "").strip()
    if not app_query or not media_query:
        return {}
    recommended_tools = [
        tool
        for tool in (
            "desktop.list_apps",
            "app.open_and_safe_shortcut",
            "app.focus_and_safe_shortcut",
            "app.open",
            "app.focus",
            "desktop.safe_shortcut",
            "desktop.safe_click",
            "desktop.safe_type_text",
            "desktop.search_submit",
            "desktop.submit_foreground",
            "media.music_app_open_and_play",
            "app.focus_and_click_ui_element",
            "app.open_and_click_ui_element",
            "desktop.click_ui_element",
        )
        if tool in allowed
    ]
    verify_tools = [
        tool
        for tool in ("desktop.ui_elements", "desktop.active_window", "screen.capture")
        if tool in allowed
    ]
    payload = {
        "kind": "desktop_discovered_media_playback",
        "app_query": app_query,
        "app_name_source": str(target.get("app_name_source") or "desktop.list_apps").strip(),
        "target_action": str(target.get("target_action") or "safe_shortcut").strip(),
        "safe_shortcut_action": str(target.get("safe_shortcut_action") or "find").strip(),
        "media_playback_query": media_query,
        "recommended_tools": recommended_tools,
        "verify_tools": verify_tools,
    }
    app_name = str(target.get("app_name") or target.get("target_app_name") or "").strip()
    if app_name:
        payload["app_name"] = app_name
    return payload


def _model_followup_communication_target_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    recipient = str(target.get("recipient") or "").strip()
    if not recipient:
        return {}
    channel = str(target.get("channel") or "").strip()
    app_name = str(target.get("app_name") or "").strip()
    if not app_name and channel == "email":
        app_name = "Mail"
    if not app_name:
        return {}
    send_action = str(target.get("send_action") or "send").strip() or "send"
    mode = str(target.get("mode") or "focus").strip() or "focus"
    prepare_tools = _model_followup_communication_prepare_tool_names(
        allowed,
        app_name=app_name,
        mode=mode,
        channel=channel,
    )
    draft_allowed = bool(
        prepare_tools
        and "desktop.safe_type_text" in allowed
        and "desktop.search_submit" in allowed
    )
    send_allowed = send_action == "send" and "desktop.submit_foreground" in allowed
    verify_tools = [
        tool
        for tool in ("desktop.ui_elements", "desktop.active_window", "screen.capture")
        if tool in allowed
    ]
    payload: dict[str, Any] = {
        "kind": "communication_message",
        "app_name": app_name,
        "recipient": recipient,
        "body_source": "model_generated_content",
        "draft_allowed": draft_allowed,
        "mode": mode,
        "send_action": send_action,
        "send_allowed": send_allowed,
        "recommended_tools": [
            *prepare_tools,
            "desktop.safe_type_text",
            "desktop.search_submit",
            "desktop.safe_type_text",
            *(["desktop.submit_foreground"] if send_allowed else []),
        ] if draft_allowed else prepare_tools,
        "verify_tools": verify_tools,
    }
    if channel:
        payload["channel"] = channel
    transform = str(target.get("transform") or "").strip()
    if transform:
        payload["transform"] = transform
    artifact_write = _model_followup_chained_artifact_write_payload(target, allowed)
    if artifact_write:
        payload["artifact_write"] = artifact_write
    return payload


def _model_followup_chained_artifact_write_payload(
    target: Mapping[str, Any],
    allowed: set[str],
) -> dict[str, Any]:
    raw = (
        target.get("artifact_write")
        if isinstance(target.get("artifact_write"), Mapping)
        else {}
    )
    path = str(raw.get("path") or "").strip()
    if not path:
        return {}
    write_allowed = "artifact.write" in allowed
    payload: dict[str, Any] = {
        "target_action": str(raw.get("target_action") or "write_artifact").strip(),
        "path": path,
        "body_source": str(raw.get("body_source") or "model_generated_content").strip(),
        "tool": str(raw.get("tool") or "artifact.write").strip(),
        "write_allowed": write_allowed,
        "recommended_tools": ["artifact.write"] if write_allowed else [],
    }
    intent_kind = str(raw.get("intent_kind") or "").strip()
    if intent_kind:
        payload["intent_kind"] = intent_kind
    return payload


def _selection_payload_has_model_followup_target(
    selection_payload: Mapping[str, Any],
) -> bool:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    kind = str(target.get("kind") or "").strip()
    if kind == "app_write":
        return bool(str(target.get("app_name") or "").strip())
    if kind == "communication_message":
        return bool(str(target.get("recipient") or "").strip())
    if kind == "artifact_write":
        return bool(str(target.get("path") or "").strip())
    if kind == "note_write":
        return True
    if kind == "desktop_observed_action":
        return bool(str(target.get("target") or "").strip())
    if kind == "desktop_discovered_app_action":
        return bool(str(target.get("app_query") or "").strip())
    if kind == "desktop_discovered_media_playback":
        return bool(str(target.get("app_query") or "").strip())
    return False


def _only_persists_model_generated_artifacts(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    items = [request for request in requests if isinstance(request, Mapping)]
    return bool(items) and all(
        str(request.get("tool") or "").strip() == "artifact.write"
        and isinstance(request.get("input"), Mapping)
        and bool(str(request["input"].get("content") or "").strip())
        for request in items
    )


def _selection_payload_has_discovered_media_playback_target(
    selection_payload: Mapping[str, Any],
) -> bool:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    return str(target.get("kind") or "").strip() == "desktop_discovered_media_playback"


def _auto_discovered_followup_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    for factory in (
        _auto_desktop_observed_action_followup_requests,
        _auto_communication_message_followup_requests,
        _auto_discovered_app_search_observed_result_requests,
        _auto_discovered_app_observed_pending_action_requests,
        _auto_discovered_app_followup_requests,
        _auto_discovered_media_playback_followup_requests,
    ):
        requests = factory(selection_payload, allowed_tools, timeline)
        if requests:
            return requests
    return []


_CODE_CONTEXT_READ_EXTENSIONS = {
    ".c",
    ".cc",
    ".cjs",
    ".cpp",
    ".css",
    ".go",
    ".h",
    ".hpp",
    ".html",
    ".java",
    ".js",
    ".json",
    ".jsx",
    ".kt",
    ".md",
    ".mjs",
    ".py",
    ".rb",
    ".rs",
    ".scss",
    ".sh",
    ".svelte",
    ".swift",
    ".toml",
    ".ts",
    ".tsx",
    ".vue",
    ".yaml",
    ".yml",
}

_CODE_CONTEXT_CONFIG_FILES = {
    "package.json",
    "pyproject.toml",
    "tsconfig.json",
    "vite.config.js",
    "vite.config.ts",
}

_CODE_CONTEXT_SKIP_PARTS = {
    ".git",
    ".next",
    ".venv",
    "__pycache__",
    "build",
    "coverage",
    "dist",
    "node_modules",
}


def _auto_code_context_read_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    max_files: int = 3,
) -> list[dict[str, Any]]:
    if str(selection_payload.get("intent_kind") or "").strip() != "code_task":
        return []
    allowed = {
        str(tool or "").strip()
        for tool in allowed_tools
        if str(tool or "").strip()
    }
    read_tool = _first_allowed_tool(
        ("workspace.read", "file.read", "fs.read_file"),
        allowed,
    )
    if not read_tool:
        return []
    already_read = _code_context_already_read_paths(timeline)
    selected_paths = _select_code_context_candidate_paths(
        timeline,
        already_read=already_read,
        max_files=max_files,
    )
    requests: list[dict[str, Any]] = []
    for index, path in enumerate(selected_paths, start=1):
        request = _request_like(
            read_tool,
            {"path": path},
            source="runtime_planner",
            planning_reason="planner_auto_code_context_read",
        )
        request["step_id"] = f"read-code-context-candidate-{index}"
        request["capability_id"] = "file.workspace_read"
        request["intent_kind"] = "code_task"
        request["runtime_doctrine"] = "discover_operate_verify"
        request["runtime_stage"] = "discover"
        request["runtime_role"] = "inspect_workspace"
        request["requires_observation"] = True
        request["requires_post_action_verification"] = False
        _attach_selection_trace_fields(request, selection_payload)
        requests.append(request)
    if requests:
        requests[-1]["continue_to_model"] = True
    return requests


def _attach_selection_trace_fields(
    request: dict[str, Any],
    selection_payload: Mapping[str, Any],
) -> None:
    for key in (
        "decision_id",
        "plan_id",
        "tool_plan_id",
        "core_id",
        "workspace_id",
        "task_id",
    ):
        value = str(selection_payload.get(key) or "").strip()
        if value:
            request[key] = value


def _select_code_context_candidate_paths(
    timeline: list[dict[str, Any]],
    *,
    already_read: set[str],
    max_files: int,
) -> list[str]:
    candidates: dict[str, tuple[int, int]] = {}
    order = 0
    for event in timeline:
        if not _code_context_search_event(event):
            continue
        result = event.get("result") if isinstance(event.get("result"), dict) else {}
        if result.get("ok") is not True:
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), dict)
            else {}
        )
        base_path = str(result.get("path") or input_preview.get("path") or "").strip()
        pattern = str(
            input_preview.get("pattern")
            or (result.get("filter") if isinstance(result.get("filter"), dict) else {}).get("pattern")
            or ""
        ).strip()
        entries = result.get("entries") if isinstance(result.get("entries"), list) else []
        for entry in entries:
            path = _code_context_entry_path(entry, base_path)
            if not path or path in already_read or not _code_context_readable_path(path):
                continue
            score = _code_context_candidate_score(path, pattern)
            if score <= 0:
                continue
            order += 1
            previous = candidates.get(path)
            value = (score, -order)
            if previous is None or value > previous:
                candidates[path] = value
    ranked = sorted(candidates.items(), key=lambda item: item[1], reverse=True)
    clean_max = max(1, int(max_files or 3))
    return [path for path, _score in ranked[:clean_max]]


def _code_context_search_event(event: Mapping[str, Any]) -> bool:
    if event.get("event") != "agent.tool.call":
        return False
    tool_name = str(event.get("detail") or event.get("tool") or "").strip()
    if tool_name not in {"workspace.list", "file.search", "fs.find_files"}:
        return False
    planning_reason = str(event.get("planning_reason") or "").strip()
    if planning_reason:
        return planning_reason == "planner_prefetch_code_context"
    step_id = str(event.get("step_id") or "").strip()
    return step_id == "inspect-workspace" or step_id.startswith("inspect-code-area-")


def _code_context_already_read_paths(timeline: list[dict[str, Any]]) -> set[str]:
    paths: set[str] = set()
    for event in timeline:
        if event.get("event") != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or event.get("tool") or "").strip()
        if tool_name not in {"workspace.read", "file.read", "fs.read_file"}:
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), dict)
            else {}
        )
        path = str(input_preview.get("path") or "").strip()
        if path:
            paths.add(path)
    return paths


def _code_context_entry_path(entry: Any, base_path: str) -> str:
    if isinstance(entry, str):
        return _join_workspace_list_path(base_path, entry)
    if not isinstance(entry, Mapping):
        return ""
    entry_type = str(entry.get("type") or entry.get("kind") or "").strip()
    if entry_type and entry_type != "file":
        return ""
    path = str(entry.get("path") or "").strip()
    if path:
        return path.strip("/")
    name = str(entry.get("name") or "").strip()
    if not name:
        return ""
    return _join_workspace_list_path(base_path, name)


def _code_context_readable_path(path: str) -> bool:
    clean = str(path or "").strip().strip("/")
    if not clean:
        return False
    parts = {part for part in clean.split("/") if part}
    if parts & _CODE_CONTEXT_SKIP_PARTS:
        return False
    name = posixpath.basename(clean)
    if name in _CODE_CONTEXT_CONFIG_FILES:
        return True
    extension = _code_context_extension(name)
    return extension in _CODE_CONTEXT_READ_EXTENSIONS


def _code_context_candidate_score(path: str, pattern: str) -> int:
    clean = str(path or "").strip()
    name = posixpath.basename(clean).lower()
    extension = _code_context_extension(name)
    score = 0
    if name in _CODE_CONTEXT_CONFIG_FILES:
        score += 45
    if extension in {".ts", ".tsx", ".py", ".js", ".jsx"}:
        score += 70
    elif extension in _CODE_CONTEXT_READ_EXTENSIONS:
        score += 35
    lowered = clean.lower()
    if "/test" in lowered or "test_" in name or name.endswith((".test.ts", ".test.tsx")):
        score += 20
    for term in re.findall(r"[a-zA-Z0-9_]+", str(pattern or "").lower()):
        if len(term) >= 3 and term in lowered:
            score += 30
    return score


def _code_context_extension(name: str) -> str:
    lowered = str(name or "").lower()
    if lowered.endswith(".d.ts"):
        return ".ts"
    return posixpath.splitext(lowered)[1]


def _auto_communication_message_followup_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_followup_communication_observed_compose",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "communication_message":
        return []
    if not _latest_desktop_observation_succeeded(timeline):
        return []
    app_name = str(target.get("app_name") or "").strip()
    recipient = str(target.get("recipient") or "").strip()
    body = str(target.get("body") or "").strip()
    if not app_name or not recipient or not body:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    channel = str(target.get("channel") or "").strip()
    recipient_target = {
        "kind": "desktop_observed_action",
        "target_action": "type_text",
        "target": _discovered_communication_recipient_target(channel),
        "text": recipient,
        "role_filter": "text",
        "limit": 80,
        "app_name": app_name,
        "submit_action": "confirm",
    }
    observation_source = _latest_desktop_observation_tool(timeline)
    if observation_source:
        recipient_target["observation_source"] = observation_source
    recipient_requests = _auto_desktop_observed_action_followup_requests(
        {"followup_target": recipient_target},
        allowed,
        timeline,
        planning_reason=planning_reason,
    )
    if not recipient_requests:
        return []
    body_observation = _communication_message_body_observation_request(
        app_name,
        body,
        target,
        allowed,
        source="runtime_planner",
        planning_reason=planning_reason,
    )
    if body_observation:
        recipient_requests.append(body_observation)
    return recipient_requests


def _communication_message_body_observation_request(
    app_name: str,
    body: str,
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    type_tool = _communication_observed_deferred_type_tool(allowed)
    observe_tool = _first_allowed_tool(
        ("desktop.ui_elements", "desktop.read_ui"),
        allowed,
    )
    if not type_tool or not observe_tool:
        return {}
    channel = str(target.get("channel") or "").strip()
    deferred_input = {
        "app_name": app_name,
        "target": _discovered_communication_body_target(channel),
        "text": body,
        "role_filter": "text",
        "limit": 80,
    }
    request = _request_like(
        observe_tool,
        {"app_name": app_name, "role_filter": "text", "limit": 80},
        source=source,
        planning_reason=planning_reason,
    )
    request["continue_to_model"] = True
    request["deferred_tool"] = type_tool
    request["deferred_input"] = deferred_input
    send_action = str(target.get("send_action") or "send").strip() or "send"
    if send_action == "send" and "desktop.submit_foreground" in allowed:
        send_request = _request_like(
            "desktop.submit_foreground",
            {"action": "send"},
            source=source,
            planning_reason=planning_reason,
        )
        send_request["approval_required"] = True
        send_request["risk_level"] = "high"
        request["deferred_continuation"] = [send_request]
    return request


def _communication_observed_deferred_type_tool(allowed: set[str]) -> str:
    return _first_allowed_tool(
        (
            "app.focus_and_type_into_ui_element",
            "app.open_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        ),
        allowed,
    ) or (
        "desktop.type_into_ui_element"
        if _first_allowed_tool(
            ("desktop.safe_type_text", "desktop.type_text", "desktop.type"),
            allowed,
        )
        and _first_allowed_tool(
            (
                "app.focus_and_click_ui_element",
                "app.open_and_click_ui_element",
                "desktop.click_ui_element",
                "desktop.safe_click",
                "desktop.click",
            ),
            allowed,
        )
        else ""
    )


def _auto_discovered_app_observed_pending_action_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_followup_discovered_app_observed_action",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    pending_action = str(target.get("pending_user_action") or "").strip()
    if not pending_action or not _latest_desktop_observation_succeeded(timeline):
        return []
    observed_target = _observed_action_target_from_discovered_pending_action(
        pending_action,
    )
    if not observed_target:
        return []
    _attach_discovered_app_context_to_observed_target(
        observed_target,
        target,
        timeline,
    )
    observation_source = _latest_desktop_observation_tool(timeline)
    if observation_source:
        observed_target["observation_source"] = observation_source
    target_label = str(observed_target.get("target") or "").strip()
    role_filter = str(observed_target.get("role_filter") or "").strip()
    if not _latest_desktop_observation_has_target_match(
        timeline,
        target_label,
        role_filter,
    ):
        retry_request = _discovered_app_observed_pending_observation_retry_request(
            target,
            observed_target,
            {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()},
            timeline,
            planning_reason=planning_reason,
        )
        if retry_request:
            return _annotate_auto_followup_requests_from_tool_plan(
                [retry_request],
                selection_payload,
            )
        return []
    requests = _auto_desktop_observed_action_followup_requests(
        {"followup_target": observed_target},
        allowed_tools,
        timeline,
        planning_reason=planning_reason,
    )
    return _annotate_auto_followup_requests_from_tool_plan(requests, selection_payload)


def _discovered_app_observed_pending_observation_retry_request(
    discovered_target: Mapping[str, Any],
    observed_target: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> dict[str, Any]:
    latest_tool = _latest_desktop_observation_tool(timeline)
    if latest_tool != "desktop.ui_elements" or "desktop.read_ui" not in allowed:
        return {}
    target_label = str(observed_target.get("target") or "").strip()
    if not target_label:
        return {}
    input_payload = {
        "role_filter": str(observed_target.get("role_filter") or "").strip(),
        "limit": _clean_model_followup_int(observed_target.get("limit"), default=80),
    }
    request = _request_like(
        "desktop.read_ui",
        input_payload,
        source="runtime_planner",
        planning_reason=planning_reason,
    )
    request["continue_to_model"] = True
    request["observation_retry"] = {
        "from_tool": latest_tool,
        "reason": "target_not_found",
        "target": target_label,
    }
    request["followup_target"] = _compact_observed_action_target(observed_target)
    app_name = _observed_action_app_name(observed_target)
    if app_name:
        request["target_app_name"] = app_name
    app_query = (
        _observed_action_app_query(observed_target)
        or str(discovered_target.get("app_query") or "").strip()
    )
    if app_query:
        request["target_app_query"] = app_query
    return request


def _observed_action_target_from_discovered_pending_action(
    pending_action: str,
) -> dict[str, Any]:
    type_target = type_into_ui_hint(pending_action)
    if isinstance(type_target, Mapping) and type_target:
        target_label = str(type_target.get("target") or "").strip()
        text = str(type_target.get("text") or "").strip()
        if target_label and text:
            return {
                "kind": "desktop_observed_action",
                "target_action": "type_text",
                "target": target_label,
                "text": text,
                "role_filter": str(type_target.get("role_filter") or "text").strip(),
                "limit": _clean_model_followup_int(type_target.get("limit"), default=80),
                "body_source": "explicit_user_text",
            }
    click_target = click_target_hint(pending_action)
    if isinstance(click_target, Mapping) and click_target:
        target_label = str(click_target.get("target") or "").strip()
        if target_label:
            return {
                "kind": "desktop_observed_action",
                "target_action": "click",
                "target": target_label,
                "role_filter": str(click_target.get("role_filter") or "").strip(),
                "click_count": _clean_model_followup_int(
                    click_target.get("click_count"),
                    default=1,
                ),
                "limit": _clean_model_followup_int(click_target.get("limit"), default=80),
            }
    return {}


def _auto_discovered_app_search_observed_result_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_followup_app_search_observed_result",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    if str(target.get("target_action") or "").strip() != "app_search":
        return []
    if not _latest_desktop_observation_succeeded(timeline):
        return []
    if not _latest_desktop_observation_follows_search_submission(timeline):
        return []
    app_search = _discovered_app_search_payload(target)
    result_selection = _discovered_app_search_result_selection(app_search)
    if str(result_selection.get("action") or "").strip() != "click":
        return []
    observed_target = _observed_action_target_from_app_search_result_selection(
        result_selection,
    )
    if not observed_target:
        return []
    _attach_discovered_app_context_to_observed_target(observed_target, target, timeline)
    latest_observation_tool = _latest_desktop_observation_tool(timeline)
    if latest_observation_tool == "desktop.inspect_app":
        observed_target["observation_source"] = latest_observation_tool
    requests = _auto_desktop_observed_action_followup_requests(
        {"followup_target": observed_target},
        allowed_tools,
        timeline,
        planning_reason=planning_reason,
    )
    return _annotate_auto_followup_requests_from_tool_plan(requests, selection_payload)


def _auto_replan_app_search_observed_result_requests(
    replan_payloads: Iterable[Mapping[str, Any]],
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    payloads = [
        dict(payload) for payload in replan_payloads if isinstance(payload, Mapping)
    ]
    requests = _auto_discovered_app_search_observed_result_requests(
        selection_payload,
        allowed_tools,
        timeline,
        planning_reason=planning_reason,
    )
    if requests:
        enriched = _replan_recovery_requests_with_task_context(
            requests,
            payloads,
            timeline,
        )
        return _drop_replayed_approved_replan_operate_requests(enriched, timeline)
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    if str(target.get("target_action") or "").strip() != "app_search":
        return []
    if not _replan_payloads_include_blocked_step(
        payloads,
        "select-app-search-result",
    ):
        return []
    if not _latest_desktop_observation_succeeded(timeline):
        return []
    if not _latest_desktop_observation_follows_search_submission(timeline):
        return []
    observed_target = {
        "kind": "desktop_observed_action",
        "target_action": "click",
        "target": "第一个结果",
        "role_filter": "",
        "click_count": 1,
        "limit": 80,
    }
    _attach_discovered_app_context_to_observed_target(
        observed_target,
        target,
        timeline,
        replan_payloads=payloads,
    )
    latest_observation_tool = _latest_desktop_observation_tool(timeline)
    if latest_observation_tool:
        observed_target["observation_source"] = latest_observation_tool
    requests = _auto_desktop_observed_action_followup_requests(
        {"followup_target": observed_target},
        allowed_tools,
        timeline,
        planning_reason=planning_reason,
    )
    requests = _annotate_auto_followup_requests_from_tool_plan(
        requests,
        selection_payload,
    )
    enriched = _replan_recovery_requests_with_task_context(
        requests,
        payloads,
        timeline,
    )
    return _drop_replayed_approved_replan_operate_requests(enriched, timeline)


def _drop_replayed_approved_replan_operate_requests(
    requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """Never replay an approved foreground mutation while replanning.

    Approval resume writes one canonical, identity-bearing tool result.  A later
    observation/replan may legitimately ask for another verifier, but it must
    not recreate the already executed operate step.  Matching is deliberately
    scoped to the exact plan and step plus the structured action signature; no
    summaries or other free-form text participate in the decision.
    """

    filtered: list[dict[str, Any]] = []
    for request in requests:
        item = dict(request)
        if not _replan_observed_action_operate_request(item):
            filtered.append(item)
            continue
        if not _canonical_approved_replan_action_state(item, timeline):
            filtered.append(item)
    return filtered


def _replayed_approved_replan_operate_requests_verified(
    requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
) -> bool:
    operate_requests = [
        request
        for request in requests
        if _replan_observed_action_operate_request(request)
    ]
    return bool(operate_requests) and all(
        _canonical_approved_replan_action_state(request, timeline) == "verified"
        for request in operate_requests
    )


def _replan_observed_action_operate_request(request: Mapping[str, Any]) -> bool:
    runtime_stage = str(request.get("runtime_stage") or "").strip()
    runtime_role = str(request.get("runtime_role") or "").strip()
    action_target = (
        request.get("action_target")
        if isinstance(request.get("action_target"), Mapping)
        else {}
    )
    action = str(action_target.get("action") or "").strip()
    if (
        runtime_stage == "verify"
        or runtime_role in {"verify_result", "inspect_ui"}
        or action == "verify_after_action"
    ):
        return False
    return runtime_stage == "operate" or action in {
        "click",
        "click_ui",
        "type_text",
        "type_ui",
    }


def _replan_observed_action_verification_request(
    request: Mapping[str, Any],
) -> bool:
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    runtime_stage = str(request.get("runtime_stage") or "").strip()
    runtime_role = str(request.get("runtime_role") or "").strip()
    action_target = (
        request.get("action_target")
        if isinstance(request.get("action_target"), Mapping)
        else {}
    )
    return bool(
        tool_name in _DAILY_DESKTOP_VERIFY_TOOLS
        and (
            runtime_stage == "verify"
            or runtime_role in {"verify_result", "inspect_ui"}
            or str(action_target.get("action") or "").strip()
            == "verify_after_action"
        )
    )


def _canonical_approved_replan_action_state(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> str:
    """Return ``executed``/``verified`` for an exact canonical approved action."""

    plan_id = str(request.get("plan_id") or "").strip()
    step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    request_tool = str(request.get("tool") or request.get("tool_name") or "").strip()
    signature = _replan_observed_action_semantic_signature(request)
    if not plan_id or not step_id or not request_tool or not signature:
        return ""
    request_id = str(request.get("request_id") or "").strip()
    semantic_hotkey = signature[0] == "hotkey"
    request_tool_call_id = str(request.get("tool_call_id") or "").strip()
    request_approval_id = str(request.get("approval_id") or "").strip()
    request_decision_id = str(request.get("decision_id") or "").strip()
    if semantic_hotkey and not all(
        (request_id, request_tool_call_id, request_approval_id)
    ):
        return ""

    canonical_index = -1
    for index in range(len(timeline) - 1, -1, -1):
        event = timeline[index]
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            event.get("approval_resume_result_canonical") is not True
            or event.get("approved") is not True
            or result.get("ok") is not True
            or result.get("approval_required")
        ):
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if event_step_id != step_id:
            continue
        if semantic_hotkey:
            if str(event.get("request_id") or "").strip() != request_id:
                continue
            if (
                str(event.get("tool_call_id") or "").strip()
                != request_tool_call_id
            ):
                continue
            if str(event.get("approval_id") or "").strip() != request_approval_id:
                continue
            if request_decision_id and (
                str(event.get("decision_id") or "").strip()
                != request_decision_id
            ):
                continue
        event_tool = str(
            event.get("detail") or event.get("tool") or event.get("tool_name") or ""
        ).strip()
        if not _followup_plan_tools_match(request_tool, event_tool):
            continue
        if semantic_hotkey:
            if str(result.get("action") or "").strip() != event_tool:
                continue
            result_data = (
                result.get("data")
                if isinstance(result.get("data"), Mapping)
                else {}
            )
            request_input = (
                request.get("input")
                if isinstance(request.get("input"), Mapping)
                else {}
            )
            if (
                str(result_data.get("key") or "").strip().casefold()
                != str(request_input.get("key") or "").strip().casefold()
                or _dispatch_receipt_modifiers(result_data.get("modifiers"))
                != _dispatch_receipt_modifiers(request_input.get("modifiers"))
            ):
                continue
        if _replan_observed_action_semantic_signature(event) != signature:
            continue
        canonical_index = index
        break
    if canonical_index < 0:
        return ""
    if _approved_replan_action_has_verified_task_progress(
        request,
        timeline,
        canonical_index=canonical_index,
    ):
        return "verified"
    return "executed"


def _replan_observed_action_semantic_signature(
    value: Mapping[str, Any],
) -> tuple[str, str, str, str, int, str]:
    payload = (
        value.get("input")
        if isinstance(value.get("input"), Mapping)
        else value.get("input_preview")
        if isinstance(value.get("input_preview"), Mapping)
        else {}
    )
    action_target = (
        value.get("action_target")
        if isinstance(value.get("action_target"), Mapping)
        else payload.get("action_target")
        if isinstance(payload.get("action_target"), Mapping)
        else {}
    )
    explicit_action = str(
        action_target.get("action")
        or value.get("runtime_role")
        or payload.get("runtime_role")
        or ""
    ).strip()
    explicit_action = {
        "click_ui": "click",
        "dispatch_shortcut": "hotkey",
        "keyboard_shortcut": "hotkey",
        "type_ui": "type_text",
    }.get(explicit_action, explicit_action)
    tool_name = str(
        value.get("tool") or value.get("tool_name") or value.get("detail") or ""
    ).strip()
    tool_action = _replan_observed_action_from_tool(tool_name)
    if explicit_action and tool_action and explicit_action != tool_action:
        return ()
    action = explicit_action or tool_action
    if action not in {"click", "hotkey", "type_text"}:
        return ()
    app_name = str(
        payload.get("app_name")
        or action_target.get("app_name")
        or value.get("target_app_name")
        or ""
    ).strip()
    if action == "hotkey":
        key = str(
            payload.get("key") or action_target.get("key") or ""
        ).strip().casefold()
        if not key:
            return ()
        modifiers = _dispatch_receipt_modifiers(
            payload.get("modifiers")
            if payload.get("modifiers") is not None
            else action_target.get("modifiers")
        )
        return (
            action,
            _strict_canonical_app_identity(app_name),
            key,
            ",".join(modifiers),
            0,
            "",
        )
    target = _normalize_observed_desktop_text(
        str(payload.get("target") or action_target.get("target") or "")
    )
    role_filter = str(
        payload.get("role_filter") or action_target.get("role_filter") or ""
    ).strip().casefold()
    click_count = (
        _clean_model_followup_int(payload.get("click_count"), default=1)
        if action == "click"
        else 0
    )
    text = str(payload.get("text") or "") if action == "type_text" else ""
    text_digest = hashlib.sha256(text.encode("utf-8")).hexdigest() if text else ""
    if not target:
        return ()
    return (
        action,
        _strict_canonical_app_identity(app_name),
        target,
        role_filter,
        click_count,
        text_digest,
    )


def _replan_observed_action_from_tool(tool_name: str) -> str:
    clean_tool = str(tool_name or "").strip()
    if clean_tool in {
        "desktop.hotkey",
        "desktop.shortcut",
    }:
        return "hotkey"
    if clean_tool in {
        "app.focus_and_click_ui_element",
        "app.open_and_click_ui_element",
        "desktop.click_ui_element",
        "desktop.safe_click",
        "desktop.click",
    }:
        return "click"
    if clean_tool in {
        "app.focus_and_type_into_ui_element",
        "app.open_and_type_into_ui_element",
        "app.focus_and_safe_type_text",
        "app.open_and_safe_type_text",
        "desktop.type_into_ui_element",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
    }:
        return "type_text"
    return ""


def _approved_replan_action_has_verified_task_progress(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    canonical_index: int,
) -> bool:
    plan_id = str(request.get("plan_id") or "").strip()
    step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    verified_progress: dict[
        str,
        dict[tuple[str, str], int],
    ] = {
        "agent.task.todo.updated": {},
        "agent.task.checkpoint.updated": {},
    }
    for event_index, event in enumerate(
        timeline[max(0, canonical_index + 1) :],
        start=max(0, canonical_index + 1),
    ):
        event_name = str(event.get("event") or event.get("event_type") or "").strip()
        base_event_name = _runtime_progress_base_event_type(event_name)
        if base_event_name not in verified_progress:
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        if str(event.get("step_id") or "").strip() != step_id:
            continue
        verifier_step_id = str(event.get("verified_by_step_id") or "").strip()
        verifier_tool = str(event.get("verification_tool") or "").strip()
        if str(event.get("status") or "").strip() != "completed":
            continue
        if str(event.get("verification_status") or "").strip() != "verified":
            continue
        if not verifier_step_id or not verifier_tool:
            continue
        if base_event_name == "agent.task.todo.updated":
            todo = event.get("todo") if isinstance(event.get("todo"), Mapping) else {}
            if str(todo.get("step_id") or "").strip() != step_id:
                continue
        else:
            checkpoint = (
                event.get("checkpoint")
                if isinstance(event.get("checkpoint"), Mapping)
                else {}
            )
            if str(checkpoint.get("after_step_id") or "").strip() != step_id:
                continue
            if step_id not in _string_list(checkpoint.get("verifies")):
                continue
        verified_progress[base_event_name][
            (verifier_step_id, verifier_tool)
        ] = event_index

    common_evidence = set(verified_progress["agent.task.todo.updated"]).intersection(
        verified_progress["agent.task.checkpoint.updated"]
    )
    for verifier_step_id, verifier_tool in common_evidence:
        progress_index = min(
            verified_progress["agent.task.todo.updated"][(verifier_step_id, verifier_tool)],
            verified_progress["agent.task.checkpoint.updated"][(verifier_step_id, verifier_tool)],
        )
        if _approved_replan_action_successful_verifier_event(
            timeline,
            canonical_index=canonical_index,
            progress_index=progress_index,
            plan_id=plan_id,
            verifier_step_id=verifier_step_id,
            verifier_tool=verifier_tool,
        ):
            return True
    return False


def _approved_replan_action_successful_verifier_event(
    timeline: list[dict[str, Any]],
    *,
    canonical_index: int,
    progress_index: int,
    plan_id: str,
    verifier_step_id: str,
    verifier_tool: str,
) -> bool:
    for event in timeline[max(0, canonical_index + 1) : max(0, progress_index)]:
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != verifier_tool:
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if event_step_id and event_step_id != verifier_step_id:
            continue
        request_id = str(event.get("request_id") or "").strip()
        if request_id and not request_id.startswith(f"{plan_id}:"):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            result.get("ok") is True
            and result.get("approval_required") is not True
            and result.get("verification_failed") is not True
        ):
            return True
    return False


def _replan_payloads_include_blocked_step(
    replan_payloads: Iterable[Mapping[str, Any]],
    step_id: str,
) -> bool:
    expected_step = str(step_id or "").strip()
    if not expected_step:
        return False
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
        task_context = (
            metadata.get("task_core_context")
            if isinstance(metadata.get("task_core_context"), Mapping)
            else {}
        )
        todos = task_context.get("todos") if isinstance(task_context.get("todos"), list) else []
        for todo in todos:
            if not isinstance(todo, Mapping):
                continue
            if str(todo.get("step_id") or "").strip() != expected_step:
                continue
            return str(todo.get("status") or "").strip() == "blocked"
    return False


def _observed_action_target_from_app_search_result_selection(
    result_selection: Mapping[str, Any],
) -> dict[str, Any]:
    raw_input = (
        result_selection.get("input")
        if isinstance(result_selection.get("input"), Mapping)
        else {}
    )
    target_label = str(
        raw_input.get("target")
        or result_selection.get("target")
        or "first result"
    ).strip()
    if not target_label:
        return {}
    role_filter = str(
        raw_input.get("role_filter")
        or result_selection.get("role_filter")
        or ""
    ).strip()
    return {
        "kind": "desktop_observed_action",
        "target_action": "click",
        "target": target_label,
        "role_filter": role_filter,
        "click_count": _clean_model_followup_int(
            raw_input.get("click_count", result_selection.get("click_count")),
            default=1,
        ),
        "limit": _clean_model_followup_int(
            raw_input.get("limit", result_selection.get("limit")),
            default=80,
        ),
        "observation_source": "desktop.read_ui",
    }


def _attach_discovered_app_context_to_observed_target(
    observed_target: dict[str, Any],
    source_target: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    replan_payloads: Iterable[Mapping[str, Any]] = (),
) -> None:
    app_name = _discovered_app_context_app_name(
        source_target,
        timeline,
        replan_payloads=replan_payloads,
    )
    if app_name:
        observed_target["app_name"] = app_name
    app_query = str(
        source_target.get("app_query") or source_target.get("target_app_query") or ""
    ).strip()
    if app_query:
        observed_target["app_query"] = app_query


def _discovered_app_context_app_name(
    target: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    replan_payloads: Iterable[Mapping[str, Any]] = (),
) -> str:
    for value in (
        target.get("app_name"),
        target.get("target_app_name"),
        target.get("resolved_app_name"),
        target.get("discovered_app_name"),
    ):
        app_name = str(value or "").strip()
        if app_name and not _runtime_planner_placeholder_app_name(app_name):
            return app_name
    app_query = str(target.get("app_query") or target.get("target_app_query") or "").strip()
    if app_query:
        discovered = _discovered_app_name_for_query(timeline, app_query)
        if discovered:
            return discovered
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        replan_target = _replan_recovery_target(payload)
        app_name = str(replan_target.get("target_app_name") or "").strip()
        if app_name and not _runtime_planner_placeholder_app_name(app_name):
            return app_name
        app_query = str(replan_target.get("target_app_query") or "").strip()
        if app_query:
            discovered = _discovered_app_name_for_query(timeline, app_query)
            if discovered:
                return discovered
    return ""


def _auto_desktop_observed_action_followup_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_followup_desktop_observed_action",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_observed_action":
        return []
    if not _latest_desktop_observation_succeeded(timeline):
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    target_action = str(target.get("target_action") or "").strip()
    if target_action == "click":
        return _auto_desktop_observed_click_requests(
            target,
            allowed,
            timeline,
            planning_reason=planning_reason,
        )
    if target_action == "type_text":
        return _auto_desktop_observed_type_requests(
            target,
            allowed,
            timeline,
            planning_reason=planning_reason,
        )
    return []


def _auto_desktop_observed_click_requests(
    target: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    target_label = str(target.get("target") or "").strip()
    if not target_label:
        return []
    role_filter = str(target.get("role_filter") or "").strip()
    click_count = _clean_model_followup_int(target.get("click_count"), default=1)
    limit = _clean_model_followup_int(target.get("limit"), default=80)
    app_name = _observed_action_app_name(target)
    app_scoped_tool = (
        _first_allowed_tool(
            ("app.focus_and_click_ui_element", "app.open_and_click_ui_element"),
            allowed,
        )
        if app_name
        else ""
    )
    if app_scoped_tool:
        request = _with_observed_action_metadata(
            _with_observed_action_app_resolution(
                _request_like(
                    app_scoped_tool,
                    {
                        "app_name": app_name,
                        "target": target_label,
                        "role_filter": role_filter,
                        "click_count": click_count,
                        "limit": limit,
                    },
                    source="runtime_planner",
                    planning_reason=planning_reason,
                ),
                target,
                app_name,
            ),
            target,
            action="click",
            target_label=target_label,
            role_filter=role_filter,
            evidence={"strategy": "app_scoped_semantic_ui_tool"},
        )
        return _with_observed_action_verification(
            [request],
            target,
            allowed,
            target_label=target_label,
            role_filter=role_filter,
            planning_reason=planning_reason,
        )
    if "desktop.click_ui_element" in allowed:
        request = _with_observed_action_metadata(
            _request_like(
                "desktop.click_ui_element",
                {
                    "target": target_label,
                    "role_filter": role_filter,
                    "click_count": click_count,
                    "limit": limit,
                },
                source="runtime_planner",
                planning_reason=planning_reason,
            ),
            target,
            action="click",
            target_label=target_label,
            role_filter=role_filter,
            evidence={"strategy": "semantic_ui_tool"},
        )
        return _with_observed_action_verification(
            [request],
            target,
            allowed,
            target_label=target_label,
            role_filter=role_filter,
            planning_reason=planning_reason,
        )
    low_level_tool = _first_allowed_tool(("desktop.safe_click", "desktop.click"), allowed)
    if not low_level_tool:
        return []
    center = _latest_desktop_observation_match_center(
        timeline,
        target_label,
        role_filter,
    )
    if not center:
        return []
    request = _with_observed_action_metadata(
        _request_like(
            low_level_tool,
            _observed_click_input(low_level_tool, center, click_count),
            source="runtime_planner",
            planning_reason=planning_reason,
        ),
        target,
        action="click",
        target_label=target_label,
        role_filter=role_filter,
        evidence={"strategy": "observed_center", "center": center},
    )
    return _with_observed_action_verification(
        [request],
        target,
        allowed,
        target_label=target_label,
        role_filter=role_filter,
        planning_reason=planning_reason,
    )


def _with_observed_action_verification(
    requests: list[dict[str, Any]],
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    target_label: str,
    role_filter: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    verification = _observed_action_verification_request(
        target,
        allowed,
        target_label=target_label,
        role_filter=role_filter,
        planning_reason=planning_reason,
    )
    return [*requests, *([verification] if verification else [])]


def _observed_action_verification_request(
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    target_label: str,
    role_filter: str,
    planning_reason: str,
) -> dict[str, Any]:
    tool_name = _first_allowed_tool(
        ("desktop.read_ui", "desktop.ui_elements", "desktop.active_window"),
        allowed,
    )
    if not tool_name:
        return {}
    input_payload: dict[str, Any] = {}
    if tool_name in {"desktop.read_ui", "desktop.ui_elements"}:
        input_payload = {
            "role_filter": str(role_filter or "").strip(),
            "limit": _clean_model_followup_int(target.get("limit"), default=80),
        }
    request = _with_observed_action_metadata(
        _request_like(
            tool_name,
            input_payload,
            source="runtime_planner",
            planning_reason=planning_reason,
        ),
        target,
        action="verify_after_action",
        target_label=target_label,
        role_filter=role_filter,
        evidence={"strategy": "post_action_observation"},
    )
    request["requires_observation"] = True
    request["requires_post_action_verification"] = False
    return request


def _auto_desktop_observed_type_requests(
    target: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    target_label = str(target.get("target") or "").strip()
    text = str(target.get("text") or "")
    if not target_label or not text:
        return []
    role_filter = str(target.get("role_filter") or "").strip()
    execution_target = _desktop_observed_action_execution_target(
        target_label,
        role_filter,
    )
    app_name = _observed_action_app_name(target)
    base_input = {
        "target": execution_target,
        "text": text,
        "role_filter": role_filter,
        "limit": _clean_model_followup_int(target.get("limit"), default=80),
    }
    app_scoped_type_tool = (
        _first_allowed_tool(
            ("app.focus_and_type_into_ui_element", "app.open_and_type_into_ui_element"),
            allowed,
        )
        if app_name
        else ""
    )
    if app_scoped_type_tool:
        request = _with_observed_action_metadata(
            _with_observed_action_app_resolution(
                _request_like(
                    app_scoped_type_tool,
                    {"app_name": app_name, **base_input},
                    source="runtime_planner",
                    planning_reason=planning_reason,
                ),
                target,
                app_name,
            ),
            target,
            action="type_text",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "app_scoped_semantic_ui_tool"},
        )
        return _with_observed_action_verification(
            [request],
            target,
            allowed,
            target_label=execution_target,
            role_filter=role_filter,
            planning_reason=planning_reason,
        )
    if "desktop.type_into_ui_element" in allowed:
        request = _with_observed_action_metadata(
            _request_like(
                "desktop.type_into_ui_element",
                base_input,
                source="runtime_planner",
                planning_reason=planning_reason,
            ),
            target,
            action="type_text",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "semantic_ui_tool"},
        )
        return _with_observed_action_verification(
            [request],
            target,
            allowed,
            target_label=execution_target,
            role_filter=role_filter,
            planning_reason=planning_reason,
        )
    type_tool = _first_allowed_tool(
        ("desktop.safe_type_text", "desktop.type_text", "desktop.type"),
        allowed,
    )
    if not type_tool:
        return []
    app_scoped_focus_tool = (
        _first_allowed_tool(
            ("app.focus_and_click_ui_element", "app.open_and_click_ui_element"),
            allowed,
        )
        if app_name
        else ""
    )
    if app_scoped_focus_tool:
        click_request = _with_observed_action_metadata(
            _with_observed_action_app_resolution(
                _request_like(
                    app_scoped_focus_tool,
                    {
                        "app_name": app_name,
                        "target": execution_target,
                        "role_filter": role_filter,
                        "click_count": 1,
                        "limit": _clean_model_followup_int(target.get("limit"), default=80),
                    },
                    source="runtime_planner",
                    planning_reason=planning_reason,
                ),
                target,
                app_name,
            ),
            target,
            action="focus_for_type",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "app_scoped_semantic_ui_tool"},
        )
    elif "desktop.click_ui_element" in allowed:
        click_request = _with_observed_action_metadata(
            _request_like(
                "desktop.click_ui_element",
                {
                    "target": execution_target,
                    "role_filter": role_filter,
                    "click_count": 1,
                    "limit": _clean_model_followup_int(target.get("limit"), default=80),
                },
                source="runtime_planner",
                planning_reason=planning_reason,
            ),
            target,
            action="focus_for_type",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "semantic_ui_tool"},
        )
    else:
        low_level_tool = _first_allowed_tool(("desktop.safe_click", "desktop.click"), allowed)
        if not low_level_tool:
            return []
        center = _latest_desktop_observation_match_center(
            timeline,
            execution_target,
            role_filter,
        )
        if not center:
            return []
        click_request = _with_observed_action_metadata(
            _request_like(
                low_level_tool,
                _observed_click_input(low_level_tool, center, 1),
                source="runtime_planner",
                planning_reason=planning_reason,
            ),
            target,
            action="focus_for_type",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "observed_center", "center": center},
        )
    requests = [
        click_request,
        _with_observed_action_metadata(
            _request_like(
                type_tool,
                {"text": text},
                source="runtime_planner",
                planning_reason=planning_reason,
            ),
            target,
            action="type_text",
            target_label=execution_target,
            role_filter=role_filter,
            evidence={"strategy": "focused_after_observed_target"},
        ),
    ]
    submit_request = _observed_type_submit_request(
        target,
        allowed,
        target_label=execution_target,
        role_filter=role_filter,
        planning_reason=planning_reason,
    )
    if submit_request:
        requests.append(submit_request)
    return _with_observed_action_verification(
        requests,
        target,
        allowed,
        target_label=execution_target,
        role_filter=role_filter,
        planning_reason=planning_reason,
    )


def _observed_type_submit_request(
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    target_label: str,
    role_filter: str,
    planning_reason: str,
) -> dict[str, Any]:
    submit_action = str(target.get("submit_action") or "").strip()
    if not submit_action:
        return {}
    submit_tool = _first_allowed_tool(("desktop.submit_foreground",), allowed)
    if not submit_tool:
        return {}
    request = _request_like(
        submit_tool,
        {"action": submit_action},
        source="runtime_planner",
        planning_reason=planning_reason,
    )
    if (
        _observed_submit_action_risk_level(
            submit_action,
            target,
            planning_reason=planning_reason,
        )
        == "high"
    ):
        request["approval_required"] = True
        request["risk_level"] = "high"
    return _with_observed_action_metadata(
        request,
        target,
        action="submit_after_type",
        target_label=target_label,
        role_filter=role_filter,
        evidence={"strategy": "focused_after_observed_target"},
    )


def _observed_submit_action_risk_level(
    action: str,
    target: Mapping[str, Any] | None = None,
    *,
    planning_reason: str = "",
) -> str:
    normalized = str(action or "").strip().lower().replace("-", "_")
    target_kind = str((target or {}).get("kind") or "").strip()
    if normalized == "confirm" and (
        target_kind == "communication_message"
        or str(planning_reason or "").strip()
        == "planner_followup_communication_observed_compose"
    ):
        return "low"
    if normalized in {"search", "select", "choose", "continue"}:
        return "low"
    return "high"


def _observed_click_input(
    tool_name: str,
    center: Mapping[str, Any],
    click_count: int,
) -> dict[str, Any]:
    payload = {"x": center["x"], "y": center["y"]}
    if str(tool_name or "").strip() == "desktop.click":
        payload["click_count"] = click_count
    return payload


def _observed_action_app_name(target: Mapping[str, Any]) -> str:
    return str(target.get("app_name") or target.get("target_app_name") or "").strip()


def _observed_action_app_query(target: Mapping[str, Any]) -> str:
    return str(target.get("app_query") or target.get("target_app_query") or "").strip()


def _with_observed_action_app_resolution(
    request: dict[str, Any],
    target: Mapping[str, Any],
    app_name: str,
) -> dict[str, Any]:
    app_query = _observed_action_app_query(target)
    if not app_query or not app_name:
        return request
    return _with_discovered_app_resolution(request, app_query, app_name)


def _latest_desktop_observation_succeeded(timeline: list[dict[str, Any]]) -> bool:
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {"desktop.read_ui", "desktop.ui_elements", "desktop.inspect_app"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        return result.get("ok") is True
    return False


def _latest_desktop_observation_tool(timeline: list[dict[str, Any]]) -> str:
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {"desktop.read_ui", "desktop.ui_elements", "desktop.inspect_app"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True:
            return tool_name
        if _desktop_observation_result_elements(tool_name, result):
            return tool_name
    return ""


def _latest_desktop_observation_event(timeline: list[dict[str, Any]]) -> dict[str, Any]:
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name in {"desktop.read_ui", "desktop.ui_elements", "desktop.inspect_app"}:
            return {**dict(event), "tool": tool_name}
    return {}


def _latest_desktop_observation_follows_search_submission(
    timeline: list[dict[str, Any]],
) -> bool:
    latest_observation_index = -1
    for index in range(len(timeline) - 1, -1, -1):
        event = timeline[index]
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {"desktop.read_ui", "desktop.ui_elements", "desktop.inspect_app"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is True:
            latest_observation_index = index
        break
    if latest_observation_index <= 0:
        return False
    for event in reversed(timeline[:latest_observation_index]):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name in {"desktop.search_submit", "desktop.submit_foreground"}:
            result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
            return result.get("ok") is not False
        if tool_name in {"desktop.list_apps", "desktop.running_apps"}:
            return False
    return False


def _with_observed_action_metadata(
    request: dict[str, Any],
    target: Mapping[str, Any],
    *,
    action: str,
    target_label: str,
    role_filter: str,
    evidence: Mapping[str, Any],
) -> dict[str, Any]:
    app_name = _observed_action_app_name(target)
    request["capability_id"] = "desktop.ui_operation"
    if app_name:
        request.setdefault("target_app_name", app_name)
    app_query = _observed_action_app_query(target)
    if app_query:
        request.setdefault("target_app_query", app_query)
    request["followup_target"] = _compact_observed_action_target(target)
    request["action_target"] = {
        "kind": "desktop_observed_action",
        "action": str(action or "").strip(),
        "target": str(target_label or "").strip(),
        "role_filter": str(role_filter or "").strip(),
    }
    if app_name:
        request["action_target"]["app_name"] = app_name
    request["observation_evidence"] = {
        "source_tool": str(target.get("observation_source") or "desktop.read_ui").strip(),
        **{
            str(key): value
            for key, value in evidence.items()
            if str(key) and value not in (None, "", [], {})
        },
    }
    return request


def _compact_observed_action_target(target: Mapping[str, Any]) -> dict[str, Any]:
    payload: dict[str, Any] = {
        "kind": "desktop_observed_action",
        "target_action": str(target.get("target_action") or "").strip(),
        "target": str(target.get("target") or "").strip(),
        "role_filter": str(target.get("role_filter") or "").strip(),
        "observation_source": str(target.get("observation_source") or "desktop.read_ui").strip(),
    }
    app_name = _observed_action_app_name(target)
    if app_name:
        payload["app_name"] = app_name
    app_query = _observed_action_app_query(target)
    if app_query:
        payload["app_query"] = app_query
    text = str(target.get("text") or "")
    if text:
        payload["text"] = text
    submit_action = str(target.get("submit_action") or "").strip()
    if submit_action:
        payload["submit_action"] = submit_action
    click_count = target.get("click_count")
    if click_count not in (None, ""):
        payload["click_count"] = _clean_model_followup_int(click_count, default=1)
    return {key: value for key, value in payload.items() if value not in ("", None, [], {})}


def _request_observability_metadata(request: Mapping[str, Any]) -> dict[str, Any]:
    payload: dict[str, Any] = {}
    for key in (
        "decision_id",
        "plan_id",
        "tool_plan_id",
        "intent_kind",
        "step_id",
        "planner_step_id",
        "source_step_id",
        "capability_id",
        "core_id",
        "workspace_id",
        "task_id",
        "run_id",
        "replan_request_id",
        "replan_trigger",
        "recovery_action_label",
        "recovery_action_tool",
        "recovery_source_tool",
        "recovery_source_event_type",
        "recovery_suggested_tool",
        "recovery_link_kind",
        "recovery_action",
        "recovery_scope_id",
        "replan_recovery_identity",
        "recovery_origin_tool_call_id",
        "goal_contract_id",
        "goal_criterion_id",
        "goal_subgoal_id",
        "root_source_tool_call_id",
        "root_source_step_id",
        "root_verifier_step_id",
        "root_plan_id",
        "deferred_tool",
        "permission_target",
        "risk_level",
        "verification_predicate_kind",
        "target_app_name",
        "target_app_query",
        "target_search_text",
    ):
        value = str(request.get(key) or "").strip()
        if value:
            payload[key] = value
    if payload.get("step_id") and not payload.get("planner_step_id"):
        payload["planner_step_id"] = payload["step_id"]
    deferred_context = (
        request.get("deferred_context")
        if isinstance(request.get("deferred_context"), Mapping)
        else {}
    )
    if deferred_context:
        for key in ("step_id", "planner_step_id", "capability_id"):
            value = str(deferred_context.get(key) or "").strip()
            if value and not payload.get(key):
                payload[key] = value
        if payload.get("step_id") and not payload.get("planner_step_id"):
            payload["planner_step_id"] = payload["step_id"]
        task_todo = (
            deferred_context.get("task_todo")
            if isinstance(deferred_context.get("task_todo"), Mapping)
            else {}
        )
        task_metadata = (
            task_todo.get("metadata")
            if isinstance(task_todo.get("metadata"), Mapping)
            else {}
        )
        for key in ("runtime_doctrine", "runtime_stage", "runtime_role"):
            value = str(task_metadata.get(key) or "").strip()
            if value and not payload.get(key):
                payload[key] = value
        for key in (
            "approval_required",
            "requires_observation",
            "requires_post_action_verification",
        ):
            if key not in payload and isinstance(task_metadata.get(key), bool):
                payload[key] = bool(task_metadata.get(key))
        if (
            "approval_required" not in payload
            and isinstance(task_todo.get("approval_required"), bool)
        ):
            payload["approval_required"] = bool(task_todo.get("approval_required"))
    for key in (
        "permission_recovery_retry",
        "approval_required",
        "requires_observation",
        "requires_post_action_verification",
        "recovery_context_trusted",
        "root_goal_unchanged",
    ):
        if isinstance(request.get(key), bool):
            payload[key] = bool(request.get(key))
    for key in (
        "replan_triggers",
        "replan_signal_ids",
        "depends_on",
        "verified_step_ids",
        "verification_target_step_ids",
    ):
        values = [
            str(item or "").strip()
            for item in request.get(key, [])
            if str(item or "").strip()
        ] if isinstance(request.get(key), list) else []
        if values:
            payload[key] = values
    payload.update(_runtime_trace_metadata_from_mapping(request))
    for key in (
        "followup_target",
        "observation_evidence",
        "observation_retry",
        "verification_target",
        "deferred_input",
        "deferred_context",
        "deferred_verifier_context",
        "runtime_observation_scope",
        "desktop_execution_policy",
        "desktop_loop",
        "desktop_provider_session",
        "sandbox_desktop_provider",
        "sandbox_provider",
    ):
        value = request.get(key)
        if isinstance(value, Mapping) and value:
            payload[key] = dict(value)
    action_target = _request_contract_action_target(request)
    if action_target:
        payload["action_target"] = action_target
    deferred_continuation = request.get("deferred_continuation")
    if isinstance(deferred_continuation, list):
        items = [dict(item) for item in deferred_continuation if isinstance(item, Mapping)]
        if items:
            payload["deferred_continuation"] = items
    return payload


def _request_contract_action_target(request: Mapping[str, Any]) -> dict[str, Any]:
    """Return the step target used when the immutable contract was planned."""

    step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    raw_checkpoints = request.get("task_checkpoints")
    for raw_checkpoint in (
        raw_checkpoints if isinstance(raw_checkpoints, (list, tuple)) else ()
    ):
        if not isinstance(raw_checkpoint, Mapping):
            continue
        after_step_id = str(raw_checkpoint.get("after_step_id") or "").strip()
        if step_id and after_step_id and after_step_id != step_id:
            continue
        checkpoint_payload = (
            raw_checkpoint.get("payload")
            if isinstance(raw_checkpoint.get("payload"), Mapping)
            else {}
        )
        action_target = checkpoint_payload.get("action_target")
        if isinstance(action_target, Mapping) and action_target:
            return dict(action_target)
    raw_workspace_items = request.get("task_workspace_items")
    for raw_item in (
        raw_workspace_items if isinstance(raw_workspace_items, (list, tuple)) else ()
    ):
        if not isinstance(raw_item, Mapping):
            continue
        source_step_id = str(raw_item.get("source_step_id") or "").strip()
        if step_id and source_step_id and source_step_id != step_id:
            continue
        metadata = (
            raw_item.get("metadata")
            if isinstance(raw_item.get("metadata"), Mapping)
            else {}
        )
        action_target = metadata.get("action_target")
        if isinstance(action_target, Mapping) and action_target:
            return dict(action_target)
    action_target = request.get("action_target")
    return dict(action_target) if isinstance(action_target, Mapping) else {}


def _runtime_planner_request_trace_metadata(
    decision: Any | None,
) -> dict[str, bool] | None:
    if not _runtime_planner_should_trace_tool_requests(decision):
        return None
    return {"runtime_planner_request_trace": True}


def _runtime_planner_execution_metadata(
    runtime_execution_metadata: Mapping[str, Any] | None,
    extra: Mapping[str, Any] | None = None,
) -> dict[str, Any]:
    payload = (
        dict(runtime_execution_metadata)
        if isinstance(runtime_execution_metadata, Mapping)
        else {}
    )
    payload["runtime_planner_execution_context"] = True
    if isinstance(extra, Mapping):
        payload.update(extra)
    return payload


def _fresh_planner_goal_contract_template(
    selection: DirectToolSelection,
    immutable_original_goal: str,
) -> dict[str, Any]:
    """Bind a fresh run only to the exact side-effect-free planner decision.

    The planner is invoked once before tools are eligible.  Every authority
    layer must still name the immutable user goal; otherwise a forged/stale
    selection could turn a conversational request into an unrelated effect.
    """

    original_goal = str(immutable_original_goal or "").strip()
    decision = selection.decision
    selected_intent = getattr(decision, "selected_intent", None)
    plan = getattr(decision, "plan", None)
    plan_intent = getattr(plan, "intent", None)
    task_core = getattr(plan, "task_core", None)
    goal_contract = getattr(task_core, "goal_contract", None)
    authority_goals = (
        str(getattr(decision, "prompt", "") or "").strip(),
        str(getattr(selected_intent, "user_goal", "") or "").strip(),
        str(getattr(plan_intent, "user_goal", "") or "").strip(),
        str(getattr(goal_contract, "original_goal", "") or "").strip(),
    )
    if (
        not original_goal
        or decision is None
        or selected_intent is None
        or plan is None
        or plan_intent is None
        or goal_contract is None
        or any(value != original_goal for value in authority_goals)
        or str(getattr(selected_intent, "intent_id", "") or "").strip()
        != str(getattr(plan_intent, "intent_id", "") or "").strip()
        or str(getattr(goal_contract, "intent_kind", "") or "").strip()
        != str(getattr(selected_intent, "kind", "") or "").strip()
    ):
        raise ValueError("planner_goal_conflict")
    payload = goal_contract.model_dump()
    if not isinstance(payload, dict):
        raise ValueError("planner_goal_conflict")
    return payload


def _runtime_planner_full_plan_tool_requests(
    decision: Any | None,
    allowed_tools: Iterable[str],
    *,
    metadata: Mapping[str, Any] | None = None,
) -> list[dict[str, Any]]:
    if _runtime_planner_full_plan_should_defer_to_context_prefetch(decision):
        return []
    intent_kind = str(
        getattr(getattr(decision, "selected_intent", None), "kind", "") or ""
    ).strip()
    if intent_kind not in _CHAT_FULL_PLAN_EXECUTION_INTENTS:
        return []
    envelope = runtime_execution_envelope_payload(
        decision,
        allowed_tools=allowed_tools,
        full_plan=True,
        metadata=metadata,
    )
    if not envelope:
        return []
    return _dedupe_runtime_planner_full_plan_requests(
        runtime_execution_requests_from_envelope_payload(
            envelope,
            allowed_tools=allowed_tools,
        )
    )


def _runtime_planner_full_plan_should_defer_to_context_prefetch(
    decision: Any | None,
) -> bool:
    selected_intent = getattr(decision, "selected_intent", None)
    if str(getattr(selected_intent, "kind", "") or "").strip() != "communication":
        return False
    inputs = getattr(selected_intent, "inputs", None)
    if not isinstance(inputs, Mapping):
        return False
    direct_hint = (
        inputs.get("direct_message_hint")
        if isinstance(inputs.get("direct_message_hint"), Mapping)
        else {}
    )
    body_source = str(
        direct_hint.get("body_source") or inputs.get("context_source") or ""
    ).strip()
    transform = str(
        direct_hint.get("content_transform_hint")
        or inputs.get("content_transform_hint")
        or ""
    ).strip()
    if transform and body_source:
        return True
    return body_source in {
        "app_search_result",
        "current_page_content",
        "file",
        "screen_capture",
        "selection",
        "visible_text",
    }


def _split_combined_foreground_app_requests(
    requests: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    """Keep broker-atomic foreground actions intact at the execution boundary.

    The composite tool identity is the locking contract. ``presentation`` is
    user-facing summary metadata and must never decide whether focus/open and
    the following shortcut or text input share one foreground lock. The
    ``allowed_tools`` argument remains part of this compatibility boundary;
    policy validation still happens in the normal tool runner.
    """
    return requests


def _split_combined_app_prefix_requests(
    request: Mapping[str, Any],
    *,
    app_name: str,
    starts_open: bool,
    allowed: set[str],
) -> list[dict[str, Any]]:
    if not app_name:
        return []
    prefix: list[dict[str, Any]] = []
    selection = _app_selection_input(request, app_name)
    if starts_open:
        if "app.open" not in allowed:
            return []
        prefix.append(_derived_foreground_request(request, "app.open", selection))
    if "app.focus" not in allowed:
        return []
    prefix.append(_derived_foreground_request(request, "app.focus", selection))
    return prefix


def _app_selection_input(request: Mapping[str, Any], app_name: str) -> dict[str, Any]:
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    result = {"app_name": app_name}
    for key in ("selection_source", "query"):
        value = str(payload.get(key) or "").strip()
        if value:
            result[key] = value
    return result


def _derived_foreground_request(
    source_request: Mapping[str, Any],
    tool_name: str,
    payload: Mapping[str, Any],
) -> dict[str, Any]:
    derived = dict(source_request)
    derived["tool"] = tool_name
    derived.pop("tool_name", None)
    derived["input"] = dict(payload)
    return derived


def _dedupe_runtime_planner_full_plan_requests(
    requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    deduped: list[dict[str, Any]] = []
    seen: set[tuple[str, Any]] = set()
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        item = dict(request)
        item = _runtime_planner_request_without_resolved_selection_hints(item)
        key = (
            str(item.get("tool") or item.get("tool_name") or "").strip(),
            _freeze_request_value(item.get("input")),
        )
        if key in seen:
            continue
        seen.add(key)
        deduped.append(item)
    return deduped


def _runtime_planner_execution_requests_with_selection_inputs(
    execution_requests: list[dict[str, Any]],
    selection_requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if not execution_requests or len(execution_requests) != len(selection_requests):
        return execution_requests
    execution_tools = [
        str(request.get("tool") or request.get("tool_name") or "").strip()
        for request in execution_requests
    ]
    selection_tools = [
        str(request.get("tool") or request.get("tool_name") or "").strip()
        for request in selection_requests
    ]
    if execution_tools != selection_tools:
        return execution_requests
    if any(tool in _DAILY_DESKTOP_DISCOVERY_TOOLS or tool in _DAILY_DESKTOP_VERIFY_TOOLS for tool in execution_tools):
        return execution_requests
    if any(bool(request.get("continue_to_model")) for request in execution_requests):
        return execution_requests
    updated: list[dict[str, Any]] = []
    changed = False
    for execution_request, selection_request in zip(execution_requests, selection_requests):
        selection_input = (
            selection_request.get("input")
            if isinstance(selection_request.get("input"), Mapping)
            else {}
        )
        if not selection_input:
            updated.append(execution_request)
            continue
        execution_input = (
            execution_request.get("input")
            if isinstance(execution_request.get("input"), Mapping)
            else {}
        )
        if dict(selection_input) == dict(execution_input):
            updated.append(execution_request)
            continue
        merged = dict(execution_request)
        merged["input"] = dict(selection_input)
        updated.append(merged)
        changed = True
    return updated if changed else execution_requests


_RUNTIME_PLANNER_OBSERVED_TYPE_TOOLS = frozenset(
    {
        "app.focus_and_type_into_ui_element",
        "app.open_and_type_into_ui_element",
        "desktop.type_into_ui_element",
    }
)

_RUNTIME_PLANNER_UI_OBSERVATION_TOOLS = frozenset(
    {
        "desktop.inspect_app",
        "desktop.read_ui",
        "desktop.ui_elements",
    }
)


def _runtime_planner_goal_contract_payload(
    decision: Any | None,
) -> dict[str, Any]:
    plan = getattr(decision, "plan", None)
    task_core = getattr(plan, "task_core", None)
    contract = getattr(task_core, "goal_contract", None)
    if contract is None:
        return {}
    payload = contract.model_dump() if hasattr(contract, "model_dump") else {}
    return dict(payload) if isinstance(payload, Mapping) else {}


def _runtime_planner_declared_verifier_step_id(
    goal_contract: Mapping[str, Any] | None,
    *,
    source_step_id: str,
) -> str:
    if not isinstance(goal_contract, Mapping) or not source_step_id:
        return ""
    criteria = goal_contract.get("criteria")
    for criterion in criteria if isinstance(criteria, list) else []:
        if not isinstance(criterion, Mapping):
            continue
        if source_step_id not in _string_list(criterion.get("source_step_ids")):
            continue
        verifier_ids = _string_list(criterion.get("verifier_step_ids"))
        if len(verifier_ids) == 1:
            return verifier_ids[0]
    return ""


def _bind_runtime_planner_declared_verifiers(
    requests: list[dict[str, Any]],
    goal_contract: Mapping[str, Any] | None,
) -> list[dict[str, Any]]:
    """Bind generated trailing observations to the declared Goal verifier."""

    bound = [deepcopy(request) for request in requests if isinstance(request, Mapping)]
    for verifier_index, verifier in enumerate(bound):
        if str(verifier.get("runtime_stage") or "").strip() != "verify":
            continue
        existing_verifier_step_id = str(
            verifier.get("step_id") or verifier.get("planner_step_id") or ""
        ).strip()
        source_index = _runtime_planner_generated_verifier_source_index(
            bound,
            before_index=verifier_index,
        )
        if source_index < 0:
            continue
        source = bound[source_index]
        source_step_id = str(
            source.get("step_id") or source.get("planner_step_id") or ""
        ).strip()
        if (
            existing_verifier_step_id
            and existing_verifier_step_id != source_step_id
        ):
            continue
        verifier_step_id = _runtime_planner_declared_verifier_step_id(
            goal_contract,
            source_step_id=source_step_id,
        )
        if not verifier_step_id:
            continue
        source_request_id = str(source.get("request_id") or "").strip()
        verifier_tool = str(verifier.get("tool") or "").strip()
        app_name = _runtime_planner_request_app_name(source, verifier)
        source_input = (
            source.get("input") if isinstance(source.get("input"), Mapping) else {}
        )
        enriched = deepcopy(verifier)
        enriched.update(
            {
                "step_id": verifier_step_id,
                "planner_step_id": verifier_step_id,
                "capability_id": "desktop.app_discovery",
                "depends_on": [source_step_id],
                "source_step_id": source_step_id,
                "runtime_stage": "verify",
                "runtime_role": "verify_result",
                "approval_required": False,
                "risk_level": "low",
                "requires_observation": True,
                "requires_post_action_verification": False,
                "source": "runtime_verification",
                "planning_reason": "planner_runtime_declared_verification",
                "verification_predicate_kind": (
                    "exact_submit_dispatch_receipt"
                    if str(source.get("tool") or "").strip()
                    == "desktop.submit_foreground"
                    else ""
                ),
                "action_target": {
                    key: value
                    for key, value in {
                        "kind": "desktop_app" if app_name else "desktop_ui",
                        "action": "verify_after_action",
                        "app_name": app_name,
                        "target": str(source_input.get("target") or "").strip(),
                        "role_filter": str(source_input.get("role_filter") or "").strip(),
                        "step_id": verifier_step_id,
                    }.items()
                    if value
                },
                "verification_targets": [{"step_id": source_step_id}],
            }
        )
        if source_request_id:
            enriched["source_request_id"] = source_request_id
            enriched["request_id"] = (
                f"{source_request_id}:verify:{verifier_step_id}:{verifier_tool}"
            )
        for key in (
            "decision_id",
            "plan_id",
            "tool_plan_id",
            "core_id",
            "workspace_id",
            "task_id",
            "intent_kind",
        ):
            value = source.get(key)
            if value not in (None, "", [], {}):
                enriched[key] = deepcopy(value)
        bound[verifier_index] = enriched
    return bound


def _runtime_planner_generated_verifier_source_index(
    requests: list[dict[str, Any]],
    *,
    before_index: int,
) -> int:
    for index in range(before_index - 1, -1, -1):
        request = requests[index]
        if request.get("requires_post_action_verification") is not True:
            continue
        if str(request.get("runtime_stage") or "").strip() != "operate":
            continue
        if not str(request.get("step_id") or request.get("planner_step_id") or "").strip():
            continue
        return index
    return -1


def _defer_runtime_planner_desktop_type_until_observed(
    requests: list[dict[str, Any]],
    *,
    goal_contract: Mapping[str, Any] | None = None,
) -> list[dict[str, Any]]:
    """Project a planner type step only after its scoped UI observation.

    The immutable planner envelope remains the source of every step.  This
    projection merely places the observation at the execution boundary and
    carries the already-planned type/verifier/submit suffix as a deferred
    continuation.  It prevents a semantic type tool from racing ahead of the
    UI state that authorizes its target.
    """

    planned = [deepcopy(request) for request in requests if isinstance(request, Mapping)]
    for action_index, action in enumerate(planned):
        action_tool = str(action.get("tool") or "").strip()
        if action_tool not in _RUNTIME_PLANNER_OBSERVED_TYPE_TOOLS:
            continue
        if str(action.get("runtime_stage") or "").strip() != "operate":
            continue
        action_input = action.get("input") if isinstance(action.get("input"), Mapping) else {}
        if not str(action_input.get("target") or "").strip() or not str(
            action_input.get("text") or ""
        ):
            continue
        action_step_id = str(
            action.get("step_id") or action.get("planner_step_id") or ""
        ).strip()
        action_plan_id = str(action.get("plan_id") or "").strip()
        dependencies = set(_string_list(action.get("depends_on")))
        if not action_step_id or not dependencies:
            continue

        observation_index = _runtime_planner_type_observation_index(
            planned,
            before_index=action_index,
            dependency_step_ids=dependencies,
            plan_id=action_plan_id,
        )
        if observation_index < 0:
            continue
        observation = planned[observation_index]
        observation_step_id = str(
            observation.get("step_id")
            or observation.get("planner_step_id")
            or ""
        ).strip()
        app_name = _runtime_planner_request_app_name(observation, action)

        deferred_observation = deepcopy(observation)
        deferred_observation["continue_to_model"] = True
        deferred_observation["deferred_tool"] = action_tool
        deferred_observation["deferred_input"] = dict(action_input)
        deferred_observation["deferred_context"] = _runtime_planner_deferred_context(
            action
        )
        deferred_observation["runtime_observation_scope"] = {
            key: value
            for key, value in {
                "decision_id": str(action.get("decision_id") or "").strip(),
                "plan_id": action_plan_id,
                "observation_step_id": observation_step_id,
                "app_name": app_name,
            }.items()
            if value
        }
        if app_name:
            deferred_observation.setdefault("target_app_name", app_name)

        verifier_index = _runtime_planner_type_verifier_index(
            planned,
            after_index=action_index,
            action_step_id=action_step_id,
            plan_id=action_plan_id,
        )
        if verifier_index >= 0:
            deferred_observation["deferred_verifier_context"] = (
                _runtime_planner_deferred_context(planned[verifier_index])
            )
            continuation = [
                deepcopy(request)
                for index, request in enumerate(planned[action_index + 1 :], start=action_index + 1)
                if index != verifier_index
            ]
        else:
            verifier_context = _runtime_planner_synthetic_type_verifier_context(
                action,
                observation,
                app_name=app_name,
                goal_contract=goal_contract,
            )
            deferred_observation["deferred_verifier_context"] = verifier_context
            continuation = [deepcopy(request) for request in planned[action_index + 1 :]]

        if continuation:
            deferred_observation["deferred_continuation"] = continuation
        return [*planned[:observation_index], deferred_observation]
    return planned


def _runtime_planner_scoped_observation_deferral(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    return any(
        isinstance(request.get("runtime_observation_scope"), Mapping)
        and bool(request.get("runtime_observation_scope"))
        for request in requests
        if isinstance(request, Mapping)
    )


def _runtime_planner_type_observation_index(
    requests: list[dict[str, Any]],
    *,
    before_index: int,
    dependency_step_ids: set[str],
    plan_id: str,
) -> int:
    for index in range(before_index - 1, -1, -1):
        request = requests[index]
        tool_name = str(request.get("tool") or "").strip()
        if tool_name not in _RUNTIME_PLANNER_UI_OBSERVATION_TOOLS:
            continue
        if str(request.get("runtime_stage") or "").strip() != "discover":
            continue
        request_plan_id = str(request.get("plan_id") or "").strip()
        if plan_id and request_plan_id != plan_id:
            continue
        step_id = str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        if step_id in dependency_step_ids:
            return index
    return -1


def _runtime_planner_type_verifier_index(
    requests: list[dict[str, Any]],
    *,
    after_index: int,
    action_step_id: str,
    plan_id: str,
) -> int:
    for index in range(after_index + 1, len(requests)):
        request = requests[index]
        if str(request.get("runtime_stage") or "").strip() != "verify":
            continue
        if str(request.get("tool") or "").strip() not in (
            _RUNTIME_PLANNER_UI_OBSERVATION_TOOLS | _DAILY_DESKTOP_VERIFY_TOOLS
        ):
            continue
        request_plan_id = str(request.get("plan_id") or "").strip()
        if plan_id and request_plan_id != plan_id:
            continue
        if action_step_id in _string_list(request.get("depends_on")):
            return index
    return -1


def _runtime_planner_request_app_name(*requests: Mapping[str, Any]) -> str:
    for request in requests:
        request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        action_target = (
            request.get("action_target")
            if isinstance(request.get("action_target"), Mapping)
            else {}
        )
        for source in (request_input, action_target, request):
            app_name = str(
                source.get("app_name") or source.get("target_app_name") or ""
            ).strip()
            if app_name and not _runtime_planner_placeholder_app_name(app_name):
                return app_name
    return ""


def _runtime_planner_deferred_context(request: Mapping[str, Any]) -> dict[str, Any]:
    context = deepcopy(dict(request))
    for key in (
        "tool",
        "tool_name",
        "input",
        "continue_to_model",
        "deferred_tool",
        "deferred_input",
        "deferred_context",
        "deferred_verifier_context",
        "deferred_continuation",
    ):
        context.pop(key, None)
    return context


def _runtime_planner_synthetic_type_verifier_context(
    action: Mapping[str, Any],
    observation: Mapping[str, Any],
    *,
    app_name: str,
    goal_contract: Mapping[str, Any] | None,
) -> dict[str, Any]:
    action_step_id = str(
        action.get("step_id") or action.get("planner_step_id") or ""
    ).strip()
    verifier_step_id = (
        _runtime_planner_declared_verifier_step_id(
            goal_contract,
            source_step_id=action_step_id,
        )
        or f"{action_step_id}-postcondition"
    )
    action_input = action.get("input") if isinstance(action.get("input"), Mapping) else {}
    action_target = {
        key: value
        for key, value in {
            "kind": "desktop_app" if app_name else "desktop_ui",
            "action": "verify_after_action",
            "app_name": app_name,
            "target": str(action_input.get("target") or "").strip(),
            "role_filter": str(action_input.get("role_filter") or "").strip(),
            "step_id": verifier_step_id,
        }.items()
        if value
    }
    return {
        key: value
        for key, value in {
            "decision_id": str(action.get("decision_id") or "").strip(),
            "plan_id": str(action.get("plan_id") or "").strip(),
            "tool_plan_id": str(action.get("tool_plan_id") or "").strip(),
            "core_id": str(action.get("core_id") or "").strip(),
            "workspace_id": str(action.get("workspace_id") or "").strip(),
            "task_id": str(action.get("task_id") or "").strip(),
            "step_id": verifier_step_id,
            "planner_step_id": verifier_step_id,
            "capability_id": str(
                observation.get("capability_id") or "desktop.app_discovery"
            ).strip(),
            "depends_on": [action_step_id],
            "runtime_doctrine": "discover_operate_verify",
            "runtime_stage": "verify",
            "runtime_role": "verify_result",
            "approval_required": False,
            "risk_level": "low",
            "requires_observation": True,
            "requires_post_action_verification": False,
            "action_target": action_target,
                "source": "runtime_verification",
                "planning_reason": "planner_runtime_type_postcondition",
                "verification_predicate_kind": "exact_typed_content_present",
            }.items()
        if value not in (None, "", [], {})
    }


def _runtime_planner_request_without_resolved_selection_hints(
    request: dict[str, Any],
) -> dict[str, Any]:
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    if tool_name in _DISCOVERED_APP_SELECTION_SOURCES:
        return request
    changed = False
    resolved = dict(request)
    for key in ("input", "deferred_input"):
        cleaned = _runtime_planner_input_without_resolved_selection_hints(
            resolved.get(key),
        )
        if cleaned is not None:
            resolved[key] = cleaned
            changed = True
    continuation = resolved.get("deferred_continuation")
    if isinstance(continuation, list):
        cleaned_items: list[Any] = []
        for item in continuation:
            if not isinstance(item, Mapping):
                cleaned_items.append(item)
                continue
            next_item = dict(item)
            cleaned = _runtime_planner_input_without_resolved_selection_hints(
                next_item.get("input"),
            )
            if cleaned is not None:
                next_item["input"] = cleaned
                changed = True
            cleaned_items.append(next_item)
        if changed:
            resolved["deferred_continuation"] = cleaned_items
    return resolved if changed else request


def _runtime_planner_input_without_resolved_selection_hints(
    value: Any,
) -> dict[str, Any] | None:
    if not isinstance(value, Mapping):
        return None
    payload = dict(value)
    app_name = str(payload.get("app_name") or "").strip()
    if not app_name or _runtime_planner_placeholder_app_name(app_name):
        return None
    cleaned = dict(payload)
    for key in ("selection_source", "app_selection_source", "query"):
        cleaned.pop(key, None)
    return cleaned if cleaned != payload else None


def _freeze_request_value(value: Any) -> Any:
    if isinstance(value, Mapping):
        return tuple(
            sorted((str(key), _freeze_request_value(item)) for key, item in value.items())
        )
    if isinstance(value, list):
        return tuple(_freeze_request_value(item) for item in value)
    if isinstance(value, tuple):
        return tuple(_freeze_request_value(item) for item in value)
    if isinstance(value, set):
        return tuple(sorted(_freeze_request_value(item) for item in value))
    return value


_CHAT_FULL_PLAN_EXECUTION_INTENTS = frozenset(
    {
        "clipboard_operation",
        "code_task",
        "communication",
        "data_analysis",
        "desktop_operation",
        "file_access",
        "file_operation",
        "file_organization",
        "information_capture",
        "media_playback",
        "report_generation",
        "schedule",
        "system_control",
        "web_research",
    }
)


def _runtime_execution_context_tool_requests(
    runtime_execution_envelope: Mapping[str, Any] | None,
    runtime_execution_metadata: Mapping[str, Any] | None,
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]] | None:
    requests = runtime_execution_requests_from_envelope_payload(
        runtime_execution_envelope,
        allowed_tools=allowed_tools,
    )
    if requests:
        return requests
    if _runtime_execution_envelope_has_requests(runtime_execution_envelope):
        # The envelope is the authoritative execution contract. When it owns
        # requests but projects none as executable, they are blocked (or
        # terminal), so legacy metadata must not resurrect and run them.
        return None
    if not isinstance(runtime_execution_metadata, Mapping):
        return None
    requests = runtime_execution_requests_from_metadata(
        runtime_execution_metadata,
        allowed_tools=allowed_tools,
    )
    return requests or None


def _runtime_execution_envelope_has_requests(
    runtime_execution_envelope: Mapping[str, Any] | None,
) -> bool:
    if not isinstance(runtime_execution_envelope, Mapping):
        return False
    requests = runtime_execution_envelope.get("requests")
    return isinstance(requests, list) and bool(requests)


def _runtime_execution_envelope_has_only_materialization_blockers(
    runtime_execution_envelope: Mapping[str, Any] | None,
    blocked_requests: Iterable[Mapping[str, Any]],
) -> bool:
    if not isinstance(runtime_execution_envelope, Mapping):
        return False
    blocked_ids = {
        str(request.get("request_id") or "").strip()
        for request in blocked_requests
        if isinstance(request, Mapping) and str(request.get("request_id") or "").strip()
    }
    requests = runtime_execution_envelope.get("requests")
    raw_blocked = [
        request
        for request in (requests if isinstance(requests, list) else [])
        if isinstance(request, Mapping)
        and str(request.get("request_id") or "").strip() in blocked_ids
    ]
    if not blocked_ids or len(raw_blocked) != len(blocked_ids):
        return False
    for request in raw_blocked:
        tool_name = str(request.get("tool_name") or request.get("tool") or "").strip()
        raw_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        status = str(request.get("status") or "").strip()
        route = (
            request.get("desktop_execution_route")
            if isinstance(request.get("desktop_execution_route"), Mapping)
            else {}
        )
        if (
            status not in {"blocked", "waiting_approval"}
            or not str(request.get("plan_id") or runtime_execution_envelope.get("plan_id") or "").strip()
            or not str(request.get("step_id") or "").strip()
            or route.get("can_execute") is False
            or not _tool_request_requires_model_materialization(
                {
                    "tool": tool_name,
                    "input": dict(raw_input),
                    "continue_to_model": True,
                }
            )
        ):
            return False
    return True


def _runtime_execution_context_selection_payload(
    runtime_execution_envelope: Mapping[str, Any] | None,
    runtime_execution_metadata: Mapping[str, Any] | None,
    direct_tool_requests: Iterable[Mapping[str, Any]] | None,
) -> dict[str, Any]:
    metadata = (
        dict(runtime_execution_metadata)
        if isinstance(runtime_execution_metadata, Mapping)
        else {}
    )
    envelope = (
        dict(runtime_execution_envelope)
        if isinstance(runtime_execution_envelope, Mapping)
        else metadata.get("yachiyo_execution_envelope")
    )
    envelope = dict(envelope) if isinstance(envelope, Mapping) else {}
    direct_requests = [
        dict(request)
        for request in (direct_tool_requests or [])
        if isinstance(request, Mapping)
    ]
    tool_names = _tool_names_from_requests(direct_requests)
    if not tool_names:
        tool_names = [
            str(tool or "").strip()
            for tool in metadata.get("yachiyo_execution_requests", [])
            if str(tool or "").strip()
        ]
    if not tool_names and envelope:
        requests = envelope.get("requests") if isinstance(envelope.get("requests"), list) else []
        tool_names = [
            str(request.get("tool_name") or request.get("tool") or "").strip()
            for request in requests
            if isinstance(request, Mapping)
            and str(request.get("tool_name") or request.get("tool") or "").strip()
        ]
    metadata_has_execution_context = any(
        metadata.get(key) not in (None, "", [], {})
        for key in (
            "yachiyo_execution_requests",
            "yachiyo_execution_envelope",
            "yachiyo_task_core",
            "yachiyo_task_progress",
        )
    )
    if not envelope and not metadata_has_execution_context:
        return {}
    payload = {
        "selected_source": "runtime_execution_envelope" if envelope else "direct_tool_requests",
        "selected_reason": "runtime_execution_direct_requests",
        "selection_source": "runtime_execution_envelope" if envelope else "direct_tool_requests",
        "selection_reason": "runtime_execution_direct_requests",
        "selected_request_count": len(tool_names),
        "execution_request_count": len(tool_names),
    }
    for request in direct_requests:
        followup_target = request.get("followup_target")
        if isinstance(followup_target, Mapping):
            payload["followup_target"] = dict(followup_target)
            break
    if envelope:
        payload["yachiyo_execution_envelope"] = envelope
    if tool_names:
        payload["selected_tools"] = tool_names
        payload["execution_tools"] = tool_names
        payload["yachiyo_execution_requests"] = tool_names
        payload["yachiyo_execution_request_count"] = len(tool_names)
    for key in (
        "yachiyo_decision_id",
        "yachiyo_plan_id",
        "yachiyo_intent_kind",
        "yachiyo_task_core",
        "yachiyo_task_progress",
        "yachiyo_capability_plan",
        "yachiyo_execution_request_previews",
    ):
        value = metadata.get(key)
        if value not in (None, "", [], {}):
            payload[key] = value
    if envelope and str(envelope.get("source") or "").strip() == "runtime_planner":
        payload["source"] = "runtime_planner"
        payload["selection_source"] = "runtime_planner"
        payload["selected_source"] = "runtime_planner"
        payload["selection_reason"] = "runtime_planner_full_plan_execution"
        payload["selected_reason"] = "runtime_planner_full_plan_execution"
        payload["yachiyo_execution_projection"] = "full_plan"
        for source_key, target_key in (
            ("decision_id", "decision_id"),
            ("plan_id", "plan_id"),
            ("intent_kind", "intent_kind"),
            ("route_to_studio", "route_to_studio"),
        ):
            value = envelope.get(source_key)
            if value not in (None, "", [], {}):
                payload[target_key] = value
        if tool_names:
            payload["planner_tools"] = list(tool_names)
            payload["planner_request_count"] = len(tool_names)
    return payload


def _selection_payload_with_runtime_execution_envelope(
    payload: Mapping[str, Any],
    decision: Any | None,
    allowed_tools: Iterable[str],
    *,
    full_plan: bool = False,
    execution_requests: Iterable[Mapping[str, Any]] | None = None,
    metadata: Mapping[str, Any] | None = None,
) -> dict[str, Any]:
    enriched = dict(payload)
    envelope = runtime_execution_envelope_payload(
        decision,
        allowed_tools=allowed_tools,
        full_plan=full_plan,
        metadata=metadata,
    )
    if not envelope:
        return enriched
    requests = envelope.get("requests") if isinstance(envelope.get("requests"), list) else []
    plan_tool_names = [
        str(request.get("tool_name") or "").strip()
        for request in requests
        if isinstance(request, Mapping) and str(request.get("tool_name") or "").strip()
    ]
    execution_tool_names = _tool_names_from_requests(execution_requests)
    tool_names = execution_tool_names or plan_tool_names
    enriched["yachiyo_execution_envelope"] = envelope
    enriched["yachiyo_execution_request_count"] = len(tool_names)
    if tool_names:
        enriched["yachiyo_execution_requests"] = tool_names
    if execution_tool_names and plan_tool_names and execution_tool_names != plan_tool_names:
        enriched["yachiyo_execution_plan_requests"] = plan_tool_names
        enriched["yachiyo_execution_normalized"] = True
    if full_plan:
        enriched["yachiyo_execution_projection"] = "full_plan"
    return enriched


def _tool_names_from_requests(
    requests: Iterable[Mapping[str, Any]] | None,
) -> list[str]:
    if requests is None:
        return []
    return [
        str(request.get("tool") or request.get("tool_name") or "").strip()
        for request in requests
        if isinstance(request, Mapping)
        and str(request.get("tool") or request.get("tool_name") or "").strip()
    ]


def _tool_requests_include_model_followup(requests: Iterable[Mapping[str, Any]]) -> bool:
    return any(
        bool(request.get("continue_to_model"))
        for request in requests
        if isinstance(request, Mapping)
    )


def _tool_requests_have_unresolved_discovered_app(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        app_name = str(payload.get("app_name") or "").strip()
        if app_name and _runtime_planner_placeholder_app_name(app_name):
            return True
        if _discovered_app_selection_source(payload.get("selection_source")):
            return True
        resolution = (
            request.get("input_resolution")
            if isinstance(request.get("input_resolution"), Mapping)
            else {}
        )
        if (
            _discovered_app_selection_source(resolution.get("source_tool"))
            and not str(resolution.get("resolved_app_name") or "").strip()
        ):
            return True
    return False


def _timeline_has_model_followup_context(
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    return any(
        str(event.get("event") or "").strip() == "agent.model.followup_context"
        for event in timeline[tool_timeline_start:]
        if isinstance(event, Mapping)
    )


def _planner_replan_context_requested(
    *,
    default_messages: bool,
    runtime_execution_metadata: Mapping[str, Any] | None,
) -> bool:
    if default_messages:
        return True
    if not isinstance(runtime_execution_metadata, Mapping):
        return False
    return any(
        bool(runtime_execution_metadata.get(key))
        for key in (
            "runtime_planner_entrypoint",
            "yachiyo_runtime_planner",
        )
    )


def _latest_message_is_model_followup_context(messages: list[dict[str, Any]]) -> bool:
    if not messages:
        return False
    content = str(messages[-1].get("content") or "")
    return content.startswith("Runtime follow-up context:")


def _discovered_app_resolution_probe_requests(
    requests: list[dict[str, Any]],
    selection_payload: Mapping[str, Any],
) -> list[dict[str, Any]]:
    if not requests:
        return requests
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return requests
    communication_compose = (
        target.get("communication_compose")
        if isinstance(target.get("communication_compose"), Mapping)
        else {}
    )
    if not communication_compose:
        return requests
    app_query = str(target.get("app_query") or "").strip()
    if not app_query:
        return requests
    discovery_index = -1
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if tool_name not in _DISCOVERED_APP_SELECTION_SOURCES:
            continue
        payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        if (
            tool_name == "desktop.running_apps"
            or str(payload.get("query") or "").strip() == app_query
        ):
            discovery_index = index
            break
    if discovery_index < 0:
        return requests
    if not any(
        _request_uses_selected_desktop_app_result(request, app_query)
        for request in requests[discovery_index + 1 :]
    ):
        return requests
    probe_requests = [dict(request) for request in requests[: discovery_index + 1]]
    probe_requests[-1]["continue_to_model"] = True
    return probe_requests


def _request_uses_selected_desktop_app_result(
    request: Mapping[str, Any],
    app_query: str,
) -> bool:
    payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    selection_source = _discovered_app_selection_source(
        payload.get("selection_source")
        or payload.get("app_selection_source")
    )
    if not selection_source:
        return False
    if str(payload.get("query") or "").strip() != str(app_query or "").strip():
        return False
    return _discovered_app_placeholder_source(payload.get("app_name")) == selection_source


def _tool_requests_without_model_followup(
    requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    cleaned: list[dict[str, Any]] = []
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        item = dict(request)
        item.pop("continue_to_model", None)
        cleaned.append(item)
    return cleaned


def _tool_requests_without_model_followup_and_deferred_continuation(
    requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    cleaned = _tool_requests_without_model_followup(requests)
    for item in cleaned:
        item.pop("deferred_continuation", None)
    return cleaned


def _direct_sequence_requests_with_safe_deferred_continuations(
    requests: Iterable[Mapping[str, Any]],
) -> list[dict[str, Any]]:
    root_requests = [dict(request) for request in requests if isinstance(request, Mapping)]
    expanded: list[dict[str, Any]] = []
    allowed = {str(tool or "").strip() for tool in _DIRECT_DAILY_DESKTOP_TOOLS}
    root_identities = {
        identity
        for request in root_requests
        if (identity := _direct_sequence_request_exact_identity(request)) is not None
    }

    def append_with_continuations(request: Mapping[str, Any]) -> None:
        if not isinstance(request, Mapping):
            return
        item = dict(request)
        expanded.append(item)
        deferred_items = materialized_deferred_items(item)
        if bool(item.get("continue_to_model")) and not deferred_items:
            return
        for continuation in deferred_items:
            next_request = safe_deferred_continuation_request(
                continuation,
                allowed,
                auto_safe_tools=_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS,
            )
            if not next_request:
                continue
            for key, value in _runtime_replan_deferred_inherited_metadata(item).items():
                next_request.setdefault(key, value)
            next_request.setdefault(
                "source",
                str(item.get("source") or "runtime_planner").strip(),
            )
            next_request.setdefault(
                "planning_reason",
                "planner_replan_deferred_continuation",
            )
            # Recovery scheduling may already have materialized this verifier
            # as a top-level, source-bound request.  Do not synthesize a weak
            # duplicate during terminal aggregation; preserve the executed
            # root request and its exact source receipt instead.
            next_identity = _direct_sequence_request_exact_identity(next_request)
            if next_identity is not None and next_identity in root_identities:
                continue
            append_with_continuations(next_request)

    for request in root_requests:
        append_with_continuations(request)
    return expanded


def _direct_sequence_request_exact_identity(
    request: Mapping[str, Any],
) -> tuple[str, str, str, str] | None:
    plan_id = str(request.get("plan_id") or "").strip()
    step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    tool_name = str(request.get("tool") or "").strip()
    if not plan_id or not step_id or not tool_name:
        return None
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    _, input_signature = _tool_request_signature(tool_name, request_input)
    return plan_id, step_id, tool_name, input_signature


def _desktop_intent_verification_evidence(
    completed_steps: Iterable[Mapping[str, Any]],
) -> dict[str, Any]:
    for step in reversed([item for item in completed_steps if isinstance(item, Mapping)]):
        tool_name = str(step.get("tool") or "").strip()
        if tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS:
            continue
        result = step.get("result") if isinstance(step.get("result"), Mapping) else {}
        if not result:
            continue
        status = _desktop_verification_status(result)
        if not status:
            continue
        evidence: dict[str, Any] = {
            "verification_status": status,
            "verification_tool": tool_name,
            "verification_result": _desktop_verification_result_preview(result),
        }
        for key in (
            "step_id",
            "planner_step_id",
            "replan_request_id",
            "replan_trigger",
            "target_app_name",
            "target_app_query",
            "target_search_text",
        ):
            value = step.get(key)
            if value not in (None, "", [], {}):
                evidence[key] = value
        verification_target = (
            step.get("verification_target")
            if isinstance(step.get("verification_target"), Mapping)
            else {}
        )
        if verification_target:
            evidence["verification_target"] = dict(verification_target)
        if tool_name == "desktop.active_window":
            evidence.update(
                _active_window_verification_evidence(
                    step,
                    result,
                    verification_target=verification_target,
                )
            )
        if tool_name in {"desktop.read_ui", "desktop.ui_elements"}:
            evidence.update(
                _ui_observation_verification_evidence(
                    step,
                    result,
                    tool_name=tool_name,
                    verification_target=verification_target,
                )
            )
        return {key: value for key, value in evidence.items() if value not in ("", [], {})}
    return {}


def _desktop_verification_status(result: Mapping[str, Any]) -> str:
    if result.get("approval_required"):
        return "waiting_approval"
    if result.get("verification_failed") is True:
        return "verification_failed"
    if result.get("ok") is True:
        return "verified"
    if result.get("ok") is False:
        return "verification_failed"
    return ""


def _desktop_verification_result_preview(result: Mapping[str, Any]) -> dict[str, Any]:
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    preview: dict[str, Any] = {}
    for key in (
        "ok",
        "action",
        "summary",
        "error",
        "hint",
        "verification_failed",
        "blocking_condition",
        "expected_app_name",
        "active_app_name",
    ):
        value = result.get(key)
        if value not in (None, "", [], {}):
            preview[key] = value
    for key in ("app_name", "title", "frontmost_app", "frontmost_app_name"):
        value = data.get(key)
        if value not in (None, "", [], {}):
            preview[key] = value
    return preview


def _active_window_verification_evidence(
    step: Mapping[str, Any],
    result: Mapping[str, Any],
    *,
    verification_target: Mapping[str, Any],
) -> dict[str, Any]:
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    expected_app_name = _first_nonempty_text(
        verification_target.get("app_name"),
        verification_target.get("target_app_name"),
        verification_target.get("expected_app_name"),
        step.get("target_app_name"),
        result.get("expected_app_name"),
        data.get("expected_app_name"),
    )
    active_app_name = _first_nonempty_text(
        result.get("active_app_name"),
        data.get("active_app_name"),
        data.get("app_name"),
        result.get("app_name"),
        data.get("frontmost_app"),
        data.get("frontmost_app_name"),
    )
    evidence = {
        "expected_app_name": expected_app_name,
        "active_app_name": active_app_name,
        "active_window_title": _first_nonempty_text(data.get("title"), result.get("title")),
    }
    if expected_app_name and active_app_name:
        evidence["focus_verified"] = _replan_app_names_match(
            expected_app_name,
            active_app_name,
        )
    return {key: value for key, value in evidence.items() if value not in ("", [], {})}


def _ui_observation_verification_evidence(
    step: Mapping[str, Any],
    result: Mapping[str, Any],
    *,
    tool_name: str,
    verification_target: Mapping[str, Any],
) -> dict[str, Any]:
    target = _ui_observation_verification_target(step, verification_target)
    target_text = _first_nonempty_text(
        target.get("target"),
        target.get("target_search_text"),
        target.get("target_label"),
        target.get("ui_target"),
        target.get("element_name"),
        target.get("name"),
    )
    role_filter = _first_nonempty_text(target.get("role_filter"), target.get("role"))
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    app_name = _first_nonempty_text(
        _observed_action_app_name(target),
        step.get("target_app_name"),
        data.get("app_name"),
    )
    elements = _desktop_observation_result_elements(tool_name, result)
    evidence: dict[str, Any] = {
        "ui_element_count": len(elements),
    }
    if app_name:
        evidence["ui_app_name"] = app_name
    if target_text:
        evidence["ui_target"] = target_text
    if role_filter:
        evidence["ui_role_filter"] = role_filter
    if target_text:
        matches = _ui_observation_target_matches(elements, target_text, role_filter)
        evidence["ui_target_found"] = bool(matches)
        evidence["ui_match_count"] = len(matches)
        if matches:
            evidence["ui_matches"] = [
                _compact_ui_observation_match(match)
                for match in matches[:3]
            ]
    return {key: value for key, value in evidence.items() if value not in ("", [], {})}


def _ui_observation_verification_target(
    step: Mapping[str, Any],
    verification_target: Mapping[str, Any],
) -> dict[str, Any]:
    target: dict[str, Any] = {}
    for source in (
        step.get("followup_target"),
        step.get("action_target"),
        step.get("input_preview"),
        verification_target,
    ):
        if not isinstance(source, Mapping):
            continue
        for key in (
            "kind",
            "target",
            "target_search_text",
            "target_label",
            "ui_target",
            "element_name",
            "name",
            "role_filter",
            "role",
            "app_name",
            "target_app_name",
            "app_query",
            "target_app_query",
        ):
            value = source.get(key)
            if value not in (None, "", [], {}) and key not in target:
                target[key] = value
    return target


def _compact_ui_observation_match(element: Mapping[str, Any]) -> dict[str, Any]:
    payload: dict[str, Any] = {}
    for key in ("role", "subrole", "name", "description", "value", "label"):
        value = _compact_ui_observation_text(element.get(key))
        if value:
            payload[key] = value
    center = element.get("center") if isinstance(element.get("center"), Mapping) else {}
    if center.get("x") is not None and center.get("y") is not None:
        payload["center"] = {"x": center.get("x"), "y": center.get("y")}
    if isinstance(element.get("enabled"), bool):
        payload["enabled"] = bool(element.get("enabled"))
    return payload


def _compact_ui_observation_text(value: Any, *, limit: int = 120) -> str:
    text = re.sub(r"\s+", " ", str(value or "").strip())
    if len(text) <= limit:
        return text
    return f"{text[: max(0, limit - 1)].rstrip()}..."


def _first_nonempty_text(*values: Any) -> str:
    for value in values:
        text = str(value or "").strip()
        if text:
            return text
    return ""


def _split_model_materialization_tool_requests(
    requests: Iterable[Mapping[str, Any]],
) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    immediate: list[dict[str, Any]] = []
    materialization: list[dict[str, Any]] = []
    defer_remaining = False
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        item = dict(request)
        if defer_remaining or _tool_request_requires_model_materialization(item):
            defer_remaining = True
            materialization.append(item)
        else:
            immediate.append(item)
    return immediate, materialization


def _tool_request_requires_model_materialization(request: Mapping[str, Any]) -> bool:
    if not isinstance(request, Mapping) or not bool(request.get("continue_to_model")):
        return False
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    body_source = str(request_input.get("body_source") or "").strip()
    if tool_name == "notes.create":
        return bool(body_source) and not str(request_input.get("body") or "").strip()
    if tool_name == "artifact.write":
        if str(request_input.get("content") or "").strip():
            return False
        return bool(
            body_source
            or str(request_input.get("path") or "").strip()
            or request_input.get("paths")
        )
    if tool_name == "clipboard.write":
        return bool(body_source) and not str(request_input.get("text") or "").strip()
    if tool_name == "workspace.write_patch":
        if any(
            str(request_input.get(key) or "").strip()
            for key in ("patch", "diff", "content")
        ):
            return False
        if any(request_input.get(key) for key in ("changes", "edits", "operations")):
            return False
        return bool(
            request_input.get("patch_source")
            or request_input.get("diff_source")
            or request_input.get("body_source")
            or request_input.get("mode")
        )
    if tool_name in {"terminal.run", "python.run"}:
        return _command_tool_request_requires_model_materialization(
            tool_name,
            request_input,
        )
    if tool_name in _MODEL_FOLLOWUP_TEXT_ENTRY_TOOLS:
        return bool(body_source) and not str(request_input.get("text") or "").strip()
    return False


def _command_tool_request_requires_model_materialization(
    tool_name: str,
    request_input: Mapping[str, Any],
) -> bool:
    command = str(request_input.get("command") or "").strip()
    code = str(request_input.get("code") or "").strip()
    if tool_name == "python.run" and code:
        return False
    if command and not _command_tool_request_looks_like_placeholder(command):
        return False
    if _command_tool_request_looks_like_placeholder(command):
        return True
    return any(
        request_input.get(key)
        for key in (
            "body_source",
            "file_type",
            "operation",
            "path",
            "paths",
            "pattern",
            "query",
            "selection",
            "source",
            "source_path",
        )
    )


def _command_tool_request_looks_like_placeholder(command: str) -> bool:
    value = str(command or "").strip().lower()
    if not value:
        return False
    return any(
        marker in value
        for marker in (
            "# analyze captured tabular data",
            "# inspect data, compute summary, generate charts",
            "# inspect data",
            "todo:",
            "<model",
            "<generated",
        )
    )


def _runtime_planner_should_trace_tool_requests(decision: Any | None) -> bool:
    steps = getattr(
        getattr(getattr(decision, "plan", None), "tool_plan", None),
        "steps",
        None,
    )
    if not isinstance(steps, Iterable) or isinstance(steps, (str, bytes)):
        return False
    observable_steps = [
        step
        for step in steps
        if str(getattr(step, "tool_name", "") or "").strip()
        and str(getattr(step, "step_id", "") or "").strip()
    ]
    if any(bool(getattr(step, "approval_required", False)) for step in observable_steps):
        return True
    return len(observable_steps) > 1


def _approval_required_planner_trace_payload(
    pending_approval: Mapping[str, Any],
    planned_request: Mapping[str, Any],
) -> dict[str, Any]:
    tool_request = (
        pending_approval.get("tool_request")
        if isinstance(pending_approval.get("tool_request"), Mapping)
        else {}
    )
    payload: dict[str, Any] = {}
    for key in (
        "decision_id",
        "plan_id",
        "tool_plan_id",
        "intent_kind",
        "step_id",
        "planner_step_id",
        "capability_id",
        "replan_request_id",
        "replan_trigger",
    ):
        value = str(tool_request.get(key) or planned_request.get(key) or "").strip()
        if value:
            payload[key] = value
    if payload.get("step_id") and not payload.get("planner_step_id"):
        payload["planner_step_id"] = payload["step_id"]
    payload.update(
        {
            **_runtime_trace_metadata_from_mapping(planned_request),
            **_runtime_trace_metadata_from_mapping(tool_request),
        }
    )
    return payload


def _step_observability_metadata(step: Mapping[str, Any]) -> dict[str, Any]:
    return _request_observability_metadata(step)


def _desktop_observed_action_execution_target(target: str, role_filter: str) -> str:
    clean_target = str(target or "").strip()
    normalized_target = _normalize_observed_desktop_text(clean_target)
    normalized_filter = _normalize_observed_desktop_text(role_filter)
    if normalized_target in {"text input", "input"} and normalized_filter in {
        "text field",
        "textfield",
        "axtextfield",
    }:
        return "text field"
    return clean_target


def _latest_desktop_observation_match_center(
    timeline: list[dict[str, Any]],
    target: str,
    role_filter: str,
) -> dict[str, Any]:
    elements = _latest_desktop_observation_elements(timeline)
    if not elements:
        return {}
    ordinal = _observed_desktop_target_ordinal(target)
    if ordinal:
        matches = _ordinal_observed_desktop_elements(elements, ordinal, role_filter)
        if matches:
            center = (
                matches[0].get("center")
                if isinstance(matches[0].get("center"), Mapping)
                else {}
            )
            x = center.get("x")
            y = center.get("y")
            if x is not None and y is not None:
                return {"x": x, "y": y}
    matches = _matching_observed_desktop_elements(elements, target, role_filter)
    if not matches:
        return {}
    center = (
        matches[0].get("center")
        if isinstance(matches[0].get("center"), Mapping)
        else {}
    )
    x = center.get("x")
    y = center.get("y")
    if x is None or y is None:
        return {}
    return {"x": x, "y": y}


def _latest_desktop_observation_has_target_match(
    timeline: list[dict[str, Any]],
    target: str,
    role_filter: str,
) -> bool:
    elements = _latest_desktop_observation_elements(timeline)
    if not elements:
        return False
    target_text = _normalize_observed_desktop_text(target)
    if not target_text:
        return False
    ordinal = _observed_desktop_target_ordinal(target)
    if ordinal:
        return bool(_ordinal_observed_desktop_elements(elements, ordinal, role_filter))
    normalized_filter = _normalize_observed_desktop_text(role_filter)
    for raw_element in elements:
        if not isinstance(raw_element, Mapping):
            continue
        element = dict(raw_element)
        if element.get("enabled") is False:
            continue
        searchable = _normalize_observed_desktop_text(
            " ".join(
                str(element.get(key) or "")
                for key in ("role", "subrole", "name", "description", "value", "label")
            )
        )
        if normalized_filter and not _observed_desktop_text_matches(
            searchable,
            normalized_filter,
        ):
            continue
        if _observed_desktop_element_match_score(element, target_text, role_filter) > 0:
            return True
    return False


def _runtime_planner_deferred_observation_scope_matches(
    request: Mapping[str, Any],
    target: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> bool:
    """Require the exact runtime-owned observation that deferred this action."""

    scope = (
        request.get("runtime_observation_scope")
        if isinstance(request.get("runtime_observation_scope"), Mapping)
        else {}
    )
    if not scope:
        return True
    observation: Mapping[str, Any] | None = None
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() not in (
            _RUNTIME_PLANNER_UI_OBSERVATION_TOOLS
        ):
            continue
        observation = event
        break
    if observation is None:
        return False
    result = (
        observation.get("result")
        if isinstance(observation.get("result"), Mapping)
        else {}
    )
    if result.get("ok") is not True:
        return False
    for scope_key, event_keys in (
        ("decision_id", ("decision_id",)),
        ("plan_id", ("plan_id",)),
        ("observation_step_id", ("step_id", "planner_step_id")),
    ):
        expected = str(scope.get(scope_key) or "").strip()
        if not expected:
            continue
        observed = next(
            (
                str(observation.get(key) or "").strip()
                for key in event_keys
                if str(observation.get(key) or "").strip()
            ),
            "",
        )
        if observed != expected:
            return False
    expected_app = str(
        scope.get("app_name")
        or target.get("app_name")
        or target.get("target_app_name")
        or ""
    ).strip()
    if expected_app:
        input_preview = (
            observation.get("input_preview")
            if isinstance(observation.get("input_preview"), Mapping)
            else {}
        )
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        observed_app = str(
            input_preview.get("app_name")
            or data.get("app_name")
            or result.get("app_name")
            or ""
        ).strip()
        if (
            not observed_app
            or compact_app_name_hint(observed_app)
            != compact_app_name_hint(expected_app)
        ):
            return False
    return True


def _latest_desktop_observation_elements(timeline: list[dict[str, Any]]) -> list[Any]:
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool_name = str(event.get("detail") or "").strip()
        if tool_name not in {"desktop.read_ui", "desktop.ui_elements", "desktop.inspect_app"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True:
            return []
        elements = _desktop_observation_result_elements(tool_name, result)
        if elements:
            return elements
    return []


def _desktop_observation_result_elements(tool_name: str, result: Mapping[str, Any]) -> list[Any]:
    if str(tool_name or "").strip() == "desktop.inspect_app":
        return _inspect_app_observation_elements(result)
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    elements = data.get("elements")
    if isinstance(elements, list):
        return elements
    elements = result.get("elements")
    return elements if isinstance(elements, list) else []


def _inspect_app_observation_elements(result: Mapping[str, Any]) -> list[Any]:
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    ui_result = data.get("ui_elements") if isinstance(data.get("ui_elements"), Mapping) else {}
    ui_data = ui_result.get("data") if isinstance(ui_result.get("data"), Mapping) else {}
    elements = ui_data.get("elements")
    if isinstance(elements, list):
        return elements
    elements = ui_result.get("elements")
    if isinstance(elements, list):
        return elements
    elements = data.get("elements")
    return elements if isinstance(elements, list) else []


def _observed_desktop_target_ordinal(target: str) -> int:
    normalized = _normalize_observed_desktop_text(target)
    if normalized in {
        "first result",
        "first item",
        "top result",
        "第一个结果",
        "第1个结果",
        "第一项",
        "第一个",
        "首个结果",
    }:
        return 1
    match = re.fullmatch(r"(?:result|item)\s*(\d+)", normalized)
    if match:
        return max(1, int(match.group(1)))
    match = re.fullmatch(r"第\s*(\d+)\s*(?:个)?(?:结果|项目|项)?", normalized)
    if match:
        return max(1, int(match.group(1)))
    return 0


def _ordinal_observed_desktop_elements(
    elements: list[Any],
    ordinal: int,
    role_filter: str,
) -> list[dict[str, Any]]:
    candidates: list[dict[str, Any]] = []
    normalized_filter = _normalize_observed_desktop_text(role_filter)
    for raw_element in elements:
        if not isinstance(raw_element, Mapping):
            continue
        element = dict(raw_element)
        if element.get("enabled") is False:
            continue
        center = element.get("center") if isinstance(element.get("center"), Mapping) else {}
        if center.get("x") is None or center.get("y") is None:
            continue
        searchable = _normalize_observed_desktop_text(
            " ".join(
                str(element.get(key) or "")
                for key in ("role", "subrole", "name", "description", "value", "label")
            )
        )
        if normalized_filter:
            if not _observed_desktop_text_matches(searchable, normalized_filter):
                continue
        elif not _observed_desktop_element_looks_actionable_result(element, searchable):
            continue
        candidates.append(element)
    index = max(0, ordinal - 1)
    return candidates[index : index + 1]


def _observed_desktop_element_looks_actionable_result(
    element: Mapping[str, Any],
    searchable: str,
) -> bool:
    if not searchable:
        return False
    if any(
        token in searchable
        for token in (
            "text field",
            "search field",
            "toolbar",
            "menu bar",
            "window",
        )
    ):
        return False
    if any(
        token in searchable
        for token in (
            "button",
            "link",
            "row",
            "cell",
            "group",
            "static text",
            "image",
        )
    ):
        return True
    label = str(
        element.get("name")
        or element.get("description")
        or element.get("label")
        or ""
    ).strip()
    return bool(label)


def _matching_observed_desktop_elements(
    elements: list[Any],
    target: str,
    role_filter: str,
) -> list[dict[str, Any]]:
    target_text = _normalize_observed_desktop_text(target)
    if not target_text:
        return []
    scored: list[tuple[int, int, dict[str, Any]]] = []
    for index, raw_element in enumerate(elements):
        if not isinstance(raw_element, Mapping):
            continue
        element = dict(raw_element)
        if element.get("enabled") is False:
            continue
        center = element.get("center") if isinstance(element.get("center"), Mapping) else {}
        if center.get("x") is None or center.get("y") is None:
            continue
        score = _observed_desktop_element_match_score(element, target_text, role_filter)
        if score <= 0:
            continue
        depth = element.get("depth") if isinstance(element.get("depth"), int) else 0
        scored.append((score - depth, -index, element))
    scored.sort(reverse=True, key=lambda item: (item[0], item[1]))
    return [element for _, _, element in scored]


def _observed_desktop_element_match_score(
    element: Mapping[str, Any],
    normalized_target: str,
    role_filter: str,
) -> int:
    label_texts = [
        _normalize_observed_desktop_text(element.get(key))
        for key in ("name", "description", "value", "label")
    ]
    label_texts = [text for text in label_texts if text]
    searchable = _normalize_observed_desktop_text(
        " ".join(
            str(element.get(key) or "")
            for key in ("role", "subrole", "name", "description", "value", "label")
        )
    )
    if not searchable:
        return 0
    score = 0
    for target in _observed_desktop_text_candidates(normalized_target):
        if target in label_texts:
            score = max(score, 100)
        elif any(target in text for text in label_texts):
            score = max(score, 85)
        elif any(text in target for text in label_texts if len(text) >= 2):
            score = max(score, 70)
        elif target in searchable:
            score = max(score, 55)
    if not score:
        return 0
    normalized_filter = _normalize_observed_desktop_text(role_filter)
    if normalized_filter and _observed_desktop_text_matches(searchable, normalized_filter):
        score += 10
    if str(element.get("role") or "").strip():
        score += 2
    return score


def _normalize_observed_desktop_text(value: Any) -> str:
    text = str(value or "").strip().casefold()
    text = re.sub(r"\baxtextfield\b", "text field", text)
    text = re.sub(r"\baxbutton\b", "button", text)
    text = re.sub(r"\baxlink\b", "link", text)
    return re.sub(r"\s+", " ", text)


_OBSERVED_DESKTOP_TEXT_EQUIVALENTS: tuple[tuple[tuple[str, ...], tuple[str, ...]], ...] = (
    (("搜索框", "搜尋框", "查找框", "搜索栏", "搜尋欄"), ("search", "search field", "search box")),
    (("搜索", "搜尋", "查找", "查詢", "查询"), ("search", "find")),
    (("输入框", "輸入框", "文本框", "文字框"), ("text field", "input", "input field", "text input")),
    (("输入", "輸入", "填写", "填寫"), ("type", "input")),
    (("按钮", "按鈕"), ("button",)),
    (("链接", "連結", "联结"), ("link",)),
)


def _observed_desktop_text_candidates(normalized_text: str) -> list[str]:
    clean_text = _normalize_observed_desktop_text(normalized_text)
    if not clean_text:
        return []
    candidates = [clean_text]
    for sources, aliases in _OBSERVED_DESKTOP_TEXT_EQUIVALENTS:
        for source in sources:
            if source not in clean_text:
                continue
            for alias in aliases:
                clean_alias = _normalize_observed_desktop_text(alias)
                if not clean_alias:
                    continue
                candidates.append(clean_alias)
                candidates.append(clean_text.replace(source, clean_alias))
    return _ordered_text_list(candidates)


def _observed_desktop_text_matches(searchable: str, normalized_query: str) -> bool:
    clean_searchable = _normalize_observed_desktop_text(searchable)
    if not clean_searchable:
        return False
    return any(
        candidate and candidate in clean_searchable
        for candidate in _observed_desktop_text_candidates(normalized_query)
    )


def _drop_completed_auto_followup_prefix(
    requests: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    remaining = list(requests)
    completion_cursor = tool_timeline_start
    while remaining:
        next_cursor = _auto_followup_request_completion_cursor(
            remaining[0],
            timeline,
            tool_timeline_start=completion_cursor,
        )
        if next_cursor is None:
            break
        completion_cursor = next_cursor
        remaining = remaining[1:]
    return remaining


def _drop_completed_artifact_write_requests(
    requests: list[dict[str, Any]],
    artifacts: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    completed_paths = {
        str(artifact.get("path") or "").strip()
        for artifact in artifacts
        if str(artifact.get("path") or "").strip()
    }
    if not completed_paths:
        return requests
    filtered: list[dict[str, Any]] = []
    for request in requests:
        request_input = (
            request.get("input") if isinstance(request.get("input"), Mapping) else {}
        )
        tool_name = str(request.get("tool") or "").strip()
        path = str(request_input.get("path") or "").strip()
        if tool_name == "artifact.write" and path and any(
            completed == path or completed.endswith(f"/{path}")
            for completed in completed_paths
        ):
            continue
        filtered.append(request)
    return filtered


def _auto_followup_request_completed(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    return (
        _auto_followup_request_completion_cursor(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        is not None
    )


def _auto_followup_request_completion_cursor(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> int | None:
    tool_name = str(request.get("tool") or "").strip()
    if not tool_name:
        return None
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    for event_index, event in enumerate(
        timeline[tool_timeline_start:],
        start=tool_timeline_start,
    ):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("detail") or "").strip() != tool_name:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True or result.get("approval_required"):
            continue
        event_input = event.get("input_preview") if isinstance(event.get("input_preview"), Mapping) else {}
        if tool_name == "artifact.write":
            request_path = str(request_input.get("path") or "").strip()
            event_path = str(event_input.get("path") or "").strip()
            if not request_path or request_path != event_path:
                continue
        elif not _auto_followup_input_matches_event(request_input, event_input):
            continue
        cursor = event_index + 1
        deferred_items = materialized_deferred_items(request)
        if any(not isinstance(item, Mapping) for item in deferred_items):
            continue
        chain_completed = True
        for item in deferred_items:
            next_cursor = _auto_followup_request_completion_cursor(
                item,
                timeline,
                tool_timeline_start=cursor,
            )
            if next_cursor is None:
                chain_completed = False
                break
            cursor = next_cursor
        if chain_completed:
            return cursor
    return None


def _auto_followup_input_matches_event(
    request_input: Mapping[str, Any],
    event_input: Mapping[str, Any],
) -> bool:
    comparable = {
        str(key): value
        for key, value in request_input.items()
        if str(key) not in {"selection_source", "query", "app_query"}
        and not (
            str(key) == "app_name"
            and str(value or "").strip().casefold().startswith("<selected app from ")
        )
    }
    if not comparable:
        return True
    return all(
        str(event_input.get(key) or "") == str(value or "")
        for key, value in comparable.items()
    )


def _auto_replan_verification_recovery_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    verification_payloads = [
        payload
        for payload in replan_payloads
        if isinstance(payload, Mapping)
        and str(payload.get("trigger") or "").strip() == "verification_failed"
        and not _replan_payload_is_focus_mismatch(payload)
        and str(payload.get("source_step_id") or "").strip()
        in {"verify-desktop-result", "observe-selected-discovered-app"}
    ]
    if not verification_payloads:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    first = verification_payloads[0]
    request_id = str(first.get("request_id") or "").strip()
    source = "runtime_planner"
    planning_reason = "planner_verification_recovery_observation"
    target = _replan_recovery_target(first)
    explicit_recovery_tools = _runtime_recovery_action_tools_for_payload(first)
    requests: list[dict[str, Any]] = []
    for tool_name in _verification_recovery_tool_order(allowed):
        if tool_name in explicit_recovery_tools:
            continue
        request_input = _verification_recovery_tool_input(tool_name, target)
        if request_input is None:
            continue
        request = _request_like(
            tool_name,
            request_input,
            source=source,
            planning_reason=planning_reason,
        )
        if request_id:
            request["replan_request_id"] = request_id
        request["replan_trigger"] = "verification_failed"
        request["continue_to_model"] = True
        for key, value in target.items():
            if value:
                request[key] = value
        _attach_replan_payload_trace_metadata(request, first)
        _attach_replan_active_window_verification_target(request, target)
        _sanitize_zero_input_observation_recovery_request(request)
        requests.append(request)
    return requests


def _sanitize_zero_input_observation_recovery_request(request: dict[str, Any]) -> None:
    tool_name = str(request.get("tool") or "").strip()
    if tool_name not in {"desktop.active_window", "desktop.running_apps"}:
        return
    request["input"] = {}
    observation_retry = (
        request.get("observation_retry")
        if isinstance(request.get("observation_retry"), Mapping)
        else None
    )
    if observation_retry is not None:
        request["observation_retry"] = {**dict(observation_retry), "input": {}}


def _runtime_recovery_action_tools_for_payload(payload: Mapping[str, Any]) -> set[str]:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    return {
        str(action.get("tool") or "").strip()
        for action in _mapping_list(metadata.get("recovery_actions"))
        if str(action.get("planning_reason") or "").strip()
        != "planner_replan_runtime_recovery_action"
        if str(action.get("tool") or "").strip()
    }


def _auto_replan_ui_observation_recovery_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "tool_failure":
            continue
        target = _replan_ui_observed_action_target(payload)
        if not target:
            continue
        tool_name = _replan_ui_observation_tool(target, allowed)
        if not tool_name:
            continue
        request = _request_like(
            tool_name,
            _replan_ui_observation_input(tool_name, target),
            source="runtime_planner",
            planning_reason="planner_replan_ui_observation_recovery",
        )
        request_id = str(payload.get("request_id") or "").strip()
        if request_id:
            request["replan_request_id"] = request_id
        request["replan_trigger"] = "tool_failure"
        request["continue_to_model"] = True
        for key in ("target_app_name", "target_app_query", "target_search_text"):
            value = str(target.get(key) or "").strip()
            if value:
                request[key] = value
        _attach_replan_payload_trace_metadata(request, payload)
        requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _auto_replan_verification_observed_action_requests(
    replan_payloads: list[dict[str, Any]],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not allowed or not planned or not _latest_desktop_observation_succeeded(timeline):
        return []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "verification_failed":
            continue
        if not _replan_payload_has_missing_ui_target(payload):
            continue
        source_request = _replan_verification_source_ui_action_request(planned, payload)
        if not source_request:
            continue
        action_payload = _replan_verification_observed_action_payload(
            payload,
            source_request,
        )
        target = _replan_ui_observed_action_target(action_payload)
        if not target:
            continue
        target = _replan_ui_observed_action_target_with_timeline_context(
            target,
            action_payload,
            timeline,
        )
        target_label = str(target.get("target") or "").strip()
        role_filter = str(target.get("role_filter") or "").strip()
        if not _latest_desktop_observation_has_target_match(
            timeline,
            target_label,
            role_filter,
        ):
            continue
        observation_source = _latest_desktop_observation_tool(timeline)
        if observation_source:
            target["observation_source"] = observation_source
        source_tool = _replan_source_tool_name(action_payload)
        scoped_allowed = _replan_ui_observed_action_scoped_allowed_tools(
            source_tool,
            target,
            allowed,
            timeline,
        )
        if not scoped_allowed:
            continue
        requests = _auto_desktop_observed_action_followup_requests(
            {"followup_target": target},
            scoped_allowed,
            timeline,
            planning_reason=planning_reason,
        )
        if not requests:
            continue
        requests = _annotate_replan_ui_observed_action_requests(requests, action_payload)
        return _replan_recovery_requests_with_task_context(
            requests,
            [action_payload],
            timeline,
        )
    return []


def _replan_payload_has_missing_ui_target(payload: Mapping[str, Any]) -> bool:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    if metadata.get("ui_target_found") is False:
        return True
    blocking_conditions = {
        str(item or "").strip()
        for item in _string_list(metadata.get("blocking_conditions"))
    }
    return "ui_target_not_found" in blocking_conditions


def _replan_verification_source_ui_action_request(
    planned_tool_requests: list[Mapping[str, Any]],
    payload: Mapping[str, Any],
) -> dict[str, Any]:
    source_index = _replan_source_request_index(planned_tool_requests, payload)
    candidates = (
        planned_tool_requests[:source_index]
        if source_index >= 0
        else planned_tool_requests
    )
    target_text = str(_replan_recovery_target(payload).get("target_search_text") or "").strip()
    for request in reversed(candidates):
        if not isinstance(request, Mapping):
            continue
        tool_name = str(request.get("tool") or "").strip()
        if _replan_ui_target_action(tool_name) != "click":
            continue
        if not _replan_ui_action_request_matches_target(request, target_text):
            continue
        return dict(request)
    return {}


def _replan_ui_action_request_matches_target(
    request: Mapping[str, Any],
    target_text: str,
) -> bool:
    clean_target = _normalize_observed_desktop_text(target_text)
    if not clean_target:
        return True
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    request_target = _normalize_observed_desktop_text(
        request_input.get("target")
        or request_input.get("target_search_text")
        or request_input.get("text")
        or request_input.get("value")
        or ""
    )
    if not request_target:
        return False
    return (
        _observed_desktop_text_matches(request_target, clean_target)
        or _observed_desktop_text_matches(clean_target, request_target)
    )


def _replan_verification_observed_action_payload(
    payload: Mapping[str, Any],
    source_request: Mapping[str, Any],
) -> dict[str, Any]:
    request_input = (
        dict(source_request.get("input"))
        if isinstance(source_request.get("input"), Mapping)
        else {}
    )
    action_payload = dict(payload)
    metadata = (
        dict(payload.get("metadata"))
        if isinstance(payload.get("metadata"), Mapping)
        else {}
    )
    metadata["verification_source_step_id"] = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    metadata["verification_source_tool_name"] = str(
        payload.get("source_tool_name") or payload.get("tool_name") or ""
    ).strip()
    metadata["input_preview"] = request_input
    action_payload["metadata"] = {
        key: value for key, value in metadata.items() if value not in (None, "", [], {})
    }
    action_payload["input_preview"] = request_input
    step_id = str(
        source_request.get("step_id") or source_request.get("planner_step_id") or ""
    ).strip()
    if step_id:
        action_payload["source_step_id"] = step_id
        action_payload["planner_step_id"] = step_id
    tool_name = str(source_request.get("tool") or "").strip()
    if tool_name:
        action_payload["source_tool_name"] = tool_name
    capability_id = str(source_request.get("capability_id") or "").strip()
    if capability_id:
        action_payload["target_capability_id"] = capability_id
    return action_payload


def _auto_replan_ui_observed_action_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed or not _latest_desktop_observation_succeeded(timeline):
        return []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "tool_failure":
            continue
        target = _replan_ui_observed_action_target(payload)
        if not target:
            continue
        target = _replan_ui_observed_action_target_with_timeline_context(
            target,
            payload,
            timeline,
        )
        if _replan_ui_observed_action_retry_succeeded(payload, timeline):
            continue
        target_label = str(target.get("target") or "").strip()
        role_filter = str(target.get("role_filter") or "").strip()
        if not _latest_desktop_observation_has_target_match(
            timeline,
            target_label,
            role_filter,
        ):
            continue
        observation_source = _latest_desktop_observation_tool(timeline)
        if observation_source:
            target["observation_source"] = observation_source
        source_tool = _replan_source_tool_name(payload)
        scoped_allowed = _replan_ui_observed_action_scoped_allowed_tools(
            source_tool,
            target,
            allowed,
            timeline,
        )
        if not scoped_allowed:
            continue
        requests = _auto_desktop_observed_action_followup_requests(
            {"followup_target": target},
            scoped_allowed,
            timeline,
            planning_reason=planning_reason,
        )
        if not requests:
            continue
        requests = _annotate_replan_ui_observed_action_requests(requests, payload)
        enriched = _replan_recovery_requests_with_task_context(
            requests,
            [dict(payload)],
            timeline,
        )
        return _drop_replayed_approved_replan_operate_requests(enriched, timeline)
    return []


def _replan_ui_observed_action_scoped_allowed_tools(
    source_tool: str,
    target: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
) -> list[str]:
    observation_tools = (
        "desktop.read_ui",
        "desktop.ui_elements",
        "desktop.active_window",
        "screen.capture",
    )
    target_action = str(target.get("target_action") or "").strip()
    if (
        target_action == "click"
        and source_tool in {
            "app.open_and_click_ui_element",
            "app.focus_and_click_ui_element",
            "desktop.click_ui_element",
        }
    ):
        target_label = str(target.get("target") or "").strip()
        role_filter = str(target.get("role_filter") or "").strip()
        if _latest_desktop_observation_match_center(timeline, target_label, role_filter):
            low_level_tools = [
                tool for tool in ("desktop.safe_click", "desktop.click") if tool in allowed
            ]
            if low_level_tools:
                return [
                    *low_level_tools,
                    *[tool for tool in observation_tools if tool in allowed],
                ]
    if (
        target_action == "type_text"
        and source_tool in {
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        }
    ):
        role_filter = str(target.get("role_filter") or "").strip()
        target_label = _desktop_observed_action_execution_target(
            str(target.get("target") or "").strip(),
            role_filter,
        )
        if _latest_desktop_observation_match_center(timeline, target_label, role_filter):
            focus_tools = [
                tool for tool in ("desktop.safe_click", "desktop.click") if tool in allowed
            ]
            type_tools = [
                tool
                for tool in ("desktop.safe_type_text", "desktop.type_text", "desktop.type")
                if tool in allowed
            ]
            if focus_tools and type_tools:
                return [
                    *focus_tools,
                    *type_tools,
                    *[tool for tool in observation_tools if tool in allowed],
                ]
    return [
        tool
        for tool in (
            source_tool,
            *observation_tools,
        )
        if tool and tool in allowed
    ]


def _replan_ui_observed_action_target(payload: Mapping[str, Any]) -> dict[str, Any]:
    source_tool = _replan_source_tool_name(payload)
    target_action = _replan_ui_target_action(source_tool)
    if not target_action:
        return {}
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    input_preview = (
        payload.get("input_preview")
        if isinstance(payload.get("input_preview"), Mapping)
        else {}
    )
    metadata_input = (
        metadata.get("input_preview")
        if isinstance(metadata.get("input_preview"), Mapping)
        else {}
    )
    source_input = {**dict(metadata_input), **dict(input_preview)}
    target_label = str(source_input.get("target") or "").strip()
    if not target_label:
        return {}
    target: dict[str, Any] = {
        "kind": "desktop_observed_action",
        "target_action": target_action,
        "target": target_label,
        "role_filter": str(source_input.get("role_filter") or "").strip(),
        "limit": _clean_model_followup_int(source_input.get("limit"), default=80),
    }
    recovery_target = _replan_recovery_target(payload)
    app_name = str(
        recovery_target.get("target_app_name")
        or source_input.get("app_name")
        or ""
    ).strip()
    if app_name:
        target["app_name"] = app_name
        target["target_app_name"] = app_name
    app_query = str(
        recovery_target.get("target_app_query")
        or source_input.get("query")
        or source_input.get("app_query")
        or ""
    ).strip()
    if app_query:
        target["app_query"] = app_query
        target["target_app_query"] = app_query
    search_text = str(
        recovery_target.get("target_search_text")
        or source_input.get("text")
        or source_input.get("value")
        or ""
    ).strip()
    if search_text:
        target["target_search_text"] = search_text
    if target_action == "click":
        target["click_count"] = _clean_model_followup_int(
            source_input.get("click_count"),
            default=1,
        )
    else:
        text = str(source_input.get("text") or "")
        if not text:
            return {}
        target["text"] = text
    return {key: value for key, value in target.items() if value not in ("", None, [], {})}


def _replan_ui_observed_action_target_with_timeline_context(
    target: Mapping[str, Any],
    payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    resolved = dict(target)
    app_query = str(
        resolved.get("app_query")
        or resolved.get("target_app_query")
        or _replan_recovery_target(payload).get("target_app_query")
        or ""
    ).strip()
    if app_query:
        resolved["app_query"] = app_query
        resolved["target_app_query"] = app_query

    app_name = str(resolved.get("app_name") or resolved.get("target_app_name") or "").strip()
    if app_name and not _runtime_planner_placeholder_app_name(app_name):
        return resolved

    discovered = _discovered_app_context_app_name(
        resolved,
        timeline,
        replan_payloads=[payload],
    )
    if not discovered:
        request_id = str(payload.get("request_id") or "").strip()
        discovered = _latest_replan_context_app_name(timeline, request_id=request_id)
    if discovered and not _runtime_planner_placeholder_app_name(discovered):
        resolved["app_name"] = discovered
        resolved["target_app_name"] = discovered
    return resolved


def _latest_replan_context_app_name(
    timeline: list[dict[str, Any]],
    *,
    request_id: str,
) -> str:
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        event_request_id = str(event.get("replan_request_id") or "").strip()
        if request_id and event_request_id and event_request_id != request_id:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        for source in (event, data, result, input_preview):
            for key in (
                "target_app_name",
                "app_name",
                "active_app_name",
                "frontmost_app",
                "resolved_app_name",
                "discovered_app_name",
            ):
                app_name = str(source.get(key) or "").strip()
                if app_name and not _runtime_planner_placeholder_app_name(app_name):
                    return app_name
    return ""


def _replan_source_tool_name(payload: Mapping[str, Any]) -> str:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    return str(
        payload.get("source_tool_name")
        or payload.get("tool_name")
        or payload.get("planned_tool_name")
        or metadata.get("planned_tool_name")
        or ""
    ).strip()


def _replan_ui_target_action(tool_name: str) -> str:
    clean_tool = str(tool_name or "").strip()
    if clean_tool in {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "desktop.click_ui_element",
    }:
        return "click"
    if clean_tool in {
        "app.open_and_type_into_ui_element",
        "app.focus_and_type_into_ui_element",
        "desktop.type_into_ui_element",
    }:
        return "type_text"
    return ""


def _replan_ui_observation_tool(target: Mapping[str, Any], allowed: set[str]) -> str:
    app_name = _observed_action_app_name(target)
    if app_name and "desktop.inspect_app" in allowed:
        return "desktop.inspect_app"
    return _first_allowed_tool(
        ("desktop.ui_elements", "desktop.read_ui", "screen.capture"),
        allowed,
    )


def _replan_ui_observation_input(
    tool_name: str,
    target: Mapping[str, Any],
) -> dict[str, Any]:
    clean_tool = str(tool_name or "").strip()
    app_name = _observed_action_app_name(target)
    role_filter = str(target.get("role_filter") or "").strip()
    limit = _clean_model_followup_int(target.get("limit"), default=80)
    if clean_tool == "desktop.inspect_app":
        payload: dict[str, Any] = {
            "app_name": app_name,
            "open_if_needed": True,
            "focus": True,
            "limit": limit,
        }
        if role_filter:
            payload["role_filter"] = role_filter
        return payload
    if clean_tool in {"desktop.ui_elements", "desktop.read_ui"}:
        payload = {"limit": limit}
        if app_name:
            payload["app_name"] = app_name
        if role_filter:
            payload["role_filter"] = role_filter
        return payload
    if clean_tool == "screen.capture":
        target_label = str(target.get("target") or "").strip()
        if app_name and target_label:
            return {"reason": f"inspect {app_name} UI target {target_label}"}
        if app_name:
            return {"reason": f"inspect {app_name} UI after failed action"}
        return {"reason": "inspect desktop UI after failed action"}
    return {}


def _annotate_replan_ui_observed_action_requests(
    requests: list[dict[str, Any]],
    payload: Mapping[str, Any],
) -> list[dict[str, Any]]:
    annotated: list[dict[str, Any]] = []
    request_id = str(payload.get("request_id") or "").strip()
    trigger = str(payload.get("trigger") or "tool_failure").strip() or "tool_failure"
    action_step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    for request in requests:
        item = dict(request)
        is_verifier = _replan_observed_action_verification_request(item)
        if request_id:
            item["replan_request_id"] = request_id
        item["replan_trigger"] = trigger
        _attach_replan_payload_trace_metadata(item, payload)
        if is_verifier:
            item["runtime_stage"] = "verify"
            item["runtime_role"] = "verify_result"
            item["requires_observation"] = True
            item["requires_post_action_verification"] = False
            if action_step_id:
                item["depends_on"] = [action_step_id]
                item["verification_target_step_ids"] = [action_step_id]
        annotated.append(item)
    return annotated


def _auto_replan_ui_continuation_requests(
    replan_payloads: list[dict[str, Any]],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
    planning_reason: str,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not planned:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "tool_failure":
            continue
        if not _replan_ui_observed_action_target(payload):
            continue
        if not _replan_ui_observed_action_retry_succeeded(payload, timeline):
            continue
        source_index = _replan_source_request_index(planned, payload)
        if source_index < 0:
            continue
        continuation = _replan_ui_continuation_slice(
            planned[source_index + 1 :],
            payload,
            allowed,
            timeline,
            planning_reason=planning_reason,
        )
        continuation = _drop_completed_auto_followup_prefix(
            continuation,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        requests.extend(continuation)
    requests = _dedupe_replan_recovery_requests(requests)
    return _replan_recovery_requests_with_task_context(
        requests,
        replan_payloads,
        timeline,
    )


def _auto_replan_verification_continuation_requests(
    replan_payloads: list[dict[str, Any]],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
    planning_reason: str,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not planned:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "verification_failed":
            continue
        if not _replan_payload_is_focus_mismatch(payload):
            continue
        if not _replan_focus_recovery_succeeded(payload, timeline):
            continue
        source_index = _replan_source_request_index(planned, payload)
        if source_index < 0:
            continue
        continuation = _replan_ui_continuation_slice(
            planned[source_index + 1 :],
            payload,
            allowed,
            timeline,
            planning_reason=planning_reason,
        )
        continuation = _drop_completed_auto_followup_prefix(
            continuation,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        requests.extend(continuation)
    requests = _dedupe_replan_recovery_requests(requests)
    return _replan_recovery_requests_with_task_context(
        requests,
        replan_payloads,
        timeline,
    )


def _replan_focus_recovery_succeeded(
    payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> bool:
    target_app_name = _replan_focus_recovery_app_name(payload)
    if not target_app_name:
        return False
    request_id = str(payload.get("request_id") or "").strip()
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        if str(event.get("detail") or "").strip() != "desktop.active_window":
            continue
        event_request_id = str(event.get("replan_request_id") or "").strip()
        planning_reason = str(event.get("planning_reason") or "").strip()
        if request_id:
            if event_request_id != request_id:
                continue
        elif planning_reason != "planner_replan_focus_recovery":
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True or result.get("approval_required"):
            continue
        active_app_name = _replan_active_window_app_name(event, result)
        if _replan_app_names_match(target_app_name, active_app_name):
            return True
    return False


def _replan_active_window_app_name(
    event: Mapping[str, Any],
    result: Mapping[str, Any],
) -> str:
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    for key in (
        "app_name",
        "active_app_name",
        "frontmost_app",
        "frontmost_app_name",
        "observed_app_name",
    ):
        value = str(data.get(key) or result.get(key) or event.get(key) or "").strip()
        if value:
            return value
    return ""


def _replan_app_names_match(expected: str, observed: str) -> bool:
    expected_compact = compact_app_name_hint(expected)
    observed_compact = compact_app_name_hint(observed)
    if not expected_compact or not observed_compact:
        return False
    return (
        expected_compact == observed_compact
        or expected_compact in observed_compact
        or observed_compact in expected_compact
    )


def _replan_ui_observed_action_retry_succeeded(
    payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> bool:
    source_tool = _replan_source_tool_name(payload)
    if not source_tool:
        return False
    target = _replan_ui_observed_action_target(payload)
    target_action = str(target.get("target_action") or "").strip()
    request_id = str(payload.get("request_id") or "").strip()
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True:
            continue
        tool_name = str(event.get("detail") or "").strip()
        event_request_id = str(event.get("replan_request_id") or "").strip()
        planning_reason = str(event.get("planning_reason") or "").strip()
        if (
            (tool_name == source_tool or _followup_plan_tools_match(tool_name, source_tool))
            and (
            (request_id and event_request_id == request_id)
            or planning_reason == "planner_replan_ui_observed_action"
            )
        ):
            return True
        if not (request_id and event_request_id == request_id):
            continue
        if not _replan_ui_observed_action_fallback_succeeded(
            source_tool,
            tool_name,
            target_action,
            event,
        ):
            continue
        return True
    return False


def _replan_ui_observed_action_fallback_succeeded(
    source_tool: str,
    tool_name: str,
    target_action: str,
    event: Mapping[str, Any],
) -> bool:
    if target_action == "click":
        if source_tool not in {
            "app.open_and_click_ui_element",
            "app.focus_and_click_ui_element",
            "desktop.click_ui_element",
        }:
            return False
        if tool_name not in {"desktop.safe_click", "desktop.click"}:
            return False
    elif target_action == "type_text":
        if source_tool not in {
            "app.open_and_type_into_ui_element",
            "app.focus_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        }:
            return False
        if tool_name not in {"desktop.safe_type_text", "desktop.type_text", "desktop.type"}:
            return False
    else:
        return False
    action_target = (
        event.get("action_target") if isinstance(event.get("action_target"), Mapping) else {}
    )
    return str(action_target.get("action") or "").strip() == target_action


def _replan_ui_continuation_slice(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    payload: Mapping[str, Any],
    allowed: set[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    requests: list[dict[str, Any]] = []
    for index, request in enumerate(planned):
        tool_name = str(request.get("tool") or "").strip()
        if not tool_name:
            continue
        if tool_name not in allowed:
            break
        if tool_name not in _DIRECT_DAILY_DESKTOP_TOOLS:
            break
        item = dict(request)
        item.pop("continue_to_model", None)
        item["source"] = str(item.get("source") or "runtime_planner")
        item["planning_reason"] = planning_reason
        trigger = str(payload.get("trigger") or "").strip()
        request_id = str(payload.get("request_id") or "").strip()
        if trigger:
            item["replan_trigger"] = trigger
        if request_id:
            item["replan_request_id"] = request_id
        _attach_replan_payload_trace_metadata(item, payload)
        item = _replan_continuation_request_with_resolved_app(
            item,
            payload,
            timeline,
        )
        _attach_replan_active_window_verification_target(
            item,
            _replan_recovery_target(payload),
        )
        requests.append(item)
        if tool_name == "desktop.search_submit" and _replan_ui_next_result_click_request(
            planned[index + 1 :]
        ):
            observation = _replan_ui_search_result_observation_request(
                _replan_ui_next_result_click_request(planned[index + 1 :]),
                payload,
                allowed,
                planning_reason=planning_reason,
            )
            if observation:
                requests.append(observation)
            break
        if len(requests) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return requests


def _replan_continuation_request_with_resolved_app(
    request: dict[str, Any],
    payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    tool_name = str(request.get("tool") or "").strip()
    request_input = (
        dict(request.get("input"))
        if isinstance(request.get("input"), Mapping)
        else {}
    )
    raw_app_name = str(
        request_input.get("app_name") or request.get("target_app_name") or ""
    ).strip()
    if raw_app_name and not _runtime_planner_placeholder_app_name(raw_app_name):
        return request

    recovery_target = _replan_recovery_target(payload)
    app_query = str(
        request_input.get("query")
        or request_input.get("app_query")
        or request.get("target_app_query")
        or recovery_target.get("target_app_query")
        or ""
    ).strip()
    discovered = _discovered_app_name_for_query(timeline, app_query) if app_query else ""
    if not discovered:
        discovered = _latest_replan_context_app_name(
            timeline,
            request_id=str(payload.get("request_id") or "").strip(),
        )
    if not discovered or _runtime_planner_placeholder_app_name(discovered):
        return request

    resolved = dict(request)
    if tool_name.startswith("app.") or raw_app_name:
        request_input["app_name"] = discovered
    for key in ("selection_source", "app_selection_source", "query", "app_query"):
        request_input.pop(key, None)
    resolved["input"] = request_input
    resolved["target_app_name"] = discovered
    if app_query:
        resolved["target_app_query"] = app_query
    for key in ("followup_target", "action_target"):
        nested = resolved.get(key) if isinstance(resolved.get(key), Mapping) else {}
        if not nested:
            continue
        item = dict(nested)
        nested_app_name = str(item.get("app_name") or item.get("target_app_name") or "").strip()
        if not nested_app_name or _runtime_planner_placeholder_app_name(nested_app_name):
            item["app_name"] = discovered
            if "target_app_name" in item:
                item["target_app_name"] = discovered
        if app_query:
            if key == "followup_target" or "app_query" in item:
                item["app_query"] = app_query
            if "target_app_query" in item:
                item["target_app_query"] = app_query
        resolved[key] = item
    if app_query and (tool_name.startswith("app.") or raw_app_name):
        resolved = _with_discovered_app_resolution(resolved, app_query, discovered)
    return resolved


def _auto_replan_ui_search_observed_result_requests(
    replan_payloads: list[dict[str, Any]],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if (
        not allowed
        or not _latest_desktop_observation_succeeded(timeline)
        or not _latest_desktop_observation_follows_search_submission(timeline)
    ):
        return []
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not planned:
        return []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if str(payload.get("trigger") or "").strip() != "tool_failure":
            continue
        if not _replan_ui_observed_action_target(payload):
            continue
        if not _replan_ui_observed_action_retry_succeeded(payload, timeline):
            continue
        source_index = _replan_source_request_index(planned, payload)
        if source_index < 0:
            continue
        result_request = _replan_ui_next_result_click_request(planned[source_index + 1 :])
        if not result_request:
            continue
        policy_payload = _trusted_replan_planned_continuation_policy_payload(
            result_request,
            payload,
            timeline,
        )
        if not policy_payload:
            continue
        observed_target = _observed_action_target_from_replan_ui_result_request(
            result_request,
            payload,
        )
        if not observed_target:
            continue
        observed_target = _replan_ui_observed_action_target_with_timeline_context(
            observed_target,
            payload,
            timeline,
        )
        observation_source = _latest_desktop_observation_tool(timeline)
        if observation_source:
            observed_target["observation_source"] = observation_source
        requests = _auto_desktop_observed_action_followup_requests(
            {"followup_target": observed_target},
            allowed,
            timeline,
            planning_reason=planning_reason,
        )
        if not requests:
            continue
        requests = _annotate_replan_ui_search_observed_result_requests(
            requests,
            payload,
            result_request,
            policy_payload=policy_payload,
        )
        return _replan_recovery_requests_with_task_context(
            requests,
            [dict(payload)],
            timeline,
        )
    return []


def _replan_ui_next_result_click_request(
    planned_tool_requests: Iterable[Mapping[str, Any]],
) -> dict[str, Any]:
    for request in planned_tool_requests:
        if not isinstance(request, Mapping):
            continue
        deferred_click = _replan_ui_deferred_result_click_request(request)
        if deferred_click:
            return deferred_click
        tool_name = str(request.get("tool") or "").strip()
        if tool_name not in {
            "app.open_and_click_ui_element",
            "app.focus_and_click_ui_element",
            "desktop.click_ui_element",
        }:
            if tool_name in _DIRECT_DAILY_DESKTOP_TOOLS:
                continue
            return {}
        payload = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        target = str(payload.get("target") or "").strip()
        if not target:
            return {}
        if _observed_desktop_target_ordinal(target) or "result" in target.casefold():
            return dict(request)
        return {}
    return {}


def _replan_ui_deferred_result_click_request(request: Mapping[str, Any]) -> dict[str, Any]:
    tool_name = str(request.get("deferred_tool") or "").strip()
    if tool_name not in {
        "app.open_and_click_ui_element",
        "app.focus_and_click_ui_element",
        "desktop.click_ui_element",
    }:
        return {}
    payload = (
        request.get("deferred_input")
        if isinstance(request.get("deferred_input"), Mapping)
        else {}
    )
    target = str(payload.get("target") or "").strip()
    if not target:
        return {}
    if not (_observed_desktop_target_ordinal(target) or "result" in target.casefold()):
        return {}
    item = {
        key: value
        for key, value in request.items()
        if key
        not in {
            "tool",
            "input",
            "deferred_tool",
            "deferred_input",
            "deferred_context",
            "continue_to_model",
        }
    }
    item["tool"] = tool_name
    item["input"] = dict(payload)
    deferred_context = (
        request.get("deferred_context")
        if isinstance(request.get("deferred_context"), Mapping)
        else {}
    )
    for key, value in deferred_context.items():
        if value not in (None, "", [], {}):
            item[key] = value
    return item


def _replan_ui_search_result_observation_request(
    result_request: Mapping[str, Any],
    payload: Mapping[str, Any],
    allowed: set[str],
    *,
    planning_reason: str,
) -> dict[str, Any]:
    if not result_request:
        return {}
    result_input = (
        result_request.get("input")
        if isinstance(result_request.get("input"), Mapping)
        else {}
    )
    app_name = str(result_input.get("app_name") or "").strip()
    if not app_name:
        app_name = str(_replan_recovery_target(payload).get("target_app_name") or "").strip()
    role_filter = str(result_input.get("role_filter") or "").strip()
    limit = _clean_model_followup_int(result_input.get("limit"), default=80)
    tool_name = _first_allowed_tool(
        ("desktop.ui_elements", "desktop.read_ui", "desktop.inspect_app", "screen.capture"),
        allowed,
    )
    if not tool_name:
        return {}
    if tool_name == "desktop.inspect_app":
        request_input: dict[str, Any] = {
            "app_name": app_name,
            "open_if_needed": False,
            "focus": True,
            "limit": limit,
        }
        if role_filter:
            request_input["role_filter"] = role_filter
    elif tool_name in {"desktop.ui_elements", "desktop.read_ui"}:
        request_input = {"limit": limit}
        if app_name:
            request_input["app_name"] = app_name
        if role_filter:
            request_input["role_filter"] = role_filter
    else:
        request_input = {"reason": "inspect search results after submit"}
    request = _request_like(
        tool_name,
        request_input,
        source="runtime_planner",
        planning_reason=planning_reason,
    )
    request_id = str(payload.get("request_id") or "").strip()
    if request_id:
        request["replan_request_id"] = request_id
    trigger = str(payload.get("trigger") or "").strip()
    if trigger:
        request["replan_trigger"] = trigger
    return request


def _observed_action_target_from_replan_ui_result_request(
    result_request: Mapping[str, Any],
    payload: Mapping[str, Any],
) -> dict[str, Any]:
    result_input = (
        result_request.get("input")
        if isinstance(result_request.get("input"), Mapping)
        else {}
    )
    target_label = str(result_input.get("target") or "first result").strip()
    if not target_label:
        return {}
    app_name = str(result_input.get("app_name") or "").strip()
    if not app_name:
        app_name = str(_replan_recovery_target(payload).get("target_app_name") or "").strip()
    observed_target = {
        "kind": "desktop_observed_action",
        "target_action": "click",
        "target": target_label,
        "role_filter": str(result_input.get("role_filter") or "").strip(),
        "click_count": _clean_model_followup_int(
            result_input.get("click_count"),
            default=1,
        ),
        "limit": _clean_model_followup_int(result_input.get("limit"), default=80),
    }
    if app_name:
        observed_target["app_name"] = app_name
        observed_target["target_app_name"] = app_name
    app_query = str(result_input.get("query") or result_input.get("app_query") or "").strip()
    if app_query:
        observed_target["app_query"] = app_query
        observed_target["target_app_query"] = app_query
    return {
        key: value
        for key, value in observed_target.items()
        if value not in ("", None, [], {})
    }


def _annotate_replan_ui_search_observed_result_requests(
    requests: list[dict[str, Any]],
    payload: Mapping[str, Any],
    result_request: Mapping[str, Any],
    *,
    policy_payload: Mapping[str, Any] | None = None,
) -> list[dict[str, Any]]:
    action_policy = _trusted_replan_observed_result_action_policy(
        result_request,
        policy_payload or payload,
    )
    if not action_policy:
        return []
    action_tool = str(result_request.get("tool") or "").strip()
    action_step_id = str(result_request.get("step_id") or "").strip()
    action_request_id = str(result_request.get("request_id") or "").strip()
    deferred_verifier_context = (
        result_request.get("deferred_verifier_context")
        if isinstance(result_request.get("deferred_verifier_context"), Mapping)
        else {}
    )
    annotated: list[dict[str, Any]] = []
    for request in requests:
        item = dict(request)
        for key in (
            "decision_id",
            "plan_id",
            "tool_plan_id",
            "intent_kind",
            "step_id",
            "planner_step_id",
            "capability_id",
            "core_id",
            "workspace_id",
            "task_id",
            "task_todo",
            "task_checkpoints",
            "task_workspace_items",
        ):
            value = result_request.get(key)
            if value not in (None, "", [], {}):
                item[key] = value
        if str(item.get("tool") or "").strip() == action_tool:
            item.update(action_policy)
        else:
            verifier_tool = str(item.get("tool") or "").strip()
            if verifier_tool not in READ_ONLY_OBSERVATION_TOOLS:
                return []
            for key in (
                "step_id",
                "planner_step_id",
                "capability_id",
                "task_todo",
                "task_checkpoints",
                "task_workspace_items",
                "verification_targets",
                "task_verification_targets",
                "verification_predicate_kind",
            ):
                value = deferred_verifier_context.get(key)
                if value not in (None, "", [], {}):
                    item[key] = deepcopy(value)
            if item.get("step_id"):
                item["planner_step_id"] = str(
                    deferred_verifier_context.get("planner_step_id")
                    or item.get("step_id")
                    or ""
                ).strip()
            verifier_target = deferred_verifier_context.get("action_target")
            if isinstance(verifier_target, Mapping):
                item["action_target"] = dict(verifier_target)
            item["source"] = str(
                deferred_verifier_context.get("source")
                or "runtime_verification"
            ).strip()
            item["approval_required"] = False
            item["risk_level"] = "low"
            item["runtime_stage"] = "verify"
            item["runtime_role"] = "verify_result"
            item["depends_on"] = [action_step_id]
            item["verification_target_step_ids"] = [action_step_id]
            item["source_request_id"] = action_request_id
        _attach_replan_payload_trace_metadata(item, payload)
        if str(item.get("tool") or "").strip() == action_tool:
            # The replan payload describes the earlier failed step.  Keep its
            # trace identity, but do not let its runtime role overwrite the
            # immutable next action's task policy.
            item.update(action_policy)
        else:
            item["approval_required"] = False
            item["risk_level"] = "low"
            item["runtime_stage"] = "verify"
            item["runtime_role"] = "verify_result"
            item["requires_observation"] = True
            item["requires_post_action_verification"] = False
        annotated.append(item)
    return annotated


def _trusted_replan_planned_continuation_policy_payload(
    result_request: Mapping[str, Any],
    payload: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    """Authorize only the next immutable planner step after recovery succeeded."""

    source_step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    result_step_id = str(
        result_request.get("step_id")
        or result_request.get("planner_step_id")
        or ""
    ).strip()
    if source_step_id == result_step_id:
        return dict(payload)
    plan_id = str(result_request.get("plan_id") or "").strip()
    decision_id = str(result_request.get("decision_id") or "").strip()
    request_id = str(result_request.get("request_id") or "").strip()
    dependencies = _string_list(result_request.get("depends_on"))
    if (
        str(payload.get("trigger") or "").strip() != "tool_failure"
        or str(result_request.get("source") or "").strip() != "runtime_planner"
        or not all((source_step_id, result_step_id, plan_id, decision_id, request_id))
        or str(payload.get("plan_id") or "").strip() != plan_id
        or str(payload.get("decision_id") or "").strip() != decision_id
        or not request_id.startswith(f"{plan_id}:request:")
        or not dependencies
        or not all(
            _timeline_has_successful_replan_dependency(
                timeline,
                plan_id=plan_id,
                step_id=dependency,
            )
            for dependency in dependencies
        )
    ):
        return {}
    source_tool = str(
        payload.get("source_tool_name") or payload.get("tool_name") or ""
    ).strip()
    failed_source_exists = any(
        str(event.get("event") or event.get("event_type") or "").strip()
        == "agent.tool.call"
        and str(event.get("plan_id") or "").strip() == plan_id
        and str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        == source_step_id
        and str(event.get("detail") or event.get("tool") or "").strip()
        == source_tool
        and isinstance(event.get("result"), Mapping)
        and event["result"].get("ok") is False
        for event in timeline
    )
    if not source_tool or not failed_source_exists:
        return {}
    return {
        **dict(payload),
        "source_step_id": result_step_id,
        "planner_step_id": result_step_id,
    }


def _trusted_replan_observed_result_action_policy(
    result_request: Mapping[str, Any],
    payload: Mapping[str, Any],
) -> dict[str, Any]:
    if str(result_request.get("source") or "").strip() != "runtime_planner":
        return {}
    request_id = str(result_request.get("request_id") or "").strip()
    decision_id = str(result_request.get("decision_id") or "").strip()
    plan_id = str(result_request.get("plan_id") or "").strip()
    step_id = str(
        result_request.get("step_id")
        or result_request.get("planner_step_id")
        or ""
    ).strip()
    run_id = str(payload.get("run_id") or "").strip()
    if not all((request_id, decision_id, plan_id, step_id, run_id)):
        return {}
    if str(payload.get("decision_id") or "").strip() != decision_id:
        return {}
    if str(payload.get("plan_id") or "").strip() != plan_id:
        return {}
    if str(
        payload.get("source_step_id")
        or payload.get("planner_step_id")
        or ""
    ).strip() != step_id:
        return {}

    task_todo = (
        result_request.get("task_todo")
        if isinstance(result_request.get("task_todo"), Mapping)
        else {}
    )
    task_metadata = (
        task_todo.get("metadata")
        if isinstance(task_todo.get("metadata"), Mapping)
        else {}
    )
    tool_name = str(result_request.get("tool") or "").strip()
    if not tool_name or str(task_todo.get("tool_name") or "").strip() != tool_name:
        return {}
    if str(task_todo.get("step_id") or "").strip() != step_id:
        return {}

    approval_required = result_request.get("approval_required")
    todo_approval_required = task_todo.get("approval_required")
    if not isinstance(approval_required, bool) or not isinstance(
        todo_approval_required,
        bool,
    ):
        return {}
    if approval_required is not todo_approval_required:
        return {}
    risk_level = str(result_request.get("risk_level") or "").strip().lower()
    todo_risk_level = str(task_metadata.get("risk_level") or "").strip().lower()
    if not risk_level or risk_level != todo_risk_level:
        return {}

    depends_on = _string_list(result_request.get("depends_on"))
    todo_depends_on = _string_list(task_todo.get("depends_on"))
    if not depends_on or depends_on != todo_depends_on:
        return {}
    action_target = (
        result_request.get("action_target")
        if isinstance(result_request.get("action_target"), Mapping)
        else {}
    )
    todo_action_target = (
        task_metadata.get("action_target")
        if isinstance(task_metadata.get("action_target"), Mapping)
        else {}
    )
    if not todo_action_target:
        todo_action_target = _request_contract_action_target(result_request)
    if not action_target or dict(action_target) != dict(todo_action_target):
        return {}
    request_input = (
        result_request.get("input")
        if isinstance(result_request.get("input"), Mapping)
        else {}
    )
    if str(request_input.get("target") or "").strip() != str(
        action_target.get("target") or ""
    ).strip():
        return {}
    return {
        "approval_required": approval_required,
        "risk_level": risk_level,
        "depends_on": depends_on,
        "action_target": dict(action_target),
        "runtime_stage": str(
            result_request.get("runtime_stage") or "operate"
        ).strip(),
        "runtime_role": str(
            result_request.get("runtime_role") or "operate_ui"
        ).strip(),
        "requires_post_action_verification": True,
        "policy_source": "runtime_planner_task_todo",
    }


def _auto_replan_focus_recovery_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    focus_tool = _replan_focus_recovery_tool(allowed)
    if not focus_tool:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if not _replan_payload_is_focus_mismatch(payload):
            continue
        app_name = _replan_focus_recovery_app_name(payload)
        if not app_name:
            continue
        request_id = str(payload.get("request_id") or "").strip()
        target = {
            **_replan_recovery_target(payload),
            "target_app_name": app_name,
        }
        for tool_name, request_input in (
            (focus_tool, {"app_name": app_name}),
            ("desktop.active_window", {}),
        ):
            if tool_name not in allowed:
                continue
            request = _request_like(
                tool_name,
                request_input,
                source="runtime_planner",
                planning_reason="planner_replan_focus_recovery",
            )
            if request_id:
                request["replan_request_id"] = request_id
            request["replan_trigger"] = "verification_failed"
            for key, value in target.items():
                if value:
                    request[key] = value
            _attach_replan_payload_trace_metadata(request, payload)
            _attach_replan_active_window_verification_target(request, target)
            requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _replan_focus_recovery_tool(allowed: set[str]) -> str:
    for tool_name in (
        "app.focus",
        "desktop.focus_app",
        "app.open",
        "desktop.open_app",
    ):
        if tool_name in allowed:
            return tool_name
    return ""


def _replan_payload_is_focus_mismatch(payload: Mapping[str, Any]) -> bool:
    if str(payload.get("trigger") or "").strip() != "verification_failed":
        return False
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    tokens = [
        str(payload.get("failure_detail") or ""),
        str(payload.get("condition") or ""),
        str(payload.get("reason") or ""),
        *_string_list(payload.get("blocking_conditions")),
        *_string_list(metadata.get("blocking_conditions")),
    ]
    return any("foreground_focus_unverified" in token for token in tokens)


def _replan_focus_recovery_app_name(payload: Mapping[str, Any]) -> str:
    target = _replan_recovery_target(payload)
    app_name = str(target.get("target_app_name") or "").strip()
    if app_name and not _runtime_planner_placeholder_app_name(app_name):
        return app_name
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    result = payload.get("result") if isinstance(payload.get("result"), Mapping) else {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    app_name = _first_replan_recovery_text(
        (
            "expected_app_name",
            "target_app_name",
            "resolved_app_name",
            "discovered_app_name",
            "requested_app_name",
            "app_name",
        ),
        metadata,
        data,
        result,
        payload,
    )
    if app_name and not _runtime_planner_placeholder_app_name(app_name):
        return app_name
    return ""


def _replan_recovery_target(payload: Mapping[str, Any]) -> dict[str, str]:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    input_preview = (
        payload.get("input_preview")
        if isinstance(payload.get("input_preview"), Mapping)
        else {}
    )
    app_name = _first_replan_recovery_text(
        (
            "target_app_name",
            "app_name",
            "expected_app_name",
            "resolved_app_name",
            "discovered_app_name",
            "requested_app_name",
        ),
        metadata,
        input_preview,
        payload,
    )
    app_query = _first_replan_recovery_text(
        ("target_app_query", "app_query", "query"),
        metadata,
        input_preview,
        payload,
    )
    search_text = _first_replan_recovery_text(
        ("target_search_text", "search_text", "text", "value"),
        metadata,
        input_preview,
        payload,
    )
    role_filter = _first_replan_recovery_text(
        ("ui_role_filter", "role_filter", "role"),
        metadata,
        input_preview,
        payload,
    )
    return {
        key: value
        for key, value in {
            "target_app_name": app_name,
            "target_app_query": app_query,
            "target_search_text": search_text,
            "ui_role_filter": role_filter,
        }.items()
        if value
    }


def _verification_recovery_tool_input(
    tool_name: str,
    target: Mapping[str, str],
) -> dict[str, Any] | None:
    clean_tool = str(tool_name or "").strip()
    app_name = str(target.get("target_app_name") or "").strip()
    search_text = str(target.get("target_search_text") or "").strip()
    role_filter = str(target.get("ui_role_filter") or "").strip()
    if clean_tool == "desktop.inspect_app":
        if not app_name:
            return None
        payload: dict[str, Any] = {
            "app_name": app_name,
            "open_if_needed": True,
            "focus": True,
            "limit": 80,
        }
        if role_filter:
            payload["role_filter"] = role_filter
        return payload
    if clean_tool in {"desktop.list_windows", "desktop.windows"}:
        return {"app_name": app_name} if app_name else {}
    if clean_tool in {"desktop.read_ui", "desktop.ui_elements"}:
        payload = {"app_name": app_name, "limit": 80} if app_name else {"limit": 80}
        if role_filter:
            payload["role_filter"] = role_filter
        return payload
    if clean_tool == "screen.capture":
        reason = "runtime verification recovery"
        if app_name and search_text:
            reason = f"verify {app_name} after action involving {search_text}"
        elif app_name:
            reason = f"verify {app_name} after desktop action"
        elif search_text:
            reason = f"verify desktop after action involving {search_text}"
        return {"reason": reason}
    return {}


def _attach_replan_active_window_verification_target(
    request: dict[str, Any],
    target: Mapping[str, Any],
) -> None:
    if str(request.get("tool") or "").strip() != "desktop.active_window":
        return
    if isinstance(request.get("verification_target"), Mapping):
        return
    app_name = str(
        target.get("target_app_name")
        or target.get("app_name")
        or request.get("target_app_name")
        or ""
    ).strip()
    if not app_name:
        return
    request["verification_target"] = {"app_name": app_name}


def _first_replan_recovery_text(
    keys: tuple[str, ...],
    *sources: Mapping[str, Any],
) -> str:
    for source in sources:
        if not isinstance(source, Mapping):
            continue
        for key in keys:
            value = str(source.get(key) or "").strip()
            if value:
                return value
    return ""


def _attach_replan_payload_trace_metadata(
    request: dict[str, Any],
    payload: Mapping[str, Any],
) -> None:
    for key in (
        "decision_id",
        "plan_id",
        "core_id",
        "workspace_id",
        "task_id",
        "run_id",
        "run_group_id",
        "group_run_id",
        "group_id",
        "workflow_id",
        "workflow_run_id",
        "workflow_node_id",
        "workflow_node_label",
    ):
        value = str(request.get(key) or payload.get(key) or "").strip()
        if value:
            request[key] = value
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    intent_kind = str(
        request.get("intent_kind")
        or payload.get("intent_kind")
        or metadata.get("original_intent_kind")
        or ""
    ).strip()
    if intent_kind:
        request["intent_kind"] = intent_kind
    request_id = str(payload.get("request_id") or "").strip()
    if request_id and not str(request.get("replan_request_id") or "").strip():
        request["replan_request_id"] = request_id
    trigger = str(payload.get("trigger") or "").strip()
    if trigger and not str(request.get("replan_trigger") or "").strip():
        request["replan_trigger"] = trigger
    step_id = str(
        request.get("step_id")
        or payload.get("source_step_id")
        or payload.get("planner_step_id")
        or ""
    ).strip()
    if step_id:
        request["step_id"] = step_id
        request.setdefault("planner_step_id", step_id)
    capability_id = str(
        request.get("capability_id")
        or payload.get("target_capability_id")
        or payload.get("capability_id")
        or ""
    ).strip()
    if capability_id:
        request["capability_id"] = capability_id
    request.update(_runtime_trace_metadata_from_replan_payload(payload))
    _attach_replan_payload_recovery_metadata(request, payload)


def _attach_replan_payload_recovery_metadata(
    request: dict[str, Any],
    payload: Mapping[str, Any],
) -> None:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    for key in ("action_target", "observation_evidence", "observation_retry"):
        if isinstance(request.get(key), Mapping) and request.get(key):
            continue
        value = payload.get(key)
        if not isinstance(value, Mapping):
            value = metadata.get(key)
        if isinstance(value, Mapping) and value:
            request[key] = dict(value)
    if _mapping_list(request.get("verification_targets")):
        return
    verification_targets = _mapping_list(payload.get("verification_targets"))
    if not verification_targets:
        verification_targets = _mapping_list(metadata.get("verification_targets"))
    if verification_targets:
        request["verification_targets"] = [
            dict(target) for target in verification_targets
        ]


_RUNTIME_TRACE_TEXT_KEYS = (
    "runtime_doctrine",
    "runtime_stage",
    "runtime_role",
)

_RUNTIME_TRACE_BOOL_KEYS = (
    "requires_observation",
    "requires_post_action_verification",
)

_RUNTIME_TRACE_LIST_KEYS = (
    "replan_triggers",
    "replan_signal_ids",
)


def _runtime_trace_metadata_from_replan_payload(payload: Mapping[str, Any]) -> dict[str, Any]:
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    trace = {
        **_runtime_trace_metadata_from_mapping(metadata),
        **_runtime_trace_metadata_from_mapping(payload),
    }
    trigger = str(payload.get("trigger") or "").strip()
    if trigger:
        triggers = _string_list(trace.get("replan_triggers"))
        if trigger not in triggers:
            triggers.append(trigger)
        trace["replan_triggers"] = triggers
    return trace


def _runtime_trace_metadata_from_mapping(value: Mapping[str, Any]) -> dict[str, Any]:
    trace: dict[str, Any] = {}
    for key in _RUNTIME_TRACE_TEXT_KEYS:
        item = str(value.get(key) or "").strip()
        if item:
            trace[key] = item
    for key in _RUNTIME_TRACE_BOOL_KEYS:
        item = value.get(key)
        if isinstance(item, bool):
            trace[key] = item
    for key in _RUNTIME_TRACE_LIST_KEYS:
        items = _string_list(value.get(key))
        if items:
            trace[key] = items
    return trace


def _replan_recovery_requests_need_model_followup(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    return any(
        bool(request.get("continue_to_model"))
        for request in requests
        if isinstance(request, Mapping)
    )


def _auto_direct_permission_recovery_requests(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    planned_signatures = _tool_request_signatures(planned_tool_requests)
    requests: list[dict[str, Any]] = []
    seen: set[tuple[str, str]] = set()
    for event in timeline[max(0, int(tool_timeline_start or 0)):]:
        if not isinstance(event, Mapping):
            continue
        if str(event.get("event") or "").strip() not in {"agent.tool.call", "agent.tool.skipped"}:
            continue
        source_tool = str(event.get("detail") or event.get("tool") or "").strip()
        if not source_tool:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if not result or result.get("approval_required"):
            continue
        if result.get("ok") is True and result.get("verification_failed") is not True:
            continue
        input_preview = (
            event.get("input_preview") if isinstance(event.get("input_preview"), Mapping) else {}
        )
        enriched_result = _with_retry_recovery_action(
            source_tool,
            dict(input_preview),
            dict(result),
        )
        if not _has_permission_recovery_signal(enriched_result):
            continue
        user_recovery_targets = set(
            _string_list(enriched_result.get("recovery_requires_user_targets"))
        )
        for action in _recovery_actions(enriched_result):
            tool_name = str(action.get("tool") or "").strip()
            if not tool_name or tool_name not in allowed:
                continue
            permission_target = str(action.get("permission_target") or "").strip()
            if permission_target and permission_target in user_recovery_targets:
                continue
            risk_level = str(action.get("risk_level") or "low").strip().lower()
            if risk_level and risk_level != "low":
                continue
            action_input = (
                dict(action.get("input"))
                if isinstance(action.get("input"), Mapping)
                else {}
            )
            signature = _tool_request_signature(tool_name, action_input)
            if signature in planned_signatures or signature in seen:
                continue
            seen.add(signature)
            request = _request_like(
                tool_name,
                action_input,
                source="runtime_planner",
                planning_reason="planner_direct_permission_recovery_action",
            )
            request["continue_to_model"] = True
            request["permission_recovery"] = True
            request["recovery_source_tool"] = source_tool
            request["recovery_source_event_type"] = str(event.get("event") or "").strip()
            label = str(action.get("label") or "").strip()
            if label:
                request["recovery_action_label"] = label
            if permission_target:
                request["permission_target"] = permission_target
            if risk_level:
                request["risk_level"] = risk_level
            for key in (
                "action_kind",
                "recovery_action_kind",
                "retry_tool",
                "recovery_retry_tool",
                "retry_prompt",
                "recovery_retry_prompt",
            ):
                value = str(action.get(key) or "").strip()
                if value:
                    request[key] = value
            for key in (
                "retry_input",
                "recovery_retry_input",
                "action_target",
                "observation_evidence",
                "observation_retry",
            ):
                value = action.get(key)
                if isinstance(value, Mapping) and value:
                    request[key] = dict(value)
            verification_targets = _mapping_list(action.get("verification_targets"))
            if verification_targets:
                request["verification_targets"] = [
                    dict(target) for target in verification_targets
                ]
            requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _auto_direct_permission_retry_requests(
    recovery_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    requests: list[dict[str, Any]] = []
    seen: set[tuple[str, str]] = set()
    for recovery_request in recovery_requests:
        if not isinstance(recovery_request, Mapping):
            continue
        recovery_tool = str(recovery_request.get("tool") or "").strip()
        if not _direct_permission_recovery_tool_supports_retry(recovery_tool):
            continue
        if not _auto_followup_request_completed(
            recovery_request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        ):
            continue
        retry_tool = str(
            recovery_request.get("recovery_retry_tool")
            or recovery_request.get("retry_tool")
            or ""
        ).strip()
        if (
            not retry_tool
            or retry_tool not in allowed
            or retry_tool not in _DIRECT_DAILY_DESKTOP_TOOLS
        ):
            continue
        retry_input = (
            recovery_request.get("recovery_retry_input")
            if isinstance(recovery_request.get("recovery_retry_input"), Mapping)
            else recovery_request.get("retry_input")
            if isinstance(recovery_request.get("retry_input"), Mapping)
            else {}
        )
        signature = _tool_request_signature(retry_tool, retry_input)
        if signature in seen:
            continue
        seen.add(signature)
        retry_request = _request_like(
            retry_tool,
            dict(retry_input),
            source="runtime_planner",
            planning_reason="planner_direct_permission_recovery_retry",
        )
        retry_request["permission_recovery_retry"] = True
        retry_request["recovery_source_tool"] = str(
            recovery_request.get("recovery_source_tool") or ""
        ).strip()
        retry_request["recovery_action_tool"] = recovery_tool
        label = str(recovery_request.get("recovery_action_label") or "").strip()
        if label:
            retry_request["recovery_action_label"] = label
        permission_target = str(recovery_request.get("permission_target") or "").strip()
        if permission_target:
            retry_request["permission_target"] = permission_target
        risk_level = str(recovery_request.get("risk_level") or "").strip()
        if risk_level:
            retry_request["risk_level"] = risk_level
        retry_prompt = str(
            recovery_request.get("recovery_retry_prompt")
            or recovery_request.get("retry_prompt")
            or ""
        ).strip()
        if retry_prompt:
            retry_request["recovery_retry_prompt"] = retry_prompt
        requests.append(retry_request)
    return _dedupe_replan_recovery_requests(requests)


def _direct_permission_recovery_tool_supports_retry(tool_name: str) -> bool:
    return str(tool_name or "").strip() in {
        "app.open",
        "app.focus",
        "desktop.open_app",
        "desktop.focus_app",
    }


def _auto_direct_permission_retry_completed(
    retry_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    requests = [request for request in retry_requests if isinstance(request, Mapping)]
    return bool(requests) and all(
        _auto_followup_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        for request in requests
    )


def _tool_request_signatures(
    requests: Iterable[Mapping[str, Any]],
) -> set[tuple[str, str]]:
    return {
        _tool_request_signature(
            str(request.get("tool") or "").strip(),
            request.get("input") if isinstance(request.get("input"), Mapping) else {},
        )
        for request in requests
        if isinstance(request, Mapping) and str(request.get("tool") or "").strip()
    }


def _tool_request_signature(
    tool_name: str,
    request_input: Mapping[str, Any],
) -> tuple[str, str]:
    return (
        str(tool_name or "").strip(),
        repr(sorted(dict(request_input).items())),
    )


def _auto_replan_recovery_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    # Planner-authored recovery actions own their deferred verification chain.
    # Keep them ahead of heuristic focus/observation fallbacks so deduplication
    # cannot discard the source action (and its deferred contract) while
    # retaining only a verifier that would run too early.
    requests = _dedupe_replan_recovery_requests(
        [
            *_auto_replan_runtime_recovery_action_requests(
                replan_payloads,
                allowed_tools,
            ),
            *_auto_replan_focus_recovery_requests(
                replan_payloads,
                allowed_tools,
            ),
            *_auto_replan_verification_recovery_requests(
                replan_payloads,
                allowed_tools,
            ),
            *_auto_replan_ui_observation_recovery_requests(
                replan_payloads,
                allowed_tools,
            ),
            *_auto_replan_fallback_recovery_requests(
                replan_payloads,
                allowed_tools,
            ),
        ]
    )
    requests = _prefer_authoritative_nonrepeat_replan_actions(requests)
    return _bind_replan_deferred_request_dependencies(requests)


def _prefer_authoritative_nonrepeat_replan_actions(
    requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """Collapse equivalent idempotent recovery effects from duplicate replans.

    A failed tool can emit both its provider-authored replan event and the
    runtime's normalized replan event.  Both may contain the same safe app-open
    recovery.  Executing both is never useful and can make a verifier depend on
    the normalized action's verifier step instead of the actual source action.
    Prefer the request that carries the strongest planner action identity and
    deferred verification contract.
    """

    result: list[dict[str, Any]] = []
    index_by_effect: dict[tuple[str, str, str], int] = {}
    for request in requests:
        tool_name = str(request.get("tool") or "").strip()
        if tool_name not in _RUNTIME_REPLAN_NO_REPEAT_AFTER_SUCCESS_TOOLS:
            result.append(request)
            continue
        request_input = (
            request.get("input")
            if isinstance(request.get("input"), Mapping)
            else {}
        )
        effect_key = (
            str(request.get("plan_id") or "").strip(),
            *_tool_request_signature(tool_name, request_input),
        )
        existing_index = index_by_effect.get(effect_key)
        if existing_index is None:
            index_by_effect[effect_key] = len(result)
            result.append(request)
            continue
        existing = result[existing_index]
        if _replan_recovery_action_authority_score(
            request
        ) > _replan_recovery_action_authority_score(existing):
            result[existing_index] = request
    return result


def _replan_recovery_action_authority_score(request: Mapping[str, Any]) -> int:
    return sum(
        (
            8 if str(request.get("replan_recovery_action_id") or "").strip() else 0,
            4 if str(request.get("action_id") or "").strip() else 0,
            2 if materialized_deferred_items(request) else 0,
            1 if str(request.get("goal_contract_id") or "").strip() else 0,
        )
    )


def _auto_replan_recovery_requests_with_task_context(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int = 0,
) -> list[dict[str, Any]]:
    allowed_tool_list = list(allowed_tools)
    requests = _auto_replan_recovery_requests(replan_payloads, allowed_tool_list)
    requests = _drop_completed_nonrepeat_replan_recovery_prefix(
        requests,
        allowed_tool_list,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    requests = _replan_recovery_requests_with_task_context(
        requests,
        replan_payloads,
        timeline,
    )
    return _topologically_order_replan_recovery_requests(requests, timeline)


def _bind_replan_deferred_request_dependencies(
    requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """Bind an already-materialized verifier to its recovery source action."""

    bound = [dict(request) for request in requests]
    for source_index, source in enumerate(bound):
        source_step_id = str(
            source.get("step_id") or source.get("planner_step_id") or ""
        ).strip()
        if not source_step_id:
            continue
        source_plan_id = str(source.get("plan_id") or "").strip()
        for deferred in materialized_deferred_items(source):
            if not isinstance(deferred, Mapping):
                continue
            match_index = _matching_replan_deferred_request_index(
                bound,
                deferred,
                after_index=source_index,
                plan_id=source_plan_id,
            )
            if match_index is None:
                continue
            verifier = dict(bound[match_index])
            for key in (
                "step_id",
                "planner_step_id",
                "capability_id",
                "target_capability_id",
                "runtime_stage",
                "runtime_role",
                "decision_id",
                "run_id",
                "replan_request_id",
                "replan_trigger",
                "replan_triggers",
                "action_id",
                "replan_recovery_action_id",
                "goal_contract_id",
                "goal_criterion_id",
                "action_target",
                "verification_target",
                "verification_targets",
            ):
                value = deferred.get(key)
                if value not in (None, "", [], {}):
                    verifier[key] = deepcopy(value)
            deferred_reason = str(deferred.get("planning_reason") or "").strip()
            if deferred_reason:
                verifier["planning_reason"] = deferred_reason
            # A materialized safe verifier is the planner-authored terminal
            # continuation for this recovery action.  A heuristic observation
            # candidate may have carried a stale model-followup flag before it
            # was bound; retaining it would call the model after exact success.
            verifier.pop("continue_to_model", None)
            dependencies = _string_list(verifier.get("depends_on"))
            if source_step_id not in dependencies:
                dependencies.append(source_step_id)
            verifier["depends_on"] = dependencies
            verifier["source_step_id"] = source_step_id
            if source_plan_id:
                verifier["plan_id"] = source_plan_id
            verifier.setdefault(
                "planning_reason",
                "planner_replan_deferred_continuation",
            )
            bound[match_index] = verifier
    return bound


def _matching_replan_deferred_request_index(
    requests: list[dict[str, Any]],
    deferred: Mapping[str, Any],
    *,
    after_index: int,
    plan_id: str,
) -> int | None:
    deferred_tool = str(
        deferred.get("tool") or deferred.get("tool_name") or ""
    ).strip()
    deferred_input = (
        deferred.get("input") if isinstance(deferred.get("input"), Mapping) else {}
    )
    for index in range(after_index + 1, len(requests)):
        candidate = requests[index]
        if str(candidate.get("tool") or "").strip() != deferred_tool:
            continue
        candidate_plan_id = str(candidate.get("plan_id") or "").strip()
        if plan_id and candidate_plan_id and candidate_plan_id != plan_id:
            continue
        candidate_input = (
            candidate.get("input")
            if isinstance(candidate.get("input"), Mapping)
            else {}
        )
        if _auto_followup_input_matches_event(deferred_input, candidate_input):
            return index
    return None


def _topologically_order_replan_recovery_requests(
    requests: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """Order same-plan recovery steps and reject ambiguous dependency graphs."""

    if not requests:
        return requests
    step_nodes: dict[tuple[str, str], list[int]] = {}
    steps_by_id: dict[str, set[str]] = {}
    for index, request in enumerate(requests):
        step_id = str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        if not step_id:
            continue
        plan_id = str(request.get("plan_id") or "").strip()
        step_nodes.setdefault((plan_id, step_id), []).append(index)
        steps_by_id.setdefault(step_id, set()).add(plan_id)

    edges: dict[int, set[int]] = {index: set() for index in range(len(requests))}
    indegree = [0] * len(requests)
    for index, request in enumerate(requests):
        plan_id = str(request.get("plan_id") or "").strip()
        for dependency in _string_list(request.get("depends_on")):
            candidates = step_nodes.get((plan_id, dependency), [])
            if not candidates:
                if dependency in steps_by_id:
                    raise ValueError("runtime_replan_recovery_dependency_wrong_plan")
                if _timeline_has_successful_replan_dependency(
                    timeline,
                    plan_id=plan_id,
                    step_id=dependency,
                ):
                    continue
                raise ValueError("runtime_replan_recovery_dependency_missing")
            for source_index in candidates:
                if source_index == index:
                    raise ValueError("runtime_replan_recovery_dependency_cycle")
                if index in edges[source_index]:
                    continue
                edges[source_index].add(index)
                indegree[index] += 1

    ready = [index for index, count in enumerate(indegree) if count == 0]
    ordered_indices: list[int] = []
    while ready:
        current = ready.pop(0)
        ordered_indices.append(current)
        for dependent in sorted(edges[current]):
            indegree[dependent] -= 1
            if indegree[dependent] == 0:
                ready.append(dependent)
    if len(ordered_indices) != len(requests):
        raise ValueError("runtime_replan_recovery_dependency_cycle")
    return [requests[index] for index in ordered_indices]


def _timeline_has_successful_replan_dependency(
    timeline: list[dict[str, Any]],
    *,
    plan_id: str,
    step_id: str,
) -> bool:
    for event in reversed(timeline):
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        event_plan_id = str(event.get("plan_id") or "").strip()
        if event_plan_id != plan_id:
            continue
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if event_step_id != step_id:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        return result.get("ok") is True and not result.get("approval_required")
    return False


def _drop_completed_nonrepeat_replan_recovery_prefix(
    requests: list[dict[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    remaining: list[dict[str, Any]] = []
    for request in requests:
        if (
            str(request.get("tool") or "").strip()
            in _RUNTIME_REPLAN_NO_REPEAT_AFTER_SUCCESS_TOOLS
            and _runtime_replan_source_request_completed(
                request,
                timeline,
                tool_timeline_start=tool_timeline_start,
            )
        ):
            remaining.extend(
                _completed_nonrepeat_replan_deferred_requests(
                    request,
                    allowed_tools,
                )
            )
            continue
        remaining.append(request)
    return _dedupe_replan_recovery_requests(remaining)


def _completed_nonrepeat_replan_deferred_requests(
    source_request: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {
        str(tool or "").strip()
        for tool in allowed_tools
        if str(tool or "").strip()
    }
    requests: list[dict[str, Any]] = []
    for item in materialized_deferred_items(source_request):
        request = safe_deferred_continuation_request(
            item,
            allowed,
            auto_safe_tools=_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS,
        )
        if not request:
            continue
        for key, value in _runtime_replan_deferred_inherited_metadata(
            source_request
        ).items():
            request.setdefault(key, value)
        request.setdefault(
            "source",
            str(source_request.get("source") or "runtime_planner").strip(),
        )
        request.setdefault(
            "planning_reason",
            "planner_replan_deferred_continuation",
        )
        requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _replan_recovery_requests_with_task_context(
    requests: list[dict[str, Any]],
    replan_payloads: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if not requests:
        return []
    task_core = _runtime_replan_task_core_payload(replan_payloads, timeline)
    if not task_core:
        task_core = _runtime_replan_task_core_context_payload(replan_payloads)
    if not task_core:
        return requests
    return [
        _replan_recovery_request_with_task_context(request, task_core)
        for request in requests
    ]


def _auto_replan_discovered_app_continuation_requests(
    replan_payloads: list[dict[str, Any]],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planned = [request for request in planned_tool_requests if isinstance(request, Mapping)]
    if not planned:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        if not _replan_payload_is_app_resolution_failure(payload):
            continue
        source_index = _replan_source_request_index(planned, payload)
        if source_index < 0:
            continue
        source_request = planned[source_index]
        app_query = _replan_discovered_app_query(source_request, payload)
        if not app_query or not _discovered_app_name_for_query(timeline, app_query):
            continue
        requests.extend(
            _replan_discovered_app_continuation_slice(
                planned[source_index:],
                payload,
                allowed,
            )
        )
    requests = _dedupe_replan_recovery_requests(requests)
    return _replan_recovery_requests_with_task_context(
        requests,
        replan_payloads,
        timeline,
    )


def _replan_payload_is_app_resolution_failure(payload: Mapping[str, Any]) -> bool:
    source_tool = str(payload.get("source_tool_name") or payload.get("tool") or "").strip()
    if source_tool not in _DISCOVERED_APP_DIRECT_COMPLETION_TOOLS:
        return False
    failure_detail = str(payload.get("failure_detail") or "").strip()
    if "app_resolution_failed" in failure_detail:
        return True
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    for action in _mapping_list(metadata.get("recovery_actions")):
        if str(action.get("tool") or "").strip() == "desktop.list_apps":
            return True
    return False


def _replan_source_request_index(
    planned_tool_requests: list[Mapping[str, Any]],
    payload: Mapping[str, Any],
) -> int:
    source_step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    source_tool = str(payload.get("source_tool_name") or payload.get("tool") or "").strip()
    for index, request in enumerate(planned_tool_requests):
        tool_name = str(request.get("tool") or "").strip()
        step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        if source_step_id and step_id == source_step_id:
            if not source_tool or tool_name == source_tool:
                return index
        if not source_step_id and source_tool and tool_name == source_tool:
            if _request_uses_discovered_app_resolution(request):
                return index
    return -1


def _replan_discovered_app_query(
    source_request: Mapping[str, Any],
    payload: Mapping[str, Any],
) -> str:
    request_input = (
        source_request.get("input") if isinstance(source_request.get("input"), Mapping) else {}
    )
    query = str(request_input.get("query") or "").strip()
    if query:
        return query
    app_name = str(request_input.get("app_name") or "").strip()
    if app_name and app_name != "<selected app from desktop.list_apps>":
        return app_name
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    for action in _mapping_list(metadata.get("recovery_actions")):
        action_input = action.get("input") if isinstance(action.get("input"), Mapping) else {}
        query = str(action_input.get("query") or "").strip()
        if query:
            return query
    return str(payload.get("target_app_query") or payload.get("target_app_name") or "").strip()


def _replan_discovered_app_continuation_slice(
    planned_tool_requests: Iterable[Mapping[str, Any]],
    payload: Mapping[str, Any],
    allowed: set[str],
) -> list[dict[str, Any]]:
    requests: list[dict[str, Any]] = []
    for request in planned_tool_requests:
        tool_name = str(request.get("tool") or "").strip()
        if not tool_name:
            continue
        if allowed and tool_name not in allowed:
            break
        if tool_name == "desktop.list_apps":
            continue
        if tool_name not in _DIRECT_DAILY_DESKTOP_TOOLS:
            break
        item = dict(request)
        item.pop("continue_to_model", None)
        item["source"] = str(item.get("source") or "runtime_planner")
        item["planning_reason"] = "planner_replan_discovered_app_continuation"
        trigger = str(payload.get("trigger") or "").strip()
        request_id = str(payload.get("request_id") or "").strip()
        if trigger:
            item["replan_trigger"] = trigger
        if request_id:
            item["replan_request_id"] = request_id
        _attach_replan_payload_trace_metadata(item, payload)
        requests.append(item)
        if len(requests) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return requests


def _replan_recovery_request_with_task_context(
    request: dict[str, Any],
    task_core: Mapping[str, Any],
) -> dict[str, Any]:
    step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    if not step_id:
        return request
    enriched = dict(request)
    core_id = str(task_core.get("core_id") or "").strip()
    if core_id:
        enriched.setdefault("core_id", core_id)
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    workspace_id = str(workspace.get("workspace_id") or "").strip()
    if workspace_id:
        enriched.setdefault("workspace_id", workspace_id)
    todo = _task_core_todo_for_step(task_core, step_id)
    if todo and "task_todo" not in enriched and _task_core_todo_matches_request(todo, request):
        enriched["task_todo"] = todo
    checkpoints = _task_core_checkpoints_for_step(task_core, step_id)
    if checkpoints and "task_checkpoints" not in enriched:
        enriched["task_checkpoints"] = checkpoints
    workspace_items = _task_core_workspace_items_for_step(task_core, step_id)
    if workspace_items and "task_workspace_items" not in enriched:
        enriched["task_workspace_items"] = workspace_items
    return enriched


def _task_core_todo_matches_request(
    todo: Mapping[str, Any],
    request: Mapping[str, Any],
) -> bool:
    todo_tool = str(todo.get("tool_name") or todo.get("tool") or "").strip()
    request_tool = str(request.get("tool") or request.get("tool_name") or "").strip()
    return not todo_tool or not request_tool or todo_tool == request_tool


def _task_core_todo_for_step(
    task_core: Mapping[str, Any],
    step_id: str,
) -> dict[str, Any]:
    for todo in _mapping_list(task_core.get("todos")):
        if str(todo.get("step_id") or "").strip() == step_id:
            return dict(todo)
    return {}


def _task_core_checkpoints_for_step(
    task_core: Mapping[str, Any],
    step_id: str,
) -> list[dict[str, Any]]:
    return [
        dict(checkpoint)
        for checkpoint in _mapping_list(task_core.get("checkpoints"))
        if str(checkpoint.get("after_step_id") or "").strip() == step_id
    ]


def _task_core_workspace_items_for_step(
    task_core: Mapping[str, Any],
    step_id: str,
) -> list[dict[str, Any]]:
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    return [
        dict(item)
        for item in _mapping_list(workspace.get("items"))
        if str(item.get("source_step_id") or "").strip() == step_id
    ]


def _tool_request_task_progress_followup_context(
    requests: Iterable[Mapping[str, Any]],
) -> dict[str, Any]:
    normalized = [request for request in requests if isinstance(request, Mapping)]
    if not normalized:
        return {}
    todos: list[dict[str, Any]] = []
    checkpoints: list[dict[str, Any]] = []
    workspace_items: list[dict[str, Any]] = []
    pending_steps: list[dict[str, Any]] = []
    for request in normalized:
        task_todo = (
            request.get("task_todo")
            if isinstance(request.get("task_todo"), Mapping)
            else {}
        )
        if task_todo:
            todos.append(dict(task_todo))
        checkpoints.extend(dict(item) for item in _mapping_list(request.get("task_checkpoints")))
        workspace_items.extend(
            dict(item) for item in _mapping_list(request.get("task_workspace_items"))
        )
        if task_todo:
            pending_steps.append(
                _tool_request_task_progress_pending_step(request, task_todo)
            )
    if not todos and not checkpoints and not workspace_items:
        return {}
    first = normalized[0]
    core_id = _first_request_text(normalized, "core_id")
    workspace_id = _first_request_text(normalized, "workspace_id")
    task_core = {
        "core_id": core_id,
        "workspace": {
            "workspace_id": workspace_id,
            "items": _dedupe_task_core_rows(workspace_items, "item_id"),
        },
        "todos": _dedupe_task_core_rows(todos, "todo_id"),
        "checkpoints": _dedupe_task_core_rows(checkpoints, "checkpoint_id"),
    }
    context = {
        "source": "runtime_planner",
        "decision_id": str(first.get("decision_id") or "").strip(),
        "plan_id": str(first.get("plan_id") or "").strip(),
        "core_id": core_id,
        "workspace_id": workspace_id,
        "task_id": _first_request_text(normalized, "task_id"),
        "task_core": {
            key: value
            for key, value in task_core.items()
            if value not in ("", [], {})
        },
        "pending_plan_steps": [
            step for step in pending_steps if step
        ],
    }
    for key in _RUNTIME_ORCHESTRATION_SCOPE_KEYS:
        value = _first_request_text(normalized, key)
        if value:
            context[key] = value
    return {key: value for key, value in context.items() if value not in ("", [], {})}


def _tool_request_task_progress_pending_step(
    request: Mapping[str, Any],
    task_todo: Mapping[str, Any],
) -> dict[str, Any]:
    step_id = str(
        request.get("step_id")
        or request.get("planner_step_id")
        or task_todo.get("step_id")
        or ""
    ).strip()
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    if not step_id or not tool_name:
        return {}
    payload = {
        "step_id": step_id,
        "tool_name": tool_name,
        "capability_id": str(
            request.get("capability_id") or task_todo.get("capability_id") or ""
        ).strip(),
        "input_preview": (
            dict(request.get("input"))
            if isinstance(request.get("input"), Mapping)
            else {}
        ),
        "task_todo": dict(task_todo),
    }
    for key in (
        "runtime_doctrine",
        "runtime_stage",
        "runtime_role",
        "requires_observation",
        "requires_post_action_verification",
    ):
        value = request.get(key)
        if value not in (None, "", [], {}):
            payload[key] = value
    return {key: value for key, value in payload.items() if value not in ("", [], {})}


def _first_request_text(requests: Iterable[Mapping[str, Any]], key: str) -> str:
    for request in requests:
        value = str(request.get(key) or "").strip()
        if value:
            return value
    return ""


def _dedupe_task_core_rows(
    rows: Iterable[Mapping[str, Any]],
    identity_key: str,
) -> list[dict[str, Any]]:
    result: list[dict[str, Any]] = []
    seen: set[str] = set()
    for row in rows:
        identity = str(row.get(identity_key) or row.get("step_id") or row).strip()
        if identity in seen:
            continue
        seen.add(identity)
        result.append(dict(row))
    return result


def _mapping_list(value: Any) -> list[Mapping[str, Any]]:
    if not isinstance(value, list):
        return []
    return [item for item in value if isinstance(item, Mapping)]


def _auto_replan_fallback_recovery_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        trigger = str(payload.get("trigger") or "").strip()
        if trigger not in {"tool_failure", "tool_unavailable"}:
            continue
        request_id = str(payload.get("request_id") or "").strip()
        step_id = str(payload.get("source_step_id") or payload.get("planner_step_id") or "").strip()
        capability_id = str(payload.get("target_capability_id") or payload.get("capability_id") or "").strip()
        for tool_name in _string_list(payload.get("fallback_tools")):
            if tool_name not in allowed:
                continue
            request_input = _replan_fallback_tool_input(tool_name, payload)
            if request_input is None:
                continue
            request = _request_like(
                tool_name,
                request_input,
                source="runtime_planner",
                planning_reason="planner_replan_fallback_recovery",
            )
            if request_id:
                request["replan_request_id"] = request_id
            request["replan_trigger"] = trigger
            if step_id:
                request["step_id"] = step_id
            if capability_id:
                request["capability_id"] = capability_id
            if _replan_fallback_request_needs_model_followup(tool_name, payload):
                request["continue_to_model"] = True
            _attach_replan_payload_trace_metadata(request, payload)
            requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _auto_replan_runtime_recovery_action_requests(
    replan_payloads: list[dict[str, Any]],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    requests: list[dict[str, Any]] = []
    for payload in replan_payloads:
        if not isinstance(payload, Mapping):
            continue
        metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
        trigger = str(payload.get("trigger") or "").strip()
        if trigger not in {"tool_failure", "tool_unavailable", "verification_failed"}:
            continue
        request_id = str(payload.get("request_id") or "").strip()
        step_id = str(payload.get("source_step_id") or payload.get("planner_step_id") or "").strip()
        capability_id = str(payload.get("target_capability_id") or payload.get("capability_id") or "").strip()
        for action in _mapping_list(metadata.get("recovery_actions")):
            tool_name = str(action.get("tool") or "").strip()
            if not tool_name or tool_name not in allowed:
                continue
            if _runtime_replan_action_auto_start_blocked(action):
                continue
            request_input = (
                dict(action.get("input"))
                if isinstance(action.get("input"), Mapping)
                else {}
            )
            request = _request_like(
                tool_name,
                request_input,
                source="runtime_planner",
                planning_reason=(
                    str(action.get("planning_reason") or "").strip()
                    or "planner_replan_runtime_recovery_action"
                ),
            )
            if request_id:
                request["replan_request_id"] = request_id
            if trigger:
                request["replan_trigger"] = trigger
            if step_id:
                request["step_id"] = step_id
            if capability_id:
                request["capability_id"] = capability_id
            label = str(action.get("label") or "").strip()
            if label:
                request["recovery_action_label"] = label
            request["recovery_action_tool"] = tool_name
            risk_level = str(action.get("risk_level") or "").strip()
            if risk_level:
                request["risk_level"] = risk_level
            permission_target = str(action.get("permission_target") or "").strip()
            if permission_target:
                request["permission_target"] = permission_target
            if bool(action.get("approval_required")):
                request["approval_required"] = True
            desktop_execution_policy = action.get("desktop_execution_policy")
            if isinstance(desktop_execution_policy, Mapping) and desktop_execution_policy:
                request["desktop_execution_policy"] = dict(desktop_execution_policy)
            action_id = str(action.get("action_id") or action.get("id") or "").strip()
            if action_id:
                request["action_id"] = action_id
                request["replan_recovery_action_id"] = action_id
            action_metadata = (
                action.get("metadata") if isinstance(action.get("metadata"), Mapping) else {}
            )
            desktop_loop = action_metadata.get("desktop_loop")
            if not isinstance(desktop_loop, Mapping):
                desktop_loop = metadata.get("desktop_loop")
            if isinstance(desktop_loop, Mapping) and desktop_loop:
                request["desktop_loop"] = dict(desktop_loop)
            for key in ("runtime_stage", "runtime_role"):
                value = str(action_metadata.get(key) or metadata.get(key) or "").strip()
                if value:
                    request[key] = value
            for key in ("action_target", "observation_evidence", "observation_retry"):
                value = action.get(key)
                if not isinstance(value, Mapping):
                    value = metadata.get(key)
                if not isinstance(value, Mapping):
                    value = payload.get(key)
                if isinstance(value, Mapping) and value:
                    request[key] = dict(value)
            verification_targets = _mapping_list(action.get("verification_targets"))
            if not verification_targets:
                verification_targets = _mapping_list(metadata.get("verification_targets"))
            if not verification_targets:
                verification_targets = _mapping_list(payload.get("verification_targets"))
            if verification_targets:
                request["verification_targets"] = [dict(target) for target in verification_targets]
            raw_deferred_items = materialized_deferred_items(action)
            deferred_continuation = _runtime_replan_action_deferred_continuation_requests(
                action,
                payload,
                allowed,
            )
            if raw_deferred_items and len(deferred_continuation) != len(raw_deferred_items):
                continue
            if deferred_continuation:
                request["deferred_continuation"] = deferred_continuation
            if _runtime_replan_action_needs_model_followup(
                action,
                deferred_continuation=deferred_continuation,
            ):
                request["continue_to_model"] = True
            _attach_replan_payload_trace_metadata(request, payload)
            _sanitize_zero_input_observation_recovery_request(request)
            requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _runtime_replan_action_auto_start_blocked(action: Mapping[str, Any]) -> bool:
    permission_target = str(action.get("permission_target") or "").strip().casefold()
    if (
        permission_target
        and permission_target not in _RUNTIME_REPLAN_INTERNAL_PERMISSION_TARGETS
        and permission_target not in _RUNTIME_REPLAN_AUTO_SAFE_CAPABILITY_TARGETS
    ):
        return True
    metadata = action.get("metadata") if isinstance(action.get("metadata"), Mapping) else {}
    explicit = metadata.get("runtime_replan_auto_start_eligible")
    if isinstance(explicit, bool):
        return not explicit
    explicit = metadata.get("auto_start_eligible")
    if isinstance(explicit, bool):
        return not explicit
    blockers = metadata.get("runtime_replan_auto_start_blockers")
    if isinstance(blockers, list) and blockers:
        return True
    blockers = metadata.get("auto_start_blockers")
    if isinstance(blockers, list) and blockers:
        return True

    tool_name = str(action.get("tool") or action.get("tool_name") or "").strip()
    risk_level = str(action.get("risk_level") or "").strip().lower()
    approval_required = bool(action.get("approval_required")) or str(
        action.get("approval_status") or ""
    ).strip().lower() in {
        "pending",
        "required",
        "approval_required",
        "waiting_approval",
    }
    return bool(
        not tool_name
        or approval_required
        or risk_level in {"high", "critical"}
        or tool_name not in _RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS
    )


def _runtime_replan_action_needs_model_followup(
    action: Mapping[str, Any],
    *,
    deferred_continuation: list[dict[str, Any]],
) -> bool:
    metadata = action.get("metadata") if isinstance(action.get("metadata"), Mapping) else {}
    explicit = action.get("continue_to_model")
    if isinstance(explicit, bool):
        return explicit
    explicit = metadata.get("continue_to_model")
    if isinstance(explicit, bool):
        return explicit
    if deferred_continuation:
        return False
    return True


def _runtime_replan_action_deferred_continuation_requests(
    action: Mapping[str, Any],
    payload: Mapping[str, Any],
    allowed: set[str],
) -> list[dict[str, Any]]:
    continuation = materialized_deferred_items(action)
    if not continuation:
        return []
    requests: list[dict[str, Any]] = []
    request_id = str(payload.get("request_id") or "").strip()
    trigger = str(payload.get("trigger") or "").strip()
    action_id = str(action.get("action_id") or action.get("id") or "").strip()
    for item in continuation:
        request = safe_deferred_continuation_request(
            item,
            allowed,
            auto_safe_tools=_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS,
        )
        if not request:
            continue
        if request_id:
            request.setdefault("replan_request_id", request_id)
        if trigger:
            request.setdefault("replan_trigger", trigger)
        if action_id:
            request.setdefault("action_id", action_id)
            request.setdefault("replan_recovery_action_id", action_id)
        request.setdefault("source", "runtime_planner")
        request.setdefault("planning_reason", "planner_replan_deferred_continuation")
        for key in (
            "source_step_id",
            "source_tool_name",
            "target_capability_id",
            "capability_id",
            "target_app_name",
            "target_app_query",
            "target_search_text",
        ):
            value = payload.get(key)
            if value not in (None, "", [], {}):
                request.setdefault(key, value)
        _attach_replan_payload_trace_metadata(request, payload)
        requests.append(request)
    return _dedupe_replan_recovery_requests(requests)


def _auto_replan_recovery_deferred_continuation_requests(
    recovery_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if not allowed:
        return []
    requests: list[dict[str, Any]] = []
    for source_request in recovery_requests:
        if not isinstance(source_request, Mapping):
            continue
        source_completion = _runtime_replan_source_request_completion_event(
            source_request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        )
        if source_completion is None:
            continue
        source_event_index, source_event = source_completion
        for item in materialized_deferred_items(source_request):
            request = safe_deferred_continuation_request(
                item,
                allowed,
                auto_safe_tools=_RUNTIME_REPLAN_ACTION_AUTO_SAFE_TOOLS,
            )
            if not request:
                continue
            for key, value in _runtime_replan_deferred_inherited_metadata(
                source_request
            ).items():
                request.setdefault(key, value)
            request.setdefault(
                "source",
                str(source_request.get("source") or "runtime_planner").strip(),
            )
            request.setdefault(
                "planning_reason",
                "planner_replan_deferred_continuation",
            )
            request = _bind_replan_deferred_request_to_source_event(
                request,
                source_request=source_request,
                source_event=source_event,
            )
            if _exact_replan_deferred_verifier_completed_after_source(
                request,
                source_event_index=source_event_index,
                source_event=source_event,
                timeline=timeline,
            ):
                continue
            requests.append(request)
    requests = _dedupe_replan_recovery_requests(requests)
    return _topologically_order_replan_recovery_requests(
        requests,
        timeline,
    )


def _runtime_replan_source_request_completion_event(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> tuple[int, Mapping[str, Any]] | None:
    """Return the exact successful invocation of a recovery source action."""

    tool_name = str(request.get("tool") or "").strip()
    if not tool_name:
        return None
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    expected = {
        key: str(request.get(key) or "").strip()
        for key in ("plan_id", "request_id", "tool_call_id")
    }
    expected_step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    for event_index in range(len(timeline) - 1, max(-1, tool_timeline_start - 1), -1):
        event = timeline[event_index]
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True or result.get("approval_required"):
            continue
        if expected["plan_id"] and str(event.get("plan_id") or "").strip() != expected[
            "plan_id"
        ]:
            continue
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if expected_step_id and event_step_id != expected_step_id:
            continue
        if expected["request_id"] and str(event.get("request_id") or "").strip() != expected[
            "request_id"
        ]:
            continue
        if expected["tool_call_id"] and str(event.get("tool_call_id") or "").strip() != expected[
            "tool_call_id"
        ]:
            continue
        event_input = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        if not _auto_followup_input_matches_event(request_input, event_input):
            continue
        return event_index, event
    return None


def _bind_replan_deferred_request_to_source_event(
    request: dict[str, Any],
    *,
    source_request: Mapping[str, Any],
    source_event: Mapping[str, Any],
) -> dict[str, Any]:
    bound = dict(request)
    source_step_id = str(
        source_event.get("step_id")
        or source_event.get("planner_step_id")
        or source_request.get("step_id")
        or source_request.get("planner_step_id")
        or ""
    ).strip()
    if source_step_id:
        bound["source_step_id"] = source_step_id
        dependencies = _string_list(bound.get("depends_on"))
        if source_step_id not in dependencies:
            dependencies.append(source_step_id)
        bound["depends_on"] = dependencies
        verification_steps = _string_list(bound.get("verification_target_step_ids"))
        if source_step_id not in verification_steps:
            verification_steps.append(source_step_id)
        bound["verification_target_step_ids"] = verification_steps
    source_request_id = str(
        source_event.get("request_id") or source_request.get("request_id") or ""
    ).strip()
    if source_request_id:
        bound["source_request_id"] = source_request_id
    source_tool_call_id = str(
        source_event.get("tool_call_id")
        or source_request.get("tool_call_id")
        or ""
    ).strip()
    if source_tool_call_id:
        bound["source_tool_call_id"] = source_tool_call_id
    source_tool = str(
        source_event.get("detail")
        or source_event.get("tool")
        or source_request.get("tool")
        or ""
    ).strip()
    if source_tool:
        bound["source_tool"] = source_tool
    for key in ("run_id", "decision_id", "plan_id", "goal_contract_id", "goal_criterion_id"):
        value = source_event.get(key) or source_request.get(key)
        if value not in (None, "", [], {}):
            bound[key] = deepcopy(value)
    return bound


def _exact_replan_deferred_verifier_completed_after_source(
    request: Mapping[str, Any],
    *,
    source_event_index: int,
    source_event: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> bool:
    """Accept only a fresh, same-plan verifier receipt for this invocation."""

    tool_name = str(request.get("tool") or "").strip()
    source_tool_call_id = str(source_event.get("tool_call_id") or "").strip()
    source_request_id = str(source_event.get("request_id") or "").strip()
    source_step_id = str(
        source_event.get("step_id") or source_event.get("planner_step_id") or ""
    ).strip()
    if not tool_name or not source_step_id or not (
        source_tool_call_id or source_request_id
    ):
        return False
    expected_plan_id = str(request.get("plan_id") or "").strip()
    expected_step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    request_input = (
        request.get("input") if isinstance(request.get("input"), Mapping) else {}
    )
    for event in timeline[source_event_index + 1 :]:
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        if str(event.get("plan_id") or "").strip() != expected_plan_id:
            continue
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if expected_step_id and event_step_id != expected_step_id:
            continue
        event_input = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        if not _auto_followup_input_matches_event(request_input, event_input):
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if (
            result.get("ok") is not True
            or result.get("approval_required")
            or result.get("verification_failed") is True
        ):
            continue
        event_source_call_id = str(
            event.get("source_tool_call_id")
            or result.get("source_tool_call_id")
            or ""
        ).strip()
        event_source_request_id = str(
            event.get("source_request_id")
            or result.get("source_request_id")
            or ""
        ).strip()
        if source_tool_call_id:
            if event_source_call_id != source_tool_call_id:
                continue
        elif event_source_request_id != source_request_id:
            continue
        event_source_step_id = str(
            event.get("source_step_id")
            or result.get("source_step_id")
            or ""
        ).strip()
        if event_source_step_id != source_step_id:
            continue
        data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
        if not (
            result.get("postcondition_verified") is True
            or result.get("verification_satisfied_by_native_receipt") is True
            or data.get("postcondition_verified") is True
        ):
            continue
        return True
    return False


def _runtime_replan_source_request_completed(
    request: Mapping[str, Any],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> bool:
    comparable = {
        key: value
        for key, value in dict(request).items()
        if key
        not in {
            "continue_to_model",
            "deferred_tool",
            "deferred_input",
            "deferred_context",
            "deferred_continuation",
        }
    }
    return _auto_followup_request_completed(
        comparable,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )


def _runtime_replan_deferred_inherited_metadata(
    request: Mapping[str, Any],
) -> dict[str, Any]:
    metadata = {
        key: value
        for key, value in _request_observability_metadata(request).items()
        if key
        not in {
            "continue_to_model",
            "deferred_tool",
            "deferred_input",
            "deferred_context",
            "deferred_continuation",
        }
    }
    for key in (
        "action_id",
        "replan_recovery_action_id",
        "source_step_id",
        "source_tool_name",
        "target_capability_id",
        "task_todo",
        "task_checkpoints",
        "task_workspace_items",
        "task_verification_targets",
        "verification_targets",
    ):
        value = request.get(key)
        if value not in (None, "", [], {}):
            metadata[key] = value
    return metadata


def _replan_fallback_request_needs_model_followup(
    tool_name: str,
    payload: Mapping[str, Any],
) -> bool:
    clean_tool = str(tool_name or "").strip()
    if clean_tool in {"terminal.run", "python.run"} and _replan_payload_is_data_analysis_fallback(payload):
        return False
    return True


def _replan_payload_is_data_analysis_fallback(payload: Mapping[str, Any]) -> bool:
    capability_id = str(
        payload.get("target_capability_id") or payload.get("capability_id") or ""
    ).strip()
    source_tool = str(
        payload.get("source_tool_name") or payload.get("tool_name") or ""
    ).strip()
    step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    return (
        capability_id == "data.analysis"
        or source_tool == "data.analyze"
        or "analysis" in step_id
    )


def _replan_fallback_tool_input(
    tool_name: str,
    payload: Mapping[str, Any],
) -> dict[str, Any] | None:
    clean_tool = str(tool_name or "").strip()
    input_preview = payload.get("input_preview") if isinstance(payload.get("input_preview"), Mapping) else {}
    metadata = payload.get("metadata") if isinstance(payload.get("metadata"), Mapping) else {}
    metadata_input = (
        metadata.get("input_preview")
        if isinstance(metadata.get("input_preview"), Mapping)
        else {}
    )
    source_input = {
        **dict(metadata_input),
        **dict(input_preview),
    }
    if clean_tool in {"browser.current_page", "desktop.active_window"}:
        return {}
    if clean_tool == "desktop.list_apps":
        query = _first_replan_fallback_app_query(source_input, payload, metadata)
        request: dict[str, Any] = {"limit": 20}
        if query:
            request["query"] = query
        return request
    if clean_tool == "desktop.running_apps":
        return {}
    if clean_tool in {"app.open", "desktop.open_app", "app.focus", "desktop.focus_app"}:
        app_name = _first_replan_fallback_app_name(source_input, payload, metadata)
        return {"app_name": app_name} if app_name else None
    if clean_tool in {"desktop.ui_elements", "desktop.read_ui"}:
        request = {"limit": source_input.get("limit", 80)}
        app_name = _first_replan_fallback_app_name(source_input, payload, metadata)
        if app_name:
            request["app_name"] = app_name
        role_filter = str(source_input.get("role_filter") or "").strip()
        if role_filter:
            request["role_filter"] = role_filter
        return request
    if clean_tool == "screen.capture":
        reason = str(
            payload.get("failure_detail")
            or metadata.get("failure_detail")
            or "recover failed runtime step"
        ).strip()
        return {"reason": reason} if reason else {}
    if clean_tool in {"desktop.open_path", "workspace.read", "fs.read_file", "file.read"}:
        path = _first_replan_fallback_path(source_input, payload, metadata)
        if not path:
            return None
        return {"path": path}
    if clean_tool in {"terminal.run", "python.run"}:
        return _terminal_data_analysis_replan_input(source_input, payload, metadata)
    return None


def _terminal_data_analysis_replan_input(
    source_input: Mapping[str, Any],
    payload: Mapping[str, Any],
    metadata: Mapping[str, Any],
) -> dict[str, Any] | None:
    capability_id = str(
        payload.get("target_capability_id") or payload.get("capability_id") or ""
    ).strip()
    source_tool = str(
        payload.get("source_tool_name") or payload.get("tool_name") or ""
    ).strip()
    step_id = str(
        payload.get("source_step_id") or payload.get("planner_step_id") or ""
    ).strip()
    if not _replan_payload_is_data_analysis_fallback(
        {
            **dict(payload),
            "target_capability_id": capability_id,
            "source_tool_name": source_tool,
            "source_step_id": step_id,
        }
    ):
        return None
    path = _first_replan_fallback_path(source_input, payload, metadata)
    if not path:
        return None
    artifact_paths = _string_list(source_input.get("artifact_paths"))
    artifact_path = (
        str(source_input.get("artifact_path") or "").strip()
        or (artifact_paths[0] if artifact_paths else "")
        or "analysis-report.md"
    )
    command = _terminal_data_analysis_replan_command(
        path,
        artifact_path=artifact_path,
        source_kind=str(source_input.get("source_kind") or "").strip(),
    )
    return {"command": command, "shell": True, "timeout_seconds": 60}


def _terminal_data_analysis_replan_command(
    path: str,
    *,
    artifact_path: str,
    source_kind: str = "",
) -> str:
    source_literal = repr(str(path or "").strip())
    artifact_literal = repr(str(artifact_path or "analysis-report.md").strip())
    kind_literal = repr(str(source_kind or "").strip())
    return f"""python3 - <<'PY'
from pathlib import Path
import csv
import json

source = Path({source_literal})
artifact_path = Path({artifact_literal})
source_kind = {kind_literal}

def text_preview(path):
    try:
        text = path.read_text(encoding="utf-8", errors="replace")
    except Exception as exc:
        return [], [], [f"read_error: {{exc}}"]
    lines = text.splitlines()
    return [], lines[:20], [f"line_count: {{len(lines)}}", f"char_count: {{len(text)}}"]

def tabular_preview(path):
    delimiter = "\\t" if path.suffix.lower() == ".tsv" or source_kind == "tsv" else ","
    try:
        with path.open("r", encoding="utf-8", errors="replace", newline="") as handle:
            rows = list(csv.reader(handle, delimiter=delimiter))
    except Exception as exc:
        return [], [], [f"csv_error: {{exc}}"]
    headers = rows[0] if rows else []
    data_rows = rows[1:] if headers else rows
    return headers, data_rows[:10], [f"row_count: {{len(data_rows)}}", f"column_count: {{len(headers)}}"]

def json_preview(path):
    try:
        text = path.read_text(encoding="utf-8", errors="replace")
        if path.suffix.lower() == ".jsonl" or source_kind == "jsonl":
            rows = [json.loads(line) for line in text.splitlines() if line.strip()]
        else:
            data = json.loads(text)
            rows = data if isinstance(data, list) else [data]
    except Exception as exc:
        return [], [], [f"json_error: {{exc}}"]
    headers = sorted({{key for row in rows[:50] if isinstance(row, dict) for key in row.keys()}})
    sample = rows[:10]
    return headers, sample, [f"row_count: {{len(rows)}}", f"field_count: {{len(headers)}}"]

suffix = source.suffix.lower()
if suffix in {{".csv", ".tsv"}} or source_kind in {{"csv", "tsv"}}:
    headers, sample, notes = tabular_preview(source)
elif suffix in {{".json", ".jsonl"}} or source_kind in {{"json", "jsonl"}}:
    headers, sample, notes = json_preview(source)
else:
    headers, sample, notes = text_preview(source)

report = [
    "# Analysis fallback report",
    "",
    f"- source: {{source}}",
    *[f"- {{note}}" for note in notes],
]
if headers:
    report.extend(["", "## Columns", ", ".join(str(item) for item in headers)])
if sample:
    report.extend(["", "## Sample", "```"])
    for row in sample[:10]:
        report.append(json.dumps(row, ensure_ascii=False) if not isinstance(row, str) else row)
    report.append("```")

artifact_path.parent.mkdir(parents=True, exist_ok=True)
artifact_path.write_text("\\n".join(report) + "\\n", encoding="utf-8")
print("\\n".join(report))
print(f"\\n[artifact_written] {{artifact_path}}")
PY"""


def _first_replan_fallback_path(
    *sources: Mapping[str, Any],
) -> str:
    for source in sources:
        for key in (
            "path",
            "file_path",
            "data_source_hint",
            "source_path",
            "display_path",
        ):
            value = str(source.get(key) or "").strip()
            if value and not value.startswith("captured:"):
                return value
    return ""


def _first_replan_fallback_app_query(
    *sources: Mapping[str, Any],
) -> str:
    for source in sources:
        for key in (
            "target_app_query",
            "app_query",
            "query",
            "target_app_name",
            "app_name",
        ):
            value = str(source.get(key) or "").strip()
            if value and value != "<selected app from desktop.list_apps>":
                return value
    return ""


def _first_replan_fallback_app_name(
    *sources: Mapping[str, Any],
) -> str:
    for source in sources:
        for key in ("target_app_name", "app_name", "target_app_query", "app_query", "query"):
            value = str(source.get(key) or "").strip()
            if value and value != "<selected app from desktop.list_apps>":
                return value
    return ""


def _dedupe_replan_recovery_requests(
    requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    result: list[dict[str, Any]] = []
    seen: set[
        tuple[str, str]
        | tuple[str, tuple[tuple[str, str], ...], str]
    ] = set()
    for request in requests:
        if not isinstance(request, dict):
            continue
        tool_name = str(request.get("tool") or "").strip()
        request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
        request_id = str(request.get("replan_request_id") or "").strip()
        identity = ensure_recovery_action_identity(request)
        signature = (
            ("recovery", identity)
            if identity
            else (
                tool_name,
                tuple(
                    sorted(
                        (str(key), str(value))
                        for key, value in request_input.items()
                    )
                ),
                request_id,
            )
        )
        if not tool_name or signature in seen:
            continue
        seen.add(signature)
        result.append(request)
    return result


def _verification_recovery_tool_order(allowed: set[str]) -> list[str]:
    tools: list[str] = []
    for tool_name in (
        "desktop.active_window",
        "desktop.inspect_app",
        _first_allowed_tool(("desktop.list_windows", "desktop.windows"), allowed),
        _first_allowed_tool(("desktop.read_ui", "desktop.ui_elements"), allowed),
        "screen.capture",
    ):
        if tool_name and tool_name in allowed and tool_name not in tools:
            tools.append(tool_name)
    return tools


def _first_allowed_tool(candidates: tuple[str, ...], allowed: set[str]) -> str:
    for candidate in candidates:
        if candidate in allowed:
            return candidate
    return ""


def _auto_discovered_app_followup_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_discovered_app_followup",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    app_query = str(target.get("app_query") or "").strip()
    app_name = _discovered_app_name_for_query(timeline, app_query)
    if not app_name:
        return []
    resolution_evidence = _discovered_app_resolution_evidence(timeline, app_query, app_name)
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    source = "runtime_planner"
    planning_reason = str(planning_reason or "planner_discovered_app_followup").strip()
    if not planning_reason:
        planning_reason = "planner_discovered_app_followup"
    target_action = str(target.get("target_action") or "").strip()
    safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
    requests: list[dict[str, Any]] = []
    if target_action == "click":
        requests.extend(
            _discovered_app_click_requests(
                app_query,
                app_name,
                target,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
        )
    elif target_action == "safe_shortcut" and safe_shortcut_action:
        requests.extend(
            _discovered_app_safe_shortcut_requests(
                app_query,
                app_name,
                safe_shortcut_action,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
        )
    elif target_action == "open_path_with_selected_app":
        target_path = str(target.get("target_path") or "").strip()
        if target_path:
            open_path_request = _discovered_app_open_path_request(
                app_query,
                app_name,
                target_path,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
            if open_path_request:
                requests.append(open_path_request)
    elif target_action in {"open_app", "open", "focus_app", "focus"}:
        open_request = _discovered_app_open_request(
            app_query,
            app_name,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if open_request:
            requests.append(open_request)
    elif target_action == "app_search":
        app_search = _discovered_app_search_payload(target)
        search_query = str(app_search.get("query") or "").strip()
        if not search_query:
            return []
        requests.extend(
            _discovered_app_search_focus_requests(
                app_query,
                app_name,
                app_search,
                safe_shortcut_action,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
        )
        if not requests:
            return []
        type_request = _discovered_app_type_text_request(
            app_query,
            app_name,
            search_query,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not type_request:
            return []
        requests.append(type_request)
        if _discovered_app_search_submit_requested(app_search):
            submit_request = _media_search_submit_request(
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
            if not submit_request:
                return []
            requests.append(_with_discovered_app_resolution(submit_request, app_query, app_name))
        result_requests = _discovered_app_search_result_requests(
            app_query,
            app_name,
            app_search,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if result_requests:
            requests.extend(result_requests)
    communication_compose = _discovered_app_communication_compose_payload(target)
    if communication_compose:
        compose_requests = _discovered_app_communication_compose_requests(
            app_query,
            app_name,
            communication_compose,
            allowed,
            prepared=bool(requests),
            source=source,
            planning_reason=planning_reason,
        )
        if not compose_requests:
            return []
        requests.extend(compose_requests)
    compose_text = str(target.get("compose_text") or "").strip()
    if requests and compose_text and not communication_compose:
        type_request = _discovered_app_type_text_request(
            app_query,
            app_name,
            compose_text,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if type_request:
            requests.append(type_request)
    if not requests:
        return []
    observation_request = _discovered_app_observation_request(
        target,
        allowed,
        app_query=app_query,
        app_name=app_name,
        source=source,
        planning_reason=planning_reason,
    )
    if observation_request:
        requests.append(observation_request)
    elif _discovered_app_post_action_continue_requested(target) and requests:
        requests[-1]["continue_to_model"] = True
    if (
        str(target.get("body_source") or "").strip() == "model_generated_content"
        and requests
    ):
        requests[-1]["continue_to_model"] = True
    if resolution_evidence:
        requests = [
            _with_discovered_app_resolution_evidence(request, resolution_evidence)
            for request in requests
        ]
    return _annotate_auto_followup_requests_from_tool_plan(requests, selection_payload)


def _annotate_auto_followup_requests_from_tool_plan(
    requests: list[dict[str, Any]],
    selection_payload: Mapping[str, Any],
) -> list[dict[str, Any]]:
    steps = _selection_tool_plan_steps(selection_payload)
    if not requests or not steps:
        return requests
    trace_base = _auto_followup_selection_trace_base(selection_payload)
    annotated: list[dict[str, Any]] = []
    step_cursor = 0
    for request in requests:
        if not isinstance(request, dict):
            continue
        item = {**trace_base, **request}
        step_index, step = _next_matching_followup_plan_step(
            item,
            steps,
            start_index=step_cursor,
        )
        if step_index >= 0 and step:
            step_cursor = step_index + 1
            for key, value in _auto_followup_plan_step_trace(step).items():
                item.setdefault(key, value)
            for key, value in _auto_followup_task_context_for_step(
                selection_payload,
                str(item.get("step_id") or "").strip(),
            ).items():
                item.setdefault(key, value)
            task_todo = (
                item.get("task_todo")
                if isinstance(item.get("task_todo"), Mapping)
                else {}
            )
            task_metadata = (
                task_todo.get("metadata")
                if isinstance(task_todo.get("metadata"), Mapping)
                else {}
            )
            for key in ("runtime_doctrine", "runtime_stage", "runtime_role"):
                value = str(task_metadata.get(key) or "").strip()
                if value:
                    item.setdefault(key, value)
            for key in (
                "requires_observation",
                "requires_post_action_verification",
            ):
                value = task_metadata.get(key)
                if isinstance(value, bool):
                    item.setdefault(key, value)
        if str(item.get("step_id") or "").strip() and not str(
            item.get("planner_step_id") or ""
        ).strip():
            item["planner_step_id"] = str(item.get("step_id") or "").strip()
        annotated.append(item)
    return annotated


def _first_annotated_auto_followup_request(
    request: dict[str, Any],
    selection_payload: Mapping[str, Any],
) -> dict[str, Any]:
    annotated = _annotate_auto_followup_requests_from_tool_plan(
        [request],
        selection_payload,
    )
    return annotated[0] if annotated else request


def _auto_followup_selection_trace_base(selection_payload: Mapping[str, Any]) -> dict[str, Any]:
    trace: dict[str, Any] = {}
    for key in ("decision_id", "plan_id", "intent_kind"):
        value = str(selection_payload.get(key) or "").strip()
        if value:
            trace[key] = value
    tool_plan = (
        selection_payload.get("tool_plan")
        if isinstance(selection_payload.get("tool_plan"), Mapping)
        else {}
    )
    tool_plan_id = str(tool_plan.get("plan_id") or "").strip()
    if tool_plan_id:
        trace["tool_plan_id"] = tool_plan_id
    trace.update(_auto_followup_task_core_trace(selection_payload))
    return trace


def _auto_followup_task_core_trace(selection_payload: Mapping[str, Any]) -> dict[str, Any]:
    task_core = _model_followup_task_core_payload(selection_payload)
    if not task_core:
        return {}
    workspace = (
        task_core.get("workspace")
        if isinstance(task_core.get("workspace"), Mapping)
        else {}
    )
    trace: dict[str, Any] = {}
    core_id = str(task_core.get("core_id") or "").strip()
    if core_id:
        trace["core_id"] = core_id
    workspace_id = str(workspace.get("workspace_id") or "").strip()
    if workspace_id:
        trace["workspace_id"] = workspace_id
    return trace


def _auto_followup_task_context_for_step(
    selection_payload: Mapping[str, Any],
    step_id: str,
) -> dict[str, Any]:
    clean_step_id = str(step_id or "").strip()
    if not clean_step_id:
        return {}
    task_core = _model_followup_task_core_payload(selection_payload)
    if not isinstance(task_core, Mapping):
        return {}
    payload: dict[str, Any] = {}
    todos = task_core.get("todos")
    if isinstance(todos, list):
        for todo in todos:
            if not isinstance(todo, Mapping):
                continue
            if str(todo.get("step_id") or "").strip() == clean_step_id:
                payload["task_todo"] = dict(todo)
                break
    checkpoints = task_core.get("checkpoints")
    if isinstance(checkpoints, list):
        checkpoint_items = [
            dict(checkpoint)
            for checkpoint in checkpoints
            if isinstance(checkpoint, Mapping)
            and str(checkpoint.get("after_step_id") or "").strip() == clean_step_id
        ]
        if checkpoint_items:
            payload["task_checkpoints"] = checkpoint_items
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    workspace_items = workspace.get("items") if isinstance(workspace, Mapping) else []
    if isinstance(workspace_items, list):
        item_payloads = [
            dict(item)
            for item in workspace_items
            if isinstance(item, Mapping)
            and str(item.get("source_step_id") or "").strip() == clean_step_id
        ]
        if item_payloads:
            payload["task_workspace_items"] = item_payloads
    replan_signals = task_core.get("replan_signals")
    if isinstance(replan_signals, list):
        signal_ids: list[str] = []
        triggers: list[str] = []
        for signal in replan_signals:
            if not isinstance(signal, Mapping):
                continue
            if str(signal.get("source_step_id") or "").strip() != clean_step_id:
                continue
            signal_id = str(signal.get("signal_id") or "").strip()
            trigger = str(signal.get("trigger") or "").strip()
            if signal_id and signal_id not in signal_ids:
                signal_ids.append(signal_id)
            if trigger and trigger not in triggers:
                triggers.append(trigger)
        if signal_ids:
            payload["replan_signal_ids"] = signal_ids
        if triggers:
            payload["replan_triggers"] = triggers
    return payload

def _next_matching_followup_plan_step(
    request: Mapping[str, Any],
    steps: list[Mapping[str, Any]],
    *,
    start_index: int,
) -> tuple[int, Mapping[str, Any] | None]:
    tool_name = str(request.get("tool") or "").strip()
    if not tool_name:
        return -1, None
    for index in range(max(0, start_index), len(steps)):
        step = steps[index]
        if str(step.get("status") or "planned").strip() not in {"", "planned"}:
            continue
        if not _followup_plan_tools_match(
            tool_name,
            str(step.get("tool_name") or "").strip(),
        ):
            continue
        return index, step
    return -1, None


_FOLLOWUP_PLAN_TOOL_EQUIVALENCE_GROUPS = (
    {
        "desktop.click_ui_element",
        "desktop.safe_click",
        "desktop.click",
        "app.focus_and_click_ui_element",
        "app.open_and_click_ui_element",
    },
    {
        "desktop.type_into_ui_element",
        "desktop.safe_type_text",
        "desktop.type_text",
        "desktop.type",
        "app.focus_and_safe_type_text",
        "app.open_and_safe_type_text",
        "app.focus_and_type_into_ui_element",
        "app.open_and_type_into_ui_element",
    },
)


def _followup_plan_tools_match(request_tool: str, plan_tool: str) -> bool:
    clean_request_tool = str(request_tool or "").strip()
    clean_plan_tool = str(plan_tool or "").strip()
    if not clean_request_tool or not clean_plan_tool:
        return False
    if clean_request_tool == clean_plan_tool:
        return True
    return any(
        clean_request_tool in group and clean_plan_tool in group
        for group in _FOLLOWUP_PLAN_TOOL_EQUIVALENCE_GROUPS
    )


def _auto_followup_plan_step_trace(step: Mapping[str, Any]) -> dict[str, Any]:
    trace: dict[str, Any] = {}
    for source_key, target_key in (
        ("step_id", "step_id"),
        ("capability_id", "capability_id"),
    ):
        value = str(step.get(source_key) or "").strip()
        if value:
            trace[target_key] = value
    dependencies = _string_list(step.get("depends_on"))
    if dependencies:
        trace["depends_on"] = dependencies
    approval_required = step.get("approval_required")
    if isinstance(approval_required, bool):
        trace["approval_required"] = approval_required
    risk_level = str(step.get("risk_level") or "").strip()
    if risk_level:
        trace["risk_level"] = risk_level
    return trace


def _auto_discovered_media_playback_followup_requests(
    selection_payload: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    planning_reason: str = "planner_discovered_media_playback_followup",
) -> list[dict[str, Any]]:
    target = (
        selection_payload.get("followup_target")
        if isinstance(selection_payload.get("followup_target"), Mapping)
        else {}
    )
    if str(target.get("kind") or "").strip() != "desktop_discovered_media_playback":
        return []
    app_query = str(target.get("app_query") or "").strip()
    media_query = str(target.get("media_playback_query") or "").strip()
    if not app_query:
        return []
    app_name = str(target.get("app_name") or target.get("target_app_name") or "").strip()
    if not app_name:
        app_name = _discovered_app_name_for_query(timeline, app_query)
    if not app_name:
        return []
    resolution_evidence = _discovered_app_resolution_evidence(timeline, app_query, app_name)
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    source = "runtime_planner"
    planning_reason = str(planning_reason or "planner_discovered_media_playback_followup").strip()
    if not planning_reason:
        planning_reason = "planner_discovered_media_playback_followup"
    requests: list[dict[str, Any]] = []
    if not media_query:
        playback_request = _discovered_media_playback_request(
            app_query,
            app_name,
            target,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not playback_request:
            return []
        if str(playback_request.get("tool") or "").strip() != "media.music_app_open_and_play":
            open_request = _discovered_app_open_request(
                app_query,
                app_name,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
            if open_request:
                requests.append(open_request)
        requests.append(playback_request)
        observation_request = _discovered_app_observation_request(
            target,
            allowed,
            app_query=app_query,
            app_name=app_name,
            source=source,
            planning_reason=planning_reason,
        )
        if observation_request:
            requests.append(observation_request)
        if resolution_evidence:
            requests = [
                _with_discovered_app_resolution_evidence(request, resolution_evidence)
                for request in requests
            ]
        return _annotate_auto_followup_requests_from_tool_plan(requests, selection_payload)
    if str(target.get("target_action") or "").strip() == "safe_shortcut":
        safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
        if not safe_shortcut_action:
            return []
        requests.extend(
            _discovered_app_safe_shortcut_requests(
                app_query,
                app_name,
                safe_shortcut_action,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
        )
        if not requests:
            requests = _discovered_media_observed_search_requests(
                app_query,
                app_name,
                media_query,
                target,
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
            if not requests:
                return []
            if resolution_evidence:
                requests = [
                    _with_discovered_app_resolution_evidence(request, resolution_evidence)
                    for request in requests
                ]
            return _annotate_auto_followup_requests_from_tool_plan(
                requests,
                selection_payload,
            )
    else:
        open_request = _discovered_app_open_request(
            app_query,
            app_name,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if open_request:
            requests.append(open_request)
    if not requests or "desktop.safe_type_text" not in allowed:
        return []
    requests.append(
        _request_like(
            "desktop.safe_type_text",
            {"text": media_query},
            source=source,
            planning_reason=planning_reason,
        )
    )
    submit_request = _media_search_submit_request(
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if not submit_request:
        return []
    requests.append(submit_request)
    playback_request = _discovered_media_playback_request(
        app_query,
        app_name,
        target,
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if not playback_request:
        return []
    requests.append(playback_request)
    observation_request = _discovered_app_observation_request(
        target,
        allowed,
        app_query=app_query,
        app_name=app_name,
        source=source,
        planning_reason=planning_reason,
    )
    if observation_request:
        requests.append(observation_request)
    if resolution_evidence:
        requests = [
            _with_discovered_app_resolution_evidence(request, resolution_evidence)
            for request in requests
        ]
    return _annotate_auto_followup_requests_from_tool_plan(requests, selection_payload)


def _discovered_media_observed_search_requests(
    app_query: str,
    app_name: str,
    media_query: str,
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    type_tool = _discovered_media_search_deferred_type_tool(allowed)
    if not type_tool:
        return []
    observation_tool = _first_allowed_tool(("desktop.ui_elements", "desktop.read_ui"), allowed)
    if not observation_tool:
        return []
    open_request = _discovered_app_open_request(
        app_query,
        app_name,
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    result_observation = _discovered_media_result_observation_request(
        app_name,
        target,
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if not result_observation:
        return []
    deferred_input: dict[str, Any] = {
        "app_name": app_name,
        "app_query": app_query,
        "target": "search 搜索",
        "text": media_query,
        "role_filter": "text",
        "limit": 80,
    }
    continuation: list[dict[str, Any]] = []
    if "desktop.submit_foreground" in allowed:
        deferred_input["submit_action"] = "confirm"
    else:
        submit_request = _media_search_submit_request(
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not submit_request:
            return []
        continuation.append(submit_request)
    continuation.append(result_observation)
    observation_input = {"app_name": app_name, "role_filter": "text", "limit": 80}
    observation_request = _with_discovered_app_resolution(
        _request_like(
            observation_tool,
            observation_input,
            source=source,
            planning_reason=planning_reason,
        ),
        app_query,
        app_name,
    )
    observation_request["continue_to_model"] = True
    observation_request["deferred_tool"] = type_tool
    observation_request["deferred_input"] = deferred_input
    observation_request["deferred_continuation"] = continuation
    return [*([open_request] if open_request else []), observation_request]


def _discovered_media_search_deferred_type_tool(allowed: set[str]) -> str:
    return _first_allowed_tool(
        (
            "app.focus_and_type_into_ui_element",
            "app.open_and_type_into_ui_element",
            "desktop.type_into_ui_element",
        ),
        allowed,
    ) or (
        "desktop.type_into_ui_element"
        if _first_allowed_tool(
            ("desktop.safe_type_text", "desktop.type_text", "desktop.type"),
            allowed,
        )
        and _first_allowed_tool(
            (
                "app.focus_and_click_ui_element",
                "app.open_and_click_ui_element",
                "desktop.click_ui_element",
                "desktop.safe_click",
                "desktop.click",
            ),
            allowed,
        )
        else ""
    )


def _discovered_media_result_observation_request(
    app_name: str,
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    click_tool = _discovered_media_result_deferred_click_tool(allowed)
    if not click_tool:
        return {}
    observation_tool = _first_allowed_tool(("desktop.ui_elements", "desktop.read_ui"), allowed)
    if not observation_tool:
        return {}
    result_selection = (
        target.get("result_selection")
        if isinstance(target.get("result_selection"), Mapping)
        else {}
    )
    result_input = {
        "app_name": app_name,
        "target": str(result_selection.get("target") or "first result").strip(),
        "role_filter": str(result_selection.get("role_filter") or "").strip(),
        "limit": _positive_int(result_selection.get("limit"), default=80),
        "click_count": _positive_int(result_selection.get("click_count"), default=1),
    }
    request = _request_like(
        observation_tool,
        {
            "app_name": app_name,
            "role_filter": result_input["role_filter"],
            "limit": result_input["limit"],
        },
        source=source,
        planning_reason=planning_reason,
    )
    request["continue_to_model"] = True
    request["deferred_tool"] = click_tool
    request["deferred_input"] = result_input
    request["deferred_context"] = {
        "step_id": "play-media-search-result",
        "planner_step_id": "play-media-search-result",
        "capability_id": "media.playback",
    }
    return request


def _discovered_media_result_deferred_click_tool(allowed: set[str]) -> str:
    return _first_allowed_tool(
        (
            "app.focus_and_click_ui_element",
            "app.open_and_click_ui_element",
            "desktop.click_ui_element",
        ),
        allowed,
    ) or (
        "desktop.click_ui_element"
        if _first_allowed_tool(("desktop.safe_click", "desktop.click"), allowed)
        else ""
    )


def _media_search_submit_request(
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    if "desktop.search_submit" in allowed:
        return _request_like(
            "desktop.search_submit",
            {},
            source=source,
            planning_reason=planning_reason,
        )
    if "desktop.submit_foreground" in allowed:
        return _request_like(
            "desktop.submit_foreground",
            {"action": "confirm"},
            source=source,
            planning_reason=planning_reason,
        )
    return {}


def _positive_int(value: Any, *, default: int) -> int:
    try:
        parsed = int(value)
    except (TypeError, ValueError):
        return default
    return parsed if parsed > 0 else default


def _discovered_media_playback_request(
    app_query: str,
    app_name: str,
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    if "media.music_app_open_and_play" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "media.music_app_open_and_play",
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    result_selection = (
        target.get("result_selection")
        if isinstance(target.get("result_selection"), Mapping)
        else {}
    )
    result_payload = {
        "target": str(result_selection.get("target") or "first result").strip(),
        "role_filter": str(result_selection.get("role_filter") or "").strip(),
        "limit": _positive_int(result_selection.get("limit"), default=80),
        "click_count": _positive_int(result_selection.get("click_count"), default=1),
    }
    if "app.focus_and_click_ui_element" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "app.focus_and_click_ui_element",
                {"app_name": app_name, **result_payload},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    if "app.open_and_click_ui_element" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "app.open_and_click_ui_element",
                {"app_name": app_name, **result_payload},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    if "desktop.click_ui_element" in allowed:
        return _request_like(
            "desktop.click_ui_element",
            result_payload,
            source=source,
            planning_reason=planning_reason,
        )
    return {}


def _discovered_app_safe_shortcut_requests(
    app_query: str,
    app_name: str,
    action: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    if "app.open_and_safe_shortcut" in allowed:
        return [
            _with_discovered_app_resolution(
                _request_like(
                    "app.open_and_safe_shortcut",
                    {"app_name": app_name, "action": action},
                    source=source,
                    planning_reason=planning_reason,
                ),
                app_query,
                app_name,
            )
        ]
    if "app.focus_and_safe_shortcut" in allowed:
        return [
            _with_discovered_app_resolution(
                _request_like(
                    "app.focus_and_safe_shortcut",
                    {"app_name": app_name, "action": action},
                    source=source,
                    planning_reason=planning_reason,
                ),
                app_query,
                app_name,
            )
        ]
    focus_tool = _first_allowed_tool(
        ("app.open", "desktop.open_app", "app.focus", "desktop.focus_app"),
        allowed,
    )
    if focus_tool and "desktop.safe_shortcut" in allowed:
        return [
            _with_discovered_app_resolution(
                _request_like(
                    focus_tool,
                    {"app_name": app_name},
                    source=source,
                    planning_reason=planning_reason,
                ),
                app_query,
                app_name,
            ),
            _request_like(
                "desktop.safe_shortcut",
                {"action": action},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
    return []


def _discovered_app_click_requests(
    app_query: str,
    app_name: str,
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    target_label = str(target.get("target") or "").strip()
    if not target_label:
        return []
    click_input = {
        "target": target_label,
        "role_filter": str(target.get("role_filter") or "").strip(),
        "limit": _positive_int(target.get("limit"), default=80),
        "click_count": _positive_int(target.get("click_count"), default=1),
    }
    app_scoped_tool = _first_allowed_tool(
        ("app.open_and_click_ui_element",),
        allowed,
    )
    if app_scoped_tool:
        request = _with_discovered_app_resolution(
            _request_like(
                app_scoped_tool,
                {"app_name": app_name, **click_input},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
        request["approval_required"] = True
        request["risk_level"] = "medium"
        return [request]
    prepare_tool = _first_allowed_tool(
        ("app.open", "desktop.open_app"),
        allowed,
    )
    if not prepare_tool or "desktop.click_ui_element" not in allowed:
        return []
    click_request = _with_discovered_app_resolution(
        _request_like(
            "desktop.click_ui_element",
            click_input,
            source=source,
            planning_reason=planning_reason,
        ),
        app_query,
        app_name,
    )
    click_request["approval_required"] = True
    click_request["risk_level"] = "medium"
    return [
        _with_discovered_app_resolution(
            _request_like(
                prepare_tool,
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        ),
        click_request,
    ]


def _discovered_app_open_path_request(
    app_query: str,
    app_name: str,
    path: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    tool_name = _first_allowed_open_path_with_app_tool(allowed)
    if not tool_name:
        return {}
    return _with_discovered_app_resolution(
        _request_like(
            tool_name,
            {"app_name": app_name, "path": path},
            source=source,
            planning_reason=planning_reason,
        ),
        app_query,
        app_name,
    )


def _first_allowed_open_path_with_app_tool(allowed: set[str]) -> str:
    for tool_name in ("desktop.open_path_with_app", "app.open_path_with_app"):
        if tool_name in allowed:
            return tool_name
    return ""


def _discovered_app_open_request(
    app_query: str,
    app_name: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    tool_name = _first_allowed_tool(
        ("app.open", "desktop.open_app", "app.focus", "desktop.focus_app"),
        allowed,
    )
    if not tool_name:
        return {}
    return _with_discovered_app_resolution(
        _request_like(
            tool_name,
            {"app_name": app_name},
            source=source,
            planning_reason=planning_reason,
        ),
        app_query,
        app_name,
    )


def _discovered_app_type_text_request(
    app_query: str,
    app_name: str,
    text: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    if "desktop.safe_type_text" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "desktop.safe_type_text",
                {"text": text},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    if "app.focus_and_safe_type_text" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "app.focus_and_safe_type_text",
                {"app_name": app_name, "text": text},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    return {}


def _discovered_app_communication_compose_payload(
    target: Mapping[str, Any],
) -> dict[str, str]:
    raw = (
        target.get("communication_compose")
        if isinstance(target.get("communication_compose"), Mapping)
        else {}
    )
    if not raw:
        return {}
    payload = {
        key: str(raw.get(key) or "").strip()
        for key in ("channel", "recipient", "body", "send_action")
        if str(raw.get(key) or "").strip()
    }
    if not payload.get("recipient") and not payload.get("body"):
        return {}
    payload.setdefault("send_action", "draft")
    return payload


def _discovered_app_search_payload(target: Mapping[str, Any]) -> dict[str, Any]:
    raw = target.get("app_search") if isinstance(target.get("app_search"), Mapping) else {}
    query = str(raw.get("query") or target.get("app_search_query") or "").strip()
    if not query:
        return {}
    payload: dict[str, Any] = {"query": query}
    for key in ("target", "scope", "submit_action", "select_result"):
        value = str(raw.get(key) or target.get(f"app_search_{key}") or "").strip()
        if value:
            payload[key] = value
    focus = raw.get("focus") if isinstance(raw.get("focus"), Mapping) else {}
    if focus:
        tool_name = str(focus.get("tool") or "").strip()
        raw_input = focus.get("input") if isinstance(focus.get("input"), Mapping) else {}
        if tool_name:
            payload["focus"] = {
                "tool": tool_name,
                "input": dict(raw_input),
            }
    result_selection = (
        raw.get("result_selection")
        if isinstance(raw.get("result_selection"), Mapping)
        else {}
    )
    if result_selection:
        payload["result_selection"] = dict(result_selection)
    submit = raw.get("submit", target.get("app_search_submit"))
    if isinstance(submit, bool):
        payload["submit"] = submit
    elif str(submit or "").strip().lower() in {"1", "true", "yes", "y"}:
        payload["submit"] = True
    verify = raw.get("verify", target.get("app_search_verify"))
    if isinstance(verify, bool):
        payload["verify"] = verify
    elif str(verify or "").strip().lower() in {"1", "true", "yes", "y"}:
        payload["verify"] = True
    return payload


def _discovered_app_search_submit_requested(app_search: Mapping[str, Any]) -> bool:
    result_selection = _discovered_app_search_result_selection(app_search)
    if str(result_selection.get("action") or "").strip() == "key_confirm":
        return False
    if str(app_search.get("submit_action") or "").strip() == "confirm":
        return False
    submit = app_search.get("submit")
    if isinstance(submit, bool):
        return submit
    if str(submit or "").strip().lower() in {"1", "true", "yes", "y"}:
        return True
    return bool(str(app_search.get("submit_action") or "").strip())


def _discovered_app_search_result_selection(app_search: Mapping[str, Any]) -> dict[str, Any]:
    raw = (
        app_search.get("result_selection")
        if isinstance(app_search.get("result_selection"), Mapping)
        else {}
    )
    if raw:
        return dict(raw)
    if str(app_search.get("select_result") or "").strip() == "arrow_down":
        return {"action": "key_confirm"}
    return {}


def _discovered_app_search_result_requests(
    app_query: str,
    app_name: str,
    app_search: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    result_selection = _discovered_app_search_result_selection(app_search)
    action = str(result_selection.get("action") or "").strip()
    if action == "click":
        request = _discovered_app_search_result_click_request(
            app_query,
            app_name,
            result_selection,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        return [request] if request else []
    if action == "key_confirm":
        return _discovered_app_search_result_key_confirm_requests(
            app_query,
            app_name,
            result_selection,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
    return []


def _discovered_app_search_result_click_request(
    app_query: str,
    app_name: str,
    result_selection: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    tool_name = str(result_selection.get("tool") or "").strip()
    raw_input = (
        result_selection.get("input")
        if isinstance(result_selection.get("input"), Mapping)
        else {}
    )
    input_payload = dict(raw_input)
    for key in ("selection_source", "app_selection_source", "query"):
        input_payload.pop(key, None)
    if not tool_name:
        tool_name = (
            "app.focus_and_click_ui_element"
            if "app.focus_and_click_ui_element" in allowed
            else "desktop.click_ui_element"
        )
    if tool_name not in allowed:
        return {}
    if tool_name.startswith("app."):
        input_payload.setdefault("app_name", app_name)
    return _with_discovered_app_resolution(
        _request_like(
            tool_name,
            input_payload,
            source=source,
            planning_reason=planning_reason,
        ),
        app_query,
        app_name,
    )


def _discovered_app_search_result_key_confirm_requests(
    app_query: str,
    app_name: str,
    result_selection: Mapping[str, Any],
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    key = result_selection.get("key") if isinstance(result_selection.get("key"), Mapping) else {}
    confirm = (
        result_selection.get("confirm")
        if isinstance(result_selection.get("confirm"), Mapping)
        else {}
    )
    key_tool = str(key.get("tool") or "desktop.safe_key").strip()
    confirm_tool = str(confirm.get("tool") or "desktop.submit_foreground").strip()
    if key_tool not in allowed or confirm_tool not in allowed:
        return []
    key_input = key.get("input") if isinstance(key.get("input"), Mapping) else {}
    confirm_input = (
        confirm.get("input") if isinstance(confirm.get("input"), Mapping) else {}
    )
    return [
        _with_discovered_app_resolution(
            _request_like(
                key_tool,
                dict(key_input) or {"action": "arrow_down", "repeat_count": 1},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        ),
        _with_discovered_app_resolution(
            _request_like(
                confirm_tool,
                dict(confirm_input) or {"action": "confirm"},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        ),
    ]


def _discovered_app_search_focus_requests(
    app_query: str,
    app_name: str,
    app_search: Mapping[str, Any],
    safe_shortcut_action: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    focus = app_search.get("focus") if isinstance(app_search.get("focus"), Mapping) else {}
    focus_tool = str(focus.get("tool") or "").strip()
    focus_input = focus.get("input") if isinstance(focus.get("input"), Mapping) else {}
    if focus_tool == "desktop.click_ui_element" and focus_tool in allowed:
        open_request = _discovered_app_open_request(
            app_query,
            app_name,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not open_request:
            return []
        click_request = _with_discovered_app_resolution(
            _request_like(
                focus_tool,
                dict(focus_input),
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
        return [open_request, click_request]
    action = str(focus_input.get("action") or safe_shortcut_action or "find").strip()
    if not action:
        return []
    return _discovered_app_safe_shortcut_requests(
        app_query,
        app_name,
        action,
        allowed,
        source=source,
        planning_reason=planning_reason,
    )


def _discovered_app_communication_compose_requests(
    app_query: str,
    app_name: str,
    compose: Mapping[str, str],
    allowed: set[str],
    *,
    prepared: bool,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    recipient = str(compose.get("recipient") or "").strip()
    body = str(compose.get("body") or "").strip()
    if not recipient and not body:
        return []
    requests: list[dict[str, Any]] = []
    if not prepared:
        requests.extend(
            _discovered_app_safe_shortcut_requests(
                app_query,
                app_name,
                "new_message",
                allowed,
                source=source,
                planning_reason=planning_reason,
            )
        )
        if not requests:
            return []
    inspect_request = _discovered_app_communication_inspect_request(
        app_query,
        app_name,
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if inspect_request:
        requests.append(inspect_request)
    channel = str(compose.get("channel") or "").strip()
    if recipient:
        recipient_request = _discovered_app_type_into_ui_element_request(
            app_query,
            app_name,
            _discovered_communication_recipient_target(channel),
            recipient,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not recipient_request or "desktop.search_submit" not in allowed:
            return []
        requests.append(recipient_request)
        requests.append(
            _request_like(
                "desktop.search_submit",
                {},
                source=source,
                planning_reason=planning_reason,
            )
        )
    if body:
        body_request = _discovered_app_type_into_ui_element_request(
            app_query,
            app_name,
            _discovered_communication_body_target(channel),
            body,
            allowed,
            source=source,
            planning_reason=planning_reason,
        )
        if not body_request:
            return []
        requests.append(body_request)
    if (
        str(compose.get("send_action") or "").strip() == "send"
        and body
        and "desktop.submit_foreground" in allowed
    ):
        send_request = _request_like(
            "desktop.submit_foreground",
            {"action": "send"},
            source=source,
            planning_reason=planning_reason,
        )
        send_request["approval_required"] = True
        send_request["risk_level"] = "high"
        requests.append(send_request)
    return requests


def _discovered_app_communication_inspect_request(
    app_query: str,
    app_name: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    if "desktop.inspect_app" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "desktop.inspect_app",
                {
                    "app_name": app_name,
                    "open_if_needed": False,
                    "focus": True,
                    "role_filter": "text",
                    "limit": 80,
                },
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    if "desktop.ui_elements" in allowed:
        return _request_like(
            "desktop.ui_elements",
            {"role_filter": "text", "limit": 80},
            source=source,
            planning_reason=planning_reason,
        )
    return {}


def _discovered_app_type_into_ui_element_request(
    app_query: str,
    app_name: str,
    target: str,
    text: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    payload = {
        "target": target,
        "text": text,
        "role_filter": "text",
        "limit": 80,
    }
    if "app.focus_and_type_into_ui_element" in allowed:
        return _with_discovered_app_resolution(
            _request_like(
                "app.focus_and_type_into_ui_element",
                {"app_name": app_name, **payload},
                source=source,
                planning_reason=planning_reason,
            ),
            app_query,
            app_name,
        )
    if "desktop.type_into_ui_element" in allowed:
        return _request_like(
            "desktop.type_into_ui_element",
            payload,
            source=source,
            planning_reason=planning_reason,
        )
    if "desktop.safe_type_text" in allowed:
        return _request_like(
            "desktop.safe_type_text",
            {"text": text},
            source=source,
            planning_reason=planning_reason,
        )
    return {}


def _discovered_communication_recipient_target(channel: str) -> str:
    return "To" if str(channel or "").strip() == "email" else "recipient"


def _discovered_communication_body_target(channel: str) -> str:
    return "message body" if str(channel or "").strip() == "email" else "message"


def _discovered_app_observation_request(
    target: Mapping[str, Any],
    allowed: set[str],
    *,
    app_query: str = "",
    app_name: str = "",
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    post_action_observation = (
        target.get("post_action_observation")
        if isinstance(target.get("post_action_observation"), Mapping)
        else {}
    )
    if (
        str(target.get("target_action") or "").strip() == "open_path_with_selected_app"
        and not post_action_observation
    ):
        return {}
    target_action = str(target.get("target_action") or "").strip()
    if (
        not post_action_observation
        and target_action in {"open_app", "open", "focus_app", "focus"}
        and "desktop.active_window" in allowed
    ):
        return _request_like(
            "desktop.active_window",
            {},
            source=source,
            planning_reason=planning_reason,
        )
    tool_name = str(post_action_observation.get("tool") or "desktop.ui_elements").strip()
    raw_input = (
        post_action_observation.get("input")
        if isinstance(post_action_observation.get("input"), Mapping)
        else {}
    )
    if tool_name not in allowed:
        alias_tool = _discovered_app_observation_alias_tool(tool_name)
        fallback_tool = _discovered_app_observation_fallback_tool(tool_name, allowed)
        if not fallback_tool:
            return {}
        tool_name = fallback_tool
        if fallback_tool != alias_tool:
            raw_input = {}
    request = _request_like(
        tool_name,
        dict(raw_input),
        source=source,
        planning_reason=planning_reason,
    )
    if (
        isinstance(target.get("creative_canvas"), Mapping)
        or _discovered_app_post_action_continue_requested(target)
    ):
        request["continue_to_model"] = True
    if app_query and app_name and _tool_accepts_discovered_app_context(tool_name):
        request = _with_discovered_app_resolution(request, app_query, app_name)
    return request


def _tool_accepts_discovered_app_context(tool_name: str) -> bool:
    return str(tool_name or "").strip() in {
        "desktop.inspect_app",
        "desktop.ui_elements",
        "desktop.read_ui",
        "desktop.verify",
        "desktop.windows",
        "desktop.list_windows",
    }


def _discovered_app_post_action_continue_requested(
    target: Mapping[str, Any],
) -> bool:
    post_action_observation = (
        target.get("post_action_observation")
        if isinstance(target.get("post_action_observation"), Mapping)
        else {}
    )
    value = post_action_observation.get("continue_to_model")
    if isinstance(value, bool):
        return value
    return str(value or "").strip().lower() in {"1", "true", "yes", "y"}


def _discovered_app_observation_fallback_tool(
    requested_tool: str,
    allowed: set[str],
) -> str:
    alias = _discovered_app_observation_alias_tool(requested_tool)
    if alias and alias in allowed:
        return alias
    if "desktop.ui_elements" in allowed:
        return "desktop.ui_elements"
    if "desktop.read_ui" in allowed:
        return "desktop.read_ui"
    return ""


def _discovered_app_observation_alias_tool(requested_tool: str) -> str:
    return {
        "desktop.ui_elements": "desktop.read_ui",
        "desktop.windows": "desktop.list_windows",
    }.get(str(requested_tool or "").strip(), "")


def _with_discovered_app_resolution(
    request: dict[str, Any],
    app_query: str,
    app_name: str,
) -> dict[str, Any]:
    tool_name = str(request.get("tool") or "").strip()
    payload = dict(request)
    request_input = (
        dict(payload.get("input"))
        if isinstance(payload.get("input"), Mapping)
        else {}
    )
    raw_app_name = str(request_input.get("app_name") or "").strip()
    if app_name and (
        not raw_app_name
        or _runtime_planner_placeholder_app_name(raw_app_name)
        or raw_app_name == app_query
    ) and (tool_name.startswith("app.") or raw_app_name):
        request_input["app_name"] = app_name
        payload["input"] = request_input
    return {
        **request,
        **payload,
        "input_resolution": {
            "tool": tool_name,
            "field": "app_name",
            "requested_app_name": app_query,
            "resolved_app_name": app_name,
            "source_tool": "desktop.list_apps",
        },
    }


def _with_discovered_app_resolution_evidence(
    request: dict[str, Any],
    evidence: Mapping[str, str],
) -> dict[str, Any]:
    if not evidence:
        return request
    resolution = (
        request.get("input_resolution")
        if isinstance(request.get("input_resolution"), dict)
        else {}
    )
    if not resolution:
        return request
    return {**request, "input_resolution": {**resolution, **dict(evidence)}}


def _model_followup_communication_prepare_tool_names(
    allowed: set[str],
    *,
    app_name: str,
    mode: str,
    channel: str,
) -> list[str]:
    action = _model_followup_communication_focus_action(channel)
    if mode == "open" and "app.open_and_safe_shortcut" in allowed:
        return ["app.open_and_safe_shortcut"]
    if "app.focus_and_safe_shortcut" in allowed:
        return ["app.focus_and_safe_shortcut"]
    if "app.open_and_safe_shortcut" in allowed:
        return ["app.open_and_safe_shortcut"]
    focus_tool = "app.open" if mode == "open" and "app.open" in allowed else ""
    if not focus_tool:
        focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if app_name and focus_tool and "desktop.safe_shortcut" in allowed and action:
        return [focus_tool, "desktop.safe_shortcut"]
    return []


def _model_followup_communication_focus_action(channel: str) -> str:
    return "new_message" if str(channel or "").strip() == "email" else "find"


def _model_followup_app_write_tool_names(
    allowed: set[str],
    container_action: str = "",
) -> list[str]:
    tools = _model_followup_container_tool_names(allowed) if container_action else []
    if container_action and not tools:
        return []
    if "app.focus_and_safe_type_text" in allowed:
        return [*tools, "app.focus_and_safe_type_text"]
    if "app.open_and_safe_type_text" in allowed:
        return [*tools, "app.open_and_safe_type_text"]
    focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if focus_tool and "desktop.safe_type_text" in allowed:
        return [*tools, focus_tool, "desktop.safe_type_text"]
    if focus_tool and "clipboard.write" in allowed and "desktop.safe_shortcut" in allowed:
        return [*tools, "clipboard.write", focus_tool, "desktop.safe_shortcut"]
    return []


def _model_followup_current_input_write_tool_names(allowed: set[str]) -> list[str]:
    if "desktop.safe_type_text" in allowed:
        return ["desktop.safe_type_text"]
    if "clipboard.write" in allowed and "desktop.safe_shortcut" in allowed:
        return ["clipboard.write", "desktop.safe_shortcut"]
    return []


def _model_followup_container_tool_names(allowed: set[str]) -> list[str]:
    if "app.focus_and_safe_shortcut" in allowed:
        return ["app.focus_and_safe_shortcut"]
    if "app.open_and_safe_shortcut" in allowed:
        return ["app.open_and_safe_shortcut"]
    focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if focus_tool and "desktop.safe_shortcut" in allowed:
        return [focus_tool, "desktop.safe_shortcut"]
    return []


def _model_followup_container_action(target: Mapping[str, Any]) -> str:
    action = str(target.get("container_action") or "").strip()
    return action if action in {"new_note", "new_document"} else ""


def _model_followup_target_instruction(target: Mapping[str, Any]) -> str:
    if not isinstance(target, Mapping):
        return ""
    kind = str(target.get("kind") or "").strip()
    if kind == "communication_message":
        return _model_followup_communication_instruction(target)
    if kind == "desktop_discovered_app_action":
        return _model_followup_desktop_discovered_app_instruction(target)
    if kind == "desktop_discovered_media_playback":
        return _model_followup_discovered_media_playback_instruction(target)
    if kind == "artifact_write":
        return _model_followup_artifact_write_instruction(target)
    if kind == "note_write":
        return _model_followup_note_write_instruction(target)
    if kind == "desktop_observed_action":
        return _model_followup_desktop_observed_action_instruction(target)
    if kind == "current_input_write":
        return _model_followup_current_input_write_instruction(target)
    app_name = str(target.get("app_name") or "").strip()
    if kind != "app_write" or not app_name:
        return ""
    if not bool(target.get("write_allowed")):
        return (
            f"The user requested the transformed content be written into {app_name}, "
            "but no allowed target-container creation or foreground text insertion tool is "
            "available. Explain the missing capability instead of claiming the app was updated."
        )
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    container_action = _model_followup_container_action(target)
    tool_text = ", ".join(tools) or "the allowed desktop text insertion tools"
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after writing."
        if verify_tools
        else ""
    )
    container_text = (
        f" First create the requested target container with {container_action}, then insert "
        "the generated transformed content. "
        if container_action
        else " "
    )
    return (
        f"The user requested the transformed content be written into {app_name}. "
        f"{container_text}"
        "After deriving the final transformed text, call desktop tools next instead of only "
        f"replying inline. Prefer {tool_text}; use the generated transformed content as the "
        "text input. Do not write the raw observed source when the user asked for summary, "
        f"cleanup, translation, or todo conversion.{verify_text} "
    )


def _model_followup_current_input_write_instruction(target: Mapping[str, Any]) -> str:
    if not bool(target.get("write_allowed")):
        return (
            "The user requested the transformed content be written back into the current "
            "foreground input or selection, but no allowed foreground text insertion tool is "
            "available. Explain the missing capability instead of claiming the UI was updated."
        )
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    tool_text = ", ".join(tools) or "the allowed foreground text insertion tools"
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after writing."
        if verify_tools
        else ""
    )
    return (
        "The user requested the transformed content be written back into the current "
        "foreground input or selected text. After deriving the final transformed text, "
        f"call desktop tools next instead of only replying inline. Prefer {tool_text}; "
        "use the generated transformed content as the text input. Do not write the raw "
        "observed source when the user asked for summary, cleanup, translation, or todo "
        f"conversion.{verify_text} "
    )


def _model_followup_artifact_write_instruction(target: Mapping[str, Any]) -> str:
    path = str(target.get("path") or "").strip()
    if not path:
        return ""
    if not bool(target.get("write_allowed")):
        return (
            f"The user requested a durable artifact at {path!r}, but artifact.write is not "
            "available. Explain the missing capability instead of claiming the file was written."
        )
    tools = _string_list(target.get("recommended_tools"))
    tool_text = ", ".join(tools) or "artifact.write"
    return (
        f"The user requested a durable artifact at {path!r}. After deriving the final artifact "
        "content, call artifact.write next instead of only replying inline. "
        f"Prefer {tool_text}; use the generated transformed content as the content input. "
        "Do not write an empty or placeholder artifact, and do not write the raw observed source "
        "when the user asked for summary, cleanup, translation, or report generation. "
    )


def _model_followup_chained_artifact_instruction(target: Mapping[str, Any]) -> str:
    raw = target.get("artifact_write") if isinstance(target.get("artifact_write"), Mapping) else {}
    path = str(raw.get("path") or "").strip()
    if not path:
        return ""
    if not bool(raw.get("write_allowed")):
        return (
            f"The user also requested a durable artifact at {path!r}, but artifact.write is not "
            "available; continue the delivery step only if its tools are available and do not "
            "claim the file was written. "
        )
    return (
        f"Before the delivery step, call artifact.write with path {path!r} and the same generated "
        "final content, then continue with the requested app or communication delivery. Do not "
        "stop after writing the artifact. "
    )


def _model_followup_note_write_instruction(target: Mapping[str, Any]) -> str:
    if not bool(target.get("write_allowed")):
        return (
            "The user requested a note in Notes, but notes.create is not available. Explain "
            "the missing capability instead of claiming the note was created."
        )
    tools = _string_list(target.get("recommended_tools"))
    tool_text = ", ".join(tools) or "notes.create"
    return (
        "The user requested the transformed content be saved as a note in Notes. "
        "After deriving the final note body, call notes.create next instead of only "
        f"replying inline. Prefer {tool_text}; use the generated transformed content as "
        "the body input. Do not write the raw observed source when the user asked for "
        "summary, cleanup, translation, or todo conversion. "
    )


def _model_followup_desktop_observed_action_instruction(target: Mapping[str, Any]) -> str:
    target_action = str(target.get("target_action") or "").strip()
    target_label = str(target.get("target") or "").strip()
    if target_action not in {"click", "type_text"} or not target_label:
        return ""
    if not bool(target.get("action_allowed")):
        return (
            f"The user requested a foreground desktop {target_action} on {target_label!r}, "
            "but no allowed desktop action tool can complete it after the UI observation. "
            "Explain the missing capability instead of claiming the action was performed."
        )
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    tool_text = ", ".join(tools) or "the allowed desktop action tools"
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after the action."
        if verify_tools
        else ""
    )
    role_filter = str(target.get("role_filter") or "").strip()
    role_text = f" with role/filter {role_filter!r}" if role_filter else ""
    if target_action == "click":
        click_count = _clean_model_followup_int(target.get("click_count"), default=1)
        return (
            "The user requested a foreground UI click after the runtime observed the current "
            f"page/window. Use the latest observed UI snapshot to find {target_label!r}"
            f"{role_text}, then call a desktop action tool next instead of only replying inline. "
            f"Prefer {tool_text}; use click_count={click_count}. If only coordinate clicking is "
            "available, infer coordinates from the observed element or screenshot before calling "
            f"desktop.click; do not ask the user to click manually.{verify_text} "
        )
    text = str(target.get("text") or "")
    return (
        "The user requested foreground UI typing after the runtime observed the current "
        f"page/window. Use the latest observed UI snapshot to find {target_label!r}"
        f"{role_text}, then call a desktop action tool next instead of only replying inline. "
        f"Prefer {tool_text}; type the explicit user text {text!r}. If only low-level tools "
        "are available, focus the matching field with desktop.click when needed and then call "
        f"desktop.type_text or desktop.type. Do not ask the user to type manually.{verify_text} "
    )


def _model_followup_desktop_discovered_app_instruction(target: Mapping[str, Any]) -> str:
    app_query = str(target.get("app_query") or "").strip()
    if not app_query:
        return ""
    creative_canvas = (
        target.get("creative_canvas")
        if isinstance(target.get("creative_canvas"), Mapping)
        else {}
    )
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    target_path = str(target.get("target_path") or "").strip()
    tool_text = ", ".join(tools) or "the allowed desktop operation tools"
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after the operation."
        if verify_tools
        else ""
    )
    app_search = _discovered_app_search_payload(target)
    if app_search:
        search_query = str(app_search.get("query") or "").strip()
        submit_text = (
            " submit the search with desktop.search_submit,"
            if _discovered_app_search_submit_requested(app_search)
            else ""
        )
        result_text = _model_followup_app_search_result_instruction(app_search)
        return (
            f"The runtime discovered an app for {app_query!r}. Continue by using desktop "
            "tools to focus the discovered app's search field, type the explicit user "
            f"search query {search_query!r},{submit_text} and then continue from the observed "
            f"result.{result_text} Prefer {tool_text}.{verify_text} If search-field focus "
            "or text input tools are unavailable, explain the missing capability instead "
            "of claiming the app search was completed. "
        )
    communication_compose = (
        target.get("communication_compose")
        if isinstance(target.get("communication_compose"), Mapping)
        else {}
    )
    if communication_compose:
        recipient = str(communication_compose.get("recipient") or "").strip()
        body = str(communication_compose.get("body") or "").strip()
        send_action = str(communication_compose.get("send_action") or "draft").strip()
        transform = str(
            target.get("transform")
            or target.get("content_transform_hint")
            or ""
        ).strip()
        transform_text = f" Apply the requested content transform: {transform}." if transform else ""
        send_text = (
            " Send only through the approval-gated submit tool after the draft is filled."
            if send_action == "send"
            else " Prepare the draft only unless the user explicitly asks to send."
        )
        return (
            f"The user requested delivery through an app matching {app_query!r}. After deriving "
            f"the final content, call desktop.list_apps for {app_query!r}, select the best "
            "matching app, then open a new message in the discovered app, inspect the compose UI, fill the explicit "
            f"recipient {recipient!r}, and typing the explicit body {body!r}. "
            f"{transform_text} Prefer {tool_text}.{send_text}{verify_text} If these UI tools are unavailable, "
            "explain the missing capability instead of claiming the message was prepared. "
        )
    if target_path:
        return (
            f"The runtime discovered an app for {app_query!r}. Continue by opening "
            f"{target_path!r} with the discovered app using the allowed desktop tools. "
            f"Prefer {tool_text}.{verify_text} If direct open-with-app tooling is unavailable, "
            "explain the missing capability and do not claim the file was opened. "
        )
    target_action = str(target.get("target_action") or "").strip()
    safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
    body_source = str(
        target.get("body_source")
        or target.get("context_source")
        or ""
    ).strip()
    if (
        target_action == "safe_shortcut"
        and safe_shortcut_action == "paste"
        and body_source
        in {"clipboard", "selection", "current_page_link", "current_page_content"}
    ):
        source_text = {
            "clipboard": "the existing clipboard contents",
            "selection": "the runtime-captured selected text",
            "current_page_link": "the runtime-captured current page link",
            "current_page_content": "the runtime-captured current page content",
        }[body_source]
        source_action = str(target.get("source_action") or "").strip()
        source_action_text = (
            f" If that context has not been captured yet, first run the source shortcut "
            f"{source_action!r}."
            if source_action and source_action != "use_existing_clipboard"
            else ""
        )
        return (
            f"The user requested transferring {source_text} into an app matching "
            f"{app_query!r}.{source_action_text} Continue by selecting the best discovered "
            "app, opening or focusing it, then paste with the allowed desktop shortcut "
            f"tools. Prefer {tool_text}.{verify_text} Do not ask the user to copy or paste "
            "manually, and do not replace the captured context with newly generated content. "
            "If app discovery or paste tools are unavailable, explain the missing capability "
            "instead of claiming the app was updated. "
        )
    if str(target.get("body_source") or "").strip() == "model_generated_content":
        safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
        container_text = (
            f" create the requested target container with {safe_shortcut_action!r}, then"
            if safe_shortcut_action
            else ""
        )
        return (
            f"The user requested the generated content be written into an app matching "
            f"{app_query!r}. After deriving the final content, call desktop.list_apps for "
            f"{app_query!r}, select the best matching app, then use desktop tools to"
            f"{container_text} insert the generated content. Prefer {tool_text}."
            f"{verify_text} Do not write the raw observed source when the user asked for "
            "summary, analysis, report generation, cleanup, translation, or todo conversion. "
            "If app discovery or text insertion tools are unavailable, explain the missing "
            "capability instead of claiming the app was updated. "
        )
    compose_text = str(target.get("compose_text") or "").strip()
    if compose_text:
        safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
        action_text = (
            f"run {safe_shortcut_action!r} in the discovered app, then type"
            if safe_shortcut_action
            else "open the discovered app, then type"
        )
        return (
            f"The runtime discovered an app for {app_query!r}. Continue by using desktop "
            f"tools to {action_text} the explicit user text {compose_text!r}. "
            f"Prefer {tool_text}.{verify_text} If text insertion tools are unavailable, "
            "explain the missing capability instead of claiming the app was updated. "
        )
    if creative_canvas:
        width = str(creative_canvas.get("width") or "").strip()
        height = str(creative_canvas.get("height") or "").strip()
        size_text = f" {width}x{height}" if width and height else ""
        pending_action = str(target.get("pending_user_action") or "").strip()
        pending_text = (
            f" The remaining user action is: {pending_action!r}; continue toward that "
            "action after the canvas is available."
            if pending_action
            else ""
        )
        return (
            f"The runtime already discovered and attempted to open the best app for {app_query!r}. "
            f"Use the latest UI observation to create or configure the requested{size_text} canvas. "
            f"{pending_text} Call desktop UI tools next instead of replying inline. Prefer {tool_text}."
            f"{verify_text} If the required fields are not visible, inspect the UI again before "
            "claiming completion. "
        )
    if _discovered_app_post_action_continue_requested(target):
        pending_action = str(target.get("pending_user_action") or "").strip()
        pending_text = (
            f" The remaining user action is: {pending_action!r}."
            if pending_action
            else ""
        )
        return (
            f"The runtime discovered and opened an app for {app_query!r}, then requested "
            "a UI observation because the user asked for more than simply opening it."
            f"{pending_text} Use the latest observed UI to continue with desktop tools next; "
            f"prefer {tool_text}.{verify_text} If the required controls are not visible, "
            "inspect the app again or explain the missing desktop UI capability instead of "
            "claiming the task is complete. "
        )
    return (
        f"The runtime discovered an app for {app_query!r}. Continue the requested desktop action "
        f"with safe app or foreground tools next. Prefer {tool_text}.{verify_text} "
    )


def _model_followup_app_search_result_instruction(app_search: Mapping[str, Any]) -> str:
    result_selection = _discovered_app_search_result_selection(app_search)
    action = str(result_selection.get("action") or "").strip()
    if action == "click":
        tool_name = str(result_selection.get("tool") or "desktop.click_ui_element").strip()
        raw_input = (
            result_selection.get("input")
            if isinstance(result_selection.get("input"), Mapping)
            else {}
        )
        target = str(raw_input.get("target") or "the requested result").strip()
        return (
            f" Then select the requested app-search result {target!r} with {tool_name}; "
            "if that click tool requires approval, pause for approval instead of claiming "
            "the result was opened. "
        )
    if action == "key_confirm":
        return (
            " Then use desktop.safe_key for the requested navigation key and request the "
            "approval-gated desktop.submit_foreground confirm action; do not claim the "
            "result was opened until the approval-gated confirm tool has executed. "
        )
    return " "


def _model_followup_discovered_media_playback_instruction(target: Mapping[str, Any]) -> str:
    app_query = str(target.get("app_query") or "").strip()
    media_query = str(target.get("media_playback_query") or "").strip()
    if not app_query or not media_query:
        return ""
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    tool_text = ", ".join(tools) or "the allowed desktop media operation tools"
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after attempting playback."
        if verify_tools
        else ""
    )
    return (
        f"The runtime discovered an app for {app_query!r}. Continue by selecting the best "
        f"discovered app, opening its search UI, typing the media query {media_query!r}, "
        "submitting the search, and playing the first result with an allowed playback or UI "
        f"click tool. Prefer {tool_text}.{verify_text} If these tools are unavailable, explain "
        "the missing capability instead of asking the user to open the app manually. "
    )


def _model_followup_communication_instruction(target: Mapping[str, Any]) -> str:
    app_name = str(target.get("app_name") or "").strip()
    recipient = str(target.get("recipient") or "").strip()
    if not app_name or not recipient:
        return ""
    if not bool(target.get("draft_allowed")):
        return (
            f"The user requested a message to {recipient} in {app_name}, but the allowed "
            "desktop tools cannot safely focus the recipient and type a draft. Explain the "
            "missing capability instead of claiming the message was prepared."
        )
    tools = _string_list(target.get("recommended_tools"))
    verify_tools = _string_list(target.get("verify_tools"))
    transform = str(target.get("transform") or "").strip()
    transform_text = f" Apply the requested content transform: {transform}." if transform else ""
    send_text = (
        " Include the approval-gated send step only after the draft is typed."
        if bool(target.get("send_allowed"))
        else " Sending is not available in the allowed tools, so prepare the draft only."
    )
    verify_text = (
        f" Verify with {', '.join(verify_tools)} after drafting."
        if verify_tools
        else ""
    )
    return (
        f"The user requested the generated content be prepared as a message to {recipient} "
        f"in {app_name}. After deriving the final message body, call desktop tools next "
        "instead of only replying inline: focus the communication target, type the explicit "
        "recipient, submit/select the recipient, then type the generated message body."
        f"{transform_text} Prefer {', '.join(tools) or 'the allowed communication tools'}."
        f"{send_text}{verify_text} "
    )


def _latest_model_followup_target(timeline: list[dict[str, Any]]) -> dict[str, Any]:
    return _model_followup_context_target(_latest_model_followup_context(timeline))


def _latest_model_followup_context(
    timeline: list[dict[str, Any]],
    *,
    include_pending_execution_only: bool = False,
) -> dict[str, Any]:
    for event in reversed(timeline):
        if not isinstance(event, dict):
            continue
        if str(event.get("event") or "").strip() != "agent.model.followup_context":
            continue
        context = {
            str(key): value
            for key, value in event.items()
            if key not in {"timestamp"}
        }
        if (
            not _followup_event_has_readable_source(event)
            and not _followup_event_has_pending_execution(event)
            and not _model_followup_target_from_task_core_context(context)
        ):
            return {}
        return context
    return {}


def _latest_model_replan_trace_context(
    timeline: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    """Return correlation-only Runtime replan context for request binding.

    A pure replan context intentionally is not a general pending-execution
    context: treating it as one after the retry completes can force an extra
    model turn.  Tool-request binding still needs its Runtime-owned trace, so
    this narrow reader is used only at that boundary; every claimed source is
    replay-validated later before it gains execution authority.
    """

    for raw_event in reversed(timeline):
        if not isinstance(raw_event, Mapping):
            continue
        if str(raw_event.get("event") or "").strip() != (
            "agent.model.followup_context"
        ):
            continue
        context = {
            str(key): value
            for key, value in raw_event.items()
            if key != "timestamp"
        }
        if (
            str(context.get("source") or "").strip() == "runtime_planner"
            and str(context.get("status") or "").strip() == "ready"
            and bool(_mapping_list(context.get("replan_requests")))
        ):
            return context
        return {}
    return {}


def _model_followup_context_target(context: Mapping[str, Any]) -> dict[str, Any]:
    target = context.get("followup_target") if isinstance(context, Mapping) else {}
    if isinstance(target, dict):
        return dict(target)
    return _model_followup_target_from_task_core_context(context)


def _model_followup_target_from_task_core_context(
    context: Mapping[str, Any],
) -> dict[str, Any]:
    if not isinstance(context, Mapping):
        return {}
    task_core = context.get("task_core") if isinstance(context.get("task_core"), Mapping) else {}
    if not task_core:
        return {}
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    workspace_items = workspace.get("items") if isinstance(workspace.get("items"), list) else []
    for item in workspace_items:
        if not isinstance(item, Mapping):
            continue
        metadata = item.get("metadata") if isinstance(item.get("metadata"), Mapping) else {}
        tool_name = str(metadata.get("tool_name") or "").strip()
        if tool_name not in _MODEL_FOLLOWUP_TEXT_ENTRY_TOOLS:
            continue
        raw_input = (
            metadata.get("input_preview")
            if isinstance(metadata.get("input_preview"), Mapping)
            else {}
        )
        app_name = str(raw_input.get("app_name") or "").strip()
        if not app_name:
            app_name = _model_followup_task_core_input_value(task_core, "target_app_hint")
        if not app_name:
            continue
        payload: dict[str, Any] = {
            "kind": "app_write",
            "app_name": app_name,
            "target_action": str(
                raw_input.get("target_action")
                or _model_followup_task_core_input_value(task_core, "target_action_hint")
                or "app_paste"
            ).strip(),
            "body_source": str(raw_input.get("body_source") or "model_generated_content").strip(),
        }
        container_action = str(
            raw_input.get("container_action")
            or _model_followup_task_core_input_value(
                task_core,
                "target_container_action_hint",
            )
            or ""
        ).strip()
        if container_action:
            payload["container_action"] = container_action
        artifact_path = str(raw_input.get("artifact_path") or "").strip()
        if artifact_path:
            payload["artifact_write"] = {
                "path": artifact_path,
                "body_source": payload["body_source"],
                "tool": "artifact.write",
            }
        return payload
    return {}


def _model_followup_task_core_input_value(
    task_core: Mapping[str, Any],
    key: str,
) -> str:
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    workspace_items = workspace.get("items") if isinstance(workspace.get("items"), list) else []
    expected = str(key or "").strip()
    if not expected:
        return ""
    for item in workspace_items:
        if not isinstance(item, Mapping):
            continue
        if str(item.get("title") or "").strip() != expected:
            continue
        return str(item.get("description") or "").strip()
    return ""


def _followup_event_has_readable_source(event: Mapping[str, Any]) -> bool:
    snapshots: list[dict[str, Any]] = []
    raw_snapshots = event.get("content_snapshots")
    if isinstance(raw_snapshots, list):
        snapshots.extend(item for item in raw_snapshots if isinstance(item, dict))
    raw_snapshot = event.get("content_snapshot")
    if isinstance(raw_snapshot, dict):
        snapshots.append(raw_snapshot)
    if not snapshots:
        return False
    return any(
        snapshot.get("ok") is not False
        and bool(str(snapshot.get("text") or "").strip())
        for snapshot in snapshots
    )


def _followup_event_has_pending_execution(event: Mapping[str, Any]) -> bool:
    pending_requests = event.get("pending_execution_requests")
    pending_steps = event.get("pending_plan_steps")
    values: list[Any] = []
    if isinstance(pending_requests, list):
        values.extend(pending_requests)
    if isinstance(pending_steps, list):
        values.extend(pending_steps)
    if not values:
        return False
    return any(
        isinstance(request, Mapping)
        and str(request.get("tool_name") or request.get("tool") or "").strip()
        for request in values
    )


_MODEL_FOLLOWUP_AUTO_PENDING_TOOLS = {
    "app.open",
    "artifact.write",
    "data.analyze",
    "app.focus",
    "app.focus_and_click_ui_element",
    "app.focus_and_hotkey",
    "app.focus_and_safe_click",
    "app.focus_and_safe_key",
    "app.focus_and_safe_scroll",
    "app.focus_and_safe_type_text",
    "app.open_and_safe_shortcut",
    "app.open_and_click_ui_element",
    "app.open_and_hotkey",
    "app.open_and_safe_click",
    "app.open_and_safe_key",
    "app.open_and_safe_scroll",
    "app.open_and_safe_type_text",
    "app.focus_and_safe_shortcut",
    "app.focus_and_type_into_ui_element",
    "app.open_and_type_into_ui_element",
    "desktop.active_window",
    "desktop.click_ui_element",
    "desktop.hotkey",
    "desktop.open_path_with_app",
    "app.open_path_with_app",
    "desktop.read_ui",
    "desktop.safe_click",
    "desktop.safe_key",
    "desktop.safe_scroll",
    "desktop.safe_shortcut",
    "desktop.safe_type_text",
    "desktop.search_submit",
    "desktop.shortcut",
    "desktop.submit_foreground",
    "desktop.type_text",
    "desktop.type_into_ui_element",
    "desktop.ui_elements",
    "clipboard.write",
    "screen.capture",
    "terminal.run",
    "workspace.read",
}

_MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS = 6
_RUNTIME_SEMANTIC_ARTIFACT_RETRY_AUTHORITY = object()

_RUNTIME_ORCHESTRATION_SCOPE_KEYS = (
    "group_run_id",
    "run_group_id",
    "group_id",
    "workflow_run_id",
    "workflow_id",
    "workflow_node_id",
    "workflow_node_label",
    "workflow_node_kind",
)


def _model_followup_pending_plan_requests(
    followup_context: Mapping[str, Any] | None,
    allowed_tools: Iterable[str],
    *,
    generated_content: str = "",
    timeline: list[dict[str, Any]] | None = None,
    allow_materialization_rebind: bool = False,
    semantic_artifact_retry: Mapping[str, Any] | None = None,
) -> list[dict[str, Any]]:
    if not isinstance(followup_context, Mapping):
        return []
    raw_steps = (
        _model_followup_materialization_rebind_steps(
            followup_context,
            timeline=timeline,
            semantic_artifact_retry=semantic_artifact_retry,
        )
        if allow_materialization_rebind
        else _model_followup_auto_executable_pending_steps(
            followup_context,
            timeline=timeline,
        )
    )
    if not raw_steps:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planning_reason = str(
        followup_context.get("planning_reason") or "planner_followup_pending_plan"
    ).strip()
    requests: list[dict[str, Any]] = []
    for raw_step in raw_steps:
        if not isinstance(raw_step, Mapping):
            continue
        request = _model_followup_pending_plan_request(
            raw_step,
            allowed,
            planning_reason=planning_reason,
            generated_content=generated_content,
            followup_context=followup_context,
        )
        if not request:
            break
        _attach_model_followup_pending_plan_trace_metadata(
            request,
            raw_step,
            followup_context,
        )
        request = _pending_plan_request_with_timeline_resolved_app(
            request,
            raw_step,
            followup_context,
            timeline or [],
        )
        requests.append(request)
        if len(requests) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return requests


def _model_followup_pending_plan_continuation_requests(
    followup_context: Mapping[str, Any] | None,
    completed_tool_requests: Iterable[Mapping[str, Any]],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> list[dict[str, Any]]:
    if not isinstance(followup_context, Mapping):
        return []
    steps = [
        step
        for step in _model_followup_pending_trace_items(followup_context)
        if isinstance(step, Mapping)
    ]
    if not steps:
        return []
    completed_requests = [
        request for request in completed_tool_requests if isinstance(request, Mapping)
    ]
    if not completed_requests:
        return []
    completed_step_ids = _model_followup_completed_step_ids(
        completed_requests,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    if not completed_step_ids:
        return []
    last_completed_index = _model_followup_last_completed_step_index(
        completed_requests,
        steps,
        timeline,
        tool_timeline_start=tool_timeline_start,
    )
    if last_completed_index < 0:
        return []
    for step in steps[: last_completed_index + 1]:
        step_id = str(step.get("step_id") or "").strip()
        if step_id:
            completed_step_ids.add(step_id)
    completed_step_ids.update(
        _model_followup_completed_step_ids_from_timeline(
            timeline,
            steps,
        )
    )
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planning_reason = str(
        followup_context.get("planning_reason") or "planner_followup_pending_plan"
    ).strip()
    requests: list[dict[str, Any]] = []
    authoritative_statuses, _has_complete_task_state = (
        _model_followup_authoritative_step_statuses(followup_context, steps)
    )
    for step in steps[last_completed_index + 1 :]:
        step_id = str(step.get("step_id") or "").strip()
        if step_id and step_id in completed_step_ids:
            continue
        status = authoritative_statuses.get(step_id) or _model_followup_step_status(step)
        if status in _MODEL_FOLLOWUP_NON_EXECUTABLE_STEP_STATUSES:
            break
        dependencies = _string_list(step.get("depends_on"))
        if any(dependency not in completed_step_ids for dependency in dependencies):
            break
        request = _model_followup_pending_plan_request(
            step,
            allowed,
            planning_reason=planning_reason,
            followup_context=followup_context,
        )
        if not request:
            break
        _attach_model_followup_pending_plan_trace_metadata(
            request,
            step,
            followup_context,
        )
        request = _pending_plan_request_with_timeline_resolved_app(
            request,
            step,
            followup_context,
            timeline,
        )
        requests.append(request)
        if len(requests) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return requests


def _model_followup_pending_plan_replan_payloads(
    followup_context: Mapping[str, Any],
    planned_tool_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
    run_id: str = "",
) -> list[dict[str, Any]]:
    if not isinstance(followup_context, Mapping):
        return []
    steps = [
        step
        for step in _model_followup_pending_trace_items(followup_context)
        if isinstance(step, Mapping)
    ]
    requests = [
        request for request in planned_tool_requests if isinstance(request, Mapping)
    ]
    if not steps or not requests:
        return []
    tool_events = [
        event
        for event in timeline[tool_timeline_start:]
        if isinstance(event, Mapping)
        and str(event.get("event") or "").strip()
        in {"agent.tool.call", "agent.tool.failed", "agent.tool.skipped"}
    ]
    if not tool_events:
        return []
    payloads: list[dict[str, Any]] = []
    step_cursor = 0
    event_cursor = 0
    for request in requests:
        step_index, step = _next_matching_model_followup_pending_step(
            request,
            steps,
            start_index=step_cursor,
        )
        if step_index >= 0:
            step_cursor = step_index + 1
        else:
            step = None
        event_index, tool_event = _next_matching_model_followup_tool_event(
            request,
            tool_events,
            start_index=event_cursor,
        )
        if event_index >= 0:
            event_cursor = event_index + 1
        if not tool_event:
            continue
        result = (
            dict(tool_event.get("result"))
            if isinstance(tool_event.get("result"), Mapping)
            else {}
        )
        if tool_event.get("verification_failed") is True:
            result["verification_failed"] = True
        if not _tool_result_requests_replan(result):
            continue
        step_mapping = step if isinstance(step, Mapping) else {}
        source_step_id = str(
            request.get("step_id")
            or request.get("planner_step_id")
            or step_mapping.get("step_id")
            or ""
        ).strip()
        source_tool_name = str(
            request.get("tool")
            or request.get("tool_name")
            or step_mapping.get("tool_name")
            or tool_event.get("detail")
            or ""
        ).strip()
        trigger = _model_followup_pending_plan_replan_trigger(
            request,
            step_mapping,
            tool_event,
            result,
        )
        target_capability_id = str(
            request.get("capability_id") or step_mapping.get("capability_id") or ""
        ).strip()
        failure_event_type = str(tool_event.get("event") or "agent.tool.call").strip()
        failure_detail = _model_followup_pending_plan_failure_detail(
            tool_event,
            result,
        )
        fallback_tools = (
            _string_list(step_mapping.get("fallback_tools"))
            or _string_list(request.get("fallback_tools"))
            or _model_followup_default_replan_fallback_tools(
                source_tool_name,
                trigger=trigger,
                capability_id=target_capability_id,
            )
        )
        task_context = _model_followup_task_core_replan_context(
            followup_context,
            source_step_id,
            matched_step_id=str(step_mapping.get("step_id") or source_step_id).strip(),
        )
        metadata = {
            **_runtime_trace_metadata_from_mapping(followup_context),
            **_runtime_trace_metadata_from_mapping(request),
            **_runtime_trace_metadata_from_mapping(tool_event),
            **_runtime_replan_failure_metadata(result),
            "original_intent_kind": str(
                request.get("intent_kind")
                or followup_context.get("intent_kind")
                or ""
            ).strip(),
            "input_preview": _model_followup_request_input_preview(request, tool_event),
            "result_preview": _task_progress_result_preview(result),
        }
        if task_context:
            metadata["task_core_context"] = task_context
        metadata = {
            key: value
            for key, value in metadata.items()
            if value not in (None, "", [], {})
        }
        payload: dict[str, Any] = {
            "request_id": _model_followup_replan_stable_id(
                "replan-request",
                followup_context.get("decision_id"),
                followup_context.get("plan_id"),
                source_step_id,
                source_tool_name,
                trigger,
                failure_detail,
            ),
            "trigger": trigger,
            "status": "requested",
            # This request is emitted only after the trusted Runner observed
            # the terminal tool result. The planner describes the fallback,
            # but it is not the authority for the failure fact.
            "source": "runtime_tool_request_runner",
            "run_id": str(run_id or request.get("run_id") or followup_context.get("run_id") or "").strip(),
            "task_id": str(request.get("task_id") or followup_context.get("task_id") or "").strip(),
            "decision_id": str(
                request.get("decision_id") or followup_context.get("decision_id") or ""
            ).strip(),
            "plan_id": str(request.get("plan_id") or followup_context.get("plan_id") or "").strip(),
            "core_id": str(request.get("core_id") or followup_context.get("core_id") or "").strip(),
            "source_step_id": source_step_id,
            "source_tool_name": source_tool_name,
            "target_capability_id": target_capability_id,
            "condition": _model_followup_pending_plan_replan_condition(trigger),
            "reason": _model_followup_pending_plan_replan_reason(
                trigger,
                source_step_id=source_step_id,
                source_tool_name=source_tool_name,
            ),
            "failure_event_type": failure_event_type,
            "failure_detail": failure_detail,
            "fallback_tools": fallback_tools,
            "metadata": metadata,
        }
        for key in _RUNTIME_ORCHESTRATION_SCOPE_KEYS:
            value = str(request.get(key) or followup_context.get(key) or "").strip()
            if value:
                payload[key] = value
        prompt = _model_followup_pending_plan_replan_prompt(payload, task_context)
        if prompt:
            payload["replan_prompt"] = prompt
        payloads.append(
            {key: value for key, value in payload.items() if value not in ("", [], {})}
        )
    return payloads


def _next_matching_model_followup_tool_event(
    request: Mapping[str, Any],
    tool_events: list[Mapping[str, Any]],
    *,
    start_index: int = 0,
) -> tuple[int, Mapping[str, Any] | None]:
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    for index in range(max(0, start_index), len(tool_events)):
        event = tool_events[index]
        event_tool = str(event.get("detail") or event.get("tool") or "").strip()
        event_step_id = str(
            event.get("step_id") or event.get("planner_step_id") or ""
        ).strip()
        if step_id and event_step_id and step_id != event_step_id:
            continue
        if tool_name and event_tool and tool_name != event_tool:
            continue
        if step_id or tool_name:
            return index, event
    return -1, None


def _model_followup_pending_plan_replan_trigger(
    request: Mapping[str, Any],
    step: Mapping[str, Any],
    event: Mapping[str, Any],
    result: Mapping[str, Any],
) -> str:
    runtime_stage = str(
        request.get("runtime_stage") or step.get("runtime_stage") or ""
    ).strip()
    runtime_role = str(
        request.get("runtime_role") or step.get("runtime_role") or ""
    ).strip()
    if (
        result.get("verification_failed") is True
        or runtime_stage == "verify"
        or runtime_role == "verify_result"
    ):
        return "verification_failed"
    haystack = " ".join(
        [
            str(event.get("event") or ""),
            str(event.get("detail") or ""),
            str(result.get("error") or ""),
            str(result.get("hint") or ""),
            str(result.get("summary") or ""),
            str(result.get("stderr") or ""),
        ]
    ).lower()
    if any(
        marker in haystack
        for marker in (
            "unavailable",
            "not available",
            "not allowed",
            "not enabled",
            "missing permission",
            "missing tool",
        )
    ):
        return "tool_unavailable"
    return "tool_failure"


def _model_followup_pending_plan_failure_detail(
    event: Mapping[str, Any],
    result: Mapping[str, Any],
) -> str:
    parts = [
        str(result.get("error") or "").strip(),
        str(result.get("hint") or "").strip(),
        str(result.get("summary") or "").strip(),
        str(result.get("stderr") or "").strip()[:500],
        str(result.get("stdout") or "").strip()[:500],
    ]
    for part in parts:
        if part:
            return part
    exit_code = result.get("exit_code", result.get("returncode"))
    if exit_code not in (None, "", 0, "0"):
        return f"{str(event.get('detail') or 'tool').strip()} exited with {exit_code}"
    return str(event.get("detail") or event.get("event") or "tool failure").strip()


def _model_followup_request_input_preview(
    request: Mapping[str, Any],
    event: Mapping[str, Any],
) -> dict[str, Any]:
    input_preview = (
        event.get("input_preview")
        if isinstance(event.get("input_preview"), Mapping)
        else {}
    )
    if input_preview:
        return dict(input_preview)
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    return dict(request_input)


def _model_followup_pending_plan_replan_condition(trigger: str) -> str:
    if trigger == "verification_failed":
        return "Pending verification step failed."
    if trigger == "tool_unavailable":
        return "Pending plan tool was unavailable or blocked by missing capability."
    return "Pending plan tool failed."


def _model_followup_pending_plan_replan_reason(
    trigger: str,
    *,
    source_step_id: str,
    source_tool_name: str,
) -> str:
    target = source_step_id or source_tool_name or "pending step"
    if trigger == "verification_failed":
        return f"Runtime requested a replan after verification failed for {target}."
    if trigger == "tool_unavailable":
        return f"Runtime requested a replan because a required tool was unavailable for {target}."
    return f"Runtime requested a replan after a failed pending step: {target}."


def _model_followup_default_replan_fallback_tools(
    tool_name: str,
    *,
    trigger: str,
    capability_id: str,
) -> list[str]:
    if trigger == "verification_failed" or tool_name == "terminal.run":
        return ["workspace.read", "workspace.write_patch", "terminal.run"]
    if tool_name.startswith("app.") or tool_name.startswith("desktop."):
        return ["screen.capture", "desktop.read_ui", "desktop.active_window"]
    if capability_id.startswith("data."):
        return ["workspace.read", "data.analyze", "artifact.write"]
    return []


def _model_followup_task_core_replan_context(
    followup_context: Mapping[str, Any],
    source_step_id: str,
    *,
    matched_step_id: str = "",
) -> dict[str, Any]:
    task_core = (
        followup_context.get("task_core")
        if isinstance(followup_context.get("task_core"), Mapping)
        else {}
    )
    if not task_core:
        return {}
    workspace = (
        task_core.get("workspace")
        if isinstance(task_core.get("workspace"), Mapping)
        else {}
    )
    plan_step = str(matched_step_id or source_step_id or "").strip()
    context: dict[str, Any] = {
        "core_id": str(task_core.get("core_id") or "").strip(),
        "workspace_id": str(workspace.get("workspace_id") or "").strip(),
        "workspace_title": str(workspace.get("title") or "").strip(),
        "source_step_id": str(source_step_id or "").strip(),
        "planner_step_id": plan_step,
    }
    workspace_items = _model_followup_task_core_context_rows(
        workspace.get("items") if isinstance(workspace.get("items"), list) else [],
        step_key="source_step_id",
        step_id=plan_step,
        keys=("item_id", "title", "kind", "path", "status", "source_step_id"),
        limit=8,
    )
    todos = _model_followup_task_core_context_rows(
        task_core.get("todos") if isinstance(task_core.get("todos"), list) else [],
        step_key="step_id",
        step_id=plan_step,
        keys=(
            "todo_id",
            "title",
            "status",
            "step_id",
            "tool_name",
            "capability_id",
            "approval_required",
        ),
        limit=8,
    )
    checkpoints = _model_followup_task_core_context_rows(
        task_core.get("checkpoints")
        if isinstance(task_core.get("checkpoints"), list)
        else [],
        step_key="after_step_id",
        step_id=plan_step,
        keys=("checkpoint_id", "title", "status", "after_step_id", "verifies"),
        limit=5,
    )
    replan_signals = _model_followup_task_core_context_rows(
        task_core.get("replan_signals")
        if isinstance(task_core.get("replan_signals"), list)
        else [],
        step_key="source_step_id",
        step_id=plan_step,
        keys=("signal_id", "trigger", "source_step_id", "target", "fallback_tools"),
        limit=5,
    )
    if workspace_items:
        context["workspace_items"] = workspace_items
    if todos:
        context["todos"] = todos
    if checkpoints:
        context["checkpoints"] = checkpoints
    if replan_signals:
        context["replan_signals"] = replan_signals
    return {key: value for key, value in context.items() if value not in ("", [], {})}


def _model_followup_task_core_context_rows(
    values: Iterable[Any],
    *,
    step_key: str,
    step_id: str,
    keys: tuple[str, ...],
    limit: int,
) -> list[dict[str, Any]]:
    rows: list[dict[str, Any]] = []
    for value in values:
        if not isinstance(value, Mapping):
            continue
        if step_id and str(value.get(step_key) or value.get("step_id") or "").strip() != step_id:
            continue
        row = {
            key: value.get(key)
            for key in keys
            if value.get(key) not in (None, "", [], {})
        }
        if row:
            rows.append(dict(row))
        if len(rows) >= limit:
            break
    return rows


def _model_followup_pending_plan_replan_prompt(
    payload: Mapping[str, Any],
    task_context: Mapping[str, Any],
) -> str:
    parts = [
        "Runtime replan request:",
        f"- trigger: {payload.get('trigger')}",
    ]
    for label, key in (
        ("failed_step", "source_step_id"),
        ("failed_tool", "source_tool_name"),
        ("target_capability", "target_capability_id"),
        ("failure_detail", "failure_detail"),
    ):
        value = str(payload.get(key) or "").strip()
        if value:
            parts.append(f"- {label}: {value}")
    fallback_tools = _string_list(payload.get("fallback_tools"))
    if fallback_tools:
        parts.append(f"- preferred_fallback_tools: {', '.join(fallback_tools)}")
    if task_context:
        parts.append("- task_workspace: continue from existing task_core_context")
    parts.append(
        "Continue from the existing task workspace; do not restart completed steps."
    )
    return "\n".join(parts)


def _model_followup_replan_stable_id(prefix: str, *parts: Any) -> str:
    raw = "|".join(str(part or "") for part in parts)
    digest = hashlib.sha256(raw.encode("utf-8")).hexdigest()[:12]
    return f"{prefix}-{digest}"


def _model_followup_completed_step_ids(
    completed_tool_requests: Iterable[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> set[str]:
    completed: set[str] = set()
    for request in completed_tool_requests:
        if not isinstance(request, Mapping):
            continue
        if not _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        ):
            continue
        step_id = str(
            request.get("step_id") or request.get("planner_step_id") or ""
        ).strip()
        if step_id:
            completed.add(step_id)
    return completed


def _model_followup_last_completed_step_index(
    completed_tool_requests: Iterable[Mapping[str, Any]],
    steps: list[Mapping[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    tool_timeline_start: int,
) -> int:
    last_index = -1
    for request in completed_tool_requests:
        if not isinstance(request, Mapping):
            continue
        if not _runtime_planner_tool_request_completed(
            request,
            timeline,
            tool_timeline_start=tool_timeline_start,
        ):
            continue
        step_index, _step = _next_matching_model_followup_pending_step(
            request,
            steps,
            start_index=0,
        )
        if step_index >= 0:
            last_index = max(last_index, step_index)
    return last_index


def _model_followup_completed_step_ids_from_timeline(
    timeline: list[dict[str, Any]],
    steps: list[Mapping[str, Any]],
) -> set[str]:
    known_steps = {
        str(step.get("step_id") or "").strip()
        for step in steps
        if str(step.get("step_id") or "").strip()
    }
    if not known_steps:
        return set()
    completed: set[str] = set()
    for event in timeline:
        if not isinstance(event, Mapping):
            continue
        step_id = str(event.get("step_id") or event.get("planner_step_id") or "").strip()
        if not step_id or step_id not in known_steps:
            continue
        event_type = str(event.get("event") or event.get("event_type") or "").strip()
        if event_type in {
            "agent.task.todo.updated",
            "agent.task.checkpoint.updated",
            "agent.task.workspace_item.updated",
        } and str(event.get("status") or "").strip() == "completed":
            completed.add(step_id)
            continue
        if event_type != "agent.tool.call":
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is True and not result.get("approval_required"):
            completed.add(step_id)
    return completed


def _attach_model_followup_pending_plan_trace_metadata(
    request: dict[str, Any],
    step: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> None:
    # Input bindings are planner authority, never model authority.  Model
    # requests are reduced to protocol/tool/input before this point, while
    # runtime-generated follow-ups may already carry metadata; in both cases
    # the exact pending step is canonical and replaces any supplied binding.
    request.pop("input_bindings", None)
    raw_input_bindings = step.get("input_bindings")
    if isinstance(raw_input_bindings, list):
        input_bindings = [
            dict(binding)
            for binding in raw_input_bindings
            if isinstance(binding, Mapping)
        ]
        if input_bindings:
            request["input_bindings"] = input_bindings
    task_todo = step.get("task_todo") if isinstance(step.get("task_todo"), Mapping) else {}
    task_metadata = (
        task_todo.get("metadata")
        if isinstance(task_todo.get("metadata"), Mapping)
        else {}
    )
    if any(
        source.get("approval_required") is True
        for source in (step, task_todo, task_metadata)
    ):
        request["approval_required"] = True
    for key in ("risk_level", "policy_reason"):
        value = str(
            step.get(key)
            or task_todo.get(key)
            or task_metadata.get(key)
            or ""
        ).strip()
        if value:
            request[key] = value
    for key in (
        "request_id",
        "decision_id",
        "plan_id",
        "tool_plan_id",
        "intent_kind",
        "core_id",
        "workspace_id",
        "task_id",
        "run_id",
        "replan_request_id",
        "replan_trigger",
    ):
        value = str(
            request.get(key)
            or step.get(key)
            or followup_context.get(key)
            or ""
        ).strip()
        if value:
            request[key] = value
    if str(request.get("step_id") or "").strip() and not str(
        request.get("planner_step_id") or ""
    ).strip():
        request["planner_step_id"] = str(request.get("step_id") or "").strip()
    for key in _RUNTIME_ORCHESTRATION_SCOPE_KEYS:
        if key in request:
            continue
        value = str(step.get(key) or followup_context.get(key) or "").strip()
        if value:
            request[key] = value
    for key in (
        "runtime_doctrine",
        "runtime_stage",
        "runtime_role",
        "requires_observation",
        "requires_post_action_verification",
    ):
        if key in request:
            continue
        value = step.get(key)
        if value not in (None, "", [], {}):
            request[key] = value
    for key in (
        "depends_on",
        "fallback_tools",
        "replan_triggers",
        "replan_signal_ids",
        "verified_step_ids",
        "verification_target_step_ids",
    ):
        if key in request:
            continue
        values = _string_list(step.get(key))
        if values:
            request[key] = values
    for key in (
        "task_todo",
        "task_checkpoints",
        "task_workspace_items",
        "verification_targets",
        "task_verification_targets",
    ):
        if key in request:
            continue
        value = step.get(key)
        if isinstance(value, Mapping) and value:
            request[key] = dict(value)
        elif isinstance(value, list) and value:
            request[key] = [dict(item) for item in value if isinstance(item, Mapping)]
    if "verification_target_step_ids" not in request:
        targets = _mapping_list(request.get("task_verification_targets"))
        if not targets:
            targets = _mapping_list(request.get("verification_targets"))
        target_step_ids = [
            str(target.get("step_id") or "").strip()
            for target in targets
            if str(target.get("step_id") or "").strip()
        ]
        if target_step_ids:
            request["verification_target_step_ids"] = list(
                dict.fromkeys(target_step_ids)
            )
    for key in (
        "action_target",
        "observation_evidence",
        "observation_retry",
        "checkpoint_policy",
        "desktop_loop",
    ):
        if key in request:
            continue
        value = step.get(key)
        if isinstance(value, Mapping) and value:
            request[key] = dict(value)


def _pending_plan_request_with_timeline_resolved_app(
    request: dict[str, Any],
    step: Mapping[str, Any],
    followup_context: Mapping[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any]:
    request_input = (
        dict(request.get("input"))
        if isinstance(request.get("input"), Mapping)
        else {}
    )
    step_input = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    raw_app_name = str(
        request_input.get("app_name")
        or step_input.get("app_name")
        or request.get("target_app_name")
        or ""
    ).strip()
    if raw_app_name and not _runtime_planner_placeholder_app_name(raw_app_name):
        return request
    if not raw_app_name and "app_name" not in request_input and "app_name" not in step_input:
        return request

    followup_target = (
        followup_context.get("followup_target")
        if isinstance(followup_context.get("followup_target"), Mapping)
        else {}
    )
    app_query = str(
        request_input.get("query")
        or request_input.get("app_query")
        or step_input.get("query")
        or step_input.get("app_query")
        or request.get("target_app_query")
        or followup_target.get("app_query")
        or followup_target.get("target_app_query")
        or ""
    ).strip()
    discovered = _discovered_app_name_for_query(timeline, app_query) if app_query else ""
    if not discovered:
        discovered = _latest_replan_context_app_name(
            timeline,
            request_id=str(request.get("replan_request_id") or "").strip(),
        )
    if not discovered or _runtime_planner_placeholder_app_name(discovered):
        return request

    resolved = dict(request)
    tool_name = str(resolved.get("tool") or resolved.get("tool_name") or "").strip()
    if tool_name.startswith("app.") or raw_app_name:
        request_input["app_name"] = discovered
    for key in ("selection_source", "app_selection_source", "query", "app_query"):
        request_input.pop(key, None)
    resolved["input"] = request_input
    resolved["target_app_name"] = discovered
    if app_query:
        resolved["target_app_query"] = app_query
    for key in ("followup_target", "action_target"):
        nested = resolved.get(key) if isinstance(resolved.get(key), Mapping) else {}
        if not nested:
            continue
        item = dict(nested)
        nested_app_name = str(item.get("app_name") or item.get("target_app_name") or "").strip()
        if not nested_app_name or _runtime_planner_placeholder_app_name(nested_app_name):
            item["app_name"] = discovered
            if "target_app_name" in item:
                item["target_app_name"] = discovered
        if app_query:
            if key == "followup_target" or "app_query" in item:
                item["app_query"] = app_query
            if "target_app_query" in item:
                item["target_app_query"] = app_query
        resolved[key] = item
    if app_query and (tool_name.startswith("app.") or raw_app_name):
        resolved = _with_discovered_app_resolution(resolved, app_query, discovered)
    return resolved


def _model_followup_requests_with_pending_plan_metadata(
    requests: list[dict[str, Any]],
    followup_context: Mapping[str, Any] | None,
    *,
    trusted_request_metadata: bool = False,
) -> list[dict[str, Any]]:
    if not requests:
        return requests
    if not trusted_request_metadata:
        requests = [
            _model_followup_request_protocol_payload(request)
            for request in requests
            if isinstance(request, Mapping)
        ]
    if not isinstance(followup_context, Mapping):
        return requests
    require_executable_binding = (
        trusted_request_metadata
        and _model_followup_context_has_authoritative_execution_state(
            followup_context
        )
    )
    raw_steps = _model_followup_pending_trace_items(followup_context)
    if not raw_steps:
        return [] if require_executable_binding else requests
    steps = (
        _model_followup_auto_executable_pending_steps(
            followup_context,
            raw_steps=raw_steps,
        )
        if require_executable_binding
        else [step for step in raw_steps if isinstance(step, Mapping)]
    )
    if not steps:
        return [] if require_executable_binding else requests
    annotated: list[dict[str, Any]] = []
    cursor = 0
    for request in requests:
        if not isinstance(request, dict):
            continue
        item = dict(request)
        step_index, step = _next_matching_model_followup_pending_step(
            item,
            steps,
            start_index=cursor,
        )
        if step_index >= 0 and step:
            cursor = step_index + 1
            _attach_model_followup_pending_plan_trace_metadata(
                item,
                step,
                followup_context,
            )
            if not str(item.get("step_id") or "").strip():
                step_id = str(step.get("step_id") or "").strip()
                if step_id:
                    item["step_id"] = step_id
            if not str(item.get("capability_id") or "").strip():
                capability_id = str(step.get("capability_id") or "").strip()
                if capability_id:
                    item["capability_id"] = capability_id
            if str(item.get("step_id") or "").strip() and not str(
                item.get("planner_step_id") or ""
            ).strip():
                item["planner_step_id"] = str(item.get("step_id") or "").strip()
            item = _resolve_dynamic_pending_plan_request_input(
                item,
                step,
                followup_context,
            )
        elif require_executable_binding:
            # Runtime-generated compatibility follow-ups are never an
            # alternate execution authority.  Once planner state exists, an
            # exact executable step must re-bind every generated request;
            # blocked, approval-waiting, dependency-unsatisfied, and stale
            # requests therefore fail closed instead of escaping through the
            # legacy followup_target lane.
            continue
        annotated.append(item)
    return annotated


def _model_followup_request_protocol_payload(
    request: Mapping[str, Any],
) -> dict[str, Any]:
    """Keep model-owned protocol data, then rebuild runtime correlation metadata."""

    return {
        key: request[key]
        for key in (
            "protocol",
            "tool",
            "input",
            "tool_call_id",
            "call_id",
            "id",
        )
        if key in request
    }


def _model_followup_pending_trace_items(
    followup_context: Mapping[str, Any],
) -> list[Mapping[str, Any]]:
    pending_execution_requests = followup_context.get("pending_execution_requests")
    if isinstance(pending_execution_requests, list):
        return [
            request
            for request in pending_execution_requests
            if isinstance(request, Mapping)
        ]
    pending_plan_steps = followup_context.get("pending_plan_steps")
    if isinstance(pending_plan_steps, list):
        return [step for step in pending_plan_steps if isinstance(step, Mapping)]
    return _model_followup_task_core_pending_trace_items(followup_context)


_MODEL_FOLLOWUP_NON_EXECUTABLE_STEP_STATUSES = frozenset(
    {
        "blocked",
        "cancelled",
        "canceled",
        "failed",
        "skipped",
        "terminal",
        "unavailable",
        "waiting_approval",
    }
)

_MODEL_FOLLOWUP_COMPLETED_STEP_STATUSES = frozenset(
    {"completed", "succeeded", "success", "verified"}
)


def _model_followup_context_has_authoritative_execution_state(
    followup_context: Mapping[str, Any],
) -> bool:
    for key in ("pending_execution_requests", "pending_plan_steps"):
        if isinstance(followup_context.get(key), list):
            return True
    for key in (
        "task_core",
        "task_progress",
        "yachiyo_execution_envelope",
        "execution_envelope",
        "runtime_execution_envelope",
    ):
        if isinstance(followup_context.get(key), Mapping) and followup_context.get(key):
            return True
    return isinstance(followup_context.get("replan_requests"), list) and bool(
        followup_context.get("replan_requests")
    )


def _model_followup_step_status(
    step: Mapping[str, Any],
    *,
    default: str = "planned",
) -> str:
    task_todo = step.get("task_todo") if isinstance(step.get("task_todo"), Mapping) else {}
    return str(
        task_todo.get("status")
        or step.get("runtime_status")
        or step.get("status")
        or default
    ).strip()


def _model_followup_authoritative_step_statuses(
    followup_context: Mapping[str, Any],
    raw_steps: Iterable[Mapping[str, Any]],
) -> tuple[dict[str, str], bool]:
    statuses: dict[str, str] = {}

    def record(step_id: Any, status: Any) -> None:
        clean_step_id = str(step_id or "").strip()
        clean_status = str(status or "").strip()
        if clean_step_id and clean_status:
            statuses[clean_step_id] = clean_status

    for step in raw_steps:
        if isinstance(step, Mapping):
            record(step.get("step_id") or step.get("planner_step_id"), _model_followup_step_status(step))

    task_progress = (
        followup_context.get("task_progress")
        if isinstance(followup_context.get("task_progress"), Mapping)
        else {}
    )
    for todo in _mapping_list(task_progress.get("todos")):
        record(todo.get("step_id"), todo.get("status"))
    for status, key in (
        ("completed", "completed_steps"),
        ("blocked", "blocked_steps"),
        ("waiting_approval", "waiting_approval_steps"),
    ):
        for step_id in _string_list(task_progress.get(key)):
            record(step_id, status)

    task_core = (
        followup_context.get("task_core")
        if isinstance(followup_context.get("task_core"), Mapping)
        else {}
    )
    task_core_todos = _mapping_list(task_core.get("todos"))
    for todo in task_core_todos:
        record(todo.get("step_id"), todo.get("status"))
    for checkpoint in _mapping_list(task_core.get("checkpoints")):
        checkpoint_status = str(checkpoint.get("status") or "").strip()
        step_id = str(
            checkpoint.get("after_step_id") or checkpoint.get("step_id") or ""
        ).strip()
        current_status = statuses.get(step_id, "")
        if (
            checkpoint_status in _MODEL_FOLLOWUP_NON_EXECUTABLE_STEP_STATUSES
            and current_status not in _MODEL_FOLLOWUP_COMPLETED_STEP_STATUSES
        ):
            record(step_id, checkpoint_status)
    return statuses, bool(task_core_todos)


def _model_followup_auto_executable_pending_steps(
    followup_context: Mapping[str, Any],
    *,
    raw_steps: Iterable[Mapping[str, Any]] | None = None,
    timeline: Iterable[Mapping[str, Any]] | None = None,
) -> list[Mapping[str, Any]]:
    steps = [
        step
        for step in (
            raw_steps
            if raw_steps is not None
            else _model_followup_pending_trace_items(followup_context)
        )
        if isinstance(step, Mapping)
    ]
    if not steps:
        return []
    statuses, has_complete_task_state = _model_followup_authoritative_step_statuses(
        followup_context,
        steps,
    )
    authoritative_completed_step_ids = _model_followup_authoritative_completed_step_ids(
        followup_context,
        timeline or (),
    )
    for completed_step_id in authoritative_completed_step_ids:
        statuses[completed_step_id] = "completed"
    completed_step_ids = set(authoritative_completed_step_ids)
    scheduled_step_ids: set[str] = set()
    executable: list[Mapping[str, Any]] = []
    for step in steps:
        step_id = str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        status = statuses.get(step_id) or _model_followup_step_status(step)
        if (
            status in _MODEL_FOLLOWUP_COMPLETED_STEP_STATUSES
            and step_id in completed_step_ids
        ):
            continue
        if status in _MODEL_FOLLOWUP_NON_EXECUTABLE_STEP_STATUSES:
            continue
        dependencies = _string_list(step.get("depends_on"))
        dependency_unsatisfied = False
        for dependency in dependencies:
            if dependency in completed_step_ids or dependency in scheduled_step_ids:
                continue
            if has_complete_task_state or dependency in statuses:
                dependency_unsatisfied = True
                break
        if dependency_unsatisfied:
            continue
        executable.append(step)
        if step_id:
            scheduled_step_ids.add(step_id)
    return executable


def _model_followup_authoritative_completed_step_ids(
    followup_context: Mapping[str, Any],
    timeline: Iterable[Mapping[str, Any]],
) -> set[str]:
    expected_plan_id = str(followup_context.get("plan_id") or "").strip()
    expected_decision_id = str(followup_context.get("decision_id") or "").strip()
    expected_run_id = str(followup_context.get("run_id") or "").strip()
    expected_step_values = [
        *_model_followup_task_core_pending_trace_items(followup_context),
        *_model_followup_pending_trace_items(followup_context),
    ]
    expected_steps = {
        str(step.get("step_id") or step.get("planner_step_id") or "").strip(): step
        for step in expected_step_values
        if isinstance(step, Mapping)
        and str(step.get("step_id") or step.get("planner_step_id") or "").strip()
    }
    timeline_events = [event for event in timeline if isinstance(event, Mapping)]
    completed: set[str] = set()
    for event in timeline_events:
        payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else {}
        event_type = str(
            event.get("event") or event.get("event_type") or payload.get("event") or ""
        ).strip()
        if event_type not in {
            "agent.task.todo.updated",
            "agent.task.checkpoint.updated",
        }:
            continue
        status = str(event.get("status") or payload.get("status") or "").strip()
        if status not in _MODEL_FOLLOWUP_COMPLETED_STEP_STATUSES:
            continue
        plan_id = str(event.get("plan_id") or payload.get("plan_id") or "").strip()
        decision_id = str(
            event.get("decision_id") or payload.get("decision_id") or ""
        ).strip()
        run_id = str(event.get("run_id") or payload.get("run_id") or "").strip()
        if expected_plan_id and plan_id != expected_plan_id:
            continue
        if expected_decision_id and decision_id != expected_decision_id:
            continue
        if expected_run_id and run_id != expected_run_id:
            continue
        source = str(event.get("source") or payload.get("source") or "").strip()
        source_event = (
            event.get("source_event")
            if isinstance(event.get("source_event"), Mapping)
            else payload.get("source_event")
            if isinstance(payload.get("source_event"), Mapping)
            else {}
        )
        result_preview = (
            event.get("result_preview")
            if isinstance(event.get("result_preview"), Mapping)
            else payload.get("result_preview")
            if isinstance(payload.get("result_preview"), Mapping)
            else {}
        )
        step_id = str(
            event.get("step_id")
            or payload.get("step_id")
            or event.get("planner_step_id")
            or payload.get("planner_step_id")
            or ""
        ).strip()
        expected_step = expected_steps.get(step_id, {})
        expected_tool = str(
            expected_step.get("tool_name")
            or expected_step.get("tool")
            or ""
        ).strip()
        event_tool = str(event.get("tool") or payload.get("tool") or "").strip()
        source_tool_call_id = str(source_event.get("tool_call_id") or "").strip()
        visibility = str(
            event.get("visibility") or payload.get("visibility") or ""
        ).strip()
        actor = str(event.get("actor") or payload.get("actor") or "").strip()
        verification_status = str(
            event.get("verification_status")
            or payload.get("verification_status")
            or ""
        ).strip()
        verified_by_step_id = str(
            event.get("verified_by_step_id")
            or payload.get("verified_by_step_id")
            or ""
        ).strip()
        verification_tool = str(
            event.get("verification_tool")
            or payload.get("verification_tool")
            or ""
        ).strip()
        verified_task_completion = bool(
            verification_status == "verified"
            and verified_by_step_id
            and verification_tool
        )
        receipt_step_id = verified_by_step_id if verified_task_completion else step_id
        receipt_tool = verification_tool if verified_task_completion else expected_tool
        receipt_step = expected_steps.get(receipt_step_id, expected_step)
        if (
            source
            not in {
                "runtime_planner",
                "runtime_post_action_auto_verify",
            }
            or str(source_event.get("event") or "").strip() != "agent.tool.call"
            or not source_tool_call_id
            or not expected_tool
            or event_tool != receipt_tool
            or str(source_event.get("detail") or "").strip() != receipt_tool
            or result_preview.get("ok") is not True
            or result_preview.get("approval_required") is True
            or result_preview.get("verification_failed") is True
            or visibility == "public"
            or actor in {"assistant", "model", "user"}
        ):
            continue
        if not _model_followup_completion_provider_matches(
            receipt_step,
            result_preview,
        ):
            continue
        if not _model_followup_completion_has_terminal_receipt(
            timeline_events,
            run_id=run_id,
            decision_id=decision_id,
            plan_id=plan_id,
            step_id=receipt_step_id,
            tool_name=receipt_tool,
            tool_call_id=source_tool_call_id,
        ):
            continue
        if not (
            expected_tool in READ_ONLY_OBSERVATION_TOOLS
            or expected_tool in _MODEL_FOLLOWUP_PRIMARY_OBSERVATION_TOOLS
            or verified_task_completion
            or _model_followup_effectful_completion_verified(event, payload, result_preview)
        ):
            continue
        if step_id:
            completed.add(step_id)
    return completed


def _model_followup_completion_has_terminal_receipt(
    timeline: Iterable[Mapping[str, Any]],
    *,
    run_id: str,
    decision_id: str,
    plan_id: str,
    step_id: str,
    tool_name: str,
    tool_call_id: str,
) -> bool:
    for event in timeline:
        if str(event.get("event") or event.get("event_type") or "").strip() != (
            "agent.tool.call"
        ):
            continue
        if str(event.get("run_id") or "").strip() != run_id:
            continue
        if str(event.get("decision_id") or "").strip() != decision_id:
            continue
        if str(event.get("plan_id") or "").strip() != plan_id:
            continue
        if str(event.get("step_id") or event.get("planner_step_id") or "").strip() != step_id:
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() != tool_name:
            continue
        if str(event.get("tool_call_id") or "").strip() != tool_call_id:
            continue
        if str(event.get("visibility") or "").strip() == "public" or str(
            event.get("actor") or ""
        ).strip() in {"assistant", "model", "user"}:
            continue
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is True and not result.get("approval_required"):
            return True
    return False


def _model_followup_completion_provider_matches(
    expected_step: Mapping[str, Any],
    result_preview: Mapping[str, Any],
) -> bool:
    route = (
        expected_step.get("desktop_execution_route")
        if isinstance(expected_step.get("desktop_execution_route"), Mapping)
        else {}
    )
    expected_kind = str(route.get("selected_provider_kind") or "").strip()
    expected_id = str(route.get("selected_provider_id") or "").strip()
    if not expected_kind and not expected_id:
        return True
    desktop_provider = (
        result_preview.get("desktop_provider")
        if isinstance(result_preview.get("desktop_provider"), Mapping)
        else {}
    )
    provider = (
        desktop_provider.get("provider")
        if isinstance(desktop_provider.get("provider"), Mapping)
        else {}
    )
    if desktop_provider.get("routed") is not True:
        return False
    return bool(
        (not expected_kind or str(provider.get("provider_kind") or "").strip() == expected_kind)
        and (not expected_id or str(provider.get("provider_id") or "").strip() == expected_id)
    )


def _model_followup_effectful_completion_verified(
    event: Mapping[str, Any],
    payload: Mapping[str, Any],
    result_preview: Mapping[str, Any],
) -> bool:
    if any(
        result_preview.get(key) is True
        for key in (
            "content_verified",
            "launch_verified",
            "postcondition_verified",
            "verification_passed",
            "verified",
        )
    ):
        return True
    verification_status = str(
        event.get("verification_status")
        or payload.get("verification_status")
        or ""
    ).strip()
    verified_by_step_id = str(
        event.get("verified_by_step_id")
        or payload.get("verified_by_step_id")
        or ""
    ).strip()
    verification_tool = str(
        event.get("verification_tool")
        or payload.get("verification_tool")
        or ""
    ).strip()
    return bool(
        verification_status == "verified"
        and verified_by_step_id
        and verification_tool
    )


def _goal_contract_requires_semantic_artifact_assessment(
    contract: GoalContract | None,
) -> bool:
    return bool(
        contract is not None
        and any(
            SEMANTIC_ARTIFACT_ADEQUACY_PREDICATE
            in criterion.required_verification_predicates
            for criterion in contract.criteria
        )
    )


def _trusted_semantic_artifact_retry_context(
    contract: GoalContract | None,
    followup_context: Mapping[str, Any] | None,
    timeline: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    """Authorize one exact planner-tail replay after a trusted failed verdict."""

    if (
        contract is None
        or not isinstance(followup_context, Mapping)
        or str(followup_context.get("source") or "").strip()
        != "runtime_planner"
    ):
        return {}
    # A newer exact digest must be assessed before an older failed digest can
    # authorize another rewrite.
    if pending_semantic_artifact_assessment_candidates(contract, timeline):
        return {}
    assessment = runtime_goal_assessment(contract, timeline)
    if assessment.completed:
        return {}
    plan_id = str(followup_context.get("plan_id") or "").strip()
    if not plan_id:
        return {}
    task_core = (
        followup_context.get("task_core")
        if isinstance(followup_context.get("task_core"), Mapping)
        else {}
    )
    task_contract = (
        task_core.get("goal_contract")
        if isinstance(task_core.get("goal_contract"), Mapping)
        else {}
    )
    task_contract_id = str(task_contract.get("contract_id") or "").strip()
    if task_contract_id and task_contract_id != contract.contract_id:
        return {}

    steps = [
        step
        for step in _model_followup_pending_trace_items(followup_context)
        if isinstance(step, Mapping)
    ]
    step_positions: dict[str, list[int]] = {}
    for index, step in enumerate(steps):
        step_id = str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        if step_id:
            step_positions.setdefault(step_id, []).append(index)

    semantic_evidence = next(
        (
            evidence
            for evidence in reversed(assessment.evidence)
            if evidence.verification_predicate
            == SEMANTIC_ARTIFACT_ADEQUACY_PREDICATE
            and evidence.status in {"insufficient", "uncertain"}
            and not evidence.verified
            and evidence.plan_id == plan_id
        ),
        None,
    )
    if semantic_evidence is None:
        return {}
    criterion = contract.criterion(semantic_evidence.criterion_id)
    if (
        criterion is None
        or EXACT_FILE_CONTENT_PRESENT_PREDICATE
        not in criterion.required_verification_predicates
        or SEMANTIC_ARTIFACT_ADEQUACY_PREDICATE
        not in criterion.required_verification_predicates
        or semantic_evidence.source_step_id not in criterion.source_step_ids
        or semantic_evidence.verifier_step_id not in criterion.verifier_step_ids
    ):
        return {}

    exact_evidence = next(
        (
            evidence
            for evidence in reversed(assessment.evidence)
            if evidence.criterion_id == semantic_evidence.criterion_id
            and evidence.verification_predicate
            == EXACT_FILE_CONTENT_PRESENT_PREDICATE
            and evidence.verified
            and evidence.source_tool_call_id
            == semantic_evidence.source_tool_call_id
            and evidence.verifier_tool_call_id
            == semantic_evidence.verifier_tool_call_id
            and evidence.source_step_id == semantic_evidence.source_step_id
            and evidence.verifier_step_id == semantic_evidence.verifier_step_id
            and evidence.plan_id == semantic_evidence.plan_id
        ),
        None,
    )
    if exact_evidence is None:
        return {}
    observed_path = str(
        semantic_evidence.observed.get("observed_path") or ""
    ).strip()
    content_sha256 = str(
        semantic_evidence.observed.get("content_sha256") or ""
    ).strip().casefold()
    content_length = semantic_evidence.observed.get("content_length")
    exact_content = exact_evidence.observed.get("content")
    if (
        normalized_workspace_relative_path(observed_path) != observed_path
        or re.fullmatch(r"[0-9a-f]{64}", content_sha256) is None
        or isinstance(content_length, bool)
        or not isinstance(content_length, int)
        or content_length <= 0
        or not isinstance(exact_content, str)
        or not exact_content
        or len(exact_content.encode("utf-8")) != content_length
        or hashlib.sha256(exact_content.encode("utf-8")).hexdigest()
        != content_sha256
        or str(exact_evidence.observed.get("observed_path") or "").strip()
        != observed_path
        or str(exact_evidence.observed.get("content_sha256") or "")
        .strip()
        .casefold()
        != content_sha256
        or exact_evidence.observed.get("content_length") != content_length
    ):
        return {}
    expected = criterion.expected if isinstance(criterion.expected, Mapping) else {}
    expected_target = (
        expected.get("target")
        if isinstance(expected.get("target"), Mapping)
        else {}
    )
    if declared_workspace_output_path(expected_target) != observed_path:
        return {}

    source_positions = step_positions.get(semantic_evidence.source_step_id, [])
    verifier_positions = step_positions.get(semantic_evidence.verifier_step_id, [])
    if (
        len(source_positions) != 1
        or len(verifier_positions) != 1
        or source_positions[0] >= verifier_positions[0]
    ):
        return {}
    source_index = source_positions[0]
    verifier_index = verifier_positions[0]
    verifier_step = steps[verifier_index]
    verifier_input = (
        verifier_step.get("input_preview")
        if isinstance(verifier_step.get("input_preview"), Mapping)
        else verifier_step.get("input")
        if isinstance(verifier_step.get("input"), Mapping)
        else {}
    )
    if normalized_workspace_relative_path(verifier_input.get("path")) != observed_path:
        return {}

    # Compute only the dependency-connected source -> verifier slice. A
    # semantic failure cannot revive unrelated completed planner work.
    slice_steps = steps[source_index : verifier_index + 1]
    reachable = {semantic_evidence.source_step_id}
    for step in slice_steps[1:]:
        step_id = str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        if step_id and any(
            dependency in reachable
            for dependency in _string_list(step.get("depends_on"))
        ):
            reachable.add(step_id)
    if semantic_evidence.verifier_step_id not in reachable:
        return {}
    required = {semantic_evidence.verifier_step_id}
    for step in reversed(slice_steps):
        step_id = str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        if step_id in required:
            required.update(
                dependency
                for dependency in _string_list(step.get("depends_on"))
                if dependency in reachable
            )
    if semantic_evidence.source_step_id not in required:
        return {}
    retry_step_ids = tuple(
        str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        for step in slice_steps
        if str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        in required
    )
    if (
        not retry_step_ids
        or retry_step_ids[0] != semantic_evidence.source_step_id
        or retry_step_ids[-1] != semantic_evidence.verifier_step_id
    ):
        return {}
    return {
        "authority": _RUNTIME_SEMANTIC_ARTIFACT_RETRY_AUTHORITY,
        "contract_id": contract.contract_id,
        "criterion_id": criterion.criterion_id,
        "plan_id": plan_id,
        "source_step_id": semantic_evidence.source_step_id,
        "verifier_step_id": semantic_evidence.verifier_step_id,
        "observed_path": observed_path,
        "previous_content_sha256": content_sha256,
        "previous_content_length": content_length,
        "retry_step_ids": retry_step_ids,
    }


def _model_followup_materialization_rebind_steps(
    followup_context: Mapping[str, Any],
    *,
    timeline: Iterable[Mapping[str, Any]] | None = None,
    semantic_artifact_retry: Mapping[str, Any] | None = None,
) -> list[Mapping[str, Any]]:
    steps = [
        step
        for step in _model_followup_pending_trace_items(followup_context)
        if isinstance(step, Mapping)
    ]
    if not steps:
        return []
    statuses, has_complete_task_state = _model_followup_authoritative_step_statuses(
        followup_context,
        steps,
    )
    authoritative_completed_step_ids = _model_followup_authoritative_completed_step_ids(
        followup_context,
        timeline or (),
    )
    for completed_step_id in authoritative_completed_step_ids:
        statuses[completed_step_id] = "completed"
    completed_step_ids = set(authoritative_completed_step_ids)
    semantic_retry_step_ids: tuple[str, ...] = ()
    if (
        isinstance(semantic_artifact_retry, Mapping)
        and semantic_artifact_retry.get("authority")
        is _RUNTIME_SEMANTIC_ARTIFACT_RETRY_AUTHORITY
        and str(semantic_artifact_retry.get("plan_id") or "").strip()
        == str(followup_context.get("plan_id") or "").strip()
    ):
        semantic_retry_step_ids = tuple(
            dict.fromkeys(_string_list(semantic_artifact_retry.get("retry_step_ids")))
        )
    semantic_retry_allowed = set(semantic_retry_step_ids)
    scheduled_step_ids: set[str] = set()
    candidates: list[Mapping[str, Any]] = []
    terminal_statuses = _MODEL_FOLLOWUP_NON_EXECUTABLE_STEP_STATUSES.difference(
        {"blocked", "waiting_approval"}
    )
    for step in steps:
        step_id = str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        if semantic_retry_step_ids and step_id not in semantic_retry_allowed:
            continue
        status = statuses.get(step_id) or _model_followup_step_status(step)
        if (
            status in _MODEL_FOLLOWUP_COMPLETED_STEP_STATUSES
            and step_id in completed_step_ids
            and step_id not in semantic_retry_allowed
        ):
            continue
        if status in terminal_statuses and step_id not in semantic_retry_allowed:
            continue
        dependencies = _string_list(step.get("depends_on"))
        if any(
            dependency not in completed_step_ids
            and dependency not in scheduled_step_ids
            and (has_complete_task_state or dependency in statuses)
            for dependency in dependencies
        ):
            continue
        candidates.append(step)
        if step_id:
            scheduled_step_ids.add(step_id)
    if semantic_retry_step_ids and tuple(
        str(step.get("step_id") or step.get("planner_step_id") or "").strip()
        for step in candidates
    ) != semantic_retry_step_ids:
        # A semantic retry may replay only the exact Runtime-authorized tail.
        # Missing, reordered, or newly injected steps fail closed.
        return []
    return candidates


def _fresh_materialized_execution_bindings(
    requests: Iterable[Mapping[str, Any]],
    followup_context: Mapping[str, Any],
    *,
    generated_content: str,
    goal_contract: GoalContract | None,
    run_id: str = "",
) -> list[dict[str, Any]]:
    """Re-authorize a model-materialized plan tail without reviving old requests."""

    content = str(generated_content or "")
    candidates = [dict(request) for request in requests if isinstance(request, Mapping)]
    if not content.strip() or not candidates or goal_contract is None:
        return []
    source_steps = [
        step
        for step in _model_followup_pending_trace_items(followup_context)
        if isinstance(step, Mapping)
    ]
    source_by_step_id = {
        str(step.get("step_id") or step.get("planner_step_id") or "").strip(): step
        for step in source_steps
        if str(step.get("step_id") or step.get("planner_step_id") or "").strip()
    }
    materialized_source_step_id = ""
    materialized_source_request_id = ""
    for request in candidates:
        step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        source_step = source_by_step_id.get(step_id, {})
        source_tool = str(
            source_step.get("tool_name")
            or source_step.get("tool")
            or request.get("tool")
            or ""
        ).strip()
        source_input = (
            source_step.get("input_preview")
            if isinstance(source_step.get("input_preview"), Mapping)
            else source_step.get("input")
            if isinstance(source_step.get("input"), Mapping)
            else {}
        )
        if _tool_request_requires_model_materialization(
            {
                "tool": source_tool,
                "input": dict(source_input),
                "continue_to_model": True,
            }
        ):
            materialized_source_step_id = step_id
            materialized_source_request_id = str(
                source_step.get("request_id") or request.get("request_id") or ""
            ).strip()
            break
    if not materialized_source_step_id or not materialized_source_request_id:
        return []
    candidate_step_ids = {
        str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        for request in candidates
        if str(request.get("step_id") or request.get("planner_step_id") or "").strip()
    }
    criterion = next(
        (
            item
            for item in goal_contract.criteria
            if candidate_step_ids.intersection(item.source_step_ids)
        ),
        None,
    )
    if criterion is None:
        return []
    plan_id = str(
        followup_context.get("plan_id")
        or candidates[0].get("plan_id")
        or ""
    ).strip()
    if not plan_id:
        return []
    content_sha256 = hashlib.sha256(content.encode("utf-8")).hexdigest()
    binding_seed = "\n".join(
        (
            "materialization-binding-v1",
            str(run_id or "").strip(),
            goal_contract.contract_id,
            criterion.criterion_id,
            plan_id,
            materialized_source_request_id,
            content_sha256,
            json.dumps(
                [
                    _materialization_immutable_request_identity(
                        source_by_step_id.get(
                            str(
                                request.get("step_id")
                                or request.get("planner_step_id")
                                or ""
                            ).strip(),
                            {},
                        ),
                        fallback_request=request,
                    )
                    for request in candidates
                ],
                ensure_ascii=False,
                separators=(",", ":"),
                sort_keys=True,
            ),
        )
    )
    binding_id = (
        "materialization-binding-"
        f"{hashlib.sha256(binding_seed.encode('utf-8')).hexdigest()[:20]}"
    )
    fresh_request_ids: dict[str, str] = {}
    rebound: list[dict[str, Any]] = []
    for index, request in enumerate(candidates):
        step_id = str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        source_step = source_by_step_id.get(step_id, {})
        old_request_id = str(
            source_step.get("request_id") or request.get("request_id") or ""
        ).strip()
        if not step_id or not old_request_id:
            return []
        request_seed = "\n".join(
            (binding_id, str(index), old_request_id, step_id, str(request.get("tool") or ""))
        )
        fresh_request_id = (
            f"{plan_id}:materialized:"
            f"{hashlib.sha256(request_seed.encode('utf-8')).hexdigest()[:20]}"
        )
        dependencies = _string_list(request.get("depends_on"))
        rebound_request = dict(request)
        for key in ("tool_call_id", "call_id", "id"):
            rebound_request.pop(key, None)
        rebound_request.update(
            {
                "request_id": fresh_request_id,
                "materialized_from_request_id": old_request_id,
                "materialization_source_request_id": materialized_source_request_id,
                "materialization_source_step_id": materialized_source_step_id,
                "materialization_binding_id": binding_id,
                "materialized_content_sha256": content_sha256,
                "materialized_content_length": len(content),
                "materialization_authority": "runtime_planner_fresh_binding",
                "goal_contract_id": goal_contract.contract_id,
                "goal_criterion_id": criterion.criterion_id,
                "root_goal_unchanged": True,
                "plan_id": plan_id,
                "step_id": step_id,
                "planner_step_id": step_id,
            }
        )
        source_dependency = next(
            (dependency for dependency in dependencies if dependency in fresh_request_ids),
            "",
        )
        if source_dependency:
            rebound_request["source_request_id"] = fresh_request_ids[source_dependency]
            rebound_request["source_step_id"] = source_dependency
        else:
            rebound_request["source_request_id"] = old_request_id
        fresh_request_ids[step_id] = fresh_request_id
        rebound.append(rebound_request)
    return rebound


def _materialization_immutable_request_identity(
    source_step: Mapping[str, Any],
    *,
    fallback_request: Mapping[str, Any],
) -> dict[str, Any]:
    source_input = (
        source_step.get("input_preview")
        if isinstance(source_step.get("input_preview"), Mapping)
        else source_step.get("input")
        if isinstance(source_step.get("input"), Mapping)
        else fallback_request.get("input")
        if isinstance(fallback_request.get("input"), Mapping)
        else {}
    )
    mutable_body_keys = {
        "body",
        "code",
        "command",
        "content",
        "diff",
        "patch",
        "text",
    }
    immutable_input = {
        str(key): value
        for key, value in source_input.items()
        if str(key) not in mutable_body_keys
    }
    action_target = (
        source_step.get("action_target")
        if isinstance(source_step.get("action_target"), Mapping)
        else fallback_request.get("action_target")
        if isinstance(fallback_request.get("action_target"), Mapping)
        else {}
    )
    return {
        "request_id": str(
            source_step.get("request_id")
            or fallback_request.get("request_id")
            or ""
        ).strip(),
        "step_id": str(
            source_step.get("step_id")
            or source_step.get("planner_step_id")
            or fallback_request.get("step_id")
            or fallback_request.get("planner_step_id")
            or ""
        ).strip(),
        "tool": str(
            source_step.get("tool_name")
            or source_step.get("tool")
            or fallback_request.get("tool")
            or ""
        ).strip(),
        "input": immutable_input,
        "action_target": dict(action_target),
    }


def _model_followup_task_core_pending_trace_items(
    followup_context: Mapping[str, Any],
) -> list[Mapping[str, Any]]:
    task_core = (
        followup_context.get("task_core")
        if isinstance(followup_context.get("task_core"), Mapping)
        else {}
    )
    if not task_core:
        return []
    workspace = task_core.get("workspace") if isinstance(task_core.get("workspace"), Mapping) else {}
    workspace_id = str(workspace.get("workspace_id") or "").strip()
    workspace_items = workspace.get("items") if isinstance(workspace.get("items"), list) else []
    todos = [todo for todo in task_core.get("todos", []) if isinstance(todo, Mapping)]
    todos_by_step = {
        str(todo.get("step_id") or "").strip(): todo
        for todo in todos
        if str(todo.get("step_id") or "").strip()
    }
    items_by_step: dict[str, list[Mapping[str, Any]]] = {}
    for item in workspace_items:
        if not isinstance(item, Mapping):
            continue
        step_id = str(item.get("source_step_id") or "").strip()
        if step_id:
            items_by_step.setdefault(step_id, []).append(item)
    checkpoints_by_step: dict[str, list[Mapping[str, Any]]] = {}
    for checkpoint in task_core.get("checkpoints", []):
        if not isinstance(checkpoint, Mapping):
            continue
        step_id = str(checkpoint.get("after_step_id") or checkpoint.get("step_id") or "").strip()
        if step_id:
            checkpoints_by_step.setdefault(step_id, []).append(checkpoint)
    items: list[Mapping[str, Any]] = []
    for todo in todos:
        status = str(todo.get("status") or "").strip()
        if status not in {"", "pending", "planned", "waiting_approval"}:
            continue
        step_id = str(todo.get("step_id") or "").strip()
        if not step_id:
            continue
        workspace_step_items = items_by_step.get(step_id, [])
        metadata = _model_followup_task_core_step_metadata(todo, workspace_step_items)
        tool_name = str(todo.get("tool_name") or metadata.get("tool_name") or "").strip()
        if not tool_name:
            continue
        input_preview = (
            metadata.get("input_preview")
            if isinstance(metadata.get("input_preview"), Mapping)
            else {}
        )
        payload: dict[str, Any] = {
            "step_id": step_id,
            "tool_name": tool_name,
            "capability_id": str(
                todo.get("capability_id") or metadata.get("capability_id") or ""
            ).strip(),
            "input_preview": dict(input_preview),
            "status": status or "pending",
            "task_todo": dict(todo),
        }
        for key in (
            "runtime_doctrine",
            "runtime_stage",
            "runtime_role",
            "requires_observation",
            "requires_post_action_verification",
        ):
            value = metadata.get(key)
            if value not in (None, "", [], {}):
                payload[key] = value
        depends_on = _string_list(todo.get("depends_on"))
        if not depends_on:
            depends_on = _string_list(metadata.get("depends_on"))
        if depends_on:
            payload["depends_on"] = depends_on
        if bool(todo.get("approval_required")) or bool(metadata.get("approval_required")):
            payload["approval_required"] = True
        if workspace_step_items:
            payload["task_workspace_items"] = [dict(item) for item in workspace_step_items]
        checkpoints = checkpoints_by_step.get(step_id, [])
        if checkpoints:
            payload["task_checkpoints"] = [dict(item) for item in checkpoints]
        if workspace_id:
            payload["workspace_id"] = workspace_id
        core_id = str(task_core.get("core_id") or "").strip()
        if core_id:
            payload["core_id"] = core_id
        verification_targets = _model_followup_task_core_verification_targets(
            payload,
            todos_by_step=todos_by_step,
            checkpoints_by_step=checkpoints_by_step,
            workspace_items_by_step=items_by_step,
        )
        if verification_targets:
            target_step_ids = [
                str(target.get("step_id") or "").strip()
                for target in verification_targets
                if str(target.get("step_id") or "").strip()
            ]
            payload["verified_step_ids"] = target_step_ids
            payload["verification_target_step_ids"] = target_step_ids
            payload["verification_targets"] = (
                _runtime_planner_compact_task_verification_targets(
                    verification_targets
                )
            )
            payload["task_verification_targets"] = verification_targets
            checkpoint_policy = (
                dict(metadata.get("checkpoint_policy"))
                if isinstance(metadata.get("checkpoint_policy"), Mapping)
                else {}
            )
            checkpoint_policy["verification_target_step_ids"] = target_step_ids
            payload["checkpoint_policy"] = checkpoint_policy
            desktop_loop = (
                dict(metadata.get("desktop_loop"))
                if isinstance(metadata.get("desktop_loop"), Mapping)
                else {}
            )
            desktop_loop["verification_target_step_ids"] = target_step_ids
            for key, target_key in (
                ("runtime_stage", "stage"),
                ("runtime_role", "role"),
            ):
                value = str(payload.get(key) or "").strip()
                if value:
                    desktop_loop[target_key] = value
            payload["desktop_loop"] = desktop_loop
        for key in _RUNTIME_ORCHESTRATION_SCOPE_KEYS:
            value = str(followup_context.get(key) or "").strip()
            if value:
                payload[key] = value
        items.append(payload)
        if len(items) >= _MODEL_FOLLOWUP_MAX_AUTO_PENDING_REQUESTS:
            break
    return items


def _model_followup_task_core_verification_targets(
    step: Mapping[str, Any],
    *,
    todos_by_step: Mapping[str, Mapping[str, Any]],
    checkpoints_by_step: Mapping[str, list[Mapping[str, Any]]],
    workspace_items_by_step: Mapping[str, list[Mapping[str, Any]]],
) -> list[dict[str, Any]]:
    runtime_stage = str(step.get("runtime_stage") or "").strip()
    runtime_role = str(step.get("runtime_role") or "").strip()
    if runtime_stage != "verify" and runtime_role not in {
        "inspect_ui",
        "verify_result",
    }:
        return []
    target_step_ids = _ordered_text_list(_string_list(step.get("depends_on")))
    targets: list[dict[str, Any]] = []
    for step_id in target_step_ids:
        target: dict[str, Any] = {"step_id": step_id}
        todo = todos_by_step.get(step_id)
        if isinstance(todo, Mapping) and todo:
            target["todo"] = dict(todo)
        checkpoints = checkpoints_by_step.get(step_id, [])
        if checkpoints:
            target["checkpoints"] = [dict(item) for item in checkpoints]
        workspace_items = workspace_items_by_step.get(step_id, [])
        if workspace_items:
            target["workspace_items"] = [dict(item) for item in workspace_items]
        if len(target) > 1:
            targets.append(target)
    return targets


def _model_followup_task_core_step_metadata(
    todo: Mapping[str, Any],
    workspace_items: Iterable[Mapping[str, Any]],
) -> Mapping[str, Any]:
    todo_metadata = todo.get("metadata") if isinstance(todo.get("metadata"), Mapping) else {}
    for item in workspace_items:
        if not isinstance(item, Mapping):
            continue
        metadata = item.get("metadata") if isinstance(item.get("metadata"), Mapping) else {}
        if metadata:
            return {**dict(todo_metadata), **dict(metadata)}
    return dict(todo_metadata)


def _next_matching_model_followup_pending_step(
    request: Mapping[str, Any],
    steps: list[Mapping[str, Any]],
    *,
    start_index: int = 0,
) -> tuple[int, Mapping[str, Any] | None]:
    tool_name = str(request.get("tool") or "").strip()
    if not tool_name:
        return -1, None
    for index in range(max(0, start_index), len(steps)):
        step = steps[index]
        if str(step.get("tool_name") or "").strip() != tool_name:
            continue
        if not _model_followup_request_matches_pending_step(request, step):
            continue
        return index, step
    return -1, None


def _model_followup_request_matches_pending_step(
    request: Mapping[str, Any],
    step: Mapping[str, Any],
) -> bool:
    step_input = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    step_path = str(
        step_input.get("path")
        or step_input.get("artifact_path")
        or step_input.get("target_path")
        or ""
    ).strip()
    request_path = str(
        request_input.get("path")
        or request_input.get("artifact_path")
        or request_input.get("target_path")
        or ""
    ).strip()
    if step_path and request_path and step_path != request_path:
        return False
    body_source = str(step_input.get("body_source") or "").strip()
    mutable_text = body_source in {
        "analysis_artifact",
        "model_generated_content",
        "report_artifact",
    }
    immutable_keys = [
        "app_name",
        "target_app_name",
        "recipient",
        "action",
        "target",
        "role_filter",
        "title_contains",
        "query",
        "url",
    ]
    if not mutable_text:
        immutable_keys.extend(("text", "body", "content"))
    for key in immutable_keys:
        expected = step_input.get(key)
        if expected in (None, "", [], {}):
            continue
        actual = request_input.get(key)
        if actual in (None, "", [], {}):
            return False
        if isinstance(expected, str) or isinstance(actual, str):
            if str(actual).strip().casefold() != str(expected).strip().casefold():
                return False
        elif actual != expected:
            return False
    return True


def _model_followup_pending_plan_request(
    step: Mapping[str, Any],
    allowed: set[str],
    *,
    planning_reason: str,
    generated_content: str = "",
    followup_context: Mapping[str, Any] | None = None,
) -> dict[str, Any]:
    tool_name = str(step.get("tool_name") or "").strip()
    if (
        not tool_name
        or tool_name not in allowed
        or tool_name not in _MODEL_FOLLOWUP_AUTO_PENDING_TOOLS
    ):
        return {}
    raw_input = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    input_resolution: dict[str, Any] = {}
    if tool_name == "terminal.run":
        input_payload = _model_followup_terminal_pending_input(
            raw_input,
            generated_content,
        )
        if not input_payload:
            return {}
        if input_resolution:
            input_resolution["tool"] = tool_name
    elif tool_name in _OPEN_PATH_WITH_APP_TOOLS:
        input_payload, input_resolution = _model_followup_open_path_with_app_pending_input(
            raw_input,
            followup_context or {},
        )
        if not input_payload:
            return {}
    elif tool_name == "artifact.write":
        input_payload = _model_followup_artifact_pending_input(
            raw_input,
            generated_content,
            followup_context or {},
        )
        if not input_payload:
            return {}
    elif tool_name == "clipboard.write":
        input_payload = _model_followup_clipboard_pending_input(
            raw_input,
            generated_content,
        )
        if not input_payload:
            return {}
    elif tool_name == "data.analyze":
        input_payload = _model_followup_data_analyze_pending_input(
            raw_input,
            followup_context or {},
        )
        if not input_payload:
            return {}
    elif tool_name in _MODEL_FOLLOWUP_TEXT_ENTRY_TOOLS:
        input_payload, input_resolution = _model_followup_text_entry_pending_input(
            tool_name,
            raw_input,
            generated_content,
            followup_context or {},
        )
        if not input_payload:
            return {}
        if _pending_plan_binds_artifact_path(step):
            # The artifact is the durable handoff.  Do not copy the model
            # response directly into the effectful desktop request; the tool
            # runner resolves the correlated artifact path and reads its
            # broker-owned content immediately before execution.
            for key in (
                "body_source",
                "artifact_path",
                "target_action",
                "container_action",
            ):
                value = raw_input.get(key)
                if value not in (None, "", [], {}):
                    input_payload[key] = value
            input_payload.pop("text", None)
    else:
        if not raw_input and tool_name not in {
            "desktop.active_window",
            "desktop.read_ui",
            "desktop.search_submit",
            "desktop.submit_foreground",
            "desktop.ui_elements",
            "screen.capture",
        }:
            return {}
        input_payload = dict(raw_input)
        input_payload, input_resolution = _model_followup_resolved_app_input(
            tool_name,
            input_payload,
            raw_input,
            followup_context or {},
        )
    request = _request_like(
        tool_name,
        input_payload,
        source="runtime_planner",
        planning_reason=planning_reason or "planner_followup_pending_plan",
    )
    step_id = str(step.get("step_id") or "").strip()
    if step_id:
        request["step_id"] = step_id
    capability_id = str(step.get("capability_id") or "").strip()
    if capability_id:
        request["capability_id"] = capability_id
    if input_resolution:
        input_resolution["tool"] = tool_name
        request["input_resolution"] = input_resolution
    return request


def _model_followup_open_path_with_app_pending_input(
    raw_input: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> tuple[dict[str, Any], dict[str, Any]]:
    if not isinstance(raw_input, Mapping):
        raw_input = {}
    app_name = str(raw_input.get("app_name") or "").strip()
    path = str(raw_input.get("path") or raw_input.get("target_path") or "").strip()
    app_resolution: dict[str, Any] = {}
    if _pending_plan_placeholder_value(path, "workspace.list"):
        path = _resolved_followup_workspace_path(followup_context, raw_input)
    if _pending_plan_placeholder_value(app_name, "desktop.list_apps"):
        app_name, app_resolution = _resolved_followup_desktop_app_name(
            followup_context,
            raw_input,
        )
    if (
        not path
        or not app_name
        or _pending_plan_placeholder_value(path, "workspace.list")
        or _pending_plan_placeholder_value(app_name, "desktop.list_apps")
    ):
        return {}, {}
    return {"path": path, "app_name": app_name}, app_resolution


def _pending_plan_placeholder_value(value: Any, source: str) -> bool:
    text = str(value or "").strip()
    if not text:
        return True
    lowered = text.lower()
    return (
        (text.startswith("<") and text.endswith(">"))
        or source.lower() in lowered
    )


_MODEL_FOLLOWUP_TEXT_ENTRY_TOOLS = {
    "app.focus_and_safe_type_text",
    "app.open_and_safe_type_text",
    "desktop.safe_type_text",
    "desktop.type",
    "desktop.type_text",
}

_MODEL_FOLLOWUP_SNAPSHOT_BODY_SOURCES = {
    "analysis_artifact",
    "analysis_result",
    "artifact",
    "artifact_content",
    "data_analysis",
    "report_artifact",
}

_MODEL_FOLLOWUP_SNAPSHOT_BODY_SOURCE_TOOLS = {
    "artifact.write",
    "data.analyze",
}


def _model_followup_text_entry_pending_input(
    tool_name: str,
    raw_input: Mapping[str, Any],
    generated_content: str,
    followup_context: Mapping[str, Any],
) -> tuple[dict[str, Any], dict[str, Any]]:
    if not isinstance(raw_input, Mapping):
        raw_input = {}
    content = _model_followup_resolved_body_content(
        generated_content,
        raw_input,
        followup_context,
    )
    if not content:
        return {}, {}
    payload: dict[str, Any] = {"text": content}
    input_resolution: dict[str, Any] = {}
    if tool_name in {"app.focus_and_safe_type_text", "app.open_and_safe_type_text"}:
        app_name = str(raw_input.get("app_name") or "").strip()
        if not app_name or _pending_plan_placeholder_value(app_name, "desktop.list_apps"):
            payload, input_resolution = _model_followup_resolved_app_input(
                tool_name,
                payload,
                raw_input,
                followup_context,
            )
            app_name = str(payload.get("app_name") or "").strip()
        if not app_name or _pending_plan_placeholder_value(app_name, "desktop.list_apps"):
            return {}, {}
        payload["app_name"] = app_name
    return payload, input_resolution


def _pending_plan_binds_artifact_path(step: Mapping[str, Any]) -> bool:
    bindings = step.get("input_bindings")
    if not isinstance(bindings, list):
        return False
    return any(
        isinstance(binding, Mapping)
        and str(binding.get("source_tool_name") or "").strip() == "artifact.write"
        and str(binding.get("source_result_path") or "").strip() == "/path"
        and str(binding.get("target_input_path") or "").strip()
        == "/input/artifact_path"
        for binding in bindings
    )


def _model_followup_resolved_app_input(
    tool_name: str,
    input_payload: Mapping[str, Any],
    raw_input: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> tuple[dict[str, Any], dict[str, Any]]:
    payload = dict(input_payload) if isinstance(input_payload, Mapping) else {}
    source = dict(raw_input) if isinstance(raw_input, Mapping) else {}
    if "app_name" not in payload and "app_name" not in source:
        return payload, {}
    app_name = str(payload.get("app_name") or source.get("app_name") or "").strip()
    if not _pending_plan_placeholder_value(app_name, "desktop.list_apps"):
        return payload, {}
    resolved_app_name, input_resolution = _resolved_followup_desktop_app_name(
        followup_context,
        {**source, **payload},
    )
    if not resolved_app_name:
        return payload, {}
    payload["app_name"] = resolved_app_name
    if input_resolution:
        input_resolution["tool"] = str(tool_name or "").strip()
    return payload, input_resolution


def _model_followup_resolved_body_content(
    generated_content: str,
    source: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> str:
    if not isinstance(source, Mapping):
        source = {}
    direct = str(source.get("content") or source.get("text") or "").strip()
    if direct and not _pending_plan_placeholder_value(direct, "captured"):
        return direct
    body_source = str(source.get("body_source") or "").strip()
    snapshot_content = _model_followup_snapshot_body_content(
        followup_context,
        body_source,
    )
    if snapshot_content and body_source in _MODEL_FOLLOWUP_SNAPSHOT_BODY_SOURCES:
        return snapshot_content
    content = str(generated_content or "").strip()
    if content:
        return content
    return snapshot_content


def _model_followup_snapshot_body_content(
    followup_context: Mapping[str, Any],
    body_source: str,
) -> str:
    source = str(body_source or "").strip()
    if source not in _MODEL_FOLLOWUP_SNAPSHOT_BODY_SOURCES:
        return ""
    snapshots = _model_followup_readable_content_snapshots(followup_context)
    for snapshot in reversed(snapshots):
        source_tool = str(snapshot.get("source_tool") or "").strip()
        if source_tool not in _MODEL_FOLLOWUP_SNAPSHOT_BODY_SOURCE_TOOLS:
            continue
        text = str(snapshot.get("text") or "").strip()
        if text:
            return text
    return ""


def _model_followup_readable_content_snapshots(
    followup_context: Mapping[str, Any],
) -> list[Mapping[str, Any]]:
    if not isinstance(followup_context, Mapping):
        return []
    snapshots: list[Mapping[str, Any]] = []
    raw_snapshots = followup_context.get("content_snapshots")
    if isinstance(raw_snapshots, list):
        snapshots.extend(snapshot for snapshot in raw_snapshots if isinstance(snapshot, Mapping))
    raw_snapshot = followup_context.get("content_snapshot")
    if isinstance(raw_snapshot, Mapping):
        snapshots.append(raw_snapshot)
    return [
        snapshot
        for snapshot in snapshots
        if snapshot.get("ok") is not False and str(snapshot.get("text") or "").strip()
    ]


def _resolved_followup_workspace_path(
    followup_context: Mapping[str, Any],
    raw_input: Mapping[str, Any],
) -> str:
    for snapshot in reversed(_followup_snapshots_for_tool(followup_context, "workspace.list")):
        entries = snapshot.get("entries")
        if not isinstance(entries, list) or not entries:
            continue
        entry = _select_followup_workspace_entry(entries, raw_input)
        if not entry:
            continue
        path = str(entry.get("path") or "").strip()
        if path:
            return path
        name = str(entry.get("name") or "").strip()
        if not name:
            continue
        return _join_followup_workspace_path(str(snapshot.get("path") or ""), name)
    return ""


def _resolved_followup_desktop_app_name(
    followup_context: Mapping[str, Any],
    raw_input: Mapping[str, Any],
) -> tuple[str, dict[str, Any]]:
    for snapshot in reversed(_followup_snapshots_for_tool(followup_context, "desktop.list_apps")):
        apps = snapshot.get("apps")
        if not isinstance(apps, list) or not apps:
            continue
        app = _select_followup_desktop_app(apps)
        app_name = str(app.get("name") or "").strip()
        if not app_name:
            continue
        query = str(
            raw_input.get("query")
            or raw_input.get("app_query")
            or snapshot.get("query")
            or ""
        ).strip()
        resolution: dict[str, Any] = {
            "tool": "desktop.open_path_with_app",
            "field": "app_name",
            "requested_app_name": query or str(raw_input.get("app_name") or "").strip(),
            "resolved_app_name": app_name,
            "source_tool": "desktop.list_apps",
        }
        app_path = str(app.get("path") or "").strip()
        if app_path:
            resolution["resolved_app_path"] = app_path
        score = app.get("match_score") if app.get("match_score") not in (None, "") else app.get("score")
        if score not in (None, ""):
            resolution["app_resolution_score"] = str(score)
        return app_name, {
            key: value
            for key, value in resolution.items()
            if value not in ("", None)
        }
    return "", {}


def _followup_snapshots_for_tool(
    followup_context: Mapping[str, Any],
    source_tool: str,
) -> list[Mapping[str, Any]]:
    snapshots: list[Mapping[str, Any]] = []
    raw_snapshots = followup_context.get("content_snapshots")
    if isinstance(raw_snapshots, list):
        snapshots.extend(snapshot for snapshot in raw_snapshots if isinstance(snapshot, Mapping))
    raw_snapshot = followup_context.get("content_snapshot")
    if isinstance(raw_snapshot, Mapping):
        snapshots.append(raw_snapshot)
    return [
        snapshot
        for snapshot in snapshots
        if str(snapshot.get("source_tool") or "").strip() == source_tool
    ]


def _select_followup_workspace_entry(
    entries: list[Any],
    raw_input: Mapping[str, Any],
) -> Mapping[str, Any]:
    candidates = [entry for entry in entries if isinstance(entry, Mapping)]
    if not candidates:
        return {}
    selection = str(raw_input.get("selection") or "").strip().lower()
    if any(marker in selection for marker in ("largest", "biggest", "最大")):
        return max(
            candidates,
            key=lambda entry: _workspace_entry_size_score(entry, candidates.index(entry)),
        )
    if any(marker in selection for marker in ("last", "最后", "末尾")):
        return candidates[-1]
    if any(
        marker in selection
        for marker in ("recent", "latest", "newest", "最近", "最新", "刚刚")
    ):
        return max(
            candidates,
            key=lambda entry: _workspace_entry_recency_score(entry, candidates.index(entry)),
        )
    return candidates[0]


def _workspace_entry_recency_score(entry: Mapping[str, Any], index: int) -> tuple[float, int, int]:
    numeric_score = 0.0
    for key in ("mtime_ns", "mtime", "modified_at", "score"):
        value = entry.get(key)
        try:
            numeric_score = max(numeric_score, float(value))
        except (TypeError, ValueError):
            pass
    name = str(entry.get("name") or entry.get("path") or "").lower()
    name_score = 0
    for marker, score in (
        ("latest", 30),
        ("newest", 25),
        ("recent", 20),
        ("final", 10),
        ("最新", 30),
        ("最近", 20),
    ):
        if marker in name:
            name_score = max(name_score, score)
    return numeric_score, name_score, -index


def _workspace_entry_size_score(entry: Mapping[str, Any], index: int) -> tuple[float, int]:
    numeric_score = 0.0
    for key in ("size_bytes", "size", "bytes", "file_size"):
        value = entry.get(key)
        try:
            numeric_score = max(numeric_score, float(value))
        except (TypeError, ValueError):
            pass
    return numeric_score, -index


def _select_followup_desktop_app(apps: list[Any]) -> Mapping[str, Any]:
    candidates = [app for app in apps if isinstance(app, Mapping)]
    if not candidates:
        return {}
    return max(candidates, key=_desktop_app_candidate_score)


def _desktop_app_candidate_score(app: Mapping[str, Any]) -> float:
    for key in ("match_score", "score", "confidence"):
        value = app.get(key)
        try:
            return float(value)
        except (TypeError, ValueError):
            continue
    return 0.0


def _join_followup_workspace_path(base_path: str, name: str) -> str:
    clean_name = str(name or "").strip().strip("/")
    clean_base = str(base_path or "").strip()
    if not clean_name:
        return clean_base
    if not clean_base or clean_base == ".":
        return clean_name
    return posixpath.normpath(posixpath.join(clean_base.rstrip("/"), clean_name))


def _tool_requests_with_pending_plan_metadata(
    tool_requests: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    run_id: str = "",
) -> list[dict[str, Any]]:
    if not tool_requests:
        return tool_requests
    followup_context = _latest_model_followup_context(timeline)
    trace_only_replan_context = False
    if not followup_context:
        followup_context = _latest_model_replan_trace_context(timeline)
        trace_only_replan_context = bool(followup_context)
    tool_requests = _model_followup_requests_with_pending_plan_metadata(
        tool_requests,
        followup_context,
    )
    if followup_context:
        tool_requests = [
            _explicit_model_tool_request_without_auto_verification(request)
            for request in tool_requests
        ]
        tool_requests = _model_followup_requests_with_context_trace_metadata(
            tool_requests,
            followup_context,
            mark_trace_only_replan=trace_only_replan_context,
        )
    return _tool_requests_with_latest_suggested_tool_recovery_identity(
        tool_requests,
        timeline,
        run_id=run_id,
    )


_RUNTIME_MODEL_REPLAN_TRACE_MARKER = "_runtime_model_replan_trace_attached"


def _runtime_untrusted_tool_selection_request(
    request: Mapping[str, Any],
) -> dict[str, Any]:
    """Project an untrusted request down to tool selection and arguments.

    Approval, risk, provider, policy, lineage, and completion fields are all
    Runtime authority.  A raw direct/model request can select a tool and its
    schema arguments only; Runtime mints every executable identity and policy
    claim after this boundary.
    """

    raw_input = request.get("input")
    return {
        "tool": str(request.get("tool") or "").strip(),
        "input": (
            deepcopy(dict(raw_input))
            if isinstance(raw_input, Mapping)
            else {}
        ),
    }


def _runtime_model_tool_selection_request(
    request: Mapping[str, Any],
) -> dict[str, Any]:
    """Keep only ToolRequestParser protocol correlation beside selection."""

    item = _runtime_untrusted_tool_selection_request(request)
    protocol = str(request.get("protocol") or "").strip()
    if protocol in {"json_fallback", "tool_calls"}:
        item["protocol"] = protocol
    tool_call_id = str(request.get("tool_call_id") or "").strip()
    if tool_call_id:
        item["tool_call_id"] = tool_call_id
    if protocol == "tool_calls":
        function_name = str(request.get("function_name") or "").strip()
        if function_name:
            item["function_name"] = function_name
    return item


def _runtime_bind_unscoped_tools_to_goal_contract(
    tool_requests: list[dict[str, Any]],
    *,
    contract: GoalContract | None,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
    iteration: int,
    trusted_runtime_metadata: bool = False,
) -> list[dict[str, Any]]:
    """Mint Runtime authority for one unambiguous model-selected goal step.

    Model output may choose a schema-valid tool and arguments, but it never
    gets to author plan, step, request, or Goal identities.  When one tool maps
    to exactly one still-unsatisfied immutable criterion, Runtime binds that
    invocation to the declared source step.  Ambiguous or foreign contracts
    remain unbound and therefore cannot create effectful completion/recovery
    evidence.
    """

    requests = [
        (
            dict(request)
            if trusted_runtime_metadata
            else _runtime_untrusted_tool_selection_request(request)
        )
        for request in tool_requests
        if isinstance(request, Mapping)
    ]
    clean_run_id = str(run_id or "").strip()
    if (
        not requests
        or contract is None
        or not clean_run_id
        or str(contract.run_id or "").strip() != clean_run_id
    ):
        return requests
    try:
        goal_assessment = runtime_goal_assessment(contract, timeline)
        unsatisfied = set(goal_assessment.unsatisfied_criterion_ids)
    except (TypeError, ValueError):
        return requests
    authority_digest = hashlib.sha256(
        (
            f"{contract.contract_id}\0{clean_run_id}\0{int(iteration)}"
        ).encode("utf-8")
    ).hexdigest()[:16]
    plan_id = f"runtime-model-plan-{authority_digest}"
    decision_id = f"runtime-model-decision-{authority_digest}"
    tool_plan_id = f"runtime-model-tool-plan-{authority_digest}"
    bound: list[dict[str, Any]] = []
    for index, request in enumerate(requests, start=1):
        item = dict(request)
        runtime_replan_trace_attached = (
            item.pop(_RUNTIME_MODEL_REPLAN_TRACE_MARKER, None) is True
        )
        if any(
            str(item.get(key) or "").strip()
            for key in ("goal_contract_id", "goal_criterion_id")
        ):
            bound.append(item)
            continue
        existing_plan_id = str(item.get("plan_id") or "").strip()
        deferred_context = (
            item.get("deferred_context")
            if isinstance(item.get("deferred_context"), Mapping)
            else {}
        )
        existing_step_id = str(
            item.get("step_id")
            or item.get("planner_step_id")
            or deferred_context.get("step_id")
            or deferred_context.get("planner_step_id")
            or ""
        ).strip()
        existing_request_id = str(item.get("request_id") or "").strip()
        has_existing_runtime_identity = bool(
            existing_plan_id or existing_step_id or existing_request_id
        )
        trusted_replan_source = _runtime_model_trusted_replan_trace_source(
            item,
            contract=contract,
            assessment=goal_assessment,
            timeline=timeline,
            run_id=clean_run_id,
        )
        trusted_replan_criterion_id = str(
            trusted_replan_source.get("criterion_id") or ""
        ).strip()
        if runtime_replan_trace_attached and not trusted_replan_source:
            # The trace was attached by Runtime, but its failed fact does not
            # replay to this Goal contract (for example, a read-only recovery
            # that is unrelated to the remaining effectful criterion).  Drop
            # correlation-only fields and resume ordinary safe model binding;
            # a stale replan must not block an otherwise exact next step.
            item = _runtime_model_tool_selection_request(item)
            existing_plan_id = ""
            existing_step_id = ""
            existing_request_id = ""
            deferred_context = {}
            has_existing_runtime_identity = False
        trusted_trace_only = bool(
            trusted_replan_criterion_id
            and existing_plan_id
            and not existing_step_id
            and not existing_request_id
        )
        if trusted_trace_only:
            # ``_tool_requests_with_pending_plan_metadata`` attached this
            # correlation-only plan id after stripping model metadata.  It is
            # not an executable identity; the binder below must mint a fresh
            # request/step identity for the refined target.
            has_existing_runtime_identity = False
        if has_existing_runtime_identity and (
            str(item.get("source") or "").strip() != "runtime_planner"
            or not all(
                (existing_plan_id, existing_step_id, existing_request_id)
            )
        ):
            # Model/public partial identities never become Runtime authority.
            bound.append(item)
            continue
        tool_name = str(item.get("tool") or "").strip()
        tool_capabilities = set(capability_ids_for_tool(tool_name))
        capabilities = {
            *tool_capabilities,
            str(item.get("capability_id") or "").strip(),
            str(deferred_context.get("capability_id") or "").strip(),
        }
        capabilities.discard("")
        if (
            trusted_trace_only
            and tool_name in _RUNTIME_REPLAN_GROUNDING_OBSERVATION_TOOLS
            and trusted_replan_source.get("source_retryable") == "true"
        ):
            criterion = contract.criterion(trusted_replan_criterion_id)
            if (
                criterion is not None
                and criterion.criterion_id in unsatisfied
                and criterion.required
                and criterion.effectful
            ):
                tool_call_id = ensure_tool_call_id(item)
                observation_digest = hashlib.sha256(
                    (
                        f"{contract.contract_id}\0"
                        f"{trusted_replan_source['replan_request_id']}\0"
                        f"{int(iteration)}\0{index}\0{tool_call_id}\0{tool_name}"
                    ).encode("utf-8")
                ).hexdigest()[:16]
                observation_step_id = (
                    f"runtime-replan-observation-{observation_digest}"
                )
                item.update(
                    {
                        "source": "runtime_model_replan_observation",
                        "planning_reason": "runtime_model_replan_observation",
                        # Keep the root plan only as a replay correlation.  A
                        # fresh step/request identity prevents this read from
                        # masquerading as the failed effectful source step.
                        "plan_id": str(
                            trusted_replan_source["source_plan_id"]
                        ),
                        "tool_plan_id": (
                            f"runtime-replan-observation-plan-{observation_digest}"
                        ),
                        "step_id": observation_step_id,
                        "planner_step_id": observation_step_id,
                        "request_id": (
                            f"runtime-replan-observation-request-{observation_digest}"
                        ),
                        "source_step_id": str(
                            trusted_replan_source["source_step_id"]
                        ),
                        "source_request_id": str(
                            trusted_replan_source["source_request_id"]
                        ),
                        "source_tool_call_id": str(
                            trusted_replan_source["source_tool_call_id"]
                        ),
                        "goal_contract_id": contract.contract_id,
                        "goal_criterion_id": criterion.criterion_id,
                        "root_goal_unchanged": True,
                        "observation_only": True,
                        "goal_completion_authority": False,
                    }
                )
                if tool_capabilities:
                    item["capability_id"] = sorted(tool_capabilities)[0]
                else:
                    item.pop("capability_id", None)
                for key in (
                    "action_target",
                    "postcondition_verified",
                    "verification_passed",
                    "verified",
                    "state",
                    "target",
                ):
                    item.pop(key, None)
                bound.append(item)
                continue
        candidates = [
            criterion
            for criterion in contract.criteria
            if criterion.criterion_id in unsatisfied
            and (
                not trusted_replan_criterion_id
                or criterion.criterion_id == trusted_replan_criterion_id
            )
            and criterion.required
            and criterion.effectful
            and (
                (
                    has_existing_runtime_identity
                    and existing_step_id
                    in {*criterion.source_step_ids, *criterion.verifier_step_ids}
                )
                or (
                    not has_existing_runtime_identity
                    and len(criterion.source_step_ids) == 1
                )
            )
            and bool(criterion.required_capabilities)
            and set(criterion.required_capabilities).issubset(capabilities)
            and _runtime_model_input_matches_goal_target(
                item,
                criterion.expected,
                contract=contract,
                criterion_id=criterion.criterion_id,
                timeline=timeline,
            )
        ]
        if len(candidates) != 1:
            bound.append(item)
            continue
        criterion = candidates[0]
        if has_existing_runtime_identity:
            item.update(
                {
                    "step_id": existing_step_id,
                    "planner_step_id": existing_step_id,
                    "goal_contract_id": contract.contract_id,
                    "goal_criterion_id": criterion.criterion_id,
                    "root_goal_unchanged": True,
                }
            )
            expected_target = criterion.expected.get("target")
            if isinstance(expected_target, Mapping) and expected_target:
                item["action_target"] = dict(expected_target)
            bound.append(item)
            continue
        tool_call_id = ensure_tool_call_id(item)
        step_id = str(criterion.source_step_ids[0]).strip()
        request_digest = hashlib.sha256(
            (
                f"{plan_id}\0{index}\0{tool_call_id}\0{tool_name}"
            ).encode("utf-8")
        ).hexdigest()[:16]
        item.update(
            {
                "source": "runtime_model_tool_binding",
                "planning_reason": "runtime_model_tool_goal_binding",
                "decision_id": decision_id,
                "plan_id": plan_id,
                "tool_plan_id": tool_plan_id,
                "step_id": step_id,
                "planner_step_id": step_id,
                "request_id": f"runtime-model-request-{request_digest}",
                "goal_contract_id": contract.contract_id,
                "goal_criterion_id": criterion.criterion_id,
                "root_goal_unchanged": True,
            }
        )
        expected_target = criterion.expected.get("target")
        if isinstance(expected_target, Mapping) and expected_target:
            item["action_target"] = dict(expected_target)
        if len(criterion.required_capabilities) == 1:
            item["capability_id"] = criterion.required_capabilities[0]
        bound.append(item)
    return bound


def _runtime_model_trusted_replan_trace_criterion_id(
    request: Mapping[str, Any],
    *,
    contract: GoalContract,
    assessment: Any,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> str:
    """Resolve Runtime-attached replan trace metadata to one Goal criterion."""

    trusted = _runtime_model_trusted_replan_trace_source(
        request,
        contract=contract,
        assessment=assessment,
        timeline=timeline,
        run_id=run_id,
    )
    return str(trusted.get("criterion_id") or "").strip()


def _runtime_model_trusted_replan_trace_source(
    request: Mapping[str, Any],
    *,
    contract: GoalContract,
    assessment: Any,
    timeline: Sequence[Mapping[str, Any]],
    run_id: str,
) -> dict[str, str]:
    """Replay one correlation-only model follow-up trace to its failed fact.

    The model never supplies this authority: its protocol projection is
    stripped before Runtime attaches the latest follow-up trace.  Both the
    ordinary tool-runner replan and a coordinator continuation still have to
    replay through ``_trusted_runtime_replan_source`` before they can guide an
    observation or a refined retry.
    """

    replan_request_id = str(request.get("replan_request_id") or "").strip()
    request_plan_id = str(request.get("plan_id") or "").strip()
    request_run_id = str(request.get("run_id") or "").strip()
    if (
        not replan_request_id
        or not request_plan_id
        or request_run_id != str(run_id or "").strip()
        or str(request.get("step_id") or request.get("planner_step_id") or "").strip()
        or str(request.get("request_id") or "").strip()
    ):
        return {}
    mutable_timeline = [
        dict(event) for event in timeline if isinstance(event, Mapping)
    ]
    candidates = [
        payload
        for payload in _pending_runtime_replan_payloads(mutable_timeline)
        if str(payload.get("request_id") or "").strip() == replan_request_id
        and str(payload.get("source") or "").strip()
        in {
            "runtime_tool_request_runner",
            _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE,
        }
        and str(payload.get("plan_id") or "").strip() == request_plan_id
        and str(payload.get("run_id") or "").strip() == request_run_id
    ]
    if len(candidates) != 1:
        return {}
    trusted = _trusted_runtime_replan_source(
        candidates[0],
        contract=contract,
        assessment=assessment,
        timeline=mutable_timeline,
        run_id=request_run_id,
    )
    if not trusted:
        return {}
    if (
        trusted.get("source_retryable") != "true"
        and str(candidates[0].get("source") or "").strip()
        != _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
    ):
        return {}
    return {
        **trusted,
        "replan_request_id": replan_request_id,
    }


def _runtime_model_input_matches_goal_target(
    request: Mapping[str, Any],
    expected: Mapping[str, Any],
    *,
    contract: GoalContract | None = None,
    criterion_id: str = "",
    timeline: Sequence[Mapping[str, Any]] = (),
) -> bool:
    target = expected.get("target")
    if not isinstance(target, Mapping):
        return True
    request_input = (
        request.get("input")
        if isinstance(request.get("input"), Mapping)
        else {}
    )
    comparable_keys = (
        "query",
        "app_name",
        "path",
        "target_path",
        "action",
        "target",
    )
    mismatches: dict[str, str] = {}
    for key in comparable_keys:
        expected_value = target.get(key)
        observed_value = request_input.get(key)
        if expected_value in (None, "") or observed_value in (None, ""):
            continue
        if str(observed_value).strip().casefold() != str(expected_value).strip().casefold():
            if key == "action":
                return False
            mismatches[key] = str(observed_value).strip()
    if not mismatches:
        return True
    return _runtime_model_replan_target_is_observation_grounded(
        mismatches,
        contract=contract,
        criterion_id=criterion_id,
        timeline=timeline,
    )


_RUNTIME_REPLAN_GROUNDABLE_TARGET_KEYS = frozenset(
    {
        "query",
        "app_name",
        "path",
        "target_path",
        "target",
    }
)

_RUNTIME_REPLAN_PATH_OBSERVATION_FIELDS = frozenset(
    {
        "artifact_path",
        "artifact_paths",
        "destination_path",
        "display_path",
        "file_path",
        "output_path",
        "path",
        "target_path",
    }
)
_RUNTIME_REPLAN_APP_OBSERVATION_FIELDS = frozenset(
    {
        "active_app_name",
        "app_name",
        "application_name",
        "bundle_id",
        "bundle_identifier",
        "canonical_name",
        "discovered_app_name",
        "frontmost_app",
        "name",
        "owner_name",
    }
)
_RUNTIME_REPLAN_QUERY_OBSERVATION_FIELDS = frozenset(
    {
        "alias",
        "aliases",
        "alternate_name",
        "alternate_names",
        "canonical_name",
        "description",
        "href",
        "label",
        "name",
        "original_name",
        "original_title",
        "snippet",
        "text",
        "title",
        "url",
        "value",
    }
)
_RUNTIME_REPLAN_OBSERVATION_FIELDS_BY_TARGET_KEY = {
    "app_name": _RUNTIME_REPLAN_APP_OBSERVATION_FIELDS,
    "path": _RUNTIME_REPLAN_PATH_OBSERVATION_FIELDS,
    "query": _RUNTIME_REPLAN_QUERY_OBSERVATION_FIELDS,
    "target": frozenset(
        {
            *_RUNTIME_REPLAN_APP_OBSERVATION_FIELDS,
            *_RUNTIME_REPLAN_PATH_OBSERVATION_FIELDS,
            *_RUNTIME_REPLAN_QUERY_OBSERVATION_FIELDS,
            "id",
            "identifier",
        }
    ),
    "target_path": _RUNTIME_REPLAN_PATH_OBSERVATION_FIELDS,
}
_RUNTIME_REPLAN_OBSERVATION_CONTROL_SUBTREES = frozenset(
    {
        "approval",
        "arguments",
        "desktop_execution_policy",
        "goal",
        "goal_contract",
        "input",
        "input_preview",
        "metadata",
        "observability",
        "policy",
        "provenance",
        "recovery",
        "request",
        "request_metadata",
        "trace",
        "verification",
    }
)
_RUNTIME_REPLAN_OBSERVATION_CONTROL_PREFIXES = (
    "_",
    "approval_",
    "execution_",
    "goal_",
    "planner_",
    "provider_",
    "recovery_",
    "request_",
    "runtime_",
    "source_",
    "tool_",
    "verification_",
)

# Some capability-level discovery tools are outside the stricter desktop
# observation policy set.  Their provider-attested payloads may still ground
# a later target identifier; they never satisfy an effectful root criterion
# by themselves.
_RUNTIME_REPLAN_GROUNDING_OBSERVATION_TOOLS = frozenset(
    {
        *READ_ONLY_OBSERVATION_TOOLS,
        "browser.search",
        "workspace.list",
    }
)


def _runtime_model_replan_target_is_observation_grounded(
    mismatches: Mapping[str, str],
    *,
    contract: GoalContract | None,
    criterion_id: str,
    timeline: Sequence[Mapping[str, Any]],
) -> bool:
    """Accept a changed target only when Runtime can replay its provenance.

    A model may use a real read-only observation to resolve an alias, app,
    path, or other target identifier after a retryable action failed.  The
    model's prose and arguments are never evidence on their own: the failed
    source, replan request, observation executor, provider receipt, and exact
    replacement value must all be present in this process-owned timeline.
    """

    clean_criterion_id = str(criterion_id or "").strip()
    if (
        not mismatches
        or contract is None
        or not clean_criterion_id
        or any(
            key not in _RUNTIME_REPLAN_GROUNDABLE_TARGET_KEYS
            or not str(value or "").strip()
            for key, value in mismatches.items()
        )
    ):
        return False
    mutable_timeline = [dict(event) for event in timeline if isinstance(event, Mapping)]
    assessment = runtime_goal_assessment(contract, mutable_timeline)
    trusted_sources: list[tuple[Mapping[str, Any], dict[str, str], int]] = []
    for payload in _pending_runtime_replan_payloads(mutable_timeline):
        source = _trusted_runtime_replan_source(
            payload,
            contract=contract,
            assessment=assessment,
            timeline=mutable_timeline,
            run_id=contract.run_id,
        )
        if (
            source.get("criterion_id") != clean_criterion_id
            or (
                source.get("source_retryable") != "true"
                and str(payload.get("source") or "").strip()
                != _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
            )
            or (
                source.get("source_observation_only") == "true"
                and str(payload.get("source") or "").strip()
                != _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
            )
        ):
            continue
        replan_request_id = str(payload.get("request_id") or "").strip()
        replan_index = next(
            (
                index
                for index, event in enumerate(mutable_timeline)
                if _runtime_replan_event_type(
                    str(event.get("event") or event.get("event_type") or "").strip()
                )
                == "agent.replan.requested"
                and isinstance(event.get("payload"), Mapping)
                and str(event["payload"].get("request_id") or "").strip()
                == replan_request_id
            ),
            -1,
        )
        if replan_index >= 0:
            trusted_sources.append((payload, source, replan_index))
    if len(trusted_sources) != 1:
        return False
    trusted_payload, trusted_source, replan_index = trusted_sources[0]
    grounded_values: dict[str, set[str]] = {
        key: set() for key in mismatches
    }
    if str(trusted_payload.get("source") or "").strip() == (
        _RUNTIME_RECOVERY_CONTINUATION_REPLAN_SOURCE
    ):
        # The coordinator executes discovery before it emits the replan
        # request. Replay validation above has already correlated each compact
        # snapshot to a provider-attested executor event. Read target evidence
        # from those executor results, never from the snapshot's provenance.
        for observation in _mapping_list(
            trusted_payload.get("grounded_observations")
        ):
            observation_match = _runtime_recovery_executor_terminal_event(
                mutable_timeline,
                run_id=contract.run_id,
                tool_call_id=str(
                    observation.get("source_tool_call_id") or ""
                ).strip(),
                tool_name=str(observation.get("source_tool") or "").strip(),
            )
            if observation_match is None:
                return False
            observation_result = observation_match[1].get("result")
            if not isinstance(observation_result, Mapping):
                return False
            for target_key in grounded_values:
                grounded_values[target_key].update(
                    _runtime_observation_scalar_values(
                        observation_result,
                        target_key=target_key,
                    )
                )
    else:
        for raw_event in mutable_timeline[replan_index + 1 :]:
            nested = (
                raw_event.get("payload")
                if isinstance(raw_event.get("payload"), Mapping)
                else {}
            )
            event = {**dict(nested), **dict(raw_event)}
            event_type = str(
                event.get("event") or event.get("event_type") or ""
            ).strip()
            tool_name = str(event.get("tool") or event.get("detail") or "").strip()
            result = event.get("result")
            event_step_id = str(
                event.get("step_id") or event.get("planner_step_id") or ""
            ).strip()
            source_step_id = str(
                trusted_source.get("source_step_id") or ""
            ).strip()
            independent_observation_step = bool(
                event_step_id
                and event_step_id != source_step_id
                and event.get("observation_only") is True
                and event.get("goal_completion_authority") is False
                and str(event.get("source_step_id") or "").strip()
                == source_step_id
                and str(event.get("source_tool_call_id") or "").strip()
                == str(
                    trusted_source.get("source_tool_call_id") or ""
                ).strip()
                and str(event.get("source_request_id") or "").strip()
                == str(
                    trusted_source.get("source_request_id") or ""
                ).strip()
                and str(event.get("goal_contract_id") or "").strip()
                == contract.contract_id
                and str(event.get("goal_criterion_id") or "").strip()
                == clean_criterion_id
            )
            if (
                event_type != "agent.tool.call"
                or tool_name not in _RUNTIME_REPLAN_GROUNDING_OBSERVATION_TOOLS
                or not isinstance(result, Mapping)
                or result.get("ok") is not True
                or str(event.get("run_id") or "").strip() != contract.run_id
                or str(event.get("actor") or "").strip() != "native_runtime"
                or str(event.get("execution_authority") or "").strip()
                != "runtime_tool_executor"
                or str(event.get("replan_request_id") or "").strip()
                != str(trusted_payload.get("request_id") or "").strip()
                or str(event.get("plan_id") or "").strip()
                != str(trusted_source.get("source_plan_id") or "").strip()
                or (
                    event_step_id != source_step_id
                    and not independent_observation_step
                )
                or not str(event.get("tool_call_id") or "").strip()
                or not _runtime_execution_provider_identity(result)
            ):
                continue
            for target_key in grounded_values:
                grounded_values[target_key].update(
                    _runtime_observation_scalar_values(
                        result,
                        target_key=target_key,
                    )
                )
    return all(
        str(value).strip().casefold() in grounded_values.get(key, set())
        for key, value in mismatches.items()
    )


def _runtime_observation_scalar_values(
    value: Any,
    *,
    target_key: str,
) -> set[str]:
    """Return only domain values that can evidence one target field.

    Observation results may carry Runtime provenance, policy, request echoes,
    and recovery lineage beside their domain payload.  Recursing through all
    strings lets those control-plane values authorize a changed model target.
    This extractor therefore uses a small semantic field allowlist and skips
    control-plane subtrees before accepting exact scalar values.
    """

    allowed_fields = _RUNTIME_REPLAN_OBSERVATION_FIELDS_BY_TARGET_KEY.get(
        str(target_key or "").strip()
    )
    if not allowed_fields:
        return set()
    values: set[str] = set()

    def collect(nested: Any, *, scalar_allowed: bool = False) -> None:
        if isinstance(nested, Mapping):
            for raw_field, child in nested.items():
                field = str(raw_field or "").strip().casefold()
                if (
                    not field
                    or field in _RUNTIME_REPLAN_OBSERVATION_CONTROL_SUBTREES
                    or field.startswith(
                        _RUNTIME_REPLAN_OBSERVATION_CONTROL_PREFIXES
                    )
                ):
                    continue
                collect(child, scalar_allowed=field in allowed_fields)
            return
        if isinstance(nested, (list, tuple, set, frozenset)):
            for child in nested:
                collect(child, scalar_allowed=scalar_allowed)
            return
        if scalar_allowed and isinstance(nested, str) and nested.strip():
            values.add(nested.strip().casefold())

    collect(value)
    return values


def _tool_requests_with_latest_suggested_tool_recovery_identity(
    tool_requests: list[dict[str, Any]],
    timeline: list[dict[str, Any]],
    *,
    run_id: str,
) -> list[dict[str, Any]]:
    """Link a model-selected suggested tool to the exact failed attempt.

    Model-authored correlation fields have already been stripped before this
    helper runs. The resulting marker is therefore runtime-owned and can be
    trusted by the final outcome evaluator without trusting model prose.
    """

    suggestions = _latest_model_tool_recovery_suggestions(
        timeline,
        run_id=run_id,
    )
    if not tool_requests or not suggestions:
        return tool_requests
    annotated: list[dict[str, Any]] = []
    used_source_call_ids: set[str] = set()
    for request in tool_requests:
        item = dict(request)
        request_tool = str(item.get("tool") or "").strip()
        if not request_tool or str(item.get("source_tool_call_id") or "").strip():
            annotated.append(item)
            continue
        for suggestion in reversed(suggestions):
            source_call_id = str(suggestion.get("tool_call_id") or "").strip()
            if (
                not source_call_id
                or source_call_id in used_source_call_ids
                or request_tool
                != str(suggestion.get("suggested_tool") or "").strip()
            ):
                continue
            item["source_tool_call_id"] = source_call_id
            source_request_id = str(suggestion.get("request_id") or "").strip()
            if source_request_id:
                item["source_request_id"] = source_request_id
            source_step_id = str(suggestion.get("step_id") or "").strip()
            if source_step_id:
                item["source_step_id"] = source_step_id
            item["recovery_link_kind"] = "suggested_tool"
            item["recovery_source_tool"] = str(suggestion.get("tool") or "").strip()
            item["recovery_suggested_tool"] = request_tool
            used_source_call_ids.add(source_call_id)
            break
        annotated.append(item)
    return annotated


def _latest_model_tool_recovery_suggestions(
    timeline: list[dict[str, Any]],
    *,
    run_id: str,
) -> list[dict[str, str]]:
    clean_run_id = str(run_id or "").strip()
    if not clean_run_id:
        return []
    latest_model_response_index = -1
    for index in range(len(timeline) - 1, -1, -1):
        event = timeline[index]
        payload = (
            event.get("payload")
            if isinstance(event.get("payload"), Mapping)
            else event
        )
        event_type = str(
            event.get("event")
            or event.get("event_type")
            or payload.get("event")
            or payload.get("event_type")
            or ""
        ).strip()
        if (
            event_type == "agent.model.response"
            and _runtime_event_matches_exact_run(
                event,
                payload,
                run_id=clean_run_id,
            )
        ):
            latest_model_response_index = index
            break

    first_by_call_id: dict[
        str,
        tuple[int, Mapping[str, Any], Mapping[str, Any]],
    ] = {}
    for index, event in enumerate(
        timeline[latest_model_response_index + 1 :],
        start=latest_model_response_index + 1,
    ):
        event_type = str(event.get("event") or event.get("event_type") or "").strip()
        if event_type not in {
            "agent.tool.call",
            "agent.tool.failed",
            "agent.tool.skipped",
        }:
            continue
        payload = event.get("payload") if isinstance(event.get("payload"), Mapping) else event
        if not _runtime_event_matches_exact_run(
            event,
            payload,
            run_id=clean_run_id,
        ):
            continue
        result = payload.get("result") if isinstance(payload.get("result"), Mapping) else {}
        tool_call_id = str(payload.get("tool_call_id") or "").strip()
        if not tool_call_id or tool_call_id in first_by_call_id:
            continue
        # A Runtime tool-call id has one terminal winner.  Later terminal
        # projections, whether success or failure, cannot rewrite recovery
        # eligibility for the first accepted fact.
        first_by_call_id[tool_call_id] = (index, payload, result)

    suggestions: list[tuple[int, dict[str, str]]] = []
    for tool_call_id, (index, payload, result) in first_by_call_id.items():
        if result.get("ok") is not False:
            continue
        source_tool = str(
            payload.get("tool") or payload.get("detail") or ""
        ).strip()
        if not source_tool:
            continue
        for suggested_tool in _string_list(result.get("suggested_tool")):
            suggestions.append(
                (
                    index,
                    {
                        "tool_call_id": tool_call_id,
                        "request_id": str(payload.get("request_id") or "").strip(),
                        "step_id": str(
                            payload.get("step_id")
                            or payload.get("planner_step_id")
                            or ""
                        ).strip(),
                        "tool": source_tool,
                        "suggested_tool": suggested_tool,
                    },
                )
            )
    return [
        suggestion
        for _index, suggestion in sorted(suggestions, key=lambda item: item[0])
    ]


def _runtime_event_matches_exact_run(
    event: Mapping[str, Any],
    payload: Mapping[str, Any],
    *,
    run_id: str,
) -> bool:
    """Reject unscoped or conflicting wrapper/payload Run identities."""

    clean_run_id = str(run_id or "").strip()
    outer_run_id = str(event.get("run_id") or "").strip()
    payload_run_id = str(payload.get("run_id") or "").strip()
    if not clean_run_id or (outer_run_id and payload_run_id and outer_run_id != payload_run_id):
        return False
    observed_run_id = payload_run_id or outer_run_id
    return bool(observed_run_id and observed_run_id == clean_run_id)


def _explicit_model_tool_request_without_auto_verification(
    request: dict[str, Any],
) -> dict[str, Any]:
    item = dict(request)
    item.pop("requires_post_action_verification", None)
    return item


def _model_tool_requests_are_verification_recovery_actions(
    requests: Iterable[Mapping[str, Any]],
) -> bool:
    for request in requests:
        if not isinstance(request, Mapping):
            continue
        trigger = str(request.get("replan_trigger") or "").strip()
        triggers = _string_list(request.get("replan_triggers"))
        if trigger == "verification_failed" or "verification_failed" in triggers:
            return True
    return False


def _model_followup_requests_with_context_trace_metadata(
    requests: list[dict[str, Any]],
    followup_context: Mapping[str, Any],
    *,
    mark_trace_only_replan: bool = False,
) -> list[dict[str, Any]]:
    if not requests or not isinstance(followup_context, Mapping):
        return requests
    replan_requests = _mapping_list(followup_context.get("replan_requests"))
    replan_assignments = _model_followup_request_replan_assignments(
        requests,
        replan_requests,
    )
    has_ambiguous_replan_context = len(replan_requests) > 1
    planning_reason = str(followup_context.get("planning_reason") or "").strip()
    annotated: list[dict[str, Any]] = []
    for index, request in enumerate(requests):
        if not isinstance(request, dict):
            continue
        item = dict(request)
        trace = _model_followup_context_trace_metadata(
            followup_context,
            replan_request=replan_assignments.get(index),
            allow_implicit_replan=not has_ambiguous_replan_context,
        )
        for key, value in trace.items():
            if key in {"replan_triggers", "replan_signal_ids"}:
                values = _ordered_text_list(
                    [*_string_list(item.get(key)), *_string_list(value)]
                )
                if values:
                    item[key] = values
                continue
            if not str(item.get(key) or "").strip():
                item[key] = value
        if (
            mark_trace_only_replan
            and str(trace.get("replan_request_id") or "").strip()
        ):
            item[_RUNTIME_MODEL_REPLAN_TRACE_MARKER] = True
        if planning_reason and not str(item.get("planning_reason") or "").strip():
            item["planning_reason"] = planning_reason
        annotated.append(item)
    return annotated


def _model_followup_request_replan_assignments(
    requests: Sequence[Mapping[str, Any]],
    replan_requests: Sequence[Mapping[str, Any]],
) -> dict[int, Mapping[str, Any]]:
    """Match each model request to one unambiguous Runtime replan, or none.

    A follow-up context may contain failures from several Goal criteria.  The
    historical first-item projection correlated every model request to the
    first failure.  This matcher uses only semantic request facts (explicit
    Runtime id, step, tool/capability, and target evidence), permits one replan
    to ground multiple follow-up requests, and rejects ties or colliding Runtime
    replan identities instead of guessing.
    """

    replans = [item for item in replan_requests if isinstance(item, Mapping)]
    if not requests or not replans:
        return {}
    if len(replans) == 1:
        return {
            index: replans[0]
            for index, request in enumerate(requests)
            if isinstance(request, Mapping)
        }

    replan_identity_counts: dict[tuple[str, ...], int] = {}
    for replan in replans:
        identity = _model_followup_replan_identity(replan)
        replan_identity_counts[identity] = replan_identity_counts.get(identity, 0) + 1

    tentative: dict[int, Mapping[str, Any]] = {}
    for index, request in enumerate(requests):
        if not isinstance(request, Mapping):
            continue
        scored = [
            (_model_followup_request_replan_match_score(request, replan), replan)
            for replan in replans
        ]
        best_score = max((score for score, _replan in scored), default=0)
        best = [
            replan
            for score, replan in scored
            if score == best_score and best_score > 0
        ]
        if (
            len(best) == 1
            and replan_identity_counts.get(
                _model_followup_replan_identity(best[0]),
                0,
            )
            == 1
        ):
            tentative[index] = best[0]
    return tentative


def _model_followup_replan_identity(
    replan: Mapping[str, Any],
) -> tuple[str, ...]:
    request_id = str(replan.get("request_id") or "").strip()
    if request_id:
        return ("request_id", request_id)
    return (
        "source",
        str(replan.get("plan_id") or "").strip(),
        str(replan.get("source_step_id") or "").strip(),
        str(replan.get("source_tool_name") or "").strip(),
    )


def _model_followup_request_replan_match_score(
    request: Mapping[str, Any],
    replan: Mapping[str, Any],
) -> int:
    """Return a correlation score; zero means the pair is not grounded."""

    replan_id = str(replan.get("request_id") or "").strip()
    explicit_replan_id = str(request.get("replan_request_id") or "").strip()
    if explicit_replan_id:
        return 10_000 if explicit_replan_id == replan_id else 0

    score = 0
    request_step_id = str(
        request.get("step_id") or request.get("planner_step_id") or ""
    ).strip()
    source_step_id = str(replan.get("source_step_id") or "").strip()
    if request_step_id and source_step_id and request_step_id == source_step_id:
        score += 1_000

    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    source_tool = str(replan.get("source_tool_name") or "").strip()
    if tool_name and tool_name == source_tool:
        score += 500
    elif tool_name and tool_name in _model_followup_replan_candidate_tools(replan):
        score += 400

    request_capabilities = {
        *capability_ids_for_tool(tool_name),
        str(request.get("capability_id") or "").strip(),
    }
    request_capabilities.discard("")
    target_capability = str(replan.get("target_capability_id") or "").strip()
    if target_capability and target_capability in request_capabilities:
        score += 250

    request_targets = _model_followup_request_target_values(request)
    replan_target = _replan_recovery_target(replan)
    explicit_target_values = {
        str(value or "").strip().casefold()
        for value in replan_target.values()
        if str(value or "").strip()
    }
    if explicit_target_values:
        score += 100 * len(request_targets.intersection(explicit_target_values))

    grounded_observations = _mapping_list(replan.get("grounded_observations"))
    if not grounded_observations and isinstance(
        replan.get("grounded_observation"), Mapping
    ):
        grounded_observations = [dict(replan["grounded_observation"])]
    for target_key, target_value in _model_followup_request_target_items(request):
        if any(
            target_value
            in _runtime_observation_scalar_values(
                observation,
                target_key=target_key,
            )
            for observation in grounded_observations
        ):
            score += 150
    return score


def _model_followup_replan_candidate_tools(
    replan: Mapping[str, Any],
) -> set[str]:
    metadata = replan.get("metadata") if isinstance(replan.get("metadata"), Mapping) else {}
    tools = {
        *_string_list(replan.get("fallback_tools")),
        *_string_list(replan.get("recommended_tools")),
        *_string_list(metadata.get("recommended_tools")),
    }
    for action in (
        *_mapping_list(replan.get("recovery_actions")),
        *_mapping_list(metadata.get("recovery_actions")),
    ):
        tool_name = str(action.get("tool") or action.get("tool_name") or "").strip()
        if tool_name:
            tools.add(tool_name)
    for observation in (
        *_mapping_list(replan.get("grounded_observations")),
        *_mapping_list(replan.get("recovery_observations")),
    ):
        tool_name = str(observation.get("source_tool") or "").strip()
        if tool_name:
            tools.add(tool_name)
    return tools


def _model_followup_request_target_items(
    request: Mapping[str, Any],
) -> tuple[tuple[str, str], ...]:
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    items: list[tuple[str, str]] = []
    for target_key in _RUNTIME_REPLAN_GROUNDABLE_TARGET_KEYS:
        raw_value = request_input.get(target_key)
        if raw_value in (None, ""):
            continue
        value = str(raw_value).strip().casefold()
        if value:
            items.append((target_key, value))
    return tuple(items)


def _model_followup_request_target_values(
    request: Mapping[str, Any],
) -> set[str]:
    return {value for _key, value in _model_followup_request_target_items(request)}


def _model_followup_context_trace_metadata(
    followup_context: Mapping[str, Any],
    *,
    replan_request: Mapping[str, Any] | None = None,
    allow_implicit_replan: bool = True,
) -> dict[str, Any]:
    trace: dict[str, Any] = {}
    replan_candidates = _mapping_list(followup_context.get("replan_requests"))
    if replan_request is None and allow_implicit_replan and len(replan_candidates) == 1:
        replan_request = replan_candidates[0]
    if replan_request is None and len(replan_candidates) > 1:
        return {}
    selected_replan = replan_request if isinstance(replan_request, Mapping) else {}
    multiple_replans = len(replan_candidates) > 1
    task_core = (
        followup_context.get("task_core")
        if isinstance(followup_context.get("task_core"), Mapping)
        else {}
    )
    workspace = (
        task_core.get("workspace")
        if isinstance(task_core, Mapping) and isinstance(task_core.get("workspace"), Mapping)
        else {}
    )
    context_trace_source = {} if multiple_replans else followup_context
    for key in (
        "decision_id",
        "plan_id",
        "tool_plan_id",
        "intent_kind",
        "core_id",
        "workspace_id",
        "task_id",
        "run_id",
        "target_app_name",
        "target_app_query",
        "target_search_text",
        "verification_predicate_kind",
    ):
        value = str(
            selected_replan.get(key)
            or context_trace_source.get(key)
            or task_core.get(key)
            or workspace.get(key)
            or ""
        ).strip()
        if value:
            trace[key] = value

    request_id = str(
        selected_replan.get("replan_request_id")
        or selected_replan.get("request_id")
        or context_trace_source.get("replan_request_id")
        or context_trace_source.get("request_id")
        or ""
    ).strip()
    if request_id:
        trace["replan_request_id"] = request_id

    trigger = str(
        selected_replan.get("replan_trigger")
        or selected_replan.get("trigger")
        or context_trace_source.get("replan_trigger")
        or context_trace_source.get("trigger")
        or ""
    ).strip()
    if trigger:
        trace["replan_trigger"] = trigger
        trace["replan_triggers"] = _ordered_text_list(
            [*_string_list(context_trace_source.get("triggers")), trigger]
        )

    target_capability_id = str(
        selected_replan.get("capability_id")
        or selected_replan.get("target_capability_id")
        or context_trace_source.get("capability_id")
        or context_trace_source.get("target_capability_id")
        or ""
    ).strip()
    if target_capability_id:
        trace["capability_id"] = target_capability_id

    if not any(trace.get(key) for key in ("target_app_name", "target_app_query", "target_search_text")):
        recovery_target = (
            _replan_recovery_target(selected_replan)
            if selected_replan
            else _first_model_followup_recovery_target(followup_context)
        )
        for key in ("target_app_name", "target_app_query", "target_search_text"):
            value = str(recovery_target.get(key) or "").strip()
            if value:
                trace[key] = value

    signal_ids = _model_followup_context_replan_signal_ids(followup_context)
    if signal_ids:
        trace["replan_signal_ids"] = signal_ids
    return trace


def _first_model_followup_replan_request(
    followup_context: Mapping[str, Any],
) -> Mapping[str, Any]:
    requests = followup_context.get("replan_requests")
    if not isinstance(requests, list):
        return {}
    for request in requests:
        if isinstance(request, Mapping):
            return request
    return {}


def _first_model_followup_recovery_target(
    followup_context: Mapping[str, Any],
) -> Mapping[str, Any]:
    targets = followup_context.get("recovery_targets")
    if not isinstance(targets, list):
        return {}
    for target in targets:
        if isinstance(target, Mapping):
            return target
    return {}


def _model_followup_context_replan_signal_ids(
    followup_context: Mapping[str, Any],
) -> list[str]:
    signal_ids: list[str] = []
    for key in ("content_snapshots", "recovery_observations"):
        snapshots = followup_context.get(key)
        if not isinstance(snapshots, list):
            continue
        for snapshot in snapshots:
            if isinstance(snapshot, Mapping):
                signal_ids.extend(_string_list(snapshot.get("replan_signal_ids")))
    snapshot = followup_context.get("content_snapshot")
    if isinstance(snapshot, Mapping):
        signal_ids.extend(_string_list(snapshot.get("replan_signal_ids")))
    return _ordered_text_list(signal_ids)

def _resolve_dynamic_pending_plan_request_input(
    request: dict[str, Any],
    step: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> dict[str, Any]:
    tool_name = str(request.get("tool") or request.get("tool_name") or "").strip()
    request_input = request.get("input") if isinstance(request.get("input"), Mapping) else {}
    step_input = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    merged_input = {**dict(step_input), **dict(request_input)}
    if tool_name not in _OPEN_PATH_WITH_APP_TOOLS:
        resolved_input, input_resolution = _model_followup_resolved_app_input(
            tool_name,
            request_input,
            merged_input,
            followup_context,
        )
        if not input_resolution:
            return request
        resolved_request = {**request, "input": resolved_input}
        if not isinstance(resolved_request.get("input_resolution"), Mapping):
            resolved_request["input_resolution"] = input_resolution
        return resolved_request
    resolved_input, input_resolution = _model_followup_open_path_with_app_pending_input(
        merged_input,
        followup_context,
    )
    if not resolved_input:
        return request
    resolved_request = {**request, "input": resolved_input}
    if input_resolution:
        input_resolution["tool"] = str(request.get("tool") or "").strip()
    if input_resolution and not isinstance(resolved_request.get("input_resolution"), Mapping):
        resolved_request["input_resolution"] = input_resolution
    return resolved_request


def _model_followup_clipboard_pending_input(
    raw_input: Mapping[str, Any],
    generated_content: str,
) -> dict[str, Any]:
    if not isinstance(raw_input, Mapping):
        raw_input = {}
    text = str(
        raw_input.get("text")
        or raw_input.get("content")
        or generated_content
        or ""
    ).strip()
    if not text:
        return {}
    return {"text": text}


def _model_followup_artifact_pending_input(
    raw_input: Mapping[str, Any],
    generated_content: str,
    followup_context: Mapping[str, Any] | None = None,
) -> dict[str, Any]:
    if not isinstance(raw_input, Mapping):
        return {}
    path = str(raw_input.get("path") or "").strip()
    if not path:
        return {}
    content = _model_followup_resolved_body_content(
        generated_content,
        raw_input,
        followup_context or {},
    )
    if not content:
        return {}
    return {"path": path, "content": content}


def _model_followup_data_analyze_pending_input(
    raw_input: Mapping[str, Any],
    followup_context: Mapping[str, Any],
) -> dict[str, Any]:
    if not isinstance(raw_input, Mapping):
        raw_input = {}
    path = str(raw_input.get("path") or "").strip()
    if path and not _pending_plan_placeholder_value(path, "captured"):
        return _data_analyze_pending_input_with_manifest(dict(raw_input), path=path)

    content = str(raw_input.get("content") or "").strip()
    snapshot: Mapping[str, Any] = {}
    if not content or _pending_plan_placeholder_value(content, "captured"):
        snapshot = _latest_data_analysis_followup_snapshot(followup_context)
        content = str(snapshot.get("text") or "").strip()
    if not content:
        return {}

    display_path = str(raw_input.get("display_path") or "").strip()
    if not display_path or _pending_plan_placeholder_value(display_path, "captured"):
        display_path = _captured_data_display_path(dict(snapshot))
    source_kind = str(raw_input.get("source_kind") or "").strip()
    if not source_kind or source_kind == "unknown":
        source_kind = _captured_data_source_kind(content)

    payload = _data_analyze_pending_input_with_manifest(
        dict(raw_input),
        content=content,
        display_path=display_path,
        source_kind=source_kind,
    )
    return payload


def _latest_data_analysis_followup_snapshot(
    followup_context: Mapping[str, Any],
) -> Mapping[str, Any]:
    snapshots: list[Mapping[str, Any]] = []
    raw_snapshots = followup_context.get("content_snapshots")
    if isinstance(raw_snapshots, list):
        snapshots.extend(snapshot for snapshot in raw_snapshots if isinstance(snapshot, Mapping))
    raw_snapshot = followup_context.get("content_snapshot")
    if isinstance(raw_snapshot, Mapping):
        snapshots.append(raw_snapshot)
    for snapshot in reversed(snapshots):
        if str(snapshot.get("source_tool") or "").strip() == "data.analyze":
            continue
        if str(snapshot.get("text") or "").strip():
            return snapshot
    return {}


def _data_analyze_pending_input_with_manifest(
    raw_input: dict[str, Any],
    *,
    path: str = "",
    content: str = "",
    display_path: str = "",
    source_kind: str = "",
) -> dict[str, Any]:
    payload: dict[str, Any] = {}
    if path:
        payload["path"] = path
    if content:
        payload["content"] = content
    if display_path:
        payload["display_path"] = display_path
    if source_kind:
        payload["source_kind"] = source_kind

    for key in ("artifact_path",):
        value = str(raw_input.get(key) or "").strip()
        if value:
            payload[key] = value
    for key in ("requested_outputs", "artifact_paths"):
        values = _string_list(raw_input.get(key))
        if values:
            payload[key] = values
    artifact_manifest = raw_input.get("artifact_manifest")
    if isinstance(artifact_manifest, list):
        manifest = [dict(item) for item in artifact_manifest if isinstance(item, Mapping)]
        if manifest:
            payload["artifact_manifest"] = manifest
    max_rows = raw_input.get("max_rows")
    if isinstance(max_rows, int) and not isinstance(max_rows, bool) and max_rows > 0:
        payload["max_rows"] = max_rows
    return payload


def _model_followup_terminal_pending_input(
    raw_input: Mapping[str, Any],
    generated_content: str,
) -> dict[str, Any]:
    if isinstance(raw_input, Mapping):
        command = str(raw_input.get("command") or "").strip()
        if command and not _command_tool_request_looks_like_placeholder(command):
            return _terminal_run_input_payload(raw_input, command)
    command = _terminal_command_from_model_followup_content(generated_content)
    if not command:
        return {}
    payload: dict[str, Any] = {"command": command, "shell": True}
    timeout = raw_input.get("timeout_seconds") if isinstance(raw_input, Mapping) else None
    if isinstance(timeout, int) and not isinstance(timeout, bool) and 1 <= timeout <= 120:
        payload["timeout_seconds"] = timeout
    else:
        payload["timeout_seconds"] = 60
    return payload


def _terminal_run_input_payload(
    raw_input: Mapping[str, Any],
    command: str,
) -> dict[str, Any]:
    payload: dict[str, Any] = {"command": command}
    timeout = raw_input.get("timeout_seconds")
    if isinstance(timeout, int) and not isinstance(timeout, bool) and 1 <= timeout <= 120:
        payload["timeout_seconds"] = timeout
    shell = raw_input.get("shell")
    if isinstance(shell, bool):
        payload["shell"] = shell
    return payload


_MODEL_FOLLOWUP_TERMINAL_LANGS = {
    "bash",
    "console",
    "fish",
    "sh",
    "shell",
    "shell-session",
    "terminal",
    "zsh",
}


def _terminal_command_from_model_followup_content(content: str) -> str:
    text = str(content or "").strip()
    if not text:
        return ""
    for match in re.finditer(r"```(?P<lang>[^\n`]*)\n(?P<body>.*?)```", text, flags=re.DOTALL):
        lang = str(match.group("lang") or "").strip().lower()
        lang = re.split(r"\s+", lang, maxsplit=1)[0] if lang else ""
        if lang not in _MODEL_FOLLOWUP_TERMINAL_LANGS:
            continue
        command = _clean_terminal_command_block(match.group("body") or "")
        if _terminal_followup_command_allowed(command):
            return command
    for line in text.splitlines():
        prompt_match = re.match(r"^\s*(?:\$|%)\s+(?P<command>.+?)\s*$", line)
        if not prompt_match:
            continue
        command = str(prompt_match.group("command") or "").strip()
        if _terminal_followup_command_allowed(command):
            return command
    inline_match = re.search(
        r"(?:command|cmd|run|execute|执行|运行|命令)\s*[:：]?\s*`(?P<command>[^`\n]{1,800})`",
        text,
        flags=re.IGNORECASE,
    )
    if inline_match:
        command = str(inline_match.group("command") or "").strip()
        if _terminal_followup_command_allowed(command):
            return command
    return ""


def _clean_terminal_command_block(body: str) -> str:
    lines = str(body or "").strip().splitlines()
    prompt_lines: list[str] = []
    plain_lines: list[str] = []
    for line in lines:
        stripped = line.strip()
        if not stripped or stripped.startswith("#"):
            continue
        prompt_match = re.match(r"^(?:\$|%)\s+(?P<command>.+?)\s*$", stripped)
        if prompt_match:
            prompt_lines.append(str(prompt_match.group("command") or "").strip())
            continue
        if prompt_lines:
            continue
        plain_lines.append(line.rstrip())
    return "\n".join(prompt_lines or plain_lines).strip()


def _terminal_followup_command_allowed(command: str) -> bool:
    clean = str(command or "").strip()
    if not clean or len(clean) > 4000 or "\x00" in clean or "```" in clean:
        return False
    unsafe_patterns = (
        r"(^|[\s;&|])sudo(\s|$)",
        r"(^|[\s;&|])su(\s|$)",
        r"\brm\s+-[^\n]*[rf]",
        r"\brm\s+[^\n]*(?:/\s*$|/\s|~|\*)",
        r"\bdd\s+",
        r"\bmkfs(?:\.|[\s])",
        r"\bdiskutil\s+erase",
        r"\bshutdown\b",
        r"\breboot\b",
        r"\bkillall\b",
        r"\bcurl\b[^\n|]*\|\s*(?:sh|bash|zsh)\b",
        r"\bwget\b[^\n|]*\|\s*(?:sh|bash|zsh)\b",
    )
    return not any(re.search(pattern, clean, flags=re.IGNORECASE) for pattern in unsafe_patterns)


def _model_followup_app_write_requests(
    generated_content: str,
    target: Mapping[str, Any] | None,
    allowed_tools: Iterable[str],
    *,
    followup_context: Mapping[str, Any] | None = None,
) -> list[dict[str, Any]]:
    if not isinstance(target, Mapping):
        return []
    content = _model_followup_resolved_body_content(
        generated_content,
        target,
        followup_context or {},
    )
    if not content:
        return []
    artifact_requests = _model_followup_chained_artifact_write_requests(
        content,
        target,
        allowed_tools,
    )
    if str(target.get("kind") or "").strip() == "communication_message":
        return [
            *artifact_requests,
            *_model_followup_communication_requests(content, target, allowed_tools),
        ]
    if str(target.get("kind") or "").strip() == "artifact_write":
        return _model_followup_artifact_write_requests(content, target, allowed_tools)
    if str(target.get("kind") or "").strip() == "note_write":
        return [
            *artifact_requests,
            *_model_followup_note_write_requests(content, target, allowed_tools),
        ]
    if str(target.get("kind") or "").strip() == "desktop_discovered_app_action":
        return [
            *artifact_requests,
            *_model_followup_discovered_app_write_discovery_requests(
                content,
                target,
                allowed_tools,
            ),
        ]
    if str(target.get("kind") or "").strip() == "current_input_write":
        return [
            *artifact_requests,
            *_model_followup_current_input_write_requests(
                content,
                target,
                allowed_tools,
            ),
        ]
    app_name = str(target.get("app_name") or "").strip()
    if str(target.get("kind") or "").strip() != "app_write" or not app_name:
        return artifact_requests
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planning_reason = "planner_followup_app_write"
    source = "runtime_planner"
    container_requests = _model_followup_container_requests(
        app_name,
        _model_followup_container_action(target),
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if _model_followup_container_action(target) and not container_requests:
        return []
    verify_request = (
        _request_like(
            "desktop.ui_elements",
            {},
            source=source,
            planning_reason=planning_reason,
        )
        if "desktop.ui_elements" in allowed
        else None
    )
    if "app.focus_and_safe_type_text" in allowed:
        requests = [
            *container_requests,
            _request_like(
                "app.focus_and_safe_type_text",
                {"app_name": app_name, "text": content},
                source=source,
                planning_reason=planning_reason,
            )
        ]
        return [*artifact_requests, *requests, *([verify_request] if verify_request else [])]
    if "app.open_and_safe_type_text" in allowed:
        requests = [
            *container_requests,
            _request_like(
                "app.open_and_safe_type_text",
                {"app_name": app_name, "text": content},
                source=source,
                planning_reason=planning_reason,
            )
        ]
        return [*artifact_requests, *requests, *([verify_request] if verify_request else [])]
    focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if focus_tool and "desktop.safe_type_text" in allowed:
        requests = [
            *container_requests,
            _request_like(
                focus_tool,
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                "desktop.safe_type_text",
                {"text": content},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
        return [*artifact_requests, *requests, *([verify_request] if verify_request else [])]
    if focus_tool and "clipboard.write" in allowed and "desktop.safe_shortcut" in allowed:
        requests = [
            *container_requests,
            _request_like(
                "clipboard.write",
                {"text": content},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                focus_tool,
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                "desktop.safe_shortcut",
                {"action": "paste"},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
        return [*artifact_requests, *requests, *([verify_request] if verify_request else [])]
    return artifact_requests


def _model_followup_current_input_write_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content or str(target.get("kind") or "").strip() != "current_input_write":
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planning_reason = "planner_followup_current_input_write"
    source = "runtime_planner"
    verify_request = (
        _request_like(
            "desktop.ui_elements",
            {},
            source=source,
            planning_reason=planning_reason,
        )
        if "desktop.ui_elements" in allowed
        else None
    )
    if "desktop.safe_type_text" in allowed:
        requests = [
            _request_like(
                "desktop.safe_type_text",
                {"text": content},
                source=source,
                planning_reason=planning_reason,
            )
        ]
        return [*requests, *([verify_request] if verify_request else [])]
    if "clipboard.write" in allowed and "desktop.safe_shortcut" in allowed:
        requests = [
            _request_like(
                "clipboard.write",
                {"text": content},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                "desktop.safe_shortcut",
                {"action": "paste"},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
        return [*requests, *([verify_request] if verify_request else [])]
    return []


def _model_followup_artifact_target_completed(
    target: Mapping[str, Any] | None,
    timeline: list[dict[str, Any]],
) -> bool:
    if not isinstance(target, Mapping):
        return False
    path = str(target.get("path") or "").strip()
    if not path:
        raw = target.get("artifact_write") if isinstance(target.get("artifact_write"), Mapping) else {}
        path = str(raw.get("path") or "").strip()
    if not path:
        return False
    for event in reversed(timeline):
        if str(event.get("event") or "").strip() != "agent.tool.call":
            continue
        tool = str(event.get("detail") or "").strip()
        result = event.get("result") if isinstance(event.get("result"), Mapping) else {}
        if result.get("ok") is not True:
            continue
        if tool == "data.analyze":
            produced_paths = {
                str(result.get("artifact_path") or "").strip(),
                *(
                    str(item).strip()
                    for item in result.get("artifact_paths") or []
                    if isinstance(item, str)
                ),
                *(
                    str(item.get("path") or "").strip()
                    for collection_name in ("artifact_manifest", "artifacts")
                    for item in (
                        result.get(collection_name)
                        if isinstance(result.get(collection_name), list)
                        else []
                    )
                    if isinstance(item, Mapping)
                ),
            }
            if any(
                produced_path
                and (produced_path == path or produced_path.endswith(f"/{path}"))
                for produced_path in produced_paths
            ):
                return True
            continue
        if tool != "artifact.write":
            continue
        input_preview = (
            event.get("input_preview")
            if isinstance(event.get("input_preview"), Mapping)
            else {}
        )
        completed_path = str(input_preview.get("path") or result.get("path") or "").strip()
        if completed_path == path or completed_path.endswith(f"/{path}"):
            return True
    return False


def _model_followup_discovered_app_write_discovery_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content or str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    if str(target.get("body_source") or "").strip() != "model_generated_content":
        return []
    app_query = str(target.get("app_query") or "").strip()
    if not app_query:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if "desktop.list_apps" not in allowed:
        return []
    target_action = str(target.get("target_action") or "").strip()
    safe_shortcut_action = str(target.get("safe_shortcut_action") or "").strip()
    communication_compose = _discovered_app_communication_compose_payload(target)
    can_prepare = False
    if target_action == "safe_shortcut" and safe_shortcut_action:
        can_prepare = (
            "app.open_and_safe_shortcut" in allowed
            or "app.focus_and_safe_shortcut" in allowed
            or (
                ("app.open" in allowed or "app.focus" in allowed)
                and "desktop.safe_shortcut" in allowed
            )
        )
    elif target_action in {"open_app", "open", "focus_app", "focus"}:
        can_prepare = "app.open" in allowed or "app.focus" in allowed
    if not can_prepare:
        return []
    if communication_compose:
        can_type = (
            "app.focus_and_type_into_ui_element" in allowed
            or "desktop.type_into_ui_element" in allowed
            or "desktop.safe_type_text" in allowed
        )
        can_send = (
            communication_compose.get("send_action") != "send"
            or "desktop.submit_foreground" in allowed
        )
        if not (can_type and "desktop.search_submit" in allowed and can_send):
            return []
        return [
            _request_like(
                "desktop.list_apps",
                {"query": app_query, "limit": 20},
                source="runtime_planner",
                planning_reason="planner_followup_discovered_app_write",
            )
        ]
    if not (
        "desktop.safe_type_text" in allowed
        or "app.focus_and_safe_type_text" in allowed
    ):
        return []
    return [
        _request_like(
            "desktop.list_apps",
            {"query": app_query, "limit": 20},
            source="runtime_planner",
            planning_reason="planner_followup_discovered_app_write",
        )
    ]


def _model_followup_discovered_app_write_requests_after_discovery(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
    timeline: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content or str(target.get("kind") or "").strip() != "desktop_discovered_app_action":
        return []
    if str(target.get("body_source") or "").strip() != "model_generated_content":
        return []
    app_query = str(target.get("app_query") or "").strip()
    if not app_query or not _discovered_app_name_for_query(timeline, app_query):
        return []
    communication_compose = (
        target.get("communication_compose")
        if isinstance(target.get("communication_compose"), Mapping)
        else {}
    )
    followup_target = {
        **dict(target),
        "body_source": "explicit_user_text",
    }
    if communication_compose:
        followup_target["communication_compose"] = {
            **dict(communication_compose),
            "body": content,
        }
    else:
        followup_target["compose_text"] = content
    return _auto_discovered_app_followup_requests(
        {"followup_target": followup_target},
        allowed_tools,
        timeline,
        planning_reason="planner_followup_discovered_app_write",
    )


def _model_followup_chained_artifact_write_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content:
        return []
    raw = (
        target.get("artifact_write")
        if isinstance(target.get("artifact_write"), Mapping)
        else {}
    )
    path = str(raw.get("path") or "").strip()
    if not path:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    tool_name = str(raw.get("tool") or "artifact.write").strip()
    if tool_name != "artifact.write" or tool_name not in allowed:
        return []
    return [
        _request_like(
            "artifact.write",
            {"path": path, "content": content},
            source="runtime_planner",
            planning_reason="planner_followup_artifact_write",
        )
    ]


def _model_followup_artifact_write_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content or str(target.get("kind") or "").strip() != "artifact_write":
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if "artifact.write" not in allowed:
        return []
    path = str(target.get("path") or "").strip()
    if not path:
        return []
    return [
        _request_like(
            "artifact.write",
            {"path": path, "content": content},
            source="runtime_planner",
            planning_reason="planner_followup_artifact_write",
        )
    ]


def _model_followup_note_write_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    if not content or str(target.get("kind") or "").strip() != "note_write":
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    if "notes.create" not in allowed:
        return []
    payload: dict[str, Any] = {"body": content}
    title = str(target.get("title") or "").strip()
    if title:
        payload["title"] = title
    folder_name = str(target.get("folder_name") or "").strip()
    if folder_name:
        payload["folder_name"] = folder_name
    return [
        _request_like(
            "notes.create",
            payload,
            source="runtime_planner",
            planning_reason="planner_followup_note_write",
        )
    ]


def _model_followup_communication_requests(
    generated_content: str,
    target: Mapping[str, Any],
    allowed_tools: Iterable[str],
) -> list[dict[str, Any]]:
    content = str(generated_content or "").strip()
    app_name = str(target.get("app_name") or "").strip()
    recipient = str(target.get("recipient") or "").strip()
    if not content or not app_name or not recipient:
        return []
    allowed = {str(tool or "").strip() for tool in allowed_tools if str(tool or "").strip()}
    planning_reason = "planner_followup_communication"
    source = "runtime_planner"
    prepare_requests = _model_followup_communication_prepare_requests(
        app_name,
        str(target.get("mode") or "focus").strip() or "focus",
        str(target.get("channel") or "").strip(),
        allowed,
        source=source,
        planning_reason=planning_reason,
    )
    if (
        not prepare_requests
        or "desktop.safe_type_text" not in allowed
        or "desktop.search_submit" not in allowed
    ):
        return []
    requests = [
        *prepare_requests,
        _request_like(
            "desktop.safe_type_text",
            {"text": recipient},
            source=source,
            planning_reason=planning_reason,
        ),
        _request_like(
            "desktop.search_submit",
            {},
            source=source,
            planning_reason=planning_reason,
        ),
        _request_like(
            "desktop.safe_type_text",
            {"text": content},
            source=source,
            planning_reason=planning_reason,
        ),
    ]
    if (
        str(target.get("send_action") or "").strip() == "send"
        and "desktop.submit_foreground" in allowed
    ):
        requests.append(
            _request_like(
                "desktop.submit_foreground",
                {"action": "send"},
                source=source,
                planning_reason=planning_reason,
            )
        )
    if "desktop.ui_elements" in allowed:
        requests.append(
            _request_like(
                "desktop.ui_elements",
                {},
                source=source,
                planning_reason=planning_reason,
            )
        )
    return requests


def _model_followup_communication_prepare_requests(
    app_name: str,
    mode: str,
    channel: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    action = _model_followup_communication_focus_action(channel)
    if mode == "open" and "app.open_and_safe_shortcut" in allowed:
        return [
            _request_like(
                "app.open_and_safe_shortcut",
                {"app_name": app_name, "action": action},
                source=source,
                planning_reason=planning_reason,
            )
        ]
    if "app.focus_and_safe_shortcut" in allowed:
        return [
            _request_like(
                "app.focus_and_safe_shortcut",
                {"app_name": app_name, "action": action},
                source=source,
                planning_reason=planning_reason,
            )
        ]
    if "app.open_and_safe_shortcut" in allowed:
        return [
            _request_like(
                "app.open_and_safe_shortcut",
                {"app_name": app_name, "action": action},
                source=source,
                planning_reason=planning_reason,
            )
        ]
    focus_tool = "app.open" if mode == "open" and "app.open" in allowed else ""
    if not focus_tool:
        focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if focus_tool and "desktop.safe_shortcut" in allowed:
        return [
            _request_like(
                focus_tool,
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                "desktop.safe_shortcut",
                {"action": action},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
    return []


def _model_followup_container_requests(
    app_name: str,
    container_action: str,
    allowed: set[str],
    *,
    source: str,
    planning_reason: str,
) -> list[dict[str, Any]]:
    if not container_action:
        return []
    if "app.focus_and_safe_shortcut" in allowed:
        return [
            _request_like(
                "app.focus_and_safe_shortcut",
                {"app_name": app_name, "action": container_action},
                source=source,
                planning_reason=planning_reason,
            )
        ]
    if "app.open_and_safe_shortcut" in allowed:
        return [
            _request_like(
                "app.open_and_safe_shortcut",
                {"app_name": app_name, "action": container_action},
                source=source,
                planning_reason=planning_reason,
            )
        ]
    focus_tool = "app.focus" if "app.focus" in allowed else ("app.open" if "app.open" in allowed else "")
    if focus_tool and "desktop.safe_shortcut" in allowed:
        return [
            _request_like(
                focus_tool,
                {"app_name": app_name},
                source=source,
                planning_reason=planning_reason,
            ),
            _request_like(
                "desktop.safe_shortcut",
                {"action": container_action},
                source=source,
                planning_reason=planning_reason,
            ),
        ]
    return []


def _request_like(
    tool_name: str,
    payload: dict[str, Any],
    *,
    source: str,
    planning_reason: str,
) -> dict[str, Any]:
    return {
        "protocol": "json_fallback",
        "tool": tool_name,
        "input": payload,
        "source": source,
        "planning_reason": planning_reason,
    }


def _followup_content_snapshot_message(value: Any) -> str:
    if not isinstance(value, dict):
        return ""
    text = str(value.get("text") or "").strip()
    if text:
        label = (
            "Desktop content snapshot"
            if str(value.get("source_tool") or "").startswith(("desktop.", "screen."))
            else "Observed content snapshot"
        )
        return f"\n\n{label}:\n{text}\n\n"
    summary = str(value.get("summary") or "").strip()
    if summary:
        return f"\n\nObserved context summary:\n{summary}\n\n"
    if value.get("ok") is False:
        error_summary = str(value.get("error") or "").strip()
        if error_summary:
            return f"\n\nObservation status: {error_summary}\n\n"
    return ""


def _followup_content_snapshots_message(payload: dict[str, Any]) -> str:
    raw_snapshots = payload.get("content_snapshots")
    snapshots = [
        item
        for item in raw_snapshots
        if isinstance(item, dict)
    ] if isinstance(raw_snapshots, list) else []
    if not snapshots:
        return _followup_content_snapshot_message(payload.get("content_snapshot"))
    if len(snapshots) == 1:
        return _followup_content_snapshot_message(snapshots[0])
    sections: list[str] = []
    for index, snapshot in enumerate(snapshots, start=1):
        body = _followup_content_snapshot_body(snapshot)
        if not body:
            continue
        source_tool = str(snapshot.get("source_tool") or f"snapshot-{index}").strip()
        sections.append(f"[{index}] {source_tool}\n{body}")
    if not sections:
        return ""
    return "\n\nObserved context snapshots:\n\n" + "\n\n".join(sections) + "\n\n"


def _followup_content_snapshot_body(value: dict[str, Any]) -> str:
    text = str(value.get("text") or "").strip()
    if text:
        return text
    summary = str(value.get("summary") or "").strip()
    if summary:
        return summary
    if value.get("ok") is False:
        return str(value.get("summary") or value.get("error") or "").strip()
    return ""


def _auto_data_analysis_request_from_captured_content(
    planned_tool_requests: list[dict[str, Any]],
    selection_payload: dict[str, Any],
    timeline: list[dict[str, Any]],
) -> dict[str, Any] | None:
    observation_tools = _ordered_text_list(
        [
            str(request.get("tool") or "").strip()
            for request in planned_tool_requests
            if isinstance(request, dict)
            and bool(request.get("continue_to_model"))
            and str(request.get("tool") or "").strip()
        ]
    )
    snapshots = followup_content_snapshots(timeline, observation_tools)
    snapshot = snapshots[-1] if snapshots else latest_followup_content_snapshot(timeline, observation_tools)
    if not snapshot or snapshot.get("ok") is False:
        return None
    source_tool = str(snapshot.get("source_tool") or "").strip()
    if source_tool == "data.analyze":
        return None
    content = str(snapshot.get("text") or "").strip()
    if not content:
        return None
    artifact_paths = _string_list(selection_payload.get("artifacts_expected"))
    if not artifact_paths:
        artifact_paths = ["analysis-report.md"]
    artifact_path = artifact_paths[0]
    input_payload: dict[str, Any] = {
        "content": content,
        "display_path": _captured_data_display_path(snapshot),
        "artifact_path": artifact_path,
        "source_kind": _captured_data_source_kind(content),
        "requested_outputs": _requested_outputs_from_artifact_paths(artifact_paths),
        "artifact_manifest": _artifact_manifest_from_paths(artifact_paths),
    }
    if len(artifact_paths) > 1:
        input_payload["artifact_paths"] = artifact_paths
    return _first_annotated_auto_followup_request(
        {
            "protocol": "json_fallback",
            "tool": "data.analyze",
            "input": input_payload,
            "source": "runtime_planner",
            "planning_reason": "planner_builtin_data_analysis",
        },
        selection_payload,
    )


def _captured_data_display_path(snapshot: dict[str, Any]) -> str:
    for key in ("path", "url", "title", "app_name", "source_tool"):
        value = str(snapshot.get(key) or "").strip()
        if value:
            if key == "source_tool":
                return f"captured:{value}"
            return value
    return "captured:data"


def _captured_data_source_kind(content: str) -> str:
    text = str(content or "").strip()
    if not text:
        return "text_table"
    if text.startswith("[") or text.startswith("{"):
        return "json"
    lines = [line.strip() for line in text.splitlines() if line.strip()]
    if lines and all(line.startswith("{") and line.endswith("}") for line in lines[:5]):
        return "jsonl"
    sample = "\n".join(lines[:5])
    if "\t" in sample:
        return "tsv"
    if "|" in sample:
        return "text_table"
    if "," in sample:
        return "csv"
    return "text_table"


def _requested_outputs_from_artifact_paths(paths: list[str]) -> list[str]:
    outputs: list[str] = []
    for path in paths:
        suffix = _artifact_path_suffix(path)
        output = {
            ".md": "report",
            ".markdown": "report",
            ".csv": "table",
            ".tsv": "table",
            ".html": "report",
            ".png": "chart",
        }.get(suffix)
        if output and output not in outputs:
            outputs.append(output)
    return outputs or ["report"]


def _artifact_manifest_from_paths(paths: list[str]) -> list[dict[str, str]]:
    manifest: list[dict[str, str]] = []
    for path in paths:
        kind = {
            ".md": "markdown",
            ".markdown": "markdown",
            ".csv": "csv",
            ".tsv": "csv",
            ".html": "html",
            ".png": "chart",
        }.get(_artifact_path_suffix(path), "artifact")
        manifest.append({"path": path, "kind": kind})
    return manifest


def _artifact_path_suffix(path: str) -> str:
    clean = str(path or "").strip().lower()
    index = clean.rfind(".")
    return clean[index:] if index >= 0 else ""


def _visible_daily_desktop_completed_steps(
    completed_steps: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    primary_indexes = [
        index
        for index, step in enumerate(completed_steps)
        if not _is_daily_desktop_discovery_completed_step(step)
        and str(step.get("tool") or "") not in _DAILY_DESKTOP_VERIFY_TOOLS
        and not _is_internal_daily_desktop_verification_step(step)
    ]
    if not primary_indexes:
        visible_steps = [
            step
            for step in completed_steps
            if not _is_internal_daily_desktop_verification_step(step)
        ]
        while (
            len(visible_steps) > 1
            and _is_daily_desktop_discovery_prefix_completed_step(visible_steps[0])
        ):
            visible_steps = visible_steps[1:]
        return visible_steps
    first_primary = primary_indexes[0]
    last_primary = primary_indexes[-1]
    visible_steps: list[dict[str, Any]] = []
    for index, step in enumerate(completed_steps):
        tool_name = str(step.get("tool") or "")
        if _is_internal_daily_desktop_verification_step(step):
            continue
        if (
            _is_daily_desktop_discovery_completed_step(step)
            and (index < first_primary or index > last_primary)
        ):
            continue
        if (
            tool_name in _DAILY_DESKTOP_VERIFY_TOOLS
            and not _is_requested_visual_capture(step)
            and not _is_requested_ui_readback(
                completed_steps,
                index,
                first_primary,
                last_primary,
            )
        ):
            continue
        visible_steps.append(step)
    return _coalesced_app_preparation_composite_steps(
        visible_steps or completed_steps
    )


def _is_internal_daily_desktop_verification_step(
    step: Mapping[str, Any],
) -> bool:
    return bool(
        str(step.get("source") or "").strip()
        in {
            "runtime_native_postcondition_receipt",
            "runtime_post_action_auto_verify",
            "runtime_verification",
        }
    )


def _is_requested_visual_capture(step: Mapping[str, Any]) -> bool:
    return bool(
        str(step.get("tool") or "").strip() == "screen.capture"
        and str(step.get("runtime_role") or "").strip() == "capture_visual_state"
    )


def _daily_desktop_step_has_placeholder_app(
    planned_input: Mapping[str, Any],
    executed_input: Mapping[str, Any],
    result: Mapping[str, Any],
) -> bool:
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    for source in (executed_input, data):
        value = str(source.get("app_name") or source.get("discovered_app_name") or "").strip()
        if value and not _runtime_planner_placeholder_app_name(value):
            return False
    for source in (planned_input, executed_input, data):
        value = str(source.get("app_name") or source.get("discovered_app_name") or "").strip()
        if value and _runtime_planner_placeholder_app_name(value):
            return True
    return False


def _is_deferred_observed_ui_request(step: Mapping[str, Any]) -> bool:
    return bool(
        isinstance(step, Mapping)
        and str(step.get("deferred_tool") or "").strip()
        and isinstance(step.get("deferred_input"), Mapping)
    )


def _drop_resolved_deferred_observation_requests(
    requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    deferred_observations = {
        (
            str(request.get("deferred_tool") or "").strip(),
            _daily_desktop_request_step_id(request),
        )
        for request in requests
        if _is_deferred_observed_ui_request(request)
    }
    if not deferred_observations:
        return requests

    resolved_deferred_steps = {
        (
            str(request.get("tool") or "").strip(),
            _daily_desktop_request_step_id(request),
        )
        for request in requests
        if (
            str(request.get("tool") or "").strip(),
            _daily_desktop_request_step_id(request),
        )
        in deferred_observations
    }
    if not resolved_deferred_steps:
        return requests

    filtered = [
        request
        for request in requests
        if not (
            _is_deferred_observed_ui_request(request)
            and (
                str(request.get("deferred_tool") or "").strip(),
                _daily_desktop_request_step_id(request),
            )
            in resolved_deferred_steps
        )
    ]
    return filtered or requests


def _daily_desktop_request_step_id(request: Mapping[str, Any]) -> str:
    step_id = str(
        request.get("step_id")
        or request.get("planner_step_id")
        or ""
    ).strip()
    if step_id:
        return step_id
    deferred_context = (
        request.get("deferred_context")
        if isinstance(request.get("deferred_context"), Mapping)
        else {}
    )
    return str(
        deferred_context.get("step_id")
        or deferred_context.get("planner_step_id")
        or ""
    ).strip()


def _is_daily_desktop_discovery_completed_step(step: dict[str, Any]) -> bool:
    tool_name = str(step.get("tool") or "")
    if tool_name in _DAILY_DESKTOP_DISCOVERY_TOOLS:
        return True
    return tool_name == "desktop.inspect_app" and isinstance(step.get("result"), dict)


def _is_daily_desktop_discovery_prefix_completed_step(step: dict[str, Any]) -> bool:
    tool_name = str(step.get("tool") or "")
    if tool_name in _DAILY_DESKTOP_DISCOVERY_PREFIX_TOOLS:
        return True
    return tool_name == "desktop.inspect_app" and isinstance(step.get("result"), dict)


def _all_noncritical_daily_desktop_observations(
    requests: list[dict[str, Any]],
) -> bool:
    if not requests:
        return False
    for request in requests:
        if not isinstance(request, dict):
            return False
        tool_name = str(request.get("tool") or "").strip()
        if (
            tool_name not in _DAILY_DESKTOP_DISCOVERY_TOOLS
            and tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS
        ):
            return False
    return True


def _drop_trailing_daily_desktop_verify_requests(
    requests: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if len(requests) <= 1:
        return requests
    if any(bool(request.get("continue_to_model")) for request in requests if isinstance(request, dict)):
        return requests
    if _direct_action_with_active_window_verification(requests):
        return requests
    last_primary = -1
    for index, request in enumerate(requests):
        tool_name = str(request.get("tool") or "").strip()
        if (
            tool_name not in _DAILY_DESKTOP_DISCOVERY_TOOLS
            and tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS
        ):
            last_primary = index
    if last_primary < 0:
        return requests
    return [
        request
        for index, request in enumerate(requests)
        if index <= last_primary
        or str(request.get("tool") or "").strip() not in _DAILY_DESKTOP_VERIFY_TOOLS
        or _is_preserved_runtime_planner_verification(requests, index, last_primary)
    ]


def _is_preserved_runtime_planner_verification(
    requests: list[dict[str, Any]],
    index: int,
    last_primary: int,
) -> bool:
    if index <= last_primary:
        return False
    request = requests[index]
    tool_name = str(request.get("tool") or "").strip()
    if tool_name not in _DAILY_DESKTOP_VERIFY_TOOLS:
        return False
    planning_reason = str(request.get("planning_reason") or "").strip()
    if planning_reason not in {
        "planner_desktop_operation",
        "planner_full_plan_desktop_operation",
    }:
        return False
    previous_primary = str(requests[last_primary].get("tool") or "").strip()
    return previous_primary.startswith(("app.", "desktop."))


def _direct_action_with_active_window_verification(
    requests: list[dict[str, Any]],
) -> bool:
    tools = [
        str(request.get("tool") or "").strip()
        for request in requests
        if isinstance(request, dict) and str(request.get("tool") or "").strip()
    ]
    if "desktop.active_window" not in tools:
        return False
    primary_tools = [
        tool
        for tool in tools
        if tool not in _DAILY_DESKTOP_DISCOVERY_TOOLS
        and tool not in _DAILY_DESKTOP_VERIFY_TOOLS
    ]
    return bool(primary_tools) and set(primary_tools) <= {
        "app.open",
        "app.focus",
        "desktop.close_window",
        "desktop.minimize_window",
        "desktop.quit_app",
    }


def _planned_daily_desktop_tools(
    planned_tool_requests: list[dict[str, Any]],
) -> list[str]:
    return [
        str(request.get("tool") or "").strip()
        for request in planned_tool_requests
        if isinstance(request, dict) and str(request.get("tool") or "").strip()
    ]


def _is_preserved_active_window_verification(
    completed_steps: list[dict[str, Any]],
    index: int,
) -> bool:
    if str(completed_steps[index].get("tool") or "") != "desktop.active_window":
        return False
    return _direct_action_with_active_window_verification(completed_steps)


def _daily_desktop_sequence_summary_steps(
    visible_steps: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if not visible_steps:
        return []
    visible_steps = _coalesced_app_preparation_composite_steps(visible_steps)
    visible_steps = _coalesced_open_focus_type_summary_steps(visible_steps)
    visible_steps = _coalesced_open_focus_find_summary_steps(visible_steps)
    if (
        str(visible_steps[-1].get("tool") or "") == "desktop.active_window"
        and _direct_action_with_active_window_verification(visible_steps)
    ):
        primary_steps = [
            step
            for step in visible_steps
            if str(step.get("tool") or "") != "desktop.active_window"
        ]
        return primary_steps or visible_steps
    return visible_steps


def _coalesced_app_preparation_composite_steps(
    visible_steps: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if len(visible_steps) < 2:
        return visible_steps
    coalesced: list[dict[str, Any]] = []
    index = 0
    while index < len(visible_steps):
        if index + 1 >= len(visible_steps):
            coalesced.append(visible_steps[index])
            break
        preparation = visible_steps[index]
        composite = visible_steps[index + 1]
        preparation_tool = str(preparation.get("tool") or "").strip()
        composite_tool = str(composite.get("tool") or "").strip()
        app_name = _summary_step_app_name(preparation)
        if (
            preparation_tool
            in {"app.open", "desktop.open_app", "app.focus", "desktop.focus_app"}
            and composite_tool.startswith(("app.focus_and_", "app.open_and_"))
            and app_name
            and _summary_step_app_name(composite) == app_name
        ):
            combined = dict(composite)
            if preparation_tool in {"app.open", "desktop.open_app"}:
                if composite_tool.startswith("app.focus_and_"):
                    combined["tool"] = composite_tool.replace(
                        "app.focus_and_",
                        "app.open_and_",
                        1,
                    )
                summary = str(combined.get("summary") or "")
                if summary.startswith("已切到"):
                    combined["summary"] = "已打开" + summary[len("已切到") :]
            coalesced.append(combined)
            index += 2
            continue
        coalesced.append(preparation)
        index += 1
    return coalesced


def _coalesced_open_focus_type_summary_steps(
    visible_steps: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if len(visible_steps) < 3:
        return visible_steps
    coalesced: list[dict[str, Any]] = []
    index = 0
    while index < len(visible_steps):
        if index + 2 >= len(visible_steps):
            coalesced.append(visible_steps[index])
            index += 1
            continue
        open_step = visible_steps[index]
        focus_step = visible_steps[index + 1]
        type_step = visible_steps[index + 2]
        app_name = _summary_step_app_name(open_step)
        text = _summary_step_text(type_step)
        if (
            str(open_step.get("tool") or "") in {"app.open", "desktop.open_app"}
            and str(focus_step.get("tool") or "") in {"app.focus", "desktop.focus_app"}
            and str(type_step.get("tool") or "") == "desktop.safe_type_text"
            and app_name
            and _summary_step_app_name(focus_step) == app_name
            and text
        ):
            target = _display_target_name(
                app_name,
                f"并输入文字（{len(text)} 个字符）",
            )
            summary = f"已打开{target}。"
            coalesced.append(
                {
                    **type_step,
                    "tool": "app.open_and_safe_type_text",
                    "input_preview": {"app_name": app_name, "text": text},
                    "summary": summary,
                }
            )
            index += 3
            continue
        coalesced.append(visible_steps[index])
        index += 1
    return coalesced


def _coalesced_open_focus_find_summary_steps(
    visible_steps: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    if len(visible_steps) < 3:
        return visible_steps
    coalesced: list[dict[str, Any]] = []
    index = 0
    while index < len(visible_steps):
        if index + 2 >= len(visible_steps):
            coalesced.append(visible_steps[index])
            index += 1
            continue
        open_step = visible_steps[index]
        focus_step = visible_steps[index + 1]
        shortcut_step = visible_steps[index + 2]
        app_name = _summary_step_app_name(open_step)
        shortcut_action = _summary_step_shortcut_action(shortcut_step)
        shortcut_summary = {
            "find": "打开查找",
            "new_document": "新建文档",
        }.get(shortcut_action, "")
        if (
            str(open_step.get("tool") or "") in {"app.open", "desktop.open_app"}
            and str(focus_step.get("tool") or "") in {"app.focus", "desktop.focus_app"}
            and str(shortcut_step.get("tool") or "") == "desktop.safe_shortcut"
            and app_name
            and _summary_step_app_name(focus_step) == app_name
            and shortcut_summary
        ):
            coalesced.append(
                {
                    **shortcut_step,
                    "tool": "app.open_and_safe_shortcut",
                    "input_preview": {"app_name": app_name, "action": shortcut_action},
                    "summary": f"已打开{_display_target_name(app_name, f'并{shortcut_summary}')}。",
                }
            )
            index += 3
            continue
        coalesced.append(open_step)
        index += 1
    return coalesced


def _summary_step_app_name(step: Mapping[str, Any]) -> str:
    input_preview = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    result = step.get("result") if isinstance(step.get("result"), Mapping) else {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    return str(
        input_preview.get("app_name")
        or data.get("app_name")
        or ""
    ).strip()


def _summary_step_shortcut_action(step: Mapping[str, Any]) -> str:
    input_preview = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    result = step.get("result") if isinstance(step.get("result"), Mapping) else {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    return str(
        input_preview.get("action")
        or data.get("shortcut_action")
        or ""
    ).strip()


def _summary_step_text(step: Mapping[str, Any]) -> str:
    input_preview = step.get("input_preview") if isinstance(step.get("input_preview"), Mapping) else {}
    result = step.get("result") if isinstance(step.get("result"), Mapping) else {}
    data = result.get("data") if isinstance(result.get("data"), Mapping) else {}
    return str(
        input_preview.get("text")
        or data.get("text")
        or data.get("typed_text")
        or ""
    )


def _is_requested_ui_readback(
    completed_steps: list[dict[str, Any]],
    index: int,
    first_primary: int,
    last_primary: int,
) -> bool:
    if str(completed_steps[index].get("tool") or "") != "desktop.ui_elements":
        return False
    primary_tools = {
        str(step.get("tool") or "")
        for step in completed_steps[first_primary : last_primary + 1]
        if isinstance(step, dict)
    }
    return bool(primary_tools) and primary_tools.issubset(
        {
            "app.open",
            "app.focus",
            "system.settings_open",
        }
    )


def _latest_workspace_list_event(timeline: list[dict[str, Any]]) -> dict[str, Any] | None:
    for event in reversed(timeline):
        if event.get("event") != "agent.tool.call":
            continue
        if str(event.get("detail") or event.get("tool") or "").strip() in {
            "workspace.list",
            "fs.find_files",
            "file.search",
        }:
            return event
    return None


def _data_analysis_file_kind(path: str) -> str:
    lowered = str(path or "").strip().lower()
    if lowered.endswith(".csv"):
        return "csv"
    if lowered.endswith(".tsv"):
        return "tsv"
    if lowered.endswith(".json"):
        return "json"
    if lowered.endswith(".jsonl"):
        return "jsonl"
    if lowered.endswith(".xlsx"):
        return "xlsx"
    return ""


def _data_analysis_input_from_workspace_entries(
    base_path: str,
    entries: list[Mapping[str, Any]],
    selection: str,
    artifact_paths: list[str],
) -> dict[str, Any]:
    clean_entries = [entry for entry in entries if isinstance(entry, Mapping)]
    if not clean_entries:
        return {}
    if selection in {"all", "multiple"}:
        paths = [
            path
            for entry in clean_entries
            for path in [_workspace_data_entry_path(base_path, entry)]
            if path
        ]
        if not paths or len(paths) > 100:
            return {}
        payload = _data_analysis_base_input_payload(artifact_paths)
        payload["paths"] = paths
        payload["display_path"] = str(base_path or "workspace.list").strip()
        source_kind = _common_data_analysis_file_kind(paths)
        if source_kind:
            payload["source_kind"] = source_kind
        return payload

    selected_entry: Mapping[str, Any] = {}
    if len(clean_entries) == 1:
        selected_entry = clean_entries[0]
    elif selection:
        selected_entry = _select_followup_workspace_entry(
            clean_entries,
            {"selection": selection},
        )
    if not selected_entry:
        return {}
    path = _workspace_data_entry_path(base_path, selected_entry)
    if not path:
        return {}
    payload = _data_analysis_base_input_payload(artifact_paths)
    payload["path"] = path
    source_kind = _data_analysis_file_kind(path) or _data_analysis_file_kind(
        str(selected_entry.get("name") or "")
    )
    if source_kind:
        payload["source_kind"] = source_kind
    return payload


def _data_analysis_base_input_payload(artifact_paths: list[str]) -> dict[str, Any]:
    return {
        "artifact_path": artifact_paths[0],
        "requested_outputs": _requested_outputs_from_artifact_paths(artifact_paths),
        "artifact_manifest": _artifact_manifest_from_paths(artifact_paths),
    }


def _workspace_data_entry_path(base_path: str, entry: Mapping[str, Any]) -> str:
    path = str(entry.get("path") or "").strip()
    if path:
        return path
    name = str(entry.get("name") or "").strip()
    if not name:
        return ""
    return _join_workspace_list_path(base_path, name)


def _common_data_analysis_file_kind(paths: list[str]) -> str:
    kinds = {
        kind
        for path in paths
        for kind in [_data_analysis_file_kind(path)]
        if kind
    }
    if len(kinds) == 1:
        return next(iter(kinds))
    return ""


def _join_workspace_list_path(base_path: str, name: str) -> str:
    clean_name = str(name or "").strip().strip("/")
    clean_base = str(base_path or "").strip().strip("/")
    if not clean_base or clean_base == ".":
        return clean_name
    return posixpath.normpath(posixpath.join(clean_base, clean_name))


def _display_target_name(value: str, suffix: str = "") -> str:
    text = str(value or "").strip()
    if not text:
        return ""
    if text[0].isascii():
        return f" {text} {suffix}".rstrip()
    return f"{text}{suffix}"


def _permission_diagnostics(result: dict[str, Any]) -> str:
    hints = _string_list(result.get("recovery_hints"))
    if not hints:
        hints = _permission_target_hints(_string_list(result.get("permission_targets")))
    if not hints:
        return ""
    return " 你可以这样处理：" + " ".join(hints)


def _permission_target_hints(targets: list[str]) -> list[str]:
    hints_by_target = {
        "accessibility": "在 macOS 系统设置 > 隐私与安全性 > 辅助功能 中允许 Oha-Yachiyo 或当前运行环境。",
        "automation": "在 macOS 系统设置 > 隐私与安全性 > 自动化 中允许 Oha-Yachiyo 控制目标 App/System Events。",
        "music_app": "先打开 Music.app，确认歌曲在资料库里，并在系统弹窗出现时允许自动化控制 Music。",
        "screen_recording": "在 macOS 系统设置 > 隐私与安全性 > 屏幕录制 中允许 Oha-Yachiyo 或当前运行环境。",
        "chrome_cdp": "启动或配置 Chrome DevTools/CDP 连接后再重试浏览器控制。",
        "open_command": "确认当前运行环境可以调用 macOS open 命令，且目标 App 名称正确。",
    }
    hints: list[str] = []
    for target in targets:
        hint = hints_by_target.get(target)
        if hint and hint not in hints:
            hints.append(hint)
    return hints


def _string_list(value: Any) -> list[str]:
    if isinstance(value, str):
        return [value] if value.strip() else []
    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]
    return []


def _ordered_text_list(values: list[str]) -> list[str]:
    result: list[str] = []
    for value in values:
        text = str(value or "").strip()
        if text and text not in result:
            result.append(text)
    return result
